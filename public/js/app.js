/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 115);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

if (true) {
  var REACT_ELEMENT_TYPE = (typeof Symbol === 'function' &&
    Symbol.for &&
    Symbol.for('react.element')) ||
    0xeac7;

  var isValidElement = function(object) {
    return typeof object === 'object' &&
      object !== null &&
      object.$$typeof === REACT_ELEMENT_TYPE;
  };

  // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod
  var throwOnDirectAccess = true;
  module.exports = __webpack_require__(130)(isValidElement, throwOnDirectAccess);
} else {
  // By explicitly using `prop-types` you are opting into new production behavior.
  // http://fb.me/prop-types-in-prod
  module.exports = require('./factoryWithThrowingShims')();
}


/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {
  module.exports = require('./cjs/react.production.min.js');
} else {
  module.exports = __webpack_require__(117);
}


/***/ }),
/* 2 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__utils_ScalingCellSizeAndPositionManager__ = __webpack_require__(64);



var babelPluginFlowReactPropTypes_proptype_CellPosition =  false ? null : {
  columnIndex: __webpack_require__(0).number.isRequired,
  rowIndex: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellPosition', {
  value: babelPluginFlowReactPropTypes_proptype_CellPosition,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellRendererParams =  false ? null : {
  columnIndex: __webpack_require__(0).number.isRequired,
  isScrolling: __webpack_require__(0).bool.isRequired,
  isVisible: __webpack_require__(0).bool.isRequired,
  key: __webpack_require__(0).string.isRequired,
  parent: __webpack_require__(0).object.isRequired,
  rowIndex: __webpack_require__(0).number.isRequired,
  style: __webpack_require__(0).object.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellRendererParams', {
  value: babelPluginFlowReactPropTypes_proptype_CellRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellRenderer =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellRenderer', {
  value: babelPluginFlowReactPropTypes_proptype_CellRenderer,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellRangeRendererParams =  false ? null : {
  cellCache: __webpack_require__(0).object.isRequired,
  cellRenderer: __webpack_require__(0).func.isRequired,
  columnSizeAndPositionManager: typeof __WEBPACK_IMPORTED_MODULE_1__utils_ScalingCellSizeAndPositionManager__["a" /* default */] === 'function' ? __webpack_require__(0).instanceOf(__WEBPACK_IMPORTED_MODULE_1__utils_ScalingCellSizeAndPositionManager__["a" /* default */]).isRequired : __webpack_require__(0).any.isRequired,
  columnStartIndex: __webpack_require__(0).number.isRequired,
  columnStopIndex: __webpack_require__(0).number.isRequired,
  deferredMeasurementCache: __webpack_require__(0).object,
  horizontalOffsetAdjustment: __webpack_require__(0).number.isRequired,
  isScrolling: __webpack_require__(0).bool.isRequired,
  parent: __webpack_require__(0).object.isRequired,
  rowSizeAndPositionManager: typeof __WEBPACK_IMPORTED_MODULE_1__utils_ScalingCellSizeAndPositionManager__["a" /* default */] === 'function' ? __webpack_require__(0).instanceOf(__WEBPACK_IMPORTED_MODULE_1__utils_ScalingCellSizeAndPositionManager__["a" /* default */]).isRequired : __webpack_require__(0).any.isRequired,
  rowStartIndex: __webpack_require__(0).number.isRequired,
  rowStopIndex: __webpack_require__(0).number.isRequired,
  scrollLeft: __webpack_require__(0).number.isRequired,
  scrollTop: __webpack_require__(0).number.isRequired,
  styleCache: __webpack_require__(0).object.isRequired,
  verticalOffsetAdjustment: __webpack_require__(0).number.isRequired,
  visibleColumnIndices: __webpack_require__(0).object.isRequired,
  visibleRowIndices: __webpack_require__(0).object.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellRangeRendererParams', {
  value: babelPluginFlowReactPropTypes_proptype_CellRangeRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellRangeRenderer =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellRangeRenderer', {
  value: babelPluginFlowReactPropTypes_proptype_CellRangeRenderer,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellSizeGetter =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellSizeGetter', {
  value: babelPluginFlowReactPropTypes_proptype_CellSizeGetter,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellSize =  false ? null : __webpack_require__(0).oneOfType([__webpack_require__(0).func, __webpack_require__(0).number]);
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellSize', {
  value: babelPluginFlowReactPropTypes_proptype_CellSize,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_NoContentRenderer =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_NoContentRenderer', {
  value: babelPluginFlowReactPropTypes_proptype_NoContentRenderer,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_Scroll =  false ? null : {
  clientHeight: __webpack_require__(0).number.isRequired,
  clientWidth: __webpack_require__(0).number.isRequired,
  scrollHeight: __webpack_require__(0).number.isRequired,
  scrollLeft: __webpack_require__(0).number.isRequired,
  scrollTop: __webpack_require__(0).number.isRequired,
  scrollWidth: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_Scroll', {
  value: babelPluginFlowReactPropTypes_proptype_Scroll,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_ScrollbarPresenceChange =  false ? null : {
  horizontal: __webpack_require__(0).bool.isRequired,
  vertical: __webpack_require__(0).bool.isRequired,
  size: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_ScrollbarPresenceChange', {
  value: babelPluginFlowReactPropTypes_proptype_ScrollbarPresenceChange,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_RenderedSection =  false ? null : {
  columnOverscanStartIndex: __webpack_require__(0).number.isRequired,
  columnOverscanStopIndex: __webpack_require__(0).number.isRequired,
  columnStartIndex: __webpack_require__(0).number.isRequired,
  columnStopIndex: __webpack_require__(0).number.isRequired,
  rowOverscanStartIndex: __webpack_require__(0).number.isRequired,
  rowOverscanStopIndex: __webpack_require__(0).number.isRequired,
  rowStartIndex: __webpack_require__(0).number.isRequired,
  rowStopIndex: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_RenderedSection', {
  value: babelPluginFlowReactPropTypes_proptype_RenderedSection,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams =  false ? null : {
  // One of SCROLL_DIRECTION_HORIZONTAL or SCROLL_DIRECTION_VERTICAL
  direction: __webpack_require__(0).oneOf(['horizontal', 'vertical']).isRequired,


  // One of SCROLL_DIRECTION_BACKWARD or SCROLL_DIRECTION_FORWARD
  scrollDirection: __webpack_require__(0).oneOf([-1, 1]).isRequired,


  // Number of rows or columns in the current axis
  cellCount: __webpack_require__(0).number.isRequired,


  // Maximum number of cells to over-render in either direction
  overscanCellsCount: __webpack_require__(0).number.isRequired,


  // Begin of range of visible cells
  startIndex: __webpack_require__(0).number.isRequired,


  // End of range of visible cells
  stopIndex: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams', {
  value: babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_OverscanIndices =  false ? null : {
  overscanStartIndex: __webpack_require__(0).number.isRequired,
  overscanStopIndex: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_OverscanIndices', {
  value: babelPluginFlowReactPropTypes_proptype_OverscanIndices,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter', {
  value: babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_Alignment =  false ? null : __webpack_require__(0).oneOf(['auto', 'end', 'start', 'center']);
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_Alignment', {
  value: babelPluginFlowReactPropTypes_proptype_Alignment,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_VisibleCellRange =  false ? null : {
  start: __webpack_require__(0).number,
  stop: __webpack_require__(0).number
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_VisibleCellRange', {
  value: babelPluginFlowReactPropTypes_proptype_VisibleCellRange,
  configurable: true
});

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

exports.default = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _defineProperty = __webpack_require__(179);

var _defineProperty2 = _interopRequireDefault(_defineProperty);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function () {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      (0, _defineProperty2.default)(target, descriptor.key, descriptor);
    }
  }

  return function (Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(176), __esModule: true };

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _typeof2 = __webpack_require__(88);

var _typeof3 = _interopRequireDefault(_typeof2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (self, call) {
  if (!self) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }

  return call && ((typeof call === "undefined" ? "undefined" : (0, _typeof3.default)(call)) === "object" || typeof call === "function") ? call : self;
};

/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _setPrototypeOf = __webpack_require__(204);

var _setPrototypeOf2 = _interopRequireDefault(_setPrototypeOf);

var _create = __webpack_require__(208);

var _create2 = _interopRequireDefault(_create);

var _typeof2 = __webpack_require__(88);

var _typeof3 = _interopRequireDefault(_typeof2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (subClass, superClass) {
  if (typeof superClass !== "function" && superClass !== null) {
    throw new TypeError("Super expression must either be null or a function, not " + (typeof superClass === "undefined" ? "undefined" : (0, _typeof3.default)(superClass)));
  }

  subClass.prototype = (0, _create2.default)(superClass && superClass.prototype, {
    constructor: {
      value: subClass,
      enumerable: false,
      writable: true,
      configurable: true
    }
  });
  if (superClass) _setPrototypeOf2.default ? (0, _setPrototypeOf2.default)(subClass, superClass) : subClass.__proto__ = superClass;
};

/***/ }),
/* 8 */
/***/ (function(module, exports) {

var core = module.exports = { version: '2.5.5' };
if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef


/***/ }),
/* 9 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Grid__ = __webpack_require__(94);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return __WEBPACK_IMPORTED_MODULE_0__Grid__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Grid", function() { return __WEBPACK_IMPORTED_MODULE_0__Grid__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__accessibilityOverscanIndicesGetter__ = __webpack_require__(223);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "accessibilityOverscanIndicesGetter", function() { return __WEBPACK_IMPORTED_MODULE_1__accessibilityOverscanIndicesGetter__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__defaultCellRangeRenderer__ = __webpack_require__(97);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultCellRangeRenderer", function() { return __WEBPACK_IMPORTED_MODULE_2__defaultCellRangeRenderer__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__defaultOverscanIndicesGetter__ = __webpack_require__(96);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultOverscanIndicesGetter", function() { return __WEBPACK_IMPORTED_MODULE_3__defaultOverscanIndicesGetter__["c"]; });









/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var bind = __webpack_require__(77);
var isBuffer = __webpack_require__(155);

/*global toString:true*/

// utils is a library of generic helper functions non-specific to axios

var toString = Object.prototype.toString;

/**
 * Determine if a value is an Array
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an Array, otherwise false
 */
function isArray(val) {
  return toString.call(val) === '[object Array]';
}

/**
 * Determine if a value is an ArrayBuffer
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an ArrayBuffer, otherwise false
 */
function isArrayBuffer(val) {
  return toString.call(val) === '[object ArrayBuffer]';
}

/**
 * Determine if a value is a FormData
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an FormData, otherwise false
 */
function isFormData(val) {
  return (typeof FormData !== 'undefined') && (val instanceof FormData);
}

/**
 * Determine if a value is a view on an ArrayBuffer
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
 */
function isArrayBufferView(val) {
  var result;
  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {
    result = ArrayBuffer.isView(val);
  } else {
    result = (val) && (val.buffer) && (val.buffer instanceof ArrayBuffer);
  }
  return result;
}

/**
 * Determine if a value is a String
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a String, otherwise false
 */
function isString(val) {
  return typeof val === 'string';
}

/**
 * Determine if a value is a Number
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Number, otherwise false
 */
function isNumber(val) {
  return typeof val === 'number';
}

/**
 * Determine if a value is undefined
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if the value is undefined, otherwise false
 */
function isUndefined(val) {
  return typeof val === 'undefined';
}

/**
 * Determine if a value is an Object
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is an Object, otherwise false
 */
function isObject(val) {
  return val !== null && typeof val === 'object';
}

/**
 * Determine if a value is a Date
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Date, otherwise false
 */
function isDate(val) {
  return toString.call(val) === '[object Date]';
}

/**
 * Determine if a value is a File
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a File, otherwise false
 */
function isFile(val) {
  return toString.call(val) === '[object File]';
}

/**
 * Determine if a value is a Blob
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Blob, otherwise false
 */
function isBlob(val) {
  return toString.call(val) === '[object Blob]';
}

/**
 * Determine if a value is a Function
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Function, otherwise false
 */
function isFunction(val) {
  return toString.call(val) === '[object Function]';
}

/**
 * Determine if a value is a Stream
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a Stream, otherwise false
 */
function isStream(val) {
  return isObject(val) && isFunction(val.pipe);
}

/**
 * Determine if a value is a URLSearchParams object
 *
 * @param {Object} val The value to test
 * @returns {boolean} True if value is a URLSearchParams object, otherwise false
 */
function isURLSearchParams(val) {
  return typeof URLSearchParams !== 'undefined' && val instanceof URLSearchParams;
}

/**
 * Trim excess whitespace off the beginning and end of a string
 *
 * @param {String} str The String to trim
 * @returns {String} The String freed of excess whitespace
 */
function trim(str) {
  return str.replace(/^\s*/, '').replace(/\s*$/, '');
}

/**
 * Determine if we're running in a standard browser environment
 *
 * This allows axios to run in a web worker, and react-native.
 * Both environments support XMLHttpRequest, but not fully standard globals.
 *
 * web workers:
 *  typeof window -> undefined
 *  typeof document -> undefined
 *
 * react-native:
 *  navigator.product -> 'ReactNative'
 */
function isStandardBrowserEnv() {
  if (typeof navigator !== 'undefined' && navigator.product === 'ReactNative') {
    return false;
  }
  return (
    typeof window !== 'undefined' &&
    typeof document !== 'undefined'
  );
}

/**
 * Iterate over an Array or an Object invoking a function for each item.
 *
 * If `obj` is an Array callback will be called passing
 * the value, index, and complete array for each item.
 *
 * If 'obj' is an Object callback will be called passing
 * the value, key, and complete object for each property.
 *
 * @param {Object|Array} obj The object to iterate
 * @param {Function} fn The callback to invoke for each item
 */
function forEach(obj, fn) {
  // Don't bother if no value provided
  if (obj === null || typeof obj === 'undefined') {
    return;
  }

  // Force an array if not already something iterable
  if (typeof obj !== 'object') {
    /*eslint no-param-reassign:0*/
    obj = [obj];
  }

  if (isArray(obj)) {
    // Iterate over array values
    for (var i = 0, l = obj.length; i < l; i++) {
      fn.call(null, obj[i], i, obj);
    }
  } else {
    // Iterate over object keys
    for (var key in obj) {
      if (Object.prototype.hasOwnProperty.call(obj, key)) {
        fn.call(null, obj[key], key, obj);
      }
    }
  }
}

/**
 * Accepts varargs expecting each argument to be an object, then
 * immutably merges the properties of each object and returns result.
 *
 * When multiple objects contain the same key the later object in
 * the arguments list will take precedence.
 *
 * Example:
 *
 * ```js
 * var result = merge({foo: 123}, {foo: 456});
 * console.log(result.foo); // outputs 456
 * ```
 *
 * @param {Object} obj1 Object to merge
 * @returns {Object} Result of all merge properties
 */
function merge(/* obj1, obj2, obj3, ... */) {
  var result = {};
  function assignValue(val, key) {
    if (typeof result[key] === 'object' && typeof val === 'object') {
      result[key] = merge(result[key], val);
    } else {
      result[key] = val;
    }
  }

  for (var i = 0, l = arguments.length; i < l; i++) {
    forEach(arguments[i], assignValue);
  }
  return result;
}

/**
 * Extends object a by mutably adding to it the properties of object b.
 *
 * @param {Object} a The object to be extended
 * @param {Object} b The object to copy properties from
 * @param {Object} thisArg The object to bind function to
 * @return {Object} The resulting value of object a
 */
function extend(a, b, thisArg) {
  forEach(b, function assignValue(val, key) {
    if (thisArg && typeof val === 'function') {
      a[key] = bind(val, thisArg);
    } else {
      a[key] = val;
    }
  });
  return a;
}

module.exports = {
  isArray: isArray,
  isArrayBuffer: isArrayBuffer,
  isBuffer: isBuffer,
  isFormData: isFormData,
  isArrayBufferView: isArrayBufferView,
  isString: isString,
  isNumber: isNumber,
  isObject: isObject,
  isUndefined: isUndefined,
  isDate: isDate,
  isFile: isFile,
  isBlob: isBlob,
  isFunction: isFunction,
  isStream: isStream,
  isURLSearchParams: isURLSearchParams,
  isStandardBrowserEnv: isStandardBrowserEnv,
  forEach: forEach,
  merge: merge,
  extend: extend,
  trim: trim
};


/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _assign = __webpack_require__(211);

var _assign2 = _interopRequireDefault(_assign);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = _assign2.default || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];

    for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }

  return target;
};

/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

var store = __webpack_require__(49)('wks');
var uid = __webpack_require__(35);
var Symbol = __webpack_require__(13).Symbol;
var USE_SYMBOL = typeof Symbol == 'function';

var $exports = module.exports = function (name) {
  return store[name] || (store[name] =
    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
};

$exports.store = store;


/***/ }),
/* 13 */
/***/ (function(module, exports) {

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
var global = module.exports = typeof window != 'undefined' && window.Math == Math
  ? window : typeof self != 'undefined' && self.Math == Math ? self
  // eslint-disable-next-line no-new-func
  : Function('return this')();
if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef


/***/ }),
/* 14 */
/***/ (function(module, exports) {

var hasOwnProperty = {}.hasOwnProperty;
module.exports = function (it, key) {
  return hasOwnProperty.call(it, key);
};


/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(13);
var core = __webpack_require__(8);
var ctx = __webpack_require__(85);
var hide = __webpack_require__(19);
var has = __webpack_require__(14);
var PROTOTYPE = 'prototype';

var $export = function (type, name, source) {
  var IS_FORCED = type & $export.F;
  var IS_GLOBAL = type & $export.G;
  var IS_STATIC = type & $export.S;
  var IS_PROTO = type & $export.P;
  var IS_BIND = type & $export.B;
  var IS_WRAP = type & $export.W;
  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
  var expProto = exports[PROTOTYPE];
  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] : (global[name] || {})[PROTOTYPE];
  var key, own, out;
  if (IS_GLOBAL) source = name;
  for (key in source) {
    // contains in native
    own = !IS_FORCED && target && target[key] !== undefined;
    if (own && has(exports, key)) continue;
    // export native or passed
    out = own ? target[key] : source[key];
    // prevent global pollution for namespaces
    exports[key] = IS_GLOBAL && typeof target[key] != 'function' ? source[key]
    // bind timers to global for call from export context
    : IS_BIND && own ? ctx(out, global)
    // wrap global constructors for prevent change them in library
    : IS_WRAP && target[key] == out ? (function (C) {
      var F = function (a, b, c) {
        if (this instanceof C) {
          switch (arguments.length) {
            case 0: return new C();
            case 1: return new C(a);
            case 2: return new C(a, b);
          } return new C(a, b, c);
        } return C.apply(this, arguments);
      };
      F[PROTOTYPE] = C[PROTOTYPE];
      return F;
    // make static versions for prototype methods
    })(out) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
    // export proto methods to core.%CONSTRUCTOR%.methods.%NAME%
    if (IS_PROTO) {
      (exports.virtual || (exports.virtual = {}))[key] = out;
      // export proto methods to core.%CONSTRUCTOR%.prototype.%NAME%
      if (type & $export.R && expProto && !expProto[key]) hide(expProto, key, out);
    }
  }
};
// type bitmap
$export.F = 1;   // forced
$export.G = 2;   // global
$export.S = 4;   // static
$export.P = 8;   // proto
$export.B = 16;  // bind
$export.W = 32;  // wrap
$export.U = 64;  // safe
$export.R = 128; // real proto method for `library`
module.exports = $export;


/***/ }),
/* 16 */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(20);
var IE8_DOM_DEFINE = __webpack_require__(86);
var toPrimitive = __webpack_require__(51);
var dP = Object.defineProperty;

exports.f = __webpack_require__(17) ? Object.defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPrimitive(P, true);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return dP(O, P, Attributes);
  } catch (e) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

// Thank's IE8 for his funny defineProperty
module.exports = !__webpack_require__(22)(function () {
  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

// to indexed object, toObject with fallback for non-array-like ES3 strings
var IObject = __webpack_require__(92);
var defined = __webpack_require__(47);
module.exports = function (it) {
  return IObject(defined(it));
};


/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

var dP = __webpack_require__(16);
var createDesc = __webpack_require__(36);
module.exports = __webpack_require__(17) ? function (object, key, value) {
  return dP.f(object, key, createDesc(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(21);
module.exports = function (it) {
  if (!isObject(it)) throw TypeError(it + ' is not an object!');
  return it;
};


/***/ }),
/* 21 */
/***/ (function(module, exports) {

module.exports = function (it) {
  return typeof it === 'object' ? it !== null : typeof it === 'function';
};


/***/ }),
/* 22 */
/***/ (function(module, exports) {

module.exports = function (exec) {
  try {
    return !!exec();
  } catch (e) {
    return true;
  }
};


/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!
  Copyright (c) 2016 Jed Watson.
  Licensed under the MIT License (MIT), see
  http://jedwatson.github.io/classnames
*/
/* global define */

(function () {
	'use strict';

	var hasOwn = {}.hasOwnProperty;

	function classNames () {
		var classes = [];

		for (var i = 0; i < arguments.length; i++) {
			var arg = arguments[i];
			if (!arg) continue;

			var argType = typeof arg;

			if (argType === 'string' || argType === 'number') {
				classes.push(arg);
			} else if (Array.isArray(arg)) {
				classes.push(classNames.apply(null, arg));
			} else if (argType === 'object') {
				for (var key in arg) {
					if (hasOwn.call(arg, key) && arg[key]) {
						classes.push(key);
					}
				}
			}
		}

		return classes.join(' ');
	}

	if (typeof module !== 'undefined' && module.exports) {
		module.exports = classNames;
	} else if (true) {
		// register as 'classnames', consistent with npm package name
		!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function () {
			return classNames;
		}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	} else {
		window.classNames = classNames;
	}
}());


/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

var babelPluginFlowReactPropTypes_proptype_Index =  false ? null : {
  index: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_Index", {
  value: babelPluginFlowReactPropTypes_proptype_Index,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_PositionInfo =  false ? null : {
  x: __webpack_require__(0).number.isRequired,
  y: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_PositionInfo", {
  value: babelPluginFlowReactPropTypes_proptype_PositionInfo,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_ScrollPosition =  false ? null : {
  scrollLeft: __webpack_require__(0).number.isRequired,
  scrollTop: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_ScrollPosition", {
  value: babelPluginFlowReactPropTypes_proptype_ScrollPosition,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo =  false ? null : {
  height: __webpack_require__(0).number.isRequired,
  width: __webpack_require__(0).number.isRequired,
  x: __webpack_require__(0).number.isRequired,
  y: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo", {
  value: babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_SizeInfo =  false ? null : {
  height: __webpack_require__(0).number.isRequired,
  width: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_SizeInfo", {
  value: babelPluginFlowReactPropTypes_proptype_SizeInfo,
  configurable: true
});

/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function checkDCE() {
  /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */
  if (
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' ||
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function'
  ) {
    return;
  }
  if (true) {
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
  }
  try {
    // Verify that the code above has been dead code eliminated (DCE'd).
    __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
  } catch (err) {
    // DevTools shouldn't crash React, no matter what.
    // We should still report in case we break this code.
    console.error(err);
  }
}

if (false) {
  // DCE check should happen before ReactDOM bundle executes so that
  // DevTools can report bad minification during injection.
  checkDCE();
  module.exports = require('./cjs/react-dom.production.min.js');
} else {
  module.exports = __webpack_require__(118);
}


/***/ }),
/* 26 */
/***/ (function(module, exports) {

module.exports = {};


/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.14 / 15.2.3.14 Object.keys(O)
var $keys = __webpack_require__(91);
var enumBugKeys = __webpack_require__(57);

module.exports = Object.keys || function keys(O) {
  return $keys(O, enumBugKeys);
};


/***/ }),
/* 28 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cancelAnimationTimeout", function() { return cancelAnimationTimeout; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestAnimationTimeout", function() { return requestAnimationTimeout; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__animationFrame__ = __webpack_require__(217);


var babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId =  false ? null : {
  id: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId', {
  value: babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId,
  configurable: true
});


var cancelAnimationTimeout = function cancelAnimationTimeout(frame) {
  return Object(__WEBPACK_IMPORTED_MODULE_0__animationFrame__["a" /* caf */])(frame.id);
};

/**
 * Recursively calls requestAnimationFrame until a specified delay has been met or exceeded.
 * When the delay time has been reached the function you're timing out will be called.
 *
 * Credit: Joe Lambert (https://gist.github.com/joelambert/1002116#file-requesttimeout-js)
 */
var requestAnimationTimeout = function requestAnimationTimeout(callback, delay) {
  var start = Date.now();

  var timeout = function timeout() {
    if (Date.now() - start >= delay) {
      callback.call();
    } else {
      frame.id = Object(__WEBPACK_IMPORTED_MODULE_0__animationFrame__["b" /* raf */])(timeout);
    }
  };

  var frame = {
    id: Object(__WEBPACK_IMPORTED_MODULE_0__animationFrame__["b" /* raf */])(timeout)
  };

  return frame;
};

/***/ }),
/* 29 */
/***/ (function(module, exports, __webpack_require__) {

var babelPluginFlowReactPropTypes_proptype_CellDataGetterParams =  false ? null : {
  columnData: __webpack_require__(0).any,
  dataKey: __webpack_require__(0).string.isRequired,
  rowData: function rowData(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error("Prop `" + propName + "` has type 'any' or 'mixed', but was not provided to `" + componentName + "`. Pass undefined or any other value.");
    }
  }
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_CellDataGetterParams", {
  value: babelPluginFlowReactPropTypes_proptype_CellDataGetterParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_CellRendererParams =  false ? null : {
  cellData: __webpack_require__(0).any,
  columnData: __webpack_require__(0).any,
  dataKey: __webpack_require__(0).string.isRequired,
  rowData: function rowData(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error("Prop `" + propName + "` has type 'any' or 'mixed', but was not provided to `" + componentName + "`. Pass undefined or any other value.");
    }
  },
  rowIndex: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_CellRendererParams", {
  value: babelPluginFlowReactPropTypes_proptype_CellRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams =  false ? null : {
  className: __webpack_require__(0).string.isRequired,
  columns: __webpack_require__(0).arrayOf(__webpack_require__(0).any).isRequired,
  style: function style(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error("Prop `" + propName + "` has type 'any' or 'mixed', but was not provided to `" + componentName + "`. Pass undefined or any other value.");
    }
  }
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams", {
  value: babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_HeaderRendererParams =  false ? null : {
  columnData: __webpack_require__(0).any,
  dataKey: __webpack_require__(0).string.isRequired,
  disableSort: __webpack_require__(0).bool,
  label: __webpack_require__(0).any,
  sortBy: __webpack_require__(0).string,
  sortDirection: __webpack_require__(0).string
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_HeaderRendererParams", {
  value: babelPluginFlowReactPropTypes_proptype_HeaderRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_RowRendererParams =  false ? null : {
  className: __webpack_require__(0).string.isRequired,
  columns: __webpack_require__(0).arrayOf(__webpack_require__(0).any).isRequired,
  index: __webpack_require__(0).number.isRequired,
  isScrolling: __webpack_require__(0).bool.isRequired,
  onRowClick: __webpack_require__(0).func,
  onRowDoubleClick: __webpack_require__(0).func,
  onRowMouseOver: __webpack_require__(0).func,
  onRowMouseOut: __webpack_require__(0).func,
  rowData: function rowData(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error("Prop `" + propName + "` has type 'any' or 'mixed', but was not provided to `" + componentName + "`. Pass undefined or any other value.");
    }
  },
  style: function style(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error("Prop `" + propName + "` has type 'any' or 'mixed', but was not provided to `" + componentName + "`. Pass undefined or any other value.");
    }
  }
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_RowRendererParams", {
  value: babelPluginFlowReactPropTypes_proptype_RowRendererParams,
  configurable: true
});

/***/ }),
/* 30 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var validateFormat = function validateFormat(format) {};

if (true) {
  validateFormat = function validateFormat(format) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  };
}

function invariant(condition, format, a, b, c, d, e, f) {
  validateFormat(format);

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
}

module.exports = invariant;

/***/ }),
/* 31 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyFunction = __webpack_require__(32);

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if (true) {
  var printWarning = function printWarning(format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  warning = function warning(condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

module.exports = warning;

/***/ }),
/* 32 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
var emptyFunction = function emptyFunction() {};

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;

/***/ }),
/* 33 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1,eval)("this");
} catch(e) {
	// This works if the window reference is available
	if(typeof window === "object")
		g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 34 */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.13 ToObject(argument)
var defined = __webpack_require__(47);
module.exports = function (it) {
  return Object(defined(it));
};


/***/ }),
/* 35 */
/***/ (function(module, exports) {

var id = 0;
var px = Math.random();
module.exports = function (key) {
  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
};


/***/ }),
/* 36 */
/***/ (function(module, exports) {

module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),
/* 37 */
/***/ (function(module, exports) {

exports.f = {}.propertyIsEnumerable;


/***/ }),
/* 38 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
var SortDirection = {
  /**
   * Sort items in ascending order.
   * This means arranging from the lowest value to the highest (e.g. a-z, 0-9).
   */
  ASC: 'ASC',

  /**
   * Sort items in descending order.
   * This means arranging from the highest value to the lowest (e.g. z-a, 9-0).
   */
  DESC: 'DESC'
};

/* harmony default export */ __webpack_exports__["a"] = (SortDirection);

/***/ }),
/* 39 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),
/* 40 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



if (true) {
  var invariant = __webpack_require__(30);
  var warning = __webpack_require__(31);
  var ReactPropTypesSecret = __webpack_require__(71);
  var loggedTypeFailures = {};
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (true) {
    for (var typeSpecName in typeSpecs) {
      if (typeSpecs.hasOwnProperty(typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          invariant(typeof typeSpecs[typeSpecName] === 'function', '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'the `prop-types` package, but received `%s`.', componentName || 'React class', location, typeSpecName, typeof typeSpecs[typeSpecName]);
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        warning(!error || error instanceof Error, '%s: type specification of %s `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', location, typeSpecName, typeof error);
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          warning(false, 'Failed %s type: %s%s', location, error.message, stack != null ? stack : '');
        }
      }
    }
  }
}

module.exports = checkPropTypes;


/***/ }),
/* 41 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__components_Provider__ = __webpack_require__(129);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__components_connectAdvanced__ = __webpack_require__(73);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__connect_connect__ = __webpack_require__(134);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Provider", function() { return __WEBPACK_IMPORTED_MODULE_0__components_Provider__["b"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "createProvider", function() { return __WEBPACK_IMPORTED_MODULE_0__components_Provider__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "connectAdvanced", function() { return __WEBPACK_IMPORTED_MODULE_1__components_connectAdvanced__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "connect", function() { return __WEBPACK_IMPORTED_MODULE_2__connect_connect__["a"]; });






/***/ }),
/* 42 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = warning;
/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */
  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */
}

/***/ }),
/* 43 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createStore", function() { return createStore; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "combineReducers", function() { return combineReducers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "bindActionCreators", function() { return bindActionCreators; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "applyMiddleware", function() { return applyMiddleware; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "compose", function() { return compose; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "__DO_NOT_USE__ActionTypes", function() { return ActionTypes; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_symbol_observable__ = __webpack_require__(137);


/**
 * These are private action types reserved by Redux.
 * For any unknown actions, you must return the current state.
 * If the current state is undefined, you must return the initial state.
 * Do not reference these action types directly in your code.
 */
var ActionTypes = {
  INIT: '@@redux/INIT' + Math.random().toString(36).substring(7).split('').join('.'),
  REPLACE: '@@redux/REPLACE' + Math.random().toString(36).substring(7).split('').join('.')
};

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) {
  return typeof obj;
} : function (obj) {
  return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
};

var _extends = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];

    for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }

  return target;
};

/**
 * @param {any} obj The object to inspect.
 * @returns {boolean} True if the argument appears to be a plain object.
 */
function isPlainObject(obj) {
  if ((typeof obj === 'undefined' ? 'undefined' : _typeof(obj)) !== 'object' || obj === null) return false;

  var proto = obj;
  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }

  return Object.getPrototypeOf(obj) === proto;
}

/**
 * Creates a Redux store that holds the state tree.
 * The only way to change the data in the store is to call `dispatch()` on it.
 *
 * There should only be a single store in your app. To specify how different
 * parts of the state tree respond to actions, you may combine several reducers
 * into a single reducer function by using `combineReducers`.
 *
 * @param {Function} reducer A function that returns the next state tree, given
 * the current state tree and the action to handle.
 *
 * @param {any} [preloadedState] The initial state. You may optionally specify it
 * to hydrate the state from the server in universal apps, or to restore a
 * previously serialized user session.
 * If you use `combineReducers` to produce the root reducer function, this must be
 * an object with the same shape as `combineReducers` keys.
 *
 * @param {Function} [enhancer] The store enhancer. You may optionally specify it
 * to enhance the store with third-party capabilities such as middleware,
 * time travel, persistence, etc. The only store enhancer that ships with Redux
 * is `applyMiddleware()`.
 *
 * @returns {Store} A Redux store that lets you read the state, dispatch actions
 * and subscribe to changes.
 */
function createStore(reducer, preloadedState, enhancer) {
  var _ref2;

  if (typeof preloadedState === 'function' && typeof enhancer === 'undefined') {
    enhancer = preloadedState;
    preloadedState = undefined;
  }

  if (typeof enhancer !== 'undefined') {
    if (typeof enhancer !== 'function') {
      throw new Error('Expected the enhancer to be a function.');
    }

    return enhancer(createStore)(reducer, preloadedState);
  }

  if (typeof reducer !== 'function') {
    throw new Error('Expected the reducer to be a function.');
  }

  var currentReducer = reducer;
  var currentState = preloadedState;
  var currentListeners = [];
  var nextListeners = currentListeners;
  var isDispatching = false;

  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = currentListeners.slice();
    }
  }

  /**
   * Reads the state tree managed by the store.
   *
   * @returns {any} The current state tree of your application.
   */
  function getState() {
    if (isDispatching) {
      throw new Error('You may not call store.getState() while the reducer is executing. ' + 'The reducer has already received the state as an argument. ' + 'Pass it down from the top reducer instead of reading it from the store.');
    }

    return currentState;
  }

  /**
   * Adds a change listener. It will be called any time an action is dispatched,
   * and some part of the state tree may potentially have changed. You may then
   * call `getState()` to read the current state tree inside the callback.
   *
   * You may call `dispatch()` from a change listener, with the following
   * caveats:
   *
   * 1. The subscriptions are snapshotted just before every `dispatch()` call.
   * If you subscribe or unsubscribe while the listeners are being invoked, this
   * will not have any effect on the `dispatch()` that is currently in progress.
   * However, the next `dispatch()` call, whether nested or not, will use a more
   * recent snapshot of the subscription list.
   *
   * 2. The listener should not expect to see all state changes, as the state
   * might have been updated multiple times during a nested `dispatch()` before
   * the listener is called. It is, however, guaranteed that all subscribers
   * registered before the `dispatch()` started will be called with the latest
   * state by the time it exits.
   *
   * @param {Function} listener A callback to be invoked on every dispatch.
   * @returns {Function} A function to remove this change listener.
   */
  function subscribe(listener) {
    if (typeof listener !== 'function') {
      throw new Error('Expected the listener to be a function.');
    }

    if (isDispatching) {
      throw new Error('You may not call store.subscribe() while the reducer is executing. ' + 'If you would like to be notified after the store has been updated, subscribe from a ' + 'component and invoke store.getState() in the callback to access the latest state. ' + 'See https://redux.js.org/api-reference/store#subscribe(listener) for more details.');
    }

    var isSubscribed = true;

    ensureCanMutateNextListeners();
    nextListeners.push(listener);

    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }

      if (isDispatching) {
        throw new Error('You may not unsubscribe from a store listener while the reducer is executing. ' + 'See https://redux.js.org/api-reference/store#subscribe(listener) for more details.');
      }

      isSubscribed = false;

      ensureCanMutateNextListeners();
      var index = nextListeners.indexOf(listener);
      nextListeners.splice(index, 1);
    };
  }

  /**
   * Dispatches an action. It is the only way to trigger a state change.
   *
   * The `reducer` function, used to create the store, will be called with the
   * current state tree and the given `action`. Its return value will
   * be considered the **next** state of the tree, and the change listeners
   * will be notified.
   *
   * The base implementation only supports plain object actions. If you want to
   * dispatch a Promise, an Observable, a thunk, or something else, you need to
   * wrap your store creating function into the corresponding middleware. For
   * example, see the documentation for the `redux-thunk` package. Even the
   * middleware will eventually dispatch plain object actions using this method.
   *
   * @param {Object} action A plain object representing “what changed”. It is
   * a good idea to keep actions serializable so you can record and replay user
   * sessions, or use the time travelling `redux-devtools`. An action must have
   * a `type` property which may not be `undefined`. It is a good idea to use
   * string constants for action types.
   *
   * @returns {Object} For convenience, the same action object you dispatched.
   *
   * Note that, if you use a custom middleware, it may wrap `dispatch()` to
   * return something else (for example, a Promise you can await).
   */
  function dispatch(action) {
    if (!isPlainObject(action)) {
      throw new Error('Actions must be plain objects. ' + 'Use custom middleware for async actions.');
    }

    if (typeof action.type === 'undefined') {
      throw new Error('Actions may not have an undefined "type" property. ' + 'Have you misspelled a constant?');
    }

    if (isDispatching) {
      throw new Error('Reducers may not dispatch actions.');
    }

    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }

    var listeners = currentListeners = nextListeners;
    for (var i = 0; i < listeners.length; i++) {
      var listener = listeners[i];
      listener();
    }

    return action;
  }

  /**
   * Replaces the reducer currently used by the store to calculate the state.
   *
   * You might need this if your app implements code splitting and you want to
   * load some of the reducers dynamically. You might also need this if you
   * implement a hot reloading mechanism for Redux.
   *
   * @param {Function} nextReducer The reducer for the store to use instead.
   * @returns {void}
   */
  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== 'function') {
      throw new Error('Expected the nextReducer to be a function.');
    }

    currentReducer = nextReducer;
    dispatch({ type: ActionTypes.REPLACE });
  }

  /**
   * Interoperability point for observable/reactive libraries.
   * @returns {observable} A minimal observable of state changes.
   * For more information, see the observable proposal:
   * https://github.com/tc39/proposal-observable
   */
  function observable() {
    var _ref;

    var outerSubscribe = subscribe;
    return _ref = {
      /**
       * The minimal observable subscription method.
       * @param {Object} observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns {subscription} An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe: function subscribe(observer) {
        if ((typeof observer === 'undefined' ? 'undefined' : _typeof(observer)) !== 'object' || observer === null) {
          throw new TypeError('Expected the observer to be an object.');
        }

        function observeState() {
          if (observer.next) {
            observer.next(getState());
          }
        }

        observeState();
        var unsubscribe = outerSubscribe(observeState);
        return { unsubscribe: unsubscribe };
      }
    }, _ref[__WEBPACK_IMPORTED_MODULE_0_symbol_observable__["a" /* default */]] = function () {
      return this;
    }, _ref;
  }

  // When a store is created, an "INIT" action is dispatched so that every
  // reducer returns their initial state. This effectively populates
  // the initial state tree.
  dispatch({ type: ActionTypes.INIT });

  return _ref2 = {
    dispatch: dispatch,
    subscribe: subscribe,
    getState: getState,
    replaceReducer: replaceReducer
  }, _ref2[__WEBPACK_IMPORTED_MODULE_0_symbol_observable__["a" /* default */]] = observable, _ref2;
}

/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */
  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
  } catch (e) {} // eslint-disable-line no-empty
}

function getUndefinedStateErrorMessage(key, action) {
  var actionType = action && action.type;
  var actionDescription = actionType && 'action "' + String(actionType) + '"' || 'an action';

  return 'Given ' + actionDescription + ', reducer "' + key + '" returned undefined. ' + 'To ignore an action, you must explicitly return the previous state. ' + 'If you want this reducer to hold no value, you can return null instead of undefined.';
}

function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  var reducerKeys = Object.keys(reducers);
  var argumentName = action && action.type === ActionTypes.INIT ? 'preloadedState argument passed to createStore' : 'previous state received by the reducer';

  if (reducerKeys.length === 0) {
    return 'Store does not have a valid reducer. Make sure the argument passed ' + 'to combineReducers is an object whose values are reducers.';
  }

  if (!isPlainObject(inputState)) {
    return 'The ' + argumentName + ' has unexpected type of "' + {}.toString.call(inputState).match(/\s([a-z|A-Z]+)/)[1] + '". Expected argument to be an object with the following ' + ('keys: "' + reducerKeys.join('", "') + '"');
  }

  var unexpectedKeys = Object.keys(inputState).filter(function (key) {
    return !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key];
  });

  unexpectedKeys.forEach(function (key) {
    unexpectedKeyCache[key] = true;
  });

  if (action && action.type === ActionTypes.REPLACE) return;

  if (unexpectedKeys.length > 0) {
    return 'Unexpected ' + (unexpectedKeys.length > 1 ? 'keys' : 'key') + ' ' + ('"' + unexpectedKeys.join('", "') + '" found in ' + argumentName + '. ') + 'Expected to find one of the known reducer keys instead: ' + ('"' + reducerKeys.join('", "') + '". Unexpected keys will be ignored.');
  }
}

function assertReducerShape(reducers) {
  Object.keys(reducers).forEach(function (key) {
    var reducer = reducers[key];
    var initialState = reducer(undefined, { type: ActionTypes.INIT });

    if (typeof initialState === 'undefined') {
      throw new Error('Reducer "' + key + '" returned undefined during initialization. ' + 'If the state passed to the reducer is undefined, you must ' + 'explicitly return the initial state. The initial state may ' + 'not be undefined. If you don\'t want to set a value for this reducer, ' + 'you can use null instead of undefined.');
    }

    var type = '@@redux/PROBE_UNKNOWN_ACTION_' + Math.random().toString(36).substring(7).split('').join('.');
    if (typeof reducer(undefined, { type: type }) === 'undefined') {
      throw new Error('Reducer "' + key + '" returned undefined when probed with a random type. ' + ('Don\'t try to handle ' + ActionTypes.INIT + ' or other actions in "redux/*" ') + 'namespace. They are considered private. Instead, you must return the ' + 'current state for any unknown actions, unless it is undefined, ' + 'in which case you must return the initial state, regardless of the ' + 'action type. The initial state may not be undefined, but can be null.');
    }
  });
}

/**
 * Turns an object whose values are different reducer functions, into a single
 * reducer function. It will call every child reducer, and gather their results
 * into a single state object, whose keys correspond to the keys of the passed
 * reducer functions.
 *
 * @param {Object} reducers An object whose values correspond to different
 * reducer functions that need to be combined into one. One handy way to obtain
 * it is to use ES6 `import * as reducers` syntax. The reducers may never return
 * undefined for any action. Instead, they should return their initial state
 * if the state passed to them was undefined, and the current state for any
 * unrecognized action.
 *
 * @returns {Function} A reducer function that invokes every reducer inside the
 * passed object, and builds a state object with the same shape.
 */
function combineReducers(reducers) {
  var reducerKeys = Object.keys(reducers);
  var finalReducers = {};
  for (var i = 0; i < reducerKeys.length; i++) {
    var key = reducerKeys[i];

    if (true) {
      if (typeof reducers[key] === 'undefined') {
        warning('No reducer provided for key "' + key + '"');
      }
    }

    if (typeof reducers[key] === 'function') {
      finalReducers[key] = reducers[key];
    }
  }
  var finalReducerKeys = Object.keys(finalReducers);

  var unexpectedKeyCache = void 0;
  if (true) {
    unexpectedKeyCache = {};
  }

  var shapeAssertionError = void 0;
  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }

  return function combination() {
    var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    var action = arguments[1];

    if (shapeAssertionError) {
      throw shapeAssertionError;
    }

    if (true) {
      var warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);
      if (warningMessage) {
        warning(warningMessage);
      }
    }

    var hasChanged = false;
    var nextState = {};
    for (var _i = 0; _i < finalReducerKeys.length; _i++) {
      var _key = finalReducerKeys[_i];
      var reducer = finalReducers[_key];
      var previousStateForKey = state[_key];
      var nextStateForKey = reducer(previousStateForKey, action);
      if (typeof nextStateForKey === 'undefined') {
        var errorMessage = getUndefinedStateErrorMessage(_key, action);
        throw new Error(errorMessage);
      }
      nextState[_key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }
    return hasChanged ? nextState : state;
  };
}

function bindActionCreator(actionCreator, dispatch) {
  return function () {
    return dispatch(actionCreator.apply(this, arguments));
  };
}

/**
 * Turns an object whose values are action creators, into an object with the
 * same keys, but with every function wrapped into a `dispatch` call so they
 * may be invoked directly. This is just a convenience method, as you can call
 * `store.dispatch(MyActionCreators.doSomething())` yourself just fine.
 *
 * For convenience, you can also pass a single function as the first argument,
 * and get a function in return.
 *
 * @param {Function|Object} actionCreators An object whose values are action
 * creator functions. One handy way to obtain it is to use ES6 `import * as`
 * syntax. You may also pass a single function.
 *
 * @param {Function} dispatch The `dispatch` function available on your Redux
 * store.
 *
 * @returns {Function|Object} The object mimicking the original object, but with
 * every action creator wrapped into the `dispatch` call. If you passed a
 * function as `actionCreators`, the return value will also be a single
 * function.
 */
function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === 'function') {
    return bindActionCreator(actionCreators, dispatch);
  }

  if ((typeof actionCreators === 'undefined' ? 'undefined' : _typeof(actionCreators)) !== 'object' || actionCreators === null) {
    throw new Error('bindActionCreators expected an object or a function, instead received ' + (actionCreators === null ? 'null' : typeof actionCreators === 'undefined' ? 'undefined' : _typeof(actionCreators)) + '. ' + 'Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?');
  }

  var keys = Object.keys(actionCreators);
  var boundActionCreators = {};
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    var actionCreator = actionCreators[key];
    if (typeof actionCreator === 'function') {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }
  return boundActionCreators;
}

/**
 * Composes single-argument functions from right to left. The rightmost
 * function can take multiple arguments as it provides the signature for
 * the resulting composite function.
 *
 * @param {...Function} funcs The functions to compose.
 * @returns {Function} A function obtained by composing the argument functions
 * from right to left. For example, compose(f, g, h) is identical to doing
 * (...args) => f(g(h(...args))).
 */

function compose() {
  for (var _len = arguments.length, funcs = Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  if (funcs.length === 0) {
    return function (arg) {
      return arg;
    };
  }

  if (funcs.length === 1) {
    return funcs[0];
  }

  return funcs.reduce(function (a, b) {
    return function () {
      return a(b.apply(undefined, arguments));
    };
  });
}

/**
 * Creates a store enhancer that applies middleware to the dispatch method
 * of the Redux store. This is handy for a variety of tasks, such as expressing
 * asynchronous actions in a concise manner, or logging every action payload.
 *
 * See `redux-thunk` package as an example of the Redux middleware.
 *
 * Because middleware is potentially asynchronous, this should be the first
 * store enhancer in the composition chain.
 *
 * Note that each middleware will be given the `dispatch` and `getState` functions
 * as named arguments.
 *
 * @param {...Function} middlewares The middleware chain to be applied.
 * @returns {Function} A store enhancer applying the middleware.
 */
function applyMiddleware() {
  for (var _len = arguments.length, middlewares = Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  return function (createStore) {
    return function () {
      for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }

      var store = createStore.apply(undefined, args);
      var _dispatch = function dispatch() {
        throw new Error('Dispatching while constructing your middleware is not allowed. ' + 'Other middleware would not be applied to this dispatch.');
      };

      var middlewareAPI = {
        getState: store.getState,
        dispatch: function dispatch() {
          return _dispatch.apply(undefined, arguments);
        }
      };
      var chain = middlewares.map(function (middleware) {
        return middleware(middlewareAPI);
      });
      _dispatch = compose.apply(undefined, chain)(store.dispatch);

      return _extends({}, store, {
        dispatch: _dispatch
      });
    };
  };
}

/*
 * This is a dummy function to check if the function name has been altered by minification.
 * If the function has been minified and NODE_ENV !== 'production', warn the user.
 */
function isCrushed() {}

if ("development" !== 'production' && typeof isCrushed.name === 'string' && isCrushed.name !== 'isCrushed') {
  warning("You are currently using minified code outside of NODE_ENV === 'production'. " + 'This means that you are running a slower development build of Redux. ' + 'You can use loose-envify (https://github.com/zertosh/loose-envify) for browserify ' + 'or DefinePlugin for webpack (http://stackoverflow.com/questions/30030031) ' + 'to ensure you have the correct code for your production build.');
}




/***/ }),
/* 44 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(154);

/***/ }),
/* 45 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

var utils = __webpack_require__(10);
var normalizeHeaderName = __webpack_require__(158);

var DEFAULT_CONTENT_TYPE = {
  'Content-Type': 'application/x-www-form-urlencoded'
};

function setContentTypeIfUnset(headers, value) {
  if (!utils.isUndefined(headers) && utils.isUndefined(headers['Content-Type'])) {
    headers['Content-Type'] = value;
  }
}

function getDefaultAdapter() {
  var adapter;
  if (typeof XMLHttpRequest !== 'undefined') {
    // For browsers use XHR adapter
    adapter = __webpack_require__(78);
  } else if (typeof process !== 'undefined') {
    // For node use HTTP adapter
    adapter = __webpack_require__(78);
  }
  return adapter;
}

var defaults = {
  adapter: getDefaultAdapter(),

  transformRequest: [function transformRequest(data, headers) {
    normalizeHeaderName(headers, 'Content-Type');
    if (utils.isFormData(data) ||
      utils.isArrayBuffer(data) ||
      utils.isBuffer(data) ||
      utils.isStream(data) ||
      utils.isFile(data) ||
      utils.isBlob(data)
    ) {
      return data;
    }
    if (utils.isArrayBufferView(data)) {
      return data.buffer;
    }
    if (utils.isURLSearchParams(data)) {
      setContentTypeIfUnset(headers, 'application/x-www-form-urlencoded;charset=utf-8');
      return data.toString();
    }
    if (utils.isObject(data)) {
      setContentTypeIfUnset(headers, 'application/json;charset=utf-8');
      return JSON.stringify(data);
    }
    return data;
  }],

  transformResponse: [function transformResponse(data) {
    /*eslint no-param-reassign:0*/
    if (typeof data === 'string') {
      try {
        data = JSON.parse(data);
      } catch (e) { /* Ignore */ }
    }
    return data;
  }],

  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,

  xsrfCookieName: 'XSRF-TOKEN',
  xsrfHeaderName: 'X-XSRF-TOKEN',

  maxContentLength: -1,

  validateStatus: function validateStatus(status) {
    return status >= 200 && status < 300;
  }
};

defaults.headers = {
  common: {
    'Accept': 'application/json, text/plain, */*'
  }
};

utils.forEach(['delete', 'get', 'head'], function forEachMethodNoData(method) {
  defaults.headers[method] = {};
});

utils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {
  defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);
});

module.exports = defaults;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(157)))

/***/ }),
/* 46 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.saveInterview = exports.scanData = exports.scanInfo = undefined;

var _axios = __webpack_require__(44);

var _axios2 = _interopRequireDefault(_axios);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var scanInfo = exports.scanInfo = window.scanInfo;
var scanData = exports.scanData = window.scanData;

var saveInterview = exports.saveInterview = function saveInterview(id, data) {
  _axios2.default.post('/scan/' + id + '/save', {
    data: data
  }).then(function (response) {
    console.log(response);
  });
};

/***/ }),
/* 47 */
/***/ (function(module, exports) {

// 7.2.1 RequireObjectCoercible(argument)
module.exports = function (it) {
  if (it == undefined) throw TypeError("Can't call method on  " + it);
  return it;
};


/***/ }),
/* 48 */
/***/ (function(module, exports, __webpack_require__) {

var shared = __webpack_require__(49)('keys');
var uid = __webpack_require__(35);
module.exports = function (key) {
  return shared[key] || (shared[key] = uid(key));
};


/***/ }),
/* 49 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(13);
var SHARED = '__core-js_shared__';
var store = global[SHARED] || (global[SHARED] = {});
module.exports = function (key) {
  return store[key] || (store[key] = {});
};


/***/ }),
/* 50 */
/***/ (function(module, exports, __webpack_require__) {

// most Object methods by ES6 should accept primitives
var $export = __webpack_require__(15);
var core = __webpack_require__(8);
var fails = __webpack_require__(22);
module.exports = function (KEY, exec) {
  var fn = (core.Object || {})[KEY] || Object[KEY];
  var exp = {};
  exp[KEY] = exec(fn);
  $export($export.S + $export.F * fails(function () { fn(1); }), 'Object', exp);
};


/***/ }),
/* 51 */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.1 ToPrimitive(input [, PreferredType])
var isObject = __webpack_require__(21);
// instead of the ES6 spec version, we didn't implement @@toPrimitive case
// and the second argument - flag - preferred type is a string
module.exports = function (it, S) {
  if (!isObject(it)) return it;
  var fn, val;
  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  throw TypeError("Can't convert object to primitive value");
};


/***/ }),
/* 52 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $at = __webpack_require__(184)(true);

// 21.1.3.27 String.prototype[@@iterator]()
__webpack_require__(89)(String, 'String', function (iterated) {
  this._t = String(iterated); // target
  this._i = 0;                // next index
// 21.1.5.2.1 %StringIteratorPrototype%.next()
}, function () {
  var O = this._t;
  var index = this._i;
  var point;
  if (index >= O.length) return { value: undefined, done: true };
  point = $at(O, index);
  this._i += point.length;
  return { value: point, done: false };
});


/***/ }),
/* 53 */
/***/ (function(module, exports) {

// 7.1.4 ToInteger
var ceil = Math.ceil;
var floor = Math.floor;
module.exports = function (it) {
  return isNaN(it = +it) ? 0 : (it > 0 ? floor : ceil)(it);
};


/***/ }),
/* 54 */
/***/ (function(module, exports) {

module.exports = true;


/***/ }),
/* 55 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
var anObject = __webpack_require__(20);
var dPs = __webpack_require__(186);
var enumBugKeys = __webpack_require__(57);
var IE_PROTO = __webpack_require__(48)('IE_PROTO');
var Empty = function () { /* empty */ };
var PROTOTYPE = 'prototype';

// Create object with fake `null` prototype: use iframe Object with cleared prototype
var createDict = function () {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = __webpack_require__(87)('iframe');
  var i = enumBugKeys.length;
  var lt = '<';
  var gt = '>';
  var iframeDocument;
  iframe.style.display = 'none';
  __webpack_require__(190).appendChild(iframe);
  iframe.src = 'javascript:'; // eslint-disable-line no-script-url
  // createDict = iframe.contentWindow.Object;
  // html.removeChild(iframe);
  iframeDocument = iframe.contentWindow.document;
  iframeDocument.open();
  iframeDocument.write(lt + 'script' + gt + 'document.F=Object' + lt + '/script' + gt);
  iframeDocument.close();
  createDict = iframeDocument.F;
  while (i--) delete createDict[PROTOTYPE][enumBugKeys[i]];
  return createDict();
};

module.exports = Object.create || function create(O, Properties) {
  var result;
  if (O !== null) {
    Empty[PROTOTYPE] = anObject(O);
    result = new Empty();
    Empty[PROTOTYPE] = null;
    // add "__proto__" for Object.getPrototypeOf polyfill
    result[IE_PROTO] = O;
  } else result = createDict();
  return Properties === undefined ? result : dPs(result, Properties);
};


/***/ }),
/* 56 */
/***/ (function(module, exports) {

var toString = {}.toString;

module.exports = function (it) {
  return toString.call(it).slice(8, -1);
};


/***/ }),
/* 57 */
/***/ (function(module, exports) {

// IE 8- don't enum bug keys
module.exports = (
  'constructor,hasOwnProperty,isPrototypeOf,propertyIsEnumerable,toLocaleString,toString,valueOf'
).split(',');


/***/ }),
/* 58 */
/***/ (function(module, exports, __webpack_require__) {

var def = __webpack_require__(16).f;
var has = __webpack_require__(14);
var TAG = __webpack_require__(12)('toStringTag');

module.exports = function (it, tag, stat) {
  if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
};


/***/ }),
/* 59 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(191);
var global = __webpack_require__(13);
var hide = __webpack_require__(19);
var Iterators = __webpack_require__(26);
var TO_STRING_TAG = __webpack_require__(12)('toStringTag');

var DOMIterables = ('CSSRuleList,CSSStyleDeclaration,CSSValueList,ClientRectList,DOMRectList,DOMStringList,' +
  'DOMTokenList,DataTransferItemList,FileList,HTMLAllCollection,HTMLCollection,HTMLFormElement,HTMLSelectElement,' +
  'MediaList,MimeTypeArray,NamedNodeMap,NodeList,PaintRequestList,Plugin,PluginArray,SVGLengthList,SVGNumberList,' +
  'SVGPathSegList,SVGPointList,SVGStringList,SVGTransformList,SourceBufferList,StyleSheetList,TextTrackCueList,' +
  'TextTrackList,TouchList').split(',');

for (var i = 0; i < DOMIterables.length; i++) {
  var NAME = DOMIterables[i];
  var Collection = global[NAME];
  var proto = Collection && Collection.prototype;
  if (proto && !proto[TO_STRING_TAG]) hide(proto, TO_STRING_TAG, NAME);
  Iterators[NAME] = Iterators.Array;
}


/***/ }),
/* 60 */
/***/ (function(module, exports, __webpack_require__) {

exports.f = __webpack_require__(12);


/***/ }),
/* 61 */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(13);
var core = __webpack_require__(8);
var LIBRARY = __webpack_require__(54);
var wksExt = __webpack_require__(60);
var defineProperty = __webpack_require__(16).f;
module.exports = function (name) {
  var $Symbol = core.Symbol || (core.Symbol = LIBRARY ? {} : global.Symbol || {});
  if (name.charAt(0) != '_' && !(name in $Symbol)) defineProperty($Symbol, name, { value: wksExt.f(name) });
};


/***/ }),
/* 62 */
/***/ (function(module, exports) {

exports.f = Object.getOwnPropertySymbols;


/***/ }),
/* 63 */
/***/ (function(module, exports, __webpack_require__) {

var pIE = __webpack_require__(37);
var createDesc = __webpack_require__(36);
var toIObject = __webpack_require__(18);
var toPrimitive = __webpack_require__(51);
var has = __webpack_require__(14);
var IE8_DOM_DEFINE = __webpack_require__(86);
var gOPD = Object.getOwnPropertyDescriptor;

exports.f = __webpack_require__(17) ? gOPD : function getOwnPropertyDescriptor(O, P) {
  O = toIObject(O);
  P = toPrimitive(P, true);
  if (IE8_DOM_DEFINE) try {
    return gOPD(O, P);
  } catch (e) { /* empty */ }
  if (has(O, P)) return createDesc(!pIE.f.call(O, P), O[P]);
};


/***/ }),
/* 64 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_objectWithoutProperties__ = __webpack_require__(65);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_objectWithoutProperties___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_objectWithoutProperties__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__CellSizeAndPositionManager__ = __webpack_require__(215);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__maxElementSize_js__ = __webpack_require__(216);




var babelPluginFlowReactPropTypes_proptype_VisibleCellRange = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_VisibleCellRange || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellSizeGetter = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellSizeGetter || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Alignment = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_Alignment || __webpack_require__(0).any;




/**
 * Browsers have scroll offset limitations (eg Chrome stops scrolling at ~33.5M pixels where as Edge tops out at ~1.5M pixels).
 * After a certain position, the browser won't allow the user to scroll further (even via JavaScript scroll offset adjustments).
 * This util picks a lower ceiling for max size and artificially adjusts positions within to make it transparent for users.
 */

/**
 * Extends CellSizeAndPositionManager and adds scaling behavior for lists that are too large to fit within a browser's native limits.
 */
var ScalingCellSizeAndPositionManager = function () {
  function ScalingCellSizeAndPositionManager(_ref) {
    var _ref$maxScrollSize = _ref.maxScrollSize,
        maxScrollSize = _ref$maxScrollSize === undefined ? Object(__WEBPACK_IMPORTED_MODULE_4__maxElementSize_js__["a" /* getMaxElementSize */])() : _ref$maxScrollSize,
        params = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_objectWithoutProperties___default()(_ref, ['maxScrollSize']);

    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, ScalingCellSizeAndPositionManager);

    // Favor composition over inheritance to simplify IE10 support
    this._cellSizeAndPositionManager = new __WEBPACK_IMPORTED_MODULE_3__CellSizeAndPositionManager__["a" /* default */](params);
    this._maxScrollSize = maxScrollSize;
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(ScalingCellSizeAndPositionManager, [{
    key: 'areOffsetsAdjusted',
    value: function areOffsetsAdjusted() {
      return this._cellSizeAndPositionManager.getTotalSize() > this._maxScrollSize;
    }
  }, {
    key: 'configure',
    value: function configure(params) {
      this._cellSizeAndPositionManager.configure(params);
    }
  }, {
    key: 'getCellCount',
    value: function getCellCount() {
      return this._cellSizeAndPositionManager.getCellCount();
    }
  }, {
    key: 'getEstimatedCellSize',
    value: function getEstimatedCellSize() {
      return this._cellSizeAndPositionManager.getEstimatedCellSize();
    }
  }, {
    key: 'getLastMeasuredIndex',
    value: function getLastMeasuredIndex() {
      return this._cellSizeAndPositionManager.getLastMeasuredIndex();
    }

    /**
     * Number of pixels a cell at the given position (offset) should be shifted in order to fit within the scaled container.
     * The offset passed to this function is scaled (safe) as well.
     */

  }, {
    key: 'getOffsetAdjustment',
    value: function getOffsetAdjustment(_ref2) {
      var containerSize = _ref2.containerSize,
          offset = _ref2.offset;

      var totalSize = this._cellSizeAndPositionManager.getTotalSize();
      var safeTotalSize = this.getTotalSize();
      var offsetPercentage = this._getOffsetPercentage({
        containerSize: containerSize,
        offset: offset,
        totalSize: safeTotalSize
      });

      return Math.round(offsetPercentage * (safeTotalSize - totalSize));
    }
  }, {
    key: 'getSizeAndPositionOfCell',
    value: function getSizeAndPositionOfCell(index) {
      return this._cellSizeAndPositionManager.getSizeAndPositionOfCell(index);
    }
  }, {
    key: 'getSizeAndPositionOfLastMeasuredCell',
    value: function getSizeAndPositionOfLastMeasuredCell() {
      return this._cellSizeAndPositionManager.getSizeAndPositionOfLastMeasuredCell();
    }

    /** See CellSizeAndPositionManager#getTotalSize */

  }, {
    key: 'getTotalSize',
    value: function getTotalSize() {
      return Math.min(this._maxScrollSize, this._cellSizeAndPositionManager.getTotalSize());
    }

    /** See CellSizeAndPositionManager#getUpdatedOffsetForIndex */

  }, {
    key: 'getUpdatedOffsetForIndex',
    value: function getUpdatedOffsetForIndex(_ref3) {
      var _ref3$align = _ref3.align,
          align = _ref3$align === undefined ? 'auto' : _ref3$align,
          containerSize = _ref3.containerSize,
          currentOffset = _ref3.currentOffset,
          targetIndex = _ref3.targetIndex;

      currentOffset = this._safeOffsetToOffset({
        containerSize: containerSize,
        offset: currentOffset
      });

      var offset = this._cellSizeAndPositionManager.getUpdatedOffsetForIndex({
        align: align,
        containerSize: containerSize,
        currentOffset: currentOffset,
        targetIndex: targetIndex
      });

      return this._offsetToSafeOffset({
        containerSize: containerSize,
        offset: offset
      });
    }

    /** See CellSizeAndPositionManager#getVisibleCellRange */

  }, {
    key: 'getVisibleCellRange',
    value: function getVisibleCellRange(_ref4) {
      var containerSize = _ref4.containerSize,
          offset = _ref4.offset;

      offset = this._safeOffsetToOffset({
        containerSize: containerSize,
        offset: offset
      });

      return this._cellSizeAndPositionManager.getVisibleCellRange({
        containerSize: containerSize,
        offset: offset
      });
    }
  }, {
    key: 'resetCell',
    value: function resetCell(index) {
      this._cellSizeAndPositionManager.resetCell(index);
    }
  }, {
    key: '_getOffsetPercentage',
    value: function _getOffsetPercentage(_ref5) {
      var containerSize = _ref5.containerSize,
          offset = _ref5.offset,
          totalSize = _ref5.totalSize;

      return totalSize <= containerSize ? 0 : offset / (totalSize - containerSize);
    }
  }, {
    key: '_offsetToSafeOffset',
    value: function _offsetToSafeOffset(_ref6) {
      var containerSize = _ref6.containerSize,
          offset = _ref6.offset;

      var totalSize = this._cellSizeAndPositionManager.getTotalSize();
      var safeTotalSize = this.getTotalSize();

      if (totalSize === safeTotalSize) {
        return offset;
      } else {
        var offsetPercentage = this._getOffsetPercentage({
          containerSize: containerSize,
          offset: offset,
          totalSize: totalSize
        });

        return Math.round(offsetPercentage * (safeTotalSize - containerSize));
      }
    }
  }, {
    key: '_safeOffsetToOffset',
    value: function _safeOffsetToOffset(_ref7) {
      var containerSize = _ref7.containerSize,
          offset = _ref7.offset;

      var totalSize = this._cellSizeAndPositionManager.getTotalSize();
      var safeTotalSize = this.getTotalSize();

      if (totalSize === safeTotalSize) {
        return offset;
      } else {
        var offsetPercentage = this._getOffsetPercentage({
          containerSize: containerSize,
          offset: offset,
          totalSize: safeTotalSize
        });

        return Math.round(offsetPercentage * (totalSize - containerSize));
      }
    }
  }]);

  return ScalingCellSizeAndPositionManager;
}();

/* harmony default export */ __webpack_exports__["a"] = (ScalingCellSizeAndPositionManager);

/***/ }),
/* 65 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

exports.default = function (obj, keys) {
  var target = {};

  for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;
    if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;
    target[i] = obj[i];
  }

  return target;
};

/***/ }),
/* 66 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createCallbackMemoizer;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys__ = __webpack_require__(95);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys__);

/**
 * Helper utility that updates the specified callback whenever any of the specified indices have changed.
 */
function createCallbackMemoizer() {
  var requireAllKeys = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;

  var cachedIndices = {};

  return function (_ref) {
    var callback = _ref.callback,
        indices = _ref.indices;

    var keys = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default()(indices);
    var allInitialized = !requireAllKeys || keys.every(function (key) {
      var value = indices[key];
      return Array.isArray(value) ? value.length > 0 : value >= 0;
    });
    var indexChanged = keys.length !== __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default()(cachedIndices).length || keys.some(function (key) {
      var cachedValue = cachedIndices[key];
      var value = indices[key];

      return Array.isArray(value) ? cachedValue.join(',') !== value.join(',') : cachedValue !== value;
    });

    cachedIndices = indices;

    if (allInitialized && indexChanged) {
      callback(indices);
    }
  };
}

/***/ }),
/* 67 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_react__);


var babelPluginFlowReactPropTypes_proptype_RowRendererParams =  false ? null : {
  index: __webpack_require__(0).number.isRequired,
  isScrolling: __webpack_require__(0).bool.isRequired,
  isVisible: __webpack_require__(0).bool.isRequired,
  key: __webpack_require__(0).string.isRequired,
  parent: __webpack_require__(0).object.isRequired,
  style: __webpack_require__(0).object.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_RowRendererParams', {
  value: babelPluginFlowReactPropTypes_proptype_RowRendererParams,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_RowRenderer =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_RowRenderer', {
  value: babelPluginFlowReactPropTypes_proptype_RowRenderer,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_RenderedRows =  false ? null : {
  overscanStartIndex: __webpack_require__(0).number.isRequired,
  overscanStopIndex: __webpack_require__(0).number.isRequired,
  startIndex: __webpack_require__(0).number.isRequired,
  stopIndex: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_RenderedRows', {
  value: babelPluginFlowReactPropTypes_proptype_RenderedRows,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_Scroll =  false ? null : {
  clientHeight: __webpack_require__(0).number.isRequired,
  scrollHeight: __webpack_require__(0).number.isRequired,
  scrollTop: __webpack_require__(0).number.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_Scroll', {
  value: babelPluginFlowReactPropTypes_proptype_Scroll,
  configurable: true
});

/***/ }),
/* 68 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DEFAULT_SCROLLING_RESET_TIME_INTERVAL", function() { return DEFAULT_SCROLLING_RESET_TIME_INTERVAL; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_classnames__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__PositionCache__ = __webpack_require__(244);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__utils_requestAnimationTimeout__ = __webpack_require__(28);











var babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId = __webpack_require__(28).babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId || __webpack_require__(0).any;

var emptyObject = {};

/**
 * Specifies the number of miliseconds during which to disable pointer events while a scroll is in progress.
 * This improves performance and makes scrolling smoother.
 */
var DEFAULT_SCROLLING_RESET_TIME_INTERVAL = 150;

/**
 * This component efficiently displays arbitrarily positioned cells using windowing techniques.
 * Cell position is determined by an injected `cellPositioner` property.
 * Windowing is vertical; this component does not support horizontal scrolling.
 *
 * Rendering occurs in two phases:
 * 1) First pass uses estimated cell sizes (provided by the cache) to determine how many cells to measure in a batch.
 *    Batch size is chosen using a fast, naive layout algorithm that stacks images in order until the viewport has been filled.
 *    After measurement is complete (componentDidMount or componentDidUpdate) this component evaluates positioned cells
 *    in order to determine if another measurement pass is required (eg if actual cell sizes were less than estimated sizes).
 *    All measurements are permanently cached (keyed by `keyMapper`) for performance purposes.
 * 2) Second pass uses the external `cellPositioner` to layout cells.
 *    At this time the positioner has access to cached size measurements for all cells.
 *    The positions it returns are cached by Masonry for fast access later.
 *    Phase one is repeated if the user scrolls beyond the current layout's bounds.
 *    If the layout is invalidated due to eg a resize, cached positions can be cleared using `recomputeCellPositions()`.
 *
 * Animation constraints:
 *   Simple animations are supported (eg translate/slide into place on initial reveal).
 *   More complex animations are not (eg flying from one position to another on resize).
 *
 * Layout constraints:
 *   This component supports multi-column layout.
 *   The height of each item may vary.
 *   The width of each item must not exceed the width of the column it is "in".
 *   The left position of all items within a column must align.
 *   (Items may not span multiple columns.)
 */

var Masonry = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(Masonry, _PureComponent);

  function Masonry(props, context) {
    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, Masonry);

    var _this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (Masonry.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(Masonry)).call(this, props, context));

    _this._invalidateOnUpdateStartIndex = null;
    _this._invalidateOnUpdateStopIndex = null;
    _this._positionCache = new __WEBPACK_IMPORTED_MODULE_8__PositionCache__["a" /* default */]();
    _this._startIndex = null;
    _this._startIndexMemoized = null;
    _this._stopIndex = null;
    _this._stopIndexMemoized = null;


    _this.state = {
      isScrolling: false,
      scrollTop: 0
    };

    _this._debounceResetIsScrollingCallback = _this._debounceResetIsScrollingCallback.bind(_this);
    _this._setScrollingContainerRef = _this._setScrollingContainerRef.bind(_this);
    _this._onScroll = _this._onScroll.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(Masonry, [{
    key: 'clearCellPositions',
    value: function clearCellPositions() {
      this._positionCache = new __WEBPACK_IMPORTED_MODULE_8__PositionCache__["a" /* default */]();
      this.forceUpdate();
    }

    // HACK This method signature was intended for Grid

  }, {
    key: 'invalidateCellSizeAfterRender',
    value: function invalidateCellSizeAfterRender(_ref) {
      var index = _ref.rowIndex;

      if (this._invalidateOnUpdateStartIndex === null) {
        this._invalidateOnUpdateStartIndex = index;
        this._invalidateOnUpdateStopIndex = index;
      } else {
        this._invalidateOnUpdateStartIndex = Math.min(this._invalidateOnUpdateStartIndex, index);
        this._invalidateOnUpdateStopIndex = Math.max(this._invalidateOnUpdateStopIndex, index);
      }
    }
  }, {
    key: 'recomputeCellPositions',
    value: function recomputeCellPositions() {
      var stopIndex = this._positionCache.count - 1;

      this._positionCache = new __WEBPACK_IMPORTED_MODULE_8__PositionCache__["a" /* default */]();
      this._populatePositionCache(0, stopIndex);

      this.forceUpdate();
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      this._checkInvalidateOnUpdate();
      this._invokeOnScrollCallback();
      this._invokeOnCellsRenderedCallback();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      this._checkInvalidateOnUpdate();
      this._invokeOnScrollCallback();
      this._invokeOnCellsRenderedCallback();
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      if (this._debounceResetIsScrollingId) {
        Object(__WEBPACK_IMPORTED_MODULE_9__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(this._debounceResetIsScrollingId);
      }
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      var scrollTop = this.props.scrollTop;


      if (scrollTop !== nextProps.scrollTop) {
        this._debounceResetIsScrolling();

        this.setState({
          isScrolling: true,
          scrollTop: nextProps.scrollTop
        });
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _this2 = this;

      var _props = this.props,
          autoHeight = _props.autoHeight,
          cellCount = _props.cellCount,
          cellMeasurerCache = _props.cellMeasurerCache,
          cellRenderer = _props.cellRenderer,
          className = _props.className,
          height = _props.height,
          id = _props.id,
          keyMapper = _props.keyMapper,
          overscanByPixels = _props.overscanByPixels,
          role = _props.role,
          style = _props.style,
          tabIndex = _props.tabIndex,
          width = _props.width;
      var _state = this.state,
          isScrolling = _state.isScrolling,
          scrollTop = _state.scrollTop;


      var children = [];

      var estimateTotalHeight = this._getEstimatedTotalHeight();

      var shortestColumnSize = this._positionCache.shortestColumnSize;
      var measuredCellCount = this._positionCache.count;

      var startIndex = 0;
      var stopIndex = void 0;

      this._positionCache.range(Math.max(0, scrollTop - overscanByPixels), height + overscanByPixels * 2, function (index, left, top) {
        if (typeof stopIndex === 'undefined') {
          startIndex = index;
          stopIndex = index;
        } else {
          startIndex = Math.min(startIndex, index);
          stopIndex = Math.max(stopIndex, index);
        }

        children.push(cellRenderer({
          index: index,
          isScrolling: isScrolling,
          key: keyMapper(index),
          parent: _this2,
          style: {
            height: cellMeasurerCache.getHeight(index),
            left: left,
            position: 'absolute',
            top: top,
            width: cellMeasurerCache.getWidth(index)
          }
        }));
      });

      // We need to measure additional cells for this layout
      if (shortestColumnSize < scrollTop + height + overscanByPixels && measuredCellCount < cellCount) {
        var batchSize = Math.min(cellCount - measuredCellCount, Math.ceil((scrollTop + height + overscanByPixels - shortestColumnSize) / cellMeasurerCache.defaultHeight * width / cellMeasurerCache.defaultWidth));

        for (var _index = measuredCellCount; _index < measuredCellCount + batchSize; _index++) {
          stopIndex = _index;

          children.push(cellRenderer({
            index: _index,
            isScrolling: isScrolling,
            key: keyMapper(_index),
            parent: this,
            style: {
              width: cellMeasurerCache.getWidth(_index)
            }
          }));
        }
      }

      this._startIndex = startIndex;
      this._stopIndex = stopIndex;

      return __WEBPACK_IMPORTED_MODULE_6_react___default.a.createElement(
        'div',
        {
          ref: this._setScrollingContainerRef,
          'aria-label': this.props['aria-label'],
          className: __WEBPACK_IMPORTED_MODULE_7_classnames___default()('ReactVirtualized__Masonry', className),
          id: id,
          onScroll: this._onScroll,
          role: role,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
            boxSizing: 'border-box',
            direction: 'ltr',
            height: autoHeight ? 'auto' : height,
            overflowX: 'hidden',
            overflowY: estimateTotalHeight < height ? 'hidden' : 'auto',
            position: 'relative',
            width: width,
            WebkitOverflowScrolling: 'touch',
            willChange: 'transform'
          }, style),
          tabIndex: tabIndex },
        __WEBPACK_IMPORTED_MODULE_6_react___default.a.createElement(
          'div',
          {
            className: 'ReactVirtualized__Masonry__innerScrollContainer',
            style: {
              width: '100%',
              height: estimateTotalHeight,
              maxWidth: '100%',
              maxHeight: estimateTotalHeight,
              overflow: 'hidden',
              pointerEvents: isScrolling ? 'none' : '',
              position: 'relative'
            } },
          children
        )
      );
    }
  }, {
    key: '_checkInvalidateOnUpdate',
    value: function _checkInvalidateOnUpdate() {
      if (typeof this._invalidateOnUpdateStartIndex === 'number') {
        var _startIndex = this._invalidateOnUpdateStartIndex;
        var _stopIndex = this._invalidateOnUpdateStopIndex;

        this._invalidateOnUpdateStartIndex = null;
        this._invalidateOnUpdateStopIndex = null;

        // Query external layout logic for position of newly-measured cells
        this._populatePositionCache(_startIndex, _stopIndex);

        this.forceUpdate();
      }
    }
  }, {
    key: '_debounceResetIsScrolling',
    value: function _debounceResetIsScrolling() {
      var scrollingResetTimeInterval = this.props.scrollingResetTimeInterval;


      if (this._debounceResetIsScrollingId) {
        Object(__WEBPACK_IMPORTED_MODULE_9__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(this._debounceResetIsScrollingId);
      }

      this._debounceResetIsScrollingId = Object(__WEBPACK_IMPORTED_MODULE_9__utils_requestAnimationTimeout__["requestAnimationTimeout"])(this._debounceResetIsScrollingCallback, scrollingResetTimeInterval);
    }
  }, {
    key: '_debounceResetIsScrollingCallback',
    value: function _debounceResetIsScrollingCallback() {
      this.setState({
        isScrolling: false
      });
    }
  }, {
    key: '_getEstimatedTotalHeight',
    value: function _getEstimatedTotalHeight() {
      var _props2 = this.props,
          cellCount = _props2.cellCount,
          cellMeasurerCache = _props2.cellMeasurerCache,
          width = _props2.width;


      var estimatedColumnCount = Math.max(1, Math.floor(width / cellMeasurerCache.defaultWidth));

      return this._positionCache.estimateTotalHeight(cellCount, estimatedColumnCount, cellMeasurerCache.defaultHeight);
    }
  }, {
    key: '_invokeOnScrollCallback',
    value: function _invokeOnScrollCallback() {
      var _props3 = this.props,
          height = _props3.height,
          onScroll = _props3.onScroll;
      var scrollTop = this.state.scrollTop;


      if (this._onScrollMemoized !== scrollTop) {
        onScroll({
          clientHeight: height,
          scrollHeight: this._getEstimatedTotalHeight(),
          scrollTop: scrollTop
        });

        this._onScrollMemoized = scrollTop;
      }
    }
  }, {
    key: '_invokeOnCellsRenderedCallback',
    value: function _invokeOnCellsRenderedCallback() {
      if (this._startIndexMemoized !== this._startIndex || this._stopIndexMemoized !== this._stopIndex) {
        var _onCellsRendered = this.props.onCellsRendered;


        _onCellsRendered({
          startIndex: this._startIndex,
          stopIndex: this._stopIndex
        });

        this._startIndexMemoized = this._startIndex;
        this._stopIndexMemoized = this._stopIndex;
      }
    }
  }, {
    key: '_populatePositionCache',
    value: function _populatePositionCache(startIndex, stopIndex) {
      var _props4 = this.props,
          cellMeasurerCache = _props4.cellMeasurerCache,
          cellPositioner = _props4.cellPositioner;


      for (var _index2 = startIndex; _index2 <= stopIndex; _index2++) {
        var _cellPositioner = cellPositioner(_index2),
            _left = _cellPositioner.left,
            _top = _cellPositioner.top;

        this._positionCache.setPosition(_index2, _left, _top, cellMeasurerCache.getHeight(_index2));
      }
    }
  }, {
    key: '_setScrollingContainerRef',
    value: function _setScrollingContainerRef(ref) {
      this._scrollingContainer = ref;
    }
  }, {
    key: '_onScroll',
    value: function _onScroll(event) {
      var height = this.props.height;


      var eventScrollTop = event.target.scrollTop;

      // When this component is shrunk drastically, React dispatches a series of back-to-back scroll events,
      // Gradually converging on a scrollTop that is within the bounds of the new, smaller height.
      // This causes a series of rapid renders that is slow for long lists.
      // We can avoid that by doing some simple bounds checking to ensure that scroll offsets never exceed their bounds.
      var scrollTop = Math.min(Math.max(0, this._getEstimatedTotalHeight() - height), eventScrollTop);

      // On iOS, we can arrive at negative offsets by swiping past the start or end.
      // Avoid re-rendering in this case as it can cause problems; see #532 for more.
      if (eventScrollTop !== scrollTop) {
        return;
      }

      // Prevent pointer events from interrupting a smooth scroll
      this._debounceResetIsScrolling();

      // Certain devices (like Apple touchpad) rapid-fire duplicate events.
      // Don't force a re-render if this is the case.
      // The mouse may move faster then the animation frame does.
      // Use requestAnimationFrame to avoid over-updating.
      if (this.state.scrollTop !== scrollTop) {
        this.setState({
          isScrolling: true,
          scrollTop: scrollTop
        });
      }
    }
  }]);

  return Masonry;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

Masonry.defaultProps = {
  autoHeight: false,
  keyMapper: identity,
  onCellsRendered: noop,
  onScroll: noop,
  overscanByPixels: 20,
  role: 'grid',
  scrollingResetTimeInterval: DEFAULT_SCROLLING_RESET_TIME_INTERVAL,
  style: emptyObject,
  tabIndex: 0
};
Masonry.propTypes =  false ? null : {
  autoHeight: __webpack_require__(0).bool.isRequired,
  cellCount: __webpack_require__(0).number.isRequired,
  cellMeasurerCache: typeof CellMeasurerCache === 'function' ? __webpack_require__(0).instanceOf(CellMeasurerCache).isRequired : __webpack_require__(0).any.isRequired,
  cellPositioner: typeof Positioner === 'function' ? __webpack_require__(0).instanceOf(Positioner).isRequired : __webpack_require__(0).any.isRequired,
  cellRenderer: typeof CellRenderer === 'function' ? __webpack_require__(0).instanceOf(CellRenderer).isRequired : __webpack_require__(0).any.isRequired,
  className: __webpack_require__(0).string,
  height: __webpack_require__(0).number.isRequired,
  id: __webpack_require__(0).string,
  keyMapper: typeof KeyMapper === 'function' ? __webpack_require__(0).instanceOf(KeyMapper).isRequired : __webpack_require__(0).any.isRequired,
  onCellsRendered: typeof OnCellsRenderedCallback === 'function' ? __webpack_require__(0).instanceOf(OnCellsRenderedCallback) : __webpack_require__(0).any,
  onScroll: typeof OnScrollCallback === 'function' ? __webpack_require__(0).instanceOf(OnScrollCallback) : __webpack_require__(0).any,
  overscanByPixels: __webpack_require__(0).number.isRequired,
  role: __webpack_require__(0).string.isRequired,
  scrollingResetTimeInterval: __webpack_require__(0).number.isRequired,
  style: function style(props, propName, componentName) {
    if (!Object.prototype.hasOwnProperty.call(props, propName)) {
      throw new Error('Prop `' + propName + '` has type \'any\' or \'mixed\', but was not provided to `' + componentName + '`. Pass undefined or any other value.');
    }
  },
  tabIndex: __webpack_require__(0).number.isRequired,
  width: __webpack_require__(0).number.isRequired
};
/* harmony default export */ __webpack_exports__["default"] = (Masonry);


function identity(value) {
  return value;
}

function noop() {}

var babelPluginFlowReactPropTypes_proptype_CellMeasurerCache =  false ? null : {
  defaultHeight: __webpack_require__(0).number.isRequired,
  defaultWidth: __webpack_require__(0).number.isRequired,
  getHeight: __webpack_require__(0).func.isRequired,
  getWidth: __webpack_require__(0).func.isRequired
};
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_CellMeasurerCache', {
  value: babelPluginFlowReactPropTypes_proptype_CellMeasurerCache,
  configurable: true
});
var babelPluginFlowReactPropTypes_proptype_Positioner =  false ? null : __webpack_require__(0).func;
if (!("development" === 'production') && typeof exports !== 'undefined') Object.defineProperty(exports, 'babelPluginFlowReactPropTypes_proptype_Positioner', {
  value: babelPluginFlowReactPropTypes_proptype_Positioner,
  configurable: true
});

/***/ }),
/* 69 */
/***/ (function(module, exports) {

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

module.exports = isObject;


/***/ }),
/* 70 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyObject = {};

if (true) {
  Object.freeze(emptyObject);
}

module.exports = emptyObject;

/***/ }),
/* 71 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),
/* 72 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return subscriptionShape; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return storeShape; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_prop_types__);


var subscriptionShape = __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.shape({
  trySubscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  tryUnsubscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  notifyNestedSubs: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  isSubscribed: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired
});

var storeShape = __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.shape({
  subscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  dispatch: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  getState: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired
});

/***/ }),
/* 73 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = connectAdvanced;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics__ = __webpack_require__(131);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(132);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__utils_Subscription__ = __webpack_require__(133);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__ = __webpack_require__(72);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }








var hotReloadingVersion = 0;
var dummyState = {};
function noop() {}
function makeSelectorStateful(sourceSelector, store) {
  // wrap the selector in an object that tracks its results between runs.
  var selector = {
    run: function runComponentSelector(props) {
      try {
        var nextProps = sourceSelector(store.getState(), props);
        if (nextProps !== selector.props || selector.error) {
          selector.shouldComponentUpdate = true;
          selector.props = nextProps;
          selector.error = null;
        }
      } catch (error) {
        selector.shouldComponentUpdate = true;
        selector.error = error;
      }
    }
  };

  return selector;
}

function connectAdvanced(
/*
  selectorFactory is a func that is responsible for returning the selector function used to
  compute new props from state, props, and dispatch. For example:
     export default connectAdvanced((dispatch, options) => (state, props) => ({
      thing: state.things[props.thingId],
      saveThing: fields => dispatch(actionCreators.saveThing(props.thingId, fields)),
    }))(YourComponent)
   Access to dispatch is provided to the factory so selectorFactories can bind actionCreators
  outside of their selector as an optimization. Options passed to connectAdvanced are passed to
  the selectorFactory, along with displayName and WrappedComponent, as the second argument.
   Note that selectorFactory is responsible for all caching/memoization of inbound and outbound
  props. Do not use connectAdvanced directly without memoizing results between calls to your
  selector, otherwise the Connect component will re-render on every state or props change.
*/
selectorFactory) {
  var _contextTypes, _childContextTypes;

  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
      _ref$getDisplayName = _ref.getDisplayName,
      getDisplayName = _ref$getDisplayName === undefined ? function (name) {
    return 'ConnectAdvanced(' + name + ')';
  } : _ref$getDisplayName,
      _ref$methodName = _ref.methodName,
      methodName = _ref$methodName === undefined ? 'connectAdvanced' : _ref$methodName,
      _ref$renderCountProp = _ref.renderCountProp,
      renderCountProp = _ref$renderCountProp === undefined ? undefined : _ref$renderCountProp,
      _ref$shouldHandleStat = _ref.shouldHandleStateChanges,
      shouldHandleStateChanges = _ref$shouldHandleStat === undefined ? true : _ref$shouldHandleStat,
      _ref$storeKey = _ref.storeKey,
      storeKey = _ref$storeKey === undefined ? 'store' : _ref$storeKey,
      _ref$withRef = _ref.withRef,
      withRef = _ref$withRef === undefined ? false : _ref$withRef,
      connectOptions = _objectWithoutProperties(_ref, ['getDisplayName', 'methodName', 'renderCountProp', 'shouldHandleStateChanges', 'storeKey', 'withRef']);

  var subscriptionKey = storeKey + 'Subscription';
  var version = hotReloadingVersion++;

  var contextTypes = (_contextTypes = {}, _contextTypes[storeKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["a" /* storeShape */], _contextTypes[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["b" /* subscriptionShape */], _contextTypes);
  var childContextTypes = (_childContextTypes = {}, _childContextTypes[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["b" /* subscriptionShape */], _childContextTypes);

  return function wrapWithConnect(WrappedComponent) {
    __WEBPACK_IMPORTED_MODULE_1_invariant___default()(typeof WrappedComponent == 'function', 'You must pass a component to the function returned by ' + (methodName + '. Instead received ' + JSON.stringify(WrappedComponent)));

    var wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || 'Component';

    var displayName = getDisplayName(wrappedComponentName);

    var selectorFactoryOptions = _extends({}, connectOptions, {
      getDisplayName: getDisplayName,
      methodName: methodName,
      renderCountProp: renderCountProp,
      shouldHandleStateChanges: shouldHandleStateChanges,
      storeKey: storeKey,
      withRef: withRef,
      displayName: displayName,
      wrappedComponentName: wrappedComponentName,
      WrappedComponent: WrappedComponent
    });

    var Connect = function (_Component) {
      _inherits(Connect, _Component);

      function Connect(props, context) {
        _classCallCheck(this, Connect);

        var _this = _possibleConstructorReturn(this, _Component.call(this, props, context));

        _this.version = version;
        _this.state = {};
        _this.renderCount = 0;
        _this.store = props[storeKey] || context[storeKey];
        _this.propsMode = Boolean(props[storeKey]);
        _this.setWrappedInstance = _this.setWrappedInstance.bind(_this);

        __WEBPACK_IMPORTED_MODULE_1_invariant___default()(_this.store, 'Could not find "' + storeKey + '" in either the context or props of ' + ('"' + displayName + '". Either wrap the root component in a <Provider>, ') + ('or explicitly pass "' + storeKey + '" as a prop to "' + displayName + '".'));

        _this.initSelector();
        _this.initSubscription();
        return _this;
      }

      Connect.prototype.getChildContext = function getChildContext() {
        var _ref2;

        // If this component received store from props, its subscription should be transparent
        // to any descendants receiving store+subscription from context; it passes along
        // subscription passed to it. Otherwise, it shadows the parent subscription, which allows
        // Connect to control ordering of notifications to flow top-down.
        var subscription = this.propsMode ? null : this.subscription;
        return _ref2 = {}, _ref2[subscriptionKey] = subscription || this.context[subscriptionKey], _ref2;
      };

      Connect.prototype.componentDidMount = function componentDidMount() {
        if (!shouldHandleStateChanges) return;

        // componentWillMount fires during server side rendering, but componentDidMount and
        // componentWillUnmount do not. Because of this, trySubscribe happens during ...didMount.
        // Otherwise, unsubscription would never take place during SSR, causing a memory leak.
        // To handle the case where a child component may have triggered a state change by
        // dispatching an action in its componentWillMount, we have to re-run the select and maybe
        // re-render.
        this.subscription.trySubscribe();
        this.selector.run(this.props);
        if (this.selector.shouldComponentUpdate) this.forceUpdate();
      };

      Connect.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
        this.selector.run(nextProps);
      };

      Connect.prototype.shouldComponentUpdate = function shouldComponentUpdate() {
        return this.selector.shouldComponentUpdate;
      };

      Connect.prototype.componentWillUnmount = function componentWillUnmount() {
        if (this.subscription) this.subscription.tryUnsubscribe();
        this.subscription = null;
        this.notifyNestedSubs = noop;
        this.store = null;
        this.selector.run = noop;
        this.selector.shouldComponentUpdate = false;
      };

      Connect.prototype.getWrappedInstance = function getWrappedInstance() {
        __WEBPACK_IMPORTED_MODULE_1_invariant___default()(withRef, 'To access the wrapped instance, you need to specify ' + ('{ withRef: true } in the options argument of the ' + methodName + '() call.'));
        return this.wrappedInstance;
      };

      Connect.prototype.setWrappedInstance = function setWrappedInstance(ref) {
        this.wrappedInstance = ref;
      };

      Connect.prototype.initSelector = function initSelector() {
        var sourceSelector = selectorFactory(this.store.dispatch, selectorFactoryOptions);
        this.selector = makeSelectorStateful(sourceSelector, this.store);
        this.selector.run(this.props);
      };

      Connect.prototype.initSubscription = function initSubscription() {
        if (!shouldHandleStateChanges) return;

        // parentSub's source should match where store came from: props vs. context. A component
        // connected to the store via props shouldn't use subscription from context, or vice versa.
        var parentSub = (this.propsMode ? this.props : this.context)[subscriptionKey];
        this.subscription = new __WEBPACK_IMPORTED_MODULE_3__utils_Subscription__["a" /* default */](this.store, parentSub, this.onStateChange.bind(this));

        // `notifyNestedSubs` is duplicated to handle the case where the component is  unmounted in
        // the middle of the notification loop, where `this.subscription` will then be null. An
        // extra null check every change can be avoided by copying the method onto `this` and then
        // replacing it with a no-op on unmount. This can probably be avoided if Subscription's
        // listeners logic is changed to not call listeners that have been unsubscribed in the
        // middle of the notification loop.
        this.notifyNestedSubs = this.subscription.notifyNestedSubs.bind(this.subscription);
      };

      Connect.prototype.onStateChange = function onStateChange() {
        this.selector.run(this.props);

        if (!this.selector.shouldComponentUpdate) {
          this.notifyNestedSubs();
        } else {
          this.componentDidUpdate = this.notifyNestedSubsOnComponentDidUpdate;
          this.setState(dummyState);
        }
      };

      Connect.prototype.notifyNestedSubsOnComponentDidUpdate = function notifyNestedSubsOnComponentDidUpdate() {
        // `componentDidUpdate` is conditionally implemented when `onStateChange` determines it
        // needs to notify nested subs. Once called, it unimplements itself until further state
        // changes occur. Doing it this way vs having a permanent `componentDidUpdate` that does
        // a boolean check every time avoids an extra method call most of the time, resulting
        // in some perf boost.
        this.componentDidUpdate = undefined;
        this.notifyNestedSubs();
      };

      Connect.prototype.isSubscribed = function isSubscribed() {
        return Boolean(this.subscription) && this.subscription.isSubscribed();
      };

      Connect.prototype.addExtraProps = function addExtraProps(props) {
        if (!withRef && !renderCountProp && !(this.propsMode && this.subscription)) return props;
        // make a shallow copy so that fields added don't leak to the original selector.
        // this is especially important for 'ref' since that's a reference back to the component
        // instance. a singleton memoized selector would then be holding a reference to the
        // instance, preventing the instance from being garbage collected, and that would be bad
        var withExtras = _extends({}, props);
        if (withRef) withExtras.ref = this.setWrappedInstance;
        if (renderCountProp) withExtras[renderCountProp] = this.renderCount++;
        if (this.propsMode && this.subscription) withExtras[subscriptionKey] = this.subscription;
        return withExtras;
      };

      Connect.prototype.render = function render() {
        var selector = this.selector;
        selector.shouldComponentUpdate = false;

        if (selector.error) {
          throw selector.error;
        } else {
          return Object(__WEBPACK_IMPORTED_MODULE_2_react__["createElement"])(WrappedComponent, this.addExtraProps(selector.props));
        }
      };

      return Connect;
    }(__WEBPACK_IMPORTED_MODULE_2_react__["Component"]);

    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = displayName;
    Connect.childContextTypes = childContextTypes;
    Connect.contextTypes = contextTypes;
    Connect.propTypes = contextTypes;

    if (true) {
      Connect.prototype.componentWillUpdate = function componentWillUpdate() {
        var _this2 = this;

        // We are hot reloading!
        if (this.version !== version) {
          this.version = version;
          this.initSelector();

          // If any connected descendants don't hot reload (and resubscribe in the process), their
          // listeners will be lost when we unsubscribe. Unfortunately, by copying over all
          // listeners, this does mean that the old versions of connected descendants will still be
          // notified of state changes; however, their onStateChange function is a no-op so this
          // isn't a huge deal.
          var oldListeners = [];

          if (this.subscription) {
            oldListeners = this.subscription.listeners.get();
            this.subscription.tryUnsubscribe();
          }
          this.initSubscription();
          if (shouldHandleStateChanges) {
            this.subscription.trySubscribe();
            oldListeners.forEach(function (listener) {
              return _this2.subscription.listeners.subscribe(listener);
            });
          }
        }
      };
    }

    return __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics___default()(Connect, WrappedComponent);
  };
}

/***/ }),
/* 74 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = wrapMapToPropsConstant;
/* unused harmony export getDependsOnOwnProps */
/* harmony export (immutable) */ __webpack_exports__["b"] = wrapMapToPropsFunc;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__ = __webpack_require__(75);


function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch, options) {
    var constant = getConstant(dispatch, options);

    function constantSelector() {
      return constant;
    }
    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
}

// dependsOnOwnProps is used by createMapToPropsProxy to determine whether to pass props as args
// to the mapToProps function being wrapped. It is also used by makePurePropsSelector to determine
// whether mapToProps needs to be invoked when props have changed.
// 
// A length of one signals that mapToProps does not depend on props from the parent component.
// A length of zero is assumed to mean mapToProps is getting args via arguments or ...args and
// therefore not reporting its length accurately..
function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps !== null && mapToProps.dependsOnOwnProps !== undefined ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
}

// Used by whenMapStateToPropsIsFunction and whenMapDispatchToPropsIsFunction,
// this function wraps mapToProps in a proxy function which does several things:
// 
//  * Detects whether the mapToProps function being called depends on props, which
//    is used by selectorFactory to decide if it should reinvoke on props changes.
//    
//  * On first call, handles mapToProps if returns another function, and treats that
//    new function as the true mapToProps for subsequent calls.
//    
//  * On first call, verifies the first result is a plain object, in order to warn
//    the developer that their mapToProps function is not returning a valid result.
//    
function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, _ref) {
    var displayName = _ref.displayName;

    var proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch);
    };

    // allow detectFactoryAndVerify to get ownProps
    proxy.dependsOnOwnProps = true;

    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      var props = proxy(stateOrDispatch, ownProps);

      if (typeof props === 'function') {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }

      if (true) Object(__WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__["a" /* default */])(props, displayName, methodName);

      return props;
    };

    return proxy;
  };
}

/***/ }),
/* 75 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = verifyPlainObject;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__ = __webpack_require__(140);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__warning__ = __webpack_require__(42);



function verifyPlainObject(value, displayName, methodName) {
  if (!Object(__WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__["a" /* default */])(value)) {
    Object(__WEBPACK_IMPORTED_MODULE_1__warning__["a" /* default */])(methodName + '() in ' + displayName + ' must return a plain object. Instead received ' + value + '.');
  }
}

/***/ }),
/* 76 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__root_js__ = __webpack_require__(142);


/** Built-in value references. */
var Symbol = __WEBPACK_IMPORTED_MODULE_0__root_js__["a" /* default */].Symbol;

/* harmony default export */ __webpack_exports__["a"] = (Symbol);


/***/ }),
/* 77 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


module.exports = function bind(fn, thisArg) {
  return function wrap() {
    var args = new Array(arguments.length);
    for (var i = 0; i < args.length; i++) {
      args[i] = arguments[i];
    }
    return fn.apply(thisArg, args);
  };
};


/***/ }),
/* 78 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);
var settle = __webpack_require__(159);
var buildURL = __webpack_require__(161);
var parseHeaders = __webpack_require__(162);
var isURLSameOrigin = __webpack_require__(163);
var createError = __webpack_require__(79);
var btoa = (typeof window !== 'undefined' && window.btoa && window.btoa.bind(window)) || __webpack_require__(164);

module.exports = function xhrAdapter(config) {
  return new Promise(function dispatchXhrRequest(resolve, reject) {
    var requestData = config.data;
    var requestHeaders = config.headers;

    if (utils.isFormData(requestData)) {
      delete requestHeaders['Content-Type']; // Let the browser set it
    }

    var request = new XMLHttpRequest();
    var loadEvent = 'onreadystatechange';
    var xDomain = false;

    // For IE 8/9 CORS support
    // Only supports POST and GET calls and doesn't returns the response headers.
    // DON'T do this for testing b/c XMLHttpRequest is mocked, not XDomainRequest.
    if ("development" !== 'test' &&
        typeof window !== 'undefined' &&
        window.XDomainRequest && !('withCredentials' in request) &&
        !isURLSameOrigin(config.url)) {
      request = new window.XDomainRequest();
      loadEvent = 'onload';
      xDomain = true;
      request.onprogress = function handleProgress() {};
      request.ontimeout = function handleTimeout() {};
    }

    // HTTP basic authentication
    if (config.auth) {
      var username = config.auth.username || '';
      var password = config.auth.password || '';
      requestHeaders.Authorization = 'Basic ' + btoa(username + ':' + password);
    }

    request.open(config.method.toUpperCase(), buildURL(config.url, config.params, config.paramsSerializer), true);

    // Set the request timeout in MS
    request.timeout = config.timeout;

    // Listen for ready state
    request[loadEvent] = function handleLoad() {
      if (!request || (request.readyState !== 4 && !xDomain)) {
        return;
      }

      // The request errored out and we didn't get a response, this will be
      // handled by onerror instead
      // With one exception: request that using file: protocol, most browsers
      // will return status as 0 even though it's a successful request
      if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {
        return;
      }

      // Prepare the response
      var responseHeaders = 'getAllResponseHeaders' in request ? parseHeaders(request.getAllResponseHeaders()) : null;
      var responseData = !config.responseType || config.responseType === 'text' ? request.responseText : request.response;
      var response = {
        data: responseData,
        // IE sends 1223 instead of 204 (https://github.com/axios/axios/issues/201)
        status: request.status === 1223 ? 204 : request.status,
        statusText: request.status === 1223 ? 'No Content' : request.statusText,
        headers: responseHeaders,
        config: config,
        request: request
      };

      settle(resolve, reject, response);

      // Clean up request
      request = null;
    };

    // Handle low level network errors
    request.onerror = function handleError() {
      // Real errors are hidden from us by the browser
      // onerror should only fire if it's a network error
      reject(createError('Network Error', config, null, request));

      // Clean up request
      request = null;
    };

    // Handle timeout
    request.ontimeout = function handleTimeout() {
      reject(createError('timeout of ' + config.timeout + 'ms exceeded', config, 'ECONNABORTED',
        request));

      // Clean up request
      request = null;
    };

    // Add xsrf header
    // This is only done if running in a standard browser environment.
    // Specifically not if we're in a web worker, or react-native.
    if (utils.isStandardBrowserEnv()) {
      var cookies = __webpack_require__(165);

      // Add xsrf header
      var xsrfValue = (config.withCredentials || isURLSameOrigin(config.url)) && config.xsrfCookieName ?
          cookies.read(config.xsrfCookieName) :
          undefined;

      if (xsrfValue) {
        requestHeaders[config.xsrfHeaderName] = xsrfValue;
      }
    }

    // Add headers to the request
    if ('setRequestHeader' in request) {
      utils.forEach(requestHeaders, function setRequestHeader(val, key) {
        if (typeof requestData === 'undefined' && key.toLowerCase() === 'content-type') {
          // Remove Content-Type if data is undefined
          delete requestHeaders[key];
        } else {
          // Otherwise add header to the request
          request.setRequestHeader(key, val);
        }
      });
    }

    // Add withCredentials to request if needed
    if (config.withCredentials) {
      request.withCredentials = true;
    }

    // Add responseType to request if needed
    if (config.responseType) {
      try {
        request.responseType = config.responseType;
      } catch (e) {
        // Expected DOMException thrown by browsers not compatible XMLHttpRequest Level 2.
        // But, this can be suppressed for 'json' type as it can be parsed by default 'transformResponse' function.
        if (config.responseType !== 'json') {
          throw e;
        }
      }
    }

    // Handle progress if needed
    if (typeof config.onDownloadProgress === 'function') {
      request.addEventListener('progress', config.onDownloadProgress);
    }

    // Not all browsers support upload events
    if (typeof config.onUploadProgress === 'function' && request.upload) {
      request.upload.addEventListener('progress', config.onUploadProgress);
    }

    if (config.cancelToken) {
      // Handle cancellation
      config.cancelToken.promise.then(function onCanceled(cancel) {
        if (!request) {
          return;
        }

        request.abort();
        reject(cancel);
        // Clean up request
        request = null;
      });
    }

    if (requestData === undefined) {
      requestData = null;
    }

    // Send the request
    request.send(requestData);
  });
};


/***/ }),
/* 79 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var enhanceError = __webpack_require__(160);

/**
 * Create an Error with the specified message, config, error code, request and response.
 *
 * @param {string} message The error message.
 * @param {Object} config The config.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 * @returns {Error} The created error.
 */
module.exports = function createError(message, config, code, request, response) {
  var error = new Error(message);
  return enhanceError(error, config, code, request, response);
};


/***/ }),
/* 80 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


module.exports = function isCancel(value) {
  return !!(value && value.__CANCEL__);
};


/***/ }),
/* 81 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * A `Cancel` is an object that is thrown when an operation is canceled.
 *
 * @class
 * @param {string=} message The message.
 */
function Cancel(message) {
  this.message = message;
}

Cancel.prototype.toString = function toString() {
  return 'Cancel' + (this.message ? ': ' + this.message : '');
};

Cancel.prototype.__CANCEL__ = true;

module.exports = Cancel;


/***/ }),
/* 82 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.setResponse = exports.setActiveItem = undefined;

var _axios = __webpack_require__(44);

var _axios2 = _interopRequireDefault(_axios);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var setActiveItem = exports.setActiveItem = function setActiveItem(key) {
  return {
    type: 'SET_ACTIVE_ITEM',
    payload: {
      key: key
    }
  };
};

var setResponse = exports.setResponse = function setResponse(key, value) {
  return {
    type: 'SET_RESPONSE',
    payload: {
      key: key,
      value: value
    }
  };
};

/***/ }),
/* 83 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_react__);






var babelPluginFlowReactPropTypes_proptype_RenderedSection = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_RenderedSection || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_ScrollIndices = __webpack_require__(224).babelPluginFlowReactPropTypes_proptype_ScrollIndices || __webpack_require__(0).any;



/**
 * This HOC decorates a virtualized component and responds to arrow-key events by scrolling one row or column at a time.
 */

var ArrowKeyStepper = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default()(ArrowKeyStepper, _React$PureComponent);

  function ArrowKeyStepper(props) {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, ArrowKeyStepper);

    var _this = __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(this, (ArrowKeyStepper.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(ArrowKeyStepper)).call(this, props));

    _this._columnStartIndex = 0;
    _this._columnStopIndex = 0;
    _this._rowStartIndex = 0;
    _this._rowStopIndex = 0;

    _this._onKeyDown = function (event) {
      var _this$props = _this.props,
          columnCount = _this$props.columnCount,
          disabled = _this$props.disabled,
          mode = _this$props.mode,
          rowCount = _this$props.rowCount;


      if (disabled) {
        return;
      }

      var _this$_getScrollState = _this._getScrollState(),
          scrollToColumnPrevious = _this$_getScrollState.scrollToColumn,
          scrollToRowPrevious = _this$_getScrollState.scrollToRow;

      var _this$_getScrollState2 = _this._getScrollState(),
          scrollToColumn = _this$_getScrollState2.scrollToColumn,
          scrollToRow = _this$_getScrollState2.scrollToRow;

      // The above cases all prevent default event event behavior.
      // This is to keep the grid from scrolling after the snap-to update.


      switch (event.key) {
        case 'ArrowDown':
          scrollToRow = mode === 'cells' ? Math.min(scrollToRow + 1, rowCount - 1) : Math.min(_this._rowStopIndex + 1, rowCount - 1);
          break;
        case 'ArrowLeft':
          scrollToColumn = mode === 'cells' ? Math.max(scrollToColumn - 1, 0) : Math.max(_this._columnStartIndex - 1, 0);
          break;
        case 'ArrowRight':
          scrollToColumn = mode === 'cells' ? Math.min(scrollToColumn + 1, columnCount - 1) : Math.min(_this._columnStopIndex + 1, columnCount - 1);
          break;
        case 'ArrowUp':
          scrollToRow = mode === 'cells' ? Math.max(scrollToRow - 1, 0) : Math.max(_this._rowStartIndex - 1, 0);
          break;
      }

      if (scrollToColumn !== scrollToColumnPrevious || scrollToRow !== scrollToRowPrevious) {
        event.preventDefault();

        _this._updateScrollState({ scrollToColumn: scrollToColumn, scrollToRow: scrollToRow });
      }
    };

    _this._onSectionRendered = function (_ref) {
      var columnStartIndex = _ref.columnStartIndex,
          columnStopIndex = _ref.columnStopIndex,
          rowStartIndex = _ref.rowStartIndex,
          rowStopIndex = _ref.rowStopIndex;

      _this._columnStartIndex = columnStartIndex;
      _this._columnStopIndex = columnStopIndex;
      _this._rowStartIndex = rowStartIndex;
      _this._rowStopIndex = rowStopIndex;
    };

    _this.state = {
      scrollToColumn: props.scrollToColumn,
      scrollToRow: props.scrollToRow
    };
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(ArrowKeyStepper, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      if (this.props.isControlled) {
        return;
      }

      var scrollToColumn = nextProps.scrollToColumn,
          scrollToRow = nextProps.scrollToRow;
      var _props = this.props,
          prevScrollToColumn = _props.scrollToColumn,
          prevScrollToRow = _props.scrollToRow;


      if (prevScrollToColumn !== scrollToColumn && prevScrollToRow !== scrollToRow) {
        this.setState({
          scrollToColumn: scrollToColumn,
          scrollToRow: scrollToRow
        });
      } else if (prevScrollToColumn !== scrollToColumn) {
        this.setState({ scrollToColumn: scrollToColumn });
      } else if (prevScrollToRow !== scrollToRow) {
        this.setState({ scrollToRow: scrollToRow });
      }
    }
  }, {
    key: 'setScrollIndexes',
    value: function setScrollIndexes(_ref2) {
      var scrollToColumn = _ref2.scrollToColumn,
          scrollToRow = _ref2.scrollToRow;

      this.setState({
        scrollToRow: scrollToRow,
        scrollToColumn: scrollToColumn
      });
    }
  }, {
    key: 'render',
    value: function render() {
      var _props2 = this.props,
          className = _props2.className,
          children = _props2.children;

      var _getScrollState2 = this._getScrollState(),
          scrollToColumn = _getScrollState2.scrollToColumn,
          scrollToRow = _getScrollState2.scrollToRow;

      return __WEBPACK_IMPORTED_MODULE_5_react__["createElement"](
        'div',
        { className: className, onKeyDown: this._onKeyDown },
        children({
          onSectionRendered: this._onSectionRendered,
          scrollToColumn: scrollToColumn,
          scrollToRow: scrollToRow
        })
      );
    }
  }, {
    key: '_getScrollState',
    value: function _getScrollState() {
      return this.props.isControlled ? this.props : this.state;
    }
  }, {
    key: '_updateScrollState',
    value: function _updateScrollState(_ref3) {
      var scrollToColumn = _ref3.scrollToColumn,
          scrollToRow = _ref3.scrollToRow;
      var _props3 = this.props,
          isControlled = _props3.isControlled,
          onScrollToChange = _props3.onScrollToChange;


      if (typeof onScrollToChange === 'function') {
        onScrollToChange({ scrollToColumn: scrollToColumn, scrollToRow: scrollToRow });
      }

      if (!isControlled) {
        this.setState({ scrollToColumn: scrollToColumn, scrollToRow: scrollToRow });
      }
    }
  }]);

  return ArrowKeyStepper;
}(__WEBPACK_IMPORTED_MODULE_5_react__["PureComponent"]);

ArrowKeyStepper.defaultProps = {
  disabled: false,
  isControlled: false,
  mode: 'edges',
  scrollToColumn: 0,
  scrollToRow: 0
};
ArrowKeyStepper.propTypes =  false ? null : {
  children: __webpack_require__(0).func.isRequired,
  className: __webpack_require__(0).string,
  columnCount: __webpack_require__(0).number.isRequired,
  disabled: __webpack_require__(0).bool.isRequired,
  isControlled: __webpack_require__(0).bool.isRequired,
  mode: __webpack_require__(0).oneOf(['cells', 'edges']).isRequired,
  onScrollToChange: __webpack_require__(0).func,
  rowCount: __webpack_require__(0).number.isRequired,
  scrollToColumn: __webpack_require__(0).number.isRequired,
  scrollToRow: __webpack_require__(0).number.isRequired
};
/* harmony default export */ __webpack_exports__["a"] = (ArrowKeyStepper);

/***/ }),
/* 84 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.9 / 15.2.3.2 Object.getPrototypeOf(O)
var has = __webpack_require__(14);
var toObject = __webpack_require__(34);
var IE_PROTO = __webpack_require__(48)('IE_PROTO');
var ObjectProto = Object.prototype;

module.exports = Object.getPrototypeOf || function (O) {
  O = toObject(O);
  if (has(O, IE_PROTO)) return O[IE_PROTO];
  if (typeof O.constructor == 'function' && O instanceof O.constructor) {
    return O.constructor.prototype;
  } return O instanceof Object ? ObjectProto : null;
};


/***/ }),
/* 85 */
/***/ (function(module, exports, __webpack_require__) {

// optional / simple context binding
var aFunction = __webpack_require__(178);
module.exports = function (fn, that, length) {
  aFunction(fn);
  if (that === undefined) return fn;
  switch (length) {
    case 1: return function (a) {
      return fn.call(that, a);
    };
    case 2: return function (a, b) {
      return fn.call(that, a, b);
    };
    case 3: return function (a, b, c) {
      return fn.call(that, a, b, c);
    };
  }
  return function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),
/* 86 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = !__webpack_require__(17) && !__webpack_require__(22)(function () {
  return Object.defineProperty(__webpack_require__(87)('div'), 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),
/* 87 */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(21);
var document = __webpack_require__(13).document;
// typeof document.createElement is 'object' in old IE
var is = isObject(document) && isObject(document.createElement);
module.exports = function (it) {
  return is ? document.createElement(it) : {};
};


/***/ }),
/* 88 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _iterator = __webpack_require__(182);

var _iterator2 = _interopRequireDefault(_iterator);

var _symbol = __webpack_require__(194);

var _symbol2 = _interopRequireDefault(_symbol);

var _typeof = typeof _symbol2.default === "function" && typeof _iterator2.default === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof _symbol2.default === "function" && obj.constructor === _symbol2.default && obj !== _symbol2.default.prototype ? "symbol" : typeof obj; };

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = typeof _symbol2.default === "function" && _typeof(_iterator2.default) === "symbol" ? function (obj) {
  return typeof obj === "undefined" ? "undefined" : _typeof(obj);
} : function (obj) {
  return obj && typeof _symbol2.default === "function" && obj.constructor === _symbol2.default && obj !== _symbol2.default.prototype ? "symbol" : typeof obj === "undefined" ? "undefined" : _typeof(obj);
};

/***/ }),
/* 89 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var LIBRARY = __webpack_require__(54);
var $export = __webpack_require__(15);
var redefine = __webpack_require__(90);
var hide = __webpack_require__(19);
var Iterators = __webpack_require__(26);
var $iterCreate = __webpack_require__(185);
var setToStringTag = __webpack_require__(58);
var getPrototypeOf = __webpack_require__(84);
var ITERATOR = __webpack_require__(12)('iterator');
var BUGGY = !([].keys && 'next' in [].keys()); // Safari has buggy iterators w/o `next`
var FF_ITERATOR = '@@iterator';
var KEYS = 'keys';
var VALUES = 'values';

var returnThis = function () { return this; };

module.exports = function (Base, NAME, Constructor, next, DEFAULT, IS_SET, FORCED) {
  $iterCreate(Constructor, NAME, next);
  var getMethod = function (kind) {
    if (!BUGGY && kind in proto) return proto[kind];
    switch (kind) {
      case KEYS: return function keys() { return new Constructor(this, kind); };
      case VALUES: return function values() { return new Constructor(this, kind); };
    } return function entries() { return new Constructor(this, kind); };
  };
  var TAG = NAME + ' Iterator';
  var DEF_VALUES = DEFAULT == VALUES;
  var VALUES_BUG = false;
  var proto = Base.prototype;
  var $native = proto[ITERATOR] || proto[FF_ITERATOR] || DEFAULT && proto[DEFAULT];
  var $default = $native || getMethod(DEFAULT);
  var $entries = DEFAULT ? !DEF_VALUES ? $default : getMethod('entries') : undefined;
  var $anyNative = NAME == 'Array' ? proto.entries || $native : $native;
  var methods, key, IteratorPrototype;
  // Fix native
  if ($anyNative) {
    IteratorPrototype = getPrototypeOf($anyNative.call(new Base()));
    if (IteratorPrototype !== Object.prototype && IteratorPrototype.next) {
      // Set @@toStringTag to native iterators
      setToStringTag(IteratorPrototype, TAG, true);
      // fix for some old engines
      if (!LIBRARY && typeof IteratorPrototype[ITERATOR] != 'function') hide(IteratorPrototype, ITERATOR, returnThis);
    }
  }
  // fix Array#{values, @@iterator}.name in V8 / FF
  if (DEF_VALUES && $native && $native.name !== VALUES) {
    VALUES_BUG = true;
    $default = function values() { return $native.call(this); };
  }
  // Define iterator
  if ((!LIBRARY || FORCED) && (BUGGY || VALUES_BUG || !proto[ITERATOR])) {
    hide(proto, ITERATOR, $default);
  }
  // Plug for library
  Iterators[NAME] = $default;
  Iterators[TAG] = returnThis;
  if (DEFAULT) {
    methods = {
      values: DEF_VALUES ? $default : getMethod(VALUES),
      keys: IS_SET ? $default : getMethod(KEYS),
      entries: $entries
    };
    if (FORCED) for (key in methods) {
      if (!(key in proto)) redefine(proto, key, methods[key]);
    } else $export($export.P + $export.F * (BUGGY || VALUES_BUG), NAME, methods);
  }
  return methods;
};


/***/ }),
/* 90 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(19);


/***/ }),
/* 91 */
/***/ (function(module, exports, __webpack_require__) {

var has = __webpack_require__(14);
var toIObject = __webpack_require__(18);
var arrayIndexOf = __webpack_require__(187)(false);
var IE_PROTO = __webpack_require__(48)('IE_PROTO');

module.exports = function (object, names) {
  var O = toIObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) if (key != IE_PROTO) has(O, key) && result.push(key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (has(O, key = names[i++])) {
    ~arrayIndexOf(result, key) || result.push(key);
  }
  return result;
};


/***/ }),
/* 92 */
/***/ (function(module, exports, __webpack_require__) {

// fallback for non-array-like ES3 and non-enumerable old V8 strings
var cof = __webpack_require__(56);
// eslint-disable-next-line no-prototype-builtins
module.exports = Object('z').propertyIsEnumerable(0) ? Object : function (it) {
  return cof(it) == 'String' ? it.split('') : Object(it);
};


/***/ }),
/* 93 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.7 / 15.2.3.4 Object.getOwnPropertyNames(O)
var $keys = __webpack_require__(91);
var hiddenKeys = __webpack_require__(57).concat('length', 'prototype');

exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
  return $keys(O, hiddenKeys);
};


/***/ }),
/* 94 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export DEFAULT_SCROLLING_RESET_TIME_INTERVAL */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_classnames__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__utils_calculateSizeAndPositionDataAndUpdateScrollOffset__ = __webpack_require__(218);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__utils_ScalingCellSizeAndPositionManager__ = __webpack_require__(64);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__utils_createCallbackMemoizer__ = __webpack_require__(66);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__ = __webpack_require__(96);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_12__utils_updateScrollIndexHelper__ = __webpack_require__(221);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_13__defaultCellRangeRenderer__ = __webpack_require__(97);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_14_dom_helpers_util_scrollbarSize__ = __webpack_require__(98);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_14_dom_helpers_util_scrollbarSize___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_14_dom_helpers_util_scrollbarSize__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_15__utils_requestAnimationTimeout__ = __webpack_require__(28);







var babelPluginFlowReactPropTypes_proptype_Alignment = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_Alignment || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_RenderedSection = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_RenderedSection || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_ScrollbarPresenceChange = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_ScrollbarPresenceChange || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Scroll = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_Scroll || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_NoContentRenderer = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_NoContentRenderer || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellSizeGetter = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellSizeGetter || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellSize = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellSize || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellPosition = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellPosition || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellRangeRenderer = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellRangeRenderer || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellRenderer = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellRenderer || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId = __webpack_require__(28).babelPluginFlowReactPropTypes_proptype_AnimationTimeoutId || __webpack_require__(0).any;












/**
 * Specifies the number of milliseconds during which to disable pointer events while a scroll is in progress.
 * This improves performance and makes scrolling smoother.
 */
var DEFAULT_SCROLLING_RESET_TIME_INTERVAL = 150;

/**
 * Controls whether the Grid updates the DOM element's scrollLeft/scrollTop based on the current state or just observes it.
 * This prevents Grid from interrupting mouse-wheel animations (see issue #2).
 */
var SCROLL_POSITION_CHANGE_REASONS = {
  OBSERVED: 'observed',
  REQUESTED: 'requested'
};

var renderNull = function renderNull() {
  return null;
};

/**
 * Renders tabular data with virtualization along the vertical and horizontal axes.
 * Row heights and column widths must be known ahead of time and specified as properties.
 */
var Grid = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(Grid, _React$PureComponent);

  // Invokes onSectionRendered callback only when start/stop row or column indices change
  function Grid(props) {
    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, Grid);

    var _this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (Grid.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(Grid)).call(this, props));

    _this.state = {
      isScrolling: false,
      scrollDirectionHorizontal: __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */],
      scrollDirectionVertical: __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */],
      scrollLeft: 0,
      scrollTop: 0,
      scrollPositionChangeReason: null
    };
    _this._onGridRenderedMemoizer = Object(__WEBPACK_IMPORTED_MODULE_10__utils_createCallbackMemoizer__["a" /* default */])();
    _this._onScrollMemoizer = Object(__WEBPACK_IMPORTED_MODULE_10__utils_createCallbackMemoizer__["a" /* default */])(false);
    _this._deferredInvalidateColumnIndex = null;
    _this._deferredInvalidateRowIndex = null;
    _this._recomputeScrollLeftFlag = false;
    _this._recomputeScrollTopFlag = false;
    _this._horizontalScrollBarSize = 0;
    _this._verticalScrollBarSize = 0;
    _this._scrollbarPresenceChanged = false;
    _this._cellCache = {};
    _this._styleCache = {};
    _this._scrollbarSizeMeasured = false;
    _this._renderedColumnStartIndex = 0;
    _this._renderedColumnStopIndex = 0;
    _this._renderedRowStartIndex = 0;
    _this._renderedRowStopIndex = 0;

    _this._debounceScrollEndedCallback = function () {
      _this._disablePointerEventsTimeoutId = null;
      _this._resetStyleCache();
    };

    _this._invokeOnGridRenderedHelper = function () {
      var onSectionRendered = _this.props.onSectionRendered;


      _this._onGridRenderedMemoizer({
        callback: onSectionRendered,
        indices: {
          columnOverscanStartIndex: _this._columnStartIndex,
          columnOverscanStopIndex: _this._columnStopIndex,
          columnStartIndex: _this._renderedColumnStartIndex,
          columnStopIndex: _this._renderedColumnStopIndex,
          rowOverscanStartIndex: _this._rowStartIndex,
          rowOverscanStopIndex: _this._rowStopIndex,
          rowStartIndex: _this._renderedRowStartIndex,
          rowStopIndex: _this._renderedRowStopIndex
        }
      });
    };

    _this._setScrollingContainerRef = function (ref) {
      _this._scrollingContainer = ref;
    };

    _this._onScroll = function (event) {
      // In certain edge-cases React dispatches an onScroll event with an invalid target.scrollLeft / target.scrollTop.
      // This invalid event can be detected by comparing event.target to this component's scrollable DOM element.
      // See issue #404 for more information.
      if (event.target === _this._scrollingContainer) {
        _this.handleScrollEvent(event.target);
      }
    };

    _this._columnWidthGetter = _this._wrapSizeGetter(props.columnWidth);
    _this._rowHeightGetter = _this._wrapSizeGetter(props.rowHeight);

    _this._columnSizeAndPositionManager = new __WEBPACK_IMPORTED_MODULE_9__utils_ScalingCellSizeAndPositionManager__["a" /* default */]({
      cellCount: props.columnCount,
      cellSizeGetter: function cellSizeGetter(params) {
        return _this._columnWidthGetter(params);
      },
      estimatedCellSize: _this._getEstimatedColumnSize(props)
    });
    _this._rowSizeAndPositionManager = new __WEBPACK_IMPORTED_MODULE_9__utils_ScalingCellSizeAndPositionManager__["a" /* default */]({
      cellCount: props.rowCount,
      cellSizeGetter: function cellSizeGetter(params) {
        return _this._rowHeightGetter(params);
      },
      estimatedCellSize: _this._getEstimatedRowSize(props)
    });
    return _this;
  }

  /**
   * Gets offsets for a given cell and alignment.
   */


  // See defaultCellRangeRenderer() for more information on the usage of these caches


  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(Grid, [{
    key: 'getOffsetForCell',
    value: function getOffsetForCell() {
      var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref$alignment = _ref.alignment,
          alignment = _ref$alignment === undefined ? this.props.scrollToAlignment : _ref$alignment,
          _ref$columnIndex = _ref.columnIndex,
          columnIndex = _ref$columnIndex === undefined ? this.props.scrollToColumn : _ref$columnIndex,
          _ref$rowIndex = _ref.rowIndex,
          rowIndex = _ref$rowIndex === undefined ? this.props.scrollToRow : _ref$rowIndex;

      var offsetProps = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, this.props, {
        scrollToAlignment: alignment,
        scrollToColumn: columnIndex,
        scrollToRow: rowIndex
      });

      return {
        scrollLeft: this._getCalculatedScrollLeft(offsetProps),
        scrollTop: this._getCalculatedScrollTop(offsetProps)
      };
    }

    /**
     * This method handles a scroll event originating from an external scroll control.
     * It's an advanced method and should probably not be used unless you're implementing a custom scroll-bar solution.
     */

  }, {
    key: 'handleScrollEvent',
    value: function handleScrollEvent(_ref2) {
      var _ref2$scrollLeft = _ref2.scrollLeft,
          scrollLeftParam = _ref2$scrollLeft === undefined ? 0 : _ref2$scrollLeft,
          _ref2$scrollTop = _ref2.scrollTop,
          scrollTopParam = _ref2$scrollTop === undefined ? 0 : _ref2$scrollTop;

      // On iOS, we can arrive at negative offsets by swiping past the start.
      // To prevent flicker here, we make playing in the negative offset zone cause nothing to happen.
      if (scrollTopParam < 0) {
        return;
      }

      // Prevent pointer events from interrupting a smooth scroll
      this._debounceScrollEnded();

      var _props = this.props,
          autoHeight = _props.autoHeight,
          autoWidth = _props.autoWidth,
          height = _props.height,
          width = _props.width;

      // When this component is shrunk drastically, React dispatches a series of back-to-back scroll events,
      // Gradually converging on a scrollTop that is within the bounds of the new, smaller height.
      // This causes a series of rapid renders that is slow for long lists.
      // We can avoid that by doing some simple bounds checking to ensure that scroll offsets never exceed their bounds.

      var scrollbarSize = this._scrollbarSize;
      var totalRowsHeight = this._rowSizeAndPositionManager.getTotalSize();
      var totalColumnsWidth = this._columnSizeAndPositionManager.getTotalSize();
      var scrollLeft = Math.min(Math.max(0, totalColumnsWidth - width + scrollbarSize), scrollLeftParam);
      var scrollTop = Math.min(Math.max(0, totalRowsHeight - height + scrollbarSize), scrollTopParam);

      // Certain devices (like Apple touchpad) rapid-fire duplicate events.
      // Don't force a re-render if this is the case.
      // The mouse may move faster then the animation frame does.
      // Use requestAnimationFrame to avoid over-updating.
      if (this.state.scrollLeft !== scrollLeft || this.state.scrollTop !== scrollTop) {
        // Track scrolling direction so we can more efficiently overscan rows to reduce empty space around the edges while scrolling.
        // Don't change direction for an axis unless scroll offset has changed.
        var _scrollDirectionHorizontal = scrollLeft !== this.state.scrollLeft ? scrollLeft > this.state.scrollLeft ? __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */] : __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["a" /* SCROLL_DIRECTION_BACKWARD */] : this.state.scrollDirectionHorizontal;
        var _scrollDirectionVertical = scrollTop !== this.state.scrollTop ? scrollTop > this.state.scrollTop ? __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */] : __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["a" /* SCROLL_DIRECTION_BACKWARD */] : this.state.scrollDirectionVertical;

        var newState = {
          isScrolling: true,
          scrollDirectionHorizontal: _scrollDirectionHorizontal,
          scrollDirectionVertical: _scrollDirectionVertical,
          scrollPositionChangeReason: SCROLL_POSITION_CHANGE_REASONS.OBSERVED
        };

        if (!autoHeight) {
          newState.scrollTop = scrollTop;
        }

        if (!autoWidth) {
          newState.scrollLeft = scrollLeft;
        }

        this.setState(newState);
      }

      this._invokeOnScrollMemoizer({
        scrollLeft: scrollLeft,
        scrollTop: scrollTop,
        totalColumnsWidth: totalColumnsWidth,
        totalRowsHeight: totalRowsHeight
      });
    }

    /**
     * Invalidate Grid size and recompute visible cells.
     * This is a deferred wrapper for recomputeGridSize().
     * It sets a flag to be evaluated on cDM/cDU to avoid unnecessary renders.
     * This method is intended for advanced use-cases like CellMeasurer.
     */
    // @TODO (bvaughn) Add automated test coverage for this.

  }, {
    key: 'invalidateCellSizeAfterRender',
    value: function invalidateCellSizeAfterRender(_ref3) {
      var columnIndex = _ref3.columnIndex,
          rowIndex = _ref3.rowIndex;

      this._deferredInvalidateColumnIndex = typeof this._deferredInvalidateColumnIndex === 'number' ? Math.min(this._deferredInvalidateColumnIndex, columnIndex) : columnIndex;
      this._deferredInvalidateRowIndex = typeof this._deferredInvalidateRowIndex === 'number' ? Math.min(this._deferredInvalidateRowIndex, rowIndex) : rowIndex;
    }

    /**
     * Pre-measure all columns and rows in a Grid.
     * Typically cells are only measured as needed and estimated sizes are used for cells that have not yet been measured.
     * This method ensures that the next call to getTotalSize() returns an exact size (as opposed to just an estimated one).
     */

  }, {
    key: 'measureAllCells',
    value: function measureAllCells() {
      var _props2 = this.props,
          columnCount = _props2.columnCount,
          rowCount = _props2.rowCount;


      this._columnSizeAndPositionManager.getSizeAndPositionOfCell(columnCount - 1);
      this._rowSizeAndPositionManager.getSizeAndPositionOfCell(rowCount - 1);
    }

    /**
     * Forced recompute of row heights and column widths.
     * This function should be called if dynamic column or row sizes have changed but nothing else has.
     * Since Grid only receives :columnCount and :rowCount it has no way of detecting when the underlying data changes.
     */

  }, {
    key: 'recomputeGridSize',
    value: function recomputeGridSize() {
      var _ref4 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref4$columnIndex = _ref4.columnIndex,
          columnIndex = _ref4$columnIndex === undefined ? 0 : _ref4$columnIndex,
          _ref4$rowIndex = _ref4.rowIndex,
          rowIndex = _ref4$rowIndex === undefined ? 0 : _ref4$rowIndex;

      var _props3 = this.props,
          scrollToColumn = _props3.scrollToColumn,
          scrollToRow = _props3.scrollToRow;


      this._columnSizeAndPositionManager.resetCell(columnIndex);
      this._rowSizeAndPositionManager.resetCell(rowIndex);

      // Cell sizes may be determined by a function property.
      // In this case the cDU handler can't know if they changed.
      // Store this flag to let the next cDU pass know it needs to recompute the scroll offset.
      this._recomputeScrollLeftFlag = scrollToColumn >= 0 && columnIndex <= scrollToColumn;
      this._recomputeScrollTopFlag = scrollToRow >= 0 && rowIndex <= scrollToRow;

      // Clear cell cache in case we are scrolling;
      // Invalid row heights likely mean invalid cached content as well.
      this._cellCache = {};
      this._styleCache = {};

      this.forceUpdate();
    }

    /**
     * Ensure column and row are visible.
     */

  }, {
    key: 'scrollToCell',
    value: function scrollToCell(_ref5) {
      var columnIndex = _ref5.columnIndex,
          rowIndex = _ref5.rowIndex;
      var columnCount = this.props.columnCount;


      var props = this.props;

      // Don't adjust scroll offset for single-column grids (eg List, Table).
      // This can cause a funky scroll offset because of the vertical scrollbar width.
      if (columnCount > 1 && columnIndex !== undefined) {
        this._updateScrollLeftForScrollToColumn(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
          scrollToColumn: columnIndex
        }));
      }

      if (rowIndex !== undefined) {
        this._updateScrollTopForScrollToRow(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
          scrollToRow: rowIndex
        }));
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      var _props4 = this.props,
          getScrollbarSize = _props4.getScrollbarSize,
          height = _props4.height,
          scrollLeft = _props4.scrollLeft,
          scrollToColumn = _props4.scrollToColumn,
          scrollTop = _props4.scrollTop,
          scrollToRow = _props4.scrollToRow,
          width = _props4.width;

      // If cell sizes have been invalidated (eg we are using CellMeasurer) then reset cached positions.
      // We must do this at the start of the method as we may calculate and update scroll position below.

      this._handleInvalidatedGridSize();

      // If this component was first rendered server-side, scrollbar size will be undefined.
      // In that event we need to remeasure.
      if (!this._scrollbarSizeMeasured) {
        this._scrollbarSize = getScrollbarSize();
        this._scrollbarSizeMeasured = true;
        this.setState({});
      }

      if (typeof scrollLeft === 'number' && scrollLeft >= 0 || typeof scrollTop === 'number' && scrollTop >= 0) {
        this.scrollToPosition({ scrollLeft: scrollLeft, scrollTop: scrollTop });
      }

      // Don't update scroll offset if the size is 0; we don't render any cells in this case.
      // Setting a state may cause us to later thing we've updated the offce when we haven't.
      var sizeIsBiggerThanZero = height > 0 && width > 0;
      if (scrollToColumn >= 0 && sizeIsBiggerThanZero) {
        this._updateScrollLeftForScrollToColumn();
      }
      if (scrollToRow >= 0 && sizeIsBiggerThanZero) {
        this._updateScrollTopForScrollToRow();
      }

      // Update onRowsRendered callback
      this._invokeOnGridRenderedHelper();

      // Initialize onScroll callback
      this._invokeOnScrollMemoizer({
        scrollLeft: scrollLeft || 0,
        scrollTop: scrollTop || 0,
        totalColumnsWidth: this._columnSizeAndPositionManager.getTotalSize(),
        totalRowsHeight: this._rowSizeAndPositionManager.getTotalSize()
      });

      this._maybeCallOnScrollbarPresenceChange();
    }

    /**
     * @private
     * This method updates scrollLeft/scrollTop in state for the following conditions:
     * 1) New scroll-to-cell props have been set
     */

  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate(prevProps, prevState) {
      var _this2 = this;

      var _props5 = this.props,
          autoHeight = _props5.autoHeight,
          autoWidth = _props5.autoWidth,
          columnCount = _props5.columnCount,
          height = _props5.height,
          rowCount = _props5.rowCount,
          scrollToAlignment = _props5.scrollToAlignment,
          scrollToColumn = _props5.scrollToColumn,
          scrollToRow = _props5.scrollToRow,
          width = _props5.width;
      var _state = this.state,
          scrollLeft = _state.scrollLeft,
          scrollPositionChangeReason = _state.scrollPositionChangeReason,
          scrollTop = _state.scrollTop;

      // If cell sizes have been invalidated (eg we are using CellMeasurer) then reset cached positions.
      // We must do this at the start of the method as we may calculate and update scroll position below.

      this._handleInvalidatedGridSize();

      // Handle edge case where column or row count has only just increased over 0.
      // In this case we may have to restore a previously-specified scroll offset.
      // For more info see bvaughn/react-virtualized/issues/218
      var columnOrRowCountJustIncreasedFromZero = columnCount > 0 && prevProps.columnCount === 0 || rowCount > 0 && prevProps.rowCount === 0;

      // Make sure requested changes to :scrollLeft or :scrollTop get applied.
      // Assigning to scrollLeft/scrollTop tells the browser to interrupt any running scroll animations,
      // And to discard any pending async changes to the scroll position that may have happened in the meantime (e.g. on a separate scrolling thread).
      // So we only set these when we require an adjustment of the scroll position.
      // See issue #2 for more information.
      if (scrollPositionChangeReason === SCROLL_POSITION_CHANGE_REASONS.REQUESTED) {
        // @TRICKY :autoHeight and :autoWidth properties instructs Grid to leave :scrollTop and :scrollLeft management to an external HOC (eg WindowScroller).
        // In this case we should avoid checking scrollingContainer.scrollTop and scrollingContainer.scrollLeft since it forces layout/flow.
        if (!autoWidth && scrollLeft >= 0 && (scrollLeft !== prevState.scrollLeft && scrollLeft !== this._scrollingContainer.scrollLeft || columnOrRowCountJustIncreasedFromZero)) {
          this._scrollingContainer.scrollLeft = scrollLeft;
        }
        if (!autoHeight && scrollTop >= 0 && (scrollTop !== prevState.scrollTop && scrollTop !== this._scrollingContainer.scrollTop || columnOrRowCountJustIncreasedFromZero)) {
          this._scrollingContainer.scrollTop = scrollTop;
        }
      }

      // Special case where the previous size was 0:
      // In this case we don't show any windowed cells at all.
      // So we should always recalculate offset afterwards.
      var sizeJustIncreasedFromZero = (prevProps.width === 0 || prevProps.height === 0) && height > 0 && width > 0;

      // Update scroll offsets if the current :scrollToColumn or :scrollToRow values requires it
      // @TODO Do we also need this check or can the one in componentWillUpdate() suffice?
      if (this._recomputeScrollLeftFlag) {
        this._recomputeScrollLeftFlag = false;
        this._updateScrollLeftForScrollToColumn(this.props);
      } else {
        Object(__WEBPACK_IMPORTED_MODULE_12__utils_updateScrollIndexHelper__["a" /* default */])({
          cellSizeAndPositionManager: this._columnSizeAndPositionManager,
          previousCellsCount: prevProps.columnCount,
          previousCellSize: prevProps.columnWidth,
          previousScrollToAlignment: prevProps.scrollToAlignment,
          previousScrollToIndex: prevProps.scrollToColumn,
          previousSize: prevProps.width,
          scrollOffset: scrollLeft,
          scrollToAlignment: scrollToAlignment,
          scrollToIndex: scrollToColumn,
          size: width,
          sizeJustIncreasedFromZero: sizeJustIncreasedFromZero,
          updateScrollIndexCallback: function updateScrollIndexCallback() {
            return _this2._updateScrollLeftForScrollToColumn(_this2.props);
          }
        });
      }

      if (this._recomputeScrollTopFlag) {
        this._recomputeScrollTopFlag = false;
        this._updateScrollTopForScrollToRow(this.props);
      } else {
        Object(__WEBPACK_IMPORTED_MODULE_12__utils_updateScrollIndexHelper__["a" /* default */])({
          cellSizeAndPositionManager: this._rowSizeAndPositionManager,
          previousCellsCount: prevProps.rowCount,
          previousCellSize: prevProps.rowHeight,
          previousScrollToAlignment: prevProps.scrollToAlignment,
          previousScrollToIndex: prevProps.scrollToRow,
          previousSize: prevProps.height,
          scrollOffset: scrollTop,
          scrollToAlignment: scrollToAlignment,
          scrollToIndex: scrollToRow,
          size: height,
          sizeJustIncreasedFromZero: sizeJustIncreasedFromZero,
          updateScrollIndexCallback: function updateScrollIndexCallback() {
            return _this2._updateScrollTopForScrollToRow(_this2.props);
          }
        });
      }

      // Update onRowsRendered callback if start/stop indices have changed
      this._invokeOnGridRenderedHelper();

      // Changes to :scrollLeft or :scrollTop should also notify :onScroll listeners
      if (scrollLeft !== prevState.scrollLeft || scrollTop !== prevState.scrollTop) {
        var totalRowsHeight = this._rowSizeAndPositionManager.getTotalSize();
        var totalColumnsWidth = this._columnSizeAndPositionManager.getTotalSize();

        this._invokeOnScrollMemoizer({
          scrollLeft: scrollLeft,
          scrollTop: scrollTop,
          totalColumnsWidth: totalColumnsWidth,
          totalRowsHeight: totalRowsHeight
        });
      }

      this._maybeCallOnScrollbarPresenceChange();
    }
  }, {
    key: 'componentWillMount',
    value: function componentWillMount() {
      var getScrollbarSize = this.props.getScrollbarSize;

      // If this component is being rendered server-side, getScrollbarSize() will return undefined.
      // We handle this case in componentDidMount()

      this._scrollbarSize = getScrollbarSize();
      if (this._scrollbarSize === undefined) {
        this._scrollbarSizeMeasured = false;
        this._scrollbarSize = 0;
      } else {
        this._scrollbarSizeMeasured = true;
      }

      this._calculateChildrenToRender();
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      if (this._disablePointerEventsTimeoutId) {
        Object(__WEBPACK_IMPORTED_MODULE_15__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(this._disablePointerEventsTimeoutId);
      }
    }

    /**
     * @private
     * This method updates scrollLeft/scrollTop in state for the following conditions:
     * 1) Empty content (0 rows or columns)
     * 2) New scroll props overriding the current state
     * 3) Cells-count or cells-size has changed, making previous scroll offsets invalid
     */

  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      var _this3 = this;

      var _state2 = this.state,
          scrollLeft = _state2.scrollLeft,
          scrollTop = _state2.scrollTop;


      if (nextProps.columnCount === 0 && scrollLeft !== 0 || nextProps.rowCount === 0 && scrollTop !== 0) {
        this.scrollToPosition({
          scrollLeft: 0,
          scrollTop: 0
        });
      } else if (nextProps.scrollLeft !== this.props.scrollLeft || nextProps.scrollTop !== this.props.scrollTop) {
        var newState = {};

        if (nextProps.scrollLeft != null) {
          newState.scrollLeft = nextProps.scrollLeft;
        }
        if (nextProps.scrollTop != null) {
          newState.scrollTop = nextProps.scrollTop;
        }

        this.scrollToPosition(newState);
      }

      if (nextProps.columnWidth !== this.props.columnWidth || nextProps.rowHeight !== this.props.rowHeight) {
        this._styleCache = {};
      }

      this._columnWidthGetter = this._wrapSizeGetter(nextProps.columnWidth);
      this._rowHeightGetter = this._wrapSizeGetter(nextProps.rowHeight);

      this._columnSizeAndPositionManager.configure({
        cellCount: nextProps.columnCount,
        estimatedCellSize: this._getEstimatedColumnSize(nextProps)
      });
      this._rowSizeAndPositionManager.configure({
        cellCount: nextProps.rowCount,
        estimatedCellSize: this._getEstimatedRowSize(nextProps)
      });

      var _props6 = this.props,
          columnCount = _props6.columnCount,
          rowCount = _props6.rowCount;

      // Special case when either cols or rows were 0
      // This would prevent any cells from rendering
      // So we need to reset row scroll if cols changed from 0 (and vice versa)

      if (columnCount === 0 || rowCount === 0) {
        columnCount = 0;
        rowCount = 0;
      }

      // If scrolling is controlled outside this component, clear cache when scrolling stops
      if (nextProps.autoHeight && nextProps.isScrolling === false && this.props.isScrolling === true) {
        this._resetStyleCache();
      }

      // Update scroll offsets if the size or number of cells have changed, invalidating the previous value
      Object(__WEBPACK_IMPORTED_MODULE_8__utils_calculateSizeAndPositionDataAndUpdateScrollOffset__["a" /* default */])({
        cellCount: columnCount,
        cellSize: typeof this.props.columnWidth === 'number' ? this.props.columnWidth : null,
        computeMetadataCallback: function computeMetadataCallback() {
          return _this3._columnSizeAndPositionManager.resetCell(0);
        },
        computeMetadataCallbackProps: nextProps,
        nextCellsCount: nextProps.columnCount,
        nextCellSize: typeof nextProps.columnWidth === 'number' ? nextProps.columnWidth : null,
        nextScrollToIndex: nextProps.scrollToColumn,
        scrollToIndex: this.props.scrollToColumn,
        updateScrollOffsetForScrollToIndex: function updateScrollOffsetForScrollToIndex() {
          return _this3._updateScrollLeftForScrollToColumn(nextProps, _this3.state);
        }
      });
      Object(__WEBPACK_IMPORTED_MODULE_8__utils_calculateSizeAndPositionDataAndUpdateScrollOffset__["a" /* default */])({
        cellCount: rowCount,
        cellSize: typeof this.props.rowHeight === 'number' ? this.props.rowHeight : null,
        computeMetadataCallback: function computeMetadataCallback() {
          return _this3._rowSizeAndPositionManager.resetCell(0);
        },
        computeMetadataCallbackProps: nextProps,
        nextCellsCount: nextProps.rowCount,
        nextCellSize: typeof nextProps.rowHeight === 'number' ? nextProps.rowHeight : null,
        nextScrollToIndex: nextProps.scrollToRow,
        scrollToIndex: this.props.scrollToRow,
        updateScrollOffsetForScrollToIndex: function updateScrollOffsetForScrollToIndex() {
          return _this3._updateScrollTopForScrollToRow(nextProps, _this3.state);
        }
      });
    }
  }, {
    key: 'componentWillUpdate',
    value: function componentWillUpdate(nextProps, nextState) {
      this._calculateChildrenToRender(nextProps, nextState);
    }
  }, {
    key: 'render',
    value: function render() {
      var _props7 = this.props,
          autoContainerWidth = _props7.autoContainerWidth,
          autoHeight = _props7.autoHeight,
          autoWidth = _props7.autoWidth,
          className = _props7.className,
          containerProps = _props7.containerProps,
          containerRole = _props7.containerRole,
          containerStyle = _props7.containerStyle,
          height = _props7.height,
          id = _props7.id,
          noContentRenderer = _props7.noContentRenderer,
          role = _props7.role,
          style = _props7.style,
          tabIndex = _props7.tabIndex,
          width = _props7.width;


      var isScrolling = this._isScrolling();

      var gridStyle = {
        boxSizing: 'border-box',
        direction: 'ltr',
        height: autoHeight ? 'auto' : height,
        position: 'relative',
        width: autoWidth ? 'auto' : width,
        WebkitOverflowScrolling: 'touch',
        willChange: 'transform'
      };

      var totalColumnsWidth = this._columnSizeAndPositionManager.getTotalSize();
      var totalRowsHeight = this._rowSizeAndPositionManager.getTotalSize();

      // Force browser to hide scrollbars when we know they aren't necessary.
      // Otherwise once scrollbars appear they may not disappear again.
      // For more info see issue #116
      var verticalScrollBarSize = totalRowsHeight > height ? this._scrollbarSize : 0;
      var horizontalScrollBarSize = totalColumnsWidth > width ? this._scrollbarSize : 0;

      if (horizontalScrollBarSize !== this._horizontalScrollBarSize || verticalScrollBarSize !== this._verticalScrollBarSize) {
        this._horizontalScrollBarSize = horizontalScrollBarSize;
        this._verticalScrollBarSize = verticalScrollBarSize;
        this._scrollbarPresenceChanged = true;
      }

      // Also explicitly init styles to 'auto' if scrollbars are required.
      // This works around an obscure edge case where external CSS styles have not yet been loaded,
      // But an initial scroll index of offset is set as an external prop.
      // Without this style, Grid would render the correct range of cells but would NOT update its internal offset.
      // This was originally reported via clauderic/react-infinite-calendar/issues/23
      gridStyle.overflowX = totalColumnsWidth + verticalScrollBarSize <= width ? 'hidden' : 'auto';
      gridStyle.overflowY = totalRowsHeight + horizontalScrollBarSize <= height ? 'hidden' : 'auto';

      var childrenToDisplay = this._childrenToDisplay;

      var showNoContentRenderer = childrenToDisplay.length === 0 && height > 0 && width > 0;

      return __WEBPACK_IMPORTED_MODULE_6_react__["createElement"](
        'div',
        __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          ref: this._setScrollingContainerRef
        }, containerProps, {
          'aria-label': this.props['aria-label'],
          'aria-readonly': this.props['aria-readonly'],
          className: __WEBPACK_IMPORTED_MODULE_7_classnames___default()('ReactVirtualized__Grid', className),
          id: id,
          onScroll: this._onScroll,
          role: role,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, gridStyle, style),
          tabIndex: tabIndex }),
        childrenToDisplay.length > 0 && __WEBPACK_IMPORTED_MODULE_6_react__["createElement"](
          'div',
          {
            className: 'ReactVirtualized__Grid__innerScrollContainer',
            role: containerRole,
            style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
              width: autoContainerWidth ? 'auto' : totalColumnsWidth,
              height: totalRowsHeight,
              maxWidth: totalColumnsWidth,
              maxHeight: totalRowsHeight,
              overflow: 'hidden',
              pointerEvents: isScrolling ? 'none' : '',
              position: 'relative'
            }, containerStyle) },
          childrenToDisplay
        ),
        showNoContentRenderer && noContentRenderer()
      );
    }

    /* ---------------------------- Helper methods ---------------------------- */

  }, {
    key: '_calculateChildrenToRender',
    value: function _calculateChildrenToRender() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;
      var cellRenderer = props.cellRenderer,
          cellRangeRenderer = props.cellRangeRenderer,
          columnCount = props.columnCount,
          deferredMeasurementCache = props.deferredMeasurementCache,
          height = props.height,
          overscanColumnCount = props.overscanColumnCount,
          overscanIndicesGetter = props.overscanIndicesGetter,
          overscanRowCount = props.overscanRowCount,
          rowCount = props.rowCount,
          width = props.width;
      var scrollDirectionHorizontal = state.scrollDirectionHorizontal,
          scrollDirectionVertical = state.scrollDirectionVertical,
          scrollLeft = state.scrollLeft,
          scrollTop = state.scrollTop;


      var isScrolling = this._isScrolling(props, state);

      this._childrenToDisplay = [];

      // Render only enough columns and rows to cover the visible area of the grid.
      if (height > 0 && width > 0) {
        var visibleColumnIndices = this._columnSizeAndPositionManager.getVisibleCellRange({
          containerSize: width,
          offset: scrollLeft
        });
        var visibleRowIndices = this._rowSizeAndPositionManager.getVisibleCellRange({
          containerSize: height,
          offset: scrollTop
        });

        var horizontalOffsetAdjustment = this._columnSizeAndPositionManager.getOffsetAdjustment({
          containerSize: width,
          offset: scrollLeft
        });
        var verticalOffsetAdjustment = this._rowSizeAndPositionManager.getOffsetAdjustment({
          containerSize: height,
          offset: scrollTop
        });

        // Store for _invokeOnGridRenderedHelper()
        this._renderedColumnStartIndex = visibleColumnIndices.start;
        this._renderedColumnStopIndex = visibleColumnIndices.stop;
        this._renderedRowStartIndex = visibleRowIndices.start;
        this._renderedRowStopIndex = visibleRowIndices.stop;

        var overscanColumnIndices = overscanIndicesGetter({
          direction: 'horizontal',
          cellCount: columnCount,
          overscanCellsCount: overscanColumnCount,
          scrollDirection: scrollDirectionHorizontal,
          startIndex: typeof visibleColumnIndices.start === 'number' ? visibleColumnIndices.start : 0,
          stopIndex: typeof visibleColumnIndices.stop === 'number' ? visibleColumnIndices.stop : -1
        });

        var overscanRowIndices = overscanIndicesGetter({
          direction: 'vertical',
          cellCount: rowCount,
          overscanCellsCount: overscanRowCount,
          scrollDirection: scrollDirectionVertical,
          startIndex: typeof visibleRowIndices.start === 'number' ? visibleRowIndices.start : 0,
          stopIndex: typeof visibleRowIndices.stop === 'number' ? visibleRowIndices.stop : -1
        });

        // Store for _invokeOnGridRenderedHelper()
        this._columnStartIndex = overscanColumnIndices.overscanStartIndex;
        this._columnStopIndex = overscanColumnIndices.overscanStopIndex;
        this._rowStartIndex = overscanRowIndices.overscanStartIndex;
        this._rowStopIndex = overscanRowIndices.overscanStopIndex;

        // Advanced use-cases (eg CellMeasurer) require batched measurements to determine accurate sizes.
        if (deferredMeasurementCache) {
          // If rows have a dynamic height, scan the rows we are about to render.
          // If any have not yet been measured, then we need to render all columns initially,
          // Because the height of the row is equal to the tallest cell within that row,
          // (And so we can't know the height without measuring all column-cells first).
          if (!deferredMeasurementCache.hasFixedHeight()) {
            for (var rowIndex = this._rowStartIndex; rowIndex <= this._rowStopIndex; rowIndex++) {
              if (!deferredMeasurementCache.has(rowIndex, 0)) {
                this._columnStartIndex = 0;
                this._columnStopIndex = columnCount - 1;
                break;
              }
            }
          }

          // If columns have a dynamic width, scan the columns we are about to render.
          // If any have not yet been measured, then we need to render all rows initially,
          // Because the width of the column is equal to the widest cell within that column,
          // (And so we can't know the width without measuring all row-cells first).
          if (!deferredMeasurementCache.hasFixedWidth()) {
            for (var columnIndex = this._columnStartIndex; columnIndex <= this._columnStopIndex; columnIndex++) {
              if (!deferredMeasurementCache.has(0, columnIndex)) {
                this._rowStartIndex = 0;
                this._rowStopIndex = rowCount - 1;
                break;
              }
            }
          }
        }

        this._childrenToDisplay = cellRangeRenderer({
          cellCache: this._cellCache,
          cellRenderer: cellRenderer,
          columnSizeAndPositionManager: this._columnSizeAndPositionManager,
          columnStartIndex: this._columnStartIndex,
          columnStopIndex: this._columnStopIndex,
          deferredMeasurementCache: deferredMeasurementCache,
          horizontalOffsetAdjustment: horizontalOffsetAdjustment,
          isScrolling: isScrolling,
          parent: this,
          rowSizeAndPositionManager: this._rowSizeAndPositionManager,
          rowStartIndex: this._rowStartIndex,
          rowStopIndex: this._rowStopIndex,
          scrollLeft: scrollLeft,
          scrollTop: scrollTop,
          styleCache: this._styleCache,
          verticalOffsetAdjustment: verticalOffsetAdjustment,
          visibleColumnIndices: visibleColumnIndices,
          visibleRowIndices: visibleRowIndices
        });
      }
    }

    /**
     * Sets an :isScrolling flag for a small window of time.
     * This flag is used to disable pointer events on the scrollable portion of the Grid.
     * This prevents jerky/stuttery mouse-wheel scrolling.
     */

  }, {
    key: '_debounceScrollEnded',
    value: function _debounceScrollEnded() {
      var scrollingResetTimeInterval = this.props.scrollingResetTimeInterval;


      if (this._disablePointerEventsTimeoutId) {
        Object(__WEBPACK_IMPORTED_MODULE_15__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(this._disablePointerEventsTimeoutId);
      }

      this._disablePointerEventsTimeoutId = Object(__WEBPACK_IMPORTED_MODULE_15__utils_requestAnimationTimeout__["requestAnimationTimeout"])(this._debounceScrollEndedCallback, scrollingResetTimeInterval);
    }
  }, {
    key: '_getEstimatedColumnSize',
    value: function _getEstimatedColumnSize(props) {
      return typeof props.columnWidth === 'number' ? props.columnWidth : props.estimatedColumnSize;
    }
  }, {
    key: '_getEstimatedRowSize',
    value: function _getEstimatedRowSize(props) {
      return typeof props.rowHeight === 'number' ? props.rowHeight : props.estimatedRowSize;
    }

    /**
     * Check for batched CellMeasurer size invalidations.
     * This will occur the first time one or more previously unmeasured cells are rendered.
     */

  }, {
    key: '_handleInvalidatedGridSize',
    value: function _handleInvalidatedGridSize() {
      if (typeof this._deferredInvalidateColumnIndex === 'number' && typeof this._deferredInvalidateRowIndex === 'number') {
        var columnIndex = this._deferredInvalidateColumnIndex;
        var rowIndex = this._deferredInvalidateRowIndex;

        this._deferredInvalidateColumnIndex = null;
        this._deferredInvalidateRowIndex = null;

        this.recomputeGridSize({ columnIndex: columnIndex, rowIndex: rowIndex });
      }
    }
  }, {
    key: '_invokeOnScrollMemoizer',
    value: function _invokeOnScrollMemoizer(_ref6) {
      var _this4 = this;

      var scrollLeft = _ref6.scrollLeft,
          scrollTop = _ref6.scrollTop,
          totalColumnsWidth = _ref6.totalColumnsWidth,
          totalRowsHeight = _ref6.totalRowsHeight;

      this._onScrollMemoizer({
        callback: function callback(_ref7) {
          var scrollLeft = _ref7.scrollLeft,
              scrollTop = _ref7.scrollTop;
          var _props8 = _this4.props,
              height = _props8.height,
              onScroll = _props8.onScroll,
              width = _props8.width;


          onScroll({
            clientHeight: height,
            clientWidth: width,
            scrollHeight: totalRowsHeight,
            scrollLeft: scrollLeft,
            scrollTop: scrollTop,
            scrollWidth: totalColumnsWidth
          });
        },
        indices: {
          scrollLeft: scrollLeft,
          scrollTop: scrollTop
        }
      });
    }
  }, {
    key: '_isScrolling',
    value: function _isScrolling() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;

      // If isScrolling is defined in props, use it to override the value in state
      // This is a performance optimization for WindowScroller + Grid
      return Object.hasOwnProperty.call(props, 'isScrolling') ? Boolean(props.isScrolling) : Boolean(state.isScrolling);
    }
  }, {
    key: '_maybeCallOnScrollbarPresenceChange',
    value: function _maybeCallOnScrollbarPresenceChange() {
      if (this._scrollbarPresenceChanged) {
        var _onScrollbarPresenceChange = this.props.onScrollbarPresenceChange;


        this._scrollbarPresenceChanged = false;

        _onScrollbarPresenceChange({
          horizontal: this._horizontalScrollBarSize > 0,
          size: this._scrollbarSize,
          vertical: this._verticalScrollBarSize > 0
        });
      }
    }
  }, {
    key: 'scrollToPosition',


    /**
     * Scroll to the specified offset(s).
     * Useful for animating position changes.
     */
    value: function scrollToPosition(_ref8) {
      var scrollLeft = _ref8.scrollLeft,
          scrollTop = _ref8.scrollTop;

      var newState = {
        scrollPositionChangeReason: SCROLL_POSITION_CHANGE_REASONS.REQUESTED
      };

      if (typeof scrollLeft === 'number' && scrollLeft >= 0) {
        newState.scrollDirectionHorizontal = scrollLeft > this.state.scrollLeft ? __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */] : __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["a" /* SCROLL_DIRECTION_BACKWARD */];
        newState.scrollLeft = scrollLeft;
      }

      if (typeof scrollTop === 'number' && scrollTop >= 0) {
        newState.scrollDirectionVertical = scrollTop > this.state.scrollTop ? __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["b" /* SCROLL_DIRECTION_FORWARD */] : __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["a" /* SCROLL_DIRECTION_BACKWARD */];
        newState.scrollTop = scrollTop;
      }

      if (typeof scrollLeft === 'number' && scrollLeft >= 0 && scrollLeft !== this.state.scrollLeft || typeof scrollTop === 'number' && scrollTop >= 0 && scrollTop !== this.state.scrollTop) {
        this.setState(newState);
      }
    }
  }, {
    key: '_wrapSizeGetter',
    value: function _wrapSizeGetter(value) {
      return typeof value === 'function' ? value : function () {
        return value;
      };
    }
  }, {
    key: '_getCalculatedScrollLeft',
    value: function _getCalculatedScrollLeft() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;
      var columnCount = props.columnCount,
          height = props.height,
          scrollToAlignment = props.scrollToAlignment,
          scrollToColumn = props.scrollToColumn,
          width = props.width;
      var scrollLeft = state.scrollLeft;


      if (columnCount > 0) {
        var finalColumn = columnCount - 1;
        var targetIndex = scrollToColumn < 0 ? finalColumn : Math.min(finalColumn, scrollToColumn);
        var totalRowsHeight = this._rowSizeAndPositionManager.getTotalSize();
        var scrollBarSize = totalRowsHeight > height ? this._scrollbarSize : 0;

        return this._columnSizeAndPositionManager.getUpdatedOffsetForIndex({
          align: scrollToAlignment,
          containerSize: width - scrollBarSize,
          currentOffset: scrollLeft,
          targetIndex: targetIndex
        });
      }
    }
  }, {
    key: '_updateScrollLeftForScrollToColumn',
    value: function _updateScrollLeftForScrollToColumn() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;
      var scrollLeft = state.scrollLeft;

      var calculatedScrollLeft = this._getCalculatedScrollLeft(props, state);

      if (typeof calculatedScrollLeft === 'number' && calculatedScrollLeft >= 0 && scrollLeft !== calculatedScrollLeft) {
        this.scrollToPosition({
          scrollLeft: calculatedScrollLeft,
          scrollTop: -1
        });
      }
    }
  }, {
    key: '_getCalculatedScrollTop',
    value: function _getCalculatedScrollTop() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;
      var height = props.height,
          rowCount = props.rowCount,
          scrollToAlignment = props.scrollToAlignment,
          scrollToRow = props.scrollToRow,
          width = props.width;
      var scrollTop = state.scrollTop;


      if (rowCount > 0) {
        var finalRow = rowCount - 1;
        var targetIndex = scrollToRow < 0 ? finalRow : Math.min(finalRow, scrollToRow);
        var totalColumnsWidth = this._columnSizeAndPositionManager.getTotalSize();
        var scrollBarSize = totalColumnsWidth > width ? this._scrollbarSize : 0;

        return this._rowSizeAndPositionManager.getUpdatedOffsetForIndex({
          align: scrollToAlignment,
          containerSize: height - scrollBarSize,
          currentOffset: scrollTop,
          targetIndex: targetIndex
        });
      }
    }
  }, {
    key: '_resetStyleCache',
    value: function _resetStyleCache() {
      var styleCache = this._styleCache;

      // Reset cell and style caches once scrolling stops.
      // This makes Grid simpler to use (since cells commonly change).
      // And it keeps the caches from growing too large.
      // Performance is most sensitive when a user is scrolling.
      this._cellCache = {};
      this._styleCache = {};

      // Copy over the visible cell styles so avoid unnecessary re-render.
      for (var rowIndex = this._rowStartIndex; rowIndex <= this._rowStopIndex; rowIndex++) {
        for (var columnIndex = this._columnStartIndex; columnIndex <= this._columnStopIndex; columnIndex++) {
          var key = rowIndex + '-' + columnIndex;
          this._styleCache[key] = styleCache[key];
        }
      }

      this.setState({
        isScrolling: false
      });
    }
  }, {
    key: '_updateScrollTopForScrollToRow',
    value: function _updateScrollTopForScrollToRow() {
      var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props;
      var state = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.state;
      var scrollTop = state.scrollTop;

      var calculatedScrollTop = this._getCalculatedScrollTop(props, state);

      if (typeof calculatedScrollTop === 'number' && calculatedScrollTop >= 0 && scrollTop !== calculatedScrollTop) {
        this.scrollToPosition({
          scrollLeft: -1,
          scrollTop: calculatedScrollTop
        });
      }
    }
  }]);

  return Grid;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

Grid.defaultProps = {
  'aria-label': 'grid',
  'aria-readonly': true,
  autoContainerWidth: false,
  autoHeight: false,
  autoWidth: false,
  cellRangeRenderer: __WEBPACK_IMPORTED_MODULE_13__defaultCellRangeRenderer__["a" /* default */],
  containerRole: 'rowgroup',
  containerStyle: {},
  estimatedColumnSize: 100,
  estimatedRowSize: 30,
  getScrollbarSize: __WEBPACK_IMPORTED_MODULE_14_dom_helpers_util_scrollbarSize___default.a,
  noContentRenderer: renderNull,
  onScroll: function onScroll() {},
  onScrollbarPresenceChange: function onScrollbarPresenceChange() {},
  onSectionRendered: function onSectionRendered() {},
  overscanColumnCount: 0,
  overscanIndicesGetter: __WEBPACK_IMPORTED_MODULE_11__defaultOverscanIndicesGetter__["c" /* default */],
  overscanRowCount: 10,
  role: 'grid',
  scrollingResetTimeInterval: DEFAULT_SCROLLING_RESET_TIME_INTERVAL,
  scrollToAlignment: 'auto',
  scrollToColumn: -1,
  scrollToRow: -1,
  style: {},
  tabIndex: 0
};
Grid.propTypes =  false ? null : {
  "aria-label": __webpack_require__(0).string.isRequired,
  "aria-readonly": __webpack_require__(0).bool,


  /**
   * Set the width of the inner scrollable container to 'auto'.
   * This is useful for single-column Grids to ensure that the column doesn't extend below a vertical scrollbar.
   */
  autoContainerWidth: __webpack_require__(0).bool.isRequired,


  /**
   * Removes fixed height from the scrollingContainer so that the total height of rows can stretch the window.
   * Intended for use with WindowScroller
   */
  autoHeight: __webpack_require__(0).bool.isRequired,


  /**
   * Removes fixed width from the scrollingContainer so that the total width of rows can stretch the window.
   * Intended for use with WindowScroller
   */
  autoWidth: __webpack_require__(0).bool.isRequired,


  /** Responsible for rendering a cell given an row and column index.  */
  cellRenderer: typeof babelPluginFlowReactPropTypes_proptype_CellRenderer === 'function' ? babelPluginFlowReactPropTypes_proptype_CellRenderer.isRequired ? babelPluginFlowReactPropTypes_proptype_CellRenderer.isRequired : babelPluginFlowReactPropTypes_proptype_CellRenderer : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_CellRenderer).isRequired,


  /** Responsible for rendering a group of cells given their index ranges.  */
  cellRangeRenderer: typeof babelPluginFlowReactPropTypes_proptype_CellRangeRenderer === 'function' ? babelPluginFlowReactPropTypes_proptype_CellRangeRenderer.isRequired ? babelPluginFlowReactPropTypes_proptype_CellRangeRenderer.isRequired : babelPluginFlowReactPropTypes_proptype_CellRangeRenderer : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_CellRangeRenderer).isRequired,


  /** Optional custom CSS class name to attach to root Grid element.  */
  className: __webpack_require__(0).string,


  /** Number of columns in grid.  */
  columnCount: __webpack_require__(0).number.isRequired,


  /** Either a fixed column width (number) or a function that returns the width of a column given its index.  */
  columnWidth: typeof babelPluginFlowReactPropTypes_proptype_CellSize === 'function' ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired : babelPluginFlowReactPropTypes_proptype_CellSize : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_CellSize).isRequired,


  /** Unfiltered props for the Grid container. */
  containerProps: __webpack_require__(0).object,


  /** ARIA role for the cell-container.  */
  containerRole: __webpack_require__(0).string.isRequired,


  /** Optional inline style applied to inner cell-container */
  containerStyle: __webpack_require__(0).object.isRequired,


  /**
   * If CellMeasurer is used to measure this Grid's children, this should be a pointer to its CellMeasurerCache.
   * A shared CellMeasurerCache reference enables Grid and CellMeasurer to share measurement data.
   */
  deferredMeasurementCache: __webpack_require__(0).object,


  /**
   * Used to estimate the total width of a Grid before all of its columns have actually been measured.
   * The estimated total width is adjusted as columns are rendered.
   */
  estimatedColumnSize: __webpack_require__(0).number.isRequired,


  /**
   * Used to estimate the total height of a Grid before all of its rows have actually been measured.
   * The estimated total height is adjusted as rows are rendered.
   */
  estimatedRowSize: __webpack_require__(0).number.isRequired,


  /** Exposed for testing purposes only.  */
  getScrollbarSize: __webpack_require__(0).func.isRequired,


  /** Height of Grid; this property determines the number of visible (vs virtualized) rows.  */
  height: __webpack_require__(0).number.isRequired,


  /** Optional custom id to attach to root Grid element.  */
  id: __webpack_require__(0).string,


  /**
   * Override internal is-scrolling state tracking.
   * This property is primarily intended for use with the WindowScroller component.
   */
  isScrolling: __webpack_require__(0).bool,


  /** Optional renderer to be used in place of rows when either :rowCount or :columnCount is 0.  */
  noContentRenderer: typeof babelPluginFlowReactPropTypes_proptype_NoContentRenderer === 'function' ? babelPluginFlowReactPropTypes_proptype_NoContentRenderer.isRequired ? babelPluginFlowReactPropTypes_proptype_NoContentRenderer.isRequired : babelPluginFlowReactPropTypes_proptype_NoContentRenderer : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_NoContentRenderer).isRequired,


  /**
   * Callback invoked whenever the scroll offset changes within the inner scrollable region.
   * This callback can be used to sync scrolling between lists, tables, or grids.
   */
  onScroll: __webpack_require__(0).func.isRequired,


  /**
   * Called whenever a horizontal or vertical scrollbar is added or removed.
   * This prop is not intended for end-user use;
   * It is used by MultiGrid to support fixed-row/fixed-column scroll syncing.
   */
  onScrollbarPresenceChange: __webpack_require__(0).func.isRequired,


  /** Callback invoked with information about the section of the Grid that was just rendered.  */
  onSectionRendered: __webpack_require__(0).func.isRequired,


  /**
   * Number of columns to render before/after the visible section of the grid.
   * These columns can help for smoother scrolling on touch devices or browsers that send scroll events infrequently.
   */
  overscanColumnCount: __webpack_require__(0).number.isRequired,


  /**
   * Calculates the number of cells to overscan before and after a specified range.
   * This function ensures that overscanning doesn't exceed the available cells.
   */
  overscanIndicesGetter: typeof babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter === 'function' ? babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter.isRequired ? babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter.isRequired : babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter).isRequired,


  /**
   * Number of rows to render above/below the visible section of the grid.
   * These rows can help for smoother scrolling on touch devices or browsers that send scroll events infrequently.
   */
  overscanRowCount: __webpack_require__(0).number.isRequired,


  /** ARIA role for the grid element.  */
  role: __webpack_require__(0).string.isRequired,


  /**
   * Either a fixed row height (number) or a function that returns the height of a row given its index.
   * Should implement the following interface: ({ index: number }): number
   */
  rowHeight: typeof babelPluginFlowReactPropTypes_proptype_CellSize === 'function' ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired : babelPluginFlowReactPropTypes_proptype_CellSize : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_CellSize).isRequired,


  /** Number of rows in grid.  */
  rowCount: __webpack_require__(0).number.isRequired,


  /** Wait this amount of time after the last scroll event before resetting Grid `pointer-events`. */
  scrollingResetTimeInterval: __webpack_require__(0).number.isRequired,


  /** Horizontal offset. */
  scrollLeft: __webpack_require__(0).number,


  /**
   * Controls scroll-to-cell behavior of the Grid.
   * The default ("auto") scrolls the least amount possible to ensure that the specified cell is fully visible.
   * Use "start" to align cells to the top/left of the Grid and "end" to align bottom/right.
   */
  scrollToAlignment: typeof babelPluginFlowReactPropTypes_proptype_Alignment === 'function' ? babelPluginFlowReactPropTypes_proptype_Alignment.isRequired ? babelPluginFlowReactPropTypes_proptype_Alignment.isRequired : babelPluginFlowReactPropTypes_proptype_Alignment : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_Alignment).isRequired,


  /** Column index to ensure visible (by forcefully scrolling if necessary) */
  scrollToColumn: __webpack_require__(0).number.isRequired,


  /** Vertical offset. */
  scrollTop: __webpack_require__(0).number,


  /** Row index to ensure visible (by forcefully scrolling if necessary) */
  scrollToRow: __webpack_require__(0).number.isRequired,


  /** Optional inline style */
  style: __webpack_require__(0).object.isRequired,


  /** Tab index for focus */
  tabIndex: __webpack_require__(0).number,


  /** Width of Grid; this property determines the number of visible (vs virtualized) columns.  */
  width: __webpack_require__(0).number.isRequired
};
/* harmony default export */ __webpack_exports__["a"] = (Grid);

/***/ }),
/* 95 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(219), __esModule: true };

/***/ }),
/* 96 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return SCROLL_DIRECTION_BACKWARD; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return SCROLL_DIRECTION_FORWARD; });
/* unused harmony export SCROLL_DIRECTION_HORIZONTAL */
/* unused harmony export SCROLL_DIRECTION_VERTICAL */
/* harmony export (immutable) */ __webpack_exports__["c"] = defaultOverscanIndicesGetter;
var babelPluginFlowReactPropTypes_proptype_OverscanIndices = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_OverscanIndices || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams || __webpack_require__(0).any;

var SCROLL_DIRECTION_BACKWARD = -1;
var SCROLL_DIRECTION_FORWARD = 1;

var SCROLL_DIRECTION_HORIZONTAL = 'horizontal';
var SCROLL_DIRECTION_VERTICAL = 'vertical';

/**
 * Calculates the number of cells to overscan before and after a specified range.
 * This function ensures that overscanning doesn't exceed the available cells.
 */

function defaultOverscanIndicesGetter(_ref) {
  var cellCount = _ref.cellCount,
      overscanCellsCount = _ref.overscanCellsCount,
      scrollDirection = _ref.scrollDirection,
      startIndex = _ref.startIndex,
      stopIndex = _ref.stopIndex;

  if (scrollDirection === SCROLL_DIRECTION_FORWARD) {
    return {
      overscanStartIndex: Math.max(0, startIndex),
      overscanStopIndex: Math.min(cellCount - 1, stopIndex + overscanCellsCount)
    };
  } else {
    return {
      overscanStartIndex: Math.max(0, startIndex - overscanCellsCount),
      overscanStopIndex: Math.min(cellCount - 1, stopIndex)
    };
  }
}

/***/ }),
/* 97 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultCellRangeRenderer;


/**
 * Default implementation of cellRangeRenderer used by Grid.
 * This renderer supports cell-caching while the user is scrolling.
 */

var babelPluginFlowReactPropTypes_proptype_CellRangeRendererParams = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellRangeRendererParams || __webpack_require__(0).any;

function defaultCellRangeRenderer(_ref) {
  var cellCache = _ref.cellCache,
      cellRenderer = _ref.cellRenderer,
      columnSizeAndPositionManager = _ref.columnSizeAndPositionManager,
      columnStartIndex = _ref.columnStartIndex,
      columnStopIndex = _ref.columnStopIndex,
      deferredMeasurementCache = _ref.deferredMeasurementCache,
      horizontalOffsetAdjustment = _ref.horizontalOffsetAdjustment,
      isScrolling = _ref.isScrolling,
      parent = _ref.parent,
      rowSizeAndPositionManager = _ref.rowSizeAndPositionManager,
      rowStartIndex = _ref.rowStartIndex,
      rowStopIndex = _ref.rowStopIndex,
      styleCache = _ref.styleCache,
      verticalOffsetAdjustment = _ref.verticalOffsetAdjustment,
      visibleColumnIndices = _ref.visibleColumnIndices,
      visibleRowIndices = _ref.visibleRowIndices;

  var renderedCells = [];

  // Browsers have native size limits for elements (eg Chrome 33M pixels, IE 1.5M pixes).
  // User cannot scroll beyond these size limitations.
  // In order to work around this, ScalingCellSizeAndPositionManager compresses offsets.
  // We should never cache styles for compressed offsets though as this can lead to bugs.
  // See issue #576 for more.
  var areOffsetsAdjusted = columnSizeAndPositionManager.areOffsetsAdjusted() || rowSizeAndPositionManager.areOffsetsAdjusted();

  var canCacheStyle = !isScrolling && !areOffsetsAdjusted;

  for (var rowIndex = rowStartIndex; rowIndex <= rowStopIndex; rowIndex++) {
    var rowDatum = rowSizeAndPositionManager.getSizeAndPositionOfCell(rowIndex);

    for (var columnIndex = columnStartIndex; columnIndex <= columnStopIndex; columnIndex++) {
      var columnDatum = columnSizeAndPositionManager.getSizeAndPositionOfCell(columnIndex);
      var isVisible = columnIndex >= visibleColumnIndices.start && columnIndex <= visibleColumnIndices.stop && rowIndex >= visibleRowIndices.start && rowIndex <= visibleRowIndices.stop;
      var key = rowIndex + '-' + columnIndex;
      var style = void 0;

      // Cache style objects so shallow-compare doesn't re-render unnecessarily.
      if (canCacheStyle && styleCache[key]) {
        style = styleCache[key];
      } else {
        // In deferred mode, cells will be initially rendered before we know their size.
        // Don't interfere with CellMeasurer's measurements by setting an invalid size.
        if (deferredMeasurementCache && !deferredMeasurementCache.has(rowIndex, columnIndex)) {
          // Position not-yet-measured cells at top/left 0,0,
          // And give them width/height of 'auto' so they can grow larger than the parent Grid if necessary.
          // Positioning them further to the right/bottom influences their measured size.
          style = {
            height: 'auto',
            left: 0,
            position: 'absolute',
            top: 0,
            width: 'auto'
          };
        } else {
          style = {
            height: rowDatum.size,
            left: columnDatum.offset + horizontalOffsetAdjustment,
            position: 'absolute',
            top: rowDatum.offset + verticalOffsetAdjustment,
            width: columnDatum.size
          };

          styleCache[key] = style;
        }
      }

      var cellRendererParams = {
        columnIndex: columnIndex,
        isScrolling: isScrolling,
        isVisible: isVisible,
        key: key,
        parent: parent,
        rowIndex: rowIndex,
        style: style
      };

      var renderedCell = void 0;

      // Avoid re-creating cells while scrolling.
      // This can lead to the same cell being created many times and can cause performance issues for "heavy" cells.
      // If a scroll is in progress- cache and reuse cells.
      // This cache will be thrown away once scrolling completes.
      // However if we are scaling scroll positions and sizes, we should also avoid caching.
      // This is because the offset changes slightly as scroll position changes and caching leads to stale values.
      // For more info refer to issue #395
      if (isScrolling && !horizontalOffsetAdjustment && !verticalOffsetAdjustment) {
        if (!cellCache[key]) {
          cellCache[key] = cellRenderer(cellRendererParams);
        }

        renderedCell = cellCache[key];

        // If the user is no longer scrolling, don't cache cells.
        // This makes dynamic cell content difficult for users and would also lead to a heavier memory footprint.
      } else {
        renderedCell = cellRenderer(cellRendererParams);
      }

      if (renderedCell == null || renderedCell === false) {
        continue;
      }

      if (true) {
        warnAboutMissingStyle(parent, renderedCell);
      }

      renderedCells.push(renderedCell);
    }
  }

  return renderedCells;
}

function warnAboutMissingStyle(parent, renderedCell) {
  if (true) {
    if (renderedCell) {
      // If the direct child is a CellMeasurer, then we should check its child
      // See issue #611
      if (renderedCell.type && renderedCell.type.__internalCellMeasurerFlag) {
        renderedCell = renderedCell.props.children;
      }

      if (renderedCell && renderedCell.props && renderedCell.props.style === undefined && parent.__warnedAboutMissingStyle !== true) {
        parent.__warnedAboutMissingStyle = true;

        console.warn('Rendered cell should include style property for positioning.');
      }
    }
  }
}

/***/ }),
/* 98 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (recalc) {
  if (!size && size !== 0 || recalc) {
    if (_inDOM2.default) {
      var scrollDiv = document.createElement('div');

      scrollDiv.style.position = 'absolute';
      scrollDiv.style.top = '-9999px';
      scrollDiv.style.width = '50px';
      scrollDiv.style.height = '50px';
      scrollDiv.style.overflow = 'scroll';

      document.body.appendChild(scrollDiv);
      size = scrollDiv.offsetWidth - scrollDiv.clientWidth;
      document.body.removeChild(scrollDiv);
    }
  }

  return size;
};

var _inDOM = __webpack_require__(222);

var _inDOM2 = _interopRequireDefault(_inDOM);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var size = void 0;

module.exports = exports['default'];

/***/ }),
/* 99 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__vendor_detectElementResize__ = __webpack_require__(100);









var AutoSizer = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(AutoSizer, _React$PureComponent);

  function AutoSizer() {
    var _ref;

    var _temp, _this, _ret;

    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, AutoSizer);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (_ref = AutoSizer.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(AutoSizer)).call.apply(_ref, [this].concat(args))), _this), _this.state = {
      height: _this.props.defaultHeight || 0,
      width: _this.props.defaultWidth || 0
    }, _this._onResize = function () {
      var _this$props = _this.props,
          disableHeight = _this$props.disableHeight,
          disableWidth = _this$props.disableWidth,
          onResize = _this$props.onResize;


      if (_this._parentNode) {
        // Guard against AutoSizer component being removed from the DOM immediately after being added.
        // This can result in invalid style values which can result in NaN values if we don't handle them.
        // See issue #150 for more context.

        var _height = _this._parentNode.offsetHeight || 0;
        var _width = _this._parentNode.offsetWidth || 0;

        var _style = window.getComputedStyle(_this._parentNode) || {};
        var paddingLeft = parseInt(_style.paddingLeft, 10) || 0;
        var paddingRight = parseInt(_style.paddingRight, 10) || 0;
        var paddingTop = parseInt(_style.paddingTop, 10) || 0;
        var paddingBottom = parseInt(_style.paddingBottom, 10) || 0;

        var newHeight = _height - paddingTop - paddingBottom;
        var newWidth = _width - paddingLeft - paddingRight;

        if (!disableHeight && _this.state.height !== newHeight || !disableWidth && _this.state.width !== newWidth) {
          _this.setState({
            height: _height - paddingTop - paddingBottom,
            width: _width - paddingLeft - paddingRight
          });

          onResize({ height: _height, width: _width });
        }
      }
    }, _this._setRef = function (autoSizer) {
      _this._autoSizer = autoSizer;
    }, _temp), __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(_this, _ret);
  }

  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(AutoSizer, [{
    key: 'componentDidMount',
    value: function componentDidMount() {
      var nonce = this.props.nonce;

      if (this._autoSizer && this._autoSizer.parentNode && this._autoSizer.parentNode.ownerDocument && this._autoSizer.parentNode.ownerDocument.defaultView && this._autoSizer.parentNode instanceof this._autoSizer.parentNode.ownerDocument.defaultView.HTMLElement) {
        // Delay access of parentNode until mount.
        // This handles edge-cases where the component has already been unmounted before its ref has been set,
        // As well as libraries like react-lite which have a slightly different lifecycle.
        this._parentNode = this._autoSizer.parentNode;

        // Defer requiring resize handler in order to support server-side rendering.
        // See issue #41
        this._detectElementResize = Object(__WEBPACK_IMPORTED_MODULE_7__vendor_detectElementResize__["a" /* default */])(nonce);
        this._detectElementResize.addResizeListener(this._parentNode, this._onResize);

        this._onResize();
      }
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      if (this._detectElementResize && this._parentNode) {
        this._detectElementResize.removeResizeListener(this._parentNode, this._onResize);
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          children = _props.children,
          className = _props.className,
          disableHeight = _props.disableHeight,
          disableWidth = _props.disableWidth,
          style = _props.style;
      var _state = this.state,
          height = _state.height,
          width = _state.width;

      // Outer div should not force width/height since that may prevent containers from shrinking.
      // Inner component should overflow and use calculated width/height.
      // See issue #68 for more information.

      var outerStyle = { overflow: 'visible' };
      var childParams = {};

      if (!disableHeight) {
        outerStyle.height = 0;
        childParams.height = height;
      }

      if (!disableWidth) {
        outerStyle.width = 0;
        childParams.width = width;
      }

      /**
       * TODO: Avoid rendering children before the initial measurements have been collected.
       * At best this would just be wasting cycles.
       * Add this check into version 10 though as it could break too many ref callbacks in version 9.
       * Note that if default width/height props were provided this would still work with SSR.
      if (
        height !== 0 &&
        width !== 0
      ) {
        child = children({ height, width })
      }
      */

      return __WEBPACK_IMPORTED_MODULE_6_react__["createElement"](
        'div',
        {
          className: className,
          ref: this._setRef,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, outerStyle, style) },
        children(childParams)
      );
    }
  }]);

  return AutoSizer;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

AutoSizer.defaultProps = {
  onResize: function onResize() {},
  disableHeight: false,
  disableWidth: false,
  style: {}
};
AutoSizer.propTypes =  false ? null : {
  /** Function responsible for rendering children.*/
  children: __webpack_require__(0).func.isRequired,


  /** Optional custom CSS class name to attach to root AutoSizer element.  */
  className: __webpack_require__(0).string,


  /** Default height to use for initial render; useful for SSR */
  defaultHeight: __webpack_require__(0).number,


  /** Default width to use for initial render; useful for SSR */
  defaultWidth: __webpack_require__(0).number,


  /** Disable dynamic :height property */
  disableHeight: __webpack_require__(0).bool.isRequired,


  /** Disable dynamic :width property */
  disableWidth: __webpack_require__(0).bool.isRequired,


  /** Nonce of the inlined stylesheet for Content Security Policy */
  nonce: __webpack_require__(0).string,


  /** Callback to be invoked on-resize */
  onResize: __webpack_require__(0).func.isRequired,


  /** Optional inline style */
  style: __webpack_require__(0).object
};
/* harmony default export */ __webpack_exports__["a"] = (AutoSizer);

/***/ }),
/* 100 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {/* harmony export (immutable) */ __webpack_exports__["a"] = createDetectElementResize;
/**
 * Detect Element Resize.
 * https://github.com/sdecima/javascript-detect-element-resize
 * Sebastian Decima
 *
 * Forked from version 0.5.3; includes the following modifications:
 * 1) Guard against unsafe 'window' and 'document' references (to support SSR).
 * 2) Defer initialization code via a top-level function wrapper (to support SSR).
 * 3) Avoid unnecessary reflows by not measuring size for scroll events bubbling from children.
 * 4) Add nonce for style element.
 **/

function createDetectElementResize(nonce) {
  // Check `document` and `window` in case of server-side rendering
  var _window;
  if (typeof window !== 'undefined') {
    _window = window;
  } else if (typeof self !== 'undefined') {
    _window = self;
  } else {
    _window = global;
  }

  var attachEvent = typeof document !== 'undefined' && document.attachEvent;

  if (!attachEvent) {
    var requestFrame = function () {
      var raf = _window.requestAnimationFrame || _window.mozRequestAnimationFrame || _window.webkitRequestAnimationFrame || function (fn) {
        return _window.setTimeout(fn, 20);
      };
      return function (fn) {
        return raf(fn);
      };
    }();

    var cancelFrame = function () {
      var cancel = _window.cancelAnimationFrame || _window.mozCancelAnimationFrame || _window.webkitCancelAnimationFrame || _window.clearTimeout;
      return function (id) {
        return cancel(id);
      };
    }();

    var resetTriggers = function resetTriggers(element) {
      var triggers = element.__resizeTriggers__,
          expand = triggers.firstElementChild,
          contract = triggers.lastElementChild,
          expandChild = expand.firstElementChild;
      contract.scrollLeft = contract.scrollWidth;
      contract.scrollTop = contract.scrollHeight;
      expandChild.style.width = expand.offsetWidth + 1 + 'px';
      expandChild.style.height = expand.offsetHeight + 1 + 'px';
      expand.scrollLeft = expand.scrollWidth;
      expand.scrollTop = expand.scrollHeight;
    };

    var checkTriggers = function checkTriggers(element) {
      return element.offsetWidth != element.__resizeLast__.width || element.offsetHeight != element.__resizeLast__.height;
    };

    var scrollListener = function scrollListener(e) {
      // Don't measure (which forces) reflow for scrolls that happen inside of children!
      if (e.target.className.indexOf('contract-trigger') < 0 && e.target.className.indexOf('expand-trigger') < 0) {
        return;
      }

      var element = this;
      resetTriggers(this);
      if (this.__resizeRAF__) {
        cancelFrame(this.__resizeRAF__);
      }
      this.__resizeRAF__ = requestFrame(function () {
        if (checkTriggers(element)) {
          element.__resizeLast__.width = element.offsetWidth;
          element.__resizeLast__.height = element.offsetHeight;
          element.__resizeListeners__.forEach(function (fn) {
            fn.call(element, e);
          });
        }
      });
    };

    /* Detect CSS Animations support to detect element display/re-attach */
    var animation = false,
        keyframeprefix = '',
        animationstartevent = 'animationstart',
        domPrefixes = 'Webkit Moz O ms'.split(' '),
        startEvents = 'webkitAnimationStart animationstart oAnimationStart MSAnimationStart'.split(' '),
        pfx = '';
    {
      var elm = document.createElement('fakeelement');
      if (elm.style.animationName !== undefined) {
        animation = true;
      }

      if (animation === false) {
        for (var i = 0; i < domPrefixes.length; i++) {
          if (elm.style[domPrefixes[i] + 'AnimationName'] !== undefined) {
            pfx = domPrefixes[i];
            keyframeprefix = '-' + pfx.toLowerCase() + '-';
            animationstartevent = startEvents[i];
            animation = true;
            break;
          }
        }
      }
    }

    var animationName = 'resizeanim';
    var animationKeyframes = '@' + keyframeprefix + 'keyframes ' + animationName + ' { from { opacity: 0; } to { opacity: 0; } } ';
    var animationStyle = keyframeprefix + 'animation: 1ms ' + animationName + '; ';
  }

  var createStyles = function createStyles(doc) {
    if (!doc.getElementById('detectElementResize')) {
      //opacity:0 works around a chrome bug https://code.google.com/p/chromium/issues/detail?id=286360
      var css = (animationKeyframes ? animationKeyframes : '') + '.resize-triggers { ' + (animationStyle ? animationStyle : '') + 'visibility: hidden; opacity: 0; } ' + '.resize-triggers, .resize-triggers > div, .contract-trigger:before { content: " "; display: block; position: absolute; top: 0; left: 0; height: 100%; width: 100%; overflow: hidden; z-index: -1; } .resize-triggers > div { background: #eee; overflow: auto; } .contract-trigger:before { width: 200%; height: 200%; }',
          head = doc.head || doc.getElementsByTagName('head')[0],
          style = doc.createElement('style');

      style.id = 'detectElementResize';
      style.type = 'text/css';

      if (nonce != null) {
        style.setAttribute('nonce', nonce);
      }

      if (style.styleSheet) {
        style.styleSheet.cssText = css;
      } else {
        style.appendChild(doc.createTextNode(css));
      }

      head.appendChild(style);
    }
  };

  var addResizeListener = function addResizeListener(element, fn) {
    if (attachEvent) {
      element.attachEvent('onresize', fn);
    } else {
      if (!element.__resizeTriggers__) {
        var doc = element.ownerDocument;
        var elementStyle = _window.getComputedStyle(element);
        if (elementStyle && elementStyle.position == 'static') {
          element.style.position = 'relative';
        }
        createStyles(doc);
        element.__resizeLast__ = {};
        element.__resizeListeners__ = [];
        (element.__resizeTriggers__ = doc.createElement('div')).className = 'resize-triggers';
        element.__resizeTriggers__.innerHTML = '<div class="expand-trigger"><div></div></div>' + '<div class="contract-trigger"></div>';
        element.appendChild(element.__resizeTriggers__);
        resetTriggers(element);
        element.addEventListener('scroll', scrollListener, true);

        /* Listen for a css animation to detect element display/re-attach */
        if (animationstartevent) {
          element.__resizeTriggers__.__animationListener__ = function animationListener(e) {
            if (e.animationName == animationName) {
              resetTriggers(element);
            }
          };
          element.__resizeTriggers__.addEventListener(animationstartevent, element.__resizeTriggers__.__animationListener__);
        }
      }
      element.__resizeListeners__.push(fn);
    }
  };

  var removeResizeListener = function removeResizeListener(element, fn) {
    if (attachEvent) {
      element.detachEvent('onresize', fn);
    } else {
      element.__resizeListeners__.splice(element.__resizeListeners__.indexOf(fn), 1);
      if (!element.__resizeListeners__.length) {
        element.removeEventListener('scroll', scrollListener, true);
        if (element.__resizeTriggers__.__animationListener__) {
          element.__resizeTriggers__.removeEventListener(animationstartevent, element.__resizeTriggers__.__animationListener__);
          element.__resizeTriggers__.__animationListener__ = null;
        }
        try {
          element.__resizeTriggers__ = !element.removeChild(element.__resizeTriggers__);
        } catch (e) {
          // Preact compat; see developit/preact-compat/issues/228
        }
      }
    }
  };

  return {
    addResizeListener: addResizeListener,
    removeResizeListener: removeResizeListener
  };
}
/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(33)))

/***/ }),
/* 101 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__CellMeasurer__ = __webpack_require__(226);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__CellMeasurerCache__ = __webpack_require__(102);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__CellMeasurer__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return __WEBPACK_IMPORTED_MODULE_1__CellMeasurerCache__["a"]; });



/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__CellMeasurer__["a" /* default */]);


/***/ }),
/* 102 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export DEFAULT_HEIGHT */
/* unused harmony export DEFAULT_WIDTH */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__);


var DEFAULT_HEIGHT = 30;
var DEFAULT_WIDTH = 100;

// Enables more intelligent mapping of a given column and row index to an item ID.
// This prevents a cell cache from being invalidated when its parent collection is modified.

/**
 * Caches measurements for a given cell.
 */
var CellMeasurerCache = function () {
  function CellMeasurerCache() {
    var _this = this;

    var params = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

    __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default()(this, CellMeasurerCache);

    this._cellHeightCache = {};
    this._cellWidthCache = {};
    this._columnWidthCache = {};
    this._rowHeightCache = {};
    this._columnCount = 0;
    this._rowCount = 0;

    this.columnWidth = function (_ref) {
      var index = _ref.index;

      var key = _this._keyMapper(0, index);

      return _this._columnWidthCache.hasOwnProperty(key) ? _this._columnWidthCache[key] : _this._defaultWidth;
    };

    this.rowHeight = function (_ref2) {
      var index = _ref2.index;

      var key = _this._keyMapper(index, 0);

      return _this._rowHeightCache.hasOwnProperty(key) ? _this._rowHeightCache[key] : _this._defaultHeight;
    };

    var defaultHeight = params.defaultHeight,
        defaultWidth = params.defaultWidth,
        fixedHeight = params.fixedHeight,
        fixedWidth = params.fixedWidth,
        keyMapper = params.keyMapper,
        minHeight = params.minHeight,
        minWidth = params.minWidth;


    this._hasFixedHeight = fixedHeight === true;
    this._hasFixedWidth = fixedWidth === true;
    this._minHeight = minHeight || 0;
    this._minWidth = minWidth || 0;
    this._keyMapper = keyMapper || defaultKeyMapper;

    this._defaultHeight = Math.max(this._minHeight, typeof defaultHeight === 'number' ? defaultHeight : DEFAULT_HEIGHT);
    this._defaultWidth = Math.max(this._minWidth, typeof defaultWidth === 'number' ? defaultWidth : DEFAULT_WIDTH);

    if (true) {
      if (this._hasFixedHeight === false && this._hasFixedWidth === false) {
        console.warn("CellMeasurerCache should only measure a cell's width or height. " + 'You have configured CellMeasurerCache to measure both. ' + 'This will result in poor performance.');
      }

      if (this._hasFixedHeight === false && this._defaultHeight === 0) {
        console.warn('Fixed height CellMeasurerCache should specify a :defaultHeight greater than 0. ' + 'Failing to do so will lead to unnecessary layout and poor performance.');
      }

      if (this._hasFixedWidth === false && this._defaultWidth === 0) {
        console.warn('Fixed width CellMeasurerCache should specify a :defaultWidth greater than 0. ' + 'Failing to do so will lead to unnecessary layout and poor performance.');
      }
    }
  }

  __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default()(CellMeasurerCache, [{
    key: 'clear',
    value: function clear(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      var key = this._keyMapper(rowIndex, columnIndex);

      delete this._cellHeightCache[key];
      delete this._cellWidthCache[key];

      this._updateCachedColumnAndRowSizes(rowIndex, columnIndex);
    }
  }, {
    key: 'clearAll',
    value: function clearAll() {
      this._cellHeightCache = {};
      this._cellWidthCache = {};
      this._columnWidthCache = {};
      this._rowHeightCache = {};
      this._rowCount = 0;
      this._columnCount = 0;
    }
  }, {
    key: 'hasFixedHeight',
    value: function hasFixedHeight() {
      return this._hasFixedHeight;
    }
  }, {
    key: 'hasFixedWidth',
    value: function hasFixedWidth() {
      return this._hasFixedWidth;
    }
  }, {
    key: 'getHeight',
    value: function getHeight(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      if (this._hasFixedHeight) {
        return this._defaultHeight;
      } else {
        var _key = this._keyMapper(rowIndex, columnIndex);

        return this._cellHeightCache.hasOwnProperty(_key) ? Math.max(this._minHeight, this._cellHeightCache[_key]) : this._defaultHeight;
      }
    }
  }, {
    key: 'getWidth',
    value: function getWidth(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      if (this._hasFixedWidth) {
        return this._defaultWidth;
      } else {
        var _key2 = this._keyMapper(rowIndex, columnIndex);

        return this._cellWidthCache.hasOwnProperty(_key2) ? Math.max(this._minWidth, this._cellWidthCache[_key2]) : this._defaultWidth;
      }
    }
  }, {
    key: 'has',
    value: function has(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      var key = this._keyMapper(rowIndex, columnIndex);

      return this._cellHeightCache.hasOwnProperty(key);
    }
  }, {
    key: 'set',
    value: function set(rowIndex, columnIndex, width, height) {
      var key = this._keyMapper(rowIndex, columnIndex);

      if (columnIndex >= this._columnCount) {
        this._columnCount = columnIndex + 1;
      }
      if (rowIndex >= this._rowCount) {
        this._rowCount = rowIndex + 1;
      }

      // Size is cached per cell so we don't have to re-measure if cells are re-ordered.
      this._cellHeightCache[key] = height;
      this._cellWidthCache[key] = width;

      this._updateCachedColumnAndRowSizes(rowIndex, columnIndex);
    }
  }, {
    key: '_updateCachedColumnAndRowSizes',
    value: function _updateCachedColumnAndRowSizes(rowIndex, columnIndex) {
      // :columnWidth and :rowHeight are derived based on all cells in a column/row.
      // Pre-cache these derived values for faster lookup later.
      // Reads are expected to occur more frequently than writes in this case.
      // Only update non-fixed dimensions though to avoid doing unnecessary work.
      if (!this._hasFixedWidth) {
        var columnWidth = 0;
        for (var i = 0; i < this._rowCount; i++) {
          columnWidth = Math.max(columnWidth, this.getWidth(i, columnIndex));
        }
        var columnKey = this._keyMapper(0, columnIndex);
        this._columnWidthCache[columnKey] = columnWidth;
      }
      if (!this._hasFixedHeight) {
        var rowHeight = 0;
        for (var _i = 0; _i < this._columnCount; _i++) {
          rowHeight = Math.max(rowHeight, this.getHeight(rowIndex, _i));
        }
        var rowKey = this._keyMapper(rowIndex, 0);
        this._rowHeightCache[rowKey] = rowHeight;
      }
    }
  }, {
    key: 'defaultHeight',
    get: function get() {
      return this._defaultHeight;
    }
  }, {
    key: 'defaultWidth',
    get: function get() {
      return this._defaultWidth;
    }
  }]);

  return CellMeasurerCache;
}();

/* harmony default export */ __webpack_exports__["a"] = (CellMeasurerCache);


function defaultKeyMapper(rowIndex, columnIndex) {
  return rowIndex + '-' + columnIndex;
}

/***/ }),
/* 103 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_own_property_descriptor__ = __webpack_require__(239);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_own_property_descriptor___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_own_property_descriptor__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__Grid__ = __webpack_require__(9);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_8_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_9_classnames__);








var babelPluginFlowReactPropTypes_proptype_Scroll = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_Scroll || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellRendererParams = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_CellRendererParams || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_RenderedSection = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_RenderedSection || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellPosition = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_CellPosition || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellSize = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_CellSize || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Alignment = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_Alignment || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_NoContentRenderer = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_NoContentRenderer || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Scroll = __webpack_require__(67).babelPluginFlowReactPropTypes_proptype_Scroll || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_RenderedRows = __webpack_require__(67).babelPluginFlowReactPropTypes_proptype_RenderedRows || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_RowRenderer = __webpack_require__(67).babelPluginFlowReactPropTypes_proptype_RowRenderer || __webpack_require__(0).any;





/**
 * It is inefficient to create and manage a large list of DOM elements within a scrolling container
 * if only a few of those elements are visible. The primary purpose of this component is to improve
 * performance by only rendering the DOM nodes that a user is able to see based on their current
 * scroll position.
 *
 * This component renders a virtualized list of elements with either fixed or dynamic heights.
 */

var List = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default()(List, _React$PureComponent);

  function List() {
    var _ref;

    var _temp, _this, _ret;

    __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default()(this, List);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default()(this, (_ref = List.__proto__ || __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default()(List)).call.apply(_ref, [this].concat(args))), _this), _this._cellRenderer = function (_ref2) {
      var parent = _ref2.parent,
          rowIndex = _ref2.rowIndex,
          style = _ref2.style,
          isScrolling = _ref2.isScrolling,
          isVisible = _ref2.isVisible,
          key = _ref2.key;
      var rowRenderer = _this.props.rowRenderer;

      // TRICKY The style object is sometimes cached by Grid.
      // This prevents new style objects from bypassing shallowCompare().
      // However as of React 16, style props are auto-frozen (at least in dev mode)
      // Check to make sure we can still modify the style before proceeding.
      // https://github.com/facebook/react/commit/977357765b44af8ff0cfea327866861073095c12#commitcomment-20648713

      var _Object$getOwnPropert = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_own_property_descriptor___default()(style, 'width'),
          writable = _Object$getOwnPropert.writable;

      if (writable) {
        // By default, List cells should be 100% width.
        // This prevents them from flowing under a scrollbar (if present).
        style.width = '100%';
      }

      return rowRenderer({
        index: rowIndex,
        style: style,
        isScrolling: isScrolling,
        isVisible: isVisible,
        key: key,
        parent: parent
      });
    }, _this._setRef = function (ref) {
      _this.Grid = ref;
    }, _this._onScroll = function (_ref3) {
      var clientHeight = _ref3.clientHeight,
          scrollHeight = _ref3.scrollHeight,
          scrollTop = _ref3.scrollTop;
      var onScroll = _this.props.onScroll;


      onScroll({ clientHeight: clientHeight, scrollHeight: scrollHeight, scrollTop: scrollTop });
    }, _this._onSectionRendered = function (_ref4) {
      var rowOverscanStartIndex = _ref4.rowOverscanStartIndex,
          rowOverscanStopIndex = _ref4.rowOverscanStopIndex,
          rowStartIndex = _ref4.rowStartIndex,
          rowStopIndex = _ref4.rowStopIndex;
      var onRowsRendered = _this.props.onRowsRendered;


      onRowsRendered({
        overscanStartIndex: rowOverscanStartIndex,
        overscanStopIndex: rowOverscanStopIndex,
        startIndex: rowStartIndex,
        stopIndex: rowStopIndex
      });
    }, _temp), __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default()(_this, _ret);
  }

  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default()(List, [{
    key: 'forceUpdateGrid',
    value: function forceUpdateGrid() {
      if (this.Grid) {
        this.Grid.forceUpdate();
      }
    }

    /** See Grid#getOffsetForCell */

  }, {
    key: 'getOffsetForRow',
    value: function getOffsetForRow(_ref5) {
      var alignment = _ref5.alignment,
          index = _ref5.index;

      if (this.Grid) {
        var _Grid$getOffsetForCel = this.Grid.getOffsetForCell({
          alignment: alignment,
          rowIndex: index,
          columnIndex: 0
        }),
            _scrollTop = _Grid$getOffsetForCel.scrollTop;

        return _scrollTop;
      }
      return 0;
    }

    /** CellMeasurer compatibility */

  }, {
    key: 'invalidateCellSizeAfterRender',
    value: function invalidateCellSizeAfterRender(_ref6) {
      var columnIndex = _ref6.columnIndex,
          rowIndex = _ref6.rowIndex;

      if (this.Grid) {
        this.Grid.invalidateCellSizeAfterRender({
          rowIndex: rowIndex,
          columnIndex: columnIndex
        });
      }
    }

    /** See Grid#measureAllCells */

  }, {
    key: 'measureAllRows',
    value: function measureAllRows() {
      if (this.Grid) {
        this.Grid.measureAllCells();
      }
    }

    /** CellMeasurer compatibility */

  }, {
    key: 'recomputeGridSize',
    value: function recomputeGridSize() {
      var _ref7 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref7$columnIndex = _ref7.columnIndex,
          columnIndex = _ref7$columnIndex === undefined ? 0 : _ref7$columnIndex,
          _ref7$rowIndex = _ref7.rowIndex,
          rowIndex = _ref7$rowIndex === undefined ? 0 : _ref7$rowIndex;

      if (this.Grid) {
        this.Grid.recomputeGridSize({
          rowIndex: rowIndex,
          columnIndex: columnIndex
        });
      }
    }

    /** See Grid#recomputeGridSize */

  }, {
    key: 'recomputeRowHeights',
    value: function recomputeRowHeights() {
      var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.recomputeGridSize({
          rowIndex: index,
          columnIndex: 0
        });
      }
    }

    /** See Grid#scrollToPosition */

  }, {
    key: 'scrollToPosition',
    value: function scrollToPosition() {
      var scrollTop = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.scrollToPosition({ scrollTop: scrollTop });
      }
    }

    /** See Grid#scrollToCell */

  }, {
    key: 'scrollToRow',
    value: function scrollToRow() {
      var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.scrollToCell({
          columnIndex: 0,
          rowIndex: index
        });
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          className = _props.className,
          noRowsRenderer = _props.noRowsRenderer,
          scrollToIndex = _props.scrollToIndex,
          width = _props.width;


      var classNames = __WEBPACK_IMPORTED_MODULE_9_classnames___default()('ReactVirtualized__List', className);

      return __WEBPACK_IMPORTED_MODULE_8_react__["createElement"](__WEBPACK_IMPORTED_MODULE_7__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, this.props, {
        autoContainerWidth: true,
        cellRenderer: this._cellRenderer,
        className: classNames,
        columnWidth: width,
        columnCount: 1,
        noContentRenderer: noRowsRenderer,
        onScroll: this._onScroll,
        onSectionRendered: this._onSectionRendered,
        ref: this._setRef,
        scrollToRow: scrollToIndex
      }));
    }
  }]);

  return List;
}(__WEBPACK_IMPORTED_MODULE_8_react__["PureComponent"]);

List.defaultProps = {
  autoHeight: false,
  estimatedRowSize: 30,
  onScroll: function onScroll() {},
  noRowsRenderer: function noRowsRenderer() {
    return null;
  },
  onRowsRendered: function onRowsRendered() {},
  overscanIndicesGetter: __WEBPACK_IMPORTED_MODULE_7__Grid__["accessibilityOverscanIndicesGetter"],
  overscanRowCount: 10,
  scrollToAlignment: 'auto',
  scrollToIndex: -1,
  style: {}
};
List.propTypes =  false ? null : {
  "aria-label": __webpack_require__(0).string,


  /**
   * Removes fixed height from the scrollingContainer so that the total height
   * of rows can stretch the window. Intended for use with WindowScroller
   */
  autoHeight: __webpack_require__(0).bool.isRequired,


  /** Optional CSS class name */
  className: __webpack_require__(0).string,


  /**
   * Used to estimate the total height of a List before all of its rows have actually been measured.
   * The estimated total height is adjusted as rows are rendered.
   */
  estimatedRowSize: __webpack_require__(0).number.isRequired,


  /** Height constraint for list (determines how many actual rows are rendered) */
  height: __webpack_require__(0).number.isRequired,


  /** Optional renderer to be used in place of rows when rowCount is 0 */
  noRowsRenderer: typeof babelPluginFlowReactPropTypes_proptype_NoContentRenderer === 'function' ? babelPluginFlowReactPropTypes_proptype_NoContentRenderer.isRequired ? babelPluginFlowReactPropTypes_proptype_NoContentRenderer.isRequired : babelPluginFlowReactPropTypes_proptype_NoContentRenderer : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_NoContentRenderer).isRequired,


  /** Callback invoked with information about the slice of rows that were just rendered.  */

  onRowsRendered: __webpack_require__(0).func.isRequired,


  /**
   * Callback invoked whenever the scroll offset changes within the inner scrollable region.
   * This callback can be used to sync scrolling between lists, tables, or grids.
   */
  onScroll: __webpack_require__(0).func.isRequired,


  /** See Grid#overscanIndicesGetter */
  overscanIndicesGetter: typeof babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter === 'function' ? babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter.isRequired ? babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter.isRequired : babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetter).isRequired,


  /**
   * Number of rows to render above/below the visible bounds of the list.
   * These rows can help for smoother scrolling on touch devices.
   */
  overscanRowCount: __webpack_require__(0).number.isRequired,


  /** Either a fixed row height (number) or a function that returns the height of a row given its index.  */
  rowHeight: typeof babelPluginFlowReactPropTypes_proptype_CellSize === 'function' ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired ? babelPluginFlowReactPropTypes_proptype_CellSize.isRequired : babelPluginFlowReactPropTypes_proptype_CellSize : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_CellSize).isRequired,


  /** Responsible for rendering a row given an index; ({ index: number }): node */
  rowRenderer: typeof babelPluginFlowReactPropTypes_proptype_RowRenderer === 'function' ? babelPluginFlowReactPropTypes_proptype_RowRenderer.isRequired ? babelPluginFlowReactPropTypes_proptype_RowRenderer.isRequired : babelPluginFlowReactPropTypes_proptype_RowRenderer : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_RowRenderer).isRequired,


  /** Number of rows in list. */
  rowCount: __webpack_require__(0).number.isRequired,


  /** See Grid#scrollToAlignment */
  scrollToAlignment: typeof babelPluginFlowReactPropTypes_proptype_Alignment === 'function' ? babelPluginFlowReactPropTypes_proptype_Alignment.isRequired ? babelPluginFlowReactPropTypes_proptype_Alignment.isRequired : babelPluginFlowReactPropTypes_proptype_Alignment : __webpack_require__(0).shape(babelPluginFlowReactPropTypes_proptype_Alignment).isRequired,


  /** Row index to ensure visible (by forcefully scrolling if necessary) */
  scrollToIndex: __webpack_require__(0).number.isRequired,


  /** Vertical offset. */
  scrollTop: __webpack_require__(0).number,


  /** Optional inline style */
  style: __webpack_require__(0).object.isRequired,


  /** Tab index for focus */
  tabIndex: __webpack_require__(0).number,


  /** Width of list */
  width: __webpack_require__(0).number.isRequired
};
/* harmony default export */ __webpack_exports__["a"] = (List);

/***/ }),
/* 104 */
/***/ (function(module, exports, __webpack_require__) {

// getting tag from 19.1.3.6 Object.prototype.toString()
var cof = __webpack_require__(56);
var TAG = __webpack_require__(12)('toStringTag');
// ES3 wrong here
var ARG = cof(function () { return arguments; }()) == 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (e) { /* empty */ }
};

module.exports = function (it) {
  var O, T, B;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (T = tryGet(O = Object(it), TAG)) == 'string' ? T
    // builtinTag case
    : ARG ? cof(O)
    // ES3 arguments fallback
    : (B = cof(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : B;
};


/***/ }),
/* 105 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultCellDataGetter;


/**
 * Default accessor for returning a cell value for a given attribute.
 * This function expects to operate on either a vanilla Object or an Immutable Map.
 * You should override the column's cellDataGetter if your data is some other type of object.
 */
var babelPluginFlowReactPropTypes_proptype_CellDataGetterParams = __webpack_require__(29).babelPluginFlowReactPropTypes_proptype_CellDataGetterParams || __webpack_require__(0).any;

function defaultCellDataGetter(_ref) {
  var dataKey = _ref.dataKey,
      rowData = _ref.rowData;

  if (typeof rowData.get === 'function') {
    return rowData.get(dataKey);
  } else {
    return rowData[dataKey];
  }
}

/***/ }),
/* 106 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultCellRenderer;


/**
 * Default cell renderer that displays an attribute as a simple string
 * You should override the column's cellRenderer if your data is some other type of object.
 */
var babelPluginFlowReactPropTypes_proptype_CellRendererParams = __webpack_require__(29).babelPluginFlowReactPropTypes_proptype_CellRendererParams || __webpack_require__(0).any;

function defaultCellRenderer(_ref) {
  var cellData = _ref.cellData;

  if (cellData == null) {
    return '';
  } else {
    return String(cellData);
  }
}

/***/ }),
/* 107 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultHeaderRowRenderer;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_react__);


var babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams = __webpack_require__(29).babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams || __webpack_require__(0).any;

function defaultHeaderRowRenderer(_ref) {
  var className = _ref.className,
      columns = _ref.columns,
      style = _ref.style;

  return __WEBPACK_IMPORTED_MODULE_0_react___default.a.createElement(
    'div',
    { className: className, role: 'row', style: style },
    columns
  );
}
defaultHeaderRowRenderer.propTypes =  false ? null : babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams === __webpack_require__(0).any ? {} : babelPluginFlowReactPropTypes_proptype_HeaderRowRendererParams;

/***/ }),
/* 108 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultHeaderRenderer;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__SortIndicator__ = __webpack_require__(109);



/**
 * Default table header renderer.
 */
var babelPluginFlowReactPropTypes_proptype_HeaderRendererParams = __webpack_require__(29).babelPluginFlowReactPropTypes_proptype_HeaderRendererParams || __webpack_require__(0).any;

function defaultHeaderRenderer(_ref) {
  var dataKey = _ref.dataKey,
      label = _ref.label,
      sortBy = _ref.sortBy,
      sortDirection = _ref.sortDirection;

  var showSortIndicator = sortBy === dataKey;
  var children = [__WEBPACK_IMPORTED_MODULE_0_react___default.a.createElement(
    'span',
    {
      className: 'ReactVirtualized__Table__headerTruncatedText',
      key: 'label',
      title: label },
    label
  )];

  if (showSortIndicator) {
    children.push(__WEBPACK_IMPORTED_MODULE_0_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_1__SortIndicator__["a" /* default */], { key: 'SortIndicator', sortDirection: sortDirection }));
  }

  return children;
}
defaultHeaderRenderer.propTypes =  false ? null : babelPluginFlowReactPropTypes_proptype_HeaderRendererParams === __webpack_require__(0).any ? {} : babelPluginFlowReactPropTypes_proptype_HeaderRendererParams;

/***/ }),
/* 109 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = SortIndicator;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_classnames__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__SortDirection__ = __webpack_require__(38);





/**
 * Displayed beside a header to indicate that a Table is currently sorted by this column.
 */
function SortIndicator(_ref) {
  var sortDirection = _ref.sortDirection;

  var classNames = __WEBPACK_IMPORTED_MODULE_0_classnames___default()('ReactVirtualized__Table__sortableHeaderIcon', {
    'ReactVirtualized__Table__sortableHeaderIcon--ASC': sortDirection === __WEBPACK_IMPORTED_MODULE_3__SortDirection__["a" /* default */].ASC,
    'ReactVirtualized__Table__sortableHeaderIcon--DESC': sortDirection === __WEBPACK_IMPORTED_MODULE_3__SortDirection__["a" /* default */].DESC
  });

  return __WEBPACK_IMPORTED_MODULE_2_react___default.a.createElement(
    'svg',
    { className: classNames, width: 18, height: 18, viewBox: '0 0 24 24' },
    sortDirection === __WEBPACK_IMPORTED_MODULE_3__SortDirection__["a" /* default */].ASC ? __WEBPACK_IMPORTED_MODULE_2_react___default.a.createElement('path', { d: 'M7 14l5-5 5 5z' }) : __WEBPACK_IMPORTED_MODULE_2_react___default.a.createElement('path', { d: 'M7 10l5 5 5-5z' }),
    __WEBPACK_IMPORTED_MODULE_2_react___default.a.createElement('path', { d: 'M0 0h24v24H0z', fill: 'none' })
  );
}

SortIndicator.propTypes =  true ? {
  sortDirection: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOf([__WEBPACK_IMPORTED_MODULE_3__SortDirection__["a" /* default */].ASC, __WEBPACK_IMPORTED_MODULE_3__SortDirection__["a" /* default */].DESC])
} : {};

/***/ }),
/* 110 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultRowRenderer;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_react__);



/**
 * Default row renderer for Table.
 */
var babelPluginFlowReactPropTypes_proptype_RowRendererParams = __webpack_require__(29).babelPluginFlowReactPropTypes_proptype_RowRendererParams || __webpack_require__(0).any;

function defaultRowRenderer(_ref) {
  var className = _ref.className,
      columns = _ref.columns,
      index = _ref.index,
      key = _ref.key,
      onRowClick = _ref.onRowClick,
      onRowDoubleClick = _ref.onRowDoubleClick,
      onRowMouseOut = _ref.onRowMouseOut,
      onRowMouseOver = _ref.onRowMouseOver,
      onRowRightClick = _ref.onRowRightClick,
      rowData = _ref.rowData,
      style = _ref.style;

  var a11yProps = {};

  if (onRowClick || onRowDoubleClick || onRowMouseOut || onRowMouseOver || onRowRightClick) {
    a11yProps['aria-label'] = 'row';
    a11yProps.tabIndex = 0;

    if (onRowClick) {
      a11yProps.onClick = function (event) {
        return onRowClick({ event: event, index: index, rowData: rowData });
      };
    }
    if (onRowDoubleClick) {
      a11yProps.onDoubleClick = function (event) {
        return onRowDoubleClick({ event: event, index: index, rowData: rowData });
      };
    }
    if (onRowMouseOut) {
      a11yProps.onMouseOut = function (event) {
        return onRowMouseOut({ event: event, index: index, rowData: rowData });
      };
    }
    if (onRowMouseOver) {
      a11yProps.onMouseOver = function (event) {
        return onRowMouseOver({ event: event, index: index, rowData: rowData });
      };
    }
    if (onRowRightClick) {
      a11yProps.onContextMenu = function (event) {
        return onRowRightClick({ event: event, index: index, rowData: rowData });
      };
    }
  }

  return __WEBPACK_IMPORTED_MODULE_1_react___default.a.createElement(
    'div',
    __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, a11yProps, {
      className: className,
      key: key,
      role: 'row',
      style: style }),
    columns
  );
}
defaultRowRenderer.propTypes =  false ? null : babelPluginFlowReactPropTypes_proptype_RowRendererParams === __webpack_require__(0).any ? {} : babelPluginFlowReactPropTypes_proptype_RowRendererParams;

/***/ }),
/* 111 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6__defaultHeaderRenderer__ = __webpack_require__(108);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__defaultCellRenderer__ = __webpack_require__(106);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__defaultCellDataGetter__ = __webpack_require__(105);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__SortDirection__ = __webpack_require__(38);











/**
 * Describes the header and cell contents of a table column.
 */

var Column = function (_Component) {
  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_inherits___default()(Column, _Component);

  function Column() {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, Column);

    return __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_possibleConstructorReturn___default()(this, (Column.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(Column)).apply(this, arguments));
  }

  return Column;
}(__WEBPACK_IMPORTED_MODULE_5_react__["Component"]);

Column.defaultProps = {
  cellDataGetter: __WEBPACK_IMPORTED_MODULE_8__defaultCellDataGetter__["a" /* default */],
  cellRenderer: __WEBPACK_IMPORTED_MODULE_7__defaultCellRenderer__["a" /* default */],
  defaultSortDirection: __WEBPACK_IMPORTED_MODULE_9__SortDirection__["a" /* default */].ASC,
  flexGrow: 0,
  flexShrink: 1,
  headerRenderer: __WEBPACK_IMPORTED_MODULE_6__defaultHeaderRenderer__["a" /* default */],
  style: {}
};
/* harmony default export */ __webpack_exports__["a"] = (Column);
Column.propTypes =  true ? {
  /** Optional aria-label value to set on the column header */
  'aria-label': __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.string,

  /**
   * Callback responsible for returning a cell's data, given its :dataKey
   * ({ columnData: any, dataKey: string, rowData: any }): any
   */
  cellDataGetter: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.func,

  /**
   * Callback responsible for rendering a cell's contents.
   * ({ cellData: any, columnData: any, dataKey: string, rowData: any, rowIndex: number }): node
   */
  cellRenderer: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.func,

  /** Optional CSS class to apply to cell */
  className: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.string,

  /** Optional additional data passed to this column's :cellDataGetter */
  columnData: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.object,

  /** Uniquely identifies the row-data attribute corresponding to this cell */
  dataKey: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.any.isRequired,

  /** Optional direction to be used when clicked the first time */
  defaultSortDirection: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.oneOf([__WEBPACK_IMPORTED_MODULE_9__SortDirection__["a" /* default */].ASC, __WEBPACK_IMPORTED_MODULE_9__SortDirection__["a" /* default */].DESC]),

  /** If sort is enabled for the table at large, disable it for this column */
  disableSort: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.bool,

  /** Flex grow style; defaults to 0 */
  flexGrow: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.number,

  /** Flex shrink style; defaults to 1 */
  flexShrink: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.number,

  /** Optional CSS class to apply to this column's header */
  headerClassName: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.string,

  /**
   * Optional callback responsible for rendering a column header contents.
   * ({ columnData: object, dataKey: string, disableSort: boolean, label: node, sortBy: string, sortDirection: string }): PropTypes.node
   */
  headerRenderer: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.func.isRequired,

  /** Optional inline style to apply to this column's header */
  headerStyle: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.object,

  /** Optional id to set on the column header */
  id: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.string,

  /** Header label for this column */
  label: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.node,

  /** Maximum width of column; this property will only be used if :flexGrow is > 0. */
  maxWidth: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.number,

  /** Minimum width of column. */
  minWidth: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.number,

  /** Optional inline style to apply to cell */
  style: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.object,

  /** Flex basis (width) for this column; This value can grow or shrink based on :flexGrow and :flexShrink properties. */
  width: __WEBPACK_IMPORTED_MODULE_4_prop_types___default.a.number.isRequired
} : {};

/***/ }),
/* 112 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export IS_SCROLLING_TIMEOUT */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_react_dom__ = __webpack_require__(25);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_react_dom___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_react_dom__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__utils_onScroll__ = __webpack_require__(264);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__utils_dimensions__ = __webpack_require__(265);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__vendor_detectElementResize__ = __webpack_require__(100);












/**
 * Specifies the number of miliseconds during which to disable pointer events while a scroll is in progress.
 * This improves performance and makes scrolling smoother.
 */
var IS_SCROLLING_TIMEOUT = 150;

var getWindow = function getWindow() {
  return typeof window !== 'undefined' ? window : undefined;
};

var WindowScroller = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(WindowScroller, _React$PureComponent);

  function WindowScroller() {
    var _ref;

    var _temp, _this, _ret;

    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, WindowScroller);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (_ref = WindowScroller.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(WindowScroller)).call.apply(_ref, [this].concat(args))), _this), _this._window = getWindow(), _this._isMounted = false, _this._positionFromTop = 0, _this._positionFromLeft = 0, _this.state = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, Object(__WEBPACK_IMPORTED_MODULE_9__utils_dimensions__["a" /* getDimensions */])(_this.props.scrollElement, _this.props), {
      isScrolling: false,
      scrollLeft: 0,
      scrollTop: 0
    }), _this._registerChild = function (element) {
      if (element && !(element instanceof Element)) {
        console.warn('WindowScroller registerChild expects to be passed Element or null');
      }
      _this._child = element;
      _this.updatePosition();
    }, _this._onChildScroll = function (_ref2) {
      var scrollTop = _ref2.scrollTop;

      if (_this.state.scrollTop === scrollTop) {
        return;
      }

      var scrollElement = _this.props.scrollElement;
      if (scrollElement) {
        if (typeof scrollElement.scrollTo === 'function') {
          scrollElement.scrollTo(0, scrollTop + _this._positionFromTop);
        } else {
          scrollElement.scrollTop = scrollTop + _this._positionFromTop;
        }
      }
    }, _this._registerResizeListener = function (element) {
      if (element === window) {
        window.addEventListener('resize', _this._onResize, false);
      } else {
        _this._detectElementResize.addResizeListener(element, _this._onResize);
      }
    }, _this._unregisterResizeListener = function (element) {
      if (element === window) {
        window.removeEventListener('resize', _this._onResize, false);
      } else if (element) {
        _this._detectElementResize.removeResizeListener(element, _this._onResize);
      }
    }, _this._onResize = function () {
      _this.updatePosition();
    }, _this.__handleWindowScrollEvent = function () {
      if (!_this._isMounted) {
        return;
      }

      var onScroll = _this.props.onScroll;


      var scrollElement = _this.props.scrollElement;
      if (scrollElement) {
        var scrollOffset = Object(__WEBPACK_IMPORTED_MODULE_9__utils_dimensions__["c" /* getScrollOffset */])(scrollElement);
        var _scrollLeft = Math.max(0, scrollOffset.left - _this._positionFromLeft);
        var _scrollTop = Math.max(0, scrollOffset.top - _this._positionFromTop);

        _this.setState({
          isScrolling: true,
          scrollLeft: _scrollLeft,
          scrollTop: _scrollTop
        });

        onScroll({
          scrollLeft: _scrollLeft,
          scrollTop: _scrollTop
        });
      }
    }, _this.__resetIsScrolling = function () {
      _this.setState({
        isScrolling: false
      });
    }, _temp), __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(_this, _ret);
  }

  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(WindowScroller, [{
    key: 'updatePosition',
    value: function updatePosition() {
      var scrollElement = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : this.props.scrollElement;
      var props = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.props;
      var onResize = this.props.onResize;
      var _state = this.state,
          height = _state.height,
          width = _state.width;


      var thisNode = this._child || __WEBPACK_IMPORTED_MODULE_7_react_dom___default.a.findDOMNode(this);
      if (thisNode instanceof Element && scrollElement) {
        var offset = Object(__WEBPACK_IMPORTED_MODULE_9__utils_dimensions__["b" /* getPositionOffset */])(thisNode, scrollElement);
        this._positionFromTop = offset.top;
        this._positionFromLeft = offset.left;
      }

      var dimensions = Object(__WEBPACK_IMPORTED_MODULE_9__utils_dimensions__["a" /* getDimensions */])(scrollElement, props);
      if (height !== dimensions.height || width !== dimensions.width) {
        this.setState({
          height: dimensions.height,
          width: dimensions.width
        });
        onResize({
          height: dimensions.height,
          width: dimensions.width
        });
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      var scrollElement = this.props.scrollElement;

      this._detectElementResize = Object(__WEBPACK_IMPORTED_MODULE_10__vendor_detectElementResize__["a" /* default */])();

      this.updatePosition(scrollElement);

      if (scrollElement) {
        Object(__WEBPACK_IMPORTED_MODULE_8__utils_onScroll__["a" /* registerScrollListener */])(this, scrollElement);
        this._registerResizeListener(scrollElement);
      }

      this._isMounted = true;
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      var scrollElement = this.props.scrollElement;
      var nextScrollElement = nextProps.scrollElement;

      if (scrollElement !== nextScrollElement && scrollElement && nextScrollElement) {
        this.updatePosition(nextScrollElement, nextProps);

        Object(__WEBPACK_IMPORTED_MODULE_8__utils_onScroll__["b" /* unregisterScrollListener */])(this, scrollElement);
        Object(__WEBPACK_IMPORTED_MODULE_8__utils_onScroll__["a" /* registerScrollListener */])(this, nextScrollElement);

        this._unregisterResizeListener(scrollElement);
        this._registerResizeListener(nextScrollElement);
      }
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      var scrollElement = this.props.scrollElement;
      if (scrollElement) {
        Object(__WEBPACK_IMPORTED_MODULE_8__utils_onScroll__["b" /* unregisterScrollListener */])(this, scrollElement);
        this._unregisterResizeListener(scrollElement);
      }

      this._isMounted = false;
    }
  }, {
    key: 'render',
    value: function render() {
      var children = this.props.children;
      var _state2 = this.state,
          isScrolling = _state2.isScrolling,
          scrollTop = _state2.scrollTop,
          scrollLeft = _state2.scrollLeft,
          height = _state2.height,
          width = _state2.width;


      return children({
        onChildScroll: this._onChildScroll,
        registerChild: this._registerChild,
        height: height,
        isScrolling: isScrolling,
        scrollLeft: scrollLeft,
        scrollTop: scrollTop,
        width: width
      });
    }

    // Referenced by utils/onScroll


    // Referenced by utils/onScroll

  }]);

  return WindowScroller;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

WindowScroller.defaultProps = {
  onResize: function onResize() {},
  onScroll: function onScroll() {},
  scrollingResetTimeInterval: IS_SCROLLING_TIMEOUT,
  scrollElement: getWindow(),
  serverHeight: 0,
  serverWidth: 0
};
WindowScroller.propTypes =  false ? null : {
  /**
   * Function responsible for rendering children.
   * This function should implement the following signature:
   * ({ height, isScrolling, scrollLeft, scrollTop, width }) => PropTypes.element
   */
  children: __webpack_require__(0).func.isRequired,


  /** Callback to be invoked on-resize: ({ height, width }) */
  onResize: __webpack_require__(0).func.isRequired,


  /** Callback to be invoked on-scroll: ({ scrollLeft, scrollTop }) */
  onScroll: __webpack_require__(0).func.isRequired,


  /** Element to attach scroll event listeners. Defaults to window. */
  scrollElement: __webpack_require__(0).oneOfType([__webpack_require__(0).any, typeof Element === 'function' ? __webpack_require__(0).instanceOf(Element) : __webpack_require__(0).any]),

  /**
   * Wait this amount of time after the last scroll event before resetting child `pointer-events`.
   */
  scrollingResetTimeInterval: __webpack_require__(0).number.isRequired,


  /** Height used for server-side rendering */
  serverHeight: __webpack_require__(0).number.isRequired,


  /** Width used for server-side rendering */
  serverWidth: __webpack_require__(0).number.isRequired
};
/* harmony default export */ __webpack_exports__["a"] = (WindowScroller);

/***/ }),
/* 113 */
/***/ (function(module, exports, __webpack_require__) {

var freeGlobal = __webpack_require__(275);

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

module.exports = root;


/***/ }),
/* 114 */
/***/ (function(module, exports, __webpack_require__) {

var root = __webpack_require__(113);

/** Built-in value references. */
var Symbol = root.Symbol;

module.exports = Symbol;


/***/ }),
/* 115 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(116);
module.exports = __webpack_require__(282);


/***/ }),
/* 116 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _react = __webpack_require__(1);

var _react2 = _interopRequireDefault(_react);

var _reactDom = __webpack_require__(25);

var _reactDom2 = _interopRequireDefault(_reactDom);

var _reactRedux = __webpack_require__(41);

var _Scan = __webpack_require__(153);

var _Scan2 = _interopRequireDefault(_Scan);

var _configureStore = __webpack_require__(267);

var _configureStore2 = _interopRequireDefault(_configureStore);

var _throttle = __webpack_require__(272);

var _throttle2 = _interopRequireDefault(_throttle);

var _data = __webpack_require__(46);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var store = (0, _configureStore2.default)();

store.subscribe((0, _throttle2.default)(function () {
  var state = store.getState();
  localStorage.setItem(state.interview.id, JSON.stringify(state.interview));
  (0, _data.saveInterview)(state.interview.id, state.interview);
}, 1000));

if (document.getElementById('scan-app')) {
  _reactDom2.default.render(_react2.default.createElement(
    _reactRedux.Provider,
    { store: store },
    _react2.default.createElement(_Scan2.default, null)
  ), document.getElementById('scan-app'));
}

/***/ }),
/* 117 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.3.2
 * react.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var _assign = __webpack_require__(39);
var invariant = __webpack_require__(30);
var emptyObject = __webpack_require__(70);
var warning = __webpack_require__(31);
var emptyFunction = __webpack_require__(32);
var checkPropTypes = __webpack_require__(40);

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.3.2';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol['for']('react.strict_mode') : 0xeacc;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol['for']('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol['for']('react.context') : 0xeace;
var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol['for']('react.async_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol['for']('react.forward_ref') : 0xead0;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

// Relying on the `invariant()` implementation lets us
// have preserve the format and params in the www builds.

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

var didWarnStateUpdateForUnmountedComponent = {};

function warnNoop(publicInstance, callerName) {
  {
    var _constructor = publicInstance.constructor;
    var componentName = _constructor && (_constructor.displayName || _constructor.name) || 'ReactClass';
    var warningKey = componentName + '.' + callerName;
    if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
      return;
    }
    warning(false, "Can't call %s on a component that is not yet mounted. " + 'This is a no-op, but it might indicate a bug in your application. ' + 'Instead, assign to `this.state` directly or define a `state = {};` ' + 'class property with the desired state in the %s component.', callerName, componentName);
    didWarnStateUpdateForUnmountedComponent[warningKey] = true;
  }
}

/**
 * This is the abstract API for an update queue.
 */
var ReactNoopUpdateQueue = {
  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    return false;
  },

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance, callback, callerName) {
    warnNoop(publicInstance, 'forceUpdate');
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {
    warnNoop(publicInstance, 'replaceState');
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} Name of the calling function in the public API.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState, callback, callerName) {
    warnNoop(publicInstance, 'setState');
  }
};

/**
 * Base class helpers for the updating state of a component.
 */
function Component(props, context, updater) {
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

Component.prototype.isReactComponent = {};

/**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */
Component.prototype.setState = function (partialState, callback) {
  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? invariant(false, 'setState(...): takes an object of state variables to update or a function which returns an object of state variables.') : void 0;
  this.updater.enqueueSetState(this, partialState, callback, 'setState');
};

/**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */
Component.prototype.forceUpdate = function (callback) {
  this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
};

/**
 * Deprecated APIs. These APIs used to exist on classic React classes but since
 * we would like to deprecate them, we're not going to move them over to this
 * modern base class. Instead, we define a getter that warns if it's accessed.
 */
{
  var deprecatedAPIs = {
    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']
  };
  var defineDeprecationWarning = function (methodName, info) {
    Object.defineProperty(Component.prototype, methodName, {
      get: function () {
        lowPriorityWarning$1(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);
        return undefined;
      }
    });
  };
  for (var fnName in deprecatedAPIs) {
    if (deprecatedAPIs.hasOwnProperty(fnName)) {
      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    }
  }
}

function ComponentDummy() {}
ComponentDummy.prototype = Component.prototype;

/**
 * Convenience component with default shallow equality check for sCU.
 */
function PureComponent(props, context, updater) {
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  this.updater = updater || ReactNoopUpdateQueue;
}

var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
pureComponentPrototype.constructor = PureComponent;
// Avoid an extra prototype jump for these methods.
_assign(pureComponentPrototype, Component.prototype);
pureComponentPrototype.isPureReactComponent = true;

// an immutable object with a single mutable value
function createRef() {
  var refObject = {
    current: null
  };
  {
    Object.seal(refObject);
  }
  return refObject;
}

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */
var ReactCurrentOwner = {
  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null
};

var hasOwnProperty = Object.prototype.hasOwnProperty;

var RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true
};

var specialPropKeyWarningShown = void 0;
var specialPropRefWarningShown = void 0;

function hasValidRef(config) {
  {
    if (hasOwnProperty.call(config, 'ref')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.ref !== undefined;
}

function hasValidKey(config) {
  {
    if (hasOwnProperty.call(config, 'key')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.key !== undefined;
}

function defineKeyPropWarningGetter(props, displayName) {
  var warnAboutAccessingKey = function () {
    if (!specialPropKeyWarningShown) {
      specialPropKeyWarningShown = true;
      warning(false, '%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingKey.isReactWarning = true;
  Object.defineProperty(props, 'key', {
    get: warnAboutAccessingKey,
    configurable: true
  });
}

function defineRefPropWarningGetter(props, displayName) {
  var warnAboutAccessingRef = function () {
    if (!specialPropRefWarningShown) {
      specialPropRefWarningShown = true;
      warning(false, '%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingRef.isReactWarning = true;
  Object.defineProperty(props, 'ref', {
    get: warnAboutAccessingRef,
    configurable: true
  });
}

/**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, no instanceof check
 * will work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} key
 * @param {string|object} ref
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @param {*} owner
 * @param {*} props
 * @internal
 */
var ReactElement = function (type, key, ref, self, source, owner, props) {
  var element = {
    // This tag allows us to uniquely identify this as a React Element
    $$typeof: REACT_ELEMENT_TYPE,

    // Built-in properties that belong on the element
    type: type,
    key: key,
    ref: ref,
    props: props,

    // Record the component responsible for creating this element.
    _owner: owner
  };

  {
    // The validation flag is currently mutative. We put it on
    // an external backing store so that we can freeze the whole object.
    // This can be replaced with a WeakMap once they are implemented in
    // commonly used development environments.
    element._store = {};

    // To make comparing ReactElements easier for testing purposes, we make
    // the validation flag non-enumerable (where possible, which should
    // include every environment we run tests in), so the test framework
    // ignores it.
    Object.defineProperty(element._store, 'validated', {
      configurable: false,
      enumerable: false,
      writable: true,
      value: false
    });
    // self and source are DEV only properties.
    Object.defineProperty(element, '_self', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: self
    });
    // Two elements created in two different places should be considered
    // equal for testing purposes and therefore we hide it from enumeration.
    Object.defineProperty(element, '_source', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: source
    });
    if (Object.freeze) {
      Object.freeze(element.props);
      Object.freeze(element);
    }
  }

  return element;
};

/**
 * Create and return a new ReactElement of the given type.
 * See https://reactjs.org/docs/react-api.html#createelement
 */
function createElement(type, config, children) {
  var propName = void 0;

  // Reserved names are extracted
  var props = {};

  var key = null;
  var ref = null;
  var self = null;
  var source = null;

  if (config != null) {
    if (hasValidRef(config)) {
      ref = config.ref;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    self = config.__self === undefined ? null : config.__self;
    source = config.__source === undefined ? null : config.__source;
    // Remaining properties are added to a new props object
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    {
      if (Object.freeze) {
        Object.freeze(childArray);
      }
    }
    props.children = childArray;
  }

  // Resolve default props
  if (type && type.defaultProps) {
    var defaultProps = type.defaultProps;
    for (propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }
  }
  {
    if (key || ref) {
      if (typeof props.$$typeof === 'undefined' || props.$$typeof !== REACT_ELEMENT_TYPE) {
        var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;
        if (key) {
          defineKeyPropWarningGetter(props, displayName);
        }
        if (ref) {
          defineRefPropWarningGetter(props, displayName);
        }
      }
    }
  }
  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
}

/**
 * Return a function that produces ReactElements of a given type.
 * See https://reactjs.org/docs/react-api.html#createfactory
 */


function cloneAndReplaceKey(oldElement, newKey) {
  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);

  return newElement;
}

/**
 * Clone and return a new ReactElement using element as the starting point.
 * See https://reactjs.org/docs/react-api.html#cloneelement
 */
function cloneElement(element, config, children) {
  !!(element === null || element === undefined) ? invariant(false, 'React.cloneElement(...): The argument must be a React element, but you passed %s.', element) : void 0;

  var propName = void 0;

  // Original props are copied
  var props = _assign({}, element.props);

  // Reserved names are extracted
  var key = element.key;
  var ref = element.ref;
  // Self is preserved since the owner is preserved.
  var self = element._self;
  // Source is preserved since cloneElement is unlikely to be targeted by a
  // transpiler, and the original source is probably a better indicator of the
  // true owner.
  var source = element._source;

  // Owner will be preserved, unless ref is overridden
  var owner = element._owner;

  if (config != null) {
    if (hasValidRef(config)) {
      // Silently steal the ref from the parent.
      ref = config.ref;
      owner = ReactCurrentOwner.current;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    // Remaining properties override existing props
    var defaultProps = void 0;
    if (element.type && element.type.defaultProps) {
      defaultProps = element.type.defaultProps;
    }
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        if (config[propName] === undefined && defaultProps !== undefined) {
          // Resolve default props
          props[propName] = defaultProps[propName];
        } else {
          props[propName] = config[propName];
        }
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  return ReactElement(element.type, key, ref, self, source, owner, props);
}

/**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a valid component.
 * @final
 */
function isValidElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}

var ReactDebugCurrentFrame = {};

{
  // Component that is being worked on
  ReactDebugCurrentFrame.getCurrentStack = null;

  ReactDebugCurrentFrame.getStackAddendum = function () {
    var impl = ReactDebugCurrentFrame.getCurrentStack;
    if (impl) {
      return impl();
    }
    return null;
  };
}

var SEPARATOR = '.';
var SUBSEPARATOR = ':';

/**
 * Escape and wrap key so it is safe to use as a reactid
 *
 * @param {string} key to be escaped.
 * @return {string} the escaped key.
 */
function escape(key) {
  var escapeRegex = /[=:]/g;
  var escaperLookup = {
    '=': '=0',
    ':': '=2'
  };
  var escapedString = ('' + key).replace(escapeRegex, function (match) {
    return escaperLookup[match];
  });

  return '$' + escapedString;
}

/**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */

var didWarnAboutMaps = false;

var userProvidedKeyEscapeRegex = /\/+/g;
function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');
}

var POOL_SIZE = 10;
var traverseContextPool = [];
function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {
  if (traverseContextPool.length) {
    var traverseContext = traverseContextPool.pop();
    traverseContext.result = mapResult;
    traverseContext.keyPrefix = keyPrefix;
    traverseContext.func = mapFunction;
    traverseContext.context = mapContext;
    traverseContext.count = 0;
    return traverseContext;
  } else {
    return {
      result: mapResult,
      keyPrefix: keyPrefix,
      func: mapFunction,
      context: mapContext,
      count: 0
    };
  }
}

function releaseTraverseContext(traverseContext) {
  traverseContext.result = null;
  traverseContext.keyPrefix = null;
  traverseContext.func = null;
  traverseContext.context = null;
  traverseContext.count = 0;
  if (traverseContextPool.length < POOL_SIZE) {
    traverseContextPool.push(traverseContext);
  }
}

/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
  var type = typeof children;

  if (type === 'undefined' || type === 'boolean') {
    // All of the above are perceived as null.
    children = null;
  }

  var invokeCallback = false;

  if (children === null) {
    invokeCallback = true;
  } else {
    switch (type) {
      case 'string':
      case 'number':
        invokeCallback = true;
        break;
      case 'object':
        switch (children.$$typeof) {
          case REACT_ELEMENT_TYPE:
          case REACT_PORTAL_TYPE:
            invokeCallback = true;
        }
    }
  }

  if (invokeCallback) {
    callback(traverseContext, children,
    // If it's the only child, treat the name as if it was wrapped in an array
    // so that it's consistent if the number of children grows.
    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
    return 1;
  }

  var child = void 0;
  var nextName = void 0;
  var subtreeCount = 0; // Count of children found in the current subtree.
  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      child = children[i];
      nextName = nextNamePrefix + getComponentKey(child, i);
      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
    }
  } else {
    var iteratorFn = getIteratorFn(children);
    if (typeof iteratorFn === 'function') {
      {
        // Warn about using Maps as children
        if (iteratorFn === children.entries) {
          !didWarnAboutMaps ? warning(false, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', ReactDebugCurrentFrame.getStackAddendum()) : void 0;
          didWarnAboutMaps = true;
        }
      }

      var iterator = iteratorFn.call(children);
      var step = void 0;
      var ii = 0;
      while (!(step = iterator.next()).done) {
        child = step.value;
        nextName = nextNamePrefix + getComponentKey(child, ii++);
        subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
      }
    } else if (type === 'object') {
      var addendum = '';
      {
        addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();
      }
      var childrenString = '' + children;
      invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum);
    }
  }

  return subtreeCount;
}

/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildren(children, callback, traverseContext) {
  if (children == null) {
    return 0;
  }

  return traverseAllChildrenImpl(children, '', callback, traverseContext);
}

/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */
function getComponentKey(component, index) {
  // Do some typechecking here since we call this blindly. We want to ensure
  // that we don't block potential future ES APIs.
  if (typeof component === 'object' && component !== null && component.key != null) {
    // Explicit key
    return escape(component.key);
  }
  // Implicit key determined by the index in the set
  return index.toString(36);
}

function forEachSingleChild(bookKeeping, child, name) {
  var func = bookKeeping.func,
      context = bookKeeping.context;

  func.call(context, child, bookKeeping.count++);
}

/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.foreach
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */
function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }
  var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  releaseTraverseContext(traverseContext);
}

function mapSingleChildIntoContext(bookKeeping, child, childKey) {
  var result = bookKeeping.result,
      keyPrefix = bookKeeping.keyPrefix,
      func = bookKeeping.func,
      context = bookKeeping.context;


  var mappedChild = func.call(context, child, bookKeeping.count++);
  if (Array.isArray(mappedChild)) {
    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);
  } else if (mappedChild != null) {
    if (isValidElement(mappedChild)) {
      mappedChild = cloneAndReplaceKey(mappedChild,
      // Keep both the (mapped) and old keys if they differ, just as
      // traverseAllChildren used to do for objects as children
      keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);
    }
    result.push(mappedChild);
  }
}

function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
  var escapedPrefix = '';
  if (prefix != null) {
    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
  }
  var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  releaseTraverseContext(traverseContext);
}

/**
 * Maps children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.map
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */
function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
  return result;
}

/**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.count
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */
function countChildren(children, context) {
  return traverseAllChildren(children, emptyFunction.thatReturnsNull, null);
}

/**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.toarray
 */
function toArray(children) {
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);
  return result;
}

/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.only
 *
 * The current implementation of this function assumes that a single child gets
 * passed without a wrapper, but the purpose of this helper function is to
 * abstract away the particular structure of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactElement} The first and only `ReactElement` contained in the
 * structure.
 */
function onlyChild(children) {
  !isValidElement(children) ? invariant(false, 'React.Children.only expected to receive a single React element child.') : void 0;
  return children;
}

function createContext(defaultValue, calculateChangedBits) {
  if (calculateChangedBits === undefined) {
    calculateChangedBits = null;
  } else {
    {
      !(calculateChangedBits === null || typeof calculateChangedBits === 'function') ? warning(false, 'createContext: Expected the optional second argument to be a ' + 'function. Instead received: %s', calculateChangedBits) : void 0;
    }
  }

  var context = {
    $$typeof: REACT_CONTEXT_TYPE,
    _calculateChangedBits: calculateChangedBits,
    _defaultValue: defaultValue,
    _currentValue: defaultValue,
    _changedBits: 0,
    // These are circular
    Provider: null,
    Consumer: null
  };

  context.Provider = {
    $$typeof: REACT_PROVIDER_TYPE,
    _context: context
  };
  context.Consumer = context;

  {
    context._currentRenderer = null;
  }

  return context;
}

function forwardRef(render) {
  {
    !(typeof render === 'function') ? warning(false, 'forwardRef requires a render function but was given %s.', render === null ? 'null' : typeof render) : void 0;
  }

  return {
    $$typeof: REACT_FORWARD_REF_TYPE,
    render: render
  };
}

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

function isValidElementType(type) {
  return typeof type === 'string' || typeof type === 'function' ||
  // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
  type === REACT_FRAGMENT_TYPE || type === REACT_ASYNC_MODE_TYPE || type === REACT_STRICT_MODE_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE);
}

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  if (typeof type === 'string') {
    return type;
  }
  switch (type) {
    case REACT_FRAGMENT_TYPE:
      return 'ReactFragment';
    case REACT_PORTAL_TYPE:
      return 'ReactPortal';
    case REACT_CALL_TYPE:
      return 'ReactCall';
    case REACT_RETURN_TYPE:
      return 'ReactReturn';
  }
  if (typeof type === 'object' && type !== null) {
    switch (type.$$typeof) {
      case REACT_FORWARD_REF_TYPE:
        var functionName = type.render.displayName || type.render.name || '';
        return functionName !== '' ? 'ForwardRef(' + functionName + ')' : 'ForwardRef';
    }
  }
  return null;
}

/**
 * ReactElementValidator provides a wrapper around a element factory
 * which validates the props passed to the element. This is intended to be
 * used only in DEV and could be replaced by a static type checker for languages
 * that support it.
 */

var currentlyValidatingElement = void 0;
var propTypesMisspellWarningShown = void 0;

var getDisplayName = function () {};
var getStackAddendum = function () {};

{
  currentlyValidatingElement = null;

  propTypesMisspellWarningShown = false;

  getDisplayName = function (element) {
    if (element == null) {
      return '#empty';
    } else if (typeof element === 'string' || typeof element === 'number') {
      return '#text';
    } else if (typeof element.type === 'string') {
      return element.type;
    } else if (element.type === REACT_FRAGMENT_TYPE) {
      return 'React.Fragment';
    } else {
      return element.type.displayName || element.type.name || 'Unknown';
    }
  };

  getStackAddendum = function () {
    var stack = '';
    if (currentlyValidatingElement) {
      var name = getDisplayName(currentlyValidatingElement);
      var owner = currentlyValidatingElement._owner;
      stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner));
    }
    stack += ReactDebugCurrentFrame.getStackAddendum() || '';
    return stack;
  };
}

function getDeclarationErrorAddendum() {
  if (ReactCurrentOwner.current) {
    var name = getComponentName(ReactCurrentOwner.current);
    if (name) {
      return '\n\nCheck the render method of `' + name + '`.';
    }
  }
  return '';
}

function getSourceInfoErrorAddendum(elementProps) {
  if (elementProps !== null && elementProps !== undefined && elementProps.__source !== undefined) {
    var source = elementProps.__source;
    var fileName = source.fileName.replace(/^.*[\\\/]/, '');
    var lineNumber = source.lineNumber;
    return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
  }
  return '';
}

/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */
var ownerHasKeyUseWarning = {};

function getCurrentComponentErrorInfo(parentType) {
  var info = getDeclarationErrorAddendum();

  if (!info) {
    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
    if (parentName) {
      info = '\n\nCheck the top-level render call using <' + parentName + '>.';
    }
  }
  return info;
}

/**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */
function validateExplicitKey(element, parentType) {
  if (!element._store || element._store.validated || element.key != null) {
    return;
  }
  element._store.validated = true;

  var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
  if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

  // Usually the current owner is the offender, but if it accepts children as a
  // property, it may be the creator of the child that's responsible for
  // assigning it a key.
  var childOwner = '';
  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
    // Give the component that originally created this child.
    childOwner = ' It was passed a child from ' + getComponentName(element._owner) + '.';
  }

  currentlyValidatingElement = element;
  {
    warning(false, 'Each child in an array or iterator should have a unique "key" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.%s', currentComponentErrorInfo, childOwner, getStackAddendum());
  }
  currentlyValidatingElement = null;
}

/**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */
function validateChildKeys(node, parentType) {
  if (typeof node !== 'object') {
    return;
  }
  if (Array.isArray(node)) {
    for (var i = 0; i < node.length; i++) {
      var child = node[i];
      if (isValidElement(child)) {
        validateExplicitKey(child, parentType);
      }
    }
  } else if (isValidElement(node)) {
    // This element was passed in a valid location.
    if (node._store) {
      node._store.validated = true;
    }
  } else if (node) {
    var iteratorFn = getIteratorFn(node);
    if (typeof iteratorFn === 'function') {
      // Entry iterators used to provide implicit keys,
      // but now we print a separate warning for them later.
      if (iteratorFn !== node.entries) {
        var iterator = iteratorFn.call(node);
        var step = void 0;
        while (!(step = iterator.next()).done) {
          if (isValidElement(step.value)) {
            validateExplicitKey(step.value, parentType);
          }
        }
      }
    }
  }
}

/**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */
function validatePropTypes(element) {
  var componentClass = element.type;
  if (typeof componentClass !== 'function') {
    return;
  }
  var name = componentClass.displayName || componentClass.name;
  var propTypes = componentClass.propTypes;
  if (propTypes) {
    currentlyValidatingElement = element;
    checkPropTypes(propTypes, element.props, 'prop', name, getStackAddendum);
    currentlyValidatingElement = null;
  } else if (componentClass.PropTypes !== undefined && !propTypesMisspellWarningShown) {
    propTypesMisspellWarningShown = true;
    warning(false, 'Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');
  }
  if (typeof componentClass.getDefaultProps === 'function') {
    !componentClass.getDefaultProps.isReactClassApproved ? warning(false, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.') : void 0;
  }
}

/**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */
function validateFragmentProps(fragment) {
  currentlyValidatingElement = fragment;

  var keys = Object.keys(fragment.props);
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    if (key !== 'children' && key !== 'key') {
      warning(false, 'Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.%s', key, getStackAddendum());
      break;
    }
  }

  if (fragment.ref !== null) {
    warning(false, 'Invalid attribute `ref` supplied to `React.Fragment`.%s', getStackAddendum());
  }

  currentlyValidatingElement = null;
}

function createElementWithValidation(type, props, children) {
  var validType = isValidElementType(type);

  // We warn in this case but don't throw. We expect the element creation to
  // succeed and there will likely be errors in render.
  if (!validType) {
    var info = '';
    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
      info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
    }

    var sourceInfo = getSourceInfoErrorAddendum(props);
    if (sourceInfo) {
      info += sourceInfo;
    } else {
      info += getDeclarationErrorAddendum();
    }

    info += getStackAddendum() || '';

    var typeString = void 0;
    if (type === null) {
      typeString = 'null';
    } else if (Array.isArray(type)) {
      typeString = 'array';
    } else {
      typeString = typeof type;
    }

    warning(false, 'React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);
  }

  var element = createElement.apply(this, arguments);

  // The result can be nullish if a mock or a custom function is used.
  // TODO: Drop this when these are no longer allowed as the type argument.
  if (element == null) {
    return element;
  }

  // Skip key warning if the type isn't valid since our key validation logic
  // doesn't expect a non-string/function type and can throw confusing errors.
  // We don't want exception behavior to differ between dev and prod.
  // (Rendering will throw with a helpful message and as soon as the type is
  // fixed, the key warnings will appear.)
  if (validType) {
    for (var i = 2; i < arguments.length; i++) {
      validateChildKeys(arguments[i], type);
    }
  }

  if (type === REACT_FRAGMENT_TYPE) {
    validateFragmentProps(element);
  } else {
    validatePropTypes(element);
  }

  return element;
}

function createFactoryWithValidation(type) {
  var validatedFactory = createElementWithValidation.bind(null, type);
  validatedFactory.type = type;
  // Legacy hook: remove it
  {
    Object.defineProperty(validatedFactory, 'type', {
      enumerable: false,
      get: function () {
        lowPriorityWarning$1(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');
        Object.defineProperty(this, 'type', {
          value: type
        });
        return type;
      }
    });
  }

  return validatedFactory;
}

function cloneElementWithValidation(element, props, children) {
  var newElement = cloneElement.apply(this, arguments);
  for (var i = 2; i < arguments.length; i++) {
    validateChildKeys(arguments[i], newElement.type);
  }
  validatePropTypes(newElement);
  return newElement;
}

var React = {
  Children: {
    map: mapChildren,
    forEach: forEachChildren,
    count: countChildren,
    toArray: toArray,
    only: onlyChild
  },

  createRef: createRef,
  Component: Component,
  PureComponent: PureComponent,

  createContext: createContext,
  forwardRef: forwardRef,

  Fragment: REACT_FRAGMENT_TYPE,
  StrictMode: REACT_STRICT_MODE_TYPE,
  unstable_AsyncMode: REACT_ASYNC_MODE_TYPE,

  createElement: createElementWithValidation,
  cloneElement: cloneElementWithValidation,
  createFactory: createFactoryWithValidation,
  isValidElement: isValidElement,

  version: ReactVersion,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    ReactCurrentOwner: ReactCurrentOwner,
    // Used by renderers to avoid bundling object-assign twice in UMD bundles:
    assign: _assign
  }
};

{
  _assign(React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, {
    // These should not be included in production.
    ReactDebugCurrentFrame: ReactDebugCurrentFrame,
    // Shim for React DOM 16.0.0 which still destructured (but not used) this.
    // TODO: remove in React 17.0.
    ReactComponentTreeHook: {}
  });
}



var React$2 = Object.freeze({
	default: React
});

var React$3 = ( React$2 && React ) || React$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var react = React$3['default'] ? React$3['default'] : React$3;

module.exports = react;
  })();
}


/***/ }),
/* 118 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.3.2
 * react-dom.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var invariant = __webpack_require__(30);
var React = __webpack_require__(1);
var warning = __webpack_require__(31);
var ExecutionEnvironment = __webpack_require__(119);
var _assign = __webpack_require__(39);
var emptyFunction = __webpack_require__(32);
var checkPropTypes = __webpack_require__(40);
var getActiveElement = __webpack_require__(120);
var shallowEqual = __webpack_require__(121);
var containsNode = __webpack_require__(122);
var emptyObject = __webpack_require__(70);
var hyphenateStyleName = __webpack_require__(125);
var camelizeStyleName = __webpack_require__(127);

// Relying on the `invariant()` implementation lets us
// have preserve the format and params in the www builds.

!React ? invariant(false, 'ReactDOM was loaded before React. Make sure you load the React package before loading ReactDOM.') : void 0;

var invokeGuardedCallback = function (name, func, context, a, b, c, d, e, f) {
  this._hasCaughtError = false;
  this._caughtError = null;
  var funcArgs = Array.prototype.slice.call(arguments, 3);
  try {
    func.apply(context, funcArgs);
  } catch (error) {
    this._caughtError = error;
    this._hasCaughtError = true;
  }
};

{
  // In DEV mode, we swap out invokeGuardedCallback for a special version
  // that plays more nicely with the browser's DevTools. The idea is to preserve
  // "Pause on exceptions" behavior. Because React wraps all user-provided
  // functions in invokeGuardedCallback, and the production version of
  // invokeGuardedCallback uses a try-catch, all user exceptions are treated
  // like caught exceptions, and the DevTools won't pause unless the developer
  // takes the extra step of enabling pause on caught exceptions. This is
  // untintuitive, though, because even though React has caught the error, from
  // the developer's perspective, the error is uncaught.
  //
  // To preserve the expected "Pause on exceptions" behavior, we don't use a
  // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
  // DOM node, and call the user-provided callback from inside an event handler
  // for that fake event. If the callback throws, the error is "captured" using
  // a global event handler. But because the error happens in a different
  // event loop context, it does not interrupt the normal program flow.
  // Effectively, this gives us try-catch behavior without actually using
  // try-catch. Neat!

  // Check that the browser supports the APIs we need to implement our special
  // DEV version of invokeGuardedCallback
  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
    var fakeNode = document.createElement('react');

    var invokeGuardedCallbackDev = function (name, func, context, a, b, c, d, e, f) {
      // If document doesn't exist we know for sure we will crash in this method
      // when we call document.createEvent(). However this can cause confusing
      // errors: https://github.com/facebookincubator/create-react-app/issues/3482
      // So we preemptively throw with a better message instead.
      !(typeof document !== 'undefined') ? invariant(false, 'The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous.') : void 0;
      var evt = document.createEvent('Event');

      // Keeps track of whether the user-provided callback threw an error. We
      // set this to true at the beginning, then set it to false right after
      // calling the function. If the function errors, `didError` will never be
      // set to false. This strategy works even if the browser is flaky and
      // fails to call our global error handler, because it doesn't rely on
      // the error event at all.
      var didError = true;

      // Create an event handler for our fake event. We will synchronously
      // dispatch our fake event using `dispatchEvent`. Inside the handler, we
      // call the user-provided callback.
      var funcArgs = Array.prototype.slice.call(arguments, 3);
      function callCallback() {
        // We immediately remove the callback from event listeners so that
        // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
        // nested call would trigger the fake event handlers of any call higher
        // in the stack.
        fakeNode.removeEventListener(evtType, callCallback, false);
        func.apply(context, funcArgs);
        didError = false;
      }

      // Create a global error event handler. We use this to capture the value
      // that was thrown. It's possible that this error handler will fire more
      // than once; for example, if non-React code also calls `dispatchEvent`
      // and a handler for that event throws. We should be resilient to most of
      // those cases. Even if our error event handler fires more than once, the
      // last error event is always used. If the callback actually does error,
      // we know that the last error event is the correct one, because it's not
      // possible for anything else to have happened in between our callback
      // erroring and the code that follows the `dispatchEvent` call below. If
      // the callback doesn't error, but the error event was fired, we know to
      // ignore it because `didError` will be false, as described above.
      var error = void 0;
      // Use this to track whether the error event is ever called.
      var didSetError = false;
      var isCrossOriginError = false;

      function onError(event) {
        error = event.error;
        didSetError = true;
        if (error === null && event.colno === 0 && event.lineno === 0) {
          isCrossOriginError = true;
        }
      }

      // Create a fake event type.
      var evtType = 'react-' + (name ? name : 'invokeguardedcallback');

      // Attach our event handlers
      window.addEventListener('error', onError);
      fakeNode.addEventListener(evtType, callCallback, false);

      // Synchronously dispatch our fake event. If the user-provided function
      // errors, it will trigger our global error handler.
      evt.initEvent(evtType, false, false);
      fakeNode.dispatchEvent(evt);

      if (didError) {
        if (!didSetError) {
          // The callback errored, but the error event never fired.
          error = new Error('An error was thrown inside one of your components, but React ' + "doesn't know what it was. This is likely due to browser " + 'flakiness. React does its best to preserve the "Pause on ' + 'exceptions" behavior of the DevTools, which requires some ' + "DEV-mode only tricks. It's possible that these don't work in " + 'your browser. Try triggering the error in production mode, ' + 'or switching to a modern browser. If you suspect that this is ' + 'actually an issue with React, please file an issue.');
        } else if (isCrossOriginError) {
          error = new Error("A cross-origin error was thrown. React doesn't have access to " + 'the actual error object in development. ' + 'See https://fb.me/react-crossorigin-error for more information.');
        }
        this._hasCaughtError = true;
        this._caughtError = error;
      } else {
        this._hasCaughtError = false;
        this._caughtError = null;
      }

      // Remove our event listeners
      window.removeEventListener('error', onError);
    };

    invokeGuardedCallback = invokeGuardedCallbackDev;
  }
}

var invokeGuardedCallback$1 = invokeGuardedCallback;

var ReactErrorUtils = {
  // Used by Fiber to simulate a try-catch.
  _caughtError: null,
  _hasCaughtError: false,

  // Used by event system to capture/rethrow the first error.
  _rethrowError: null,
  _hasRethrowError: false,

  /**
   * Call a function while guarding against errors that happens within it.
   * Returns an error if it throws, otherwise null.
   *
   * In production, this is implemented using a try-catch. The reason we don't
   * use a try-catch directly is so that we can swap out a different
   * implementation in DEV mode.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallback: function (name, func, context, a, b, c, d, e, f) {
    invokeGuardedCallback$1.apply(ReactErrorUtils, arguments);
  },

  /**
   * Same as invokeGuardedCallback, but instead of returning an error, it stores
   * it in a global so it can be rethrown by `rethrowCaughtError` later.
   * TODO: See if _caughtError and _rethrowError can be unified.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallbackAndCatchFirstError: function (name, func, context, a, b, c, d, e, f) {
    ReactErrorUtils.invokeGuardedCallback.apply(this, arguments);
    if (ReactErrorUtils.hasCaughtError()) {
      var error = ReactErrorUtils.clearCaughtError();
      if (!ReactErrorUtils._hasRethrowError) {
        ReactErrorUtils._hasRethrowError = true;
        ReactErrorUtils._rethrowError = error;
      }
    }
  },

  /**
   * During execution of guarded functions we will capture the first error which
   * we will rethrow to be handled by the top level error handler.
   */
  rethrowCaughtError: function () {
    return rethrowCaughtError.apply(ReactErrorUtils, arguments);
  },

  hasCaughtError: function () {
    return ReactErrorUtils._hasCaughtError;
  },

  clearCaughtError: function () {
    if (ReactErrorUtils._hasCaughtError) {
      var error = ReactErrorUtils._caughtError;
      ReactErrorUtils._caughtError = null;
      ReactErrorUtils._hasCaughtError = false;
      return error;
    } else {
      invariant(false, 'clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.');
    }
  }
};

var rethrowCaughtError = function () {
  if (ReactErrorUtils._hasRethrowError) {
    var error = ReactErrorUtils._rethrowError;
    ReactErrorUtils._rethrowError = null;
    ReactErrorUtils._hasRethrowError = false;
    throw error;
  }
};

/**
 * Injectable ordering of event plugins.
 */
var eventPluginOrder = null;

/**
 * Injectable mapping from names to event plugin modules.
 */
var namesToPlugins = {};

/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */
function recomputePluginOrdering() {
  if (!eventPluginOrder) {
    // Wait until an `eventPluginOrder` is injected.
    return;
  }
  for (var pluginName in namesToPlugins) {
    var pluginModule = namesToPlugins[pluginName];
    var pluginIndex = eventPluginOrder.indexOf(pluginName);
    !(pluginIndex > -1) ? invariant(false, 'EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `%s`.', pluginName) : void 0;
    if (plugins[pluginIndex]) {
      continue;
    }
    !pluginModule.extractEvents ? invariant(false, 'EventPluginRegistry: Event plugins must implement an `extractEvents` method, but `%s` does not.', pluginName) : void 0;
    plugins[pluginIndex] = pluginModule;
    var publishedEvents = pluginModule.eventTypes;
    for (var eventName in publishedEvents) {
      !publishEventForPlugin(publishedEvents[eventName], pluginModule, eventName) ? invariant(false, 'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.', eventName, pluginName) : void 0;
    }
  }
}

/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */
function publishEventForPlugin(dispatchConfig, pluginModule, eventName) {
  !!eventNameDispatchConfigs.hasOwnProperty(eventName) ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same event name, `%s`.', eventName) : void 0;
  eventNameDispatchConfigs[eventName] = dispatchConfig;

  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(phasedRegistrationName, pluginModule, eventName);
      }
    }
    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(dispatchConfig.registrationName, pluginModule, eventName);
    return true;
  }
  return false;
}

/**
 * Publishes a registration name that is used to identify dispatched events.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */
function publishRegistrationName(registrationName, pluginModule, eventName) {
  !!registrationNameModules[registrationName] ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same registration name, `%s`.', registrationName) : void 0;
  registrationNameModules[registrationName] = pluginModule;
  registrationNameDependencies[registrationName] = pluginModule.eventTypes[eventName].dependencies;

  {
    var lowerCasedName = registrationName.toLowerCase();
    possibleRegistrationNames[lowerCasedName] = registrationName;

    if (registrationName === 'onDoubleClick') {
      possibleRegistrationNames.ondblclick = registrationName;
    }
  }
}

/**
 * Registers plugins so that they can extract and dispatch events.
 *
 * @see {EventPluginHub}
 */

/**
 * Ordered list of injected plugins.
 */
var plugins = [];

/**
 * Mapping from event name to dispatch config
 */
var eventNameDispatchConfigs = {};

/**
 * Mapping from registration name to plugin module
 */
var registrationNameModules = {};

/**
 * Mapping from registration name to event name
 */
var registrationNameDependencies = {};

/**
 * Mapping from lowercase registration names to the properly cased version,
 * used to warn in the case of missing event handlers. Available
 * only in true.
 * @type {Object}
 */
var possibleRegistrationNames = {};
// Trust the developer to only use possibleRegistrationNames in true

/**
 * Injects an ordering of plugins (by plugin name). This allows the ordering
 * to be decoupled from injection of the actual plugins so that ordering is
 * always deterministic regardless of packaging, on-the-fly injection, etc.
 *
 * @param {array} InjectedEventPluginOrder
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginOrder}
 */
function injectEventPluginOrder(injectedEventPluginOrder) {
  !!eventPluginOrder ? invariant(false, 'EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React.') : void 0;
  // Clone the ordering so it cannot be dynamically mutated.
  eventPluginOrder = Array.prototype.slice.call(injectedEventPluginOrder);
  recomputePluginOrdering();
}

/**
 * Injects plugins to be used by `EventPluginHub`. The plugin names must be
 * in the ordering injected by `injectEventPluginOrder`.
 *
 * Plugins can be injected as part of page initialization or on-the-fly.
 *
 * @param {object} injectedNamesToPlugins Map from names to plugin modules.
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginsByName}
 */
function injectEventPluginsByName(injectedNamesToPlugins) {
  var isOrderingDirty = false;
  for (var pluginName in injectedNamesToPlugins) {
    if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
      continue;
    }
    var pluginModule = injectedNamesToPlugins[pluginName];
    if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== pluginModule) {
      !!namesToPlugins[pluginName] ? invariant(false, 'EventPluginRegistry: Cannot inject two different event plugins using the same name, `%s`.', pluginName) : void 0;
      namesToPlugins[pluginName] = pluginModule;
      isOrderingDirty = true;
    }
  }
  if (isOrderingDirty) {
    recomputePluginOrdering();
  }
}

var EventPluginRegistry = Object.freeze({
	plugins: plugins,
	eventNameDispatchConfigs: eventNameDispatchConfigs,
	registrationNameModules: registrationNameModules,
	registrationNameDependencies: registrationNameDependencies,
	possibleRegistrationNames: possibleRegistrationNames,
	injectEventPluginOrder: injectEventPluginOrder,
	injectEventPluginsByName: injectEventPluginsByName
});

var getFiberCurrentPropsFromNode = null;
var getInstanceFromNode = null;
var getNodeFromInstance = null;

var injection$1 = {
  injectComponentTree: function (Injected) {
    getFiberCurrentPropsFromNode = Injected.getFiberCurrentPropsFromNode;
    getInstanceFromNode = Injected.getInstanceFromNode;
    getNodeFromInstance = Injected.getNodeFromInstance;

    {
      !(getNodeFromInstance && getInstanceFromNode) ? warning(false, 'EventPluginUtils.injection.injectComponentTree(...): Injected ' + 'module is missing getNodeFromInstance or getInstanceFromNode.') : void 0;
    }
  }
};






var validateEventDispatches = void 0;
{
  validateEventDispatches = function (event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchInstances = event._dispatchInstances;

    var listenersIsArr = Array.isArray(dispatchListeners);
    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;

    var instancesIsArr = Array.isArray(dispatchInstances);
    var instancesLen = instancesIsArr ? dispatchInstances.length : dispatchInstances ? 1 : 0;

    !(instancesIsArr === listenersIsArr && instancesLen === listenersLen) ? warning(false, 'EventPluginUtils: Invalid `event`.') : void 0;
  };
}

/**
 * Dispatch the event to the listener.
 * @param {SyntheticEvent} event SyntheticEvent to handle
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @param {function} listener Application-level callback
 * @param {*} inst Internal component instance
 */
function executeDispatch(event, simulated, listener, inst) {
  var type = event.type || 'unknown-event';
  event.currentTarget = getNodeFromInstance(inst);
  ReactErrorUtils.invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
  event.currentTarget = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches.
 */
function executeDispatchesInOrder(event, simulated) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchInstances = event._dispatchInstances;
  {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and Instances are two parallel arrays that are always in sync.
      executeDispatch(event, simulated, dispatchListeners[i], dispatchInstances[i]);
    }
  } else if (dispatchListeners) {
    executeDispatch(event, simulated, dispatchListeners, dispatchInstances);
  }
  event._dispatchListeners = null;
  event._dispatchInstances = null;
}

/**
 * @see executeDispatchesInOrderStopAtTrueImpl
 */


/**
 * Execution of a "direct" dispatch - there must be at most one dispatch
 * accumulated on the event or it is considered an error. It doesn't really make
 * sense for an event with multiple dispatches (bubbled) to keep track of the
 * return values at each dispatch execution, but it does tend to make sense when
 * dealing with "direct" dispatches.
 *
 * @return {*} The return value of executing the single dispatch.
 */


/**
 * @param {SyntheticEvent} event
 * @return {boolean} True iff number of dispatches accumulated is greater than 0.
 */

/**
 * Accumulates items that must not be null or undefined into the first one. This
 * is used to conserve memory by avoiding array allocations, and thus sacrifices
 * API cleanness. Since `current` can be null before being passed in and not
 * null after this function, make sure to assign it back to `current`:
 *
 * `a = accumulateInto(a, b);`
 *
 * This API should be sparingly used. Try `accumulate` for something cleaner.
 *
 * @return {*|array<*>} An accumulation of items.
 */

function accumulateInto(current, next) {
  !(next != null) ? invariant(false, 'accumulateInto(...): Accumulated items must not be null or undefined.') : void 0;

  if (current == null) {
    return next;
  }

  // Both are not empty. Warning: Never call x.concat(y) when you are not
  // certain that x is an Array (x could be a string with concat method).
  if (Array.isArray(current)) {
    if (Array.isArray(next)) {
      current.push.apply(current, next);
      return current;
    }
    current.push(next);
    return current;
  }

  if (Array.isArray(next)) {
    // A bit too dangerous to mutate `next`.
    return [current].concat(next);
  }

  return [current, next];
}

/**
 * @param {array} arr an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 * @param {function} cb Callback invoked with each element or a collection.
 * @param {?} [scope] Scope used as `this` in a callback.
 */
function forEachAccumulated(arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
}

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */
var eventQueue = null;

/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @private
 */
var executeDispatchesAndRelease = function (event, simulated) {
  if (event) {
    executeDispatchesInOrder(event, simulated);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};
var executeDispatchesAndReleaseSimulated = function (e) {
  return executeDispatchesAndRelease(e, true);
};
var executeDispatchesAndReleaseTopLevel = function (e) {
  return executeDispatchesAndRelease(e, false);
};

function isInteractive(tag) {
  return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
}

function shouldPreventMouseEvent(name, type, props) {
  switch (name) {
    case 'onClick':
    case 'onClickCapture':
    case 'onDoubleClick':
    case 'onDoubleClickCapture':
    case 'onMouseDown':
    case 'onMouseDownCapture':
    case 'onMouseMove':
    case 'onMouseMoveCapture':
    case 'onMouseUp':
    case 'onMouseUpCapture':
      return !!(props.disabled && isInteractive(type));
    default:
      return false;
  }
}

/**
 * This is a unified interface for event plugins to be installed and configured.
 *
 * Event plugins can implement the following properties:
 *
 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
 *     Required. When a top-level event is fired, this method is expected to
 *     extract synthetic events that will in turn be queued and dispatched.
 *
 *   `eventTypes` {object}
 *     Optional, plugins that fire events must publish a mapping of registration
 *     names that are used to register listeners. Values of this mapping must
 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
 *
 *   `executeDispatch` {function(object, function, string)}
 *     Optional, allows plugins to override how an event gets dispatched. By
 *     default, the listener is simply invoked.
 *
 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
 *
 * @public
 */

/**
 * Methods for injecting dependencies.
 */
var injection = {
  /**
   * @param {array} InjectedEventPluginOrder
   * @public
   */
  injectEventPluginOrder: injectEventPluginOrder,

  /**
   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
   */
  injectEventPluginsByName: injectEventPluginsByName
};

/**
 * @param {object} inst The instance, which is the source of events.
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @return {?function} The stored callback.
 */
function getListener(inst, registrationName) {
  var listener = void 0;

  // TODO: shouldPreventMouseEvent is DOM-specific and definitely should not
  // live here; needs to be moved to a better place soon
  var stateNode = inst.stateNode;
  if (!stateNode) {
    // Work in progress (ex: onload events in incremental mode).
    return null;
  }
  var props = getFiberCurrentPropsFromNode(stateNode);
  if (!props) {
    // Work in progress.
    return null;
  }
  listener = props[registrationName];
  if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
    return null;
  }
  !(!listener || typeof listener === 'function') ? invariant(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener) : void 0;
  return listener;
}

/**
 * Allows registered plugins an opportunity to extract events from top-level
 * native browser events.
 *
 * @return {*} An accumulation of synthetic events.
 * @internal
 */
function extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events = null;
  for (var i = 0; i < plugins.length; i++) {
    // Not every plugin in the ordering may be loaded at runtime.
    var possiblePlugin = plugins[i];
    if (possiblePlugin) {
      var extractedEvents = possiblePlugin.extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
      if (extractedEvents) {
        events = accumulateInto(events, extractedEvents);
      }
    }
  }
  return events;
}

function runEventsInBatch(events, simulated) {
  if (events !== null) {
    eventQueue = accumulateInto(eventQueue, events);
  }

  // Set `eventQueue` to null before processing it so that we can tell if more
  // events get enqueued while processing.
  var processingEventQueue = eventQueue;
  eventQueue = null;

  if (!processingEventQueue) {
    return;
  }

  if (simulated) {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseSimulated);
  } else {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);
  }
  !!eventQueue ? invariant(false, 'processEventQueue(): Additional events were enqueued while processing an event queue. Support for this has not yet been implemented.') : void 0;
  // This would be a good time to rethrow if any of the event handlers threw.
  ReactErrorUtils.rethrowCaughtError();
}

function runExtractedEventsInBatch(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events = extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
  runEventsInBatch(events, false);
}

var EventPluginHub = Object.freeze({
	injection: injection,
	getListener: getListener,
	runEventsInBatch: runEventsInBatch,
	runExtractedEventsInBatch: runExtractedEventsInBatch
});

var IndeterminateComponent = 0; // Before we know whether it is functional or class
var FunctionalComponent = 1;
var ClassComponent = 2;
var HostRoot = 3; // Root of a host tree. Could be nested inside another node.
var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.
var HostComponent = 5;
var HostText = 6;
var CallComponent = 7;
var CallHandlerPhase = 8;
var ReturnComponent = 9;
var Fragment = 10;
var Mode = 11;
var ContextConsumer = 12;
var ContextProvider = 13;
var ForwardRef = 14;

var randomKey = Math.random().toString(36).slice(2);
var internalInstanceKey = '__reactInternalInstance$' + randomKey;
var internalEventHandlersKey = '__reactEventHandlers$' + randomKey;

function precacheFiberNode$1(hostInst, node) {
  node[internalInstanceKey] = hostInst;
}

/**
 * Given a DOM node, return the closest ReactDOMComponent or
 * ReactDOMTextComponent instance ancestor.
 */
function getClosestInstanceFromNode(node) {
  if (node[internalInstanceKey]) {
    return node[internalInstanceKey];
  }

  while (!node[internalInstanceKey]) {
    if (node.parentNode) {
      node = node.parentNode;
    } else {
      // Top of the tree. This node must not be part of a React tree (or is
      // unmounted, potentially).
      return null;
    }
  }

  var inst = node[internalInstanceKey];
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber, this will always be the deepest root.
    return inst;
  }

  return null;
}

/**
 * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
 * instance, or null if the node was not rendered by this React.
 */
function getInstanceFromNode$1(node) {
  var inst = node[internalInstanceKey];
  if (inst) {
    if (inst.tag === HostComponent || inst.tag === HostText) {
      return inst;
    } else {
      return null;
    }
  }
  return null;
}

/**
 * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
 * DOM node.
 */
function getNodeFromInstance$1(inst) {
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber this, is just the state node right now. We assume it will be
    // a host component or host text.
    return inst.stateNode;
  }

  // Without this first invariant, passing a non-DOM-component triggers the next
  // invariant for a missing parent, which is super confusing.
  invariant(false, 'getNodeFromInstance: Invalid argument.');
}

function getFiberCurrentPropsFromNode$1(node) {
  return node[internalEventHandlersKey] || null;
}

function updateFiberProps$1(node, props) {
  node[internalEventHandlersKey] = props;
}

var ReactDOMComponentTree = Object.freeze({
	precacheFiberNode: precacheFiberNode$1,
	getClosestInstanceFromNode: getClosestInstanceFromNode,
	getInstanceFromNode: getInstanceFromNode$1,
	getNodeFromInstance: getNodeFromInstance$1,
	getFiberCurrentPropsFromNode: getFiberCurrentPropsFromNode$1,
	updateFiberProps: updateFiberProps$1
});

function getParent(inst) {
  do {
    inst = inst['return'];
    // TODO: If this is a HostRoot we might want to bail out.
    // That is depending on if we want nested subtrees (layers) to bubble
    // events to their parent. We could also go through parentNode on the
    // host node but that wouldn't work for React Native and doesn't let us
    // do the portal feature.
  } while (inst && inst.tag !== HostComponent);
  if (inst) {
    return inst;
  }
  return null;
}

/**
 * Return the lowest common ancestor of A and B, or null if they are in
 * different trees.
 */
function getLowestCommonAncestor(instA, instB) {
  var depthA = 0;
  for (var tempA = instA; tempA; tempA = getParent(tempA)) {
    depthA++;
  }
  var depthB = 0;
  for (var tempB = instB; tempB; tempB = getParent(tempB)) {
    depthB++;
  }

  // If A is deeper, crawl up.
  while (depthA - depthB > 0) {
    instA = getParent(instA);
    depthA--;
  }

  // If B is deeper, crawl up.
  while (depthB - depthA > 0) {
    instB = getParent(instB);
    depthB--;
  }

  // Walk in lockstep until we find a match.
  var depth = depthA;
  while (depth--) {
    if (instA === instB || instA === instB.alternate) {
      return instA;
    }
    instA = getParent(instA);
    instB = getParent(instB);
  }
  return null;
}

/**
 * Return if A is an ancestor of B.
 */


/**
 * Return the parent instance of the passed-in instance.
 */
function getParentInstance(inst) {
  return getParent(inst);
}

/**
 * Simulates the traversal of a two-phase, capture/bubble event dispatch.
 */
function traverseTwoPhase(inst, fn, arg) {
  var path = [];
  while (inst) {
    path.push(inst);
    inst = getParent(inst);
  }
  var i = void 0;
  for (i = path.length; i-- > 0;) {
    fn(path[i], 'captured', arg);
  }
  for (i = 0; i < path.length; i++) {
    fn(path[i], 'bubbled', arg);
  }
}

/**
 * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
 * should would receive a `mouseEnter` or `mouseLeave` event.
 *
 * Does not invoke the callback on the nearest common ancestor because nothing
 * "entered" or "left" that element.
 */
function traverseEnterLeave(from, to, fn, argFrom, argTo) {
  var common = from && to ? getLowestCommonAncestor(from, to) : null;
  var pathFrom = [];
  while (true) {
    if (!from) {
      break;
    }
    if (from === common) {
      break;
    }
    var alternate = from.alternate;
    if (alternate !== null && alternate === common) {
      break;
    }
    pathFrom.push(from);
    from = getParent(from);
  }
  var pathTo = [];
  while (true) {
    if (!to) {
      break;
    }
    if (to === common) {
      break;
    }
    var _alternate = to.alternate;
    if (_alternate !== null && _alternate === common) {
      break;
    }
    pathTo.push(to);
    to = getParent(to);
  }
  for (var i = 0; i < pathFrom.length; i++) {
    fn(pathFrom[i], 'bubbled', argFrom);
  }
  for (var _i = pathTo.length; _i-- > 0;) {
    fn(pathTo[_i], 'captured', argTo);
  }
}

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(inst, event, propagationPhase) {
  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(inst, registrationName);
}

/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing even a
 * single one.
 */

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */
function accumulateDirectionalDispatches(inst, phase, event) {
  {
    !inst ? warning(false, 'Dispatching inst must not be null') : void 0;
  }
  var listener = listenerAtPhase(inst, event, phase);
  if (listener) {
    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
    event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
  }
}

/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We cannot perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */
function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    traverseTwoPhase(event._targetInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Same as `accumulateTwoPhaseDispatchesSingle`, but skips over the targetID.
 */
function accumulateTwoPhaseDispatchesSingleSkipTarget(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    var targetInst = event._targetInst;
    var parentInst = targetInst ? getParentInstance(targetInst) : null;
    traverseTwoPhase(parentInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */
function accumulateDispatches(inst, ignoredDirection, event) {
  if (inst && event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(inst, registrationName);
    if (listener) {
      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
      event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
    }
  }
}

/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */
function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event._targetInst, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}

function accumulateTwoPhaseDispatchesSkipTarget(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingleSkipTarget);
}

function accumulateEnterLeaveDispatches(leave, enter, from, to) {
  traverseEnterLeave(from, to, accumulateDispatches, leave, enter);
}

function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}

var EventPropagators = Object.freeze({
	accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
	accumulateTwoPhaseDispatchesSkipTarget: accumulateTwoPhaseDispatchesSkipTarget,
	accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches,
	accumulateDirectDispatches: accumulateDirectDispatches
});

var contentKey = null;

/**
 * Gets the key used to access text content on a DOM node.
 *
 * @return {?string} Key used to access text content.
 * @internal
 */
function getTextContentAccessor() {
  if (!contentKey && ExecutionEnvironment.canUseDOM) {
    // Prefer textContent to innerText because many browsers support both but
    // SVG <text> elements don't support innerText even when <div> does.
    contentKey = 'textContent' in document.documentElement ? 'textContent' : 'innerText';
  }
  return contentKey;
}

/**
 * This helper object stores information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 *
 */
var compositionState = {
  _root: null,
  _startText: null,
  _fallbackText: null
};

function initialize(nativeEventTarget) {
  compositionState._root = nativeEventTarget;
  compositionState._startText = getText();
  return true;
}

function reset() {
  compositionState._root = null;
  compositionState._startText = null;
  compositionState._fallbackText = null;
}

function getData() {
  if (compositionState._fallbackText) {
    return compositionState._fallbackText;
  }

  var start = void 0;
  var startValue = compositionState._startText;
  var startLength = startValue.length;
  var end = void 0;
  var endValue = getText();
  var endLength = endValue.length;

  for (start = 0; start < startLength; start++) {
    if (startValue[start] !== endValue[start]) {
      break;
    }
  }

  var minEnd = startLength - start;
  for (end = 1; end <= minEnd; end++) {
    if (startValue[startLength - end] !== endValue[endLength - end]) {
      break;
    }
  }

  var sliceTail = end > 1 ? 1 - end : undefined;
  compositionState._fallbackText = endValue.slice(start, sliceTail);
  return compositionState._fallbackText;
}

function getText() {
  if ('value' in compositionState._root) {
    return compositionState._root.value;
  }
  return compositionState._root[getTextContentAccessor()];
}

/* eslint valid-typeof: 0 */

var didWarnForAddedNewProperty = false;
var EVENT_POOL_SIZE = 10;

var shouldBeReleasedProperties = ['dispatchConfig', '_targetInst', 'nativeEvent', 'isDefaultPrevented', 'isPropagationStopped', '_dispatchListeners', '_dispatchInstances'];

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var EventInterface = {
  type: null,
  target: null,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: emptyFunction.thatReturnsNull,
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function (event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {*} targetInst Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @param {DOMEventTarget} nativeEventTarget Target node.
 */
function SyntheticEvent(dispatchConfig, targetInst, nativeEvent, nativeEventTarget) {
  {
    // these have a getter/setter for warnings
    delete this.nativeEvent;
    delete this.preventDefault;
    delete this.stopPropagation;
  }

  this.dispatchConfig = dispatchConfig;
  this._targetInst = targetInst;
  this.nativeEvent = nativeEvent;

  var Interface = this.constructor.Interface;
  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }
    {
      delete this[propName]; // this has a getter/setter for warnings
    }
    var normalize = Interface[propName];
    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      if (propName === 'target') {
        this.target = nativeEventTarget;
      } else {
        this[propName] = nativeEvent[propName];
      }
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
  if (defaultPrevented) {
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  } else {
    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
  }
  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
  return this;
}

_assign(SyntheticEvent.prototype, {
  preventDefault: function () {
    this.defaultPrevented = true;
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.preventDefault) {
      event.preventDefault();
    } else if (typeof event.returnValue !== 'unknown') {
      event.returnValue = false;
    }
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  },

  stopPropagation: function () {
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.stopPropagation) {
      event.stopPropagation();
    } else if (typeof event.cancelBubble !== 'unknown') {
      // The ChangeEventPlugin registers a "propertychange" event for
      // IE. This event does not support bubbling or cancelling, and
      // any references to cancelBubble throw "Member not found".  A
      // typeof check of "unknown" circumvents this issue (and is also
      // IE specific).
      event.cancelBubble = true;
    }

    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function () {
    this.isPersistent = emptyFunction.thatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: emptyFunction.thatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function () {
    var Interface = this.constructor.Interface;
    for (var propName in Interface) {
      {
        Object.defineProperty(this, propName, getPooledWarningPropertyDefinition(propName, Interface[propName]));
      }
    }
    for (var i = 0; i < shouldBeReleasedProperties.length; i++) {
      this[shouldBeReleasedProperties[i]] = null;
    }
    {
      Object.defineProperty(this, 'nativeEvent', getPooledWarningPropertyDefinition('nativeEvent', null));
      Object.defineProperty(this, 'preventDefault', getPooledWarningPropertyDefinition('preventDefault', emptyFunction));
      Object.defineProperty(this, 'stopPropagation', getPooledWarningPropertyDefinition('stopPropagation', emptyFunction));
    }
  }
});

SyntheticEvent.Interface = EventInterface;

/**
 * Helper to reduce boilerplate when creating subclasses.
 */
SyntheticEvent.extend = function (Interface) {
  var Super = this;

  var E = function () {};
  E.prototype = Super.prototype;
  var prototype = new E();

  function Class() {
    return Super.apply(this, arguments);
  }
  _assign(prototype, Class.prototype);
  Class.prototype = prototype;
  Class.prototype.constructor = Class;

  Class.Interface = _assign({}, Super.Interface, Interface);
  Class.extend = Super.extend;
  addEventPoolingTo(Class);

  return Class;
};

/** Proxying after everything set on SyntheticEvent
 * to resolve Proxy issue on some WebKit browsers
 * in which some Event properties are set to undefined (GH#10010)
 */
{
  var isProxySupported = typeof Proxy === 'function' &&
  // https://github.com/facebook/react/issues/12011
  !Object.isSealed(new Proxy({}, {}));

  if (isProxySupported) {
    /*eslint-disable no-func-assign */
    SyntheticEvent = new Proxy(SyntheticEvent, {
      construct: function (target, args) {
        return this.apply(target, Object.create(target.prototype), args);
      },
      apply: function (constructor, that, args) {
        return new Proxy(constructor.apply(that, args), {
          set: function (target, prop, value) {
            if (prop !== 'isPersistent' && !target.constructor.Interface.hasOwnProperty(prop) && shouldBeReleasedProperties.indexOf(prop) === -1) {
              !(didWarnForAddedNewProperty || target.isPersistent()) ? warning(false, "This synthetic event is reused for performance reasons. If you're " + "seeing this, you're adding a new property in the synthetic event object. " + 'The property is never released. See ' + 'https://fb.me/react-event-pooling for more information.') : void 0;
              didWarnForAddedNewProperty = true;
            }
            target[prop] = value;
            return true;
          }
        });
      }
    });
    /*eslint-enable no-func-assign */
  }
}

addEventPoolingTo(SyntheticEvent);

/**
 * Helper to nullify syntheticEvent instance properties when destructing
 *
 * @param {String} propName
 * @param {?object} getVal
 * @return {object} defineProperty object
 */
function getPooledWarningPropertyDefinition(propName, getVal) {
  var isFunction = typeof getVal === 'function';
  return {
    configurable: true,
    set: set,
    get: get
  };

  function set(val) {
    var action = isFunction ? 'setting the method' : 'setting the property';
    warn(action, 'This is effectively a no-op');
    return val;
  }

  function get() {
    var action = isFunction ? 'accessing the method' : 'accessing the property';
    var result = isFunction ? 'This is a no-op function' : 'This is set to null';
    warn(action, result);
    return getVal;
  }

  function warn(action, result) {
    var warningCondition = false;
    !warningCondition ? warning(false, "This synthetic event is reused for performance reasons. If you're seeing this, " + "you're %s `%s` on a released/nullified synthetic event. %s. " + 'If you must keep the original synthetic event around, use event.persist(). ' + 'See https://fb.me/react-event-pooling for more information.', action, propName, result) : void 0;
  }
}

function getPooledEvent(dispatchConfig, targetInst, nativeEvent, nativeInst) {
  var EventConstructor = this;
  if (EventConstructor.eventPool.length) {
    var instance = EventConstructor.eventPool.pop();
    EventConstructor.call(instance, dispatchConfig, targetInst, nativeEvent, nativeInst);
    return instance;
  }
  return new EventConstructor(dispatchConfig, targetInst, nativeEvent, nativeInst);
}

function releasePooledEvent(event) {
  var EventConstructor = this;
  !(event instanceof EventConstructor) ? invariant(false, 'Trying to release an event instance  into a pool of a different type.') : void 0;
  event.destructor();
  if (EventConstructor.eventPool.length < EVENT_POOL_SIZE) {
    EventConstructor.eventPool.push(event);
  }
}

function addEventPoolingTo(EventConstructor) {
  EventConstructor.eventPool = [];
  EventConstructor.getPooled = getPooledEvent;
  EventConstructor.release = releasePooledEvent;
}

var SyntheticEvent$1 = SyntheticEvent;

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */
var SyntheticCompositionEvent = SyntheticEvent$1.extend({
  data: null
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */
var SyntheticInputEvent = SyntheticEvent$1.extend({
  data: null
});

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
var START_KEYCODE = 229;

var canUseCompositionEvent = ExecutionEnvironment.canUseDOM && 'CompositionEvent' in window;

var documentMode = null;
if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
  documentMode = document.documentMode;
}

// Webkit offers a very useful `textInput` event that can be used to
// directly represent `beforeInput`. The IE `textinput` event is not as
// useful, so we don't use it.
var canUseTextInputEvent = ExecutionEnvironment.canUseDOM && 'TextEvent' in window && !documentMode;

// In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. Japanese ideographic
// spaces, for instance (\u3000) are not recorded correctly.
var useFallbackCompositionData = ExecutionEnvironment.canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);

var SPACEBAR_CODE = 32;
var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

// Events and their corresponding property names.
var eventTypes = {
  beforeInput: {
    phasedRegistrationNames: {
      bubbled: 'onBeforeInput',
      captured: 'onBeforeInputCapture'
    },
    dependencies: ['topCompositionEnd', 'topKeyPress', 'topTextInput', 'topPaste']
  },
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionEnd',
      captured: 'onCompositionEndCapture'
    },
    dependencies: ['topBlur', 'topCompositionEnd', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionStart',
      captured: 'onCompositionStartCapture'
    },
    dependencies: ['topBlur', 'topCompositionStart', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionUpdate',
      captured: 'onCompositionUpdateCapture'
    },
    dependencies: ['topBlur', 'topCompositionUpdate', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  }
};

// Track whether we've ever handled a keypress on the space key.
var hasSpaceKeypress = false;

/**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */
function isKeypressCommand(nativeEvent) {
  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
  // ctrlKey && altKey is equivalent to AltGr, and is not a command.
  !(nativeEvent.ctrlKey && nativeEvent.altKey);
}

/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */
function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case 'topCompositionStart':
      return eventTypes.compositionStart;
    case 'topCompositionEnd':
      return eventTypes.compositionEnd;
    case 'topCompositionUpdate':
      return eventTypes.compositionUpdate;
  }
}

/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionStart(topLevelType, nativeEvent) {
  return topLevelType === 'topKeyDown' && nativeEvent.keyCode === START_KEYCODE;
}

/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topKeyUp':
      // Command keys insert or clear IME input.
      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
    case 'topKeyDown':
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return nativeEvent.keyCode !== START_KEYCODE;
    case 'topKeyPress':
    case 'topMouseDown':
    case 'topBlur':
      // Events are not possible without cancelling IME.
      return true;
    default:
      return false;
  }
}

/**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */
function getDataFromCustomEvent(nativeEvent) {
  var detail = nativeEvent.detail;
  if (typeof detail === 'object' && 'data' in detail) {
    return detail.data;
  }
  return null;
}

// Track the current IME composition status, if any.
var isComposing = false;

/**
 * @return {?object} A SyntheticCompositionEvent.
 */
function extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var eventType = void 0;
  var fallbackData = void 0;

  if (canUseCompositionEvent) {
    eventType = getCompositionEventType(topLevelType);
  } else if (!isComposing) {
    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionStart;
    }
  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
    eventType = eventTypes.compositionEnd;
  }

  if (!eventType) {
    return null;
  }

  if (useFallbackCompositionData) {
    // The current composition is stored statically and must not be
    // overwritten while composition continues.
    if (!isComposing && eventType === eventTypes.compositionStart) {
      isComposing = initialize(nativeEventTarget);
    } else if (eventType === eventTypes.compositionEnd) {
      if (isComposing) {
        fallbackData = getData();
      }
    }
  }

  var event = SyntheticCompositionEvent.getPooled(eventType, targetInst, nativeEvent, nativeEventTarget);

  if (fallbackData) {
    // Inject data generated from fallback path into the synthetic event.
    // This matches the property of native CompositionEventInterface.
    event.data = fallbackData;
  } else {
    var customData = getDataFromCustomEvent(nativeEvent);
    if (customData !== null) {
      event.data = customData;
    }
  }

  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * @param {TopLevelTypes} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The string corresponding to this `beforeInput` event.
 */
function getNativeBeforeInputChars(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topCompositionEnd':
      return getDataFromCustomEvent(nativeEvent);
    case 'topKeyPress':
      /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */
      var which = nativeEvent.which;
      if (which !== SPACEBAR_CODE) {
        return null;
      }

      hasSpaceKeypress = true;
      return SPACEBAR_CHAR;

    case 'topTextInput':
      // Record the characters to be added to the DOM.
      var chars = nativeEvent.data;

      // If it's a spacebar character, assume that we have already handled
      // it at the keypress level and bail immediately. Android Chrome
      // doesn't give us keycodes, so we need to blacklist it.
      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
        return null;
      }

      return chars;

    default:
      // For other native event types, do nothing.
      return null;
  }
}

/**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The fallback string for this `beforeInput` event.
 */
function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
  // If we are currently composing (IME) and using a fallback to do so,
  // try to extract the composed characters from the fallback object.
  // If composition event is available, we extract a string only at
  // compositionevent, otherwise extract it at fallback events.
  if (isComposing) {
    if (topLevelType === 'topCompositionEnd' || !canUseCompositionEvent && isFallbackCompositionEnd(topLevelType, nativeEvent)) {
      var chars = getData();
      reset();
      isComposing = false;
      return chars;
    }
    return null;
  }

  switch (topLevelType) {
    case 'topPaste':
      // If a paste event occurs after a keypress, throw out the input
      // chars. Paste events should not lead to BeforeInput events.
      return null;
    case 'topKeyPress':
      /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */
      if (!isKeypressCommand(nativeEvent)) {
        // IE fires the `keypress` event when a user types an emoji via
        // Touch keyboard of Windows.  In such a case, the `char` property
        // holds an emoji character like `\uD83D\uDE0A`.  Because its length
        // is 2, the property `which` does not represent an emoji correctly.
        // In such a case, we directly return the `char` property instead of
        // using `which`.
        if (nativeEvent.char && nativeEvent.char.length > 1) {
          return nativeEvent.char;
        } else if (nativeEvent.which) {
          return String.fromCharCode(nativeEvent.which);
        }
      }
      return null;
    case 'topCompositionEnd':
      return useFallbackCompositionData ? null : nativeEvent.data;
    default:
      return null;
  }
}

/**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @return {?object} A SyntheticInputEvent.
 */
function extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var chars = void 0;

  if (canUseTextInputEvent) {
    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
  } else {
    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
  }

  // If no characters are being inserted, no BeforeInput event should
  // be fired.
  if (!chars) {
    return null;
  }

  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, targetInst, nativeEvent, nativeEventTarget);

  event.data = chars;
  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */
var BeforeInputEventPlugin = {
  eventTypes: eventTypes,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var composition = extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);

    var beforeInput = extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);

    if (composition === null) {
      return beforeInput;
    }

    if (beforeInput === null) {
      return composition;
    }

    return [composition, beforeInput];
  }
};

// Use to restore controlled state after a change event has fired.

var fiberHostComponent = null;

var ReactControlledComponentInjection = {
  injectFiberControlledHostComponent: function (hostComponentImpl) {
    // The fiber implementation doesn't use dynamic dispatch so we need to
    // inject the implementation.
    fiberHostComponent = hostComponentImpl;
  }
};

var restoreTarget = null;
var restoreQueue = null;

function restoreStateOfTarget(target) {
  // We perform this translation at the end of the event loop so that we
  // always receive the correct fiber here
  var internalInstance = getInstanceFromNode(target);
  if (!internalInstance) {
    // Unmounted
    return;
  }
  !(fiberHostComponent && typeof fiberHostComponent.restoreControlledState === 'function') ? invariant(false, 'Fiber needs to be injected to handle a fiber target for controlled events. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  var props = getFiberCurrentPropsFromNode(internalInstance.stateNode);
  fiberHostComponent.restoreControlledState(internalInstance.stateNode, internalInstance.type, props);
}

var injection$2 = ReactControlledComponentInjection;

function enqueueStateRestore(target) {
  if (restoreTarget) {
    if (restoreQueue) {
      restoreQueue.push(target);
    } else {
      restoreQueue = [target];
    }
  } else {
    restoreTarget = target;
  }
}

function needsStateRestore() {
  return restoreTarget !== null || restoreQueue !== null;
}

function restoreStateIfNeeded() {
  if (!restoreTarget) {
    return;
  }
  var target = restoreTarget;
  var queuedTargets = restoreQueue;
  restoreTarget = null;
  restoreQueue = null;

  restoreStateOfTarget(target);
  if (queuedTargets) {
    for (var i = 0; i < queuedTargets.length; i++) {
      restoreStateOfTarget(queuedTargets[i]);
    }
  }
}

var ReactControlledComponent = Object.freeze({
	injection: injection$2,
	enqueueStateRestore: enqueueStateRestore,
	needsStateRestore: needsStateRestore,
	restoreStateIfNeeded: restoreStateIfNeeded
});

// Used as a way to call batchedUpdates when we don't have a reference to
// the renderer. Such as when we're dispatching events or if third party
// libraries need to call batchedUpdates. Eventually, this API will go away when
// everything is batched by default. We'll then have a similar API to opt-out of
// scheduled work and instead do synchronous work.

// Defaults
var _batchedUpdates = function (fn, bookkeeping) {
  return fn(bookkeeping);
};
var _interactiveUpdates = function (fn, a, b) {
  return fn(a, b);
};
var _flushInteractiveUpdates = function () {};

var isBatching = false;
function batchedUpdates(fn, bookkeeping) {
  if (isBatching) {
    // If we are currently inside another batch, we need to wait until it
    // fully completes before restoring state.
    return fn(bookkeeping);
  }
  isBatching = true;
  try {
    return _batchedUpdates(fn, bookkeeping);
  } finally {
    // Here we wait until all updates have propagated, which is important
    // when using controlled components within layers:
    // https://github.com/facebook/react/issues/1698
    // Then we restore state of any controlled component.
    isBatching = false;
    var controlledComponentsHavePendingUpdates = needsStateRestore();
    if (controlledComponentsHavePendingUpdates) {
      // If a controlled event was fired, we may need to restore the state of
      // the DOM node back to the controlled value. This is necessary when React
      // bails out of the update without touching the DOM.
      _flushInteractiveUpdates();
      restoreStateIfNeeded();
    }
  }
}

function interactiveUpdates(fn, a, b) {
  return _interactiveUpdates(fn, a, b);
}



var injection$3 = {
  injectRenderer: function (renderer) {
    _batchedUpdates = renderer.batchedUpdates;
    _interactiveUpdates = renderer.interactiveUpdates;
    _flushInteractiveUpdates = renderer.flushInteractiveUpdates;
  }
};

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  color: true,
  date: true,
  datetime: true,
  'datetime-local': true,
  email: true,
  month: true,
  number: true,
  password: true,
  range: true,
  search: true,
  tel: true,
  text: true,
  time: true,
  url: true,
  week: true
};

function isTextInputElement(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();

  if (nodeName === 'input') {
    return !!supportedInputTypes[elem.type];
  }

  if (nodeName === 'textarea') {
    return true;
  }

  return false;
}

/**
 * HTML nodeType values that represent the type of the node
 */

var ELEMENT_NODE = 1;
var TEXT_NODE = 3;
var COMMENT_NODE = 8;
var DOCUMENT_NODE = 9;
var DOCUMENT_FRAGMENT_NODE = 11;

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */
function getEventTarget(nativeEvent) {
  var target = nativeEvent.target || window;

  // Normalize SVG <use> element events #4963
  if (target.correspondingUseElement) {
    target = target.correspondingUseElement;
  }

  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html
  return target.nodeType === TEXT_NODE ? target.parentNode : target;
}

/**
 * Checks if an event is supported in the current execution environment.
 *
 * NOTE: This will not work correctly for non-generic events such as `change`,
 * `reset`, `load`, `error`, and `select`.
 *
 * Borrows from Modernizr.
 *
 * @param {string} eventNameSuffix Event name, e.g. "click".
 * @param {?boolean} capture Check if the capture phase is supported.
 * @return {boolean} True if the event is supported.
 * @internal
 * @license Modernizr 3.0.0pre (Custom Build) | MIT
 */
function isEventSupported(eventNameSuffix, capture) {
  if (!ExecutionEnvironment.canUseDOM || capture && !('addEventListener' in document)) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = eventName in document;

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  return isSupported;
}

function isCheckable(elem) {
  var type = elem.type;
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
}

function getTracker(node) {
  return node._valueTracker;
}

function detachTracker(node) {
  node._valueTracker = null;
}

function getValueFromNode(node) {
  var value = '';
  if (!node) {
    return value;
  }

  if (isCheckable(node)) {
    value = node.checked ? 'true' : 'false';
  } else {
    value = node.value;
  }

  return value;
}

function trackValueOnNode(node) {
  var valueField = isCheckable(node) ? 'checked' : 'value';
  var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);

  var currentValue = '' + node[valueField];

  // if someone has already defined a value or Safari, then bail
  // and don't track value will cause over reporting of changes,
  // but it's better then a hard failure
  // (needed for certain tests that spyOn input values and Safari)
  if (node.hasOwnProperty(valueField) || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') {
    return;
  }

  Object.defineProperty(node, valueField, {
    configurable: true,
    get: function () {
      return descriptor.get.call(this);
    },
    set: function (value) {
      currentValue = '' + value;
      descriptor.set.call(this, value);
    }
  });
  // We could've passed this the first time
  // but it triggers a bug in IE11 and Edge 14/15.
  // Calling defineProperty() again should be equivalent.
  // https://github.com/facebook/react/issues/11768
  Object.defineProperty(node, valueField, {
    enumerable: descriptor.enumerable
  });

  var tracker = {
    getValue: function () {
      return currentValue;
    },
    setValue: function (value) {
      currentValue = '' + value;
    },
    stopTracking: function () {
      detachTracker(node);
      delete node[valueField];
    }
  };
  return tracker;
}

function track(node) {
  if (getTracker(node)) {
    return;
  }

  // TODO: Once it's just Fiber we can move this to node._wrapperState
  node._valueTracker = trackValueOnNode(node);
}

function updateValueIfChanged(node) {
  if (!node) {
    return false;
  }

  var tracker = getTracker(node);
  // if there is no tracker at this point it's unlikely
  // that trying again will succeed
  if (!tracker) {
    return true;
  }

  var lastValue = tracker.getValue();
  var nextValue = getValueFromNode(node);
  if (nextValue !== lastValue) {
    tracker.setValue(nextValue);
    return true;
  }
  return false;
}

var ReactInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;

var ReactCurrentOwner = ReactInternals.ReactCurrentOwner;
var ReactDebugCurrentFrame = ReactInternals.ReactDebugCurrentFrame;

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol['for']('react.strict_mode') : 0xeacc;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol['for']('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol['for']('react.context') : 0xeace;
var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol['for']('react.async_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol['for']('react.forward_ref') : 0xead0;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  if (typeof type === 'string') {
    return type;
  }
  switch (type) {
    case REACT_FRAGMENT_TYPE:
      return 'ReactFragment';
    case REACT_PORTAL_TYPE:
      return 'ReactPortal';
    case REACT_CALL_TYPE:
      return 'ReactCall';
    case REACT_RETURN_TYPE:
      return 'ReactReturn';
  }
  if (typeof type === 'object' && type !== null) {
    switch (type.$$typeof) {
      case REACT_FORWARD_REF_TYPE:
        var functionName = type.render.displayName || type.render.name || '';
        return functionName !== '' ? 'ForwardRef(' + functionName + ')' : 'ForwardRef';
    }
  }
  return null;
}

function describeFiber(fiber) {
  switch (fiber.tag) {
    case IndeterminateComponent:
    case FunctionalComponent:
    case ClassComponent:
    case HostComponent:
      var owner = fiber._debugOwner;
      var source = fiber._debugSource;
      var name = getComponentName(fiber);
      var ownerName = null;
      if (owner) {
        ownerName = getComponentName(owner);
      }
      return describeComponentFrame(name, source, ownerName);
    default:
      return '';
  }
}

// This function can only be called with a work-in-progress fiber and
// only during begin or complete phase. Do not call it under any other
// circumstances.
function getStackAddendumByWorkInProgressFiber(workInProgress) {
  var info = '';
  var node = workInProgress;
  do {
    info += describeFiber(node);
    // Otherwise this return pointer might point to the wrong tree:
    node = node['return'];
  } while (node);
  return info;
}

function getCurrentFiberOwnerName$1() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    var owner = fiber._debugOwner;
    if (owner !== null && typeof owner !== 'undefined') {
      return getComponentName(owner);
    }
  }
  return null;
}

function getCurrentFiberStackAddendum$1() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    // Safe because if current fiber exists, we are reconciling,
    // and it is guaranteed to be the work-in-progress version.
    return getStackAddendumByWorkInProgressFiber(fiber);
  }
  return null;
}

function resetCurrentFiber() {
  ReactDebugCurrentFrame.getCurrentStack = null;
  ReactDebugCurrentFiber.current = null;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentFiber(fiber) {
  ReactDebugCurrentFrame.getCurrentStack = getCurrentFiberStackAddendum$1;
  ReactDebugCurrentFiber.current = fiber;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentPhase(phase) {
  ReactDebugCurrentFiber.phase = phase;
}

var ReactDebugCurrentFiber = {
  current: null,
  phase: null,
  resetCurrentFiber: resetCurrentFiber,
  setCurrentFiber: setCurrentFiber,
  setCurrentPhase: setCurrentPhase,
  getCurrentFiberOwnerName: getCurrentFiberOwnerName$1,
  getCurrentFiberStackAddendum: getCurrentFiberStackAddendum$1
};

// A reserved attribute.
// It is handled by React separately and shouldn't be written to the DOM.
var RESERVED = 0;

// A simple string attribute.
// Attributes that aren't in the whitelist are presumed to have this type.
var STRING = 1;

// A string attribute that accepts booleans in React. In HTML, these are called
// "enumerated" attributes with "true" and "false" as possible values.
// When true, it should be set to a "true" string.
// When false, it should be set to a "false" string.
var BOOLEANISH_STRING = 2;

// A real boolean attribute.
// When true, it should be present (set either to an empty string or its name).
// When false, it should be omitted.
var BOOLEAN = 3;

// An attribute that can be used as a flag as well as with a value.
// When true, it should be present (set either to an empty string or its name).
// When false, it should be omitted.
// For any other value, should be present with that value.
var OVERLOADED_BOOLEAN = 4;

// An attribute that must be numeric or parse as a numeric.
// When falsy, it should be removed.
var NUMERIC = 5;

// An attribute that must be positive numeric or parse as a positive numeric.
// When falsy, it should be removed.
var POSITIVE_NUMERIC = 6;

/* eslint-disable max-len */
var ATTRIBUTE_NAME_START_CHAR = ':A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD';
/* eslint-enable max-len */
var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + '\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040';


var ROOT_ATTRIBUTE_NAME = 'data-reactroot';
var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');

var illegalAttributeNameCache = {};
var validatedAttributeNameCache = {};

function isAttributeNameSafe(attributeName) {
  if (validatedAttributeNameCache.hasOwnProperty(attributeName)) {
    return true;
  }
  if (illegalAttributeNameCache.hasOwnProperty(attributeName)) {
    return false;
  }
  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
    validatedAttributeNameCache[attributeName] = true;
    return true;
  }
  illegalAttributeNameCache[attributeName] = true;
  {
    warning(false, 'Invalid attribute name: `%s`', attributeName);
  }
  return false;
}

function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
  if (propertyInfo !== null) {
    return propertyInfo.type === RESERVED;
  }
  if (isCustomComponentTag) {
    return false;
  }
  if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) {
    return true;
  }
  return false;
}

function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
  if (propertyInfo !== null && propertyInfo.type === RESERVED) {
    return false;
  }
  switch (typeof value) {
    case 'function':
    // $FlowIssue symbol is perfectly valid here
    case 'symbol':
      // eslint-disable-line
      return true;
    case 'boolean':
      {
        if (isCustomComponentTag) {
          return false;
        }
        if (propertyInfo !== null) {
          return !propertyInfo.acceptsBooleans;
        } else {
          var prefix = name.toLowerCase().slice(0, 5);
          return prefix !== 'data-' && prefix !== 'aria-';
        }
      }
    default:
      return false;
  }
}

function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
  if (value === null || typeof value === 'undefined') {
    return true;
  }
  if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) {
    return true;
  }
  if (propertyInfo !== null) {
    switch (propertyInfo.type) {
      case BOOLEAN:
        return !value;
      case OVERLOADED_BOOLEAN:
        return value === false;
      case NUMERIC:
        return isNaN(value);
      case POSITIVE_NUMERIC:
        return isNaN(value) || value < 1;
    }
  }
  return false;
}

function getPropertyInfo(name) {
  return properties.hasOwnProperty(name) ? properties[name] : null;
}

function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace) {
  this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
  this.attributeName = attributeName;
  this.attributeNamespace = attributeNamespace;
  this.mustUseProperty = mustUseProperty;
  this.propertyName = name;
  this.type = type;
}

// When adding attributes to this list, be sure to also add them to
// the `possibleStandardNames` module to ensure casing and incorrect
// name warnings.
var properties = {};

// These props are reserved by React. They shouldn't be written to the DOM.
['children', 'dangerouslySetInnerHTML',
// TODO: This prevents the assignment of defaultValue to regular
// elements (not just inputs). Now that ReactDOMInput assigns to the
// defaultValue property -- do we need this?
'defaultValue', 'defaultChecked', 'innerHTML', 'suppressContentEditableWarning', 'suppressHydrationWarning', 'style'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, RESERVED, false, // mustUseProperty
  name, // attributeName
  null);
});

// A few React string attributes have a different name.
// This is a mapping from React prop names to the attribute names.
[['acceptCharset', 'accept-charset'], ['className', 'class'], ['htmlFor', 'for'], ['httpEquiv', 'http-equiv']].forEach(function (_ref) {
  var name = _ref[0],
      attributeName = _ref[1];

  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, // attributeName
  null);
});

// These are "enumerated" HTML attributes that accept "true" and "false".
// In React, we let users pass `true` and `false` even though technically
// these aren't boolean attributes (they are coerced to strings).
['contentEditable', 'draggable', 'spellCheck', 'value'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

// These are "enumerated" SVG attributes that accept "true" and "false".
// In React, we let users pass `true` and `false` even though technically
// these aren't boolean attributes (they are coerced to strings).
// Since these are SVG attributes, their attribute names are case-sensitive.
['autoReverse', 'externalResourcesRequired', 'preserveAlpha'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
  name, // attributeName
  null);
});

// These are HTML boolean attributes.
['allowFullScreen', 'async',
// Note: there is a special case that prevents it from being written to the DOM
// on the client side because the browsers are inconsistent. Instead we call focus().
'autoFocus', 'autoPlay', 'controls', 'default', 'defer', 'disabled', 'formNoValidate', 'hidden', 'loop', 'noModule', 'noValidate', 'open', 'playsInline', 'readOnly', 'required', 'reversed', 'scoped', 'seamless',
// Microdata
'itemScope'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEAN, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

// These are the few React props that we set as DOM properties
// rather than attributes. These are all booleans.
['checked',
// Note: `option.selected` is not updated if `select.multiple` is
// disabled with `removeAttribute`. We have special logic for handling this.
'multiple', 'muted', 'selected'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEAN, true, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

// These are HTML attributes that are "overloaded booleans": they behave like
// booleans, but can also accept a string value.
['capture', 'download'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, OVERLOADED_BOOLEAN, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

// These are HTML attributes that must be positive numbers.
['cols', 'rows', 'size', 'span'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, POSITIVE_NUMERIC, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

// These are HTML attributes that must be numbers.
['rowSpan', 'start'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, NUMERIC, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null);
});

var CAMELIZE = /[\-\:]([a-z])/g;
var capitalize = function (token) {
  return token[1].toUpperCase();
};

// This is a list of all SVG attributes that need special casing, namespacing,
// or boolean value assignment. Regular attributes that just accept strings
// and have the same names are omitted, just like in the HTML whitelist.
// Some of these attributes can be hard to find. This list was created by
// scrapping the MDN documentation.
['accent-height', 'alignment-baseline', 'arabic-form', 'baseline-shift', 'cap-height', 'clip-path', 'clip-rule', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'dominant-baseline', 'enable-background', 'fill-opacity', 'fill-rule', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'glyph-name', 'glyph-orientation-horizontal', 'glyph-orientation-vertical', 'horiz-adv-x', 'horiz-origin-x', 'image-rendering', 'letter-spacing', 'lighting-color', 'marker-end', 'marker-mid', 'marker-start', 'overline-position', 'overline-thickness', 'paint-order', 'panose-1', 'pointer-events', 'rendering-intent', 'shape-rendering', 'stop-color', 'stop-opacity', 'strikethrough-position', 'strikethrough-thickness', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke-width', 'text-anchor', 'text-decoration', 'text-rendering', 'underline-position', 'underline-thickness', 'unicode-bidi', 'unicode-range', 'units-per-em', 'v-alphabetic', 'v-hanging', 'v-ideographic', 'v-mathematical', 'vector-effect', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'word-spacing', 'writing-mode', 'xmlns:xlink', 'x-height'].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, null);
});

// String SVG attributes with the xlink namespace.
['xlink:actuate', 'xlink:arcrole', 'xlink:href', 'xlink:role', 'xlink:show', 'xlink:title', 'xlink:type'].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, 'http://www.w3.org/1999/xlink');
});

// String SVG attributes with the xml namespace.
['xml:base', 'xml:lang', 'xml:space'].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, 'http://www.w3.org/XML/1998/namespace');
});

// Special case: this attribute exists both in HTML and SVG.
// Its "tabindex" attribute name is case-sensitive in SVG so we can't just use
// its React `tabIndex` name, like we do for attributes that exist only in HTML.
properties.tabIndex = new PropertyInfoRecord('tabIndex', STRING, false, // mustUseProperty
'tabindex', // attributeName
null);

/**
 * Get the value for a property on a node. Only used in DEV for SSR validation.
 * The "expected" argument is used as a hint of what the expected value is.
 * Some properties have multiple equivalent values.
 */
function getValueForProperty(node, name, expected, propertyInfo) {
  {
    if (propertyInfo.mustUseProperty) {
      var propertyName = propertyInfo.propertyName;

      return node[propertyName];
    } else {
      var attributeName = propertyInfo.attributeName;

      var stringValue = null;

      if (propertyInfo.type === OVERLOADED_BOOLEAN) {
        if (node.hasAttribute(attributeName)) {
          var value = node.getAttribute(attributeName);
          if (value === '') {
            return true;
          }
          if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
            return value;
          }
          if (value === '' + expected) {
            return expected;
          }
          return value;
        }
      } else if (node.hasAttribute(attributeName)) {
        if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
          // We had an attribute but shouldn't have had one, so read it
          // for the error message.
          return node.getAttribute(attributeName);
        }
        if (propertyInfo.type === BOOLEAN) {
          // If this was a boolean, it doesn't matter what the value is
          // the fact that we have it is the same as the expected.
          return expected;
        }
        // Even if this property uses a namespace we use getAttribute
        // because we assume its namespaced name is the same as our config.
        // To use getAttributeNS we need the local name which we don't have
        // in our config atm.
        stringValue = node.getAttribute(attributeName);
      }

      if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
        return stringValue === null ? expected : stringValue;
      } else if (stringValue === '' + expected) {
        return expected;
      } else {
        return stringValue;
      }
    }
  }
}

/**
 * Get the value for a attribute on a node. Only used in DEV for SSR validation.
 * The third argument is used as a hint of what the expected value is. Some
 * attributes have multiple equivalent values.
 */
function getValueForAttribute(node, name, expected) {
  {
    if (!isAttributeNameSafe(name)) {
      return;
    }
    if (!node.hasAttribute(name)) {
      return expected === undefined ? undefined : null;
    }
    var value = node.getAttribute(name);
    if (value === '' + expected) {
      return expected;
    }
    return value;
  }
}

/**
 * Sets the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 * @param {*} value
 */
function setValueForProperty(node, name, value, isCustomComponentTag) {
  var propertyInfo = getPropertyInfo(name);
  if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) {
    return;
  }
  if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) {
    value = null;
  }
  // If the prop isn't in the special list, treat it as a simple attribute.
  if (isCustomComponentTag || propertyInfo === null) {
    if (isAttributeNameSafe(name)) {
      var _attributeName = name;
      if (value === null) {
        node.removeAttribute(_attributeName);
      } else {
        node.setAttribute(_attributeName, '' + value);
      }
    }
    return;
  }
  var mustUseProperty = propertyInfo.mustUseProperty;

  if (mustUseProperty) {
    var propertyName = propertyInfo.propertyName;

    if (value === null) {
      var type = propertyInfo.type;

      node[propertyName] = type === BOOLEAN ? false : '';
    } else {
      // Contrary to `setAttribute`, object properties are properly
      // `toString`ed by IE8/9.
      node[propertyName] = value;
    }
    return;
  }
  // The rest are treated as attributes with special cases.
  var attributeName = propertyInfo.attributeName,
      attributeNamespace = propertyInfo.attributeNamespace;

  if (value === null) {
    node.removeAttribute(attributeName);
  } else {
    var _type = propertyInfo.type;

    var attributeValue = void 0;
    if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) {
      attributeValue = '';
    } else {
      // `setAttribute` with objects becomes only `[object]` in IE8/9,
      // ('' + value) makes it output the correct toString()-value.
      attributeValue = '' + value;
    }
    if (attributeNamespace) {
      node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
    } else {
      node.setAttribute(attributeName, attributeValue);
    }
  }
}

var ReactControlledValuePropTypes = {
  checkPropTypes: null
};

{
  var hasReadOnlyValue = {
    button: true,
    checkbox: true,
    image: true,
    hidden: true,
    radio: true,
    reset: true,
    submit: true
  };

  var propTypes = {
    value: function (props, propName, componentName) {
      if (!props[propName] || hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    },
    checked: function (props, propName, componentName) {
      if (!props[propName] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    }
  };

  /**
   * Provide a linked `value` attribute for controlled forms. You should not use
   * this outside of the ReactDOM controlled form components.
   */
  ReactControlledValuePropTypes.checkPropTypes = function (tagName, props, getStack) {
    checkPropTypes(propTypes, props, 'prop', tagName, getStack);
  };
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValueDefaultValue = false;
var didWarnCheckedDefaultChecked = false;
var didWarnControlledToUncontrolled = false;
var didWarnUncontrolledToControlled = false;

function isControlled(props) {
  var usesChecked = props.type === 'checkbox' || props.type === 'radio';
  return usesChecked ? props.checked != null : props.value != null;
}

/**
 * Implements an <input> host component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */

function getHostProps(element, props) {
  var node = element;
  var checked = props.checked;

  var hostProps = _assign({}, props, {
    defaultChecked: undefined,
    defaultValue: undefined,
    value: undefined,
    checked: checked != null ? checked : node._wrapperState.initialChecked
  });

  return hostProps;
}

function initWrapperState(element, props) {
  {
    ReactControlledValuePropTypes.checkPropTypes('input', props, getCurrentFiberStackAddendum);

    if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
      warning(false, '%s contains an input of type %s with both checked and defaultChecked props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the checked prop, or the defaultChecked prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName() || 'A component', props.type);
      didWarnCheckedDefaultChecked = true;
    }
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
      warning(false, '%s contains an input of type %s with both value and defaultValue props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName() || 'A component', props.type);
      didWarnValueDefaultValue = true;
    }
  }

  var node = element;
  var defaultValue = props.defaultValue == null ? '' : props.defaultValue;

  node._wrapperState = {
    initialChecked: props.checked != null ? props.checked : props.defaultChecked,
    initialValue: getSafeValue(props.value != null ? props.value : defaultValue),
    controlled: isControlled(props)
  };
}

function updateChecked(element, props) {
  var node = element;
  var checked = props.checked;
  if (checked != null) {
    setValueForProperty(node, 'checked', checked, false);
  }
}

function updateWrapper(element, props) {
  var node = element;
  {
    var _controlled = isControlled(props);

    if (!node._wrapperState.controlled && _controlled && !didWarnUncontrolledToControlled) {
      warning(false, 'A component is changing an uncontrolled input of type %s to be controlled. ' + 'Input elements should not switch from uncontrolled to controlled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum());
      didWarnUncontrolledToControlled = true;
    }
    if (node._wrapperState.controlled && !_controlled && !didWarnControlledToUncontrolled) {
      warning(false, 'A component is changing a controlled input of type %s to be uncontrolled. ' + 'Input elements should not switch from controlled to uncontrolled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum());
      didWarnControlledToUncontrolled = true;
    }
  }

  updateChecked(element, props);

  var value = getSafeValue(props.value);

  if (value != null) {
    if (props.type === 'number') {
      if (value === 0 && node.value === '' ||
      // eslint-disable-next-line
      node.value != value) {
        node.value = '' + value;
      }
    } else if (node.value !== '' + value) {
      node.value = '' + value;
    }
  }

  if (props.hasOwnProperty('value')) {
    setDefaultValue(node, props.type, value);
  } else if (props.hasOwnProperty('defaultValue')) {
    setDefaultValue(node, props.type, getSafeValue(props.defaultValue));
  }

  if (props.checked == null && props.defaultChecked != null) {
    node.defaultChecked = !!props.defaultChecked;
  }
}

function postMountWrapper(element, props) {
  var node = element;

  if (props.hasOwnProperty('value') || props.hasOwnProperty('defaultValue')) {
    // Do not assign value if it is already set. This prevents user text input
    // from being lost during SSR hydration.
    if (node.value === '') {
      node.value = '' + node._wrapperState.initialValue;
    }

    // value must be assigned before defaultValue. This fixes an issue where the
    // visually displayed value of date inputs disappears on mobile Safari and Chrome:
    // https://github.com/facebook/react/issues/7233
    node.defaultValue = '' + node._wrapperState.initialValue;
  }

  // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
  // this is needed to work around a chrome bug where setting defaultChecked
  // will sometimes influence the value of checked (even after detachment).
  // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
  // We need to temporarily unset name to avoid disrupting radio button groups.
  var name = node.name;
  if (name !== '') {
    node.name = '';
  }
  node.defaultChecked = !node.defaultChecked;
  node.defaultChecked = !node.defaultChecked;
  if (name !== '') {
    node.name = name;
  }
}

function restoreControlledState(element, props) {
  var node = element;
  updateWrapper(node, props);
  updateNamedCousins(node, props);
}

function updateNamedCousins(rootNode, props) {
  var name = props.name;
  if (props.type === 'radio' && name != null) {
    var queryRoot = rootNode;

    while (queryRoot.parentNode) {
      queryRoot = queryRoot.parentNode;
    }

    // If `rootNode.form` was non-null, then we could try `form.elements`,
    // but that sometimes behaves strangely in IE8. We could also try using
    // `form.getElementsByName`, but that will only return direct children
    // and won't include inputs that use the HTML5 `form=` attribute. Since
    // the input might not even be in a form. It might not even be in the
    // document. Let's just use the local `querySelectorAll` to ensure we don't
    // miss anything.
    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

    for (var i = 0; i < group.length; i++) {
      var otherNode = group[i];
      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
        continue;
      }
      // This will throw if radio buttons rendered by different copies of React
      // and the same name are rendered into the same form (same as #1939).
      // That's probably okay; we don't support it just as we don't support
      // mixing React radio buttons with non-React ones.
      var otherProps = getFiberCurrentPropsFromNode$1(otherNode);
      !otherProps ? invariant(false, 'ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.') : void 0;

      // We need update the tracked value on the named cousin since the value
      // was changed but the input saw no event or value set
      updateValueIfChanged(otherNode);

      // If this is a controlled radio button group, forcing the input that
      // was previously checked to update will cause it to be come re-checked
      // as appropriate.
      updateWrapper(otherNode, otherProps);
    }
  }
}

// In Chrome, assigning defaultValue to certain input types triggers input validation.
// For number inputs, the display value loses trailing decimal points. For email inputs,
// Chrome raises "The specified value <x> is not a valid email address".
//
// Here we check to see if the defaultValue has actually changed, avoiding these problems
// when the user is inputting text
//
// https://github.com/facebook/react/issues/7253
function setDefaultValue(node, type, value) {
  if (
  // Focused number inputs synchronize on blur. See ChangeEventPlugin.js
  type !== 'number' || node.ownerDocument.activeElement !== node) {
    if (value == null) {
      node.defaultValue = '' + node._wrapperState.initialValue;
    } else if (node.defaultValue !== '' + value) {
      node.defaultValue = '' + value;
    }
  }
}

function getSafeValue(value) {
  switch (typeof value) {
    case 'boolean':
    case 'number':
    case 'object':
    case 'string':
    case 'undefined':
      return value;
    default:
      // function, symbol are assigned as empty strings
      return '';
  }
}

var eventTypes$1 = {
  change: {
    phasedRegistrationNames: {
      bubbled: 'onChange',
      captured: 'onChangeCapture'
    },
    dependencies: ['topBlur', 'topChange', 'topClick', 'topFocus', 'topInput', 'topKeyDown', 'topKeyUp', 'topSelectionChange']
  }
};

function createAndAccumulateChangeEvent(inst, nativeEvent, target) {
  var event = SyntheticEvent$1.getPooled(eventTypes$1.change, inst, nativeEvent, target);
  event.type = 'change';
  // Flag this event loop as needing state restore.
  enqueueStateRestore(target);
  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * For IE shims
 */
var activeElement = null;
var activeElementInst = null;

/**
 * SECTION: handle `change` event
 */
function shouldUseChangeEvent(elem) {
  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = createAndAccumulateChangeEvent(activeElementInst, nativeEvent, getEventTarget(nativeEvent));

  // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactBrowserEventEmitter. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.
  batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  runEventsInBatch(event, false);
}

function getInstIfValueChanged(targetInst) {
  var targetNode = getNodeFromInstance$1(targetInst);
  if (updateValueIfChanged(targetNode)) {
    return targetInst;
  }
}

function getTargetInstForChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topChange') {
    return targetInst;
  }
}

/**
 * SECTION: handle `input` event
 */
var isInputEventSupported = false;
if (ExecutionEnvironment.canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events.
  isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
}

/**
 * (For IE <=9) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */
function startWatchingForValueChange(target, targetInst) {
  activeElement = target;
  activeElementInst = targetInst;
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}

/**
 * (For IE <=9) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */
function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }
  activeElement.detachEvent('onpropertychange', handlePropertyChange);
  activeElement = null;
  activeElementInst = null;
}

/**
 * (For IE <=9) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */
function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }
  if (getInstIfValueChanged(activeElementInst)) {
    manualDispatchChangeEvent(nativeEvent);
  }
}

function handleEventsForInputEventPolyfill(topLevelType, target, targetInst) {
  if (topLevelType === 'topFocus') {
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(target, targetInst);
  } else if (topLevelType === 'topBlur') {
    stopWatchingForValueChange();
  }
}

// For IE8 and IE9.
function getTargetInstForInputEventPolyfill(topLevelType, targetInst) {
  if (topLevelType === 'topSelectionChange' || topLevelType === 'topKeyUp' || topLevelType === 'topKeyDown') {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    return getInstIfValueChanged(activeElementInst);
  }
}

/**
 * SECTION: handle `click` event
 */
function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
}

function getTargetInstForClickEvent(topLevelType, targetInst) {
  if (topLevelType === 'topClick') {
    return getInstIfValueChanged(targetInst);
  }
}

function getTargetInstForInputOrChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topInput' || topLevelType === 'topChange') {
    return getInstIfValueChanged(targetInst);
  }
}

function handleControlledInputBlur(inst, node) {
  // TODO: In IE, inst is occasionally null. Why?
  if (inst == null) {
    return;
  }

  // Fiber and ReactDOM keep wrapper state in separate places
  var state = inst._wrapperState || node._wrapperState;

  if (!state || !state.controlled || node.type !== 'number') {
    return;
  }

  // If controlled, assign the value attribute to the current value on blur
  setDefaultValue(node, 'number', node.value);
}

/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */
var ChangeEventPlugin = {
  eventTypes: eventTypes$1,

  _isInputEventSupported: isInputEventSupported,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    var getTargetInstFunc = void 0,
        handleEventFunc = void 0;
    if (shouldUseChangeEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForChangeEvent;
    } else if (isTextInputElement(targetNode)) {
      if (isInputEventSupported) {
        getTargetInstFunc = getTargetInstForInputOrChangeEvent;
      } else {
        getTargetInstFunc = getTargetInstForInputEventPolyfill;
        handleEventFunc = handleEventsForInputEventPolyfill;
      }
    } else if (shouldUseClickEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForClickEvent;
    }

    if (getTargetInstFunc) {
      var inst = getTargetInstFunc(topLevelType, targetInst);
      if (inst) {
        var event = createAndAccumulateChangeEvent(inst, nativeEvent, nativeEventTarget);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(topLevelType, targetNode, targetInst);
    }

    // When blurring, set the value attribute for number inputs
    if (topLevelType === 'topBlur') {
      handleControlledInputBlur(targetInst, targetNode);
    }
  }
};

/**
 * Module that is injectable into `EventPluginHub`, that specifies a
 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
 * plugins, without having to package every one of them. This is better than
 * having plugins be ordered in the same order that they are injected because
 * that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */
var DOMEventPluginOrder = ['ResponderEventPlugin', 'SimpleEventPlugin', 'TapEventPlugin', 'EnterLeaveEventPlugin', 'ChangeEventPlugin', 'SelectEventPlugin', 'BeforeInputEventPlugin'];

var SyntheticUIEvent = SyntheticEvent$1.extend({
  view: null,
  detail: null
});

/**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */

var modifierKeyToProp = {
  Alt: 'altKey',
  Control: 'ctrlKey',
  Meta: 'metaKey',
  Shift: 'shiftKey'
};

// IE8 does not implement getModifierState so we simply map it to the only
// modifier keys exposed by the event itself, does not support Lock-keys.
// Currently, all major browsers except Chrome seems to support Lock-keys.
function modifierStateGetter(keyArg) {
  var syntheticEvent = this;
  var nativeEvent = syntheticEvent.nativeEvent;
  if (nativeEvent.getModifierState) {
    return nativeEvent.getModifierState(keyArg);
  }
  var keyProp = modifierKeyToProp[keyArg];
  return keyProp ? !!nativeEvent[keyProp] : false;
}

function getEventModifierState(nativeEvent) {
  return modifierStateGetter;
}

/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var SyntheticMouseEvent = SyntheticUIEvent.extend({
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  pageX: null,
  pageY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  getModifierState: getEventModifierState,
  button: null,
  buttons: null,
  relatedTarget: function (event) {
    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
  }
});

var eventTypes$2 = {
  mouseEnter: {
    registrationName: 'onMouseEnter',
    dependencies: ['topMouseOut', 'topMouseOver']
  },
  mouseLeave: {
    registrationName: 'onMouseLeave',
    dependencies: ['topMouseOut', 'topMouseOver']
  }
};

var EnterLeaveEventPlugin = {
  eventTypes: eventTypes$2,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   */
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    if (topLevelType === 'topMouseOver' && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      return null;
    }
    if (topLevelType !== 'topMouseOut' && topLevelType !== 'topMouseOver') {
      // Must not be a mouse in or mouse out - ignoring.
      return null;
    }

    var win = void 0;
    if (nativeEventTarget.window === nativeEventTarget) {
      // `nativeEventTarget` is probably a window object.
      win = nativeEventTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = nativeEventTarget.ownerDocument;
      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from = void 0;
    var to = void 0;
    if (topLevelType === 'topMouseOut') {
      from = targetInst;
      var related = nativeEvent.relatedTarget || nativeEvent.toElement;
      to = related ? getClosestInstanceFromNode(related) : null;
    } else {
      // Moving to a node from outside the window.
      from = null;
      to = targetInst;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var fromNode = from == null ? win : getNodeFromInstance$1(from);
    var toNode = to == null ? win : getNodeFromInstance$1(to);

    var leave = SyntheticMouseEvent.getPooled(eventTypes$2.mouseLeave, from, nativeEvent, nativeEventTarget);
    leave.type = 'mouseleave';
    leave.target = fromNode;
    leave.relatedTarget = toNode;

    var enter = SyntheticMouseEvent.getPooled(eventTypes$2.mouseEnter, to, nativeEvent, nativeEventTarget);
    enter.type = 'mouseenter';
    enter.target = toNode;
    enter.relatedTarget = fromNode;

    accumulateEnterLeaveDispatches(leave, enter, from, to);

    return [leave, enter];
  }
};

/**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 *
 * Note that this module is currently shared and assumed to be stateless.
 * If this becomes an actual Map, that will break.
 */

/**
 * This API should be called `delete` but we'd have to make sure to always
 * transform these to strings for IE support. When this transform is fully
 * supported we can rename it.
 */


function get(key) {
  return key._reactInternalFiber;
}

function has(key) {
  return key._reactInternalFiber !== undefined;
}

function set(key, value) {
  key._reactInternalFiber = value;
}

// Don't change these two values. They're used by React Dev Tools.
var NoEffect = /*              */0;
var PerformedWork = /*         */1;

// You can change the rest (and add more).
var Placement = /*             */2;
var Update = /*                */4;
var PlacementAndUpdate = /*    */6;
var Deletion = /*              */8;
var ContentReset = /*          */16;
var Callback = /*              */32;
var DidCapture = /*            */64;
var Ref = /*                   */128;
var ErrLog = /*                */256;
var Snapshot = /*              */2048;

// Union of all host effects
var HostEffectMask = /*        */2559;

var Incomplete = /*            */512;
var ShouldCapture = /*         */1024;

var MOUNTING = 1;
var MOUNTED = 2;
var UNMOUNTED = 3;

function isFiberMountedImpl(fiber) {
  var node = fiber;
  if (!fiber.alternate) {
    // If there is no alternate, this might be a new tree that isn't inserted
    // yet. If it is, then it will have a pending insertion effect on it.
    if ((node.effectTag & Placement) !== NoEffect) {
      return MOUNTING;
    }
    while (node['return']) {
      node = node['return'];
      if ((node.effectTag & Placement) !== NoEffect) {
        return MOUNTING;
      }
    }
  } else {
    while (node['return']) {
      node = node['return'];
    }
  }
  if (node.tag === HostRoot) {
    // TODO: Check if this was a nested HostRoot when used with
    // renderContainerIntoSubtree.
    return MOUNTED;
  }
  // If we didn't hit the root, that means that we're in an disconnected tree
  // that has been unmounted.
  return UNMOUNTED;
}

function isFiberMounted(fiber) {
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function isMounted(component) {
  {
    var owner = ReactCurrentOwner.current;
    if (owner !== null && owner.tag === ClassComponent) {
      var ownerFiber = owner;
      var instance = ownerFiber.stateNode;
      !instance._warnedAboutRefsInRender ? warning(false, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(ownerFiber) || 'A component') : void 0;
      instance._warnedAboutRefsInRender = true;
    }
  }

  var fiber = get(component);
  if (!fiber) {
    return false;
  }
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function assertIsMounted(fiber) {
  !(isFiberMountedImpl(fiber) === MOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
}

function findCurrentFiberUsingSlowPath(fiber) {
  var alternate = fiber.alternate;
  if (!alternate) {
    // If there is no alternate, then we only need to check if it is mounted.
    var state = isFiberMountedImpl(fiber);
    !(state !== UNMOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
    if (state === MOUNTING) {
      return null;
    }
    return fiber;
  }
  // If we have two possible branches, we'll walk backwards up to the root
  // to see what path the root points to. On the way we may hit one of the
  // special cases and we'll deal with them.
  var a = fiber;
  var b = alternate;
  while (true) {
    var parentA = a['return'];
    var parentB = parentA ? parentA.alternate : null;
    if (!parentA || !parentB) {
      // We're at the root.
      break;
    }

    // If both copies of the parent fiber point to the same child, we can
    // assume that the child is current. This happens when we bailout on low
    // priority: the bailed out fiber's child reuses the current child.
    if (parentA.child === parentB.child) {
      var child = parentA.child;
      while (child) {
        if (child === a) {
          // We've determined that A is the current branch.
          assertIsMounted(parentA);
          return fiber;
        }
        if (child === b) {
          // We've determined that B is the current branch.
          assertIsMounted(parentA);
          return alternate;
        }
        child = child.sibling;
      }
      // We should never have an alternate for any mounting node. So the only
      // way this could possibly happen is if this was unmounted, if at all.
      invariant(false, 'Unable to find node on an unmounted component.');
    }

    if (a['return'] !== b['return']) {
      // The return pointer of A and the return pointer of B point to different
      // fibers. We assume that return pointers never criss-cross, so A must
      // belong to the child set of A.return, and B must belong to the child
      // set of B.return.
      a = parentA;
      b = parentB;
    } else {
      // The return pointers point to the same fiber. We'll have to use the
      // default, slow path: scan the child sets of each parent alternate to see
      // which child belongs to which set.
      //
      // Search parent A's child set
      var didFindChild = false;
      var _child = parentA.child;
      while (_child) {
        if (_child === a) {
          didFindChild = true;
          a = parentA;
          b = parentB;
          break;
        }
        if (_child === b) {
          didFindChild = true;
          b = parentA;
          a = parentB;
          break;
        }
        _child = _child.sibling;
      }
      if (!didFindChild) {
        // Search parent B's child set
        _child = parentB.child;
        while (_child) {
          if (_child === a) {
            didFindChild = true;
            a = parentB;
            b = parentA;
            break;
          }
          if (_child === b) {
            didFindChild = true;
            b = parentB;
            a = parentA;
            break;
          }
          _child = _child.sibling;
        }
        !didFindChild ? invariant(false, 'Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.') : void 0;
      }
    }

    !(a.alternate === b) ? invariant(false, 'Return fibers should always be each others\' alternates. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  }
  // If the root is not a host container, we're in a disconnected tree. I.e.
  // unmounted.
  !(a.tag === HostRoot) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
  if (a.stateNode.current === a) {
    // We've determined that A is the current branch.
    return fiber;
  }
  // Otherwise B has to be current branch.
  return alternate;
}

function findCurrentHostFiber(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

function findCurrentHostFiberWithNoPortals(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child && node.tag !== HostPortal) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

function addEventBubbleListener(element, eventType, listener) {
  element.addEventListener(eventType, listener, false);
}

function addEventCaptureListener(element, eventType, listener) {
  element.addEventListener(eventType, listener, true);
}

/**
 * @interface Event
 * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
 * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
 */
var SyntheticAnimationEvent = SyntheticEvent$1.extend({
  animationName: null,
  elapsedTime: null,
  pseudoElement: null
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */
var SyntheticClipboardEvent = SyntheticEvent$1.extend({
  clipboardData: function (event) {
    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
  }
});

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var SyntheticFocusEvent = SyntheticUIEvent.extend({
  relatedTarget: null
});

/**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */
function getEventCharCode(nativeEvent) {
  var charCode = void 0;
  var keyCode = nativeEvent.keyCode;

  if ('charCode' in nativeEvent) {
    charCode = nativeEvent.charCode;

    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
    if (charCode === 0 && keyCode === 13) {
      charCode = 13;
    }
  } else {
    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
    charCode = keyCode;
  }

  // IE and Edge (on Windows) and Chrome / Safari (on Windows and Linux)
  // report Enter as charCode 10 when ctrl is pressed.
  if (charCode === 10) {
    charCode = 13;
  }

  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
  // Must not discard the (non-)printable Enter-key.
  if (charCode >= 32 || charCode === 13) {
    return charCode;
  }

  return 0;
}

/**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var normalizeKey = {
  Esc: 'Escape',
  Spacebar: ' ',
  Left: 'ArrowLeft',
  Up: 'ArrowUp',
  Right: 'ArrowRight',
  Down: 'ArrowDown',
  Del: 'Delete',
  Win: 'OS',
  Menu: 'ContextMenu',
  Apps: 'ContextMenu',
  Scroll: 'ScrollLock',
  MozPrintableKey: 'Unidentified'
};

/**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var translateToKey = {
  '8': 'Backspace',
  '9': 'Tab',
  '12': 'Clear',
  '13': 'Enter',
  '16': 'Shift',
  '17': 'Control',
  '18': 'Alt',
  '19': 'Pause',
  '20': 'CapsLock',
  '27': 'Escape',
  '32': ' ',
  '33': 'PageUp',
  '34': 'PageDown',
  '35': 'End',
  '36': 'Home',
  '37': 'ArrowLeft',
  '38': 'ArrowUp',
  '39': 'ArrowRight',
  '40': 'ArrowDown',
  '45': 'Insert',
  '46': 'Delete',
  '112': 'F1',
  '113': 'F2',
  '114': 'F3',
  '115': 'F4',
  '116': 'F5',
  '117': 'F6',
  '118': 'F7',
  '119': 'F8',
  '120': 'F9',
  '121': 'F10',
  '122': 'F11',
  '123': 'F12',
  '144': 'NumLock',
  '145': 'ScrollLock',
  '224': 'Meta'
};

/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */
function getEventKey(nativeEvent) {
  if (nativeEvent.key) {
    // Normalize inconsistent values reported by browsers due to
    // implementations of a working draft specification.

    // FireFox implements `key` but returns `MozPrintableKey` for all
    // printable characters (normalized to `Unidentified`), ignore it.
    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
    if (key !== 'Unidentified') {
      return key;
    }
  }

  // Browser does not implement `key`, polyfill as much of it as we can.
  if (nativeEvent.type === 'keypress') {
    var charCode = getEventCharCode(nativeEvent);

    // The enter-key is technically both printable and non-printable and can
    // thus be captured by `keypress`, no other non-printable key should.
    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
  }
  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
    // While user keyboard layout determines the actual meaning of each
    // `keyCode` value, almost all function keys have a universal value.
    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
  }
  return '';
}

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var SyntheticKeyboardEvent = SyntheticUIEvent.extend({
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  getModifierState: getEventModifierState,
  // Legacy Interface
  charCode: function (event) {
    // `charCode` is the result of a KeyPress event and represents the value of
    // the actual printable character.

    // KeyPress is deprecated, but its replacement is not yet final and not
    // implemented in any major browser. Only KeyPress has charCode.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    return 0;
  },
  keyCode: function (event) {
    // `keyCode` is the result of a KeyDown/Up event and represents the value of
    // physical keyboard key.

    // The actual meaning of the value depends on the users' keyboard layout
    // which cannot be detected. Assuming that it is a US keyboard layout
    // provides a surprisingly accurate mapping for US and European users.
    // Due to this, it is left to the user to implement at this time.
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  },
  which: function (event) {
    // `which` is an alias for either `keyCode` or `charCode` depending on the
    // type of the event.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  }
});

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var SyntheticDragEvent = SyntheticMouseEvent.extend({
  dataTransfer: null
});

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */
var SyntheticTouchEvent = SyntheticUIEvent.extend({
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null,
  getModifierState: getEventModifierState
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
 * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
 */
var SyntheticTransitionEvent = SyntheticEvent$1.extend({
  propertyName: null,
  elapsedTime: null,
  pseudoElement: null
});

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var SyntheticWheelEvent = SyntheticMouseEvent.extend({
  deltaX: function (event) {
    return 'deltaX' in event ? event.deltaX : // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
  },
  deltaY: function (event) {
    return 'deltaY' in event ? event.deltaY : // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
    'wheelDeltaY' in event ? -event.wheelDeltaY : // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
    'wheelDelta' in event ? -event.wheelDelta : 0;
  },

  deltaZ: null,

  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
});

/**
 * Turns
 * ['abort', ...]
 * into
 * eventTypes = {
 *   'abort': {
 *     phasedRegistrationNames: {
 *       bubbled: 'onAbort',
 *       captured: 'onAbortCapture',
 *     },
 *     dependencies: ['topAbort'],
 *   },
 *   ...
 * };
 * topLevelEventsToDispatchConfig = {
 *   'topAbort': { sameConfig }
 * };
 */
var interactiveEventTypeNames = ['blur', 'cancel', 'click', 'close', 'contextMenu', 'copy', 'cut', 'doubleClick', 'dragEnd', 'dragStart', 'drop', 'focus', 'input', 'invalid', 'keyDown', 'keyPress', 'keyUp', 'mouseDown', 'mouseUp', 'paste', 'pause', 'play', 'rateChange', 'reset', 'seeked', 'submit', 'touchCancel', 'touchEnd', 'touchStart', 'volumeChange'];
var nonInteractiveEventTypeNames = ['abort', 'animationEnd', 'animationIteration', 'animationStart', 'canPlay', 'canPlayThrough', 'drag', 'dragEnter', 'dragExit', 'dragLeave', 'dragOver', 'durationChange', 'emptied', 'encrypted', 'ended', 'error', 'load', 'loadedData', 'loadedMetadata', 'loadStart', 'mouseMove', 'mouseOut', 'mouseOver', 'playing', 'progress', 'scroll', 'seeking', 'stalled', 'suspend', 'timeUpdate', 'toggle', 'touchMove', 'transitionEnd', 'waiting', 'wheel'];

var eventTypes$4 = {};
var topLevelEventsToDispatchConfig = {};

function addEventTypeNameToConfig(event, isInteractive) {
  var capitalizedEvent = event[0].toUpperCase() + event.slice(1);
  var onEvent = 'on' + capitalizedEvent;
  var topEvent = 'top' + capitalizedEvent;

  var type = {
    phasedRegistrationNames: {
      bubbled: onEvent,
      captured: onEvent + 'Capture'
    },
    dependencies: [topEvent],
    isInteractive: isInteractive
  };
  eventTypes$4[event] = type;
  topLevelEventsToDispatchConfig[topEvent] = type;
}

interactiveEventTypeNames.forEach(function (eventTypeName) {
  addEventTypeNameToConfig(eventTypeName, true);
});
nonInteractiveEventTypeNames.forEach(function (eventTypeName) {
  addEventTypeNameToConfig(eventTypeName, false);
});

// Only used in DEV for exhaustiveness validation.
var knownHTMLTopLevelTypes = ['topAbort', 'topCancel', 'topCanPlay', 'topCanPlayThrough', 'topClose', 'topDurationChange', 'topEmptied', 'topEncrypted', 'topEnded', 'topError', 'topInput', 'topInvalid', 'topLoad', 'topLoadedData', 'topLoadedMetadata', 'topLoadStart', 'topPause', 'topPlay', 'topPlaying', 'topProgress', 'topRateChange', 'topReset', 'topSeeked', 'topSeeking', 'topStalled', 'topSubmit', 'topSuspend', 'topTimeUpdate', 'topToggle', 'topVolumeChange', 'topWaiting'];

var SimpleEventPlugin = {
  eventTypes: eventTypes$4,

  isInteractiveTopLevelEventType: function (topLevelType) {
    var config = topLevelEventsToDispatchConfig[topLevelType];
    return config !== undefined && config.isInteractive === true;
  },


  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
    if (!dispatchConfig) {
      return null;
    }
    var EventConstructor = void 0;
    switch (topLevelType) {
      case 'topKeyPress':
        // Firefox creates a keypress event for function keys too. This removes
        // the unwanted keypress events. Enter is however both printable and
        // non-printable. One would expect Tab to be as well (but it isn't).
        if (getEventCharCode(nativeEvent) === 0) {
          return null;
        }
      /* falls through */
      case 'topKeyDown':
      case 'topKeyUp':
        EventConstructor = SyntheticKeyboardEvent;
        break;
      case 'topBlur':
      case 'topFocus':
        EventConstructor = SyntheticFocusEvent;
        break;
      case 'topClick':
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }
      /* falls through */
      case 'topDoubleClick':
      case 'topMouseDown':
      case 'topMouseMove':
      case 'topMouseUp':
      // TODO: Disabled elements should not respond to mouse events
      /* falls through */
      case 'topMouseOut':
      case 'topMouseOver':
      case 'topContextMenu':
        EventConstructor = SyntheticMouseEvent;
        break;
      case 'topDrag':
      case 'topDragEnd':
      case 'topDragEnter':
      case 'topDragExit':
      case 'topDragLeave':
      case 'topDragOver':
      case 'topDragStart':
      case 'topDrop':
        EventConstructor = SyntheticDragEvent;
        break;
      case 'topTouchCancel':
      case 'topTouchEnd':
      case 'topTouchMove':
      case 'topTouchStart':
        EventConstructor = SyntheticTouchEvent;
        break;
      case 'topAnimationEnd':
      case 'topAnimationIteration':
      case 'topAnimationStart':
        EventConstructor = SyntheticAnimationEvent;
        break;
      case 'topTransitionEnd':
        EventConstructor = SyntheticTransitionEvent;
        break;
      case 'topScroll':
        EventConstructor = SyntheticUIEvent;
        break;
      case 'topWheel':
        EventConstructor = SyntheticWheelEvent;
        break;
      case 'topCopy':
      case 'topCut':
      case 'topPaste':
        EventConstructor = SyntheticClipboardEvent;
        break;
      default:
        {
          if (knownHTMLTopLevelTypes.indexOf(topLevelType) === -1) {
            warning(false, 'SimpleEventPlugin: Unhandled event type, `%s`. This warning ' + 'is likely caused by a bug in React. Please file an issue.', topLevelType);
          }
        }
        // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0
        EventConstructor = SyntheticEvent$1;
        break;
    }
    var event = EventConstructor.getPooled(dispatchConfig, targetInst, nativeEvent, nativeEventTarget);
    accumulateTwoPhaseDispatches(event);
    return event;
  }
};

var isInteractiveTopLevelEventType = SimpleEventPlugin.isInteractiveTopLevelEventType;


var CALLBACK_BOOKKEEPING_POOL_SIZE = 10;
var callbackBookkeepingPool = [];

/**
 * Find the deepest React component completely containing the root of the
 * passed-in instance (for use when entire React trees are nested within each
 * other). If React trees are not nested, returns null.
 */
function findRootContainerNode(inst) {
  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.
  while (inst['return']) {
    inst = inst['return'];
  }
  if (inst.tag !== HostRoot) {
    // This can happen if we're in a detached tree.
    return null;
  }
  return inst.stateNode.containerInfo;
}

// Used to store ancestor hierarchy in top level callback
function getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst) {
  if (callbackBookkeepingPool.length) {
    var instance = callbackBookkeepingPool.pop();
    instance.topLevelType = topLevelType;
    instance.nativeEvent = nativeEvent;
    instance.targetInst = targetInst;
    return instance;
  }
  return {
    topLevelType: topLevelType,
    nativeEvent: nativeEvent,
    targetInst: targetInst,
    ancestors: []
  };
}

function releaseTopLevelCallbackBookKeeping(instance) {
  instance.topLevelType = null;
  instance.nativeEvent = null;
  instance.targetInst = null;
  instance.ancestors.length = 0;
  if (callbackBookkeepingPool.length < CALLBACK_BOOKKEEPING_POOL_SIZE) {
    callbackBookkeepingPool.push(instance);
  }
}

function handleTopLevel(bookKeeping) {
  var targetInst = bookKeeping.targetInst;

  // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.
  var ancestor = targetInst;
  do {
    if (!ancestor) {
      bookKeeping.ancestors.push(ancestor);
      break;
    }
    var root = findRootContainerNode(ancestor);
    if (!root) {
      break;
    }
    bookKeeping.ancestors.push(ancestor);
    ancestor = getClosestInstanceFromNode(root);
  } while (ancestor);

  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
    targetInst = bookKeeping.ancestors[i];
    runExtractedEventsInBatch(bookKeeping.topLevelType, targetInst, bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
  }
}

// TODO: can we stop exporting these?
var _enabled = true;

function setEnabled(enabled) {
  _enabled = !!enabled;
}

function isEnabled() {
  return _enabled;
}

/**
 * Traps top-level events by using event bubbling.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapBubbledEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  var dispatch = isInteractiveTopLevelEventType(topLevelType) ? dispatchInteractiveEvent : dispatchEvent;

  addEventBubbleListener(element, handlerBaseName,
  // Check if interactive and wrap in interactiveUpdates
  dispatch.bind(null, topLevelType));
}

/**
 * Traps a top-level event by using event capturing.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapCapturedEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  var dispatch = isInteractiveTopLevelEventType(topLevelType) ? dispatchInteractiveEvent : dispatchEvent;

  addEventCaptureListener(element, handlerBaseName,
  // Check if interactive and wrap in interactiveUpdates
  dispatch.bind(null, topLevelType));
}

function dispatchInteractiveEvent(topLevelType, nativeEvent) {
  interactiveUpdates(dispatchEvent, topLevelType, nativeEvent);
}

function dispatchEvent(topLevelType, nativeEvent) {
  if (!_enabled) {
    return;
  }

  var nativeEventTarget = getEventTarget(nativeEvent);
  var targetInst = getClosestInstanceFromNode(nativeEventTarget);
  if (targetInst !== null && typeof targetInst.tag === 'number' && !isFiberMounted(targetInst)) {
    // If we get an event (ex: img onload) before committing that
    // component's mount, ignore it for now (that is, treat it as if it was an
    // event on a non-React tree). We might also consider queueing events and
    // dispatching them after the mount.
    targetInst = null;
  }

  var bookKeeping = getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst);

  try {
    // Event queue being processed in the same cycle allows
    // `preventDefault`.
    batchedUpdates(handleTopLevel, bookKeeping);
  } finally {
    releaseTopLevelCallbackBookKeeping(bookKeeping);
  }
}

var ReactDOMEventListener = Object.freeze({
	get _enabled () { return _enabled; },
	setEnabled: setEnabled,
	isEnabled: isEnabled,
	trapBubbledEvent: trapBubbledEvent,
	trapCapturedEvent: trapCapturedEvent,
	dispatchEvent: dispatchEvent
});

/**
 * Generate a mapping of standard vendor prefixes using the defined style property and event name.
 *
 * @param {string} styleProp
 * @param {string} eventName
 * @returns {object}
 */
function makePrefixMap(styleProp, eventName) {
  var prefixes = {};

  prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
  prefixes['Webkit' + styleProp] = 'webkit' + eventName;
  prefixes['Moz' + styleProp] = 'moz' + eventName;
  prefixes['ms' + styleProp] = 'MS' + eventName;
  prefixes['O' + styleProp] = 'o' + eventName.toLowerCase();

  return prefixes;
}

/**
 * A list of event names to a configurable list of vendor prefixes.
 */
var vendorPrefixes = {
  animationend: makePrefixMap('Animation', 'AnimationEnd'),
  animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
  animationstart: makePrefixMap('Animation', 'AnimationStart'),
  transitionend: makePrefixMap('Transition', 'TransitionEnd')
};

/**
 * Event names that have already been detected and prefixed (if applicable).
 */
var prefixedEventNames = {};

/**
 * Element to check for prefixes on.
 */
var style = {};

/**
 * Bootstrap if a DOM exists.
 */
if (ExecutionEnvironment.canUseDOM) {
  style = document.createElement('div').style;

  // On some platforms, in particular some releases of Android 4.x,
  // the un-prefixed "animation" and "transition" properties are defined on the
  // style object but the events that fire will still be prefixed, so we need
  // to check if the un-prefixed events are usable, and if not remove them from the map.
  if (!('AnimationEvent' in window)) {
    delete vendorPrefixes.animationend.animation;
    delete vendorPrefixes.animationiteration.animation;
    delete vendorPrefixes.animationstart.animation;
  }

  // Same as above
  if (!('TransitionEvent' in window)) {
    delete vendorPrefixes.transitionend.transition;
  }
}

/**
 * Attempts to determine the correct vendor prefixed event name.
 *
 * @param {string} eventName
 * @returns {string}
 */
function getVendorPrefixedEventName(eventName) {
  if (prefixedEventNames[eventName]) {
    return prefixedEventNames[eventName];
  } else if (!vendorPrefixes[eventName]) {
    return eventName;
  }

  var prefixMap = vendorPrefixes[eventName];

  for (var styleProp in prefixMap) {
    if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
      return prefixedEventNames[eventName] = prefixMap[styleProp];
    }
  }

  return eventName;
}

/**
 * Types of raw signals from the browser caught at the top level.
 *
 * For events like 'submit' or audio/video events which don't consistently
 * bubble (which we trap at a lower node than `document`), binding
 * at `document` would cause duplicate events so we don't include them here.
 */
var topLevelTypes = {
  topAnimationEnd: getVendorPrefixedEventName('animationend'),
  topAnimationIteration: getVendorPrefixedEventName('animationiteration'),
  topAnimationStart: getVendorPrefixedEventName('animationstart'),
  topBlur: 'blur',
  topCancel: 'cancel',
  topChange: 'change',
  topClick: 'click',
  topClose: 'close',
  topCompositionEnd: 'compositionend',
  topCompositionStart: 'compositionstart',
  topCompositionUpdate: 'compositionupdate',
  topContextMenu: 'contextmenu',
  topCopy: 'copy',
  topCut: 'cut',
  topDoubleClick: 'dblclick',
  topDrag: 'drag',
  topDragEnd: 'dragend',
  topDragEnter: 'dragenter',
  topDragExit: 'dragexit',
  topDragLeave: 'dragleave',
  topDragOver: 'dragover',
  topDragStart: 'dragstart',
  topDrop: 'drop',
  topFocus: 'focus',
  topInput: 'input',
  topKeyDown: 'keydown',
  topKeyPress: 'keypress',
  topKeyUp: 'keyup',
  topLoad: 'load',
  topLoadStart: 'loadstart',
  topMouseDown: 'mousedown',
  topMouseMove: 'mousemove',
  topMouseOut: 'mouseout',
  topMouseOver: 'mouseover',
  topMouseUp: 'mouseup',
  topPaste: 'paste',
  topScroll: 'scroll',
  topSelectionChange: 'selectionchange',
  topTextInput: 'textInput',
  topToggle: 'toggle',
  topTouchCancel: 'touchcancel',
  topTouchEnd: 'touchend',
  topTouchMove: 'touchmove',
  topTouchStart: 'touchstart',
  topTransitionEnd: getVendorPrefixedEventName('transitionend'),
  topWheel: 'wheel'
};

// There are so many media events, it makes sense to just
// maintain a list of them. Note these aren't technically
// "top-level" since they don't bubble. We should come up
// with a better naming convention if we come to refactoring
// the event system.
var mediaEventTypes = {
  topAbort: 'abort',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topLoadedData: 'loadeddata',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTimeUpdate: 'timeupdate',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting'
};

/**
 * Summary of `ReactBrowserEventEmitter` event handling:
 *
 *  - Top-level delegation is used to trap most native browser events. This
 *    may only occur in the main thread and is the responsibility of
 *    ReactDOMEventListener, which is injected and can therefore support
 *    pluggable event sources. This is the only work that occurs in the main
 *    thread.
 *
 *  - We normalize and de-duplicate events to account for browser quirks. This
 *    may be done in the worker thread.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginHub` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginHub` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .
 *       |           .
 *       v           .
 * +------------+    .
 * | ReactEvent |    .
 * |  Listener  |    .
 * +------------+    .                         +-----------+
 *       |           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|EventPluginHub|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var alreadyListeningTo = {};
var reactTopListenersCounter = 0;

/**
 * To ensure no conflicts with other potential React instances on the page
 */
var topListenersIDKey = '_reactListenersID' + ('' + Math.random()).slice(2);

function getListeningForDocument(mountAt) {
  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
  // directly.
  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
    mountAt[topListenersIDKey] = reactTopListenersCounter++;
    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
  }
  return alreadyListeningTo[mountAt[topListenersIDKey]];
}

/**
 * We listen for bubbled touch events on the document object.
 *
 * Firefox v8.01 (and possibly others) exhibited strange behavior when
 * mounting `onmousemove` events at some node that was not the document
 * element. The symptoms were that if your mouse is not moving over something
 * contained within that mount point (for example on the background) the
 * top-level listeners for `onmousemove` won't be called. However, if you
 * register the `mousemove` on the document object, then it will of course
 * catch all `mousemove`s. This along with iOS quirks, justifies restricting
 * top-level listeners to the document object only, at least for these
 * movement types of events and possibly all events.
 *
 * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
 *
 * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
 * they bubble to document.
 *
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @param {object} contentDocumentHandle Document which owns the container
 */
function listenTo(registrationName, contentDocumentHandle) {
  var mountAt = contentDocumentHandle;
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];

  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      if (dependency === 'topScroll') {
        trapCapturedEvent('topScroll', 'scroll', mountAt);
      } else if (dependency === 'topFocus' || dependency === 'topBlur') {
        trapCapturedEvent('topFocus', 'focus', mountAt);
        trapCapturedEvent('topBlur', 'blur', mountAt);

        // to make sure blur and focus event listeners are only attached once
        isListening.topBlur = true;
        isListening.topFocus = true;
      } else if (dependency === 'topCancel') {
        if (isEventSupported('cancel', true)) {
          trapCapturedEvent('topCancel', 'cancel', mountAt);
        }
        isListening.topCancel = true;
      } else if (dependency === 'topClose') {
        if (isEventSupported('close', true)) {
          trapCapturedEvent('topClose', 'close', mountAt);
        }
        isListening.topClose = true;
      } else if (topLevelTypes.hasOwnProperty(dependency)) {
        trapBubbledEvent(dependency, topLevelTypes[dependency], mountAt);
      }

      isListening[dependency] = true;
    }
  }
}

function isListeningToAllDependencies(registrationName, mountAt) {
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];
  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      return false;
    }
  }
  return true;
}

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */
function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }
  return node;
}

/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */
function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }
    node = node.parentNode;
  }
}

/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */
function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType === TEXT_NODE) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

/**
 * @param {DOMElement} outerNode
 * @return {?object}
 */
function getOffsets(outerNode) {
  var selection = window.getSelection && window.getSelection();

  if (!selection || selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode,
      anchorOffset = selection.anchorOffset,
      focusNode = selection.focusNode,
      focusOffset = selection.focusOffset;

  // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
  // up/down buttons on an <input type="number">. Anonymous divs do not seem to
  // expose properties, triggering a "Permission denied error" if any of its
  // properties are accessed. The only seemingly possible way to avoid erroring
  // is to access a property that typically works for non-anonymous divs and
  // catch any error that may otherwise arise. See
  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427

  try {
    /* eslint-disable no-unused-expressions */
    anchorNode.nodeType;
    focusNode.nodeType;
    /* eslint-enable no-unused-expressions */
  } catch (e) {
    return null;
  }

  return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
}

/**
 * Returns {start, end} where `start` is the character/codepoint index of
 * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
 * `end` is the index of (focusNode, focusOffset).
 *
 * Returns null if you pass in garbage input but we should probably just crash.
 *
 * Exported only for testing.
 */
function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
  var length = 0;
  var start = -1;
  var end = -1;
  var indexWithinAnchor = 0;
  var indexWithinFocus = 0;
  var node = outerNode;
  var parentNode = null;

  outer: while (true) {
    var next = null;

    while (true) {
      if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
        start = length + anchorOffset;
      }
      if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
        end = length + focusOffset;
      }

      if (node.nodeType === TEXT_NODE) {
        length += node.nodeValue.length;
      }

      if ((next = node.firstChild) === null) {
        break;
      }
      // Moving from `node` to its first child `next`.
      parentNode = node;
      node = next;
    }

    while (true) {
      if (node === outerNode) {
        // If `outerNode` has children, this is always the second time visiting
        // it. If it has no children, this is still the first loop, and the only
        // valid selection is anchorNode and focusNode both equal to this node
        // and both offsets 0, in which case we will have handled above.
        break outer;
      }
      if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
        start = length;
      }
      if (parentNode === focusNode && ++indexWithinFocus === focusOffset) {
        end = length;
      }
      if ((next = node.nextSibling) !== null) {
        break;
      }
      node = parentNode;
      parentNode = node.parentNode;
    }

    // Moving from `node` to its next sibling `next`.
    node = next;
  }

  if (start === -1 || end === -1) {
    // This should never happen. (Would happen if the anchor/focus nodes aren't
    // actually inside the passed-in node.)
    return null;
  }

  return {
    start: start,
    end: end
  };
}

/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programmatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setOffsets(node, offsets) {
  if (!window.getSelection) {
    return;
  }

  var selection = window.getSelection();
  var length = node[getTextContentAccessor()].length;
  var start = Math.min(offsets.start, length);
  var end = offsets.end === undefined ? start : Math.min(offsets.end, length);

  // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.
  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
      return;
    }
    var range = document.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }
  }
}

function isInDocument(node) {
  return containsNode(document.documentElement, node);
}

/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */

function hasSelectionCapabilities(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName && (nodeName === 'input' && elem.type === 'text' || nodeName === 'textarea' || elem.contentEditable === 'true');
}

function getSelectionInformation() {
  var focusedElem = getActiveElement();
  return {
    focusedElem: focusedElem,
    selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection$1(focusedElem) : null
  };
}

/**
 * @restoreSelection: If any selection information was potentially lost,
 * restore it. This is useful when performing operations that could remove dom
 * nodes and place them back in, resulting in focus being lost.
 */
function restoreSelection(priorSelectionInformation) {
  var curFocusedElem = getActiveElement();
  var priorFocusedElem = priorSelectionInformation.focusedElem;
  var priorSelectionRange = priorSelectionInformation.selectionRange;
  if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
    if (hasSelectionCapabilities(priorFocusedElem)) {
      setSelection(priorFocusedElem, priorSelectionRange);
    }

    // Focusing a node can change the scroll position, which is undesirable
    var ancestors = [];
    var ancestor = priorFocusedElem;
    while (ancestor = ancestor.parentNode) {
      if (ancestor.nodeType === ELEMENT_NODE) {
        ancestors.push({
          element: ancestor,
          left: ancestor.scrollLeft,
          top: ancestor.scrollTop
        });
      }
    }

    priorFocusedElem.focus();

    for (var i = 0; i < ancestors.length; i++) {
      var info = ancestors[i];
      info.element.scrollLeft = info.left;
      info.element.scrollTop = info.top;
    }
  }
}

/**
 * @getSelection: Gets the selection bounds of a focused textarea, input or
 * contentEditable node.
 * -@input: Look up selection bounds of this input
 * -@return {start: selectionStart, end: selectionEnd}
 */
function getSelection$1(input) {
  var selection = void 0;

  if ('selectionStart' in input) {
    // Modern browser with input or textarea.
    selection = {
      start: input.selectionStart,
      end: input.selectionEnd
    };
  } else {
    // Content editable or old IE textarea.
    selection = getOffsets(input);
  }

  return selection || { start: 0, end: 0 };
}

/**
 * @setSelection: Sets the selection bounds of a textarea or input and focuses
 * the input.
 * -@input     Set selection bounds of this input or textarea
 * -@offsets   Object of same form that is returned from get*
 */
function setSelection(input, offsets) {
  var start = offsets.start,
      end = offsets.end;

  if (end === undefined) {
    end = start;
  }

  if ('selectionStart' in input) {
    input.selectionStart = start;
    input.selectionEnd = Math.min(end, input.value.length);
  } else {
    setOffsets(input, offsets);
  }
}

var skipSelectionChangeEvent = ExecutionEnvironment.canUseDOM && 'documentMode' in document && document.documentMode <= 11;

var eventTypes$3 = {
  select: {
    phasedRegistrationNames: {
      bubbled: 'onSelect',
      captured: 'onSelectCapture'
    },
    dependencies: ['topBlur', 'topContextMenu', 'topFocus', 'topKeyDown', 'topKeyUp', 'topMouseDown', 'topMouseUp', 'topSelectionChange']
  }
};

var activeElement$1 = null;
var activeElementInst$1 = null;
var lastSelection = null;
var mouseDown = false;

/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getSelection(node) {
  if ('selectionStart' in node && hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else if (window.getSelection) {
    var selection = window.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  }
}

/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @return {?SyntheticEvent}
 */
function constructSelectEvent(nativeEvent, nativeEventTarget) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement()) {
    return null;
  }

  // Only fire when selection has actually changed.
  var currentSelection = getSelection(activeElement$1);
  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;

    var syntheticEvent = SyntheticEvent$1.getPooled(eventTypes$3.select, activeElementInst$1, nativeEvent, nativeEventTarget);

    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement$1;

    accumulateTwoPhaseDispatches(syntheticEvent);

    return syntheticEvent;
  }

  return null;
}

/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */
var SelectEventPlugin = {
  eventTypes: eventTypes$3,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : nativeEventTarget.nodeType === DOCUMENT_NODE ? nativeEventTarget : nativeEventTarget.ownerDocument;
    // Track whether all listeners exists for this plugin. If none exist, we do
    // not extract events. See #3639.
    if (!doc || !isListeningToAllDependencies('onSelect', doc)) {
      return null;
    }

    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    switch (topLevelType) {
      // Track the input node that has focus.
      case 'topFocus':
        if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
          activeElement$1 = targetNode;
          activeElementInst$1 = targetInst;
          lastSelection = null;
        }
        break;
      case 'topBlur':
        activeElement$1 = null;
        activeElementInst$1 = null;
        lastSelection = null;
        break;
      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.
      case 'topMouseDown':
        mouseDown = true;
        break;
      case 'topContextMenu':
      case 'topMouseUp':
        mouseDown = false;
        return constructSelectEvent(nativeEvent, nativeEventTarget);
      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't). IE's event fires out of order with respect
      // to key and input events on deletion, so we discard it.
      //
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      // This is also our approach for IE handling, for the reason above.
      case 'topSelectionChange':
        if (skipSelectionChangeEvent) {
          break;
        }
      // falls through
      case 'topKeyDown':
      case 'topKeyUp':
        return constructSelectEvent(nativeEvent, nativeEventTarget);
    }

    return null;
  }
};

/**
 * Inject modules for resolving DOM hierarchy and plugin ordering.
 */
injection.injectEventPluginOrder(DOMEventPluginOrder);
injection$1.injectComponentTree(ReactDOMComponentTree);

/**
 * Some important event plugins included by default (without having to require
 * them).
 */
injection.injectEventPluginsByName({
  SimpleEventPlugin: SimpleEventPlugin,
  EnterLeaveEventPlugin: EnterLeaveEventPlugin,
  ChangeEventPlugin: ChangeEventPlugin,
  SelectEventPlugin: SelectEventPlugin,
  BeforeInputEventPlugin: BeforeInputEventPlugin
});

// Max 31 bit integer. The max integer size in V8 for 32-bit systems.
// Math.pow(2, 30) - 1
// 0b111111111111111111111111111111
var MAX_SIGNED_31_BIT_INT = 1073741823;

// TODO: Use an opaque type once ESLint et al support the syntax


var NoWork = 0;
var Sync = 1;
var Never = MAX_SIGNED_31_BIT_INT;

var UNIT_SIZE = 10;
var MAGIC_NUMBER_OFFSET = 2;

// 1 unit of expiration time represents 10ms.
function msToExpirationTime(ms) {
  // Always add an offset so that we don't clash with the magic number for NoWork.
  return (ms / UNIT_SIZE | 0) + MAGIC_NUMBER_OFFSET;
}

function expirationTimeToMs(expirationTime) {
  return (expirationTime - MAGIC_NUMBER_OFFSET) * UNIT_SIZE;
}

function ceiling(num, precision) {
  return ((num / precision | 0) + 1) * precision;
}

function computeExpirationBucket(currentTime, expirationInMs, bucketSizeMs) {
  return ceiling(currentTime + expirationInMs / UNIT_SIZE, bucketSizeMs / UNIT_SIZE);
}

var NoContext = 0;
var AsyncMode = 1;
var StrictMode = 2;

var hasBadMapPolyfill = void 0;

{
  hasBadMapPolyfill = false;
  try {
    var nonExtensibleObject = Object.preventExtensions({});
    var testMap = new Map([[nonExtensibleObject, null]]);
    var testSet = new Set([nonExtensibleObject]);
    // This is necessary for Rollup to not consider these unused.
    // https://github.com/rollup/rollup/issues/1771
    // TODO: we can remove these if Rollup fixes the bug.
    testMap.set(0, 0);
    testSet.add(0);
  } catch (e) {
    // TODO: Consider warning about bad polyfills
    hasBadMapPolyfill = true;
  }
}

// A Fiber is work on a Component that needs to be done or was done. There can
// be more than one per component.


var debugCounter = void 0;

{
  debugCounter = 1;
}

function FiberNode(tag, pendingProps, key, mode) {
  // Instance
  this.tag = tag;
  this.key = key;
  this.type = null;
  this.stateNode = null;

  // Fiber
  this['return'] = null;
  this.child = null;
  this.sibling = null;
  this.index = 0;

  this.ref = null;

  this.pendingProps = pendingProps;
  this.memoizedProps = null;
  this.updateQueue = null;
  this.memoizedState = null;

  this.mode = mode;

  // Effects
  this.effectTag = NoEffect;
  this.nextEffect = null;

  this.firstEffect = null;
  this.lastEffect = null;

  this.expirationTime = NoWork;

  this.alternate = null;

  {
    this._debugID = debugCounter++;
    this._debugSource = null;
    this._debugOwner = null;
    this._debugIsCurrentlyTiming = false;
    if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') {
      Object.preventExtensions(this);
    }
  }
}

// This is a constructor function, rather than a POJO constructor, still
// please ensure we do the following:
// 1) Nobody should add any instance methods on this. Instance methods can be
//    more difficult to predict when they get optimized and they are almost
//    never inlined properly in static compilers.
// 2) Nobody should rely on `instanceof Fiber` for type testing. We should
//    always know when it is a fiber.
// 3) We might want to experiment with using numeric keys since they are easier
//    to optimize in a non-JIT environment.
// 4) We can easily go from a constructor to a createFiber object literal if that
//    is faster.
// 5) It should be easy to port this to a C struct and keep a C implementation
//    compatible.
var createFiber = function (tag, pendingProps, key, mode) {
  // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
  return new FiberNode(tag, pendingProps, key, mode);
};

function shouldConstruct(Component) {
  return !!(Component.prototype && Component.prototype.isReactComponent);
}

// This is used to create an alternate fiber to do work on.
function createWorkInProgress(current, pendingProps, expirationTime) {
  var workInProgress = current.alternate;
  if (workInProgress === null) {
    // We use a double buffering pooling technique because we know that we'll
    // only ever need at most two versions of a tree. We pool the "other" unused
    // node that we're free to reuse. This is lazily created to avoid allocating
    // extra objects for things that are never updated. It also allow us to
    // reclaim the extra memory if needed.
    workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode);
    workInProgress.type = current.type;
    workInProgress.stateNode = current.stateNode;

    {
      // DEV-only fields
      workInProgress._debugID = current._debugID;
      workInProgress._debugSource = current._debugSource;
      workInProgress._debugOwner = current._debugOwner;
    }

    workInProgress.alternate = current;
    current.alternate = workInProgress;
  } else {
    workInProgress.pendingProps = pendingProps;

    // We already have an alternate.
    // Reset the effect tag.
    workInProgress.effectTag = NoEffect;

    // The effect list is no longer valid.
    workInProgress.nextEffect = null;
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;
  }

  workInProgress.expirationTime = expirationTime;

  workInProgress.child = current.child;
  workInProgress.memoizedProps = current.memoizedProps;
  workInProgress.memoizedState = current.memoizedState;
  workInProgress.updateQueue = current.updateQueue;

  // These will be overridden during the parent's reconciliation
  workInProgress.sibling = current.sibling;
  workInProgress.index = current.index;
  workInProgress.ref = current.ref;

  return workInProgress;
}

function createHostRootFiber(isAsync) {
  var mode = isAsync ? AsyncMode | StrictMode : NoContext;
  return createFiber(HostRoot, null, null, mode);
}

function createFiberFromElement(element, mode, expirationTime) {
  var owner = null;
  {
    owner = element._owner;
  }

  var fiber = void 0;
  var type = element.type;
  var key = element.key;
  var pendingProps = element.props;

  var fiberTag = void 0;
  if (typeof type === 'function') {
    fiberTag = shouldConstruct(type) ? ClassComponent : IndeterminateComponent;
  } else if (typeof type === 'string') {
    fiberTag = HostComponent;
  } else {
    switch (type) {
      case REACT_FRAGMENT_TYPE:
        return createFiberFromFragment(pendingProps.children, mode, expirationTime, key);
      case REACT_ASYNC_MODE_TYPE:
        fiberTag = Mode;
        mode |= AsyncMode | StrictMode;
        break;
      case REACT_STRICT_MODE_TYPE:
        fiberTag = Mode;
        mode |= StrictMode;
        break;
      case REACT_CALL_TYPE:
        fiberTag = CallComponent;
        break;
      case REACT_RETURN_TYPE:
        fiberTag = ReturnComponent;
        break;
      default:
        {
          if (typeof type === 'object' && type !== null) {
            switch (type.$$typeof) {
              case REACT_PROVIDER_TYPE:
                fiberTag = ContextProvider;
                break;
              case REACT_CONTEXT_TYPE:
                // This is a consumer
                fiberTag = ContextConsumer;
                break;
              case REACT_FORWARD_REF_TYPE:
                fiberTag = ForwardRef;
                break;
              default:
                if (typeof type.tag === 'number') {
                  // Currently assumed to be a continuation and therefore is a
                  // fiber already.
                  // TODO: The yield system is currently broken for updates in
                  // some cases. The reified yield stores a fiber, but we don't
                  // know which fiber that is; the current or a workInProgress?
                  // When the continuation gets rendered here we don't know if we
                  // can reuse that fiber or if we need to clone it. There is
                  // probably a clever way to restructure this.
                  fiber = type;
                  fiber.pendingProps = pendingProps;
                  fiber.expirationTime = expirationTime;
                  return fiber;
                } else {
                  throwOnInvalidElementType(type, owner);
                }
                break;
            }
          } else {
            throwOnInvalidElementType(type, owner);
          }
        }
    }
  }

  fiber = createFiber(fiberTag, pendingProps, key, mode);
  fiber.type = type;
  fiber.expirationTime = expirationTime;

  {
    fiber._debugSource = element._source;
    fiber._debugOwner = element._owner;
  }

  return fiber;
}

function throwOnInvalidElementType(type, owner) {
  var info = '';
  {
    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
      info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and " + 'named imports.';
    }
    var ownerName = owner ? getComponentName(owner) : null;
    if (ownerName) {
      info += '\n\nCheck the render method of `' + ownerName + '`.';
    }
  }
  invariant(false, 'Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s', type == null ? type : typeof type, info);
}

function createFiberFromFragment(elements, mode, expirationTime, key) {
  var fiber = createFiber(Fragment, elements, key, mode);
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromText(content, mode, expirationTime) {
  var fiber = createFiber(HostText, content, null, mode);
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromHostInstanceForDeletion() {
  var fiber = createFiber(HostComponent, null, null, NoContext);
  fiber.type = 'DELETED';
  return fiber;
}

function createFiberFromPortal(portal, mode, expirationTime) {
  var pendingProps = portal.children !== null ? portal.children : [];
  var fiber = createFiber(HostPortal, pendingProps, portal.key, mode);
  fiber.expirationTime = expirationTime;
  fiber.stateNode = {
    containerInfo: portal.containerInfo,
    pendingChildren: null, // Used by persistent updates
    implementation: portal.implementation
  };
  return fiber;
}

// Used for stashing WIP properties to replay failed work in DEV.
function assignFiberPropertiesInDEV(target, source) {
  if (target === null) {
    // This Fiber's initial properties will always be overwritten.
    // We only use a Fiber to ensure the same hidden class so DEV isn't slow.
    target = createFiber(IndeterminateComponent, null, null, NoContext);
  }

  // This is intentionally written as a list of all properties.
  // We tried to use Object.assign() instead but this is called in
  // the hottest path, and Object.assign() was too slow:
  // https://github.com/facebook/react/issues/12502
  // This code is DEV-only so size is not a concern.

  target.tag = source.tag;
  target.key = source.key;
  target.type = source.type;
  target.stateNode = source.stateNode;
  target['return'] = source['return'];
  target.child = source.child;
  target.sibling = source.sibling;
  target.index = source.index;
  target.ref = source.ref;
  target.pendingProps = source.pendingProps;
  target.memoizedProps = source.memoizedProps;
  target.updateQueue = source.updateQueue;
  target.memoizedState = source.memoizedState;
  target.mode = source.mode;
  target.effectTag = source.effectTag;
  target.nextEffect = source.nextEffect;
  target.firstEffect = source.firstEffect;
  target.lastEffect = source.lastEffect;
  target.expirationTime = source.expirationTime;
  target.alternate = source.alternate;
  target._debugID = source._debugID;
  target._debugSource = source._debugSource;
  target._debugOwner = source._debugOwner;
  target._debugIsCurrentlyTiming = source._debugIsCurrentlyTiming;
  return target;
}

// TODO: This should be lifted into the renderer.


function createFiberRoot(containerInfo, isAsync, hydrate) {
  // Cyclic construction. This cheats the type system right now because
  // stateNode is any.
  var uninitializedFiber = createHostRootFiber(isAsync);
  var root = {
    current: uninitializedFiber,
    containerInfo: containerInfo,
    pendingChildren: null,
    pendingCommitExpirationTime: NoWork,
    finishedWork: null,
    context: null,
    pendingContext: null,
    hydrate: hydrate,
    remainingExpirationTime: NoWork,
    firstBatch: null,
    nextScheduledRoot: null
  };
  uninitializedFiber.stateNode = root;
  return root;
}

var onCommitFiberRoot = null;
var onCommitFiberUnmount = null;
var hasLoggedError = false;

function catchErrors(fn) {
  return function (arg) {
    try {
      return fn(arg);
    } catch (err) {
      if (true && !hasLoggedError) {
        hasLoggedError = true;
        warning(false, 'React DevTools encountered an error: %s', err);
      }
    }
  };
}

function injectInternals(internals) {
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
    // No DevTools
    return false;
  }
  var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
  if (hook.isDisabled) {
    // This isn't a real property on the hook, but it can be set to opt out
    // of DevTools integration and associated warnings and logs.
    // https://github.com/facebook/react/issues/3877
    return true;
  }
  if (!hook.supportsFiber) {
    {
      warning(false, 'The installed version of React DevTools is too old and will not work ' + 'with the current version of React. Please update React DevTools. ' + 'https://fb.me/react-devtools');
    }
    // DevTools exists, even though it doesn't support Fiber.
    return true;
  }
  try {
    var rendererID = hook.inject(internals);
    // We have successfully injected, so now it is safe to set up hooks.
    onCommitFiberRoot = catchErrors(function (root) {
      return hook.onCommitFiberRoot(rendererID, root);
    });
    onCommitFiberUnmount = catchErrors(function (fiber) {
      return hook.onCommitFiberUnmount(rendererID, fiber);
    });
  } catch (err) {
    // Catch all errors because it is unsafe to throw during initialization.
    {
      warning(false, 'React DevTools encountered an error: %s.', err);
    }
  }
  // DevTools exists
  return true;
}

function onCommitRoot(root) {
  if (typeof onCommitFiberRoot === 'function') {
    onCommitFiberRoot(root);
  }
}

function onCommitUnmount(fiber) {
  if (typeof onCommitFiberUnmount === 'function') {
    onCommitFiberUnmount(fiber);
  }
}

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

var ReactStrictModeWarnings = {
  discardPendingWarnings: function () {},
  flushPendingDeprecationWarnings: function () {},
  flushPendingUnsafeLifecycleWarnings: function () {},
  recordDeprecationWarnings: function (fiber, instance) {},
  recordUnsafeLifecycleWarnings: function (fiber, instance) {}
};

{
  var LIFECYCLE_SUGGESTIONS = {
    UNSAFE_componentWillMount: 'componentDidMount',
    UNSAFE_componentWillReceiveProps: 'static getDerivedStateFromProps',
    UNSAFE_componentWillUpdate: 'componentDidUpdate'
  };

  var pendingComponentWillMountWarnings = [];
  var pendingComponentWillReceivePropsWarnings = [];
  var pendingComponentWillUpdateWarnings = [];
  var pendingUnsafeLifecycleWarnings = new Map();

  // Tracks components we have already warned about.
  var didWarnAboutDeprecatedLifecycles = new Set();
  var didWarnAboutUnsafeLifecycles = new Set();

  var setToSortedString = function (set) {
    var array = [];
    set.forEach(function (value) {
      array.push(value);
    });
    return array.sort().join(', ');
  };

  ReactStrictModeWarnings.discardPendingWarnings = function () {
    pendingComponentWillMountWarnings = [];
    pendingComponentWillReceivePropsWarnings = [];
    pendingComponentWillUpdateWarnings = [];
    pendingUnsafeLifecycleWarnings = new Map();
  };

  ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function () {
    pendingUnsafeLifecycleWarnings.forEach(function (lifecycleWarningsMap, strictRoot) {
      var lifecyclesWarningMesages = [];

      Object.keys(lifecycleWarningsMap).forEach(function (lifecycle) {
        var lifecycleWarnings = lifecycleWarningsMap[lifecycle];
        if (lifecycleWarnings.length > 0) {
          var componentNames = new Set();
          lifecycleWarnings.forEach(function (fiber) {
            componentNames.add(getComponentName(fiber) || 'Component');
            didWarnAboutUnsafeLifecycles.add(fiber.type);
          });

          var formatted = lifecycle.replace('UNSAFE_', '');
          var suggestion = LIFECYCLE_SUGGESTIONS[lifecycle];
          var sortedComponentNames = setToSortedString(componentNames);

          lifecyclesWarningMesages.push(formatted + ': Please update the following components to use ' + (suggestion + ' instead: ' + sortedComponentNames));
        }
      });

      if (lifecyclesWarningMesages.length > 0) {
        var strictRootComponentStack = getStackAddendumByWorkInProgressFiber(strictRoot);

        warning(false, 'Unsafe lifecycle methods were found within a strict-mode tree:%s' + '\n\n%s' + '\n\nLearn more about this warning here:' + '\nhttps://fb.me/react-strict-mode-warnings', strictRootComponentStack, lifecyclesWarningMesages.join('\n\n'));
      }
    });

    pendingUnsafeLifecycleWarnings = new Map();
  };

  var getStrictRoot = function (fiber) {
    var maybeStrictRoot = null;

    while (fiber !== null) {
      if (fiber.mode & StrictMode) {
        maybeStrictRoot = fiber;
      }

      fiber = fiber['return'];
    }

    return maybeStrictRoot;
  };

  ReactStrictModeWarnings.flushPendingDeprecationWarnings = function () {
    if (pendingComponentWillMountWarnings.length > 0) {
      var uniqueNames = new Set();
      pendingComponentWillMountWarnings.forEach(function (fiber) {
        uniqueNames.add(getComponentName(fiber) || 'Component');
        didWarnAboutDeprecatedLifecycles.add(fiber.type);
      });

      var sortedNames = setToSortedString(uniqueNames);

      lowPriorityWarning$1(false, 'componentWillMount is deprecated and will be removed in the next major version. ' + 'Use componentDidMount instead. As a temporary workaround, ' + 'you can rename to UNSAFE_componentWillMount.' + '\n\nPlease update the following components: %s' + '\n\nLearn more about this warning here:' + '\nhttps://fb.me/react-async-component-lifecycle-hooks', sortedNames);

      pendingComponentWillMountWarnings = [];
    }

    if (pendingComponentWillReceivePropsWarnings.length > 0) {
      var _uniqueNames = new Set();
      pendingComponentWillReceivePropsWarnings.forEach(function (fiber) {
        _uniqueNames.add(getComponentName(fiber) || 'Component');
        didWarnAboutDeprecatedLifecycles.add(fiber.type);
      });

      var _sortedNames = setToSortedString(_uniqueNames);

      lowPriorityWarning$1(false, 'componentWillReceiveProps is deprecated and will be removed in the next major version. ' + 'Use static getDerivedStateFromProps instead.' + '\n\nPlease update the following components: %s' + '\n\nLearn more about this warning here:' + '\nhttps://fb.me/react-async-component-lifecycle-hooks', _sortedNames);

      pendingComponentWillReceivePropsWarnings = [];
    }

    if (pendingComponentWillUpdateWarnings.length > 0) {
      var _uniqueNames2 = new Set();
      pendingComponentWillUpdateWarnings.forEach(function (fiber) {
        _uniqueNames2.add(getComponentName(fiber) || 'Component');
        didWarnAboutDeprecatedLifecycles.add(fiber.type);
      });

      var _sortedNames2 = setToSortedString(_uniqueNames2);

      lowPriorityWarning$1(false, 'componentWillUpdate is deprecated and will be removed in the next major version. ' + 'Use componentDidUpdate instead. As a temporary workaround, ' + 'you can rename to UNSAFE_componentWillUpdate.' + '\n\nPlease update the following components: %s' + '\n\nLearn more about this warning here:' + '\nhttps://fb.me/react-async-component-lifecycle-hooks', _sortedNames2);

      pendingComponentWillUpdateWarnings = [];
    }
  };

  ReactStrictModeWarnings.recordDeprecationWarnings = function (fiber, instance) {
    // Dedup strategy: Warn once per component.
    if (didWarnAboutDeprecatedLifecycles.has(fiber.type)) {
      return;
    }

    // Don't warn about react-lifecycles-compat polyfilled components.
    if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) {
      pendingComponentWillMountWarnings.push(fiber);
    }
    if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
      pendingComponentWillReceivePropsWarnings.push(fiber);
    }
    if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
      pendingComponentWillUpdateWarnings.push(fiber);
    }
  };

  ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function (fiber, instance) {
    var strictRoot = getStrictRoot(fiber);

    // Dedup strategy: Warn once per component.
    // This is difficult to track any other way since component names
    // are often vague and are likely to collide between 3rd party libraries.
    // An expand property is probably okay to use here since it's DEV-only,
    // and will only be set in the event of serious warnings.
    if (didWarnAboutUnsafeLifecycles.has(fiber.type)) {
      return;
    }

    // Don't warn about react-lifecycles-compat polyfilled components.
    // Note that it is sufficient to check for the presence of a
    // single lifecycle, componentWillMount, with the polyfill flag.
    if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning === true) {
      return;
    }

    var warningsForRoot = void 0;
    if (!pendingUnsafeLifecycleWarnings.has(strictRoot)) {
      warningsForRoot = {
        UNSAFE_componentWillMount: [],
        UNSAFE_componentWillReceiveProps: [],
        UNSAFE_componentWillUpdate: []
      };

      pendingUnsafeLifecycleWarnings.set(strictRoot, warningsForRoot);
    } else {
      warningsForRoot = pendingUnsafeLifecycleWarnings.get(strictRoot);
    }

    var unsafeLifecycles = [];
    if (typeof instance.componentWillMount === 'function' || typeof instance.UNSAFE_componentWillMount === 'function') {
      unsafeLifecycles.push('UNSAFE_componentWillMount');
    }
    if (typeof instance.componentWillReceiveProps === 'function' || typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
      unsafeLifecycles.push('UNSAFE_componentWillReceiveProps');
    }
    if (typeof instance.componentWillUpdate === 'function' || typeof instance.UNSAFE_componentWillUpdate === 'function') {
      unsafeLifecycles.push('UNSAFE_componentWillUpdate');
    }

    if (unsafeLifecycles.length > 0) {
      unsafeLifecycles.forEach(function (lifecycle) {
        warningsForRoot[lifecycle].push(fiber);
      });
    }
  };
}

// Exports ReactDOM.createRoot
var enableUserTimingAPI = true;

// Mutating mode (React DOM, React ART, React Native):
var enableMutatingReconciler = true;
// Experimental noop mode (currently unused):
var enableNoopReconciler = false;
// Experimental persistent mode (Fabric):
var enablePersistentReconciler = false;
// Experimental error-boundary API that can recover from errors within a single
// render phase
var enableGetDerivedStateFromCatch = false;
// Helps identify side effects in begin-phase lifecycle hooks and setState reducers:
var debugRenderPhaseSideEffects = false;

// In some cases, StrictMode should also double-render lifecycles.
// This can be confusing for tests though,
// And it can be bad for performance in production.
// This feature flag can be used to control the behavior:
var debugRenderPhaseSideEffectsForStrictMode = true;

// To preserve the "Pause on caught exceptions" behavior of the debugger, we
// replay the begin phase of a failed component inside invokeGuardedCallback.
var replayFailedUnitOfWorkWithInvokeGuardedCallback = true;

// Warn about deprecated, async-unsafe lifecycles; relates to RFC #6:
var warnAboutDeprecatedLifecycles = false;

var alwaysUseRequestIdleCallbackPolyfill = false;

// Only used in www builds.

// Prefix measurements so that it's possible to filter them.
// Longer prefixes are hard to read in DevTools.
var reactEmoji = '\u269B';
var warningEmoji = '\u26D4';
var supportsUserTiming = typeof performance !== 'undefined' && typeof performance.mark === 'function' && typeof performance.clearMarks === 'function' && typeof performance.measure === 'function' && typeof performance.clearMeasures === 'function';

// Keep track of current fiber so that we know the path to unwind on pause.
// TODO: this looks the same as nextUnitOfWork in scheduler. Can we unify them?
var currentFiber = null;
// If we're in the middle of user code, which fiber and method is it?
// Reusing `currentFiber` would be confusing for this because user code fiber
// can change during commit phase too, but we don't need to unwind it (since
// lifecycles in the commit phase don't resemble a tree).
var currentPhase = null;
var currentPhaseFiber = null;
// Did lifecycle hook schedule an update? This is often a performance problem,
// so we will keep track of it, and include it in the report.
// Track commits caused by cascading updates.
var isCommitting = false;
var hasScheduledUpdateInCurrentCommit = false;
var hasScheduledUpdateInCurrentPhase = false;
var commitCountInCurrentWorkLoop = 0;
var effectCountInCurrentCommit = 0;
var isWaitingForCallback = false;
// During commits, we only show a measurement once per method name
// to avoid stretch the commit phase with measurement overhead.
var labelsInCurrentCommit = new Set();

var formatMarkName = function (markName) {
  return reactEmoji + ' ' + markName;
};

var formatLabel = function (label, warning$$1) {
  var prefix = warning$$1 ? warningEmoji + ' ' : reactEmoji + ' ';
  var suffix = warning$$1 ? ' Warning: ' + warning$$1 : '';
  return '' + prefix + label + suffix;
};

var beginMark = function (markName) {
  performance.mark(formatMarkName(markName));
};

var clearMark = function (markName) {
  performance.clearMarks(formatMarkName(markName));
};

var endMark = function (label, markName, warning$$1) {
  var formattedMarkName = formatMarkName(markName);
  var formattedLabel = formatLabel(label, warning$$1);
  try {
    performance.measure(formattedLabel, formattedMarkName);
  } catch (err) {}
  // If previous mark was missing for some reason, this will throw.
  // This could only happen if React crashed in an unexpected place earlier.
  // Don't pile on with more errors.

  // Clear marks immediately to avoid growing buffer.
  performance.clearMarks(formattedMarkName);
  performance.clearMeasures(formattedLabel);
};

var getFiberMarkName = function (label, debugID) {
  return label + ' (#' + debugID + ')';
};

var getFiberLabel = function (componentName, isMounted, phase) {
  if (phase === null) {
    // These are composite component total time measurements.
    return componentName + ' [' + (isMounted ? 'update' : 'mount') + ']';
  } else {
    // Composite component methods.
    return componentName + '.' + phase;
  }
};

var beginFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);

  if (isCommitting && labelsInCurrentCommit.has(label)) {
    // During the commit phase, we don't show duplicate labels because
    // there is a fixed overhead for every measurement, and we don't
    // want to stretch the commit phase beyond necessary.
    return false;
  }
  labelsInCurrentCommit.add(label);

  var markName = getFiberMarkName(label, debugID);
  beginMark(markName);
  return true;
};

var clearFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  clearMark(markName);
};

var endFiberMark = function (fiber, phase, warning$$1) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  endMark(label, markName, warning$$1);
};

var shouldIgnoreFiber = function (fiber) {
  // Host components should be skipped in the timeline.
  // We could check typeof fiber.type, but does this work with RN?
  switch (fiber.tag) {
    case HostRoot:
    case HostComponent:
    case HostText:
    case HostPortal:
    case CallComponent:
    case ReturnComponent:
    case Fragment:
    case ContextProvider:
    case ContextConsumer:
    case Mode:
      return true;
    default:
      return false;
  }
};

var clearPendingPhaseMeasurement = function () {
  if (currentPhase !== null && currentPhaseFiber !== null) {
    clearFiberMark(currentPhaseFiber, currentPhase);
  }
  currentPhaseFiber = null;
  currentPhase = null;
  hasScheduledUpdateInCurrentPhase = false;
};

var pauseTimers = function () {
  // Stops all currently active measurements so that they can be resumed
  // if we continue in a later deferred loop from the same unit of work.
  var fiber = currentFiber;
  while (fiber) {
    if (fiber._debugIsCurrentlyTiming) {
      endFiberMark(fiber, null, null);
    }
    fiber = fiber['return'];
  }
};

var resumeTimersRecursively = function (fiber) {
  if (fiber['return'] !== null) {
    resumeTimersRecursively(fiber['return']);
  }
  if (fiber._debugIsCurrentlyTiming) {
    beginFiberMark(fiber, null);
  }
};

var resumeTimers = function () {
  // Resumes all measurements that were active during the last deferred loop.
  if (currentFiber !== null) {
    resumeTimersRecursively(currentFiber);
  }
};

function recordEffect() {
  if (enableUserTimingAPI) {
    effectCountInCurrentCommit++;
  }
}

function recordScheduleUpdate() {
  if (enableUserTimingAPI) {
    if (isCommitting) {
      hasScheduledUpdateInCurrentCommit = true;
    }
    if (currentPhase !== null && currentPhase !== 'componentWillMount' && currentPhase !== 'componentWillReceiveProps') {
      hasScheduledUpdateInCurrentPhase = true;
    }
  }
}

function startRequestCallbackTimer() {
  if (enableUserTimingAPI) {
    if (supportsUserTiming && !isWaitingForCallback) {
      isWaitingForCallback = true;
      beginMark('(Waiting for async callback...)');
    }
  }
}

function stopRequestCallbackTimer(didExpire, expirationTime) {
  if (enableUserTimingAPI) {
    if (supportsUserTiming) {
      isWaitingForCallback = false;
      var warning$$1 = didExpire ? 'React was blocked by main thread' : null;
      endMark('(Waiting for async callback... will force flush in ' + expirationTime + ' ms)', '(Waiting for async callback...)', warning$$1);
    }
  }
}

function startWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, this is the fiber to unwind from.
    currentFiber = fiber;
    if (!beginFiberMark(fiber, null)) {
      return;
    }
    fiber._debugIsCurrentlyTiming = true;
  }
}

function cancelWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // Remember we shouldn't complete measurement for this fiber.
    // Otherwise flamechart will be deep even for small updates.
    fiber._debugIsCurrentlyTiming = false;
    clearFiberMark(fiber, null);
  }
}

function stopWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    endFiberMark(fiber, null, null);
  }
}

function stopFailedWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    var warning$$1 = 'An error was thrown inside this error boundary';
    endFiberMark(fiber, null, warning$$1);
  }
}

function startPhaseTimer(fiber, phase) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    clearPendingPhaseMeasurement();
    if (!beginFiberMark(fiber, phase)) {
      return;
    }
    currentPhaseFiber = fiber;
    currentPhase = phase;
  }
}

function stopPhaseTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    if (currentPhase !== null && currentPhaseFiber !== null) {
      var warning$$1 = hasScheduledUpdateInCurrentPhase ? 'Scheduled a cascading update' : null;
      endFiberMark(currentPhaseFiber, currentPhase, warning$$1);
    }
    currentPhase = null;
    currentPhaseFiber = null;
  }
}

function startWorkLoopTimer(nextUnitOfWork) {
  if (enableUserTimingAPI) {
    currentFiber = nextUnitOfWork;
    if (!supportsUserTiming) {
      return;
    }
    commitCountInCurrentWorkLoop = 0;
    // This is top level call.
    // Any other measurements are performed within.
    beginMark('(React Tree Reconciliation)');
    // Resume any measurements that were in progress during the last loop.
    resumeTimers();
  }
}

function stopWorkLoopTimer(interruptedBy, didCompleteRoot) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var warning$$1 = null;
    if (interruptedBy !== null) {
      if (interruptedBy.tag === HostRoot) {
        warning$$1 = 'A top-level update interrupted the previous render';
      } else {
        var componentName = getComponentName(interruptedBy) || 'Unknown';
        warning$$1 = 'An update to ' + componentName + ' interrupted the previous render';
      }
    } else if (commitCountInCurrentWorkLoop > 1) {
      warning$$1 = 'There were cascading updates';
    }
    commitCountInCurrentWorkLoop = 0;
    var label = didCompleteRoot ? '(React Tree Reconciliation: Completed Root)' : '(React Tree Reconciliation: Yielded)';
    // Pause any measurements until the next loop.
    pauseTimers();
    endMark(label, '(React Tree Reconciliation)', warning$$1);
  }
}

function startCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    isCommitting = true;
    hasScheduledUpdateInCurrentCommit = false;
    labelsInCurrentCommit.clear();
    beginMark('(Committing Changes)');
  }
}

function stopCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }

    var warning$$1 = null;
    if (hasScheduledUpdateInCurrentCommit) {
      warning$$1 = 'Lifecycle hook scheduled a cascading update';
    } else if (commitCountInCurrentWorkLoop > 0) {
      warning$$1 = 'Caused by a cascading update in earlier commit';
    }
    hasScheduledUpdateInCurrentCommit = false;
    commitCountInCurrentWorkLoop++;
    isCommitting = false;
    labelsInCurrentCommit.clear();

    endMark('(Committing Changes)', '(Committing Changes)', warning$$1);
  }
}

function startCommitSnapshotEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Committing Snapshot Effects)');
  }
}

function stopCommitSnapshotEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Committing Snapshot Effects: ' + count + ' Total)', '(Committing Snapshot Effects)', null);
  }
}

function startCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Committing Host Effects)');
  }
}

function stopCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Committing Host Effects: ' + count + ' Total)', '(Committing Host Effects)', null);
  }
}

function startCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Calling Lifecycle Methods)');
  }
}

function stopCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Calling Lifecycle Methods: ' + count + ' Total)', '(Calling Lifecycle Methods)', null);
  }
}

var didWarnUpdateInsideUpdate = void 0;

{
  didWarnUpdateInsideUpdate = false;
}

// Callbacks are not validated until invocation


// Singly linked-list of updates. When an update is scheduled, it is added to
// the queue of the current fiber and the work-in-progress fiber. The two queues
// are separate but they share a persistent structure.
//
// During reconciliation, updates are removed from the work-in-progress fiber,
// but they remain on the current fiber. That ensures that if a work-in-progress
// is aborted, the aborted updates are recovered by cloning from current.
//
// The work-in-progress queue is always a subset of the current queue.
//
// When the tree is committed, the work-in-progress becomes the current.


function createUpdateQueue(baseState) {
  var queue = {
    baseState: baseState,
    expirationTime: NoWork,
    first: null,
    last: null,
    callbackList: null,
    hasForceUpdate: false,
    isInitialized: false,
    capturedValues: null
  };
  {
    queue.isProcessing = false;
  }
  return queue;
}

function insertUpdateIntoQueue(queue, update) {
  // Append the update to the end of the list.
  if (queue.last === null) {
    // Queue is empty
    queue.first = queue.last = update;
  } else {
    queue.last.next = update;
    queue.last = update;
  }
  if (queue.expirationTime === NoWork || queue.expirationTime > update.expirationTime) {
    queue.expirationTime = update.expirationTime;
  }
}

var q1 = void 0;
var q2 = void 0;
function ensureUpdateQueues(fiber) {
  q1 = q2 = null;
  // We'll have at least one and at most two distinct update queues.
  var alternateFiber = fiber.alternate;
  var queue1 = fiber.updateQueue;
  if (queue1 === null) {
    // TODO: We don't know what the base state will be until we begin work.
    // It depends on which fiber is the next current. Initialize with an empty
    // base state, then set to the memoizedState when rendering. Not super
    // happy with this approach.
    queue1 = fiber.updateQueue = createUpdateQueue(null);
  }

  var queue2 = void 0;
  if (alternateFiber !== null) {
    queue2 = alternateFiber.updateQueue;
    if (queue2 === null) {
      queue2 = alternateFiber.updateQueue = createUpdateQueue(null);
    }
  } else {
    queue2 = null;
  }
  queue2 = queue2 !== queue1 ? queue2 : null;

  // Use module variables instead of returning a tuple
  q1 = queue1;
  q2 = queue2;
}

function insertUpdateIntoFiber(fiber, update) {
  ensureUpdateQueues(fiber);
  var queue1 = q1;
  var queue2 = q2;

  // Warn if an update is scheduled from inside an updater function.
  {
    if ((queue1.isProcessing || queue2 !== null && queue2.isProcessing) && !didWarnUpdateInsideUpdate) {
      warning(false, 'An update (setState, replaceState, or forceUpdate) was scheduled ' + 'from inside an update function. Update functions should be pure, ' + 'with zero side-effects. Consider using componentDidUpdate or a ' + 'callback.');
      didWarnUpdateInsideUpdate = true;
    }
  }

  // If there's only one queue, add the update to that queue and exit.
  if (queue2 === null) {
    insertUpdateIntoQueue(queue1, update);
    return;
  }

  // If either queue is empty, we need to add to both queues.
  if (queue1.last === null || queue2.last === null) {
    insertUpdateIntoQueue(queue1, update);
    insertUpdateIntoQueue(queue2, update);
    return;
  }

  // If both lists are not empty, the last update is the same for both lists
  // because of structural sharing. So, we should only append to one of
  // the lists.
  insertUpdateIntoQueue(queue1, update);
  // But we still need to update the `last` pointer of queue2.
  queue2.last = update;
}

function getUpdateExpirationTime(fiber) {
  switch (fiber.tag) {
    case HostRoot:
    case ClassComponent:
      var updateQueue = fiber.updateQueue;
      if (updateQueue === null) {
        return NoWork;
      }
      return updateQueue.expirationTime;
    default:
      return NoWork;
  }
}

function getStateFromUpdate(update, instance, prevState, props) {
  var partialState = update.partialState;
  if (typeof partialState === 'function') {
    return partialState.call(instance, prevState, props);
  } else {
    return partialState;
  }
}

function processUpdateQueue(current, workInProgress, queue, instance, props, renderExpirationTime) {
  if (current !== null && current.updateQueue === queue) {
    // We need to create a work-in-progress queue, by cloning the current queue.
    var currentQueue = queue;
    queue = workInProgress.updateQueue = {
      baseState: currentQueue.baseState,
      expirationTime: currentQueue.expirationTime,
      first: currentQueue.first,
      last: currentQueue.last,
      isInitialized: currentQueue.isInitialized,
      capturedValues: currentQueue.capturedValues,
      // These fields are no longer valid because they were already committed.
      // Reset them.
      callbackList: null,
      hasForceUpdate: false
    };
  }

  {
    // Set this flag so we can warn if setState is called inside the update
    // function of another setState.
    queue.isProcessing = true;
  }

  // Reset the remaining expiration time. If we skip over any updates, we'll
  // increase this accordingly.
  queue.expirationTime = NoWork;

  // TODO: We don't know what the base state will be until we begin work.
  // It depends on which fiber is the next current. Initialize with an empty
  // base state, then set to the memoizedState when rendering. Not super
  // happy with this approach.
  var state = void 0;
  if (queue.isInitialized) {
    state = queue.baseState;
  } else {
    state = queue.baseState = workInProgress.memoizedState;
    queue.isInitialized = true;
  }
  var dontMutatePrevState = true;
  var update = queue.first;
  var didSkip = false;
  while (update !== null) {
    var updateExpirationTime = update.expirationTime;
    if (updateExpirationTime > renderExpirationTime) {
      // This update does not have sufficient priority. Skip it.
      var remainingExpirationTime = queue.expirationTime;
      if (remainingExpirationTime === NoWork || remainingExpirationTime > updateExpirationTime) {
        // Update the remaining expiration time.
        queue.expirationTime = updateExpirationTime;
      }
      if (!didSkip) {
        didSkip = true;
        queue.baseState = state;
      }
      // Continue to the next update.
      update = update.next;
      continue;
    }

    // This update does have sufficient priority.

    // If no previous updates were skipped, drop this update from the queue by
    // advancing the head of the list.
    if (!didSkip) {
      queue.first = update.next;
      if (queue.first === null) {
        queue.last = null;
      }
    }

    // Invoke setState callback an extra time to help detect side-effects.
    // Ignore the return value in this case.
    if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
      getStateFromUpdate(update, instance, state, props);
    }

    // Process the update
    var _partialState = void 0;
    if (update.isReplace) {
      state = getStateFromUpdate(update, instance, state, props);
      dontMutatePrevState = true;
    } else {
      _partialState = getStateFromUpdate(update, instance, state, props);
      if (_partialState) {
        if (dontMutatePrevState) {
          // $FlowFixMe: Idk how to type this properly.
          state = _assign({}, state, _partialState);
        } else {
          state = _assign(state, _partialState);
        }
        dontMutatePrevState = false;
      }
    }
    if (update.isForced) {
      queue.hasForceUpdate = true;
    }
    if (update.callback !== null) {
      // Append to list of callbacks.
      var _callbackList = queue.callbackList;
      if (_callbackList === null) {
        _callbackList = queue.callbackList = [];
      }
      _callbackList.push(update);
    }
    if (update.capturedValue !== null) {
      var _capturedValues = queue.capturedValues;
      if (_capturedValues === null) {
        queue.capturedValues = [update.capturedValue];
      } else {
        _capturedValues.push(update.capturedValue);
      }
    }
    update = update.next;
  }

  if (queue.callbackList !== null) {
    workInProgress.effectTag |= Callback;
  } else if (queue.first === null && !queue.hasForceUpdate && queue.capturedValues === null) {
    // The queue is empty. We can reset it.
    workInProgress.updateQueue = null;
  }

  if (!didSkip) {
    didSkip = true;
    queue.baseState = state;
  }

  {
    // No longer processing.
    queue.isProcessing = false;
  }

  return state;
}

function commitCallbacks(queue, context) {
  var callbackList = queue.callbackList;
  if (callbackList === null) {
    return;
  }
  // Set the list to null to make sure they don't get called more than once.
  queue.callbackList = null;
  for (var i = 0; i < callbackList.length; i++) {
    var update = callbackList[i];
    var _callback = update.callback;
    // This update might be processed again. Clear the callback so it's only
    // called once.
    update.callback = null;
    !(typeof _callback === 'function') ? invariant(false, 'Invalid argument passed as callback. Expected a function. Instead received: %s', _callback) : void 0;
    _callback.call(context);
  }
}

var fakeInternalInstance = {};
var isArray = Array.isArray;

var didWarnAboutStateAssignmentForComponent = void 0;
var didWarnAboutUndefinedDerivedState = void 0;
var didWarnAboutUninitializedState = void 0;
var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = void 0;
var didWarnAboutLegacyLifecyclesAndDerivedState = void 0;
var warnOnInvalidCallback$1 = void 0;

{
  didWarnAboutStateAssignmentForComponent = new Set();
  didWarnAboutUndefinedDerivedState = new Set();
  didWarnAboutUninitializedState = new Set();
  didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = new Set();
  didWarnAboutLegacyLifecyclesAndDerivedState = new Set();

  var didWarnOnInvalidCallback = new Set();

  warnOnInvalidCallback$1 = function (callback, callerName) {
    if (callback === null || typeof callback === 'function') {
      return;
    }
    var key = callerName + '_' + callback;
    if (!didWarnOnInvalidCallback.has(key)) {
      didWarnOnInvalidCallback.add(key);
      warning(false, '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
    }
  };

  // This is so gross but it's at least non-critical and can be removed if
  // it causes problems. This is meant to give a nicer error message for
  // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
  // ...)) which otherwise throws a "_processChildContext is not a function"
  // exception.
  Object.defineProperty(fakeInternalInstance, '_processChildContext', {
    enumerable: false,
    value: function () {
      invariant(false, '_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn\'t supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).');
    }
  });
  Object.freeze(fakeInternalInstance);
}
function callGetDerivedStateFromCatch(ctor, capturedValues) {
  var resultState = {};
  for (var i = 0; i < capturedValues.length; i++) {
    var capturedValue = capturedValues[i];
    var error = capturedValue.value;
    var partialState = ctor.getDerivedStateFromCatch.call(null, error);
    if (partialState !== null && partialState !== undefined) {
      _assign(resultState, partialState);
    }
  }
  return resultState;
}

var ReactFiberClassComponent = function (legacyContext, scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState) {
  var cacheContext = legacyContext.cacheContext,
      getMaskedContext = legacyContext.getMaskedContext,
      getUnmaskedContext = legacyContext.getUnmaskedContext,
      isContextConsumer = legacyContext.isContextConsumer,
      hasContextChanged = legacyContext.hasContextChanged;

  // Class component state updater

  var updater = {
    isMounted: isMounted,
    enqueueSetState: function (instance, partialState, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback$1(callback, 'setState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: partialState,
        callback: callback,
        isReplace: false,
        isForced: false,
        capturedValue: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueReplaceState: function (instance, state, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback$1(callback, 'replaceState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: state,
        callback: callback,
        isReplace: true,
        isForced: false,
        capturedValue: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueForceUpdate: function (instance, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback$1(callback, 'forceUpdate');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: null,
        callback: callback,
        isReplace: false,
        isForced: true,
        capturedValue: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    }
  };

  function checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext) {
    if (oldProps === null || workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate) {
      // If the workInProgress already has an Update effect, return true
      return true;
    }

    var instance = workInProgress.stateNode;
    var ctor = workInProgress.type;
    if (typeof instance.shouldComponentUpdate === 'function') {
      startPhaseTimer(workInProgress, 'shouldComponentUpdate');
      var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, newContext);
      stopPhaseTimer();

      {
        !(shouldUpdate !== undefined) ? warning(false, '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', getComponentName(workInProgress) || 'Component') : void 0;
      }

      return shouldUpdate;
    }

    if (ctor.prototype && ctor.prototype.isPureReactComponent) {
      return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
    }

    return true;
  }

  function checkClassInstance(workInProgress) {
    var instance = workInProgress.stateNode;
    var type = workInProgress.type;
    {
      var name = getComponentName(workInProgress) || 'Component';
      var renderPresent = instance.render;

      if (!renderPresent) {
        if (type.prototype && typeof type.prototype.render === 'function') {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: did you accidentally return an object from the constructor?', name);
        } else {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`.', name);
        }
      }

      var noGetInitialStateOnES6 = !instance.getInitialState || instance.getInitialState.isReactClassApproved || instance.state;
      !noGetInitialStateOnES6 ? warning(false, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', name) : void 0;
      var noGetDefaultPropsOnES6 = !instance.getDefaultProps || instance.getDefaultProps.isReactClassApproved;
      !noGetDefaultPropsOnES6 ? warning(false, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', name) : void 0;
      var noInstancePropTypes = !instance.propTypes;
      !noInstancePropTypes ? warning(false, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', name) : void 0;
      var noInstanceContextTypes = !instance.contextTypes;
      !noInstanceContextTypes ? warning(false, 'contextTypes was defined as an instance property on %s. Use a static ' + 'property to define contextTypes instead.', name) : void 0;
      var noComponentShouldUpdate = typeof instance.componentShouldUpdate !== 'function';
      !noComponentShouldUpdate ? warning(false, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', name) : void 0;
      if (type.prototype && type.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') {
        warning(false, '%s has a method called shouldComponentUpdate(). ' + 'shouldComponentUpdate should not be used when extending React.PureComponent. ' + 'Please extend React.Component if shouldComponentUpdate is used.', getComponentName(workInProgress) || 'A pure component');
      }
      var noComponentDidUnmount = typeof instance.componentDidUnmount !== 'function';
      !noComponentDidUnmount ? warning(false, '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', name) : void 0;
      var noComponentDidReceiveProps = typeof instance.componentDidReceiveProps !== 'function';
      !noComponentDidReceiveProps ? warning(false, '%s has a method called ' + 'componentDidReceiveProps(). But there is no such lifecycle method. ' + 'If you meant to update the state in response to changing props, ' + 'use componentWillReceiveProps(). If you meant to fetch data or ' + 'run side-effects or mutations after React has updated the UI, use componentDidUpdate().', name) : void 0;
      var noComponentWillRecieveProps = typeof instance.componentWillRecieveProps !== 'function';
      !noComponentWillRecieveProps ? warning(false, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', name) : void 0;
      var noUnsafeComponentWillRecieveProps = typeof instance.UNSAFE_componentWillRecieveProps !== 'function';
      !noUnsafeComponentWillRecieveProps ? warning(false, '%s has a method called ' + 'UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?', name) : void 0;
      var hasMutatedProps = instance.props !== workInProgress.pendingProps;
      !(instance.props === undefined || !hasMutatedProps) ? warning(false, '%s(...): When calling super() in `%s`, make sure to pass ' + "up the same props that your component's constructor was passed.", name, name) : void 0;
      var noInstanceDefaultProps = !instance.defaultProps;
      !noInstanceDefaultProps ? warning(false, 'Setting defaultProps as an instance property on %s is not supported and will be ignored.' + ' Instead, define defaultProps as a static property on %s.', name, name) : void 0;

      if (typeof instance.getSnapshotBeforeUpdate === 'function' && typeof instance.componentDidUpdate !== 'function' && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(type)) {
        didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(type);
        warning(false, '%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). ' + 'This component defines getSnapshotBeforeUpdate() only.', getComponentName(workInProgress));
      }

      var noInstanceGetDerivedStateFromProps = typeof instance.getDerivedStateFromProps !== 'function';
      !noInstanceGetDerivedStateFromProps ? warning(false, '%s: getDerivedStateFromProps() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name) : void 0;
      var noInstanceGetDerivedStateFromCatch = typeof instance.getDerivedStateFromCatch !== 'function';
      !noInstanceGetDerivedStateFromCatch ? warning(false, '%s: getDerivedStateFromCatch() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name) : void 0;
      var noStaticGetSnapshotBeforeUpdate = typeof type.getSnapshotBeforeUpdate !== 'function';
      !noStaticGetSnapshotBeforeUpdate ? warning(false, '%s: getSnapshotBeforeUpdate() is defined as a static method ' + 'and will be ignored. Instead, declare it as an instance method.', name) : void 0;
      var _state = instance.state;
      if (_state && (typeof _state !== 'object' || isArray(_state))) {
        warning(false, '%s.state: must be set to an object or null', name);
      }
      if (typeof instance.getChildContext === 'function') {
        !(typeof type.childContextTypes === 'object') ? warning(false, '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', name) : void 0;
      }
    }
  }

  function resetInputPointers(workInProgress, instance) {
    instance.props = workInProgress.memoizedProps;
    instance.state = workInProgress.memoizedState;
  }

  function adoptClassInstance(workInProgress, instance) {
    instance.updater = updater;
    workInProgress.stateNode = instance;
    // The instance needs access to the fiber so that it can schedule updates
    set(instance, workInProgress);
    {
      instance._reactInternalInstance = fakeInternalInstance;
    }
  }

  function constructClassInstance(workInProgress, props) {
    var ctor = workInProgress.type;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var needsContext = isContextConsumer(workInProgress);
    var context = needsContext ? getMaskedContext(workInProgress, unmaskedContext) : emptyObject;

    // Instantiate twice to help detect side-effects.
    if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
      new ctor(props, context); // eslint-disable-line no-new
    }

    var instance = new ctor(props, context);
    var state = instance.state !== null && instance.state !== undefined ? instance.state : null;
    adoptClassInstance(workInProgress, instance);

    {
      if (typeof ctor.getDerivedStateFromProps === 'function' && state === null) {
        var componentName = getComponentName(workInProgress) || 'Component';
        if (!didWarnAboutUninitializedState.has(componentName)) {
          didWarnAboutUninitializedState.add(componentName);
          warning(false, '%s: Did not properly initialize state during construction. ' + 'Expected state to be an object, but it was %s.', componentName, instance.state === null ? 'null' : 'undefined');
        }
      }

      // If new component APIs are defined, "unsafe" lifecycles won't be called.
      // Warn about these lifecycles if they are present.
      // Don't warn about react-lifecycles-compat polyfilled methods though.
      if (typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function') {
        var foundWillMountName = null;
        var foundWillReceivePropsName = null;
        var foundWillUpdateName = null;
        if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) {
          foundWillMountName = 'componentWillMount';
        } else if (typeof instance.UNSAFE_componentWillMount === 'function') {
          foundWillMountName = 'UNSAFE_componentWillMount';
        }
        if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
          foundWillReceivePropsName = 'componentWillReceiveProps';
        } else if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
          foundWillReceivePropsName = 'UNSAFE_componentWillReceiveProps';
        }
        if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
          foundWillUpdateName = 'componentWillUpdate';
        } else if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
          foundWillUpdateName = 'UNSAFE_componentWillUpdate';
        }
        if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
          var _componentName = getComponentName(workInProgress) || 'Component';
          var newApiName = typeof ctor.getDerivedStateFromProps === 'function' ? 'getDerivedStateFromProps()' : 'getSnapshotBeforeUpdate()';
          if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
            didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);
            warning(false, 'Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n' + '%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\n' + 'The above lifecycles should be removed. Learn more about this warning here:\n' + 'https://fb.me/react-async-component-lifecycle-hooks', _componentName, newApiName, foundWillMountName !== null ? '\n  ' + foundWillMountName : '', foundWillReceivePropsName !== null ? '\n  ' + foundWillReceivePropsName : '', foundWillUpdateName !== null ? '\n  ' + foundWillUpdateName : '');
          }
        }
      }
    }

    workInProgress.memoizedState = state;

    var partialState = callGetDerivedStateFromProps(workInProgress, instance, props, state);

    if (partialState !== null && partialState !== undefined) {
      // Render-phase updates (like this) should not be added to the update queue,
      // So that multiple render passes do not enqueue multiple updates.
      // Instead, just synchronously merge the returned state into the instance.
      workInProgress.memoizedState = _assign({}, workInProgress.memoizedState, partialState);
    }

    // Cache unmasked context so we can avoid recreating masked context unless necessary.
    // ReactFiberContext usually updates this cache but can't for newly-created instances.
    if (needsContext) {
      cacheContext(workInProgress, unmaskedContext, context);
    }

    return instance;
  }

  function callComponentWillMount(workInProgress, instance) {
    startPhaseTimer(workInProgress, 'componentWillMount');
    var oldState = instance.state;

    if (typeof instance.componentWillMount === 'function') {
      instance.componentWillMount();
    }
    if (typeof instance.UNSAFE_componentWillMount === 'function') {
      instance.UNSAFE_componentWillMount();
    }

    stopPhaseTimer();

    if (oldState !== instance.state) {
      {
        warning(false, '%s.componentWillMount(): Assigning directly to this.state is ' + "deprecated (except inside a component's " + 'constructor). Use setState instead.', getComponentName(workInProgress) || 'Component');
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  function callComponentWillReceiveProps(workInProgress, instance, newProps, newContext) {
    var oldState = instance.state;
    startPhaseTimer(workInProgress, 'componentWillReceiveProps');
    if (typeof instance.componentWillReceiveProps === 'function') {
      instance.componentWillReceiveProps(newProps, newContext);
    }
    if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
      instance.UNSAFE_componentWillReceiveProps(newProps, newContext);
    }
    stopPhaseTimer();

    if (instance.state !== oldState) {
      {
        var componentName = getComponentName(workInProgress) || 'Component';
        if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
          didWarnAboutStateAssignmentForComponent.add(componentName);
          warning(false, '%s.componentWillReceiveProps(): Assigning directly to ' + "this.state is deprecated (except inside a component's " + 'constructor). Use setState instead.', componentName);
        }
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  function callGetDerivedStateFromProps(workInProgress, instance, nextProps, prevState) {
    var type = workInProgress.type;


    if (typeof type.getDerivedStateFromProps === 'function') {
      if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
        // Invoke method an extra time to help detect side-effects.
        type.getDerivedStateFromProps.call(null, nextProps, prevState);
      }

      var partialState = type.getDerivedStateFromProps.call(null, nextProps, prevState);

      {
        if (partialState === undefined) {
          var componentName = getComponentName(workInProgress) || 'Component';
          if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
            didWarnAboutUndefinedDerivedState.add(componentName);
            warning(false, '%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. ' + 'You have returned undefined.', componentName);
          }
        }
      }

      return partialState;
    }
  }

  // Invokes the mount life-cycles on a previously never rendered instance.
  function mountClassInstance(workInProgress, renderExpirationTime) {
    var ctor = workInProgress.type;
    var current = workInProgress.alternate;

    {
      checkClassInstance(workInProgress);
    }

    var instance = workInProgress.stateNode;
    var props = workInProgress.pendingProps;
    var unmaskedContext = getUnmaskedContext(workInProgress);

    instance.props = props;
    instance.state = workInProgress.memoizedState;
    instance.refs = emptyObject;
    instance.context = getMaskedContext(workInProgress, unmaskedContext);

    {
      if (workInProgress.mode & StrictMode) {
        ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, instance);
      }

      if (warnAboutDeprecatedLifecycles) {
        ReactStrictModeWarnings.recordDeprecationWarnings(workInProgress, instance);
      }
    }

    // In order to support react-lifecycles-compat polyfilled components,
    // Unsafe lifecycles should not be invoked for components using the new APIs.
    if (typeof ctor.getDerivedStateFromProps !== 'function' && typeof instance.getSnapshotBeforeUpdate !== 'function' && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
      callComponentWillMount(workInProgress, instance);
      // If we had additional state updates during this life-cycle, let's
      // process them now.
      var updateQueue = workInProgress.updateQueue;
      if (updateQueue !== null) {
        instance.state = processUpdateQueue(current, workInProgress, updateQueue, instance, props, renderExpirationTime);
      }
    }
    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    }
  }

  function resumeMountClassInstance(workInProgress, renderExpirationTime) {
    var ctor = workInProgress.type;
    var instance = workInProgress.stateNode;
    resetInputPointers(workInProgress, instance);

    var oldProps = workInProgress.memoizedProps;
    var newProps = workInProgress.pendingProps;
    var oldContext = instance.context;
    var newUnmaskedContext = getUnmaskedContext(workInProgress);
    var newContext = getMaskedContext(workInProgress, newUnmaskedContext);

    var hasNewLifecycles = typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function';

    // Note: During these life-cycles, instance.props/instance.state are what
    // ever the previously attempted to render - not the "current". However,
    // during componentDidUpdate we pass the "current" props.

    // In order to support react-lifecycles-compat polyfilled components,
    // Unsafe lifecycles should not be invoked for components using the new APIs.
    if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
      if (oldProps !== newProps || oldContext !== newContext) {
        callComponentWillReceiveProps(workInProgress, instance, newProps, newContext);
      }
    }

    // Compute the next state using the memoized state and the update queue.
    var oldState = workInProgress.memoizedState;
    // TODO: Previous state can be null.
    var newState = void 0;
    var derivedStateFromCatch = void 0;
    if (workInProgress.updateQueue !== null) {
      newState = processUpdateQueue(null, workInProgress, workInProgress.updateQueue, instance, newProps, renderExpirationTime);

      var updateQueue = workInProgress.updateQueue;
      if (updateQueue !== null && updateQueue.capturedValues !== null && enableGetDerivedStateFromCatch && typeof ctor.getDerivedStateFromCatch === 'function') {
        var capturedValues = updateQueue.capturedValues;
        // Don't remove these from the update queue yet. We need them in
        // finishClassComponent. Do the reset there.
        // TODO: This is awkward. Refactor class components.
        // updateQueue.capturedValues = null;
        derivedStateFromCatch = callGetDerivedStateFromCatch(ctor, capturedValues);
      }
    } else {
      newState = oldState;
    }

    var derivedStateFromProps = void 0;
    if (oldProps !== newProps) {
      // The prevState parameter should be the partially updated state.
      // Otherwise, spreading state in return values could override updates.
      derivedStateFromProps = callGetDerivedStateFromProps(workInProgress, instance, newProps, newState);
    }

    if (derivedStateFromProps !== null && derivedStateFromProps !== undefined) {
      // Render-phase updates (like this) should not be added to the update queue,
      // So that multiple render passes do not enqueue multiple updates.
      // Instead, just synchronously merge the returned state into the instance.
      newState = newState === null || newState === undefined ? derivedStateFromProps : _assign({}, newState, derivedStateFromProps);

      // Update the base state of the update queue.
      // FIXME: This is getting ridiculous. Refactor plz!
      var _updateQueue = workInProgress.updateQueue;
      if (_updateQueue !== null) {
        _updateQueue.baseState = _assign({}, _updateQueue.baseState, derivedStateFromProps);
      }
    }
    if (derivedStateFromCatch !== null && derivedStateFromCatch !== undefined) {
      // Render-phase updates (like this) should not be added to the update queue,
      // So that multiple render passes do not enqueue multiple updates.
      // Instead, just synchronously merge the returned state into the instance.
      newState = newState === null || newState === undefined ? derivedStateFromCatch : _assign({}, newState, derivedStateFromCatch);

      // Update the base state of the update queue.
      // FIXME: This is getting ridiculous. Refactor plz!
      var _updateQueue2 = workInProgress.updateQueue;
      if (_updateQueue2 !== null) {
        _updateQueue2.baseState = _assign({}, _updateQueue2.baseState, derivedStateFromCatch);
      }
    }

    if (oldProps === newProps && oldState === newState && !hasContextChanged() && !(workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate)) {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidMount === 'function') {
        workInProgress.effectTag |= Update;
      }
      return false;
    }

    var shouldUpdate = checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext);

    if (shouldUpdate) {
      // In order to support react-lifecycles-compat polyfilled components,
      // Unsafe lifecycles should not be invoked for components using the new APIs.
      if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
        startPhaseTimer(workInProgress, 'componentWillMount');
        if (typeof instance.componentWillMount === 'function') {
          instance.componentWillMount();
        }
        if (typeof instance.UNSAFE_componentWillMount === 'function') {
          instance.UNSAFE_componentWillMount();
        }
        stopPhaseTimer();
      }
      if (typeof instance.componentDidMount === 'function') {
        workInProgress.effectTag |= Update;
      }
    } else {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidMount === 'function') {
        workInProgress.effectTag |= Update;
      }

      // If shouldComponentUpdate returned false, we should still update the
      // memoized props/state to indicate that this work can be reused.
      memoizeProps(workInProgress, newProps);
      memoizeState(workInProgress, newState);
    }

    // Update the existing instance's state, props, and context pointers even
    // if shouldComponentUpdate returns false.
    instance.props = newProps;
    instance.state = newState;
    instance.context = newContext;

    return shouldUpdate;
  }

  // Invokes the update life-cycles and returns false if it shouldn't rerender.
  function updateClassInstance(current, workInProgress, renderExpirationTime) {
    var ctor = workInProgress.type;
    var instance = workInProgress.stateNode;
    resetInputPointers(workInProgress, instance);

    var oldProps = workInProgress.memoizedProps;
    var newProps = workInProgress.pendingProps;
    var oldContext = instance.context;
    var newUnmaskedContext = getUnmaskedContext(workInProgress);
    var newContext = getMaskedContext(workInProgress, newUnmaskedContext);

    var hasNewLifecycles = typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function';

    // Note: During these life-cycles, instance.props/instance.state are what
    // ever the previously attempted to render - not the "current". However,
    // during componentDidUpdate we pass the "current" props.

    // In order to support react-lifecycles-compat polyfilled components,
    // Unsafe lifecycles should not be invoked for components using the new APIs.
    if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
      if (oldProps !== newProps || oldContext !== newContext) {
        callComponentWillReceiveProps(workInProgress, instance, newProps, newContext);
      }
    }

    // Compute the next state using the memoized state and the update queue.
    var oldState = workInProgress.memoizedState;
    // TODO: Previous state can be null.
    var newState = void 0;
    var derivedStateFromCatch = void 0;

    if (workInProgress.updateQueue !== null) {
      newState = processUpdateQueue(current, workInProgress, workInProgress.updateQueue, instance, newProps, renderExpirationTime);

      var updateQueue = workInProgress.updateQueue;
      if (updateQueue !== null && updateQueue.capturedValues !== null && enableGetDerivedStateFromCatch && typeof ctor.getDerivedStateFromCatch === 'function') {
        var capturedValues = updateQueue.capturedValues;
        // Don't remove these from the update queue yet. We need them in
        // finishClassComponent. Do the reset there.
        // TODO: This is awkward. Refactor class components.
        // updateQueue.capturedValues = null;
        derivedStateFromCatch = callGetDerivedStateFromCatch(ctor, capturedValues);
      }
    } else {
      newState = oldState;
    }

    var derivedStateFromProps = void 0;
    if (oldProps !== newProps) {
      // The prevState parameter should be the partially updated state.
      // Otherwise, spreading state in return values could override updates.
      derivedStateFromProps = callGetDerivedStateFromProps(workInProgress, instance, newProps, newState);
    }

    if (derivedStateFromProps !== null && derivedStateFromProps !== undefined) {
      // Render-phase updates (like this) should not be added to the update queue,
      // So that multiple render passes do not enqueue multiple updates.
      // Instead, just synchronously merge the returned state into the instance.
      newState = newState === null || newState === undefined ? derivedStateFromProps : _assign({}, newState, derivedStateFromProps);

      // Update the base state of the update queue.
      // FIXME: This is getting ridiculous. Refactor plz!
      var _updateQueue3 = workInProgress.updateQueue;
      if (_updateQueue3 !== null) {
        _updateQueue3.baseState = _assign({}, _updateQueue3.baseState, derivedStateFromProps);
      }
    }
    if (derivedStateFromCatch !== null && derivedStateFromCatch !== undefined) {
      // Render-phase updates (like this) should not be added to the update queue,
      // So that multiple render passes do not enqueue multiple updates.
      // Instead, just synchronously merge the returned state into the instance.
      newState = newState === null || newState === undefined ? derivedStateFromCatch : _assign({}, newState, derivedStateFromCatch);

      // Update the base state of the update queue.
      // FIXME: This is getting ridiculous. Refactor plz!
      var _updateQueue4 = workInProgress.updateQueue;
      if (_updateQueue4 !== null) {
        _updateQueue4.baseState = _assign({}, _updateQueue4.baseState, derivedStateFromCatch);
      }
    }

    if (oldProps === newProps && oldState === newState && !hasContextChanged() && !(workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate)) {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }
      if (typeof instance.getSnapshotBeforeUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Snapshot;
        }
      }
      return false;
    }

    var shouldUpdate = checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext);

    if (shouldUpdate) {
      // In order to support react-lifecycles-compat polyfilled components,
      // Unsafe lifecycles should not be invoked for components using the new APIs.
      if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === 'function' || typeof instance.componentWillUpdate === 'function')) {
        startPhaseTimer(workInProgress, 'componentWillUpdate');
        if (typeof instance.componentWillUpdate === 'function') {
          instance.componentWillUpdate(newProps, newState, newContext);
        }
        if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
          instance.UNSAFE_componentWillUpdate(newProps, newState, newContext);
        }
        stopPhaseTimer();
      }
      if (typeof instance.componentDidUpdate === 'function') {
        workInProgress.effectTag |= Update;
      }
      if (typeof instance.getSnapshotBeforeUpdate === 'function') {
        workInProgress.effectTag |= Snapshot;
      }
    } else {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }
      if (typeof instance.getSnapshotBeforeUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Snapshot;
        }
      }

      // If shouldComponentUpdate returned false, we should still update the
      // memoized props/state to indicate that this work can be reused.
      memoizeProps(workInProgress, newProps);
      memoizeState(workInProgress, newState);
    }

    // Update the existing instance's state, props, and context pointers even
    // if shouldComponentUpdate returns false.
    instance.props = newProps;
    instance.state = newState;
    instance.context = newContext;

    return shouldUpdate;
  }

  return {
    adoptClassInstance: adoptClassInstance,
    callGetDerivedStateFromProps: callGetDerivedStateFromProps,
    constructClassInstance: constructClassInstance,
    mountClassInstance: mountClassInstance,
    resumeMountClassInstance: resumeMountClassInstance,
    updateClassInstance: updateClassInstance
  };
};

var getCurrentFiberStackAddendum$2 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


var didWarnAboutMaps = void 0;
var didWarnAboutStringRefInStrictMode = void 0;
var ownerHasKeyUseWarning = void 0;
var ownerHasFunctionTypeWarning = void 0;
var warnForMissingKey = function (child) {};

{
  didWarnAboutMaps = false;
  didWarnAboutStringRefInStrictMode = {};

  /**
   * Warn if there's no key explicitly set on dynamic arrays of children or
   * object keys are not valid. This allows us to keep track of children between
   * updates.
   */
  ownerHasKeyUseWarning = {};
  ownerHasFunctionTypeWarning = {};

  warnForMissingKey = function (child) {
    if (child === null || typeof child !== 'object') {
      return;
    }
    if (!child._store || child._store.validated || child.key != null) {
      return;
    }
    !(typeof child._store === 'object') ? invariant(false, 'React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    child._store.validated = true;

    var currentComponentErrorInfo = 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.' + (getCurrentFiberStackAddendum$2() || '');
    if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
      return;
    }
    ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

    warning(false, 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.%s', getCurrentFiberStackAddendum$2());
  };
}

var isArray$1 = Array.isArray;

function coerceRef(returnFiber, current, element) {
  var mixedRef = element.ref;
  if (mixedRef !== null && typeof mixedRef !== 'function' && typeof mixedRef !== 'object') {
    {
      if (returnFiber.mode & StrictMode) {
        var componentName = getComponentName(returnFiber) || 'Component';
        if (!didWarnAboutStringRefInStrictMode[componentName]) {
          warning(false, 'A string ref, "%s", has been found within a strict mode tree. ' + 'String refs are a source of potential bugs and should be avoided. ' + 'We recommend using createRef() instead.' + '\n%s' + '\n\nLearn more about using refs safely here:' + '\nhttps://fb.me/react-strict-mode-string-ref', mixedRef, getStackAddendumByWorkInProgressFiber(returnFiber));
          didWarnAboutStringRefInStrictMode[componentName] = true;
        }
      }
    }

    if (element._owner) {
      var owner = element._owner;
      var inst = void 0;
      if (owner) {
        var ownerFiber = owner;
        !(ownerFiber.tag === ClassComponent) ? invariant(false, 'Stateless function components cannot have refs.') : void 0;
        inst = ownerFiber.stateNode;
      }
      !inst ? invariant(false, 'Missing owner for string ref %s. This error is likely caused by a bug in React. Please file an issue.', mixedRef) : void 0;
      var stringRef = '' + mixedRef;
      // Check if previous string ref matches new string ref
      if (current !== null && current.ref !== null && current.ref._stringRef === stringRef) {
        return current.ref;
      }
      var ref = function (value) {
        var refs = inst.refs === emptyObject ? inst.refs = {} : inst.refs;
        if (value === null) {
          delete refs[stringRef];
        } else {
          refs[stringRef] = value;
        }
      };
      ref._stringRef = stringRef;
      return ref;
    } else {
      !(typeof mixedRef === 'string') ? invariant(false, 'Expected ref to be a function or a string.') : void 0;
      !element._owner ? invariant(false, 'Element ref was specified as a string (%s) but no owner was set. This could happen for one of the following reasons:\n1. You may be adding a ref to a functional component\n2. You may be adding a ref to a component that was not created inside a component\'s render method\n3. You have multiple copies of React loaded\nSee https://fb.me/react-refs-must-have-owner for more information.', mixedRef) : void 0;
    }
  }
  return mixedRef;
}

function throwOnInvalidObjectType(returnFiber, newChild) {
  if (returnFiber.type !== 'textarea') {
    var addendum = '';
    {
      addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + (getCurrentFiberStackAddendum$2() || '');
    }
    invariant(false, 'Objects are not valid as a React child (found: %s).%s', Object.prototype.toString.call(newChild) === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : newChild, addendum);
  }
}

function warnOnFunctionType() {
  var currentComponentErrorInfo = 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.' + (getCurrentFiberStackAddendum$2() || '');

  if (ownerHasFunctionTypeWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasFunctionTypeWarning[currentComponentErrorInfo] = true;

  warning(false, 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.%s', getCurrentFiberStackAddendum$2() || '');
}

// This wrapper function exists because I expect to clone the code in each path
// to be able to optimize each path individually by branching early. This needs
// a compiler or we can do it manually. Helpers that don't need this branching
// live outside of this function.
function ChildReconciler(shouldTrackSideEffects) {
  function deleteChild(returnFiber, childToDelete) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return;
    }
    // Deletions are added in reversed order so we add it to the front.
    // At this point, the return fiber's effect list is empty except for
    // deletions, so we can just append the deletion to the list. The remaining
    // effects aren't added until the complete phase. Once we implement
    // resuming, this may not be true.
    var last = returnFiber.lastEffect;
    if (last !== null) {
      last.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
    childToDelete.nextEffect = null;
    childToDelete.effectTag = Deletion;
  }

  function deleteRemainingChildren(returnFiber, currentFirstChild) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return null;
    }

    // TODO: For the shouldClone case, this could be micro-optimized a bit by
    // assuming that after the first child we've already added everything.
    var childToDelete = currentFirstChild;
    while (childToDelete !== null) {
      deleteChild(returnFiber, childToDelete);
      childToDelete = childToDelete.sibling;
    }
    return null;
  }

  function mapRemainingChildren(returnFiber, currentFirstChild) {
    // Add the remaining children to a temporary map so that we can find them by
    // keys quickly. Implicit (null) keys get added to this set with their index
    var existingChildren = new Map();

    var existingChild = currentFirstChild;
    while (existingChild !== null) {
      if (existingChild.key !== null) {
        existingChildren.set(existingChild.key, existingChild);
      } else {
        existingChildren.set(existingChild.index, existingChild);
      }
      existingChild = existingChild.sibling;
    }
    return existingChildren;
  }

  function useFiber(fiber, pendingProps, expirationTime) {
    // We currently set sibling to null and index to 0 here because it is easy
    // to forget to do before returning it. E.g. for the single child case.
    var clone = createWorkInProgress(fiber, pendingProps, expirationTime);
    clone.index = 0;
    clone.sibling = null;
    return clone;
  }

  function placeChild(newFiber, lastPlacedIndex, newIndex) {
    newFiber.index = newIndex;
    if (!shouldTrackSideEffects) {
      // Noop.
      return lastPlacedIndex;
    }
    var current = newFiber.alternate;
    if (current !== null) {
      var oldIndex = current.index;
      if (oldIndex < lastPlacedIndex) {
        // This is a move.
        newFiber.effectTag = Placement;
        return lastPlacedIndex;
      } else {
        // This item can stay in place.
        return oldIndex;
      }
    } else {
      // This is an insertion.
      newFiber.effectTag = Placement;
      return lastPlacedIndex;
    }
  }

  function placeSingleChild(newFiber) {
    // This is simpler for the single child case. We only need to do a
    // placement for inserting new children.
    if (shouldTrackSideEffects && newFiber.alternate === null) {
      newFiber.effectTag = Placement;
    }
    return newFiber;
  }

  function updateTextNode(returnFiber, current, textContent, expirationTime) {
    if (current === null || current.tag !== HostText) {
      // Insert
      var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateElement(returnFiber, current, element, expirationTime) {
    if (current !== null && current.type === element.type) {
      // Move based on index
      var existing = useFiber(current, element.props, expirationTime);
      existing.ref = coerceRef(returnFiber, current, element);
      existing['return'] = returnFiber;
      {
        existing._debugSource = element._source;
        existing._debugOwner = element._owner;
      }
      return existing;
    } else {
      // Insert
      var created = createFiberFromElement(element, returnFiber.mode, expirationTime);
      created.ref = coerceRef(returnFiber, current, element);
      created['return'] = returnFiber;
      return created;
    }
  }

  function updatePortal(returnFiber, current, portal, expirationTime) {
    if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
      // Insert
      var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, portal.children || [], expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateFragment(returnFiber, current, fragment, expirationTime, key) {
    if (current === null || current.tag !== Fragment) {
      // Insert
      var created = createFiberFromFragment(fragment, returnFiber.mode, expirationTime, key);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, fragment, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function createChild(returnFiber, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      var created = createFiberFromText('' + newChild, returnFiber.mode, expirationTime);
      created['return'] = returnFiber;
      return created;
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _created = createFiberFromElement(newChild, returnFiber.mode, expirationTime);
            _created.ref = coerceRef(returnFiber, null, newChild);
            _created['return'] = returnFiber;
            return _created;
          }
        case REACT_PORTAL_TYPE:
          {
            var _created2 = createFiberFromPortal(newChild, returnFiber.mode, expirationTime);
            _created2['return'] = returnFiber;
            return _created2;
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _created3 = createFiberFromFragment(newChild, returnFiber.mode, expirationTime, null);
        _created3['return'] = returnFiber;
        return _created3;
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateSlot(returnFiber, oldFiber, newChild, expirationTime) {
    // Update the fiber if the keys match, otherwise return null.

    var key = oldFiber !== null ? oldFiber.key : null;

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      if (key !== null) {
        return null;
      }
      return updateTextNode(returnFiber, oldFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.key === key) {
              if (newChild.type === REACT_FRAGMENT_TYPE) {
                return updateFragment(returnFiber, oldFiber, newChild.props.children, expirationTime, key);
              }
              return updateElement(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }
        case REACT_PORTAL_TYPE:
          {
            if (newChild.key === key) {
              return updatePortal(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        if (key !== null) {
          return null;
        }

        return updateFragment(returnFiber, oldFiber, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateFromMap(existingChildren, returnFiber, newIdx, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys, so we neither have to check the old nor
      // new node for the key. If both are text nodes, they match.
      var matchedFiber = existingChildren.get(newIdx) || null;
      return updateTextNode(returnFiber, matchedFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            if (newChild.type === REACT_FRAGMENT_TYPE) {
              return updateFragment(returnFiber, _matchedFiber, newChild.props.children, expirationTime, newChild.key);
            }
            return updateElement(returnFiber, _matchedFiber, newChild, expirationTime);
          }
        case REACT_PORTAL_TYPE:
          {
            var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            return updatePortal(returnFiber, _matchedFiber2, newChild, expirationTime);
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _matchedFiber3 = existingChildren.get(newIdx) || null;
        return updateFragment(returnFiber, _matchedFiber3, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  /**
   * Warns if there is a duplicate or missing key
   */
  function warnOnInvalidKey(child, knownKeys) {
    {
      if (typeof child !== 'object' || child === null) {
        return knownKeys;
      }
      switch (child.$$typeof) {
        case REACT_ELEMENT_TYPE:
        case REACT_PORTAL_TYPE:
          warnForMissingKey(child);
          var key = child.key;
          if (typeof key !== 'string') {
            break;
          }
          if (knownKeys === null) {
            knownKeys = new Set();
            knownKeys.add(key);
            break;
          }
          if (!knownKeys.has(key)) {
            knownKeys.add(key);
            break;
          }
          warning(false, 'Encountered two children with the same key, `%s`. ' + 'Keys should be unique so that components maintain their identity ' + 'across updates. Non-unique keys may cause children to be ' + 'duplicated and/or omitted — the behavior is unsupported and ' + 'could change in a future version.%s', key, getCurrentFiberStackAddendum$2());
          break;
        default:
          break;
      }
    }
    return knownKeys;
  }

  function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, expirationTime) {
    // This algorithm can't optimize by searching from boths ends since we
    // don't have backpointers on fibers. I'm trying to see how far we can get
    // with that model. If it ends up not being worth the tradeoffs, we can
    // add it later.

    // Even with a two ended optimization, we'd want to optimize for the case
    // where there are few changes and brute force the comparison instead of
    // going for the Map. It'd like to explore hitting that path first in
    // forward-only mode and only go for the Map once we notice that we need
    // lots of look ahead. This doesn't handle reversal as well as two ended
    // search but that's unusual. Besides, for the two ended optimization to
    // work on Iterables, we'd need to copy the whole set.

    // In this first iteration, we'll just live with hitting the bad case
    // (adding everything to a Map) in for every insert/move.

    // If you change this code, also update reconcileChildrenIterator() which
    // uses the same algorithm.

    {
      // First, validate keys.
      var knownKeys = null;
      for (var i = 0; i < newChildren.length; i++) {
        var child = newChildren[i];
        knownKeys = warnOnInvalidKey(child, knownKeys);
      }
    }

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;
    for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (oldFiber === null) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (newIdx === newChildren.length) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; newIdx < newChildren.length; newIdx++) {
        var _newFiber = createChild(returnFiber, newChildren[newIdx], expirationTime);
        if (!_newFiber) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber;
        } else {
          previousNewFiber.sibling = _newFiber;
        }
        previousNewFiber = _newFiber;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; newIdx < newChildren.length; newIdx++) {
      var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], expirationTime);
      if (_newFiber2) {
        if (shouldTrackSideEffects) {
          if (_newFiber2.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber2.key === null ? newIdx : _newFiber2.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber2;
        } else {
          previousNewFiber.sibling = _newFiber2;
        }
        previousNewFiber = _newFiber2;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, expirationTime) {
    // This is the same implementation as reconcileChildrenArray(),
    // but using the iterator instead.

    var iteratorFn = getIteratorFn(newChildrenIterable);
    !(typeof iteratorFn === 'function') ? invariant(false, 'An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    {
      // Warn about using Maps as children
      if (typeof newChildrenIterable.entries === 'function') {
        var possibleMap = newChildrenIterable;
        if (possibleMap.entries === iteratorFn) {
          !didWarnAboutMaps ? warning(false, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', getCurrentFiberStackAddendum$2()) : void 0;
          didWarnAboutMaps = true;
        }
      }

      // First, validate keys.
      // We'll get a different iterator later for the main pass.
      var _newChildren = iteratorFn.call(newChildrenIterable);
      if (_newChildren) {
        var knownKeys = null;
        var _step = _newChildren.next();
        for (; !_step.done; _step = _newChildren.next()) {
          var child = _step.value;
          knownKeys = warnOnInvalidKey(child, knownKeys);
        }
      }
    }

    var newChildren = iteratorFn.call(newChildrenIterable);
    !(newChildren != null) ? invariant(false, 'An iterable object provided no iterator.') : void 0;

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;

    var step = newChildren.next();
    for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, step.value, expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (!oldFiber) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (step.done) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; !step.done; newIdx++, step = newChildren.next()) {
        var _newFiber3 = createChild(returnFiber, step.value, expirationTime);
        if (_newFiber3 === null) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber3;
        } else {
          previousNewFiber.sibling = _newFiber3;
        }
        previousNewFiber = _newFiber3;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; !step.done; newIdx++, step = newChildren.next()) {
      var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, expirationTime);
      if (_newFiber4 !== null) {
        if (shouldTrackSideEffects) {
          if (_newFiber4.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber4.key === null ? newIdx : _newFiber4.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber4;
        } else {
          previousNewFiber.sibling = _newFiber4;
        }
        previousNewFiber = _newFiber4;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, expirationTime) {
    // There's no need to check for keys on text nodes since we don't have a
    // way to define them.
    if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
      // We already have an existing node so let's just update it and delete
      // the rest.
      deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
      var existing = useFiber(currentFirstChild, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
    // The existing first child is not a text node so we need to create one
    // and delete the existing ones.
    deleteRemainingChildren(returnFiber, currentFirstChild);
    var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSingleElement(returnFiber, currentFirstChild, element, expirationTime) {
    var key = element.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === Fragment ? element.type === REACT_FRAGMENT_TYPE : child.type === element.type) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, element.type === REACT_FRAGMENT_TYPE ? element.props.children : element.props, expirationTime);
          existing.ref = coerceRef(returnFiber, child, element);
          existing['return'] = returnFiber;
          {
            existing._debugSource = element._source;
            existing._debugOwner = element._owner;
          }
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    if (element.type === REACT_FRAGMENT_TYPE) {
      var created = createFiberFromFragment(element.props.children, returnFiber.mode, expirationTime, element.key);
      created['return'] = returnFiber;
      return created;
    } else {
      var _created4 = createFiberFromElement(element, returnFiber.mode, expirationTime);
      _created4.ref = coerceRef(returnFiber, currentFirstChild, element);
      _created4['return'] = returnFiber;
      return _created4;
    }
  }

  function reconcileSinglePortal(returnFiber, currentFirstChild, portal, expirationTime) {
    var key = portal.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, portal.children || [], expirationTime);
          existing['return'] = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  // This API will tag the children with the side-effect of the reconciliation
  // itself. They will be added to the side-effect list as we pass through the
  // children and the parent.
  function reconcileChildFibers(returnFiber, currentFirstChild, newChild, expirationTime) {
    // This function is not recursive.
    // If the top level item is an array, we treat it as a set of children,
    // not as a fragment. Nested arrays on the other hand will be treated as
    // fragment nodes. Recursion happens at the normal flow.

    // Handle top level unkeyed fragments as if they were arrays.
    // This leads to an ambiguity between <>{[...]}</> and <>...</>.
    // We treat the ambiguous cases above the same.
    if (typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null) {
      newChild = newChild.props.children;
    }

    // Handle object types
    var isObject = typeof newChild === 'object' && newChild !== null;

    if (isObject) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, expirationTime));
        case REACT_PORTAL_TYPE:
          return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, expirationTime));
      }
    }

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, expirationTime));
    }

    if (isArray$1(newChild)) {
      return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (getIteratorFn(newChild)) {
      return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (isObject) {
      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }
    if (typeof newChild === 'undefined') {
      // If the new child is undefined, and the return fiber is a composite
      // component, throw an error. If Fiber return types are disabled,
      // we already threw above.
      switch (returnFiber.tag) {
        case ClassComponent:
          {
            {
              var instance = returnFiber.stateNode;
              if (instance.render._isMockFunction) {
                // We allow auto-mocks to proceed as if they're returning null.
                break;
              }
            }
          }
        // Intentionally fall through to the next case, which handles both
        // functions and classes
        // eslint-disable-next-lined no-fallthrough
        case FunctionalComponent:
          {
            var Component = returnFiber.type;
            invariant(false, '%s(...): Nothing was returned from render. This usually means a return statement is missing. Or, to render nothing, return null.', Component.displayName || Component.name || 'Component');
          }
      }
    }

    // Remaining cases are all treated as empty.
    return deleteRemainingChildren(returnFiber, currentFirstChild);
  }

  return reconcileChildFibers;
}

var reconcileChildFibers = ChildReconciler(true);
var mountChildFibers = ChildReconciler(false);

function cloneChildFibers(current, workInProgress) {
  !(current === null || workInProgress.child === current.child) ? invariant(false, 'Resuming work not yet implemented.') : void 0;

  if (workInProgress.child === null) {
    return;
  }

  var currentChild = workInProgress.child;
  var newChild = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
  workInProgress.child = newChild;

  newChild['return'] = workInProgress;
  while (currentChild.sibling !== null) {
    currentChild = currentChild.sibling;
    newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
    newChild['return'] = workInProgress;
  }
  newChild.sibling = null;
}

var didWarnAboutBadClass = void 0;
var didWarnAboutGetDerivedStateOnFunctionalComponent = void 0;
var didWarnAboutStatelessRefs = void 0;

{
  didWarnAboutBadClass = {};
  didWarnAboutGetDerivedStateOnFunctionalComponent = {};
  didWarnAboutStatelessRefs = {};
}

var ReactFiberBeginWork = function (config, hostContext, legacyContext, newContext, hydrationContext, scheduleWork, computeExpirationForFiber) {
  var shouldSetTextContent = config.shouldSetTextContent,
      shouldDeprioritizeSubtree = config.shouldDeprioritizeSubtree;
  var pushHostContext = hostContext.pushHostContext,
      pushHostContainer = hostContext.pushHostContainer;
  var pushProvider = newContext.pushProvider;
  var getMaskedContext = legacyContext.getMaskedContext,
      getUnmaskedContext = legacyContext.getUnmaskedContext,
      hasLegacyContextChanged = legacyContext.hasContextChanged,
      pushLegacyContextProvider = legacyContext.pushContextProvider,
      pushTopLevelContextObject = legacyContext.pushTopLevelContextObject,
      invalidateContextProvider = legacyContext.invalidateContextProvider;
  var enterHydrationState = hydrationContext.enterHydrationState,
      resetHydrationState = hydrationContext.resetHydrationState,
      tryToClaimNextHydratableInstance = hydrationContext.tryToClaimNextHydratableInstance;

  var _ReactFiberClassCompo = ReactFiberClassComponent(legacyContext, scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState),
      adoptClassInstance = _ReactFiberClassCompo.adoptClassInstance,
      callGetDerivedStateFromProps = _ReactFiberClassCompo.callGetDerivedStateFromProps,
      constructClassInstance = _ReactFiberClassCompo.constructClassInstance,
      mountClassInstance = _ReactFiberClassCompo.mountClassInstance,
      resumeMountClassInstance = _ReactFiberClassCompo.resumeMountClassInstance,
      updateClassInstance = _ReactFiberClassCompo.updateClassInstance;

  // TODO: Remove this and use reconcileChildrenAtExpirationTime directly.


  function reconcileChildren(current, workInProgress, nextChildren) {
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, workInProgress.expirationTime);
  }

  function reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime) {
    if (current === null) {
      // If this is a fresh new component that hasn't been rendered yet, we
      // won't update its child set by applying minimal side-effects. Instead,
      // we will add them all to the child before it gets rendered. That means
      // we can optimize this reconciliation pass by not tracking side-effects.
      workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
    } else {
      // If the current child is the same as the work in progress, it means that
      // we haven't yet started any work on these children. Therefore, we use
      // the clone algorithm to create a copy of all the current children.

      // If we had any progressed work already, that is invalid at this point so
      // let's throw it out.
      workInProgress.child = reconcileChildFibers(workInProgress, current.child, nextChildren, renderExpirationTime);
    }
  }

  function updateForwardRef(current, workInProgress) {
    var render = workInProgress.type.render;
    var nextChildren = render(workInProgress.pendingProps, workInProgress.ref);
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextChildren);
    return workInProgress.child;
  }

  function updateFragment(current, workInProgress) {
    var nextChildren = workInProgress.pendingProps;
    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextChildren);
    return workInProgress.child;
  }

  function updateMode(current, workInProgress) {
    var nextChildren = workInProgress.pendingProps.children;
    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (nextChildren === null || workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextChildren);
    return workInProgress.child;
  }

  function markRef(current, workInProgress) {
    var ref = workInProgress.ref;
    if (current === null && ref !== null || current !== null && current.ref !== ref) {
      // Schedule a Ref effect
      workInProgress.effectTag |= Ref;
    }
  }

  function updateFunctionalComponent(current, workInProgress) {
    var fn = workInProgress.type;
    var nextProps = workInProgress.pendingProps;

    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else {
      if (workInProgress.memoizedProps === nextProps) {
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      // TODO: consider bringing fn.shouldComponentUpdate() back.
      // It used to be here.
    }

    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var nextChildren = void 0;

    {
      ReactCurrentOwner.current = workInProgress;
      ReactDebugCurrentFiber.setCurrentPhase('render');
      nextChildren = fn(nextProps, context);
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateClassComponent(current, workInProgress, renderExpirationTime) {
    // Push context providers early to prevent context stack mismatches.
    // During mounting we don't know the child context yet as the instance doesn't exist.
    // We will invalidate the child context in finishClassComponent() right after rendering.
    var hasContext = pushLegacyContextProvider(workInProgress);
    var shouldUpdate = void 0;
    if (current === null) {
      if (workInProgress.stateNode === null) {
        // In the initial pass we might need to construct the instance.
        constructClassInstance(workInProgress, workInProgress.pendingProps);
        mountClassInstance(workInProgress, renderExpirationTime);

        shouldUpdate = true;
      } else {
        // In a resume, we'll already have an instance we can reuse.
        shouldUpdate = resumeMountClassInstance(workInProgress, renderExpirationTime);
      }
    } else {
      shouldUpdate = updateClassInstance(current, workInProgress, renderExpirationTime);
    }

    // We processed the update queue inside updateClassInstance. It may have
    // included some errors that were dispatched during the commit phase.
    // TODO: Refactor class components so this is less awkward.
    var didCaptureError = false;
    var updateQueue = workInProgress.updateQueue;
    if (updateQueue !== null && updateQueue.capturedValues !== null) {
      shouldUpdate = true;
      didCaptureError = true;
    }
    return finishClassComponent(current, workInProgress, shouldUpdate, hasContext, didCaptureError, renderExpirationTime);
  }

  function finishClassComponent(current, workInProgress, shouldUpdate, hasContext, didCaptureError, renderExpirationTime) {
    // Refs should update even if shouldComponentUpdate returns false
    markRef(current, workInProgress);

    if (!shouldUpdate && !didCaptureError) {
      // Context providers should defer to sCU for rendering
      if (hasContext) {
        invalidateContextProvider(workInProgress, false);
      }

      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var ctor = workInProgress.type;
    var instance = workInProgress.stateNode;

    // Rerender
    ReactCurrentOwner.current = workInProgress;
    var nextChildren = void 0;
    if (didCaptureError && (!enableGetDerivedStateFromCatch || typeof ctor.getDerivedStateFromCatch !== 'function')) {
      // If we captured an error, but getDerivedStateFrom catch is not defined,
      // unmount all the children. componentDidCatch will schedule an update to
      // re-render a fallback. This is temporary until we migrate everyone to
      // the new API.
      // TODO: Warn in a future release.
      nextChildren = null;
    } else {
      {
        ReactDebugCurrentFiber.setCurrentPhase('render');
        nextChildren = instance.render();
        if (debugRenderPhaseSideEffects || debugRenderPhaseSideEffectsForStrictMode && workInProgress.mode & StrictMode) {
          instance.render();
        }
        ReactDebugCurrentFiber.setCurrentPhase(null);
      }
    }

    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    if (didCaptureError) {
      // If we're recovering from an error, reconcile twice: first to delete
      // all the existing children.
      reconcileChildrenAtExpirationTime(current, workInProgress, null, renderExpirationTime);
      workInProgress.child = null;
      // Now we can continue reconciling like normal. This has the effect of
      // remounting all children regardless of whether their their
      // identity matches.
    }
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime);
    // Memoize props and state using the values we just used to render.
    // TODO: Restructure so we never read values from the instance.
    memoizeState(workInProgress, instance.state);
    memoizeProps(workInProgress, instance.props);

    // The context might have changed so we need to recalculate it.
    if (hasContext) {
      invalidateContextProvider(workInProgress, true);
    }

    return workInProgress.child;
  }

  function pushHostRootContext(workInProgress) {
    var root = workInProgress.stateNode;
    if (root.pendingContext) {
      pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
    } else if (root.context) {
      // Should always be set
      pushTopLevelContextObject(workInProgress, root.context, false);
    }
    pushHostContainer(workInProgress, root.containerInfo);
  }

  function updateHostRoot(current, workInProgress, renderExpirationTime) {
    pushHostRootContext(workInProgress);
    var updateQueue = workInProgress.updateQueue;
    if (updateQueue !== null) {
      var prevState = workInProgress.memoizedState;
      var state = processUpdateQueue(current, workInProgress, updateQueue, null, null, renderExpirationTime);
      memoizeState(workInProgress, state);
      updateQueue = workInProgress.updateQueue;

      var element = void 0;
      if (updateQueue !== null && updateQueue.capturedValues !== null) {
        // There's an uncaught error. Unmount the whole root.
        element = null;
      } else if (prevState === state) {
        // If the state is the same as before, that's a bailout because we had
        // no work that expires at this time.
        resetHydrationState();
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      } else {
        element = state.element;
      }
      var root = workInProgress.stateNode;
      if ((current === null || current.child === null) && root.hydrate && enterHydrationState(workInProgress)) {
        // If we don't have any current children this might be the first pass.
        // We always try to hydrate. If this isn't a hydration pass there won't
        // be any children to hydrate which is effectively the same thing as
        // not hydrating.

        // This is a bit of a hack. We track the host root as a placement to
        // know that we're currently in a mounting state. That way isMounted
        // works as expected. We must reset this before committing.
        // TODO: Delete this when we delete isMounted and findDOMNode.
        workInProgress.effectTag |= Placement;

        // Ensure that children mount into this root without tracking
        // side-effects. This ensures that we don't store Placement effects on
        // nodes that will be hydrated.
        workInProgress.child = mountChildFibers(workInProgress, null, element, renderExpirationTime);
      } else {
        // Otherwise reset hydration state in case we aborted and resumed another
        // root.
        resetHydrationState();
        reconcileChildren(current, workInProgress, element);
      }
      memoizeState(workInProgress, state);
      return workInProgress.child;
    }
    resetHydrationState();
    // If there is no update queue, that's a bailout because the root has no props.
    return bailoutOnAlreadyFinishedWork(current, workInProgress);
  }

  function updateHostComponent(current, workInProgress, renderExpirationTime) {
    pushHostContext(workInProgress);

    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }

    var type = workInProgress.type;
    var memoizedProps = workInProgress.memoizedProps;
    var nextProps = workInProgress.pendingProps;
    var prevProps = current !== null ? current.memoizedProps : null;

    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (memoizedProps === nextProps) {
      var isHidden = workInProgress.mode & AsyncMode && shouldDeprioritizeSubtree(type, nextProps);
      if (isHidden) {
        // Before bailing out, make sure we've deprioritized a hidden component.
        workInProgress.expirationTime = Never;
      }
      if (!isHidden || renderExpirationTime !== Never) {
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      // If we're rendering a hidden node at hidden priority, don't bailout. The
      // parent is complete, but the children may not be.
    }

    var nextChildren = nextProps.children;
    var isDirectTextChild = shouldSetTextContent(type, nextProps);

    if (isDirectTextChild) {
      // We special case a direct text child of a host node. This is a common
      // case. We won't handle it as a reified child. We will instead handle
      // this in the host environment that also have access to this prop. That
      // avoids allocating another HostText fiber and traversing it.
      nextChildren = null;
    } else if (prevProps && shouldSetTextContent(type, prevProps)) {
      // If we're switching from a direct text child to a normal child, or to
      // empty, we need to schedule the text content to be reset.
      workInProgress.effectTag |= ContentReset;
    }

    markRef(current, workInProgress);

    // Check the host config to see if the children are offscreen/hidden.
    if (renderExpirationTime !== Never && workInProgress.mode & AsyncMode && shouldDeprioritizeSubtree(type, nextProps)) {
      // Down-prioritize the children.
      workInProgress.expirationTime = Never;
      // Bailout and come back to this fiber later.
      workInProgress.memoizedProps = nextProps;
      return null;
    }

    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateHostText(current, workInProgress) {
    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }
    var nextProps = workInProgress.pendingProps;
    memoizeProps(workInProgress, nextProps);
    // Nothing to do here. This is terminal. We'll do the completion step
    // immediately after.
    return null;
  }

  function mountIndeterminateComponent(current, workInProgress, renderExpirationTime) {
    !(current === null) ? invariant(false, 'An indeterminate component should never have mounted. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    var fn = workInProgress.type;
    var props = workInProgress.pendingProps;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var value = void 0;

    {
      if (fn.prototype && typeof fn.prototype.render === 'function') {
        var componentName = getComponentName(workInProgress) || 'Unknown';

        if (!didWarnAboutBadClass[componentName]) {
          warning(false, "The <%s /> component appears to have a render method, but doesn't extend React.Component. " + 'This is likely to cause errors. Change %s to extend React.Component instead.', componentName, componentName);
          didWarnAboutBadClass[componentName] = true;
        }
      }
      ReactCurrentOwner.current = workInProgress;
      value = fn(props, context);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;

    if (typeof value === 'object' && value !== null && typeof value.render === 'function' && value.$$typeof === undefined) {
      var Component = workInProgress.type;

      // Proceed under the assumption that this is a class instance
      workInProgress.tag = ClassComponent;

      workInProgress.memoizedState = value.state !== null && value.state !== undefined ? value.state : null;

      if (typeof Component.getDerivedStateFromProps === 'function') {
        var partialState = callGetDerivedStateFromProps(workInProgress, value, props, workInProgress.memoizedState);

        if (partialState !== null && partialState !== undefined) {
          workInProgress.memoizedState = _assign({}, workInProgress.memoizedState, partialState);
        }
      }

      // Push context providers early to prevent context stack mismatches.
      // During mounting we don't know the child context yet as the instance doesn't exist.
      // We will invalidate the child context in finishClassComponent() right after rendering.
      var hasContext = pushLegacyContextProvider(workInProgress);
      adoptClassInstance(workInProgress, value);
      mountClassInstance(workInProgress, renderExpirationTime);
      return finishClassComponent(current, workInProgress, true, hasContext, false, renderExpirationTime);
    } else {
      // Proceed under the assumption that this is a functional component
      workInProgress.tag = FunctionalComponent;
      {
        var _Component = workInProgress.type;

        if (_Component) {
          !!_Component.childContextTypes ? warning(false, '%s(...): childContextTypes cannot be defined on a functional component.', _Component.displayName || _Component.name || 'Component') : void 0;
        }
        if (workInProgress.ref !== null) {
          var info = '';
          var ownerName = ReactDebugCurrentFiber.getCurrentFiberOwnerName();
          if (ownerName) {
            info += '\n\nCheck the render method of `' + ownerName + '`.';
          }

          var warningKey = ownerName || workInProgress._debugID || '';
          var debugSource = workInProgress._debugSource;
          if (debugSource) {
            warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
          }
          if (!didWarnAboutStatelessRefs[warningKey]) {
            didWarnAboutStatelessRefs[warningKey] = true;
            warning(false, 'Stateless function components cannot be given refs. ' + 'Attempts to access this ref will fail.%s%s', info, ReactDebugCurrentFiber.getCurrentFiberStackAddendum());
          }
        }

        if (typeof fn.getDerivedStateFromProps === 'function') {
          var _componentName = getComponentName(workInProgress) || 'Unknown';

          if (!didWarnAboutGetDerivedStateOnFunctionalComponent[_componentName]) {
            warning(false, '%s: Stateless functional components do not support getDerivedStateFromProps.', _componentName);
            didWarnAboutGetDerivedStateOnFunctionalComponent[_componentName] = true;
          }
        }
      }
      reconcileChildren(current, workInProgress, value);
      memoizeProps(workInProgress, props);
      return workInProgress.child;
    }
  }

  function updateCallComponent(current, workInProgress, renderExpirationTime) {
    var nextProps = workInProgress.pendingProps;
    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (workInProgress.memoizedProps === nextProps) {
      nextProps = workInProgress.memoizedProps;
      // TODO: When bailing out, we might need to return the stateNode instead
      // of the child. To check it for work.
      // return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var nextChildren = nextProps.children;

    // The following is a fork of reconcileChildrenAtExpirationTime but using
    // stateNode to store the child.
    if (current === null) {
      workInProgress.stateNode = mountChildFibers(workInProgress, workInProgress.stateNode, nextChildren, renderExpirationTime);
    } else {
      workInProgress.stateNode = reconcileChildFibers(workInProgress, current.stateNode, nextChildren, renderExpirationTime);
    }

    memoizeProps(workInProgress, nextProps);
    // This doesn't take arbitrary time so we could synchronously just begin
    // eagerly do the work of workInProgress.child as an optimization.
    return workInProgress.stateNode;
  }

  function updatePortalComponent(current, workInProgress, renderExpirationTime) {
    pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
    var nextChildren = workInProgress.pendingProps;
    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    if (current === null) {
      // Portals are special because we don't append the children during mount
      // but at commit. Therefore we need to track insertions which the normal
      // flow doesn't do during mount. This doesn't happen at the root because
      // the root always starts with a "current" with a null child.
      // TODO: Consider unifying this with how the root works.
      workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
      memoizeProps(workInProgress, nextChildren);
    } else {
      reconcileChildren(current, workInProgress, nextChildren);
      memoizeProps(workInProgress, nextChildren);
    }
    return workInProgress.child;
  }

  function propagateContextChange(workInProgress, context, changedBits, renderExpirationTime) {
    var fiber = workInProgress.child;
    if (fiber !== null) {
      // Set the return pointer of the child to the work-in-progress fiber.
      fiber['return'] = workInProgress;
    }
    while (fiber !== null) {
      var nextFiber = void 0;
      // Visit this fiber.
      switch (fiber.tag) {
        case ContextConsumer:
          // Check if the context matches.
          var observedBits = fiber.stateNode | 0;
          if (fiber.type === context && (observedBits & changedBits) !== 0) {
            // Update the expiration time of all the ancestors, including
            // the alternates.
            var node = fiber;
            while (node !== null) {
              var alternate = node.alternate;
              if (node.expirationTime === NoWork || node.expirationTime > renderExpirationTime) {
                node.expirationTime = renderExpirationTime;
                if (alternate !== null && (alternate.expirationTime === NoWork || alternate.expirationTime > renderExpirationTime)) {
                  alternate.expirationTime = renderExpirationTime;
                }
              } else if (alternate !== null && (alternate.expirationTime === NoWork || alternate.expirationTime > renderExpirationTime)) {
                alternate.expirationTime = renderExpirationTime;
              } else {
                // Neither alternate was updated, which means the rest of the
                // ancestor path already has sufficient priority.
                break;
              }
              node = node['return'];
            }
            // Don't scan deeper than a matching consumer. When we render the
            // consumer, we'll continue scanning from that point. This way the
            // scanning work is time-sliced.
            nextFiber = null;
          } else {
            // Traverse down.
            nextFiber = fiber.child;
          }
          break;
        case ContextProvider:
          // Don't scan deeper if this is a matching provider
          nextFiber = fiber.type === workInProgress.type ? null : fiber.child;
          break;
        default:
          // Traverse down.
          nextFiber = fiber.child;
          break;
      }
      if (nextFiber !== null) {
        // Set the return pointer of the child to the work-in-progress fiber.
        nextFiber['return'] = fiber;
      } else {
        // No child. Traverse to next sibling.
        nextFiber = fiber;
        while (nextFiber !== null) {
          if (nextFiber === workInProgress) {
            // We're back to the root of this subtree. Exit.
            nextFiber = null;
            break;
          }
          var sibling = nextFiber.sibling;
          if (sibling !== null) {
            nextFiber = sibling;
            break;
          }
          // No more siblings. Traverse up.
          nextFiber = nextFiber['return'];
        }
      }
      fiber = nextFiber;
    }
  }

  function updateContextProvider(current, workInProgress, renderExpirationTime) {
    var providerType = workInProgress.type;
    var context = providerType._context;

    var newProps = workInProgress.pendingProps;
    var oldProps = workInProgress.memoizedProps;

    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (oldProps === newProps) {
      workInProgress.stateNode = 0;
      pushProvider(workInProgress);
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var newValue = newProps.value;
    workInProgress.memoizedProps = newProps;

    var changedBits = void 0;
    if (oldProps === null) {
      // Initial render
      changedBits = MAX_SIGNED_31_BIT_INT;
    } else {
      if (oldProps.value === newProps.value) {
        // No change. Bailout early if children are the same.
        if (oldProps.children === newProps.children) {
          workInProgress.stateNode = 0;
          pushProvider(workInProgress);
          return bailoutOnAlreadyFinishedWork(current, workInProgress);
        }
        changedBits = 0;
      } else {
        var oldValue = oldProps.value;
        // Use Object.is to compare the new context value to the old value.
        // Inlined Object.is polyfill.
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
        if (oldValue === newValue && (oldValue !== 0 || 1 / oldValue === 1 / newValue) || oldValue !== oldValue && newValue !== newValue // eslint-disable-line no-self-compare
        ) {
            // No change. Bailout early if children are the same.
            if (oldProps.children === newProps.children) {
              workInProgress.stateNode = 0;
              pushProvider(workInProgress);
              return bailoutOnAlreadyFinishedWork(current, workInProgress);
            }
            changedBits = 0;
          } else {
          changedBits = typeof context._calculateChangedBits === 'function' ? context._calculateChangedBits(oldValue, newValue) : MAX_SIGNED_31_BIT_INT;
          {
            !((changedBits & MAX_SIGNED_31_BIT_INT) === changedBits) ? warning(false, 'calculateChangedBits: Expected the return value to be a ' + '31-bit integer. Instead received: %s', changedBits) : void 0;
          }
          changedBits |= 0;

          if (changedBits === 0) {
            // No change. Bailout early if children are the same.
            if (oldProps.children === newProps.children) {
              workInProgress.stateNode = 0;
              pushProvider(workInProgress);
              return bailoutOnAlreadyFinishedWork(current, workInProgress);
            }
          } else {
            propagateContextChange(workInProgress, context, changedBits, renderExpirationTime);
          }
        }
      }
    }

    workInProgress.stateNode = changedBits;
    pushProvider(workInProgress);

    var newChildren = newProps.children;
    reconcileChildren(current, workInProgress, newChildren);
    return workInProgress.child;
  }

  function updateContextConsumer(current, workInProgress, renderExpirationTime) {
    var context = workInProgress.type;
    var newProps = workInProgress.pendingProps;
    var oldProps = workInProgress.memoizedProps;

    var newValue = context._currentValue;
    var changedBits = context._changedBits;

    if (hasLegacyContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (changedBits === 0 && oldProps === newProps) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    workInProgress.memoizedProps = newProps;

    var observedBits = newProps.unstable_observedBits;
    if (observedBits === undefined || observedBits === null) {
      // Subscribe to all changes by default
      observedBits = MAX_SIGNED_31_BIT_INT;
    }
    // Store the observedBits on the fiber's stateNode for quick access.
    workInProgress.stateNode = observedBits;

    if ((changedBits & observedBits) !== 0) {
      // Context change propagation stops at matching consumers, for time-
      // slicing. Continue the propagation here.
      propagateContextChange(workInProgress, context, changedBits, renderExpirationTime);
    } else if (oldProps === newProps) {
      // Skip over a memoized parent with a bitmask bailout even
      // if we began working on it because of a deeper matching child.
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    // There is no bailout on `children` equality because we expect people
    // to often pass a bound method as a child, but it may reference
    // `this.state` or `this.props` (and thus needs to re-render on `setState`).

    var render = newProps.children;

    {
      !(typeof render === 'function') ? warning(false, 'A context consumer was rendered with multiple children, or a child ' + "that isn't a function. A context consumer expects a single child " + 'that is a function. If you did pass a function, make sure there ' + 'is no trailing or leading whitespace around it.') : void 0;
    }

    var newChildren = render(newValue);
    reconcileChildren(current, workInProgress, newChildren);
    return workInProgress.child;
  }

  /*
  function reuseChildrenEffects(returnFiber : Fiber, firstChild : Fiber) {
    let child = firstChild;
    do {
      // Ensure that the first and last effect of the parent corresponds
      // to the children's first and last effect.
      if (!returnFiber.firstEffect) {
        returnFiber.firstEffect = child.firstEffect;
      }
      if (child.lastEffect) {
        if (returnFiber.lastEffect) {
          returnFiber.lastEffect.nextEffect = child.firstEffect;
        }
        returnFiber.lastEffect = child.lastEffect;
      }
    } while (child = child.sibling);
  }
  */

  function bailoutOnAlreadyFinishedWork(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: We should ideally be able to bail out early if the children have no
    // more work to do. However, since we don't have a separation of this
    // Fiber's priority and its children yet - we don't know without doing lots
    // of the same work we do anyway. Once we have that separation we can just
    // bail out here if the children has no more work at this priority level.
    // if (workInProgress.priorityOfChildren <= priorityLevel) {
    //   // If there are side-effects in these children that have not yet been
    //   // committed we need to ensure that they get properly transferred up.
    //   if (current && current.child !== workInProgress.child) {
    //     reuseChildrenEffects(workInProgress, child);
    //   }
    //   return null;
    // }

    cloneChildFibers(current, workInProgress);
    return workInProgress.child;
  }

  function bailoutOnLowPriority(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: Handle HostComponent tags here as well and call pushHostContext()?
    // See PR 8590 discussion for context
    switch (workInProgress.tag) {
      case HostRoot:
        pushHostRootContext(workInProgress);
        break;
      case ClassComponent:
        pushLegacyContextProvider(workInProgress);
        break;
      case HostPortal:
        pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
        break;
      case ContextProvider:
        pushProvider(workInProgress);
        break;
    }
    // TODO: What if this is currently in progress?
    // How can that happen? How is this not being cloned?
    return null;
  }

  // TODO: Delete memoizeProps/State and move to reconcile/bailout instead
  function memoizeProps(workInProgress, nextProps) {
    workInProgress.memoizedProps = nextProps;
  }

  function memoizeState(workInProgress, nextState) {
    workInProgress.memoizedState = nextState;
    // Don't reset the updateQueue, in case there are pending updates. Resetting
    // is handled by processUpdateQueue.
  }

  function beginWork(current, workInProgress, renderExpirationTime) {
    if (workInProgress.expirationTime === NoWork || workInProgress.expirationTime > renderExpirationTime) {
      return bailoutOnLowPriority(current, workInProgress);
    }

    switch (workInProgress.tag) {
      case IndeterminateComponent:
        return mountIndeterminateComponent(current, workInProgress, renderExpirationTime);
      case FunctionalComponent:
        return updateFunctionalComponent(current, workInProgress);
      case ClassComponent:
        return updateClassComponent(current, workInProgress, renderExpirationTime);
      case HostRoot:
        return updateHostRoot(current, workInProgress, renderExpirationTime);
      case HostComponent:
        return updateHostComponent(current, workInProgress, renderExpirationTime);
      case HostText:
        return updateHostText(current, workInProgress);
      case CallHandlerPhase:
        // This is a restart. Reset the tag to the initial phase.
        workInProgress.tag = CallComponent;
      // Intentionally fall through since this is now the same.
      case CallComponent:
        return updateCallComponent(current, workInProgress, renderExpirationTime);
      case ReturnComponent:
        // A return component is just a placeholder, we can just run through the
        // next one immediately.
        return null;
      case HostPortal:
        return updatePortalComponent(current, workInProgress, renderExpirationTime);
      case ForwardRef:
        return updateForwardRef(current, workInProgress);
      case Fragment:
        return updateFragment(current, workInProgress);
      case Mode:
        return updateMode(current, workInProgress);
      case ContextProvider:
        return updateContextProvider(current, workInProgress, renderExpirationTime);
      case ContextConsumer:
        return updateContextConsumer(current, workInProgress, renderExpirationTime);
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  return {
    beginWork: beginWork
  };
};

var ReactFiberCompleteWork = function (config, hostContext, legacyContext, newContext, hydrationContext) {
  var createInstance = config.createInstance,
      createTextInstance = config.createTextInstance,
      appendInitialChild = config.appendInitialChild,
      finalizeInitialChildren = config.finalizeInitialChildren,
      prepareUpdate = config.prepareUpdate,
      mutation = config.mutation,
      persistence = config.persistence;
  var getRootHostContainer = hostContext.getRootHostContainer,
      popHostContext = hostContext.popHostContext,
      getHostContext = hostContext.getHostContext,
      popHostContainer = hostContext.popHostContainer;
  var popLegacyContextProvider = legacyContext.popContextProvider,
      popTopLevelLegacyContextObject = legacyContext.popTopLevelContextObject;
  var popProvider = newContext.popProvider;
  var prepareToHydrateHostInstance = hydrationContext.prepareToHydrateHostInstance,
      prepareToHydrateHostTextInstance = hydrationContext.prepareToHydrateHostTextInstance,
      popHydrationState = hydrationContext.popHydrationState;


  function markUpdate(workInProgress) {
    // Tag the fiber with an update effect. This turns a Placement into
    // a PlacementAndUpdate.
    workInProgress.effectTag |= Update;
  }

  function markRef(workInProgress) {
    workInProgress.effectTag |= Ref;
  }

  function appendAllReturns(returns, workInProgress) {
    var node = workInProgress.stateNode;
    if (node) {
      node['return'] = workInProgress;
    }
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText || node.tag === HostPortal) {
        invariant(false, 'A call cannot have host component children.');
      } else if (node.tag === ReturnComponent) {
        returns.push(node.pendingProps.value);
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function moveCallToHandlerPhase(current, workInProgress, renderExpirationTime) {
    var props = workInProgress.memoizedProps;
    !props ? invariant(false, 'Should be resolved by now. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    // First step of the call has completed. Now we need to do the second.
    // TODO: It would be nice to have a multi stage call represented by a
    // single component, or at least tail call optimize nested ones. Currently
    // that requires additional fields that we don't want to add to the fiber.
    // So this requires nested handlers.
    // Note: This doesn't mutate the alternate node. I don't think it needs to
    // since this stage is reset for every pass.
    workInProgress.tag = CallHandlerPhase;

    // Build up the returns.
    // TODO: Compare this to a generator or opaque helpers like Children.
    var returns = [];
    appendAllReturns(returns, workInProgress);
    var fn = props.handler;
    var childProps = props.props;
    var nextChildren = fn(childProps, returns);

    var currentFirstChild = current !== null ? current.child : null;
    workInProgress.child = reconcileChildFibers(workInProgress, currentFirstChild, nextChildren, renderExpirationTime);
    return workInProgress.child;
  }

  function appendAllChildren(parent, workInProgress) {
    // We only have the top Fiber that was created but we need recurse down its
    // children to find all the terminal nodes.
    var node = workInProgress.child;
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText) {
        appendInitialChild(parent, node.stateNode);
      } else if (node.tag === HostPortal) {
        // If we have a portal child, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === workInProgress) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  var updateHostContainer = void 0;
  var updateHostComponent = void 0;
  var updateHostText = void 0;
  if (mutation) {
    if (enableMutatingReconciler) {
      // Mutation mode
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance, currentHostContext) {
        // TODO: Type this specific to this type of component.
        workInProgress.updateQueue = updatePayload;
        // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update. All the work is done in commitWork.
        if (updatePayload) {
          markUpdate(workInProgress);
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // If the text differs, mark it as an update. All the work in done in commitWork.
        if (oldText !== newText) {
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Mutating reconciler is disabled.');
    }
  } else if (persistence) {
    if (enablePersistentReconciler) {
      // Persistent host tree mode
      var cloneInstance = persistence.cloneInstance,
          createContainerChildSet = persistence.createContainerChildSet,
          appendChildToContainerChildSet = persistence.appendChildToContainerChildSet,
          finalizeContainerChildren = persistence.finalizeContainerChildren;

      // An unfortunate fork of appendAllChildren because we have two different parent types.

      var appendAllChildrenToContainer = function (containerChildSet, workInProgress) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;
        while (node !== null) {
          if (node.tag === HostComponent || node.tag === HostText) {
            appendChildToContainerChildSet(containerChildSet, node.stateNode);
          } else if (node.tag === HostPortal) {
            // If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.child !== null) {
            node.child['return'] = node;
            node = node.child;
            continue;
          }
          if (node === workInProgress) {
            return;
          }
          while (node.sibling === null) {
            if (node['return'] === null || node['return'] === workInProgress) {
              return;
            }
            node = node['return'];
          }
          node.sibling['return'] = node['return'];
          node = node.sibling;
        }
      };
      updateHostContainer = function (workInProgress) {
        var portalOrRoot = workInProgress.stateNode;
        var childrenUnchanged = workInProgress.firstEffect === null;
        if (childrenUnchanged) {
          // No changes, just reuse the existing instance.
        } else {
          var container = portalOrRoot.containerInfo;
          var newChildSet = createContainerChildSet(container);
          // If children might have changed, we have to add them all to the set.
          appendAllChildrenToContainer(newChildSet, workInProgress);
          portalOrRoot.pendingChildren = newChildSet;
          // Schedule an update on the container to swap out the container.
          markUpdate(workInProgress);
          finalizeContainerChildren(container, newChildSet);
        }
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance, currentHostContext) {
        // If there are no effects associated with this node, then none of our children had any updates.
        // This guarantees that we can reuse all of them.
        var childrenUnchanged = workInProgress.firstEffect === null;
        var currentInstance = current.stateNode;
        if (childrenUnchanged && updatePayload === null) {
          // No changes, just reuse the existing instance.
          // Note that this might release a previous clone.
          workInProgress.stateNode = currentInstance;
        } else {
          var recyclableInstance = workInProgress.stateNode;
          var newInstance = cloneInstance(currentInstance, updatePayload, type, oldProps, newProps, workInProgress, childrenUnchanged, recyclableInstance);
          if (finalizeInitialChildren(newInstance, type, newProps, rootContainerInstance, currentHostContext)) {
            markUpdate(workInProgress);
          }
          workInProgress.stateNode = newInstance;
          if (childrenUnchanged) {
            // If there are no other effects in this tree, we need to flag this node as having one.
            // Even though we're not going to use it for anything.
            // Otherwise parents won't know that there are new children to propagate upwards.
            markUpdate(workInProgress);
          } else {
            // If children might have changed, we have to add them all to the set.
            appendAllChildren(newInstance, workInProgress);
          }
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        if (oldText !== newText) {
          // If the text content differs, we'll create a new text instance for it.
          var rootContainerInstance = getRootHostContainer();
          var currentHostContext = getHostContext();
          workInProgress.stateNode = createTextInstance(newText, rootContainerInstance, currentHostContext, workInProgress);
          // We'll have to mark it as having an effect, even though we won't use the effect for anything.
          // This lets the parents know that at least one of their children has changed.
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Persistent reconciler is disabled.');
    }
  } else {
    if (enableNoopReconciler) {
      // No host operations
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance, currentHostContext) {
        // Noop
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // Noop
      };
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }

  function completeWork(current, workInProgress, renderExpirationTime) {
    var newProps = workInProgress.pendingProps;
    switch (workInProgress.tag) {
      case FunctionalComponent:
        return null;
      case ClassComponent:
        {
          // We are leaving this subtree, so pop context if any.
          popLegacyContextProvider(workInProgress);

          // If this component caught an error, schedule an error log effect.
          var instance = workInProgress.stateNode;
          var updateQueue = workInProgress.updateQueue;
          if (updateQueue !== null && updateQueue.capturedValues !== null) {
            workInProgress.effectTag &= ~DidCapture;
            if (typeof instance.componentDidCatch === 'function') {
              workInProgress.effectTag |= ErrLog;
            } else {
              // Normally we clear this in the commit phase, but since we did not
              // schedule an effect, we need to reset it here.
              updateQueue.capturedValues = null;
            }
          }
          return null;
        }
      case HostRoot:
        {
          popHostContainer(workInProgress);
          popTopLevelLegacyContextObject(workInProgress);
          var fiberRoot = workInProgress.stateNode;
          if (fiberRoot.pendingContext) {
            fiberRoot.context = fiberRoot.pendingContext;
            fiberRoot.pendingContext = null;
          }
          if (current === null || current.child === null) {
            // If we hydrated, pop so that we can delete any remaining children
            // that weren't hydrated.
            popHydrationState(workInProgress);
            // This resets the hacky state to fix isMounted before committing.
            // TODO: Delete this when we delete isMounted and findDOMNode.
            workInProgress.effectTag &= ~Placement;
          }
          updateHostContainer(workInProgress);

          var _updateQueue = workInProgress.updateQueue;
          if (_updateQueue !== null && _updateQueue.capturedValues !== null) {
            workInProgress.effectTag |= ErrLog;
          }
          return null;
        }
      case HostComponent:
        {
          popHostContext(workInProgress);
          var rootContainerInstance = getRootHostContainer();
          var type = workInProgress.type;
          if (current !== null && workInProgress.stateNode != null) {
            // If we have an alternate, that means this is an update and we need to
            // schedule a side-effect to do the updates.
            var oldProps = current.memoizedProps;
            // If we get updated because one of our children updated, we don't
            // have newProps so we'll have to reuse them.
            // TODO: Split the update API as separate for the props vs. children.
            // Even better would be if children weren't special cased at all tho.
            var _instance = workInProgress.stateNode;
            var currentHostContext = getHostContext();
            // TODO: Experiencing an error where oldProps is null. Suggests a host
            // component is hitting the resume path. Figure out why. Possibly
            // related to `hidden`.
            var updatePayload = prepareUpdate(_instance, type, oldProps, newProps, rootContainerInstance, currentHostContext);

            updateHostComponent(current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance, currentHostContext);

            if (current.ref !== workInProgress.ref) {
              markRef(workInProgress);
            }
          } else {
            if (!newProps) {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }

            var _currentHostContext = getHostContext();
            // TODO: Move createInstance to beginWork and keep it on a context
            // "stack" as the parent. Then append children as we go in beginWork
            // or completeWork depending on we want to add then top->down or
            // bottom->up. Top->down is faster in IE11.
            var wasHydrated = popHydrationState(workInProgress);
            if (wasHydrated) {
              // TODO: Move this and createInstance step into the beginPhase
              // to consolidate.
              if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, _currentHostContext)) {
                // If changes to the hydrated node needs to be applied at the
                // commit-phase we mark this as such.
                markUpdate(workInProgress);
              }
            } else {
              var _instance2 = createInstance(type, newProps, rootContainerInstance, _currentHostContext, workInProgress);

              appendAllChildren(_instance2, workInProgress);

              // Certain renderers require commit-time effects for initial mount.
              // (eg DOM renderer supports auto-focus for certain elements).
              // Make sure such renderers get scheduled for later work.
              if (finalizeInitialChildren(_instance2, type, newProps, rootContainerInstance, _currentHostContext)) {
                markUpdate(workInProgress);
              }
              workInProgress.stateNode = _instance2;
            }

            if (workInProgress.ref !== null) {
              // If there is a ref on a host node we need to schedule a callback
              markRef(workInProgress);
            }
          }
          return null;
        }
      case HostText:
        {
          var newText = newProps;
          if (current && workInProgress.stateNode != null) {
            var oldText = current.memoizedProps;
            // If we have an alternate, that means this is an update and we need
            // to schedule a side-effect to do the updates.
            updateHostText(current, workInProgress, oldText, newText);
          } else {
            if (typeof newText !== 'string') {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }
            var _rootContainerInstance = getRootHostContainer();
            var _currentHostContext2 = getHostContext();
            var _wasHydrated = popHydrationState(workInProgress);
            if (_wasHydrated) {
              if (prepareToHydrateHostTextInstance(workInProgress)) {
                markUpdate(workInProgress);
              }
            } else {
              workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext2, workInProgress);
            }
          }
          return null;
        }
      case CallComponent:
        return moveCallToHandlerPhase(current, workInProgress, renderExpirationTime);
      case CallHandlerPhase:
        // Reset the tag to now be a first phase call.
        workInProgress.tag = CallComponent;
        return null;
      case ReturnComponent:
        // Does nothing.
        return null;
      case ForwardRef:
        return null;
      case Fragment:
        return null;
      case Mode:
        return null;
      case HostPortal:
        popHostContainer(workInProgress);
        updateHostContainer(workInProgress);
        return null;
      case ContextProvider:
        // Pop provider fiber
        popProvider(workInProgress);
        return null;
      case ContextConsumer:
        return null;
      // Error cases
      case IndeterminateComponent:
        invariant(false, 'An indeterminate component should have become determinate before completing. This error is likely caused by a bug in React. Please file an issue.');
      // eslint-disable-next-line no-fallthrough
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  return {
    completeWork: completeWork
  };
};

function createCapturedValue(value, source) {
  // If the value is an error, call this function immediately after it is thrown
  // so the stack is accurate.
  return {
    value: value,
    source: source,
    stack: getStackAddendumByWorkInProgressFiber(source)
  };
}

var ReactFiberUnwindWork = function (hostContext, legacyContext, newContext, scheduleWork, isAlreadyFailedLegacyErrorBoundary) {
  var popHostContainer = hostContext.popHostContainer,
      popHostContext = hostContext.popHostContext;
  var popLegacyContextProvider = legacyContext.popContextProvider,
      popTopLevelLegacyContextObject = legacyContext.popTopLevelContextObject;
  var popProvider = newContext.popProvider;


  function throwException(returnFiber, sourceFiber, rawValue) {
    // The source fiber did not complete.
    sourceFiber.effectTag |= Incomplete;
    // Its effect list is no longer valid.
    sourceFiber.firstEffect = sourceFiber.lastEffect = null;

    var value = createCapturedValue(rawValue, sourceFiber);

    var workInProgress = returnFiber;
    do {
      switch (workInProgress.tag) {
        case HostRoot:
          {
            // Uncaught error
            var errorInfo = value;
            ensureUpdateQueues(workInProgress);
            var updateQueue = workInProgress.updateQueue;
            updateQueue.capturedValues = [errorInfo];
            workInProgress.effectTag |= ShouldCapture;
            return;
          }
        case ClassComponent:
          // Capture and retry
          var ctor = workInProgress.type;
          var _instance = workInProgress.stateNode;
          if ((workInProgress.effectTag & DidCapture) === NoEffect && (typeof ctor.getDerivedStateFromCatch === 'function' && enableGetDerivedStateFromCatch || _instance !== null && typeof _instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(_instance))) {
            ensureUpdateQueues(workInProgress);
            var _updateQueue = workInProgress.updateQueue;
            var capturedValues = _updateQueue.capturedValues;
            if (capturedValues === null) {
              _updateQueue.capturedValues = [value];
            } else {
              capturedValues.push(value);
            }
            workInProgress.effectTag |= ShouldCapture;
            return;
          }
          break;
        default:
          break;
      }
      workInProgress = workInProgress['return'];
    } while (workInProgress !== null);
  }

  function unwindWork(workInProgress) {
    switch (workInProgress.tag) {
      case ClassComponent:
        {
          popLegacyContextProvider(workInProgress);
          var effectTag = workInProgress.effectTag;
          if (effectTag & ShouldCapture) {
            workInProgress.effectTag = effectTag & ~ShouldCapture | DidCapture;
            return workInProgress;
          }
          return null;
        }
      case HostRoot:
        {
          popHostContainer(workInProgress);
          popTopLevelLegacyContextObject(workInProgress);
          var _effectTag = workInProgress.effectTag;
          if (_effectTag & ShouldCapture) {
            workInProgress.effectTag = _effectTag & ~ShouldCapture | DidCapture;
            return workInProgress;
          }
          return null;
        }
      case HostComponent:
        {
          popHostContext(workInProgress);
          return null;
        }
      case HostPortal:
        popHostContainer(workInProgress);
        return null;
      case ContextProvider:
        popProvider(workInProgress);
        return null;
      default:
        return null;
    }
  }

  function unwindInterruptedWork(interruptedWork) {
    switch (interruptedWork.tag) {
      case ClassComponent:
        {
          popLegacyContextProvider(interruptedWork);
          break;
        }
      case HostRoot:
        {
          popHostContainer(interruptedWork);
          popTopLevelLegacyContextObject(interruptedWork);
          break;
        }
      case HostComponent:
        {
          popHostContext(interruptedWork);
          break;
        }
      case HostPortal:
        popHostContainer(interruptedWork);
        break;
      case ContextProvider:
        popProvider(interruptedWork);
        break;
      default:
        break;
    }
  }

  return {
    throwException: throwException,
    unwindWork: unwindWork,
    unwindInterruptedWork: unwindInterruptedWork
  };
};

// This module is forked in different environments.
// By default, return `true` to log errors to the console.
// Forks can return `false` if this isn't desirable.
function showErrorDialog(capturedError) {
  return true;
}

function logCapturedError(capturedError) {
  var logError = showErrorDialog(capturedError);

  // Allow injected showErrorDialog() to prevent default console.error logging.
  // This enables renderers like ReactNative to better manage redbox behavior.
  if (logError === false) {
    return;
  }

  var error = capturedError.error;
  var suppressLogging = error && error.suppressReactErrorLogging;
  if (suppressLogging) {
    return;
  }

  {
    var componentName = capturedError.componentName,
        componentStack = capturedError.componentStack,
        errorBoundaryName = capturedError.errorBoundaryName,
        errorBoundaryFound = capturedError.errorBoundaryFound,
        willRetry = capturedError.willRetry;


    var componentNameMessage = componentName ? 'The above error occurred in the <' + componentName + '> component:' : 'The above error occurred in one of your React components:';

    var errorBoundaryMessage = void 0;
    // errorBoundaryFound check is sufficient; errorBoundaryName check is to satisfy Flow.
    if (errorBoundaryFound && errorBoundaryName) {
      if (willRetry) {
        errorBoundaryMessage = 'React will try to recreate this component tree from scratch ' + ('using the error boundary you provided, ' + errorBoundaryName + '.');
      } else {
        errorBoundaryMessage = 'This error was initially handled by the error boundary ' + errorBoundaryName + '.\n' + 'Recreating the tree from scratch failed so React will unmount the tree.';
      }
    } else {
      errorBoundaryMessage = 'Consider adding an error boundary to your tree to customize error handling behavior.\n' + 'Visit https://fb.me/react-error-boundaries to learn more about error boundaries.';
    }
    var combinedMessage = '' + componentNameMessage + componentStack + '\n\n' + ('' + errorBoundaryMessage);

    // In development, we provide our own message with just the component stack.
    // We don't include the original error message and JS stack because the browser
    // has already printed it. Even if the application swallows the error, it is still
    // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.
    console.error(combinedMessage);
  }
}

var invokeGuardedCallback$3 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError$1 = ReactErrorUtils.hasCaughtError;
var clearCaughtError$1 = ReactErrorUtils.clearCaughtError;


var didWarnAboutUndefinedSnapshotBeforeUpdate = null;
{
  didWarnAboutUndefinedSnapshotBeforeUpdate = new Set();
}

function logError(boundary, errorInfo) {
  var source = errorInfo.source;
  var stack = errorInfo.stack;
  if (stack === null) {
    stack = getStackAddendumByWorkInProgressFiber(source);
  }

  var capturedError = {
    componentName: source !== null ? getComponentName(source) : null,
    componentStack: stack !== null ? stack : '',
    error: errorInfo.value,
    errorBoundary: null,
    errorBoundaryName: null,
    errorBoundaryFound: false,
    willRetry: false
  };

  if (boundary !== null && boundary.tag === ClassComponent) {
    capturedError.errorBoundary = boundary.stateNode;
    capturedError.errorBoundaryName = getComponentName(boundary);
    capturedError.errorBoundaryFound = true;
    capturedError.willRetry = true;
  }

  try {
    logCapturedError(capturedError);
  } catch (e) {
    // Prevent cycle if logCapturedError() throws.
    // A cycle may still occur if logCapturedError renders a component that throws.
    var suppressLogging = e && e.suppressReactErrorLogging;
    if (!suppressLogging) {
      console.error(e);
    }
  }
}

var ReactFiberCommitWork = function (config, captureError, scheduleWork, computeExpirationForFiber, markLegacyErrorBoundaryAsFailed, recalculateCurrentTime) {
  var getPublicInstance = config.getPublicInstance,
      mutation = config.mutation,
      persistence = config.persistence;


  var callComponentWillUnmountWithTimer = function (current, instance) {
    startPhaseTimer(current, 'componentWillUnmount');
    instance.props = current.memoizedProps;
    instance.state = current.memoizedState;
    instance.componentWillUnmount();
    stopPhaseTimer();
  };

  // Capture errors so they don't interrupt unmounting.
  function safelyCallComponentWillUnmount(current, instance) {
    {
      invokeGuardedCallback$3(null, callComponentWillUnmountWithTimer, null, current, instance);
      if (hasCaughtError$1()) {
        var unmountError = clearCaughtError$1();
        captureError(current, unmountError);
      }
    }
  }

  function safelyDetachRef(current) {
    var ref = current.ref;
    if (ref !== null) {
      if (typeof ref === 'function') {
        {
          invokeGuardedCallback$3(null, ref, null, null);
          if (hasCaughtError$1()) {
            var refError = clearCaughtError$1();
            captureError(current, refError);
          }
        }
      } else {
        ref.current = null;
      }
    }
  }

  function commitBeforeMutationLifeCycles(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          if (finishedWork.effectTag & Snapshot) {
            if (current !== null) {
              var prevProps = current.memoizedProps;
              var prevState = current.memoizedState;
              startPhaseTimer(finishedWork, 'getSnapshotBeforeUpdate');
              var _instance = finishedWork.stateNode;
              _instance.props = finishedWork.memoizedProps;
              _instance.state = finishedWork.memoizedState;
              var snapshot = _instance.getSnapshotBeforeUpdate(prevProps, prevState);
              {
                var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;
                if (snapshot === undefined && !didWarnSet.has(finishedWork.type)) {
                  didWarnSet.add(finishedWork.type);
                  warning(false, '%s.getSnapshotBeforeUpdate(): A snapshot value (or null) ' + 'must be returned. You have returned undefined.', getComponentName(finishedWork));
                }
              }
              _instance.__reactInternalSnapshotBeforeUpdate = snapshot;
              stopPhaseTimer();
            }
          }
          return;
        }
      case HostRoot:
      case HostComponent:
      case HostText:
      case HostPortal:
        // Nothing to do for these component types
        return;
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitLifeCycles(finishedRoot, current, finishedWork, currentTime, committedExpirationTime) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          var _instance2 = finishedWork.stateNode;
          if (finishedWork.effectTag & Update) {
            if (current === null) {
              startPhaseTimer(finishedWork, 'componentDidMount');
              _instance2.props = finishedWork.memoizedProps;
              _instance2.state = finishedWork.memoizedState;
              _instance2.componentDidMount();
              stopPhaseTimer();
            } else {
              var prevProps = current.memoizedProps;
              var prevState = current.memoizedState;
              startPhaseTimer(finishedWork, 'componentDidUpdate');
              _instance2.props = finishedWork.memoizedProps;
              _instance2.state = finishedWork.memoizedState;
              _instance2.componentDidUpdate(prevProps, prevState, _instance2.__reactInternalSnapshotBeforeUpdate);
              stopPhaseTimer();
            }
          }
          var updateQueue = finishedWork.updateQueue;
          if (updateQueue !== null) {
            commitCallbacks(updateQueue, _instance2);
          }
          return;
        }
      case HostRoot:
        {
          var _updateQueue = finishedWork.updateQueue;
          if (_updateQueue !== null) {
            var _instance3 = null;
            if (finishedWork.child !== null) {
              switch (finishedWork.child.tag) {
                case HostComponent:
                  _instance3 = getPublicInstance(finishedWork.child.stateNode);
                  break;
                case ClassComponent:
                  _instance3 = finishedWork.child.stateNode;
                  break;
              }
            }
            commitCallbacks(_updateQueue, _instance3);
          }
          return;
        }
      case HostComponent:
        {
          var _instance4 = finishedWork.stateNode;

          // Renderers may schedule work to be done after host components are mounted
          // (eg DOM renderer may schedule auto-focus for inputs and form controls).
          // These effects should only be committed when components are first mounted,
          // aka when there is no current/alternate.
          if (current === null && finishedWork.effectTag & Update) {
            var type = finishedWork.type;
            var props = finishedWork.memoizedProps;
            commitMount(_instance4, type, props, finishedWork);
          }

          return;
        }
      case HostText:
        {
          // We have no life-cycles associated with text.
          return;
        }
      case HostPortal:
        {
          // We have no life-cycles associated with portals.
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitErrorLogging(finishedWork, onUncaughtError) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          var ctor = finishedWork.type;
          var _instance5 = finishedWork.stateNode;
          var updateQueue = finishedWork.updateQueue;
          !(updateQueue !== null && updateQueue.capturedValues !== null) ? invariant(false, 'An error logging effect should not have been scheduled if no errors were captured. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          var capturedErrors = updateQueue.capturedValues;
          updateQueue.capturedValues = null;

          if (typeof ctor.getDerivedStateFromCatch !== 'function') {
            // To preserve the preexisting retry behavior of error boundaries,
            // we keep track of which ones already failed during this batch.
            // This gets reset before we yield back to the browser.
            // TODO: Warn in strict mode if getDerivedStateFromCatch is
            // not defined.
            markLegacyErrorBoundaryAsFailed(_instance5);
          }

          _instance5.props = finishedWork.memoizedProps;
          _instance5.state = finishedWork.memoizedState;
          for (var i = 0; i < capturedErrors.length; i++) {
            var errorInfo = capturedErrors[i];
            var _error = errorInfo.value;
            var stack = errorInfo.stack;
            logError(finishedWork, errorInfo);
            _instance5.componentDidCatch(_error, {
              componentStack: stack !== null ? stack : ''
            });
          }
        }
        break;
      case HostRoot:
        {
          var _updateQueue2 = finishedWork.updateQueue;
          !(_updateQueue2 !== null && _updateQueue2.capturedValues !== null) ? invariant(false, 'An error logging effect should not have been scheduled if no errors were captured. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          var _capturedErrors = _updateQueue2.capturedValues;
          _updateQueue2.capturedValues = null;
          for (var _i = 0; _i < _capturedErrors.length; _i++) {
            var _errorInfo = _capturedErrors[_i];
            logError(finishedWork, _errorInfo);
            onUncaughtError(_errorInfo.value);
          }
          break;
        }
      default:
        invariant(false, 'This unit of work tag cannot capture errors.  This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  function commitAttachRef(finishedWork) {
    var ref = finishedWork.ref;
    if (ref !== null) {
      var _instance6 = finishedWork.stateNode;
      var instanceToUse = void 0;
      switch (finishedWork.tag) {
        case HostComponent:
          instanceToUse = getPublicInstance(_instance6);
          break;
        default:
          instanceToUse = _instance6;
      }
      if (typeof ref === 'function') {
        ref(instanceToUse);
      } else {
        {
          if (!ref.hasOwnProperty('current')) {
            warning(false, 'Unexpected ref object provided for %s. ' + 'Use either a ref-setter function or React.createRef().%s', getComponentName(finishedWork), getStackAddendumByWorkInProgressFiber(finishedWork));
          }
        }

        ref.current = instanceToUse;
      }
    }
  }

  function commitDetachRef(current) {
    var currentRef = current.ref;
    if (currentRef !== null) {
      if (typeof currentRef === 'function') {
        currentRef(null);
      } else {
        currentRef.current = null;
      }
    }
  }

  // User-originating errors (lifecycles and refs) should not interrupt
  // deletion, so don't let them throw. Host-originating errors should
  // interrupt deletion, so it's okay
  function commitUnmount(current) {
    if (typeof onCommitUnmount === 'function') {
      onCommitUnmount(current);
    }

    switch (current.tag) {
      case ClassComponent:
        {
          safelyDetachRef(current);
          var _instance7 = current.stateNode;
          if (typeof _instance7.componentWillUnmount === 'function') {
            safelyCallComponentWillUnmount(current, _instance7);
          }
          return;
        }
      case HostComponent:
        {
          safelyDetachRef(current);
          return;
        }
      case CallComponent:
        {
          commitNestedUnmounts(current.stateNode);
          return;
        }
      case HostPortal:
        {
          // TODO: this is recursive.
          // We are also not using this parent because
          // the portal will get pushed immediately.
          if (enableMutatingReconciler && mutation) {
            unmountHostComponents(current);
          } else if (enablePersistentReconciler && persistence) {
            emptyPortalContainer(current);
          }
          return;
        }
    }
  }

  function commitNestedUnmounts(root) {
    // While we're inside a removed host node we don't want to call
    // removeChild on the inner nodes because they're removed by the top
    // call anyway. We also want to call componentWillUnmount on all
    // composites before this host node is removed from the tree. Therefore
    var node = root;
    while (true) {
      commitUnmount(node);
      // Visit children because they may contain more composite or host nodes.
      // Skip portals because commitUnmount() currently visits them recursively.
      if (node.child !== null && (
      // If we use mutation we drill down into portals using commitUnmount above.
      // If we don't use mutation we drill down into portals here instead.
      !mutation || node.tag !== HostPortal)) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === root) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === root) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function detachFiber(current) {
    // Cut off the return pointers to disconnect it from the tree. Ideally, we
    // should clear the child pointer of the parent alternate to let this
    // get GC:ed but we don't know which for sure which parent is the current
    // one so we'll settle for GC:ing the subtree of this child. This child
    // itself will be GC:ed when the parent updates the next time.
    current['return'] = null;
    current.child = null;
    if (current.alternate) {
      current.alternate.child = null;
      current.alternate['return'] = null;
    }
  }

  var emptyPortalContainer = void 0;

  if (!mutation) {
    var commitContainer = void 0;
    if (persistence) {
      var replaceContainerChildren = persistence.replaceContainerChildren,
          createContainerChildSet = persistence.createContainerChildSet;

      emptyPortalContainer = function (current) {
        var portal = current.stateNode;
        var containerInfo = portal.containerInfo;

        var emptyChildSet = createContainerChildSet(containerInfo);
        replaceContainerChildren(containerInfo, emptyChildSet);
      };
      commitContainer = function (finishedWork) {
        switch (finishedWork.tag) {
          case ClassComponent:
            {
              return;
            }
          case HostComponent:
            {
              return;
            }
          case HostText:
            {
              return;
            }
          case HostRoot:
          case HostPortal:
            {
              var portalOrRoot = finishedWork.stateNode;
              var containerInfo = portalOrRoot.containerInfo,
                  _pendingChildren = portalOrRoot.pendingChildren;

              replaceContainerChildren(containerInfo, _pendingChildren);
              return;
            }
          default:
            {
              invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
            }
        }
      };
    } else {
      commitContainer = function (finishedWork) {
        // Noop
      };
    }
    if (enablePersistentReconciler || enableNoopReconciler) {
      return {
        commitResetTextContent: function (finishedWork) {},
        commitPlacement: function (finishedWork) {},
        commitDeletion: function (current) {
          // Detach refs and call componentWillUnmount() on the whole subtree.
          commitNestedUnmounts(current);
          detachFiber(current);
        },
        commitWork: function (current, finishedWork) {
          commitContainer(finishedWork);
        },

        commitLifeCycles: commitLifeCycles,
        commitBeforeMutationLifeCycles: commitBeforeMutationLifeCycles,
        commitErrorLogging: commitErrorLogging,
        commitAttachRef: commitAttachRef,
        commitDetachRef: commitDetachRef
      };
    } else if (persistence) {
      invariant(false, 'Persistent reconciler is disabled.');
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }
  var commitMount = mutation.commitMount,
      commitUpdate = mutation.commitUpdate,
      resetTextContent = mutation.resetTextContent,
      commitTextUpdate = mutation.commitTextUpdate,
      appendChild = mutation.appendChild,
      appendChildToContainer = mutation.appendChildToContainer,
      insertBefore = mutation.insertBefore,
      insertInContainerBefore = mutation.insertInContainerBefore,
      removeChild = mutation.removeChild,
      removeChildFromContainer = mutation.removeChildFromContainer;


  function getHostParentFiber(fiber) {
    var parent = fiber['return'];
    while (parent !== null) {
      if (isHostParent(parent)) {
        return parent;
      }
      parent = parent['return'];
    }
    invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.');
  }

  function isHostParent(fiber) {
    return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
  }

  function getHostSibling(fiber) {
    // We're going to search forward into the tree until we find a sibling host
    // node. Unfortunately, if multiple insertions are done in a row we have to
    // search past them. This leads to exponential search for the next sibling.
    var node = fiber;
    siblings: while (true) {
      // If we didn't find anything, let's try the next sibling.
      while (node.sibling === null) {
        if (node['return'] === null || isHostParent(node['return'])) {
          // If we pop out of the root or hit the parent the fiber we are the
          // last sibling.
          return null;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
      while (node.tag !== HostComponent && node.tag !== HostText) {
        // If it is not host node and, we might have a host node inside it.
        // Try to search down until we find one.
        if (node.effectTag & Placement) {
          // If we don't have a child, try the siblings instead.
          continue siblings;
        }
        // If we don't have a child, try the siblings instead.
        // We also skip portals because they are not part of this host tree.
        if (node.child === null || node.tag === HostPortal) {
          continue siblings;
        } else {
          node.child['return'] = node;
          node = node.child;
        }
      }
      // Check if this host node is stable or about to be placed.
      if (!(node.effectTag & Placement)) {
        // Found it!
        return node.stateNode;
      }
    }
  }

  function commitPlacement(finishedWork) {
    // Recursively insert all host nodes into the parent.
    var parentFiber = getHostParentFiber(finishedWork);
    var parent = void 0;
    var isContainer = void 0;
    switch (parentFiber.tag) {
      case HostComponent:
        parent = parentFiber.stateNode;
        isContainer = false;
        break;
      case HostRoot:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      case HostPortal:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      default:
        invariant(false, 'Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.');
    }
    if (parentFiber.effectTag & ContentReset) {
      // Reset the text content of the parent before doing any insertions
      resetTextContent(parent);
      // Clear ContentReset from the effect tag
      parentFiber.effectTag &= ~ContentReset;
    }

    var before = getHostSibling(finishedWork);
    // We only have the top Fiber that was inserted but we need recurse down its
    // children to find all the terminal nodes.
    var node = finishedWork;
    while (true) {
      if (node.tag === HostComponent || node.tag === HostText) {
        if (before) {
          if (isContainer) {
            insertInContainerBefore(parent, node.stateNode, before);
          } else {
            insertBefore(parent, node.stateNode, before);
          }
        } else {
          if (isContainer) {
            appendChildToContainer(parent, node.stateNode);
          } else {
            appendChild(parent, node.stateNode);
          }
        }
      } else if (node.tag === HostPortal) {
        // If the insertion itself is a portal, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === finishedWork) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === finishedWork) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function unmountHostComponents(current) {
    // We only have the top Fiber that was inserted but we need recurse down its
    var node = current;

    // Each iteration, currentParent is populated with node's host parent if not
    // currentParentIsValid.
    var currentParentIsValid = false;
    var currentParent = void 0;
    var currentParentIsContainer = void 0;

    while (true) {
      if (!currentParentIsValid) {
        var parent = node['return'];
        findParent: while (true) {
          !(parent !== null) ? invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          switch (parent.tag) {
            case HostComponent:
              currentParent = parent.stateNode;
              currentParentIsContainer = false;
              break findParent;
            case HostRoot:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
            case HostPortal:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
          }
          parent = parent['return'];
        }
        currentParentIsValid = true;
      }

      if (node.tag === HostComponent || node.tag === HostText) {
        commitNestedUnmounts(node);
        // After all the children have unmounted, it is now safe to remove the
        // node from the tree.
        if (currentParentIsContainer) {
          removeChildFromContainer(currentParent, node.stateNode);
        } else {
          removeChild(currentParent, node.stateNode);
        }
        // Don't visit children because we already visited them.
      } else if (node.tag === HostPortal) {
        // When we go into a portal, it becomes the parent to remove from.
        // We will reassign it back when we pop the portal on the way up.
        currentParent = node.stateNode.containerInfo;
        // Visit children because portals might contain host components.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      } else {
        commitUnmount(node);
        // Visit children because we may find more host components below.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      }
      if (node === current) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === current) {
          return;
        }
        node = node['return'];
        if (node.tag === HostPortal) {
          // When we go out of the portal, we need to restore the parent.
          // Since we don't keep a stack of them, we will search for it.
          currentParentIsValid = false;
        }
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function commitDeletion(current) {
    // Recursively delete all host nodes from the parent.
    // Detach refs and call componentWillUnmount() on the whole subtree.
    unmountHostComponents(current);
    detachFiber(current);
  }

  function commitWork(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          return;
        }
      case HostComponent:
        {
          var _instance8 = finishedWork.stateNode;
          if (_instance8 != null) {
            // Commit the work prepared earlier.
            var newProps = finishedWork.memoizedProps;
            // For hydration we reuse the update path but we treat the oldProps
            // as the newProps. The updatePayload will contain the real change in
            // this case.
            var oldProps = current !== null ? current.memoizedProps : newProps;
            var type = finishedWork.type;
            // TODO: Type the updateQueue to be specific to host components.
            var updatePayload = finishedWork.updateQueue;
            finishedWork.updateQueue = null;
            if (updatePayload !== null) {
              commitUpdate(_instance8, updatePayload, type, oldProps, newProps, finishedWork);
            }
          }
          return;
        }
      case HostText:
        {
          !(finishedWork.stateNode !== null) ? invariant(false, 'This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          var textInstance = finishedWork.stateNode;
          var newText = finishedWork.memoizedProps;
          // For hydration we reuse the update path but we treat the oldProps
          // as the newProps. The updatePayload will contain the real change in
          // this case.
          var oldText = current !== null ? current.memoizedProps : newText;
          commitTextUpdate(textInstance, oldText, newText);
          return;
        }
      case HostRoot:
        {
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitResetTextContent(current) {
    resetTextContent(current.stateNode);
  }

  if (enableMutatingReconciler) {
    return {
      commitBeforeMutationLifeCycles: commitBeforeMutationLifeCycles,
      commitResetTextContent: commitResetTextContent,
      commitPlacement: commitPlacement,
      commitDeletion: commitDeletion,
      commitWork: commitWork,
      commitLifeCycles: commitLifeCycles,
      commitErrorLogging: commitErrorLogging,
      commitAttachRef: commitAttachRef,
      commitDetachRef: commitDetachRef
    };
  } else {
    invariant(false, 'Mutating reconciler is disabled.');
  }
};

var NO_CONTEXT = {};

var ReactFiberHostContext = function (config, stack) {
  var getChildHostContext = config.getChildHostContext,
      getRootHostContext = config.getRootHostContext;
  var createCursor = stack.createCursor,
      push = stack.push,
      pop = stack.pop;


  var contextStackCursor = createCursor(NO_CONTEXT);
  var contextFiberStackCursor = createCursor(NO_CONTEXT);
  var rootInstanceStackCursor = createCursor(NO_CONTEXT);

  function requiredContext(c) {
    !(c !== NO_CONTEXT) ? invariant(false, 'Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    return c;
  }

  function getRootHostContainer() {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    return rootInstance;
  }

  function pushHostContainer(fiber, nextRootInstance) {
    // Push current root instance onto the stack;
    // This allows us to reset root when portals are popped.
    push(rootInstanceStackCursor, nextRootInstance, fiber);
    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);

    // Finally, we need to push the host context to the stack.
    // However, we can't just call getRootHostContext() and push it because
    // we'd have a different number of entries on the stack depending on
    // whether getRootHostContext() throws somewhere in renderer code or not.
    // So we push an empty value first. This lets us safely unwind on errors.
    push(contextStackCursor, NO_CONTEXT, fiber);
    var nextRootContext = getRootHostContext(nextRootInstance);
    // Now that we know this function doesn't throw, replace it.
    pop(contextStackCursor, fiber);
    push(contextStackCursor, nextRootContext, fiber);
  }

  function popHostContainer(fiber) {
    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
    pop(rootInstanceStackCursor, fiber);
  }

  function getHostContext() {
    var context = requiredContext(contextStackCursor.current);
    return context;
  }

  function pushHostContext(fiber) {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    var context = requiredContext(contextStackCursor.current);
    var nextContext = getChildHostContext(context, fiber.type, rootInstance);

    // Don't push this Fiber's context unless it's unique.
    if (context === nextContext) {
      return;
    }

    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);
    push(contextStackCursor, nextContext, fiber);
  }

  function popHostContext(fiber) {
    // Do not pop unless this Fiber provided the current context.
    // pushHostContext() only pushes Fibers that provide unique contexts.
    if (contextFiberStackCursor.current !== fiber) {
      return;
    }

    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
  }

  return {
    getHostContext: getHostContext,
    getRootHostContainer: getRootHostContainer,
    popHostContainer: popHostContainer,
    popHostContext: popHostContext,
    pushHostContainer: pushHostContainer,
    pushHostContext: pushHostContext
  };
};

var ReactFiberHydrationContext = function (config) {
  var shouldSetTextContent = config.shouldSetTextContent,
      hydration = config.hydration;

  // If this doesn't have hydration mode.

  if (!hydration) {
    return {
      enterHydrationState: function () {
        return false;
      },
      resetHydrationState: function () {},
      tryToClaimNextHydratableInstance: function () {},
      prepareToHydrateHostInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      prepareToHydrateHostTextInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostTextInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      popHydrationState: function (fiber) {
        return false;
      }
    };
  }

  var canHydrateInstance = hydration.canHydrateInstance,
      canHydrateTextInstance = hydration.canHydrateTextInstance,
      getNextHydratableSibling = hydration.getNextHydratableSibling,
      getFirstHydratableChild = hydration.getFirstHydratableChild,
      hydrateInstance = hydration.hydrateInstance,
      hydrateTextInstance = hydration.hydrateTextInstance,
      didNotMatchHydratedContainerTextInstance = hydration.didNotMatchHydratedContainerTextInstance,
      didNotMatchHydratedTextInstance = hydration.didNotMatchHydratedTextInstance,
      didNotHydrateContainerInstance = hydration.didNotHydrateContainerInstance,
      didNotHydrateInstance = hydration.didNotHydrateInstance,
      didNotFindHydratableContainerInstance = hydration.didNotFindHydratableContainerInstance,
      didNotFindHydratableContainerTextInstance = hydration.didNotFindHydratableContainerTextInstance,
      didNotFindHydratableInstance = hydration.didNotFindHydratableInstance,
      didNotFindHydratableTextInstance = hydration.didNotFindHydratableTextInstance;

  // The deepest Fiber on the stack involved in a hydration context.
  // This may have been an insertion or a hydration.

  var hydrationParentFiber = null;
  var nextHydratableInstance = null;
  var isHydrating = false;

  function enterHydrationState(fiber) {
    var parentInstance = fiber.stateNode.containerInfo;
    nextHydratableInstance = getFirstHydratableChild(parentInstance);
    hydrationParentFiber = fiber;
    isHydrating = true;
    return true;
  }

  function deleteHydratableInstance(returnFiber, instance) {
    {
      switch (returnFiber.tag) {
        case HostRoot:
          didNotHydrateContainerInstance(returnFiber.stateNode.containerInfo, instance);
          break;
        case HostComponent:
          didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance);
          break;
      }
    }

    var childToDelete = createFiberFromHostInstanceForDeletion();
    childToDelete.stateNode = instance;
    childToDelete['return'] = returnFiber;
    childToDelete.effectTag = Deletion;

    // This might seem like it belongs on progressedFirstDeletion. However,
    // these children are not part of the reconciliation list of children.
    // Even if we abort and rereconcile the children, that will try to hydrate
    // again and the nodes are still in the host tree so these will be
    // recreated.
    if (returnFiber.lastEffect !== null) {
      returnFiber.lastEffect.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
  }

  function insertNonHydratedInstance(returnFiber, fiber) {
    fiber.effectTag |= Placement;
    {
      switch (returnFiber.tag) {
        case HostRoot:
          {
            var parentContainer = returnFiber.stateNode.containerInfo;
            switch (fiber.tag) {
              case HostComponent:
                var type = fiber.type;
                var props = fiber.pendingProps;
                didNotFindHydratableContainerInstance(parentContainer, type, props);
                break;
              case HostText:
                var text = fiber.pendingProps;
                didNotFindHydratableContainerTextInstance(parentContainer, text);
                break;
            }
            break;
          }
        case HostComponent:
          {
            var parentType = returnFiber.type;
            var parentProps = returnFiber.memoizedProps;
            var parentInstance = returnFiber.stateNode;
            switch (fiber.tag) {
              case HostComponent:
                var _type = fiber.type;
                var _props = fiber.pendingProps;
                didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props);
                break;
              case HostText:
                var _text = fiber.pendingProps;
                didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text);
                break;
            }
            break;
          }
        default:
          return;
      }
    }
  }

  function tryHydrate(fiber, nextInstance) {
    switch (fiber.tag) {
      case HostComponent:
        {
          var type = fiber.type;
          var props = fiber.pendingProps;
          var instance = canHydrateInstance(nextInstance, type, props);
          if (instance !== null) {
            fiber.stateNode = instance;
            return true;
          }
          return false;
        }
      case HostText:
        {
          var text = fiber.pendingProps;
          var textInstance = canHydrateTextInstance(nextInstance, text);
          if (textInstance !== null) {
            fiber.stateNode = textInstance;
            return true;
          }
          return false;
        }
      default:
        return false;
    }
  }

  function tryToClaimNextHydratableInstance(fiber) {
    if (!isHydrating) {
      return;
    }
    var nextInstance = nextHydratableInstance;
    if (!nextInstance) {
      // Nothing to hydrate. Make it an insertion.
      insertNonHydratedInstance(hydrationParentFiber, fiber);
      isHydrating = false;
      hydrationParentFiber = fiber;
      return;
    }
    if (!tryHydrate(fiber, nextInstance)) {
      // If we can't hydrate this instance let's try the next one.
      // We use this as a heuristic. It's based on intuition and not data so it
      // might be flawed or unnecessary.
      nextInstance = getNextHydratableSibling(nextInstance);
      if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
        // Nothing to hydrate. Make it an insertion.
        insertNonHydratedInstance(hydrationParentFiber, fiber);
        isHydrating = false;
        hydrationParentFiber = fiber;
        return;
      }
      // We matched the next one, we'll now assume that the first one was
      // superfluous and we'll delete it. Since we can't eagerly delete it
      // we'll have to schedule a deletion. To do that, this node needs a dummy
      // fiber associated with it.
      deleteHydratableInstance(hydrationParentFiber, nextHydratableInstance);
    }
    hydrationParentFiber = fiber;
    nextHydratableInstance = getFirstHydratableChild(nextInstance);
  }

  function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
    var instance = fiber.stateNode;
    var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber);
    // TODO: Type this specific to this type of component.
    fiber.updateQueue = updatePayload;
    // If the update payload indicates that there is a change or if there
    // is a new ref we mark this as an update.
    if (updatePayload !== null) {
      return true;
    }
    return false;
  }

  function prepareToHydrateHostTextInstance(fiber) {
    var textInstance = fiber.stateNode;
    var textContent = fiber.memoizedProps;
    var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
    {
      if (shouldUpdate) {
        // We assume that prepareToHydrateHostTextInstance is called in a context where the
        // hydration parent is the parent host component of this host text.
        var returnFiber = hydrationParentFiber;
        if (returnFiber !== null) {
          switch (returnFiber.tag) {
            case HostRoot:
              {
                var parentContainer = returnFiber.stateNode.containerInfo;
                didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent);
                break;
              }
            case HostComponent:
              {
                var parentType = returnFiber.type;
                var parentProps = returnFiber.memoizedProps;
                var parentInstance = returnFiber.stateNode;
                didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent);
                break;
              }
          }
        }
      }
    }
    return shouldUpdate;
  }

  function popToNextHostParent(fiber) {
    var parent = fiber['return'];
    while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot) {
      parent = parent['return'];
    }
    hydrationParentFiber = parent;
  }

  function popHydrationState(fiber) {
    if (fiber !== hydrationParentFiber) {
      // We're deeper than the current hydration context, inside an inserted
      // tree.
      return false;
    }
    if (!isHydrating) {
      // If we're not currently hydrating but we're in a hydration context, then
      // we were an insertion and now need to pop up reenter hydration of our
      // siblings.
      popToNextHostParent(fiber);
      isHydrating = true;
      return false;
    }

    var type = fiber.type;

    // If we have any remaining hydratable nodes, we need to delete them now.
    // We only do this deeper than head and body since they tend to have random
    // other nodes in them. We also ignore components with pure text content in
    // side of them.
    // TODO: Better heuristic.
    if (fiber.tag !== HostComponent || type !== 'head' && type !== 'body' && !shouldSetTextContent(type, fiber.memoizedProps)) {
      var nextInstance = nextHydratableInstance;
      while (nextInstance) {
        deleteHydratableInstance(fiber, nextInstance);
        nextInstance = getNextHydratableSibling(nextInstance);
      }
    }

    popToNextHostParent(fiber);
    nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
    return true;
  }

  function resetHydrationState() {
    hydrationParentFiber = null;
    nextHydratableInstance = null;
    isHydrating = false;
  }

  return {
    enterHydrationState: enterHydrationState,
    resetHydrationState: resetHydrationState,
    tryToClaimNextHydratableInstance: tryToClaimNextHydratableInstance,
    prepareToHydrateHostInstance: prepareToHydrateHostInstance,
    prepareToHydrateHostTextInstance: prepareToHydrateHostTextInstance,
    popHydrationState: popHydrationState
  };
};

// This lets us hook into Fiber to debug what it's doing.
// See https://github.com/facebook/react/pull/8033.
// This is not part of the public API, not even for React DevTools.
// You may only inject a debugTool if you work on React Fiber itself.
var ReactFiberInstrumentation = {
  debugTool: null
};

var ReactFiberInstrumentation_1 = ReactFiberInstrumentation;

var warnedAboutMissingGetChildContext = void 0;

{
  warnedAboutMissingGetChildContext = {};
}

var ReactFiberLegacyContext = function (stack) {
  var createCursor = stack.createCursor,
      push = stack.push,
      pop = stack.pop;

  // A cursor to the current merged context object on the stack.

  var contextStackCursor = createCursor(emptyObject);
  // A cursor to a boolean indicating whether the context has changed.
  var didPerformWorkStackCursor = createCursor(false);
  // Keep track of the previous context object that was on the stack.
  // We use this to get access to the parent context after we have already
  // pushed the next context provider, and now need to merge their contexts.
  var previousContext = emptyObject;

  function getUnmaskedContext(workInProgress) {
    var hasOwnContext = isContextProvider(workInProgress);
    if (hasOwnContext) {
      // If the fiber is a context provider itself, when we read its context
      // we have already pushed its own child context on the stack. A context
      // provider should not "see" its own child context. Therefore we read the
      // previous (parent) context instead for a context provider.
      return previousContext;
    }
    return contextStackCursor.current;
  }

  function cacheContext(workInProgress, unmaskedContext, maskedContext) {
    var instance = workInProgress.stateNode;
    instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
    instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
  }

  function getMaskedContext(workInProgress, unmaskedContext) {
    var type = workInProgress.type;
    var contextTypes = type.contextTypes;
    if (!contextTypes) {
      return emptyObject;
    }

    // Avoid recreating masked context unless unmasked context has changed.
    // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
    // This may trigger infinite loops if componentWillReceiveProps calls setState.
    var instance = workInProgress.stateNode;
    if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
      return instance.__reactInternalMemoizedMaskedChildContext;
    }

    var context = {};
    for (var key in contextTypes) {
      context[key] = unmaskedContext[key];
    }

    {
      var name = getComponentName(workInProgress) || 'Unknown';
      checkPropTypes(contextTypes, context, 'context', name, ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
    }

    // Cache unmasked context so we can avoid recreating masked context unless necessary.
    // Context is created before the class component is instantiated so check for instance.
    if (instance) {
      cacheContext(workInProgress, unmaskedContext, context);
    }

    return context;
  }

  function hasContextChanged() {
    return didPerformWorkStackCursor.current;
  }

  function isContextConsumer(fiber) {
    return fiber.tag === ClassComponent && fiber.type.contextTypes != null;
  }

  function isContextProvider(fiber) {
    return fiber.tag === ClassComponent && fiber.type.childContextTypes != null;
  }

  function popContextProvider(fiber) {
    if (!isContextProvider(fiber)) {
      return;
    }

    pop(didPerformWorkStackCursor, fiber);
    pop(contextStackCursor, fiber);
  }

  function popTopLevelContextObject(fiber) {
    pop(didPerformWorkStackCursor, fiber);
    pop(contextStackCursor, fiber);
  }

  function pushTopLevelContextObject(fiber, context, didChange) {
    !(contextStackCursor.cursor == null) ? invariant(false, 'Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    push(contextStackCursor, context, fiber);
    push(didPerformWorkStackCursor, didChange, fiber);
  }

  function processChildContext(fiber, parentContext) {
    var instance = fiber.stateNode;
    var childContextTypes = fiber.type.childContextTypes;

    // TODO (bvaughn) Replace this behavior with an invariant() in the future.
    // It has only been added in Fiber to match the (unintentional) behavior in Stack.
    if (typeof instance.getChildContext !== 'function') {
      {
        var componentName = getComponentName(fiber) || 'Unknown';

        if (!warnedAboutMissingGetChildContext[componentName]) {
          warnedAboutMissingGetChildContext[componentName] = true;
          warning(false, '%s.childContextTypes is specified but there is no getChildContext() method ' + 'on the instance. You can either define getChildContext() on %s or remove ' + 'childContextTypes from it.', componentName, componentName);
        }
      }
      return parentContext;
    }

    var childContext = void 0;
    {
      ReactDebugCurrentFiber.setCurrentPhase('getChildContext');
    }
    startPhaseTimer(fiber, 'getChildContext');
    childContext = instance.getChildContext();
    stopPhaseTimer();
    {
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    for (var contextKey in childContext) {
      !(contextKey in childContextTypes) ? invariant(false, '%s.getChildContext(): key "%s" is not defined in childContextTypes.', getComponentName(fiber) || 'Unknown', contextKey) : void 0;
    }
    {
      var name = getComponentName(fiber) || 'Unknown';
      checkPropTypes(childContextTypes, childContext, 'child context', name,
      // In practice, there is one case in which we won't get a stack. It's when
      // somebody calls unstable_renderSubtreeIntoContainer() and we process
      // context from the parent component instance. The stack will be missing
      // because it's outside of the reconciliation, and so the pointer has not
      // been set. This is rare and doesn't matter. We'll also remove that API.
      ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
    }

    return _assign({}, parentContext, childContext);
  }

  function pushContextProvider(workInProgress) {
    if (!isContextProvider(workInProgress)) {
      return false;
    }

    var instance = workInProgress.stateNode;
    // We push the context as early as possible to ensure stack integrity.
    // If the instance does not exist yet, we will push null at first,
    // and replace it on the stack later when invalidating the context.
    var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyObject;

    // Remember the parent context so we can merge with it later.
    // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.
    previousContext = contextStackCursor.current;
    push(contextStackCursor, memoizedMergedChildContext, workInProgress);
    push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);

    return true;
  }

  function invalidateContextProvider(workInProgress, didChange) {
    var instance = workInProgress.stateNode;
    !instance ? invariant(false, 'Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    if (didChange) {
      // Merge parent and own context.
      // Skip this if we're not updating due to sCU.
      // This avoids unnecessarily recomputing memoized values.
      var mergedContext = processChildContext(workInProgress, previousContext);
      instance.__reactInternalMemoizedMergedChildContext = mergedContext;

      // Replace the old (or empty) context with the new one.
      // It is important to unwind the context in the reverse order.
      pop(didPerformWorkStackCursor, workInProgress);
      pop(contextStackCursor, workInProgress);
      // Now push the new context and mark that it has changed.
      push(contextStackCursor, mergedContext, workInProgress);
      push(didPerformWorkStackCursor, didChange, workInProgress);
    } else {
      pop(didPerformWorkStackCursor, workInProgress);
      push(didPerformWorkStackCursor, didChange, workInProgress);
    }
  }

  function findCurrentUnmaskedContext(fiber) {
    // Currently this is only used with renderSubtreeIntoContainer; not sure if it
    // makes sense elsewhere
    !(isFiberMounted(fiber) && fiber.tag === ClassComponent) ? invariant(false, 'Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    var node = fiber;
    while (node.tag !== HostRoot) {
      if (isContextProvider(node)) {
        return node.stateNode.__reactInternalMemoizedMergedChildContext;
      }
      var parent = node['return'];
      !parent ? invariant(false, 'Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
      node = parent;
    }
    return node.stateNode.context;
  }

  return {
    getUnmaskedContext: getUnmaskedContext,
    cacheContext: cacheContext,
    getMaskedContext: getMaskedContext,
    hasContextChanged: hasContextChanged,
    isContextConsumer: isContextConsumer,
    isContextProvider: isContextProvider,
    popContextProvider: popContextProvider,
    popTopLevelContextObject: popTopLevelContextObject,
    pushTopLevelContextObject: pushTopLevelContextObject,
    processChildContext: processChildContext,
    pushContextProvider: pushContextProvider,
    invalidateContextProvider: invalidateContextProvider,
    findCurrentUnmaskedContext: findCurrentUnmaskedContext
  };
};

var ReactFiberNewContext = function (stack) {
  var createCursor = stack.createCursor,
      push = stack.push,
      pop = stack.pop;


  var providerCursor = createCursor(null);
  var valueCursor = createCursor(null);
  var changedBitsCursor = createCursor(0);

  var rendererSigil = void 0;
  {
    // Use this to detect multiple renderers using the same context
    rendererSigil = {};
  }

  function pushProvider(providerFiber) {
    var context = providerFiber.type._context;

    push(changedBitsCursor, context._changedBits, providerFiber);
    push(valueCursor, context._currentValue, providerFiber);
    push(providerCursor, providerFiber, providerFiber);

    context._currentValue = providerFiber.pendingProps.value;
    context._changedBits = providerFiber.stateNode;

    {
      !(context._currentRenderer === null || context._currentRenderer === rendererSigil) ? warning(false, 'Detected multiple renderers concurrently rendering the ' + 'same context provider. This is currently unsupported.') : void 0;
      context._currentRenderer = rendererSigil;
    }
  }

  function popProvider(providerFiber) {
    var changedBits = changedBitsCursor.current;
    var currentValue = valueCursor.current;

    pop(providerCursor, providerFiber);
    pop(valueCursor, providerFiber);
    pop(changedBitsCursor, providerFiber);

    var context = providerFiber.type._context;
    context._currentValue = currentValue;
    context._changedBits = changedBits;
  }

  return {
    pushProvider: pushProvider,
    popProvider: popProvider
  };
};

var ReactFiberStack = function () {
  var valueStack = [];

  var fiberStack = void 0;

  {
    fiberStack = [];
  }

  var index = -1;

  function createCursor(defaultValue) {
    return {
      current: defaultValue
    };
  }

  function isEmpty() {
    return index === -1;
  }

  function pop(cursor, fiber) {
    if (index < 0) {
      {
        warning(false, 'Unexpected pop.');
      }
      return;
    }

    {
      if (fiber !== fiberStack[index]) {
        warning(false, 'Unexpected Fiber popped.');
      }
    }

    cursor.current = valueStack[index];

    valueStack[index] = null;

    {
      fiberStack[index] = null;
    }

    index--;
  }

  function push(cursor, value, fiber) {
    index++;

    valueStack[index] = cursor.current;

    {
      fiberStack[index] = fiber;
    }

    cursor.current = value;
  }

  function checkThatStackIsEmpty() {
    {
      if (index !== -1) {
        warning(false, 'Expected an empty stack. Something was not reset properly.');
      }
    }
  }

  function resetStackAfterFatalErrorInDev() {
    {
      index = -1;
      valueStack.length = 0;
      fiberStack.length = 0;
    }
  }

  return {
    createCursor: createCursor,
    isEmpty: isEmpty,
    pop: pop,
    push: push,
    checkThatStackIsEmpty: checkThatStackIsEmpty,
    resetStackAfterFatalErrorInDev: resetStackAfterFatalErrorInDev
  };
};

var invokeGuardedCallback$2 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError = ReactErrorUtils.hasCaughtError;
var clearCaughtError = ReactErrorUtils.clearCaughtError;


var didWarnAboutStateTransition = void 0;
var didWarnSetStateChildContext = void 0;
var warnAboutUpdateOnUnmounted = void 0;
var warnAboutInvalidUpdates = void 0;

{
  didWarnAboutStateTransition = false;
  didWarnSetStateChildContext = false;
  var didWarnStateUpdateForUnmountedComponent = {};

  warnAboutUpdateOnUnmounted = function (fiber) {
    // We show the whole stack but dedupe on the top component's name because
    // the problematic code almost always lies inside that component.
    var componentName = getComponentName(fiber) || 'ReactClass';
    if (didWarnStateUpdateForUnmountedComponent[componentName]) {
      return;
    }
    warning(false, "Can't call setState (or forceUpdate) on an unmounted component. This " + 'is a no-op, but it indicates a memory leak in your application. To ' + 'fix, cancel all subscriptions and asynchronous tasks in the ' + 'componentWillUnmount method.%s', getStackAddendumByWorkInProgressFiber(fiber));
    didWarnStateUpdateForUnmountedComponent[componentName] = true;
  };

  warnAboutInvalidUpdates = function (instance) {
    switch (ReactDebugCurrentFiber.phase) {
      case 'getChildContext':
        if (didWarnSetStateChildContext) {
          return;
        }
        warning(false, 'setState(...): Cannot call setState() inside getChildContext()');
        didWarnSetStateChildContext = true;
        break;
      case 'render':
        if (didWarnAboutStateTransition) {
          return;
        }
        warning(false, 'Cannot update during an existing state transition (such as within ' + "`render` or another component's constructor). Render methods should " + 'be a pure function of props and state; constructor side-effects are ' + 'an anti-pattern, but can be moved to `componentWillMount`.');
        didWarnAboutStateTransition = true;
        break;
    }
  };
}

var ReactFiberScheduler = function (config) {
  var stack = ReactFiberStack();
  var hostContext = ReactFiberHostContext(config, stack);
  var legacyContext = ReactFiberLegacyContext(stack);
  var newContext = ReactFiberNewContext(stack);
  var popHostContext = hostContext.popHostContext,
      popHostContainer = hostContext.popHostContainer;
  var popTopLevelLegacyContextObject = legacyContext.popTopLevelContextObject,
      popLegacyContextProvider = legacyContext.popContextProvider;
  var popProvider = newContext.popProvider;

  var hydrationContext = ReactFiberHydrationContext(config);

  var _ReactFiberBeginWork = ReactFiberBeginWork(config, hostContext, legacyContext, newContext, hydrationContext, scheduleWork, computeExpirationForFiber),
      beginWork = _ReactFiberBeginWork.beginWork;

  var _ReactFiberCompleteWo = ReactFiberCompleteWork(config, hostContext, legacyContext, newContext, hydrationContext),
      completeWork = _ReactFiberCompleteWo.completeWork;

  var _ReactFiberUnwindWork = ReactFiberUnwindWork(hostContext, legacyContext, newContext, scheduleWork, isAlreadyFailedLegacyErrorBoundary),
      throwException = _ReactFiberUnwindWork.throwException,
      unwindWork = _ReactFiberUnwindWork.unwindWork,
      unwindInterruptedWork = _ReactFiberUnwindWork.unwindInterruptedWork;

  var _ReactFiberCommitWork = ReactFiberCommitWork(config, onCommitPhaseError, scheduleWork, computeExpirationForFiber, markLegacyErrorBoundaryAsFailed, recalculateCurrentTime),
      commitBeforeMutationLifeCycles = _ReactFiberCommitWork.commitBeforeMutationLifeCycles,
      commitResetTextContent = _ReactFiberCommitWork.commitResetTextContent,
      commitPlacement = _ReactFiberCommitWork.commitPlacement,
      commitDeletion = _ReactFiberCommitWork.commitDeletion,
      commitWork = _ReactFiberCommitWork.commitWork,
      commitLifeCycles = _ReactFiberCommitWork.commitLifeCycles,
      commitErrorLogging = _ReactFiberCommitWork.commitErrorLogging,
      commitAttachRef = _ReactFiberCommitWork.commitAttachRef,
      commitDetachRef = _ReactFiberCommitWork.commitDetachRef;

  var now = config.now,
      scheduleDeferredCallback = config.scheduleDeferredCallback,
      cancelDeferredCallback = config.cancelDeferredCallback,
      prepareForCommit = config.prepareForCommit,
      resetAfterCommit = config.resetAfterCommit;

  // Represents the current time in ms.

  var originalStartTimeMs = now();
  var mostRecentCurrentTime = msToExpirationTime(0);
  var mostRecentCurrentTimeMs = originalStartTimeMs;

  // Used to ensure computeUniqueAsyncExpiration is monotonically increases.
  var lastUniqueAsyncExpiration = 0;

  // Represents the expiration time that incoming updates should use. (If this
  // is NoWork, use the default strategy: async updates in async mode, sync
  // updates in sync mode.)
  var expirationContext = NoWork;

  var isWorking = false;

  // The next work in progress fiber that we're currently working on.
  var nextUnitOfWork = null;
  var nextRoot = null;
  // The time at which we're currently rendering work.
  var nextRenderExpirationTime = NoWork;

  // The next fiber with an effect that we're currently committing.
  var nextEffect = null;

  var isCommitting = false;

  var isRootReadyForCommit = false;

  var legacyErrorBoundariesThatAlreadyFailed = null;

  // Used for performance tracking.
  var interruptedBy = null;

  var stashedWorkInProgressProperties = void 0;
  var replayUnitOfWork = void 0;
  var isReplayingFailedUnitOfWork = void 0;
  var originalReplayError = void 0;
  var rethrowOriginalError = void 0;
  if (true && replayFailedUnitOfWorkWithInvokeGuardedCallback) {
    stashedWorkInProgressProperties = null;
    isReplayingFailedUnitOfWork = false;
    originalReplayError = null;
    replayUnitOfWork = function (failedUnitOfWork, error, isAsync) {
      // Restore the original state of the work-in-progress
      assignFiberPropertiesInDEV(failedUnitOfWork, stashedWorkInProgressProperties);
      switch (failedUnitOfWork.tag) {
        case HostRoot:
          popHostContainer(failedUnitOfWork);
          popTopLevelLegacyContextObject(failedUnitOfWork);
          break;
        case HostComponent:
          popHostContext(failedUnitOfWork);
          break;
        case ClassComponent:
          popLegacyContextProvider(failedUnitOfWork);
          break;
        case HostPortal:
          popHostContainer(failedUnitOfWork);
          break;
        case ContextProvider:
          popProvider(failedUnitOfWork);
          break;
      }
      // Replay the begin phase.
      isReplayingFailedUnitOfWork = true;
      originalReplayError = error;
      invokeGuardedCallback$2(null, workLoop, null, isAsync);
      isReplayingFailedUnitOfWork = false;
      originalReplayError = null;
      if (hasCaughtError()) {
        clearCaughtError();
      } else {
        // If the begin phase did not fail the second time, set this pointer
        // back to the original value.
        nextUnitOfWork = failedUnitOfWork;
      }
    };
    rethrowOriginalError = function () {
      throw originalReplayError;
    };
  }

  function resetStack() {
    if (nextUnitOfWork !== null) {
      var interruptedWork = nextUnitOfWork['return'];
      while (interruptedWork !== null) {
        unwindInterruptedWork(interruptedWork);
        interruptedWork = interruptedWork['return'];
      }
    }

    {
      ReactStrictModeWarnings.discardPendingWarnings();
      stack.checkThatStackIsEmpty();
    }

    nextRoot = null;
    nextRenderExpirationTime = NoWork;
    nextUnitOfWork = null;

    isRootReadyForCommit = false;
  }

  function commitAllHostEffects() {
    while (nextEffect !== null) {
      {
        ReactDebugCurrentFiber.setCurrentFiber(nextEffect);
      }
      recordEffect();

      var effectTag = nextEffect.effectTag;

      if (effectTag & ContentReset) {
        commitResetTextContent(nextEffect);
      }

      if (effectTag & Ref) {
        var current = nextEffect.alternate;
        if (current !== null) {
          commitDetachRef(current);
        }
      }

      // The following switch statement is only concerned about placement,
      // updates, and deletions. To avoid needing to add a case for every
      // possible bitmap value, we remove the secondary effects from the
      // effect tag and switch on that value.
      var primaryEffectTag = effectTag & (Placement | Update | Deletion);
      switch (primaryEffectTag) {
        case Placement:
          {
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            // TODO: findDOMNode doesn't rely on this any more but isMounted
            // does and isMounted is deprecated anyway so we should be able
            // to kill this.
            nextEffect.effectTag &= ~Placement;
            break;
          }
        case PlacementAndUpdate:
          {
            // Placement
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            nextEffect.effectTag &= ~Placement;

            // Update
            var _current = nextEffect.alternate;
            commitWork(_current, nextEffect);
            break;
          }
        case Update:
          {
            var _current2 = nextEffect.alternate;
            commitWork(_current2, nextEffect);
            break;
          }
        case Deletion:
          {
            commitDeletion(nextEffect);
            break;
          }
      }
      nextEffect = nextEffect.nextEffect;
    }

    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
  }

  function commitBeforeMutationLifecycles() {
    while (nextEffect !== null) {
      var effectTag = nextEffect.effectTag;

      if (effectTag & Snapshot) {
        recordEffect();
        var current = nextEffect.alternate;
        commitBeforeMutationLifeCycles(current, nextEffect);
      }

      // Don't cleanup effects yet;
      // This will be done by commitAllLifeCycles()
      nextEffect = nextEffect.nextEffect;
    }
  }

  function commitAllLifeCycles(finishedRoot, currentTime, committedExpirationTime) {
    {
      ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();

      if (warnAboutDeprecatedLifecycles) {
        ReactStrictModeWarnings.flushPendingDeprecationWarnings();
      }
    }
    while (nextEffect !== null) {
      var effectTag = nextEffect.effectTag;

      if (effectTag & (Update | Callback)) {
        recordEffect();
        var current = nextEffect.alternate;
        commitLifeCycles(finishedRoot, current, nextEffect, currentTime, committedExpirationTime);
      }

      if (effectTag & ErrLog) {
        commitErrorLogging(nextEffect, onUncaughtError);
      }

      if (effectTag & Ref) {
        recordEffect();
        commitAttachRef(nextEffect);
      }

      var next = nextEffect.nextEffect;
      // Ensure that we clean these up so that we don't accidentally keep them.
      // I'm not actually sure this matters because we can't reset firstEffect
      // and lastEffect since they're on every node, not just the effectful
      // ones. So we have to clean everything as we reuse nodes anyway.
      nextEffect.nextEffect = null;
      // Ensure that we reset the effectTag here so that we can rely on effect
      // tags to reason about the current life-cycle.
      nextEffect = next;
    }
  }

  function isAlreadyFailedLegacyErrorBoundary(instance) {
    return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
  }

  function markLegacyErrorBoundaryAsFailed(instance) {
    if (legacyErrorBoundariesThatAlreadyFailed === null) {
      legacyErrorBoundariesThatAlreadyFailed = new Set([instance]);
    } else {
      legacyErrorBoundariesThatAlreadyFailed.add(instance);
    }
  }

  function commitRoot(finishedWork) {
    isWorking = true;
    isCommitting = true;
    startCommitTimer();

    var root = finishedWork.stateNode;
    !(root.current !== finishedWork) ? invariant(false, 'Cannot commit the same tree as before. This is probably a bug related to the return field. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    var committedExpirationTime = root.pendingCommitExpirationTime;
    !(committedExpirationTime !== NoWork) ? invariant(false, 'Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    root.pendingCommitExpirationTime = NoWork;

    var currentTime = recalculateCurrentTime();

    // Reset this to null before calling lifecycles
    ReactCurrentOwner.current = null;

    var firstEffect = void 0;
    if (finishedWork.effectTag > PerformedWork) {
      // A fiber's effect list consists only of its children, not itself. So if
      // the root has an effect, we need to add it to the end of the list. The
      // resulting list is the set that would belong to the root's parent, if
      // it had one; that is, all the effects in the tree including the root.
      if (finishedWork.lastEffect !== null) {
        finishedWork.lastEffect.nextEffect = finishedWork;
        firstEffect = finishedWork.firstEffect;
      } else {
        firstEffect = finishedWork;
      }
    } else {
      // There is no effect on the root.
      firstEffect = finishedWork.firstEffect;
    }

    prepareForCommit(root.containerInfo);

    // Invoke instances of getSnapshotBeforeUpdate before mutation.
    nextEffect = firstEffect;
    startCommitSnapshotEffectsTimer();
    while (nextEffect !== null) {
      var didError = false;
      var error = void 0;
      {
        invokeGuardedCallback$2(null, commitBeforeMutationLifecycles, null);
        if (hasCaughtError()) {
          didError = true;
          error = clearCaughtError();
        }
      }
      if (didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        onCommitPhaseError(nextEffect, error);
        // Clean-up
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }
    stopCommitSnapshotEffectsTimer();

    // Commit all the side-effects within a tree. We'll do this in two passes.
    // The first pass performs all the host insertions, updates, deletions and
    // ref unmounts.
    nextEffect = firstEffect;
    startCommitHostEffectsTimer();
    while (nextEffect !== null) {
      var _didError = false;
      var _error = void 0;
      {
        invokeGuardedCallback$2(null, commitAllHostEffects, null);
        if (hasCaughtError()) {
          _didError = true;
          _error = clearCaughtError();
        }
      }
      if (_didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        onCommitPhaseError(nextEffect, _error);
        // Clean-up
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }
    stopCommitHostEffectsTimer();

    resetAfterCommit(root.containerInfo);

    // The work-in-progress tree is now the current tree. This must come after
    // the first pass of the commit phase, so that the previous tree is still
    // current during componentWillUnmount, but before the second pass, so that
    // the finished work is current during componentDidMount/Update.
    root.current = finishedWork;

    // In the second pass we'll perform all life-cycles and ref callbacks.
    // Life-cycles happen as a separate pass so that all placements, updates,
    // and deletions in the entire tree have already been invoked.
    // This pass also triggers any renderer-specific initial effects.
    nextEffect = firstEffect;
    startCommitLifeCyclesTimer();
    while (nextEffect !== null) {
      var _didError2 = false;
      var _error2 = void 0;
      {
        invokeGuardedCallback$2(null, commitAllLifeCycles, null, root, currentTime, committedExpirationTime);
        if (hasCaughtError()) {
          _didError2 = true;
          _error2 = clearCaughtError();
        }
      }
      if (_didError2) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        onCommitPhaseError(nextEffect, _error2);
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }

    isCommitting = false;
    isWorking = false;
    stopCommitLifeCyclesTimer();
    stopCommitTimer();
    if (typeof onCommitRoot === 'function') {
      onCommitRoot(finishedWork.stateNode);
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onCommitWork(finishedWork);
    }

    var remainingTime = root.current.expirationTime;
    if (remainingTime === NoWork) {
      // If there's no remaining work, we can clear the set of already failed
      // error boundaries.
      legacyErrorBoundariesThatAlreadyFailed = null;
    }
    return remainingTime;
  }

  function resetExpirationTime(workInProgress, renderTime) {
    if (renderTime !== Never && workInProgress.expirationTime === Never) {
      // The children of this component are hidden. Don't bubble their
      // expiration times.
      return;
    }

    // Check for pending updates.
    var newExpirationTime = getUpdateExpirationTime(workInProgress);

    // TODO: Calls need to visit stateNode

    // Bubble up the earliest expiration time.
    var child = workInProgress.child;
    while (child !== null) {
      if (child.expirationTime !== NoWork && (newExpirationTime === NoWork || newExpirationTime > child.expirationTime)) {
        newExpirationTime = child.expirationTime;
      }
      child = child.sibling;
    }
    workInProgress.expirationTime = newExpirationTime;
  }

  function completeUnitOfWork(workInProgress) {
    // Attempt to complete the current unit of work, then move to the
    // next sibling. If there are no more siblings, return to the
    // parent fiber.
    while (true) {
      // The current, flushed, state of this fiber is the alternate.
      // Ideally nothing should rely on this, but relying on it here
      // means that we don't need an additional field on the work in
      // progress.
      var current = workInProgress.alternate;
      {
        ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
      }

      var returnFiber = workInProgress['return'];
      var siblingFiber = workInProgress.sibling;

      if ((workInProgress.effectTag & Incomplete) === NoEffect) {
        // This fiber completed.
        var next = completeWork(current, workInProgress, nextRenderExpirationTime);
        stopWorkTimer(workInProgress);
        resetExpirationTime(workInProgress, nextRenderExpirationTime);
        {
          ReactDebugCurrentFiber.resetCurrentFiber();
        }

        if (next !== null) {
          stopWorkTimer(workInProgress);
          if (true && ReactFiberInstrumentation_1.debugTool) {
            ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
          }
          // If completing this work spawned new work, do that next. We'll come
          // back here again.
          return next;
        }

        if (returnFiber !== null &&
        // Do not append effects to parents if a sibling failed to complete
        (returnFiber.effectTag & Incomplete) === NoEffect) {
          // Append all the effects of the subtree and this fiber onto the effect
          // list of the parent. The completion order of the children affects the
          // side-effect order.
          if (returnFiber.firstEffect === null) {
            returnFiber.firstEffect = workInProgress.firstEffect;
          }
          if (workInProgress.lastEffect !== null) {
            if (returnFiber.lastEffect !== null) {
              returnFiber.lastEffect.nextEffect = workInProgress.firstEffect;
            }
            returnFiber.lastEffect = workInProgress.lastEffect;
          }

          // If this fiber had side-effects, we append it AFTER the children's
          // side-effects. We can perform certain side-effects earlier if
          // needed, by doing multiple passes over the effect list. We don't want
          // to schedule our own side-effect on our own list because if end up
          // reusing children we'll schedule this effect onto itself since we're
          // at the end.
          var effectTag = workInProgress.effectTag;
          // Skip both NoWork and PerformedWork tags when creating the effect list.
          // PerformedWork effect is read by React DevTools but shouldn't be committed.
          if (effectTag > PerformedWork) {
            if (returnFiber.lastEffect !== null) {
              returnFiber.lastEffect.nextEffect = workInProgress;
            } else {
              returnFiber.firstEffect = workInProgress;
            }
            returnFiber.lastEffect = workInProgress;
          }
        }

        if (true && ReactFiberInstrumentation_1.debugTool) {
          ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
        }

        if (siblingFiber !== null) {
          // If there is more work to do in this returnFiber, do that next.
          return siblingFiber;
        } else if (returnFiber !== null) {
          // If there's no more work in this returnFiber. Complete the returnFiber.
          workInProgress = returnFiber;
          continue;
        } else {
          // We've reached the root.
          isRootReadyForCommit = true;
          return null;
        }
      } else {
        // This fiber did not complete because something threw. Pop values off
        // the stack without entering the complete phase. If this is a boundary,
        // capture values if possible.
        var _next = unwindWork(workInProgress);
        // Because this fiber did not complete, don't reset its expiration time.
        if (workInProgress.effectTag & DidCapture) {
          // Restarting an error boundary
          stopFailedWorkTimer(workInProgress);
        } else {
          stopWorkTimer(workInProgress);
        }

        {
          ReactDebugCurrentFiber.resetCurrentFiber();
        }

        if (_next !== null) {
          stopWorkTimer(workInProgress);
          if (true && ReactFiberInstrumentation_1.debugTool) {
            ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
          }
          // If completing this work spawned new work, do that next. We'll come
          // back here again.
          // Since we're restarting, remove anything that is not a host effect
          // from the effect tag.
          _next.effectTag &= HostEffectMask;
          return _next;
        }

        if (returnFiber !== null) {
          // Mark the parent fiber as incomplete and clear its effect list.
          returnFiber.firstEffect = returnFiber.lastEffect = null;
          returnFiber.effectTag |= Incomplete;
        }

        if (true && ReactFiberInstrumentation_1.debugTool) {
          ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
        }

        if (siblingFiber !== null) {
          // If there is more work to do in this returnFiber, do that next.
          return siblingFiber;
        } else if (returnFiber !== null) {
          // If there's no more work in this returnFiber. Complete the returnFiber.
          workInProgress = returnFiber;
          continue;
        } else {
          return null;
        }
      }
    }

    // Without this explicit null return Flow complains of invalid return type
    // TODO Remove the above while(true) loop
    // eslint-disable-next-line no-unreachable
    return null;
  }

  function performUnitOfWork(workInProgress) {
    // The current, flushed, state of this fiber is the alternate.
    // Ideally nothing should rely on this, but relying on it here
    // means that we don't need an additional field on the work in
    // progress.
    var current = workInProgress.alternate;

    // See if beginning this work spawns more work.
    startWorkTimer(workInProgress);
    {
      ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
    }

    if (true && replayFailedUnitOfWorkWithInvokeGuardedCallback) {
      stashedWorkInProgressProperties = assignFiberPropertiesInDEV(stashedWorkInProgressProperties, workInProgress);
    }
    var next = beginWork(current, workInProgress, nextRenderExpirationTime);
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
      if (isReplayingFailedUnitOfWork) {
        // Currently replaying a failed unit of work. This should be unreachable,
        // because the render phase is meant to be idempotent, and it should
        // have thrown again. Since it didn't, rethrow the original error, so
        // React's internal stack is not misaligned.
        rethrowOriginalError();
      }
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onBeginWork(workInProgress);
    }

    if (next === null) {
      // If this doesn't spawn new work, complete the current work.
      next = completeUnitOfWork(workInProgress);
    }

    ReactCurrentOwner.current = null;

    return next;
  }

  function workLoop(isAsync) {
    if (!isAsync) {
      // Flush all expired work.
      while (nextUnitOfWork !== null) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    } else {
      // Flush asynchronous work until the deadline runs out of time.
      while (nextUnitOfWork !== null && !shouldYield()) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    }
  }

  function renderRoot(root, expirationTime, isAsync) {
    !!isWorking ? invariant(false, 'renderRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    isWorking = true;

    // Check if we're starting from a fresh stack, or if we're resuming from
    // previously yielded work.
    if (expirationTime !== nextRenderExpirationTime || root !== nextRoot || nextUnitOfWork === null) {
      // Reset the stack and start working from the root.
      resetStack();
      nextRoot = root;
      nextRenderExpirationTime = expirationTime;
      nextUnitOfWork = createWorkInProgress(nextRoot.current, null, nextRenderExpirationTime);
      root.pendingCommitExpirationTime = NoWork;
    }

    var didFatal = false;

    startWorkLoopTimer(nextUnitOfWork);

    do {
      try {
        workLoop(isAsync);
      } catch (thrownValue) {
        if (nextUnitOfWork === null) {
          // This is a fatal error.
          didFatal = true;
          onUncaughtError(thrownValue);
          break;
        }

        if (true && replayFailedUnitOfWorkWithInvokeGuardedCallback) {
          var failedUnitOfWork = nextUnitOfWork;
          replayUnitOfWork(failedUnitOfWork, thrownValue, isAsync);
        }

        var sourceFiber = nextUnitOfWork;
        var returnFiber = sourceFiber['return'];
        if (returnFiber === null) {
          // This is the root. The root could capture its own errors. However,
          // we don't know if it errors before or after we pushed the host
          // context. This information is needed to avoid a stack mismatch.
          // Because we're not sure, treat this as a fatal error. We could track
          // which phase it fails in, but doesn't seem worth it. At least
          // for now.
          didFatal = true;
          onUncaughtError(thrownValue);
          break;
        }
        throwException(returnFiber, sourceFiber, thrownValue);
        nextUnitOfWork = completeUnitOfWork(sourceFiber);
      }
      break;
    } while (true);

    // We're done performing work. Time to clean up.
    var didCompleteRoot = false;
    isWorking = false;

    // Yield back to main thread.
    if (didFatal) {
      stopWorkLoopTimer(interruptedBy, didCompleteRoot);
      interruptedBy = null;
      // There was a fatal error.
      {
        stack.resetStackAfterFatalErrorInDev();
      }
      return null;
    } else if (nextUnitOfWork === null) {
      // We reached the root.
      if (isRootReadyForCommit) {
        didCompleteRoot = true;
        stopWorkLoopTimer(interruptedBy, didCompleteRoot);
        interruptedBy = null;
        // The root successfully completed. It's ready for commit.
        root.pendingCommitExpirationTime = expirationTime;
        var finishedWork = root.current.alternate;
        return finishedWork;
      } else {
        // The root did not complete.
        stopWorkLoopTimer(interruptedBy, didCompleteRoot);
        interruptedBy = null;
        invariant(false, 'Expired work should have completed. This error is likely caused by a bug in React. Please file an issue.');
      }
    } else {
      stopWorkLoopTimer(interruptedBy, didCompleteRoot);
      interruptedBy = null;
      // There's more work to do, but we ran out of time. Yield back to
      // the renderer.
      return null;
    }
  }

  function scheduleCapture(sourceFiber, boundaryFiber, value, expirationTime) {
    // TODO: We only support dispatching errors.
    var capturedValue = createCapturedValue(value, sourceFiber);
    var update = {
      expirationTime: expirationTime,
      partialState: null,
      callback: null,
      isReplace: false,
      isForced: false,
      capturedValue: capturedValue,
      next: null
    };
    insertUpdateIntoFiber(boundaryFiber, update);
    scheduleWork(boundaryFiber, expirationTime);
  }

  function dispatch(sourceFiber, value, expirationTime) {
    !(!isWorking || isCommitting) ? invariant(false, 'dispatch: Cannot dispatch during the render phase.') : void 0;

    // TODO: Handle arrays

    var fiber = sourceFiber['return'];
    while (fiber !== null) {
      switch (fiber.tag) {
        case ClassComponent:
          var ctor = fiber.type;
          var instance = fiber.stateNode;
          if (typeof ctor.getDerivedStateFromCatch === 'function' || typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance)) {
            scheduleCapture(sourceFiber, fiber, value, expirationTime);
            return;
          }
          break;
        // TODO: Handle async boundaries
        case HostRoot:
          scheduleCapture(sourceFiber, fiber, value, expirationTime);
          return;
      }
      fiber = fiber['return'];
    }

    if (sourceFiber.tag === HostRoot) {
      // Error was thrown at the root. There is no parent, so the root
      // itself should capture it.
      scheduleCapture(sourceFiber, sourceFiber, value, expirationTime);
    }
  }

  function onCommitPhaseError(fiber, error) {
    return dispatch(fiber, error, Sync);
  }

  function computeAsyncExpiration(currentTime) {
    // Given the current clock time, returns an expiration time. We use rounding
    // to batch like updates together.
    // Should complete within ~1000ms. 1200ms max.
    var expirationMs = 5000;
    var bucketSizeMs = 250;
    return computeExpirationBucket(currentTime, expirationMs, bucketSizeMs);
  }

  function computeInteractiveExpiration(currentTime) {
    var expirationMs = void 0;
    // We intentionally set a higher expiration time for interactive updates in
    // dev than in production.
    // If the main thread is being blocked so long that you hit the expiration,
    // it's a problem that could be solved with better scheduling.
    // People will be more likely to notice this and fix it with the long
    // expiration time in development.
    // In production we opt for better UX at the risk of masking scheduling
    // problems, by expiring fast.
    {
      // Should complete within ~500ms. 600ms max.
      expirationMs = 500;
    }
    var bucketSizeMs = 100;
    return computeExpirationBucket(currentTime, expirationMs, bucketSizeMs);
  }

  // Creates a unique async expiration time.
  function computeUniqueAsyncExpiration() {
    var currentTime = recalculateCurrentTime();
    var result = computeAsyncExpiration(currentTime);
    if (result <= lastUniqueAsyncExpiration) {
      // Since we assume the current time monotonically increases, we only hit
      // this branch when computeUniqueAsyncExpiration is fired multiple times
      // within a 200ms window (or whatever the async bucket size is).
      result = lastUniqueAsyncExpiration + 1;
    }
    lastUniqueAsyncExpiration = result;
    return lastUniqueAsyncExpiration;
  }

  function computeExpirationForFiber(fiber) {
    var expirationTime = void 0;
    if (expirationContext !== NoWork) {
      // An explicit expiration context was set;
      expirationTime = expirationContext;
    } else if (isWorking) {
      if (isCommitting) {
        // Updates that occur during the commit phase should have sync priority
        // by default.
        expirationTime = Sync;
      } else {
        // Updates during the render phase should expire at the same time as
        // the work that is being rendered.
        expirationTime = nextRenderExpirationTime;
      }
    } else {
      // No explicit expiration context was set, and we're not currently
      // performing work. Calculate a new expiration time.
      if (fiber.mode & AsyncMode) {
        if (isBatchingInteractiveUpdates) {
          // This is an interactive update
          var currentTime = recalculateCurrentTime();
          expirationTime = computeInteractiveExpiration(currentTime);
        } else {
          // This is an async update
          var _currentTime = recalculateCurrentTime();
          expirationTime = computeAsyncExpiration(_currentTime);
        }
      } else {
        // This is a sync update
        expirationTime = Sync;
      }
    }
    if (isBatchingInteractiveUpdates) {
      // This is an interactive update. Keep track of the lowest pending
      // interactive expiration time. This allows us to synchronously flush
      // all interactive updates when needed.
      if (lowestPendingInteractiveExpirationTime === NoWork || expirationTime > lowestPendingInteractiveExpirationTime) {
        lowestPendingInteractiveExpirationTime = expirationTime;
      }
    }
    return expirationTime;
  }

  function scheduleWork(fiber, expirationTime) {
    return scheduleWorkImpl(fiber, expirationTime, false);
  }

  function scheduleWorkImpl(fiber, expirationTime, isErrorRecovery) {
    recordScheduleUpdate();

    {
      if (!isErrorRecovery && fiber.tag === ClassComponent) {
        var instance = fiber.stateNode;
        warnAboutInvalidUpdates(instance);
      }
    }

    var node = fiber;
    while (node !== null) {
      // Walk the parent path to the root and update each node's
      // expiration time.
      if (node.expirationTime === NoWork || node.expirationTime > expirationTime) {
        node.expirationTime = expirationTime;
      }
      if (node.alternate !== null) {
        if (node.alternate.expirationTime === NoWork || node.alternate.expirationTime > expirationTime) {
          node.alternate.expirationTime = expirationTime;
        }
      }
      if (node['return'] === null) {
        if (node.tag === HostRoot) {
          var root = node.stateNode;
          if (!isWorking && nextRenderExpirationTime !== NoWork && expirationTime < nextRenderExpirationTime) {
            // This is an interruption. (Used for performance tracking.)
            interruptedBy = fiber;
            resetStack();
          }
          if (
          // If we're in the render phase, we don't need to schedule this root
          // for an update, because we'll do it before we exit...
          !isWorking || isCommitting ||
          // ...unless this is a different root than the one we're rendering.
          nextRoot !== root) {
            // Add this root to the root schedule.
            requestWork(root, expirationTime);
          }
          if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
            invariant(false, 'Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.');
          }
        } else {
          {
            if (!isErrorRecovery && fiber.tag === ClassComponent) {
              warnAboutUpdateOnUnmounted(fiber);
            }
          }
          return;
        }
      }
      node = node['return'];
    }
  }

  function recalculateCurrentTime() {
    // Subtract initial time so it fits inside 32bits
    mostRecentCurrentTimeMs = now() - originalStartTimeMs;
    mostRecentCurrentTime = msToExpirationTime(mostRecentCurrentTimeMs);
    return mostRecentCurrentTime;
  }

  function deferredUpdates(fn) {
    var previousExpirationContext = expirationContext;
    var currentTime = recalculateCurrentTime();
    expirationContext = computeAsyncExpiration(currentTime);
    try {
      return fn();
    } finally {
      expirationContext = previousExpirationContext;
    }
  }
  function syncUpdates(fn, a, b, c, d) {
    var previousExpirationContext = expirationContext;
    expirationContext = Sync;
    try {
      return fn(a, b, c, d);
    } finally {
      expirationContext = previousExpirationContext;
    }
  }

  // TODO: Everything below this is written as if it has been lifted to the
  // renderers. I'll do this in a follow-up.

  // Linked-list of roots
  var firstScheduledRoot = null;
  var lastScheduledRoot = null;

  var callbackExpirationTime = NoWork;
  var callbackID = -1;
  var isRendering = false;
  var nextFlushedRoot = null;
  var nextFlushedExpirationTime = NoWork;
  var lowestPendingInteractiveExpirationTime = NoWork;
  var deadlineDidExpire = false;
  var hasUnhandledError = false;
  var unhandledError = null;
  var deadline = null;

  var isBatchingUpdates = false;
  var isUnbatchingUpdates = false;
  var isBatchingInteractiveUpdates = false;

  var completedBatches = null;

  // Use these to prevent an infinite loop of nested updates
  var NESTED_UPDATE_LIMIT = 1000;
  var nestedUpdateCount = 0;

  var timeHeuristicForUnitOfWork = 1;

  function scheduleCallbackWithExpiration(expirationTime) {
    if (callbackExpirationTime !== NoWork) {
      // A callback is already scheduled. Check its expiration time (timeout).
      if (expirationTime > callbackExpirationTime) {
        // Existing callback has sufficient timeout. Exit.
        return;
      } else {
        // Existing callback has insufficient timeout. Cancel and schedule a
        // new one.
        cancelDeferredCallback(callbackID);
      }
      // The request callback timer is already running. Don't start a new one.
    } else {
      startRequestCallbackTimer();
    }

    // Compute a timeout for the given expiration time.
    var currentMs = now() - originalStartTimeMs;
    var expirationMs = expirationTimeToMs(expirationTime);
    var timeout = expirationMs - currentMs;

    callbackExpirationTime = expirationTime;
    callbackID = scheduleDeferredCallback(performAsyncWork, { timeout: timeout });
  }

  // requestWork is called by the scheduler whenever a root receives an update.
  // It's up to the renderer to call renderRoot at some point in the future.
  function requestWork(root, expirationTime) {
    addRootToSchedule(root, expirationTime);

    if (isRendering) {
      // Prevent reentrancy. Remaining work will be scheduled at the end of
      // the currently rendering batch.
      return;
    }

    if (isBatchingUpdates) {
      // Flush work at the end of the batch.
      if (isUnbatchingUpdates) {
        // ...unless we're inside unbatchedUpdates, in which case we should
        // flush it now.
        nextFlushedRoot = root;
        nextFlushedExpirationTime = Sync;
        performWorkOnRoot(root, Sync, false);
      }
      return;
    }

    // TODO: Get rid of Sync and use current time?
    if (expirationTime === Sync) {
      performSyncWork();
    } else {
      scheduleCallbackWithExpiration(expirationTime);
    }
  }

  function addRootToSchedule(root, expirationTime) {
    // Add the root to the schedule.
    // Check if this root is already part of the schedule.
    if (root.nextScheduledRoot === null) {
      // This root is not already scheduled. Add it.
      root.remainingExpirationTime = expirationTime;
      if (lastScheduledRoot === null) {
        firstScheduledRoot = lastScheduledRoot = root;
        root.nextScheduledRoot = root;
      } else {
        lastScheduledRoot.nextScheduledRoot = root;
        lastScheduledRoot = root;
        lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
      }
    } else {
      // This root is already scheduled, but its priority may have increased.
      var remainingExpirationTime = root.remainingExpirationTime;
      if (remainingExpirationTime === NoWork || expirationTime < remainingExpirationTime) {
        // Update the priority.
        root.remainingExpirationTime = expirationTime;
      }
    }
  }

  function findHighestPriorityRoot() {
    var highestPriorityWork = NoWork;
    var highestPriorityRoot = null;
    if (lastScheduledRoot !== null) {
      var previousScheduledRoot = lastScheduledRoot;
      var root = firstScheduledRoot;
      while (root !== null) {
        var remainingExpirationTime = root.remainingExpirationTime;
        if (remainingExpirationTime === NoWork) {
          // This root no longer has work. Remove it from the scheduler.

          // TODO: This check is redudant, but Flow is confused by the branch
          // below where we set lastScheduledRoot to null, even though we break
          // from the loop right after.
          !(previousScheduledRoot !== null && lastScheduledRoot !== null) ? invariant(false, 'Should have a previous and last root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          if (root === root.nextScheduledRoot) {
            // This is the only root in the list.
            root.nextScheduledRoot = null;
            firstScheduledRoot = lastScheduledRoot = null;
            break;
          } else if (root === firstScheduledRoot) {
            // This is the first root in the list.
            var next = root.nextScheduledRoot;
            firstScheduledRoot = next;
            lastScheduledRoot.nextScheduledRoot = next;
            root.nextScheduledRoot = null;
          } else if (root === lastScheduledRoot) {
            // This is the last root in the list.
            lastScheduledRoot = previousScheduledRoot;
            lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
            root.nextScheduledRoot = null;
            break;
          } else {
            previousScheduledRoot.nextScheduledRoot = root.nextScheduledRoot;
            root.nextScheduledRoot = null;
          }
          root = previousScheduledRoot.nextScheduledRoot;
        } else {
          if (highestPriorityWork === NoWork || remainingExpirationTime < highestPriorityWork) {
            // Update the priority, if it's higher
            highestPriorityWork = remainingExpirationTime;
            highestPriorityRoot = root;
          }
          if (root === lastScheduledRoot) {
            break;
          }
          previousScheduledRoot = root;
          root = root.nextScheduledRoot;
        }
      }
    }

    // If the next root is the same as the previous root, this is a nested
    // update. To prevent an infinite loop, increment the nested update count.
    var previousFlushedRoot = nextFlushedRoot;
    if (previousFlushedRoot !== null && previousFlushedRoot === highestPriorityRoot && highestPriorityWork === Sync) {
      nestedUpdateCount++;
    } else {
      // Reset whenever we switch roots.
      nestedUpdateCount = 0;
    }
    nextFlushedRoot = highestPriorityRoot;
    nextFlushedExpirationTime = highestPriorityWork;
  }

  function performAsyncWork(dl) {
    performWork(NoWork, true, dl);
  }

  function performSyncWork() {
    performWork(Sync, false, null);
  }

  function performWork(minExpirationTime, isAsync, dl) {
    deadline = dl;

    // Keep working on roots until there's no more work, or until the we reach
    // the deadline.
    findHighestPriorityRoot();

    if (enableUserTimingAPI && deadline !== null) {
      var didExpire = nextFlushedExpirationTime < recalculateCurrentTime();
      var timeout = expirationTimeToMs(nextFlushedExpirationTime);
      stopRequestCallbackTimer(didExpire, timeout);
    }

    if (isAsync) {
      while (nextFlushedRoot !== null && nextFlushedExpirationTime !== NoWork && (minExpirationTime === NoWork || minExpirationTime >= nextFlushedExpirationTime) && (!deadlineDidExpire || recalculateCurrentTime() >= nextFlushedExpirationTime)) {
        performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime, !deadlineDidExpire);
        findHighestPriorityRoot();
      }
    } else {
      while (nextFlushedRoot !== null && nextFlushedExpirationTime !== NoWork && (minExpirationTime === NoWork || minExpirationTime >= nextFlushedExpirationTime)) {
        performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime, false);
        findHighestPriorityRoot();
      }
    }

    // We're done flushing work. Either we ran out of time in this callback,
    // or there's no more work left with sufficient priority.

    // If we're inside a callback, set this to false since we just completed it.
    if (deadline !== null) {
      callbackExpirationTime = NoWork;
      callbackID = -1;
    }
    // If there's work left over, schedule a new callback.
    if (nextFlushedExpirationTime !== NoWork) {
      scheduleCallbackWithExpiration(nextFlushedExpirationTime);
    }

    // Clean-up.
    deadline = null;
    deadlineDidExpire = false;

    finishRendering();
  }

  function flushRoot(root, expirationTime) {
    !!isRendering ? invariant(false, 'work.commit(): Cannot commit while already rendering. This likely means you attempted to commit from inside a lifecycle method.') : void 0;
    // Perform work on root as if the given expiration time is the current time.
    // This has the effect of synchronously flushing all work up to and
    // including the given time.
    nextFlushedRoot = root;
    nextFlushedExpirationTime = expirationTime;
    performWorkOnRoot(root, expirationTime, false);
    // Flush any sync work that was scheduled by lifecycles
    performSyncWork();
    finishRendering();
  }

  function finishRendering() {
    nestedUpdateCount = 0;

    if (completedBatches !== null) {
      var batches = completedBatches;
      completedBatches = null;
      for (var i = 0; i < batches.length; i++) {
        var batch = batches[i];
        try {
          batch._onComplete();
        } catch (error) {
          if (!hasUnhandledError) {
            hasUnhandledError = true;
            unhandledError = error;
          }
        }
      }
    }

    if (hasUnhandledError) {
      var error = unhandledError;
      unhandledError = null;
      hasUnhandledError = false;
      throw error;
    }
  }

  function performWorkOnRoot(root, expirationTime, isAsync) {
    !!isRendering ? invariant(false, 'performWorkOnRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    isRendering = true;

    // Check if this is async work or sync/expired work.
    if (!isAsync) {
      // Flush sync work.
      var finishedWork = root.finishedWork;
      if (finishedWork !== null) {
        // This root is already complete. We can commit it.
        completeRoot(root, finishedWork, expirationTime);
      } else {
        root.finishedWork = null;
        finishedWork = renderRoot(root, expirationTime, false);
        if (finishedWork !== null) {
          // We've completed the root. Commit it.
          completeRoot(root, finishedWork, expirationTime);
        }
      }
    } else {
      // Flush async work.
      var _finishedWork = root.finishedWork;
      if (_finishedWork !== null) {
        // This root is already complete. We can commit it.
        completeRoot(root, _finishedWork, expirationTime);
      } else {
        root.finishedWork = null;
        _finishedWork = renderRoot(root, expirationTime, true);
        if (_finishedWork !== null) {
          // We've completed the root. Check the deadline one more time
          // before committing.
          if (!shouldYield()) {
            // Still time left. Commit the root.
            completeRoot(root, _finishedWork, expirationTime);
          } else {
            // There's no time left. Mark this root as complete. We'll come
            // back and commit it later.
            root.finishedWork = _finishedWork;
          }
        }
      }
    }

    isRendering = false;
  }

  function completeRoot(root, finishedWork, expirationTime) {
    // Check if there's a batch that matches this expiration time.
    var firstBatch = root.firstBatch;
    if (firstBatch !== null && firstBatch._expirationTime <= expirationTime) {
      if (completedBatches === null) {
        completedBatches = [firstBatch];
      } else {
        completedBatches.push(firstBatch);
      }
      if (firstBatch._defer) {
        // This root is blocked from committing by a batch. Unschedule it until
        // we receive another update.
        root.finishedWork = finishedWork;
        root.remainingExpirationTime = NoWork;
        return;
      }
    }

    // Commit the root.
    root.finishedWork = null;
    root.remainingExpirationTime = commitRoot(finishedWork);
  }

  // When working on async work, the reconciler asks the renderer if it should
  // yield execution. For DOM, we implement this with requestIdleCallback.
  function shouldYield() {
    if (deadline === null) {
      return false;
    }
    if (deadline.timeRemaining() > timeHeuristicForUnitOfWork) {
      // Disregard deadline.didTimeout. Only expired work should be flushed
      // during a timeout. This path is only hit for non-expired work.
      return false;
    }
    deadlineDidExpire = true;
    return true;
  }

  function onUncaughtError(error) {
    !(nextFlushedRoot !== null) ? invariant(false, 'Should be working on a root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    // Unschedule this root so we don't work on it again until there's
    // another update.
    nextFlushedRoot.remainingExpirationTime = NoWork;
    if (!hasUnhandledError) {
      hasUnhandledError = true;
      unhandledError = error;
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function batchedUpdates(fn, a) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return fn(a);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      if (!isBatchingUpdates && !isRendering) {
        performSyncWork();
      }
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function unbatchedUpdates(fn, a) {
    if (isBatchingUpdates && !isUnbatchingUpdates) {
      isUnbatchingUpdates = true;
      try {
        return fn(a);
      } finally {
        isUnbatchingUpdates = false;
      }
    }
    return fn(a);
  }

  // TODO: Batching should be implemented at the renderer level, not within
  // the reconciler.
  function flushSync(fn, a) {
    !!isRendering ? invariant(false, 'flushSync was called from inside a lifecycle method. It cannot be called when React is already rendering.') : void 0;
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return syncUpdates(fn, a);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      performSyncWork();
    }
  }

  function interactiveUpdates(fn, a, b) {
    if (isBatchingInteractiveUpdates) {
      return fn(a, b);
    }
    // If there are any pending interactive updates, synchronously flush them.
    // This needs to happen before we read any handlers, because the effect of
    // the previous event may influence which handlers are called during
    // this event.
    if (!isBatchingUpdates && !isRendering && lowestPendingInteractiveExpirationTime !== NoWork) {
      // Synchronously flush pending interactive updates.
      performWork(lowestPendingInteractiveExpirationTime, false, null);
      lowestPendingInteractiveExpirationTime = NoWork;
    }
    var previousIsBatchingInteractiveUpdates = isBatchingInteractiveUpdates;
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingInteractiveUpdates = true;
    isBatchingUpdates = true;
    try {
      return fn(a, b);
    } finally {
      isBatchingInteractiveUpdates = previousIsBatchingInteractiveUpdates;
      isBatchingUpdates = previousIsBatchingUpdates;
      if (!isBatchingUpdates && !isRendering) {
        performSyncWork();
      }
    }
  }

  function flushInteractiveUpdates() {
    if (!isRendering && lowestPendingInteractiveExpirationTime !== NoWork) {
      // Synchronously flush pending interactive updates.
      performWork(lowestPendingInteractiveExpirationTime, false, null);
      lowestPendingInteractiveExpirationTime = NoWork;
    }
  }

  function flushControlled(fn) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      syncUpdates(fn);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      if (!isBatchingUpdates && !isRendering) {
        performWork(Sync, false, null);
      }
    }
  }

  return {
    recalculateCurrentTime: recalculateCurrentTime,
    computeExpirationForFiber: computeExpirationForFiber,
    scheduleWork: scheduleWork,
    requestWork: requestWork,
    flushRoot: flushRoot,
    batchedUpdates: batchedUpdates,
    unbatchedUpdates: unbatchedUpdates,
    flushSync: flushSync,
    flushControlled: flushControlled,
    deferredUpdates: deferredUpdates,
    syncUpdates: syncUpdates,
    interactiveUpdates: interactiveUpdates,
    flushInteractiveUpdates: flushInteractiveUpdates,
    computeUniqueAsyncExpiration: computeUniqueAsyncExpiration,
    legacyContext: legacyContext
  };
};

var didWarnAboutNestedUpdates = void 0;

{
  didWarnAboutNestedUpdates = false;
}

// 0 is PROD, 1 is DEV.
// Might add PROFILE later.


var ReactFiberReconciler$1 = function (config) {
  var getPublicInstance = config.getPublicInstance;

  var _ReactFiberScheduler = ReactFiberScheduler(config),
      computeUniqueAsyncExpiration = _ReactFiberScheduler.computeUniqueAsyncExpiration,
      recalculateCurrentTime = _ReactFiberScheduler.recalculateCurrentTime,
      computeExpirationForFiber = _ReactFiberScheduler.computeExpirationForFiber,
      scheduleWork = _ReactFiberScheduler.scheduleWork,
      requestWork = _ReactFiberScheduler.requestWork,
      flushRoot = _ReactFiberScheduler.flushRoot,
      batchedUpdates = _ReactFiberScheduler.batchedUpdates,
      unbatchedUpdates = _ReactFiberScheduler.unbatchedUpdates,
      flushSync = _ReactFiberScheduler.flushSync,
      flushControlled = _ReactFiberScheduler.flushControlled,
      deferredUpdates = _ReactFiberScheduler.deferredUpdates,
      syncUpdates = _ReactFiberScheduler.syncUpdates,
      interactiveUpdates = _ReactFiberScheduler.interactiveUpdates,
      flushInteractiveUpdates = _ReactFiberScheduler.flushInteractiveUpdates,
      legacyContext = _ReactFiberScheduler.legacyContext;

  var findCurrentUnmaskedContext = legacyContext.findCurrentUnmaskedContext,
      isContextProvider = legacyContext.isContextProvider,
      processChildContext = legacyContext.processChildContext;


  function getContextForSubtree(parentComponent) {
    if (!parentComponent) {
      return emptyObject;
    }

    var fiber = get(parentComponent);
    var parentContext = findCurrentUnmaskedContext(fiber);
    return isContextProvider(fiber) ? processChildContext(fiber, parentContext) : parentContext;
  }

  function scheduleRootUpdate(current, element, currentTime, expirationTime, callback) {
    {
      if (ReactDebugCurrentFiber.phase === 'render' && ReactDebugCurrentFiber.current !== null && !didWarnAboutNestedUpdates) {
        didWarnAboutNestedUpdates = true;
        warning(false, 'Render methods should be a pure function of props and state; ' + 'triggering nested component updates from render is not allowed. ' + 'If necessary, trigger nested updates in componentDidUpdate.\n\n' + 'Check the render method of %s.', getComponentName(ReactDebugCurrentFiber.current) || 'Unknown');
      }
    }

    callback = callback === undefined ? null : callback;
    {
      !(callback === null || typeof callback === 'function') ? warning(false, 'render(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callback) : void 0;
    }

    var update = {
      expirationTime: expirationTime,
      partialState: { element: element },
      callback: callback,
      isReplace: false,
      isForced: false,
      capturedValue: null,
      next: null
    };
    insertUpdateIntoFiber(current, update);
    scheduleWork(current, expirationTime);

    return expirationTime;
  }

  function updateContainerAtExpirationTime(element, container, parentComponent, currentTime, expirationTime, callback) {
    // TODO: If this is a nested container, this won't be the root.
    var current = container.current;

    {
      if (ReactFiberInstrumentation_1.debugTool) {
        if (current.alternate === null) {
          ReactFiberInstrumentation_1.debugTool.onMountContainer(container);
        } else if (element === null) {
          ReactFiberInstrumentation_1.debugTool.onUnmountContainer(container);
        } else {
          ReactFiberInstrumentation_1.debugTool.onUpdateContainer(container);
        }
      }
    }

    var context = getContextForSubtree(parentComponent);
    if (container.context === null) {
      container.context = context;
    } else {
      container.pendingContext = context;
    }

    return scheduleRootUpdate(current, element, currentTime, expirationTime, callback);
  }

  function findHostInstance(component) {
    var fiber = get(component);
    if (fiber === undefined) {
      if (typeof component.render === 'function') {
        invariant(false, 'Unable to find node on an unmounted component.');
      } else {
        invariant(false, 'Argument appears to not be a ReactComponent. Keys: %s', Object.keys(component));
      }
    }
    var hostFiber = findCurrentHostFiber(fiber);
    if (hostFiber === null) {
      return null;
    }
    return hostFiber.stateNode;
  }

  return {
    createContainer: function (containerInfo, isAsync, hydrate) {
      return createFiberRoot(containerInfo, isAsync, hydrate);
    },
    updateContainer: function (element, container, parentComponent, callback) {
      var current = container.current;
      var currentTime = recalculateCurrentTime();
      var expirationTime = computeExpirationForFiber(current);
      return updateContainerAtExpirationTime(element, container, parentComponent, currentTime, expirationTime, callback);
    },
    updateContainerAtExpirationTime: function (element, container, parentComponent, expirationTime, callback) {
      var currentTime = recalculateCurrentTime();
      return updateContainerAtExpirationTime(element, container, parentComponent, currentTime, expirationTime, callback);
    },


    flushRoot: flushRoot,

    requestWork: requestWork,

    computeUniqueAsyncExpiration: computeUniqueAsyncExpiration,

    batchedUpdates: batchedUpdates,

    unbatchedUpdates: unbatchedUpdates,

    deferredUpdates: deferredUpdates,

    syncUpdates: syncUpdates,

    interactiveUpdates: interactiveUpdates,

    flushInteractiveUpdates: flushInteractiveUpdates,

    flushControlled: flushControlled,

    flushSync: flushSync,

    getPublicRootInstance: function (container) {
      var containerFiber = container.current;
      if (!containerFiber.child) {
        return null;
      }
      switch (containerFiber.child.tag) {
        case HostComponent:
          return getPublicInstance(containerFiber.child.stateNode);
        default:
          return containerFiber.child.stateNode;
      }
    },


    findHostInstance: findHostInstance,

    findHostInstanceWithNoPortals: function (fiber) {
      var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
      if (hostFiber === null) {
        return null;
      }
      return hostFiber.stateNode;
    },
    injectIntoDevTools: function (devToolsConfig) {
      var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;

      return injectInternals(_assign({}, devToolsConfig, {
        findHostInstanceByFiber: function (fiber) {
          var hostFiber = findCurrentHostFiber(fiber);
          if (hostFiber === null) {
            return null;
          }
          return hostFiber.stateNode;
        },
        findFiberByHostInstance: function (instance) {
          if (!findFiberByHostInstance) {
            // Might not be implemented by the renderer.
            return null;
          }
          return findFiberByHostInstance(instance);
        }
      }));
    }
  };
};

var ReactFiberReconciler$2 = Object.freeze({
	default: ReactFiberReconciler$1
});

var ReactFiberReconciler$3 = ( ReactFiberReconciler$2 && ReactFiberReconciler$1 ) || ReactFiberReconciler$2;

// TODO: bundle Flow types with the package.



// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactReconciler = ReactFiberReconciler$3['default'] ? ReactFiberReconciler$3['default'] : ReactFiberReconciler$3;

function createPortal$1(children, containerInfo,
// TODO: figure out the API for cross-renderer implementation.
implementation) {
  var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;

  return {
    // This tag allow us to uniquely identify this as a React Portal
    $$typeof: REACT_PORTAL_TYPE,
    key: key == null ? null : '' + key,
    children: children,
    containerInfo: containerInfo,
    implementation: implementation
  };
}

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.3.2';

// a requestAnimationFrame, storing the time for the start of the frame, then
// scheduling a postMessage which gets scheduled after paint. Within the
// postMessage handler do as much work as possible until time + frame rate.
// By separating the idle call into a separate event tick we ensure that
// layout, paint and other browser work is counted against the available time.
// The frame rate is dynamically adjusted.

{
  if (ExecutionEnvironment.canUseDOM && typeof requestAnimationFrame !== 'function') {
    warning(false, 'React depends on requestAnimationFrame. Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
  }
}

var hasNativePerformanceNow = typeof performance === 'object' && typeof performance.now === 'function';

var now = void 0;
if (hasNativePerformanceNow) {
  now = function () {
    return performance.now();
  };
} else {
  now = function () {
    return Date.now();
  };
}

// TODO: There's no way to cancel, because Fiber doesn't atm.
var rIC = void 0;
var cIC = void 0;

if (!ExecutionEnvironment.canUseDOM) {
  rIC = function (frameCallback) {
    return setTimeout(function () {
      frameCallback({
        timeRemaining: function () {
          return Infinity;
        },

        didTimeout: false
      });
    });
  };
  cIC = function (timeoutID) {
    clearTimeout(timeoutID);
  };
} else if (alwaysUseRequestIdleCallbackPolyfill || typeof requestIdleCallback !== 'function' || typeof cancelIdleCallback !== 'function') {
  // Polyfill requestIdleCallback and cancelIdleCallback

  var scheduledRICCallback = null;
  var isIdleScheduled = false;
  var timeoutTime = -1;

  var isAnimationFrameScheduled = false;

  var frameDeadline = 0;
  // We start out assuming that we run at 30fps but then the heuristic tracking
  // will adjust this value to a faster fps if we get more frequent animation
  // frames.
  var previousFrameTime = 33;
  var activeFrameTime = 33;

  var frameDeadlineObject = void 0;
  if (hasNativePerformanceNow) {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // We assume that if we have a performance timer that the rAF callback
        // gets a performance timer value. Not sure if this is always true.
        var remaining = frameDeadline - performance.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  } else {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // Fallback to Date.now()
        var remaining = frameDeadline - Date.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  }

  // We use the postMessage trick to defer idle work until after the repaint.
  var messageKey = '__reactIdleCallback$' + Math.random().toString(36).slice(2);
  var idleTick = function (event) {
    if (event.source !== window || event.data !== messageKey) {
      return;
    }

    isIdleScheduled = false;

    var currentTime = now();
    if (frameDeadline - currentTime <= 0) {
      // There's no time left in this idle period. Check if the callback has
      // a timeout and whether it's been exceeded.
      if (timeoutTime !== -1 && timeoutTime <= currentTime) {
        // Exceeded the timeout. Invoke the callback even though there's no
        // time left.
        frameDeadlineObject.didTimeout = true;
      } else {
        // No timeout.
        if (!isAnimationFrameScheduled) {
          // Schedule another animation callback so we retry later.
          isAnimationFrameScheduled = true;
          requestAnimationFrame(animationTick);
        }
        // Exit without invoking the callback.
        return;
      }
    } else {
      // There's still time left in this idle period.
      frameDeadlineObject.didTimeout = false;
    }

    timeoutTime = -1;
    var callback = scheduledRICCallback;
    scheduledRICCallback = null;
    if (callback !== null) {
      callback(frameDeadlineObject);
    }
  };
  // Assumes that we have addEventListener in this environment. Might need
  // something better for old IE.
  window.addEventListener('message', idleTick, false);

  var animationTick = function (rafTime) {
    isAnimationFrameScheduled = false;
    var nextFrameTime = rafTime - frameDeadline + activeFrameTime;
    if (nextFrameTime < activeFrameTime && previousFrameTime < activeFrameTime) {
      if (nextFrameTime < 8) {
        // Defensive coding. We don't support higher frame rates than 120hz.
        // If we get lower than that, it is probably a bug.
        nextFrameTime = 8;
      }
      // If one frame goes long, then the next one can be short to catch up.
      // If two frames are short in a row, then that's an indication that we
      // actually have a higher frame rate than what we're currently optimizing.
      // We adjust our heuristic dynamically accordingly. For example, if we're
      // running on 120hz display or 90hz VR display.
      // Take the max of the two in case one of them was an anomaly due to
      // missed frame deadlines.
      activeFrameTime = nextFrameTime < previousFrameTime ? previousFrameTime : nextFrameTime;
    } else {
      previousFrameTime = nextFrameTime;
    }
    frameDeadline = rafTime + activeFrameTime;
    if (!isIdleScheduled) {
      isIdleScheduled = true;
      window.postMessage(messageKey, '*');
    }
  };

  rIC = function (callback, options) {
    // This assumes that we only schedule one callback at a time because that's
    // how Fiber uses it.
    scheduledRICCallback = callback;
    if (options != null && typeof options.timeout === 'number') {
      timeoutTime = now() + options.timeout;
    }
    if (!isAnimationFrameScheduled) {
      // If rAF didn't already schedule one, we need to schedule a frame.
      // TODO: If this rAF doesn't materialize because the browser throttles, we
      // might want to still have setTimeout trigger rIC as a backup to ensure
      // that we keep performing work.
      isAnimationFrameScheduled = true;
      requestAnimationFrame(animationTick);
    }
    return 0;
  };

  cIC = function () {
    scheduledRICCallback = null;
    isIdleScheduled = false;
    timeoutTime = -1;
  };
} else {
  rIC = window.requestIdleCallback;
  cIC = window.cancelIdleCallback;
}

var didWarnSelectedSetOnOption = false;

function flattenChildren(children) {
  var content = '';

  // Flatten children and warn if they aren't strings or numbers;
  // invalid types are ignored.
  // We can silently skip them because invalid DOM nesting warning
  // catches these cases in Fiber.
  React.Children.forEach(children, function (child) {
    if (child == null) {
      return;
    }
    if (typeof child === 'string' || typeof child === 'number') {
      content += child;
    }
  });

  return content;
}

/**
 * Implements an <option> host component that warns when `selected` is set.
 */

function validateProps(element, props) {
  // TODO (yungsters): Remove support for `selected` in <option>.
  {
    if (props.selected != null && !didWarnSelectedSetOnOption) {
      warning(false, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.');
      didWarnSelectedSetOnOption = true;
    }
  }
}

function postMountWrapper$1(element, props) {
  // value="" should make a value attribute (#6219)
  if (props.value != null) {
    element.setAttribute('value', props.value);
  }
}

function getHostProps$1(element, props) {
  var hostProps = _assign({ children: undefined }, props);
  var content = flattenChildren(props.children);

  if (content) {
    hostProps.children = content;
  }

  return hostProps;
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$3 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$4 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


var didWarnValueDefaultValue$1 = void 0;

{
  didWarnValueDefaultValue$1 = false;
}

function getDeclarationErrorAddendum() {
  var ownerName = getCurrentFiberOwnerName$3();
  if (ownerName) {
    return '\n\nCheck the render method of `' + ownerName + '`.';
  }
  return '';
}

var valuePropNames = ['value', 'defaultValue'];

/**
 * Validation function for `value` and `defaultValue`.
 */
function checkSelectPropTypes(props) {
  ReactControlledValuePropTypes.checkPropTypes('select', props, getCurrentFiberStackAddendum$4);

  for (var i = 0; i < valuePropNames.length; i++) {
    var propName = valuePropNames[i];
    if (props[propName] == null) {
      continue;
    }
    var isArray = Array.isArray(props[propName]);
    if (props.multiple && !isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum());
    } else if (!props.multiple && isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum());
    }
  }
}

function updateOptions(node, multiple, propValue, setDefaultSelected) {
  var options = node.options;

  if (multiple) {
    var selectedValues = propValue;
    var selectedValue = {};
    for (var i = 0; i < selectedValues.length; i++) {
      // Prefix to avoid chaos with special keys.
      selectedValue['$' + selectedValues[i]] = true;
    }
    for (var _i = 0; _i < options.length; _i++) {
      var selected = selectedValue.hasOwnProperty('$' + options[_i].value);
      if (options[_i].selected !== selected) {
        options[_i].selected = selected;
      }
      if (selected && setDefaultSelected) {
        options[_i].defaultSelected = true;
      }
    }
  } else {
    // Do not set `select.value` as exact behavior isn't consistent across all
    // browsers for all cases.
    var _selectedValue = '' + propValue;
    var defaultSelected = null;
    for (var _i2 = 0; _i2 < options.length; _i2++) {
      if (options[_i2].value === _selectedValue) {
        options[_i2].selected = true;
        if (setDefaultSelected) {
          options[_i2].defaultSelected = true;
        }
        return;
      }
      if (defaultSelected === null && !options[_i2].disabled) {
        defaultSelected = options[_i2];
      }
    }
    if (defaultSelected !== null) {
      defaultSelected.selected = true;
    }
  }
}

/**
 * Implements a <select> host component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */

function getHostProps$2(element, props) {
  return _assign({}, props, {
    value: undefined
  });
}

function initWrapperState$1(element, props) {
  var node = element;
  {
    checkSelectPropTypes(props);
  }

  var value = props.value;
  node._wrapperState = {
    initialValue: value != null ? value : props.defaultValue,
    wasMultiple: !!props.multiple
  };

  {
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
      warning(false, 'Select elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled select ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValueDefaultValue$1 = true;
    }
  }
}

function postMountWrapper$2(element, props) {
  var node = element;
  node.multiple = !!props.multiple;
  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (props.defaultValue != null) {
    updateOptions(node, !!props.multiple, props.defaultValue, true);
  }
}

function postUpdateWrapper(element, props) {
  var node = element;
  // After the initial mount, we control selected-ness manually so don't pass
  // this value down
  node._wrapperState.initialValue = undefined;

  var wasMultiple = node._wrapperState.wasMultiple;
  node._wrapperState.wasMultiple = !!props.multiple;

  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (wasMultiple !== !!props.multiple) {
    // For simplicity, reapply `defaultValue` if `multiple` is toggled.
    if (props.defaultValue != null) {
      updateOptions(node, !!props.multiple, props.defaultValue, true);
    } else {
      // Revert the select back to its default unselected state.
      updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
    }
  }
}

function restoreControlledState$2(element, props) {
  var node = element;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  }
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$5 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValDefaultVal = false;

/**
 * Implements a <textarea> host component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */

function getHostProps$3(element, props) {
  var node = element;
  !(props.dangerouslySetInnerHTML == null) ? invariant(false, '`dangerouslySetInnerHTML` does not make sense on <textarea>.') : void 0;

  // Always set children to the same thing. In IE9, the selection range will
  // get reset if `textContent` is mutated.  We could add a check in setTextContent
  // to only set the value if/when the value differs from the node value (which would
  // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
  // solution. The value can be a boolean or object so that's why it's forced
  // to be a string.
  var hostProps = _assign({}, props, {
    value: undefined,
    defaultValue: undefined,
    children: '' + node._wrapperState.initialValue
  });

  return hostProps;
}

function initWrapperState$2(element, props) {
  var node = element;
  {
    ReactControlledValuePropTypes.checkPropTypes('textarea', props, getCurrentFiberStackAddendum$5);
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
      warning(false, 'Textarea elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled textarea ' + 'and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValDefaultVal = true;
    }
  }

  var initialValue = props.value;

  // Only bother fetching default value if we're going to use it
  if (initialValue == null) {
    var defaultValue = props.defaultValue;
    // TODO (yungsters): Remove support for children content in <textarea>.
    var children = props.children;
    if (children != null) {
      {
        warning(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.');
      }
      !(defaultValue == null) ? invariant(false, 'If you supply `defaultValue` on a <textarea>, do not pass children.') : void 0;
      if (Array.isArray(children)) {
        !(children.length <= 1) ? invariant(false, '<textarea> can only have at most one child.') : void 0;
        children = children[0];
      }

      defaultValue = '' + children;
    }
    if (defaultValue == null) {
      defaultValue = '';
    }
    initialValue = defaultValue;
  }

  node._wrapperState = {
    initialValue: '' + initialValue
  };
}

function updateWrapper$1(element, props) {
  var node = element;
  var value = props.value;
  if (value != null) {
    // Cast `value` to a string to ensure the value is set correctly. While
    // browsers typically do this as necessary, jsdom doesn't.
    var newValue = '' + value;

    // To avoid side effects (such as losing text selection), only set value if changed
    if (newValue !== node.value) {
      node.value = newValue;
    }
    if (props.defaultValue == null) {
      node.defaultValue = newValue;
    }
  }
  if (props.defaultValue != null) {
    node.defaultValue = props.defaultValue;
  }
}

function postMountWrapper$3(element, props) {
  var node = element;
  // This is in postMount because we need access to the DOM node, which is not
  // available until after the component has mounted.
  var textContent = node.textContent;

  // Only set node.value if textContent is equal to the expected
  // initial value. In IE10/IE11 there is a bug where the placeholder attribute
  // will populate textContent as well.
  // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/
  if (textContent === node._wrapperState.initialValue) {
    node.value = textContent;
  }
}

function restoreControlledState$3(element, props) {
  // DOM component is still mounted; update
  updateWrapper$1(element, props);
}

var HTML_NAMESPACE$1 = 'http://www.w3.org/1999/xhtml';
var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

var Namespaces = {
  html: HTML_NAMESPACE$1,
  mathml: MATH_NAMESPACE,
  svg: SVG_NAMESPACE
};

// Assumes there is no parent namespace.
function getIntrinsicNamespace(type) {
  switch (type) {
    case 'svg':
      return SVG_NAMESPACE;
    case 'math':
      return MATH_NAMESPACE;
    default:
      return HTML_NAMESPACE$1;
  }
}

function getChildNamespace(parentNamespace, type) {
  if (parentNamespace == null || parentNamespace === HTML_NAMESPACE$1) {
    // No (or default) parent namespace: potential entry point.
    return getIntrinsicNamespace(type);
  }
  if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') {
    // We're leaving SVG.
    return HTML_NAMESPACE$1;
  }
  // By default, pass namespace below.
  return parentNamespace;
}

/* globals MSApp */

/**
 * Create a function which has 'unsafe' privileges (required by windows8 apps)
 */
var createMicrosoftUnsafeLocalFunction = function (func) {
  if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
    return function (arg0, arg1, arg2, arg3) {
      MSApp.execUnsafeLocalFunction(function () {
        return func(arg0, arg1, arg2, arg3);
      });
    };
  } else {
    return func;
  }
};

// SVG temp container for IE lacking innerHTML
var reusableSVGContainer = void 0;

/**
 * Set the innerHTML property of a node
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */
var setInnerHTML = createMicrosoftUnsafeLocalFunction(function (node, html) {
  // IE does not have innerHTML for SVG nodes, so instead we inject the
  // new markup in a temp node and then move the child nodes across into
  // the target node

  if (node.namespaceURI === Namespaces.svg && !('innerHTML' in node)) {
    reusableSVGContainer = reusableSVGContainer || document.createElement('div');
    reusableSVGContainer.innerHTML = '<svg>' + html + '</svg>';
    var svgNode = reusableSVGContainer.firstChild;
    while (node.firstChild) {
      node.removeChild(node.firstChild);
    }
    while (svgNode.firstChild) {
      node.appendChild(svgNode.firstChild);
    }
  } else {
    node.innerHTML = html;
  }
});

/**
 * Set the textContent property of a node. For text updates, it's faster
 * to set the `nodeValue` of the Text node directly instead of using
 * `.textContent` which will remove the existing node and create a new one.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */
var setTextContent = function (node, text) {
  if (text) {
    var firstChild = node.firstChild;

    if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
      firstChild.nodeValue = text;
      return;
    }
  }
  node.textContent = text;
};

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,

  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};

/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}

/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */
function dangerousStyleValue(name, value, isCustomProperty) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901

  var isEmpty = value == null || typeof value === 'boolean' || value === '';
  if (isEmpty) {
    return '';
  }

  if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
    return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
  }

  return ('' + value).trim();
}

var warnValidStyle = emptyFunction;

{
  // 'msTransform' is correct, but the other prefixes should be capitalized
  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

  // style values shouldn't contain a semicolon
  var badStyleValueWithSemicolonPattern = /;\s*$/;

  var warnedStyleNames = {};
  var warnedStyleValues = {};
  var warnedForNaNValue = false;
  var warnedForInfinityValue = false;

  var warnHyphenatedStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported style property %s. Did you mean %s?%s', name, camelizeStyleName(name), getStack());
  };

  var warnBadVendoredStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?%s', name, name.charAt(0).toUpperCase() + name.slice(1), getStack());
  };

  var warnStyleValueWithSemicolon = function (name, value, getStack) {
    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
      return;
    }

    warnedStyleValues[value] = true;
    warning(false, "Style property values shouldn't contain a semicolon. " + 'Try "%s: %s" instead.%s', name, value.replace(badStyleValueWithSemicolonPattern, ''), getStack());
  };

  var warnStyleValueIsNaN = function (name, value, getStack) {
    if (warnedForNaNValue) {
      return;
    }

    warnedForNaNValue = true;
    warning(false, '`NaN` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  var warnStyleValueIsInfinity = function (name, value, getStack) {
    if (warnedForInfinityValue) {
      return;
    }

    warnedForInfinityValue = true;
    warning(false, '`Infinity` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  warnValidStyle = function (name, value, getStack) {
    if (name.indexOf('-') > -1) {
      warnHyphenatedStyleName(name, getStack);
    } else if (badVendoredStyleNamePattern.test(name)) {
      warnBadVendoredStyleName(name, getStack);
    } else if (badStyleValueWithSemicolonPattern.test(value)) {
      warnStyleValueWithSemicolon(name, value, getStack);
    }

    if (typeof value === 'number') {
      if (isNaN(value)) {
        warnStyleValueIsNaN(name, value, getStack);
      } else if (!isFinite(value)) {
        warnStyleValueIsInfinity(name, value, getStack);
      }
    }
  };
}

var warnValidStyle$1 = warnValidStyle;

/**
 * Operations for dealing with CSS properties.
 */

/**
 * This creates a string that is expected to be equivalent to the style
 * attribute generated by server-side rendering. It by-passes warnings and
 * security checks so it's not safe to use this value for anything other than
 * comparison. It is only used in DEV for SSR validation.
 */
function createDangerousStringForStyles(styles) {
  {
    var serialized = '';
    var delimiter = '';
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = styles[styleName];
      if (styleValue != null) {
        var isCustomProperty = styleName.indexOf('--') === 0;
        serialized += delimiter + hyphenateStyleName(styleName) + ':';
        serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);

        delimiter = ';';
      }
    }
    return serialized || null;
  }
}

/**
 * Sets the value for multiple styles on a node.  If a value is specified as
 * '' (empty string), the corresponding style property will be unset.
 *
 * @param {DOMElement} node
 * @param {object} styles
 */
function setValueForStyles(node, styles, getStack) {
  var style = node.style;
  for (var styleName in styles) {
    if (!styles.hasOwnProperty(styleName)) {
      continue;
    }
    var isCustomProperty = styleName.indexOf('--') === 0;
    {
      if (!isCustomProperty) {
        warnValidStyle$1(styleName, styles[styleName], getStack);
      }
    }
    var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
    if (styleName === 'float') {
      styleName = 'cssFloat';
    }
    if (isCustomProperty) {
      style.setProperty(styleName, styleValue);
    } else {
      style[styleName] = styleValue;
    }
  }
}

// For HTML, certain tags should omit their close tag. We keep a whitelist for
// those special-case tags.

var omittedCloseTags = {
  area: true,
  base: true,
  br: true,
  col: true,
  embed: true,
  hr: true,
  img: true,
  input: true,
  keygen: true,
  link: true,
  meta: true,
  param: true,
  source: true,
  track: true,
  wbr: true
};

// For HTML, certain tags cannot have children. This has the same purpose as
// `omittedCloseTags` except that `menuitem` should still have its closing tag.

var voidElementTags = _assign({
  menuitem: true
}, omittedCloseTags);

var HTML$1 = '__html';

function assertValidProps(tag, props, getStack) {
  if (!props) {
    return;
  }
  // Note the use of `==` which checks for null or undefined.
  if (voidElementTags[tag]) {
    !(props.children == null && props.dangerouslySetInnerHTML == null) ? invariant(false, '%s is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.%s', tag, getStack()) : void 0;
  }
  if (props.dangerouslySetInnerHTML != null) {
    !(props.children == null) ? invariant(false, 'Can only set one of `children` or `props.dangerouslySetInnerHTML`.') : void 0;
    !(typeof props.dangerouslySetInnerHTML === 'object' && HTML$1 in props.dangerouslySetInnerHTML) ? invariant(false, '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://fb.me/react-invariant-dangerously-set-inner-html for more information.') : void 0;
  }
  {
    !(props.suppressContentEditableWarning || !props.contentEditable || props.children == null) ? warning(false, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.%s', getStack()) : void 0;
  }
  !(props.style == null || typeof props.style === 'object') ? invariant(false, 'The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + \'em\'}} when using JSX.%s', getStack()) : void 0;
}

function isCustomComponent(tagName, props) {
  if (tagName.indexOf('-') === -1) {
    return typeof props.is === 'string';
  }
  switch (tagName) {
    // These are reserved SVG and MathML elements.
    // We don't mind this whitelist too much because we expect it to never grow.
    // The alternative is to track the namespace in a few places which is convoluted.
    // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
    case 'annotation-xml':
    case 'color-profile':
    case 'font-face':
    case 'font-face-src':
    case 'font-face-uri':
    case 'font-face-format':
    case 'font-face-name':
    case 'missing-glyph':
      return false;
    default:
      return true;
  }
}

// When adding attributes to the HTML or SVG whitelist, be sure to
// also add them to this module to ensure casing and incorrect name
// warnings.
var possibleStandardNames = {
  // HTML
  accept: 'accept',
  acceptcharset: 'acceptCharset',
  'accept-charset': 'acceptCharset',
  accesskey: 'accessKey',
  action: 'action',
  allowfullscreen: 'allowFullScreen',
  alt: 'alt',
  as: 'as',
  async: 'async',
  autocapitalize: 'autoCapitalize',
  autocomplete: 'autoComplete',
  autocorrect: 'autoCorrect',
  autofocus: 'autoFocus',
  autoplay: 'autoPlay',
  autosave: 'autoSave',
  capture: 'capture',
  cellpadding: 'cellPadding',
  cellspacing: 'cellSpacing',
  challenge: 'challenge',
  charset: 'charSet',
  checked: 'checked',
  children: 'children',
  cite: 'cite',
  'class': 'className',
  classid: 'classID',
  classname: 'className',
  cols: 'cols',
  colspan: 'colSpan',
  content: 'content',
  contenteditable: 'contentEditable',
  contextmenu: 'contextMenu',
  controls: 'controls',
  controlslist: 'controlsList',
  coords: 'coords',
  crossorigin: 'crossOrigin',
  dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
  data: 'data',
  datetime: 'dateTime',
  'default': 'default',
  defaultchecked: 'defaultChecked',
  defaultvalue: 'defaultValue',
  defer: 'defer',
  dir: 'dir',
  disabled: 'disabled',
  download: 'download',
  draggable: 'draggable',
  enctype: 'encType',
  'for': 'htmlFor',
  form: 'form',
  formmethod: 'formMethod',
  formaction: 'formAction',
  formenctype: 'formEncType',
  formnovalidate: 'formNoValidate',
  formtarget: 'formTarget',
  frameborder: 'frameBorder',
  headers: 'headers',
  height: 'height',
  hidden: 'hidden',
  high: 'high',
  href: 'href',
  hreflang: 'hrefLang',
  htmlfor: 'htmlFor',
  httpequiv: 'httpEquiv',
  'http-equiv': 'httpEquiv',
  icon: 'icon',
  id: 'id',
  innerhtml: 'innerHTML',
  inputmode: 'inputMode',
  integrity: 'integrity',
  is: 'is',
  itemid: 'itemID',
  itemprop: 'itemProp',
  itemref: 'itemRef',
  itemscope: 'itemScope',
  itemtype: 'itemType',
  keyparams: 'keyParams',
  keytype: 'keyType',
  kind: 'kind',
  label: 'label',
  lang: 'lang',
  list: 'list',
  loop: 'loop',
  low: 'low',
  manifest: 'manifest',
  marginwidth: 'marginWidth',
  marginheight: 'marginHeight',
  max: 'max',
  maxlength: 'maxLength',
  media: 'media',
  mediagroup: 'mediaGroup',
  method: 'method',
  min: 'min',
  minlength: 'minLength',
  multiple: 'multiple',
  muted: 'muted',
  name: 'name',
  nomodule: 'noModule',
  nonce: 'nonce',
  novalidate: 'noValidate',
  open: 'open',
  optimum: 'optimum',
  pattern: 'pattern',
  placeholder: 'placeholder',
  playsinline: 'playsInline',
  poster: 'poster',
  preload: 'preload',
  profile: 'profile',
  radiogroup: 'radioGroup',
  readonly: 'readOnly',
  referrerpolicy: 'referrerPolicy',
  rel: 'rel',
  required: 'required',
  reversed: 'reversed',
  role: 'role',
  rows: 'rows',
  rowspan: 'rowSpan',
  sandbox: 'sandbox',
  scope: 'scope',
  scoped: 'scoped',
  scrolling: 'scrolling',
  seamless: 'seamless',
  selected: 'selected',
  shape: 'shape',
  size: 'size',
  sizes: 'sizes',
  span: 'span',
  spellcheck: 'spellCheck',
  src: 'src',
  srcdoc: 'srcDoc',
  srclang: 'srcLang',
  srcset: 'srcSet',
  start: 'start',
  step: 'step',
  style: 'style',
  summary: 'summary',
  tabindex: 'tabIndex',
  target: 'target',
  title: 'title',
  type: 'type',
  usemap: 'useMap',
  value: 'value',
  width: 'width',
  wmode: 'wmode',
  wrap: 'wrap',

  // SVG
  about: 'about',
  accentheight: 'accentHeight',
  'accent-height': 'accentHeight',
  accumulate: 'accumulate',
  additive: 'additive',
  alignmentbaseline: 'alignmentBaseline',
  'alignment-baseline': 'alignmentBaseline',
  allowreorder: 'allowReorder',
  alphabetic: 'alphabetic',
  amplitude: 'amplitude',
  arabicform: 'arabicForm',
  'arabic-form': 'arabicForm',
  ascent: 'ascent',
  attributename: 'attributeName',
  attributetype: 'attributeType',
  autoreverse: 'autoReverse',
  azimuth: 'azimuth',
  basefrequency: 'baseFrequency',
  baselineshift: 'baselineShift',
  'baseline-shift': 'baselineShift',
  baseprofile: 'baseProfile',
  bbox: 'bbox',
  begin: 'begin',
  bias: 'bias',
  by: 'by',
  calcmode: 'calcMode',
  capheight: 'capHeight',
  'cap-height': 'capHeight',
  clip: 'clip',
  clippath: 'clipPath',
  'clip-path': 'clipPath',
  clippathunits: 'clipPathUnits',
  cliprule: 'clipRule',
  'clip-rule': 'clipRule',
  color: 'color',
  colorinterpolation: 'colorInterpolation',
  'color-interpolation': 'colorInterpolation',
  colorinterpolationfilters: 'colorInterpolationFilters',
  'color-interpolation-filters': 'colorInterpolationFilters',
  colorprofile: 'colorProfile',
  'color-profile': 'colorProfile',
  colorrendering: 'colorRendering',
  'color-rendering': 'colorRendering',
  contentscripttype: 'contentScriptType',
  contentstyletype: 'contentStyleType',
  cursor: 'cursor',
  cx: 'cx',
  cy: 'cy',
  d: 'd',
  datatype: 'datatype',
  decelerate: 'decelerate',
  descent: 'descent',
  diffuseconstant: 'diffuseConstant',
  direction: 'direction',
  display: 'display',
  divisor: 'divisor',
  dominantbaseline: 'dominantBaseline',
  'dominant-baseline': 'dominantBaseline',
  dur: 'dur',
  dx: 'dx',
  dy: 'dy',
  edgemode: 'edgeMode',
  elevation: 'elevation',
  enablebackground: 'enableBackground',
  'enable-background': 'enableBackground',
  end: 'end',
  exponent: 'exponent',
  externalresourcesrequired: 'externalResourcesRequired',
  fill: 'fill',
  fillopacity: 'fillOpacity',
  'fill-opacity': 'fillOpacity',
  fillrule: 'fillRule',
  'fill-rule': 'fillRule',
  filter: 'filter',
  filterres: 'filterRes',
  filterunits: 'filterUnits',
  floodopacity: 'floodOpacity',
  'flood-opacity': 'floodOpacity',
  floodcolor: 'floodColor',
  'flood-color': 'floodColor',
  focusable: 'focusable',
  fontfamily: 'fontFamily',
  'font-family': 'fontFamily',
  fontsize: 'fontSize',
  'font-size': 'fontSize',
  fontsizeadjust: 'fontSizeAdjust',
  'font-size-adjust': 'fontSizeAdjust',
  fontstretch: 'fontStretch',
  'font-stretch': 'fontStretch',
  fontstyle: 'fontStyle',
  'font-style': 'fontStyle',
  fontvariant: 'fontVariant',
  'font-variant': 'fontVariant',
  fontweight: 'fontWeight',
  'font-weight': 'fontWeight',
  format: 'format',
  from: 'from',
  fx: 'fx',
  fy: 'fy',
  g1: 'g1',
  g2: 'g2',
  glyphname: 'glyphName',
  'glyph-name': 'glyphName',
  glyphorientationhorizontal: 'glyphOrientationHorizontal',
  'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
  glyphorientationvertical: 'glyphOrientationVertical',
  'glyph-orientation-vertical': 'glyphOrientationVertical',
  glyphref: 'glyphRef',
  gradienttransform: 'gradientTransform',
  gradientunits: 'gradientUnits',
  hanging: 'hanging',
  horizadvx: 'horizAdvX',
  'horiz-adv-x': 'horizAdvX',
  horizoriginx: 'horizOriginX',
  'horiz-origin-x': 'horizOriginX',
  ideographic: 'ideographic',
  imagerendering: 'imageRendering',
  'image-rendering': 'imageRendering',
  in2: 'in2',
  'in': 'in',
  inlist: 'inlist',
  intercept: 'intercept',
  k1: 'k1',
  k2: 'k2',
  k3: 'k3',
  k4: 'k4',
  k: 'k',
  kernelmatrix: 'kernelMatrix',
  kernelunitlength: 'kernelUnitLength',
  kerning: 'kerning',
  keypoints: 'keyPoints',
  keysplines: 'keySplines',
  keytimes: 'keyTimes',
  lengthadjust: 'lengthAdjust',
  letterspacing: 'letterSpacing',
  'letter-spacing': 'letterSpacing',
  lightingcolor: 'lightingColor',
  'lighting-color': 'lightingColor',
  limitingconeangle: 'limitingConeAngle',
  local: 'local',
  markerend: 'markerEnd',
  'marker-end': 'markerEnd',
  markerheight: 'markerHeight',
  markermid: 'markerMid',
  'marker-mid': 'markerMid',
  markerstart: 'markerStart',
  'marker-start': 'markerStart',
  markerunits: 'markerUnits',
  markerwidth: 'markerWidth',
  mask: 'mask',
  maskcontentunits: 'maskContentUnits',
  maskunits: 'maskUnits',
  mathematical: 'mathematical',
  mode: 'mode',
  numoctaves: 'numOctaves',
  offset: 'offset',
  opacity: 'opacity',
  operator: 'operator',
  order: 'order',
  orient: 'orient',
  orientation: 'orientation',
  origin: 'origin',
  overflow: 'overflow',
  overlineposition: 'overlinePosition',
  'overline-position': 'overlinePosition',
  overlinethickness: 'overlineThickness',
  'overline-thickness': 'overlineThickness',
  paintorder: 'paintOrder',
  'paint-order': 'paintOrder',
  panose1: 'panose1',
  'panose-1': 'panose1',
  pathlength: 'pathLength',
  patterncontentunits: 'patternContentUnits',
  patterntransform: 'patternTransform',
  patternunits: 'patternUnits',
  pointerevents: 'pointerEvents',
  'pointer-events': 'pointerEvents',
  points: 'points',
  pointsatx: 'pointsAtX',
  pointsaty: 'pointsAtY',
  pointsatz: 'pointsAtZ',
  prefix: 'prefix',
  preservealpha: 'preserveAlpha',
  preserveaspectratio: 'preserveAspectRatio',
  primitiveunits: 'primitiveUnits',
  property: 'property',
  r: 'r',
  radius: 'radius',
  refx: 'refX',
  refy: 'refY',
  renderingintent: 'renderingIntent',
  'rendering-intent': 'renderingIntent',
  repeatcount: 'repeatCount',
  repeatdur: 'repeatDur',
  requiredextensions: 'requiredExtensions',
  requiredfeatures: 'requiredFeatures',
  resource: 'resource',
  restart: 'restart',
  result: 'result',
  results: 'results',
  rotate: 'rotate',
  rx: 'rx',
  ry: 'ry',
  scale: 'scale',
  security: 'security',
  seed: 'seed',
  shaperendering: 'shapeRendering',
  'shape-rendering': 'shapeRendering',
  slope: 'slope',
  spacing: 'spacing',
  specularconstant: 'specularConstant',
  specularexponent: 'specularExponent',
  speed: 'speed',
  spreadmethod: 'spreadMethod',
  startoffset: 'startOffset',
  stddeviation: 'stdDeviation',
  stemh: 'stemh',
  stemv: 'stemv',
  stitchtiles: 'stitchTiles',
  stopcolor: 'stopColor',
  'stop-color': 'stopColor',
  stopopacity: 'stopOpacity',
  'stop-opacity': 'stopOpacity',
  strikethroughposition: 'strikethroughPosition',
  'strikethrough-position': 'strikethroughPosition',
  strikethroughthickness: 'strikethroughThickness',
  'strikethrough-thickness': 'strikethroughThickness',
  string: 'string',
  stroke: 'stroke',
  strokedasharray: 'strokeDasharray',
  'stroke-dasharray': 'strokeDasharray',
  strokedashoffset: 'strokeDashoffset',
  'stroke-dashoffset': 'strokeDashoffset',
  strokelinecap: 'strokeLinecap',
  'stroke-linecap': 'strokeLinecap',
  strokelinejoin: 'strokeLinejoin',
  'stroke-linejoin': 'strokeLinejoin',
  strokemiterlimit: 'strokeMiterlimit',
  'stroke-miterlimit': 'strokeMiterlimit',
  strokewidth: 'strokeWidth',
  'stroke-width': 'strokeWidth',
  strokeopacity: 'strokeOpacity',
  'stroke-opacity': 'strokeOpacity',
  suppresscontenteditablewarning: 'suppressContentEditableWarning',
  suppresshydrationwarning: 'suppressHydrationWarning',
  surfacescale: 'surfaceScale',
  systemlanguage: 'systemLanguage',
  tablevalues: 'tableValues',
  targetx: 'targetX',
  targety: 'targetY',
  textanchor: 'textAnchor',
  'text-anchor': 'textAnchor',
  textdecoration: 'textDecoration',
  'text-decoration': 'textDecoration',
  textlength: 'textLength',
  textrendering: 'textRendering',
  'text-rendering': 'textRendering',
  to: 'to',
  transform: 'transform',
  'typeof': 'typeof',
  u1: 'u1',
  u2: 'u2',
  underlineposition: 'underlinePosition',
  'underline-position': 'underlinePosition',
  underlinethickness: 'underlineThickness',
  'underline-thickness': 'underlineThickness',
  unicode: 'unicode',
  unicodebidi: 'unicodeBidi',
  'unicode-bidi': 'unicodeBidi',
  unicoderange: 'unicodeRange',
  'unicode-range': 'unicodeRange',
  unitsperem: 'unitsPerEm',
  'units-per-em': 'unitsPerEm',
  unselectable: 'unselectable',
  valphabetic: 'vAlphabetic',
  'v-alphabetic': 'vAlphabetic',
  values: 'values',
  vectoreffect: 'vectorEffect',
  'vector-effect': 'vectorEffect',
  version: 'version',
  vertadvy: 'vertAdvY',
  'vert-adv-y': 'vertAdvY',
  vertoriginx: 'vertOriginX',
  'vert-origin-x': 'vertOriginX',
  vertoriginy: 'vertOriginY',
  'vert-origin-y': 'vertOriginY',
  vhanging: 'vHanging',
  'v-hanging': 'vHanging',
  videographic: 'vIdeographic',
  'v-ideographic': 'vIdeographic',
  viewbox: 'viewBox',
  viewtarget: 'viewTarget',
  visibility: 'visibility',
  vmathematical: 'vMathematical',
  'v-mathematical': 'vMathematical',
  vocab: 'vocab',
  widths: 'widths',
  wordspacing: 'wordSpacing',
  'word-spacing': 'wordSpacing',
  writingmode: 'writingMode',
  'writing-mode': 'writingMode',
  x1: 'x1',
  x2: 'x2',
  x: 'x',
  xchannelselector: 'xChannelSelector',
  xheight: 'xHeight',
  'x-height': 'xHeight',
  xlinkactuate: 'xlinkActuate',
  'xlink:actuate': 'xlinkActuate',
  xlinkarcrole: 'xlinkArcrole',
  'xlink:arcrole': 'xlinkArcrole',
  xlinkhref: 'xlinkHref',
  'xlink:href': 'xlinkHref',
  xlinkrole: 'xlinkRole',
  'xlink:role': 'xlinkRole',
  xlinkshow: 'xlinkShow',
  'xlink:show': 'xlinkShow',
  xlinktitle: 'xlinkTitle',
  'xlink:title': 'xlinkTitle',
  xlinktype: 'xlinkType',
  'xlink:type': 'xlinkType',
  xmlbase: 'xmlBase',
  'xml:base': 'xmlBase',
  xmllang: 'xmlLang',
  'xml:lang': 'xmlLang',
  xmlns: 'xmlns',
  'xml:space': 'xmlSpace',
  xmlnsxlink: 'xmlnsXlink',
  'xmlns:xlink': 'xmlnsXlink',
  xmlspace: 'xmlSpace',
  y1: 'y1',
  y2: 'y2',
  y: 'y',
  ychannelselector: 'yChannelSelector',
  z: 'z',
  zoomandpan: 'zoomAndPan'
};

var ariaProperties = {
  'aria-current': 0, // state
  'aria-details': 0,
  'aria-disabled': 0, // state
  'aria-hidden': 0, // state
  'aria-invalid': 0, // state
  'aria-keyshortcuts': 0,
  'aria-label': 0,
  'aria-roledescription': 0,
  // Widget Attributes
  'aria-autocomplete': 0,
  'aria-checked': 0,
  'aria-expanded': 0,
  'aria-haspopup': 0,
  'aria-level': 0,
  'aria-modal': 0,
  'aria-multiline': 0,
  'aria-multiselectable': 0,
  'aria-orientation': 0,
  'aria-placeholder': 0,
  'aria-pressed': 0,
  'aria-readonly': 0,
  'aria-required': 0,
  'aria-selected': 0,
  'aria-sort': 0,
  'aria-valuemax': 0,
  'aria-valuemin': 0,
  'aria-valuenow': 0,
  'aria-valuetext': 0,
  // Live Region Attributes
  'aria-atomic': 0,
  'aria-busy': 0,
  'aria-live': 0,
  'aria-relevant': 0,
  // Drag-and-Drop Attributes
  'aria-dropeffect': 0,
  'aria-grabbed': 0,
  // Relationship Attributes
  'aria-activedescendant': 0,
  'aria-colcount': 0,
  'aria-colindex': 0,
  'aria-colspan': 0,
  'aria-controls': 0,
  'aria-describedby': 0,
  'aria-errormessage': 0,
  'aria-flowto': 0,
  'aria-labelledby': 0,
  'aria-owns': 0,
  'aria-posinset': 0,
  'aria-rowcount': 0,
  'aria-rowindex': 0,
  'aria-rowspan': 0,
  'aria-setsize': 0
};

var warnedProperties = {};
var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

var hasOwnProperty = Object.prototype.hasOwnProperty;

function getStackAddendum() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperty(tagName, name) {
  if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) {
    return true;
  }

  if (rARIACamel.test(name)) {
    var ariaName = 'aria-' + name.slice(4).toLowerCase();
    var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (correctName == null) {
      warning(false, 'Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.%s', name, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== correctName) {
      warning(false, 'Invalid ARIA attribute `%s`. Did you mean `%s`?%s', name, correctName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  if (rARIA.test(name)) {
    var lowerCasedName = name.toLowerCase();
    var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (standardName == null) {
      warnedProperties[name] = true;
      return false;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== standardName) {
      warning(false, 'Unknown ARIA attribute `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  return true;
}

function warnInvalidARIAProps(type, props) {
  var invalidProps = [];

  for (var key in props) {
    var isValid = validateProperty(type, key);
    if (!isValid) {
      invalidProps.push(key);
    }
  }

  var unknownPropString = invalidProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');

  if (invalidProps.length === 1) {
    warning(false, 'Invalid aria prop %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  } else if (invalidProps.length > 1) {
    warning(false, 'Invalid aria props %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  }
}

function validateProperties(type, props) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnInvalidARIAProps(type, props);
}

var didWarnValueNull = false;

function getStackAddendum$1() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperties$1(type, props) {
  if (type !== 'input' && type !== 'textarea' && type !== 'select') {
    return;
  }

  if (props != null && props.value === null && !didWarnValueNull) {
    didWarnValueNull = true;
    if (type === 'select' && props.multiple) {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty array when `multiple` is set to `true` ' + 'to clear the component or `undefined` for uncontrolled components.%s', type, getStackAddendum$1());
    } else {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty string to clear the component or `undefined` ' + 'for uncontrolled components.%s', type, getStackAddendum$1());
    }
  }
}

function getStackAddendum$2() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

var validateProperty$1 = function () {};

{
  var warnedProperties$1 = {};
  var _hasOwnProperty = Object.prototype.hasOwnProperty;
  var EVENT_NAME_REGEX = /^on./;
  var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
  var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
  var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

  validateProperty$1 = function (tagName, name, value, canUseEventSystem) {
    if (_hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) {
      return true;
    }

    var lowerCasedName = name.toLowerCase();
    if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
      warning(false, 'React uses onFocus and onBlur instead of onFocusIn and onFocusOut. ' + 'All React events are normalized to bubble, so onFocusIn and onFocusOut ' + 'are not needed/supported by React.');
      warnedProperties$1[name] = true;
      return true;
    }

    // We can't rely on the event system being injected on the server.
    if (canUseEventSystem) {
      if (registrationNameModules.hasOwnProperty(name)) {
        return true;
      }
      var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;
      if (registrationName != null) {
        warning(false, 'Invalid event handler property `%s`. Did you mean `%s`?%s', name, registrationName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
      if (EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Unknown event handler property `%s`. It will be ignored.%s', name, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (EVENT_NAME_REGEX.test(name)) {
      // If no event plugins have been injected, we are in a server environment.
      // So we can't tell if the event name is correct for sure, but we can filter
      // out known bad ones like `onclick`. We can't suggest a specific replacement though.
      if (INVALID_EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Invalid event handler property `%s`. ' + 'React events use the camelCase naming convention, for example `onClick`.%s', name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Let the ARIA attribute hook validate ARIA attributes
    if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
      return true;
    }

    if (lowerCasedName === 'innerhtml') {
      warning(false, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'aria') {
      warning(false, 'The `aria` attribute is reserved for future use in React. ' + 'Pass individual `aria-` attributes instead.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
      warning(false, 'Received a `%s` for a string attribute `is`. If this is expected, cast ' + 'the value to a string.%s', typeof value, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'number' && isNaN(value)) {
      warning(false, 'Received NaN for the `%s` attribute. If this is expected, cast ' + 'the value to a string.%s', name, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    var propertyInfo = getPropertyInfo(name);
    var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED;

    // Known attributes should match the casing specified in the property config.
    if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      var standardName = possibleStandardNames[lowerCasedName];
      if (standardName !== name) {
        warning(false, 'Invalid DOM property `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (!isReserved && name !== lowerCasedName) {
      // Unknown attributes should have lowercase casing since that's how they
      // will be cased anyway with server rendering.
      warning(false, 'React does not recognize the `%s` prop on a DOM element. If you ' + 'intentionally want it to appear in the DOM as a custom ' + 'attribute, spell it as lowercase `%s` instead. ' + 'If you accidentally passed it from a parent component, remove ' + 'it from the DOM element.%s', name, lowerCasedName, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'boolean' && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
      if (value) {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.%s', value, name, name, value, name, getStackAddendum$2());
      } else {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', value, name, name, value, name, name, name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Now that we've validated casing, do not validate
    // data types for reserved props
    if (isReserved) {
      return true;
    }

    // Warn when a known attribute is a bad type
    if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
      warnedProperties$1[name] = true;
      return false;
    }

    return true;
  };
}

var warnUnknownProperties = function (type, props, canUseEventSystem) {
  var unknownProps = [];
  for (var key in props) {
    var isValid = validateProperty$1(type, key, props[key], canUseEventSystem);
    if (!isValid) {
      unknownProps.push(key);
    }
  }

  var unknownPropString = unknownProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');
  if (unknownProps.length === 1) {
    warning(false, 'Invalid value for prop %s on <%s> tag. Either remove it from the element, ' + 'or pass a string or number value to keep it in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  } else if (unknownProps.length > 1) {
    warning(false, 'Invalid values for props %s on <%s> tag. Either remove them from the element, ' + 'or pass a string or number value to keep them in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  }
};

function validateProperties$2(type, props, canUseEventSystem) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnUnknownProperties(type, props, canUseEventSystem);
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$2 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$3 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnInvalidHydration = false;
var didWarnShadyDOM = false;

var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
var SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
var AUTOFOCUS = 'autoFocus';
var CHILDREN = 'children';
var STYLE = 'style';
var HTML = '__html';

var HTML_NAMESPACE = Namespaces.html;


var getStack = emptyFunction.thatReturns('');

var warnedUnknownTags = void 0;
var suppressHydrationWarning = void 0;

var validatePropertiesInDevelopment = void 0;
var warnForTextDifference = void 0;
var warnForPropDifference = void 0;
var warnForExtraAttributes = void 0;
var warnForInvalidEventListener = void 0;

var normalizeMarkupForTextOrAttribute = void 0;
var normalizeHTML = void 0;

{
  getStack = getCurrentFiberStackAddendum$3;

  warnedUnknownTags = {
    // Chrome is the only major browser not shipping <time>. But as of July
    // 2017 it intends to ship it due to widespread usage. We intentionally
    // *don't* warn for <time> even if it's unrecognized by Chrome because
    // it soon will be, and many apps have been using it anyway.
    time: true,
    // There are working polyfills for <dialog>. Let people use it.
    dialog: true
  };

  validatePropertiesInDevelopment = function (type, props) {
    validateProperties(type, props);
    validateProperties$1(type, props);
    validateProperties$2(type, props, /* canUseEventSystem */true);
  };

  // HTML parsing normalizes CR and CRLF to LF.
  // It also can turn \u0000 into \uFFFD inside attributes.
  // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
  // If we have a mismatch, it might be caused by that.
  // We will still patch up in this case but not fire the warning.
  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
  var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;

  normalizeMarkupForTextOrAttribute = function (markup) {
    var markupString = typeof markup === 'string' ? markup : '' + markup;
    return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
  };

  warnForTextDifference = function (serverText, clientText) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
    var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
    if (normalizedServerText === normalizedClientText) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
  };

  warnForPropDifference = function (propName, serverValue, clientValue) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
    var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
    if (normalizedServerValue === normalizedClientValue) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
  };

  warnForExtraAttributes = function (attributeNames) {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    var names = [];
    attributeNames.forEach(function (name) {
      names.push(name);
    });
    warning(false, 'Extra attributes from the server: %s', names);
  };

  warnForInvalidEventListener = function (registrationName, listener) {
    if (listener === false) {
      warning(false, 'Expected `%s` listener to be a function, instead got `false`.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', registrationName, registrationName, registrationName, getCurrentFiberStackAddendum$3());
    } else {
      warning(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.%s', registrationName, typeof listener, getCurrentFiberStackAddendum$3());
    }
  };

  // Parse the HTML and read it back to normalize the HTML string so that it
  // can be used for comparison.
  normalizeHTML = function (parent, html) {
    // We could have created a separate document here to avoid
    // re-initializing custom elements if they exist. But this breaks
    // how <noscript> is being handled. So we use the same document.
    // See the discussion in https://github.com/facebook/react/pull/11157.
    var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
    testElement.innerHTML = html;
    return testElement.innerHTML;
  };
}

function ensureListeningTo(rootContainerElement, registrationName) {
  var isDocumentOrFragment = rootContainerElement.nodeType === DOCUMENT_NODE || rootContainerElement.nodeType === DOCUMENT_FRAGMENT_NODE;
  var doc = isDocumentOrFragment ? rootContainerElement : rootContainerElement.ownerDocument;
  listenTo(registrationName, doc);
}

function getOwnerDocumentFromRootContainer(rootContainerElement) {
  return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
}

function trapClickOnNonInteractiveElement(node) {
  // Mobile Safari does not fire properly bubble click events on
  // non-interactive elements, which means delegated click listeners do not
  // fire. The workaround for this bug involves attaching an empty click
  // listener on the target node.
  // http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
  // Just set it using the onclick property so that we don't have to manage any
  // bookkeeping for it. Not sure if we need to clear it when the listener is
  // removed.
  // TODO: Only do this for the relevant Safaris maybe?
  node.onclick = emptyFunction;
}

function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
  for (var propKey in nextProps) {
    if (!nextProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = nextProps[propKey];
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      // Relies on `updateStylesByID` not mutating `styleUpdates`.
      setValueForStyles(domElement, nextProp, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      if (nextHtml != null) {
        setInnerHTML(domElement, nextHtml);
      }
    } else if (propKey === CHILDREN) {
      if (typeof nextProp === 'string') {
        // Avoid setting initial textContent when the text is empty. In IE11 setting
        // textContent on a <textarea> will cause the placeholder to not
        // show within the <textarea> until it has been focused and blurred again.
        // https://github.com/facebook/react/issues/6731#issuecomment-254874553
        var canSetTextContent = tag !== 'textarea' || nextProp !== '';
        if (canSetTextContent) {
          setTextContent(domElement, nextProp);
        }
      } else if (typeof nextProp === 'number') {
        setTextContent(domElement, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // We polyfill it separately on the client during commit.
      // We blacklist it here rather than in the property list because we emit it in SSR.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if (nextProp != null) {
      setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
    }
  }
}

function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
  // TODO: Handle wasCustomComponentTag
  for (var i = 0; i < updatePayload.length; i += 2) {
    var propKey = updatePayload[i];
    var propValue = updatePayload[i + 1];
    if (propKey === STYLE) {
      setValueForStyles(domElement, propValue, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      setInnerHTML(domElement, propValue);
    } else if (propKey === CHILDREN) {
      setTextContent(domElement, propValue);
    } else {
      setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
    }
  }
}

function createElement$1(type, props, rootContainerElement, parentNamespace) {
  var isCustomComponentTag = void 0;

  // We create tags in the namespace of their parent container, except HTML
  // tags get no namespace.
  var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
  var domElement = void 0;
  var namespaceURI = parentNamespace;
  if (namespaceURI === HTML_NAMESPACE) {
    namespaceURI = getIntrinsicNamespace(type);
  }
  if (namespaceURI === HTML_NAMESPACE) {
    {
      isCustomComponentTag = isCustomComponent(type, props);
      // Should this check be gated by parent namespace? Not sure we want to
      // allow <SVG> or <mATH>.
      !(isCustomComponentTag || type === type.toLowerCase()) ? warning(false, '<%s /> is using incorrect casing. ' + 'Use PascalCase for React components, ' + 'or lowercase for HTML elements.', type) : void 0;
    }

    if (type === 'script') {
      // Create the script via .innerHTML so its "parser-inserted" flag is
      // set to true and it does not execute
      var div = ownerDocument.createElement('div');
      div.innerHTML = '<script><' + '/script>'; // eslint-disable-line
      // This is guaranteed to yield a script element.
      var firstChild = div.firstChild;
      domElement = div.removeChild(firstChild);
    } else if (typeof props.is === 'string') {
      // $FlowIssue `createElement` should be updated for Web Components
      domElement = ownerDocument.createElement(type, { is: props.is });
    } else {
      // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
      // See discussion in https://github.com/facebook/react/pull/6896
      // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
      domElement = ownerDocument.createElement(type);
    }
  } else {
    domElement = ownerDocument.createElementNS(namespaceURI, type);
  }

  {
    if (namespaceURI === HTML_NAMESPACE) {
      if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !Object.prototype.hasOwnProperty.call(warnedUnknownTags, type)) {
        warnedUnknownTags[type] = true;
        warning(false, 'The tag <%s> is unrecognized in this browser. ' + 'If you meant to render a React component, start its name with ' + 'an uppercase letter.', type);
      }
    }
  }

  return domElement;
}

function createTextNode$1(text, rootContainerElement) {
  return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
}

function setInitialProperties$1(domElement, tag, rawProps, rootContainerElement) {
  var isCustomComponentTag = isCustomComponent(tag, rawProps);
  {
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$2() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  var props = void 0;
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEventTypes) {
        if (mediaEventTypes.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEventTypes[event], domElement);
        }
      }
      props = rawProps;
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      props = rawProps;
      break;
    case 'img':
    case 'image':
    case 'link':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      props = rawProps;
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      props = rawProps;
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      props = getHostProps(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      props = getHostProps$1(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      props = getHostProps$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      props = getHostProps$3(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    default:
      props = rawProps;
  }

  assertValidProps(tag, props, getStack);

  setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'option':
      postMountWrapper$1(domElement, rawProps);
      break;
    case 'select':
      postMountWrapper$2(domElement, rawProps);
      break;
    default:
      if (typeof props.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }
}

// Calculate the diff between the two objects.
function diffProperties$1(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
  {
    validatePropertiesInDevelopment(tag, nextRawProps);
  }

  var updatePayload = null;

  var lastProps = void 0;
  var nextProps = void 0;
  switch (tag) {
    case 'input':
      lastProps = getHostProps(domElement, lastRawProps);
      nextProps = getHostProps(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'option':
      lastProps = getHostProps$1(domElement, lastRawProps);
      nextProps = getHostProps$1(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'select':
      lastProps = getHostProps$2(domElement, lastRawProps);
      nextProps = getHostProps$2(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'textarea':
      lastProps = getHostProps$3(domElement, lastRawProps);
      nextProps = getHostProps$3(domElement, nextRawProps);
      updatePayload = [];
      break;
    default:
      lastProps = lastRawProps;
      nextProps = nextRawProps;
      if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  assertValidProps(tag, nextProps, getStack);

  var propKey = void 0;
  var styleName = void 0;
  var styleUpdates = null;
  for (propKey in lastProps) {
    if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
      continue;
    }
    if (propKey === STYLE) {
      var lastStyle = lastProps[propKey];
      for (styleName in lastStyle) {
        if (lastStyle.hasOwnProperty(styleName)) {
          if (!styleUpdates) {
            styleUpdates = {};
          }
          styleUpdates[styleName] = '';
        }
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) {
      // Noop. This is handled by the clear text mechanism.
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // Noop. It doesn't work on updates anyway.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      // This is a special case. If any listener updates we need to ensure
      // that the "current" fiber pointer gets updated so we need a commit
      // to update this element.
      if (!updatePayload) {
        updatePayload = [];
      }
    } else {
      // For all other deleted properties we add it to the queue. We use
      // the whitelist in the commit phase instead.
      (updatePayload = updatePayload || []).push(propKey, null);
    }
  }
  for (propKey in nextProps) {
    var nextProp = nextProps[propKey];
    var lastProp = lastProps != null ? lastProps[propKey] : undefined;
    if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
      continue;
    }
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      if (lastProp) {
        // Unset styles on `lastProp` but not on `nextProp`.
        for (styleName in lastProp) {
          if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = '';
          }
        }
        // Update styles that changed since `lastProp`.
        for (styleName in nextProp) {
          if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = nextProp[styleName];
          }
        }
      } else {
        // Relies on `updateStylesByID` not mutating `styleUpdates`.
        if (!styleUpdates) {
          if (!updatePayload) {
            updatePayload = [];
          }
          updatePayload.push(propKey, styleUpdates);
        }
        styleUpdates = nextProp;
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      var lastHtml = lastProp ? lastProp[HTML] : undefined;
      if (nextHtml != null) {
        if (lastHtml !== nextHtml) {
          (updatePayload = updatePayload || []).push(propKey, '' + nextHtml);
        }
      } else {
        // TODO: It might be too late to clear this if we have children
        // inserted already.
      }
    } else if (propKey === CHILDREN) {
      if (lastProp !== nextProp && (typeof nextProp === 'string' || typeof nextProp === 'number')) {
        (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        // We eagerly listen to this even though we haven't committed yet.
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
      if (!updatePayload && lastProp !== nextProp) {
        // This is a special case. If any listener updates we need to ensure
        // that the "current" props pointer gets updated so we need a commit
        // to update this element.
        updatePayload = [];
      }
    } else {
      // For any other property we always add it to the queue and then we
      // filter it out using the whitelist during the commit.
      (updatePayload = updatePayload || []).push(propKey, nextProp);
    }
  }
  if (styleUpdates) {
    (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
  }
  return updatePayload;
}

// Apply the diff.
function updateProperties$1(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
  // Update checked *before* name.
  // In the middle of an update, it is possible to have multiple checked.
  // When a checked radio tries to change name, browser makes another radio's checked false.
  if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) {
    updateChecked(domElement, nextRawProps);
  }

  var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
  var isCustomComponentTag = isCustomComponent(tag, nextRawProps);
  // Apply the diff.
  updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag);

  // TODO: Ensure that an update gets scheduled if any of the special props
  // changed.
  switch (tag) {
    case 'input':
      // Update the wrapper around inputs *after* updating props. This has to
      // happen after `updateDOMProperties`. Otherwise HTML5 input validations
      // raise warnings and prevent the new value from being assigned.
      updateWrapper(domElement, nextRawProps);
      break;
    case 'textarea':
      updateWrapper$1(domElement, nextRawProps);
      break;
    case 'select':
      // <select> value update needs to occur after <option> children
      // reconciliation
      postUpdateWrapper(domElement, nextRawProps);
      break;
  }
}

function getPossibleStandardName(propName) {
  {
    var lowerCasedName = propName.toLowerCase();
    if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      return null;
    }
    return possibleStandardNames[lowerCasedName] || null;
  }
  return null;
}

function diffHydratedProperties$1(domElement, tag, rawProps, parentNamespace, rootContainerElement) {
  var isCustomComponentTag = void 0;
  var extraAttributeNames = void 0;

  {
    suppressHydrationWarning = rawProps[SUPPRESS_HYDRATION_WARNING$1] === true;
    isCustomComponentTag = isCustomComponent(tag, rawProps);
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$2() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEventTypes) {
        if (mediaEventTypes.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEventTypes[event], domElement);
        }
      }
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      break;
    case 'img':
    case 'image':
    case 'link':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
  }

  assertValidProps(tag, rawProps, getStack);

  {
    extraAttributeNames = new Set();
    var attributes = domElement.attributes;
    for (var i = 0; i < attributes.length; i++) {
      var name = attributes[i].name.toLowerCase();
      switch (name) {
        // Built-in SSR attribute is whitelisted
        case 'data-reactroot':
          break;
        // Controlled attributes are not validated
        // TODO: Only ignore them on controlled tags.
        case 'value':
          break;
        case 'checked':
          break;
        case 'selected':
          break;
        default:
          // Intentionally use the original name.
          // See discussion in https://github.com/facebook/react/pull/10676.
          extraAttributeNames.add(attributes[i].name);
      }
    }
  }

  var updatePayload = null;
  for (var propKey in rawProps) {
    if (!rawProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = rawProps[propKey];
    if (propKey === CHILDREN) {
      // For text content children we compare against textContent. This
      // might match additional HTML that is hidden when we read it using
      // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
      // satisfies our requirement. Our requirement is not to produce perfect
      // HTML and attributes. Ideally we should preserve structure but it's
      // ok not to if the visible content is still enough to indicate what
      // even listeners these nodes might be wired up to.
      // TODO: Warn if there is more than a single textNode as a child.
      // TODO: Should we use domElement.firstChild.nodeValue to compare?
      if (typeof nextProp === 'string') {
        if (domElement.textContent !== nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, nextProp];
        }
      } else if (typeof nextProp === 'number') {
        if (domElement.textContent !== '' + nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, '' + nextProp];
        }
      }
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if (true &&
    // Convince Flow we've calculated it (it's DEV-only in this method.)
    typeof isCustomComponentTag === 'boolean') {
      // Validate that the properties correspond to their expected values.
      var serverValue = void 0;
      var propertyInfo = getPropertyInfo(propKey);
      if (suppressHydrationWarning) {
        // Don't bother comparing. We're ignoring all these warnings.
      } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1 ||
      // Controlled attributes are not validated
      // TODO: Only ignore them on controlled tags.
      propKey === 'value' || propKey === 'checked' || propKey === 'selected') {
        // Noop
      } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
        var rawHtml = nextProp ? nextProp[HTML] || '' : '';
        var serverHTML = domElement.innerHTML;
        var expectedHTML = normalizeHTML(domElement, rawHtml);
        if (expectedHTML !== serverHTML) {
          warnForPropDifference(propKey, serverHTML, expectedHTML);
        }
      } else if (propKey === STYLE) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey);
        var expectedStyle = createDangerousStringForStyles(nextProp);
        serverValue = domElement.getAttribute('style');
        if (expectedStyle !== serverValue) {
          warnForPropDifference(propKey, serverValue, expectedStyle);
        }
      } else if (isCustomComponentTag) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey.toLowerCase());
        serverValue = getValueForAttribute(domElement, propKey, nextProp);

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      } else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
        var isMismatchDueToBadCasing = false;
        if (propertyInfo !== null) {
          // $FlowFixMe - Should be inferred as not undefined.
          extraAttributeNames['delete'](propertyInfo.attributeName);
          serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
        } else {
          var ownNamespace = parentNamespace;
          if (ownNamespace === HTML_NAMESPACE) {
            ownNamespace = getIntrinsicNamespace(tag);
          }
          if (ownNamespace === HTML_NAMESPACE) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey.toLowerCase());
          } else {
            var standardName = getPossibleStandardName(propKey);
            if (standardName !== null && standardName !== propKey) {
              // If an SVG prop is supplied with bad casing, it will
              // be successfully parsed from HTML, but will produce a mismatch
              // (and would be incorrectly rendered on the client).
              // However, we already warn about bad casing elsewhere.
              // So we'll skip the misleading extra mismatch warning in this case.
              isMismatchDueToBadCasing = true;
              // $FlowFixMe - Should be inferred as not undefined.
              extraAttributeNames['delete'](standardName);
            }
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey);
          }
          serverValue = getValueForAttribute(domElement, propKey, nextProp);
        }

        if (nextProp !== serverValue && !isMismatchDueToBadCasing) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      }
    }
  }

  {
    // $FlowFixMe - Should be inferred as not undefined.
    if (extraAttributeNames.size > 0 && !suppressHydrationWarning) {
      // $FlowFixMe - Should be inferred as not undefined.
      warnForExtraAttributes(extraAttributeNames);
    }
  }

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'select':
    case 'option':
      // For input and textarea we current always set the value property at
      // post mount to force it to diverge from attributes. However, for
      // option and select we don't quite do the same thing and select
      // is not resilient to the DOM state changing so we don't do that here.
      // TODO: Consider not doing this for input and textarea.
      break;
    default:
      if (typeof rawProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  return updatePayload;
}

function diffHydratedText$1(textNode, text) {
  var isDifferent = textNode.nodeValue !== text;
  return isDifferent;
}

function warnForUnmatchedText$1(textNode, text) {
  {
    warnForTextDifference(textNode.nodeValue, text);
  }
}

function warnForDeletedHydratableElement$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
  }
}

function warnForDeletedHydratableText$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedElement$1(parentNode, tag, props) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedText$1(parentNode, text) {
  {
    if (text === '') {
      // We expect to insert empty text nodes since they're not represented in
      // the HTML.
      // TODO: Remove this special case if we can just avoid inserting empty
      // text nodes.
      return;
    }
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
  }
}

function restoreControlledState$1(domElement, tag, props) {
  switch (tag) {
    case 'input':
      restoreControlledState(domElement, props);
      return;
    case 'textarea':
      restoreControlledState$3(domElement, props);
      return;
    case 'select':
      restoreControlledState$2(domElement, props);
      return;
  }
}

var ReactDOMFiberComponent = Object.freeze({
	createElement: createElement$1,
	createTextNode: createTextNode$1,
	setInitialProperties: setInitialProperties$1,
	diffProperties: diffProperties$1,
	updateProperties: updateProperties$1,
	diffHydratedProperties: diffHydratedProperties$1,
	diffHydratedText: diffHydratedText$1,
	warnForUnmatchedText: warnForUnmatchedText$1,
	warnForDeletedHydratableElement: warnForDeletedHydratableElement$1,
	warnForDeletedHydratableText: warnForDeletedHydratableText$1,
	warnForInsertedHydratedElement: warnForInsertedHydratedElement$1,
	warnForInsertedHydratedText: warnForInsertedHydratedText$1,
	restoreControlledState: restoreControlledState$1
});

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$6 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var validateDOMNesting = emptyFunction;

{
  // This validation code was written based on the HTML5 parsing spec:
  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  //
  // Note: this does not catch all invalid nesting, nor does it try to (as it's
  // not clear what practical benefit doing so provides); instead, we warn only
  // for cases where the parser will give a parse tree differing from what React
  // intended. For example, <b><div></div></b> is invalid but we don't warn
  // because it still parses correctly; we do warn for other cases like nested
  // <p> tags where the beginning of the second element implicitly closes the
  // first, causing a confusing mess.

  // https://html.spec.whatwg.org/multipage/syntax.html#special
  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template',

  // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
  // TODO: Distinguish by namespace here -- for <title>, including it here
  // errs on the side of fewer warnings
  'foreignObject', 'desc', 'title'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
  var buttonScopeTags = inScopeTags.concat(['button']);

  // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];

  var emptyAncestorInfo = {
    current: null,

    formTag: null,
    aTagInScope: null,
    buttonTagInScope: null,
    nobrTagInScope: null,
    pTagInButtonScope: null,

    listItemTagAutoclosing: null,
    dlItemTagAutoclosing: null
  };

  var updatedAncestorInfo$1 = function (oldInfo, tag, instance) {
    var ancestorInfo = _assign({}, oldInfo || emptyAncestorInfo);
    var info = { tag: tag, instance: instance };

    if (inScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.aTagInScope = null;
      ancestorInfo.buttonTagInScope = null;
      ancestorInfo.nobrTagInScope = null;
    }
    if (buttonScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.pTagInButtonScope = null;
    }

    // See rules for 'li', 'dd', 'dt' start tags in
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
      ancestorInfo.listItemTagAutoclosing = null;
      ancestorInfo.dlItemTagAutoclosing = null;
    }

    ancestorInfo.current = info;

    if (tag === 'form') {
      ancestorInfo.formTag = info;
    }
    if (tag === 'a') {
      ancestorInfo.aTagInScope = info;
    }
    if (tag === 'button') {
      ancestorInfo.buttonTagInScope = info;
    }
    if (tag === 'nobr') {
      ancestorInfo.nobrTagInScope = info;
    }
    if (tag === 'p') {
      ancestorInfo.pTagInButtonScope = info;
    }
    if (tag === 'li') {
      ancestorInfo.listItemTagAutoclosing = info;
    }
    if (tag === 'dd' || tag === 'dt') {
      ancestorInfo.dlItemTagAutoclosing = info;
    }

    return ancestorInfo;
  };

  /**
   * Returns whether
   */
  var isTagValidWithParent = function (tag, parentTag) {
    // First, let's check if we're in an unusual parsing mode...
    switch (parentTag) {
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
      case 'select':
        return tag === 'option' || tag === 'optgroup' || tag === '#text';
      case 'optgroup':
        return tag === 'option' || tag === '#text';
      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
      // but
      case 'option':
        return tag === '#text';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
      // No special behavior since these rules fall back to "in body" mode for
      // all except special table nodes which cause bad parsing behavior anyway.

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
      case 'tr':
        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
      case 'tbody':
      case 'thead':
      case 'tfoot':
        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
      case 'colgroup':
        return tag === 'col' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
      case 'table':
        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
      case 'head':
        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
      case 'html':
        return tag === 'head' || tag === 'body';
      case '#document':
        return tag === 'html';
    }

    // Probably in the "in body" parsing mode, so we outlaw only tag combos
    // where the parsing rules cause implicit opens or closes to be added.
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    switch (tag) {
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

      case 'rp':
      case 'rt':
        return impliedEndTags.indexOf(parentTag) === -1;

      case 'body':
      case 'caption':
      case 'col':
      case 'colgroup':
      case 'frame':
      case 'head':
      case 'html':
      case 'tbody':
      case 'td':
      case 'tfoot':
      case 'th':
      case 'thead':
      case 'tr':
        // These tags are only valid with a few parents that have special child
        // parsing rules -- if we're down here, then none of those matched and
        // so we allow it only if we don't know what the parent is, as all other
        // cases are invalid.
        return parentTag == null;
    }

    return true;
  };

  /**
   * Returns whether
   */
  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
    switch (tag) {
      case 'address':
      case 'article':
      case 'aside':
      case 'blockquote':
      case 'center':
      case 'details':
      case 'dialog':
      case 'dir':
      case 'div':
      case 'dl':
      case 'fieldset':
      case 'figcaption':
      case 'figure':
      case 'footer':
      case 'header':
      case 'hgroup':
      case 'main':
      case 'menu':
      case 'nav':
      case 'ol':
      case 'p':
      case 'section':
      case 'summary':
      case 'ul':
      case 'pre':
      case 'listing':
      case 'table':
      case 'hr':
      case 'xmp':
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return ancestorInfo.pTagInButtonScope;

      case 'form':
        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

      case 'li':
        return ancestorInfo.listItemTagAutoclosing;

      case 'dd':
      case 'dt':
        return ancestorInfo.dlItemTagAutoclosing;

      case 'button':
        return ancestorInfo.buttonTagInScope;

      case 'a':
        // Spec says something about storing a list of markers, but it sounds
        // equivalent to this check.
        return ancestorInfo.aTagInScope;

      case 'nobr':
        return ancestorInfo.nobrTagInScope;
    }

    return null;
  };

  var didWarn = {};

  validateDOMNesting = function (childTag, childText, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;

    if (childText != null) {
      !(childTag == null) ? warning(false, 'validateDOMNesting: when childText is passed, childTag should be null') : void 0;
      childTag = '#text';
    }

    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
    var invalidParentOrAncestor = invalidParent || invalidAncestor;
    if (!invalidParentOrAncestor) {
      return;
    }

    var ancestorTag = invalidParentOrAncestor.tag;
    var addendum = getCurrentFiberStackAddendum$6();

    var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + addendum;
    if (didWarn[warnKey]) {
      return;
    }
    didWarn[warnKey] = true;

    var tagDisplayName = childTag;
    var whitespaceInfo = '';
    if (childTag === '#text') {
      if (/\S/.test(childText)) {
        tagDisplayName = 'Text nodes';
      } else {
        tagDisplayName = 'Whitespace text nodes';
        whitespaceInfo = " Make sure you don't have any extra whitespace between tags on " + 'each line of your source code.';
      }
    } else {
      tagDisplayName = '<' + childTag + '>';
    }

    if (invalidParent) {
      var info = '';
      if (ancestorTag === 'table' && childTag === 'tr') {
        info += ' Add a <tbody> to your code to match the DOM tree generated by ' + 'the browser.';
      }
      warning(false, 'validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info, addendum);
    } else {
      warning(false, 'validateDOMNesting(...): %s cannot appear as a descendant of ' + '<%s>.%s', tagDisplayName, ancestorTag, addendum);
    }
  };

  // TODO: turn this into a named export
  validateDOMNesting.updatedAncestorInfo = updatedAncestorInfo$1;
}

var validateDOMNesting$1 = validateDOMNesting;

// TODO: This type is shared between the reconciler and ReactDOM, but will
// eventually be lifted out to the renderer.

// TODO: direct imports like some-package/src/* are bad. Fix me.
var createElement = createElement$1;
var createTextNode = createTextNode$1;
var setInitialProperties = setInitialProperties$1;
var diffProperties = diffProperties$1;
var updateProperties = updateProperties$1;
var diffHydratedProperties = diffHydratedProperties$1;
var diffHydratedText = diffHydratedText$1;
var warnForUnmatchedText = warnForUnmatchedText$1;
var warnForDeletedHydratableElement = warnForDeletedHydratableElement$1;
var warnForDeletedHydratableText = warnForDeletedHydratableText$1;
var warnForInsertedHydratedElement = warnForInsertedHydratedElement$1;
var warnForInsertedHydratedText = warnForInsertedHydratedText$1;
var updatedAncestorInfo = validateDOMNesting$1.updatedAncestorInfo;
var precacheFiberNode = precacheFiberNode$1;
var updateFiberProps = updateFiberProps$1;


var SUPPRESS_HYDRATION_WARNING = void 0;
var topLevelUpdateWarnings = void 0;
var warnOnInvalidCallback = void 0;
var didWarnAboutUnstableCreatePortal = false;

{
  SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
  if (typeof Map !== 'function' || Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') {
    warning(false, 'React depends on Map and Set built-in types. Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
  }

  topLevelUpdateWarnings = function (container) {
    if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
      var hostInstance = DOMRenderer.findHostInstanceWithNoPortals(container._reactRootContainer._internalRoot.current);
      if (hostInstance) {
        !(hostInstance.parentNode === container) ? warning(false, 'render(...): It looks like the React-rendered content of this ' + 'container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + 'ReactDOM.unmountComponentAtNode to empty a container.') : void 0;
      }
    }

    var isRootRenderedBySomeReact = !!container._reactRootContainer;
    var rootEl = getReactRootElementInContainer(container);
    var hasNonRootReactChild = !!(rootEl && getInstanceFromNode$1(rootEl));

    !(!hasNonRootReactChild || isRootRenderedBySomeReact) ? warning(false, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.') : void 0;

    !(container.nodeType !== ELEMENT_NODE || !container.tagName || container.tagName.toUpperCase() !== 'BODY') ? warning(false, 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.') : void 0;
  };

  warnOnInvalidCallback = function (callback, callerName) {
    !(callback === null || typeof callback === 'function') ? warning(false, '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback) : void 0;
  };
}

injection$2.injectFiberControlledHostComponent(ReactDOMFiberComponent);

var eventsEnabled = null;
var selectionInformation = null;

function ReactBatch(root) {
  var expirationTime = DOMRenderer.computeUniqueAsyncExpiration();
  this._expirationTime = expirationTime;
  this._root = root;
  this._next = null;
  this._callbacks = null;
  this._didComplete = false;
  this._hasChildren = false;
  this._children = null;
  this._defer = true;
}
ReactBatch.prototype.render = function (children) {
  !this._defer ? invariant(false, 'batch.render: Cannot render a batch that already committed.') : void 0;
  this._hasChildren = true;
  this._children = children;
  var internalRoot = this._root._internalRoot;
  var expirationTime = this._expirationTime;
  var work = new ReactWork();
  DOMRenderer.updateContainerAtExpirationTime(children, internalRoot, null, expirationTime, work._onCommit);
  return work;
};
ReactBatch.prototype.then = function (onComplete) {
  if (this._didComplete) {
    onComplete();
    return;
  }
  var callbacks = this._callbacks;
  if (callbacks === null) {
    callbacks = this._callbacks = [];
  }
  callbacks.push(onComplete);
};
ReactBatch.prototype.commit = function () {
  var internalRoot = this._root._internalRoot;
  var firstBatch = internalRoot.firstBatch;
  !(this._defer && firstBatch !== null) ? invariant(false, 'batch.commit: Cannot commit a batch multiple times.') : void 0;

  if (!this._hasChildren) {
    // This batch is empty. Return.
    this._next = null;
    this._defer = false;
    return;
  }

  var expirationTime = this._expirationTime;

  // Ensure this is the first batch in the list.
  if (firstBatch !== this) {
    // This batch is not the earliest batch. We need to move it to the front.
    // Update its expiration time to be the expiration time of the earliest
    // batch, so that we can flush it without flushing the other batches.
    if (this._hasChildren) {
      expirationTime = this._expirationTime = firstBatch._expirationTime;
      // Rendering this batch again ensures its children will be the final state
      // when we flush (updates are processed in insertion order: last
      // update wins).
      // TODO: This forces a restart. Should we print a warning?
      this.render(this._children);
    }

    // Remove the batch from the list.
    var previous = null;
    var batch = firstBatch;
    while (batch !== this) {
      previous = batch;
      batch = batch._next;
    }
    !(previous !== null) ? invariant(false, 'batch.commit: Cannot commit a batch multiple times.') : void 0;
    previous._next = batch._next;

    // Add it to the front.
    this._next = firstBatch;
    firstBatch = internalRoot.firstBatch = this;
  }

  // Synchronously flush all the work up to this batch's expiration time.
  this._defer = false;
  DOMRenderer.flushRoot(internalRoot, expirationTime);

  // Pop the batch from the list.
  var next = this._next;
  this._next = null;
  firstBatch = internalRoot.firstBatch = next;

  // Append the next earliest batch's children to the update queue.
  if (firstBatch !== null && firstBatch._hasChildren) {
    firstBatch.render(firstBatch._children);
  }
};
ReactBatch.prototype._onComplete = function () {
  if (this._didComplete) {
    return;
  }
  this._didComplete = true;
  var callbacks = this._callbacks;
  if (callbacks === null) {
    return;
  }
  // TODO: Error handling.
  for (var i = 0; i < callbacks.length; i++) {
    var _callback = callbacks[i];
    _callback();
  }
};

function ReactWork() {
  this._callbacks = null;
  this._didCommit = false;
  // TODO: Avoid need to bind by replacing callbacks in the update queue with
  // list of Work objects.
  this._onCommit = this._onCommit.bind(this);
}
ReactWork.prototype.then = function (onCommit) {
  if (this._didCommit) {
    onCommit();
    return;
  }
  var callbacks = this._callbacks;
  if (callbacks === null) {
    callbacks = this._callbacks = [];
  }
  callbacks.push(onCommit);
};
ReactWork.prototype._onCommit = function () {
  if (this._didCommit) {
    return;
  }
  this._didCommit = true;
  var callbacks = this._callbacks;
  if (callbacks === null) {
    return;
  }
  // TODO: Error handling.
  for (var i = 0; i < callbacks.length; i++) {
    var _callback2 = callbacks[i];
    !(typeof _callback2 === 'function') ? invariant(false, 'Invalid argument passed as callback. Expected a function. Instead received: %s', _callback2) : void 0;
    _callback2();
  }
};

function ReactRoot(container, isAsync, hydrate) {
  var root = DOMRenderer.createContainer(container, isAsync, hydrate);
  this._internalRoot = root;
}
ReactRoot.prototype.render = function (children, callback) {
  var root = this._internalRoot;
  var work = new ReactWork();
  callback = callback === undefined ? null : callback;
  {
    warnOnInvalidCallback(callback, 'render');
  }
  if (callback !== null) {
    work.then(callback);
  }
  DOMRenderer.updateContainer(children, root, null, work._onCommit);
  return work;
};
ReactRoot.prototype.unmount = function (callback) {
  var root = this._internalRoot;
  var work = new ReactWork();
  callback = callback === undefined ? null : callback;
  {
    warnOnInvalidCallback(callback, 'render');
  }
  if (callback !== null) {
    work.then(callback);
  }
  DOMRenderer.updateContainer(null, root, null, work._onCommit);
  return work;
};
ReactRoot.prototype.legacy_renderSubtreeIntoContainer = function (parentComponent, children, callback) {
  var root = this._internalRoot;
  var work = new ReactWork();
  callback = callback === undefined ? null : callback;
  {
    warnOnInvalidCallback(callback, 'render');
  }
  if (callback !== null) {
    work.then(callback);
  }
  DOMRenderer.updateContainer(children, root, parentComponent, work._onCommit);
  return work;
};
ReactRoot.prototype.createBatch = function () {
  var batch = new ReactBatch(this);
  var expirationTime = batch._expirationTime;

  var internalRoot = this._internalRoot;
  var firstBatch = internalRoot.firstBatch;
  if (firstBatch === null) {
    internalRoot.firstBatch = batch;
    batch._next = null;
  } else {
    // Insert sorted by expiration time then insertion order
    var insertAfter = null;
    var insertBefore = firstBatch;
    while (insertBefore !== null && insertBefore._expirationTime <= expirationTime) {
      insertAfter = insertBefore;
      insertBefore = insertBefore._next;
    }
    batch._next = insertBefore;
    if (insertAfter !== null) {
      insertAfter._next = batch;
    }
  }

  return batch;
};

/**
 * True if the supplied DOM node is a valid node element.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @return {boolean} True if the DOM is a valid DOM node.
 * @internal
 */
function isValidContainer(node) {
  return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
}

function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOCUMENT_NODE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

function shouldHydrateDueToLegacyHeuristic(container) {
  var rootElement = getReactRootElementInContainer(container);
  return !!(rootElement && rootElement.nodeType === ELEMENT_NODE && rootElement.hasAttribute(ROOT_ATTRIBUTE_NAME));
}

function shouldAutoFocusHostComponent(type, props) {
  switch (type) {
    case 'button':
    case 'input':
    case 'select':
    case 'textarea':
      return !!props.autoFocus;
  }
  return false;
}

var DOMRenderer = reactReconciler({
  getRootHostContext: function (rootContainerInstance) {
    var type = void 0;
    var namespace = void 0;
    var nodeType = rootContainerInstance.nodeType;
    switch (nodeType) {
      case DOCUMENT_NODE:
      case DOCUMENT_FRAGMENT_NODE:
        {
          type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
          var root = rootContainerInstance.documentElement;
          namespace = root ? root.namespaceURI : getChildNamespace(null, '');
          break;
        }
      default:
        {
          var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
          var ownNamespace = container.namespaceURI || null;
          type = container.tagName;
          namespace = getChildNamespace(ownNamespace, type);
          break;
        }
    }
    {
      var validatedTag = type.toLowerCase();
      var _ancestorInfo = updatedAncestorInfo(null, validatedTag, null);
      return { namespace: namespace, ancestorInfo: _ancestorInfo };
    }
    return namespace;
  },
  getChildHostContext: function (parentHostContext, type) {
    {
      var parentHostContextDev = parentHostContext;
      var _namespace = getChildNamespace(parentHostContextDev.namespace, type);
      var _ancestorInfo2 = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type, null);
      return { namespace: _namespace, ancestorInfo: _ancestorInfo2 };
    }
    var parentNamespace = parentHostContext;
    return getChildNamespace(parentNamespace, type);
  },
  getPublicInstance: function (instance) {
    return instance;
  },
  prepareForCommit: function () {
    eventsEnabled = isEnabled();
    selectionInformation = getSelectionInformation();
    setEnabled(false);
  },
  resetAfterCommit: function () {
    restoreSelection(selectionInformation);
    selectionInformation = null;
    setEnabled(eventsEnabled);
    eventsEnabled = null;
  },
  createInstance: function (type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
    var parentNamespace = void 0;
    {
      // TODO: take namespace into account when validating.
      var hostContextDev = hostContext;
      validateDOMNesting$1(type, null, hostContextDev.ancestorInfo);
      if (typeof props.children === 'string' || typeof props.children === 'number') {
        var string = '' + props.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
      parentNamespace = hostContextDev.namespace;
    }
    var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
    precacheFiberNode(internalInstanceHandle, domElement);
    updateFiberProps(domElement, props);
    return domElement;
  },
  appendInitialChild: function (parentInstance, child) {
    parentInstance.appendChild(child);
  },
  finalizeInitialChildren: function (domElement, type, props, rootContainerInstance) {
    setInitialProperties(domElement, type, props, rootContainerInstance);
    return shouldAutoFocusHostComponent(type, props);
  },
  prepareUpdate: function (domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
    {
      var hostContextDev = hostContext;
      if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
        var string = '' + newProps.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
    }
    return diffProperties(domElement, type, oldProps, newProps, rootContainerInstance);
  },
  shouldSetTextContent: function (type, props) {
    return type === 'textarea' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && typeof props.dangerouslySetInnerHTML.__html === 'string';
  },
  shouldDeprioritizeSubtree: function (type, props) {
    return !!props.hidden;
  },
  createTextInstance: function (text, rootContainerInstance, hostContext, internalInstanceHandle) {
    {
      var hostContextDev = hostContext;
      validateDOMNesting$1(null, text, hostContextDev.ancestorInfo);
    }
    var textNode = createTextNode(text, rootContainerInstance);
    precacheFiberNode(internalInstanceHandle, textNode);
    return textNode;
  },


  now: now,

  mutation: {
    commitMount: function (domElement, type, newProps, internalInstanceHandle) {
      // Despite the naming that might imply otherwise, this method only
      // fires if there is an `Update` effect scheduled during mounting.
      // This happens if `finalizeInitialChildren` returns `true` (which it
      // does to implement the `autoFocus` attribute on the client). But
      // there are also other cases when this might happen (such as patching
      // up text content during hydration mismatch). So we'll check this again.
      if (shouldAutoFocusHostComponent(type, newProps)) {
        domElement.focus();
      }
    },
    commitUpdate: function (domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
      // Update the props handle so that we know which props are the ones with
      // with current event handlers.
      updateFiberProps(domElement, newProps);
      // Apply the diff to the DOM node.
      updateProperties(domElement, updatePayload, type, oldProps, newProps);
    },
    resetTextContent: function (domElement) {
      setTextContent(domElement, '');
    },
    commitTextUpdate: function (textInstance, oldText, newText) {
      textInstance.nodeValue = newText;
    },
    appendChild: function (parentInstance, child) {
      parentInstance.appendChild(child);
    },
    appendChildToContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, container);
      } else {
        container.appendChild(child);
      }
    },
    insertBefore: function (parentInstance, child, beforeChild) {
      parentInstance.insertBefore(child, beforeChild);
    },
    insertInContainerBefore: function (container, child, beforeChild) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, beforeChild);
      } else {
        container.insertBefore(child, beforeChild);
      }
    },
    removeChild: function (parentInstance, child) {
      parentInstance.removeChild(child);
    },
    removeChildFromContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.removeChild(child);
      } else {
        container.removeChild(child);
      }
    }
  },

  hydration: {
    canHydrateInstance: function (instance, type, props) {
      if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
        return null;
      }
      // This has now been refined to an element node.
      return instance;
    },
    canHydrateTextInstance: function (instance, text) {
      if (text === '' || instance.nodeType !== TEXT_NODE) {
        // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
      }
      // This has now been refined to a text node.
      return instance;
    },
    getNextHydratableSibling: function (instance) {
      var node = instance.nextSibling;
      // Skip non-hydratable nodes.
      while (node && node.nodeType !== ELEMENT_NODE && node.nodeType !== TEXT_NODE) {
        node = node.nextSibling;
      }
      return node;
    },
    getFirstHydratableChild: function (parentInstance) {
      var next = parentInstance.firstChild;
      // Skip non-hydratable nodes.
      while (next && next.nodeType !== ELEMENT_NODE && next.nodeType !== TEXT_NODE) {
        next = next.nextSibling;
      }
      return next;
    },
    hydrateInstance: function (instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, instance);
      // TODO: Possibly defer this until the commit phase where all the events
      // get attached.
      updateFiberProps(instance, props);
      var parentNamespace = void 0;
      {
        var hostContextDev = hostContext;
        parentNamespace = hostContextDev.namespace;
      }
      return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance);
    },
    hydrateTextInstance: function (textInstance, text, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, textInstance);
      return diffHydratedText(textInstance, text);
    },
    didNotMatchHydratedContainerTextInstance: function (parentContainer, textInstance, text) {
      {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotMatchHydratedTextInstance: function (parentType, parentProps, parentInstance, textInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotHydrateContainerInstance: function (parentContainer, instance) {
      {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentContainer, instance);
        } else {
          warnForDeletedHydratableText(parentContainer, instance);
        }
      }
    },
    didNotHydrateInstance: function (parentType, parentProps, parentInstance, instance) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentInstance, instance);
        } else {
          warnForDeletedHydratableText(parentInstance, instance);
        }
      }
    },
    didNotFindHydratableContainerInstance: function (parentContainer, type, props) {
      {
        warnForInsertedHydratedElement(parentContainer, type, props);
      }
    },
    didNotFindHydratableContainerTextInstance: function (parentContainer, text) {
      {
        warnForInsertedHydratedText(parentContainer, text);
      }
    },
    didNotFindHydratableInstance: function (parentType, parentProps, parentInstance, type, props) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedElement(parentInstance, type, props);
      }
    },
    didNotFindHydratableTextInstance: function (parentType, parentProps, parentInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedText(parentInstance, text);
      }
    }
  },

  scheduleDeferredCallback: rIC,
  cancelDeferredCallback: cIC
});

injection$3.injectRenderer(DOMRenderer);

var warnedAboutHydrateAPI = false;

function legacyCreateRootFromDOMContainer(container, forceHydrate) {
  var shouldHydrate = forceHydrate || shouldHydrateDueToLegacyHeuristic(container);
  // First clear any existing content.
  if (!shouldHydrate) {
    var warned = false;
    var rootSibling = void 0;
    while (rootSibling = container.lastChild) {
      {
        if (!warned && rootSibling.nodeType === ELEMENT_NODE && rootSibling.hasAttribute(ROOT_ATTRIBUTE_NAME)) {
          warned = true;
          warning(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.');
        }
      }
      container.removeChild(rootSibling);
    }
  }
  {
    if (shouldHydrate && !forceHydrate && !warnedAboutHydrateAPI) {
      warnedAboutHydrateAPI = true;
      lowPriorityWarning$1(false, 'render(): Calling ReactDOM.render() to hydrate server-rendered markup ' + 'will stop working in React v17. Replace the ReactDOM.render() call ' + 'with ReactDOM.hydrate() if you want React to attach to the server HTML.');
    }
  }
  // Legacy roots are not async by default.
  var isAsync = false;
  return new ReactRoot(container, isAsync, shouldHydrate);
}

function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
  // TODO: Ensure all entry points contain this check
  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;

  {
    topLevelUpdateWarnings(container);
  }

  // TODO: Without `any` type, Flow says "Property cannot be accessed on any
  // member of intersection type." Whyyyyyy.
  var root = container._reactRootContainer;
  if (!root) {
    // Initial mount
    root = container._reactRootContainer = legacyCreateRootFromDOMContainer(container, forceHydrate);
    if (typeof callback === 'function') {
      var originalCallback = callback;
      callback = function () {
        var instance = DOMRenderer.getPublicRootInstance(root._internalRoot);
        originalCallback.call(instance);
      };
    }
    // Initial mount should not be batched.
    DOMRenderer.unbatchedUpdates(function () {
      if (parentComponent != null) {
        root.legacy_renderSubtreeIntoContainer(parentComponent, children, callback);
      } else {
        root.render(children, callback);
      }
    });
  } else {
    if (typeof callback === 'function') {
      var _originalCallback = callback;
      callback = function () {
        var instance = DOMRenderer.getPublicRootInstance(root._internalRoot);
        _originalCallback.call(instance);
      };
    }
    // Update
    if (parentComponent != null) {
      root.legacy_renderSubtreeIntoContainer(parentComponent, children, callback);
    } else {
      root.render(children, callback);
    }
  }
  return DOMRenderer.getPublicRootInstance(root._internalRoot);
}

function createPortal(children, container) {
  var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;
  // TODO: pass ReactDOM portal implementation as third argument
  return createPortal$1(children, container, null, key);
}

var ReactDOM = {
  createPortal: createPortal,

  findDOMNode: function (componentOrElement) {
    {
      var owner = ReactCurrentOwner.current;
      if (owner !== null && owner.stateNode !== null) {
        var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
        !warnedAboutRefsInRender ? warning(false, '%s is accessing findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(owner) || 'A component') : void 0;
        owner.stateNode._warnedAboutRefsInRender = true;
      }
    }
    if (componentOrElement == null) {
      return null;
    }
    if (componentOrElement.nodeType === ELEMENT_NODE) {
      return componentOrElement;
    }

    return DOMRenderer.findHostInstance(componentOrElement);
  },
  hydrate: function (element, container, callback) {
    // TODO: throw or warn if we couldn't hydrate?
    return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
  },
  render: function (element, container, callback) {
    return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
  },
  unstable_renderSubtreeIntoContainer: function (parentComponent, element, containerNode, callback) {
    !(parentComponent != null && has(parentComponent)) ? invariant(false, 'parentComponent must be a valid React Component') : void 0;
    return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
  },
  unmountComponentAtNode: function (container) {
    !isValidContainer(container) ? invariant(false, 'unmountComponentAtNode(...): Target container is not a DOM element.') : void 0;

    if (container._reactRootContainer) {
      {
        var rootEl = getReactRootElementInContainer(container);
        var renderedByDifferentReact = rootEl && !getInstanceFromNode$1(rootEl);
        !!renderedByDifferentReact ? warning(false, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by another copy of React.') : void 0;
      }

      // Unmount should not be batched.
      DOMRenderer.unbatchedUpdates(function () {
        legacyRenderSubtreeIntoContainer(null, null, container, false, function () {
          container._reactRootContainer = null;
        });
      });
      // If you call unmountComponentAtNode twice in quick succession, you'll
      // get `true` twice. That's probably fine?
      return true;
    } else {
      {
        var _rootEl = getReactRootElementInContainer(container);
        var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode$1(_rootEl));

        // Check if the container itself is a React root node.
        var isContainerReactRoot = container.nodeType === 1 && isValidContainer(container.parentNode) && !!container.parentNode._reactRootContainer;

        !!hasNonRootReactChild ? warning(false, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.') : void 0;
      }

      return false;
    }
  },


  // Temporary alias since we already shipped React 16 RC with it.
  // TODO: remove in React 17.
  unstable_createPortal: function () {
    if (!didWarnAboutUnstableCreatePortal) {
      didWarnAboutUnstableCreatePortal = true;
      lowPriorityWarning$1(false, 'The ReactDOM.unstable_createPortal() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactDOM.createPortal() instead. It has the exact same API, ' + 'but without the "unstable_" prefix.');
    }
    return createPortal.apply(undefined, arguments);
  },


  unstable_batchedUpdates: DOMRenderer.batchedUpdates,

  unstable_deferredUpdates: DOMRenderer.deferredUpdates,

  flushSync: DOMRenderer.flushSync,

  unstable_flushControlled: DOMRenderer.flushControlled,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    // For TapEventPlugin which is popular in open source
    EventPluginHub: EventPluginHub,
    // Used by test-utils
    EventPluginRegistry: EventPluginRegistry,
    EventPropagators: EventPropagators,
    ReactControlledComponent: ReactControlledComponent,
    ReactDOMComponentTree: ReactDOMComponentTree,
    ReactDOMEventListener: ReactDOMEventListener
  }
};

ReactDOM.unstable_createRoot = function createRoot(container, options) {
  var hydrate = options != null && options.hydrate === true;
  return new ReactRoot(container, true, hydrate);
};

var foundDevTools = DOMRenderer.injectIntoDevTools({
  findFiberByHostInstance: getClosestInstanceFromNode,
  bundleType: 1,
  version: ReactVersion,
  rendererPackageName: 'react-dom'
});

{
  if (!foundDevTools && ExecutionEnvironment.canUseDOM && window.top === window.self) {
    // If we're in Chrome or Firefox, provide a download link if not installed.
    if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
      var protocol = window.location.protocol;
      // Don't warn in exotic cases like chrome-extension://.
      if (/^(https?|file):$/.test(protocol)) {
        console.info('%cDownload the React DevTools ' + 'for a better development experience: ' + 'https://fb.me/react-devtools' + (protocol === 'file:' ? '\nYou might need to use a local HTTP server (instead of file://): ' + 'https://fb.me/react-devtools-faq' : ''), 'font-weight:bold');
      }
    }
  }
}



var ReactDOM$2 = Object.freeze({
	default: ReactDOM
});

var ReactDOM$3 = ( ReactDOM$2 && ReactDOM ) || ReactDOM$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactDom = ReactDOM$3['default'] ? ReactDOM$3['default'] : ReactDOM$3;

module.exports = reactDom;
  })();
}


/***/ }),
/* 119 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

/**
 * Simple, lightweight module assisting with the detection and context of
 * Worker. Helps avoid circular dependencies and allows code to reason about
 * whether or not they are in a Worker, even if they never include the main
 * `ReactWorker` dependency.
 */
var ExecutionEnvironment = {

  canUseDOM: canUseDOM,

  canUseWorkers: typeof Worker !== 'undefined',

  canUseEventListeners: canUseDOM && !!(window.addEventListener || window.attachEvent),

  canUseViewport: canUseDOM && !!window.screen,

  isInWorker: !canUseDOM // For now, this is true - might change in the future.

};

module.exports = ExecutionEnvironment;

/***/ }),
/* 120 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/* eslint-disable fb-www/typeof-undefined */

/**
 * Same as document.activeElement but wraps in a try-catch block. In IE it is
 * not safe to call document.activeElement if there is nothing focused.
 *
 * The activeElement will be null only if the document or document body is not
 * yet defined.
 *
 * @param {?DOMDocument} doc Defaults to current document.
 * @return {?DOMElement}
 */
function getActiveElement(doc) /*?DOMElement*/{
  doc = doc || (typeof document !== 'undefined' ? document : undefined);
  if (typeof doc === 'undefined') {
    return null;
  }
  try {
    return doc.activeElement || doc.body;
  } catch (e) {
    return doc.body;
  }
}

module.exports = getActiveElement;

/***/ }),
/* 121 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 * 
 */

/*eslint-disable no-self-compare */



var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * inlined Object.is polyfill to avoid requiring consumers ship their own
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
 */
function is(x, y) {
  // SameValue algorithm
  if (x === y) {
    // Steps 1-5, 7-10
    // Steps 6.b-6.e: +0 != -0
    // Added the nonzero y check to make Flow happy, but it is redundant
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    // Step 6.a: NaN == NaN
    return x !== x && y !== y;
  }
}

/**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */
function shallowEqual(objA, objB) {
  if (is(objA, objB)) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) {
    return false;
  }

  // Test for A's keys different from B.
  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

module.exports = shallowEqual;

/***/ }),
/* 122 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

var isTextNode = __webpack_require__(123);

/*eslint-disable no-bitwise */

/**
 * Checks if a given DOM node contains or is another DOM node.
 */
function containsNode(outerNode, innerNode) {
  if (!outerNode || !innerNode) {
    return false;
  } else if (outerNode === innerNode) {
    return true;
  } else if (isTextNode(outerNode)) {
    return false;
  } else if (isTextNode(innerNode)) {
    return containsNode(outerNode, innerNode.parentNode);
  } else if ('contains' in outerNode) {
    return outerNode.contains(innerNode);
  } else if (outerNode.compareDocumentPosition) {
    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
  } else {
    return false;
  }
}

module.exports = containsNode;

/***/ }),
/* 123 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var isNode = __webpack_require__(124);

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM text node.
 */
function isTextNode(object) {
  return isNode(object) && object.nodeType == 3;
}

module.exports = isTextNode;

/***/ }),
/* 124 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM node.
 */
function isNode(object) {
  var doc = object ? object.ownerDocument || object : document;
  var defaultView = doc.defaultView || window;
  return !!(object && (typeof defaultView.Node === 'function' ? object instanceof defaultView.Node : typeof object === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string'));
}

module.exports = isNode;

/***/ }),
/* 125 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var hyphenate = __webpack_require__(126);

var msPattern = /^ms-/;

/**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenateStyleName(string) {
  return hyphenate(string).replace(msPattern, '-ms-');
}

module.exports = hyphenateStyleName;

/***/ }),
/* 126 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _uppercasePattern = /([A-Z])/g;

/**
 * Hyphenates a camelcased string, for example:
 *
 *   > hyphenate('backgroundColor')
 *   < "background-color"
 *
 * For CSS style names, use `hyphenateStyleName` instead which works properly
 * with all vendor prefixes, including `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenate(string) {
  return string.replace(_uppercasePattern, '-$1').toLowerCase();
}

module.exports = hyphenate;

/***/ }),
/* 127 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var camelize = __webpack_require__(128);

var msPattern = /^-ms-/;

/**
 * Camelcases a hyphenated CSS property name, for example:
 *
 *   > camelizeStyleName('background-color')
 *   < "backgroundColor"
 *   > camelizeStyleName('-moz-transition')
 *   < "MozTransition"
 *   > camelizeStyleName('-ms-transition')
 *   < "msTransition"
 *
 * As Andi Smith suggests
 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
 * is converted to lowercase `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function camelizeStyleName(string) {
  return camelize(string.replace(msPattern, 'ms-'));
}

module.exports = camelizeStyleName;

/***/ }),
/* 128 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _hyphenPattern = /-(.)/g;

/**
 * Camelcases a hyphenated string, for example:
 *
 *   > camelize('background-color')
 *   < "backgroundColor"
 *
 * @param {string} string
 * @return {string}
 */
function camelize(string) {
  return string.replace(_hyphenPattern, function (_, character) {
    return character.toUpperCase();
  });
}

module.exports = camelize;

/***/ }),
/* 129 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createProvider;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__ = __webpack_require__(72);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__utils_warning__ = __webpack_require__(42);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }






var didWarnAboutReceivingStore = false;
function warnAboutReceivingStore() {
  if (didWarnAboutReceivingStore) {
    return;
  }
  didWarnAboutReceivingStore = true;

  Object(__WEBPACK_IMPORTED_MODULE_3__utils_warning__["a" /* default */])('<Provider> does not support changing `store` on the fly. ' + 'It is most likely that you see this error because you updated to ' + 'Redux 2.x and React Redux 2.x which no longer hot reload reducers ' + 'automatically. See https://github.com/reactjs/react-redux/releases/' + 'tag/v2.0.0 for the migration instructions.');
}

function createProvider() {
  var _Provider$childContex;

  var storeKey = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'store';
  var subKey = arguments[1];

  var subscriptionKey = subKey || storeKey + 'Subscription';

  var Provider = function (_Component) {
    _inherits(Provider, _Component);

    Provider.prototype.getChildContext = function getChildContext() {
      var _ref;

      return _ref = {}, _ref[storeKey] = this[storeKey], _ref[subscriptionKey] = null, _ref;
    };

    function Provider(props, context) {
      _classCallCheck(this, Provider);

      var _this = _possibleConstructorReturn(this, _Component.call(this, props, context));

      _this[storeKey] = props.store;
      return _this;
    }

    Provider.prototype.render = function render() {
      return __WEBPACK_IMPORTED_MODULE_0_react__["Children"].only(this.props.children);
    };

    return Provider;
  }(__WEBPACK_IMPORTED_MODULE_0_react__["Component"]);

  if (true) {
    Provider.prototype.componentWillReceiveProps = function (nextProps) {
      if (this[storeKey] !== nextProps.store) {
        warnAboutReceivingStore();
      }
    };
  }

  Provider.propTypes = {
    store: __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["a" /* storeShape */].isRequired,
    children: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.element.isRequired
  };
  Provider.childContextTypes = (_Provider$childContex = {}, _Provider$childContex[storeKey] = __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["a" /* storeShape */].isRequired, _Provider$childContex[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["b" /* subscriptionShape */], _Provider$childContex);

  return Provider;
}

/* harmony default export */ __webpack_exports__["b"] = (createProvider());

/***/ }),
/* 130 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var emptyFunction = __webpack_require__(32);
var invariant = __webpack_require__(30);
var warning = __webpack_require__(31);
var assign = __webpack_require__(39);

var ReactPropTypesSecret = __webpack_require__(71);
var checkPropTypes = __webpack_require__(40);

module.exports = function(isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */
  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */

  var ANONYMOUS = '<<anonymous>>';

  // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),

    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker,
  };

  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */
  /*eslint-disable no-self-compare*/
  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */
  function PropTypeError(message) {
    this.message = message;
    this.stack = '';
  }
  // Make `instanceof Error` still work for returned errors.
  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if (true) {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }
    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          invariant(
            false,
            'Calling PropTypes validators directly is not supported by the `prop-types` package. ' +
            'Use `PropTypes.checkPropTypes()` to call them. ' +
            'Read more at http://fb.me/use-check-prop-types'
          );
        } else if ("development" !== 'production' && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;
          if (
            !manualPropTypeCallCache[cacheKey] &&
            // Avoid spamming the console because they are often not actionable except for lib authors
            manualPropTypeWarningCount < 3
          ) {
            warning(
              false,
              'You are manually calling a React.PropTypes validation ' +
              'function for the `%s` prop on `%s`. This is deprecated ' +
              'and will throw in the standalone `prop-types` package. ' +
              'You may be seeing this warning due to a third-party PropTypes ' +
              'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.',
              propFullName,
              componentName
            );
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }
      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }
          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }
        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);

    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);

        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunction.thatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }
      var propValue = props[propName];
      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }
      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
        if (error instanceof Error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
       true ? warning(false, 'Invalid argument supplied to oneOf, expected an instance of array.') : void 0;
      return emptyFunction.thatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues);
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + propValue + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }
      for (var key in propValue) {
        if (propValue.hasOwnProperty(key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
          if (error instanceof Error) {
            return error;
          }
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
       true ? warning(false, 'Invalid argument supplied to oneOfType, expected an instance of array.') : void 0;
      return emptyFunction.thatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (typeof checker !== 'function') {
        warning(
          false,
          'Invalid argument supplied to oneOfType. Expected an array of check functions, but ' +
          'received %s at index %s.',
          getPostfixForTypeWarning(checker),
          i
        );
        return emptyFunction.thatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];
        if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) {
          return null;
        }
      }

      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (!checker) {
          continue;
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      // We need to check all keys in case some are required but missing from
      // props.
      var allKeys = assign({}, props[propName], shapeTypes);
      for (var key in allKeys) {
        var checker = shapeTypes[key];
        if (!checker) {
          return new PropTypeError(
            'Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' +
            '\nBad object: ' + JSON.stringify(props[propName], null, '  ') +
            '\nValid keys: ' +  JSON.stringify(Object.keys(shapeTypes), null, '  ')
          );
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;
      case 'boolean':
        return !propValue;
      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }
        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);
        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;
          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;
              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;
      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    }

    // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    }

    // Fallback for non-spec compliant Symbols which are polyfilled.
    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  }

  // Equivalent of `typeof` but with special handling for array and regexp.
  function getPropType(propValue) {
    var propType = typeof propValue;
    if (Array.isArray(propValue)) {
      return 'array';
    }
    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }
    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }
    return propType;
  }

  // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.
  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }
    var propType = getPropType(propValue);
    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }
    return propType;
  }

  // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"
  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);
    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;
      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;
      default:
        return type;
    }
  }

  // Returns class name of the object, if any.
  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }
    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};


/***/ }),
/* 131 */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */
(function (global, factory) {
     true ? module.exports = factory() :
    typeof define === 'function' && define.amd ? define(factory) :
    (global.hoistNonReactStatics = factory());
}(this, (function () {
    'use strict';
    
    var REACT_STATICS = {
        childContextTypes: true,
        contextTypes: true,
        defaultProps: true,
        displayName: true,
        getDefaultProps: true,
        getDerivedStateFromProps: true,
        mixins: true,
        propTypes: true,
        type: true
    };
    
    var KNOWN_STATICS = {
        name: true,
        length: true,
        prototype: true,
        caller: true,
        callee: true,
        arguments: true,
        arity: true
    };
    
    var defineProperty = Object.defineProperty;
    var getOwnPropertyNames = Object.getOwnPropertyNames;
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
    var getPrototypeOf = Object.getPrototypeOf;
    var objectPrototype = getPrototypeOf && getPrototypeOf(Object);
    
    return function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
        if (typeof sourceComponent !== 'string') { // don't hoist over string (html) components
            
            if (objectPrototype) {
                var inheritedComponent = getPrototypeOf(sourceComponent);
                if (inheritedComponent && inheritedComponent !== objectPrototype) {
                    hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
                }
            }
            
            var keys = getOwnPropertyNames(sourceComponent);
            
            if (getOwnPropertySymbols) {
                keys = keys.concat(getOwnPropertySymbols(sourceComponent));
            }
            
            for (var i = 0; i < keys.length; ++i) {
                var key = keys[i];
                if (!REACT_STATICS[key] && !KNOWN_STATICS[key] && (!blacklist || !blacklist[key])) {
                    var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
                    try { // Avoid failures from read-only properties
                        defineProperty(targetComponent, key, descriptor);
                    } catch (e) {}
                }
            }
            
            return targetComponent;
        }
        
        return targetComponent;
    };
})));


/***/ }),
/* 132 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var invariant = function(condition, format, a, b, c, d, e, f) {
  if (true) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  }

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error(
        'Minified exception occurred; use the non-minified dev environment ' +
        'for the full error message and additional helpful warnings.'
      );
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(
        format.replace(/%s/g, function() { return args[argIndex++]; })
      );
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
};

module.exports = invariant;


/***/ }),
/* 133 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return Subscription; });
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

// encapsulates the subscription logic for connecting a component to the redux store, as
// well as nesting subscriptions of descendant components, so that we can ensure the
// ancestor components re-render before descendants

var CLEARED = null;
var nullListeners = {
  notify: function notify() {}
};

function createListenerCollection() {
  // the current/next pattern is copied from redux's createStore code.
  // TODO: refactor+expose that code to be reusable here?
  var current = [];
  var next = [];

  return {
    clear: function clear() {
      next = CLEARED;
      current = CLEARED;
    },
    notify: function notify() {
      var listeners = current = next;
      for (var i = 0; i < listeners.length; i++) {
        listeners[i]();
      }
    },
    get: function get() {
      return next;
    },
    subscribe: function subscribe(listener) {
      var isSubscribed = true;
      if (next === current) next = current.slice();
      next.push(listener);

      return function unsubscribe() {
        if (!isSubscribed || current === CLEARED) return;
        isSubscribed = false;

        if (next === current) next = current.slice();
        next.splice(next.indexOf(listener), 1);
      };
    }
  };
}

var Subscription = function () {
  function Subscription(store, parentSub, onStateChange) {
    _classCallCheck(this, Subscription);

    this.store = store;
    this.parentSub = parentSub;
    this.onStateChange = onStateChange;
    this.unsubscribe = null;
    this.listeners = nullListeners;
  }

  Subscription.prototype.addNestedSub = function addNestedSub(listener) {
    this.trySubscribe();
    return this.listeners.subscribe(listener);
  };

  Subscription.prototype.notifyNestedSubs = function notifyNestedSubs() {
    this.listeners.notify();
  };

  Subscription.prototype.isSubscribed = function isSubscribed() {
    return Boolean(this.unsubscribe);
  };

  Subscription.prototype.trySubscribe = function trySubscribe() {
    if (!this.unsubscribe) {
      this.unsubscribe = this.parentSub ? this.parentSub.addNestedSub(this.onStateChange) : this.store.subscribe(this.onStateChange);

      this.listeners = createListenerCollection();
    }
  };

  Subscription.prototype.tryUnsubscribe = function tryUnsubscribe() {
    if (this.unsubscribe) {
      this.unsubscribe();
      this.unsubscribe = null;
      this.listeners.clear();
      this.listeners = nullListeners;
    }
  };

  return Subscription;
}();



/***/ }),
/* 134 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export createConnect */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__components_connectAdvanced__ = __webpack_require__(73);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__ = __webpack_require__(135);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__mapDispatchToProps__ = __webpack_require__(136);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__mapStateToProps__ = __webpack_require__(149);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__mergeProps__ = __webpack_require__(150);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__selectorFactory__ = __webpack_require__(151);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }








/*
  connect is a facade over connectAdvanced. It turns its args into a compatible
  selectorFactory, which has the signature:

    (dispatch, options) => (nextState, nextOwnProps) => nextFinalProps
  
  connect passes its args to connectAdvanced as options, which will in turn pass them to
  selectorFactory each time a Connect component instance is instantiated or hot reloaded.

  selectorFactory returns a final props selector from its mapStateToProps,
  mapStateToPropsFactories, mapDispatchToProps, mapDispatchToPropsFactories, mergeProps,
  mergePropsFactories, and pure args.

  The resulting final props selector is called by the Connect component instance whenever
  it receives new props or store state.
 */

function match(arg, factories, name) {
  for (var i = factories.length - 1; i >= 0; i--) {
    var result = factories[i](arg);
    if (result) return result;
  }

  return function (dispatch, options) {
    throw new Error('Invalid value of type ' + typeof arg + ' for ' + name + ' argument when connecting component ' + options.wrappedComponentName + '.');
  };
}

function strictEqual(a, b) {
  return a === b;
}

// createConnect with default args builds the 'official' connect behavior. Calling it with
// different options opens up some testing and extensibility scenarios
function createConnect() {
  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
      _ref$connectHOC = _ref.connectHOC,
      connectHOC = _ref$connectHOC === undefined ? __WEBPACK_IMPORTED_MODULE_0__components_connectAdvanced__["a" /* default */] : _ref$connectHOC,
      _ref$mapStateToPropsF = _ref.mapStateToPropsFactories,
      mapStateToPropsFactories = _ref$mapStateToPropsF === undefined ? __WEBPACK_IMPORTED_MODULE_3__mapStateToProps__["a" /* default */] : _ref$mapStateToPropsF,
      _ref$mapDispatchToPro = _ref.mapDispatchToPropsFactories,
      mapDispatchToPropsFactories = _ref$mapDispatchToPro === undefined ? __WEBPACK_IMPORTED_MODULE_2__mapDispatchToProps__["a" /* default */] : _ref$mapDispatchToPro,
      _ref$mergePropsFactor = _ref.mergePropsFactories,
      mergePropsFactories = _ref$mergePropsFactor === undefined ? __WEBPACK_IMPORTED_MODULE_4__mergeProps__["a" /* default */] : _ref$mergePropsFactor,
      _ref$selectorFactory = _ref.selectorFactory,
      selectorFactory = _ref$selectorFactory === undefined ? __WEBPACK_IMPORTED_MODULE_5__selectorFactory__["a" /* default */] : _ref$selectorFactory;

  return function connect(mapStateToProps, mapDispatchToProps, mergeProps) {
    var _ref2 = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {},
        _ref2$pure = _ref2.pure,
        pure = _ref2$pure === undefined ? true : _ref2$pure,
        _ref2$areStatesEqual = _ref2.areStatesEqual,
        areStatesEqual = _ref2$areStatesEqual === undefined ? strictEqual : _ref2$areStatesEqual,
        _ref2$areOwnPropsEqua = _ref2.areOwnPropsEqual,
        areOwnPropsEqual = _ref2$areOwnPropsEqua === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areOwnPropsEqua,
        _ref2$areStatePropsEq = _ref2.areStatePropsEqual,
        areStatePropsEqual = _ref2$areStatePropsEq === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areStatePropsEq,
        _ref2$areMergedPropsE = _ref2.areMergedPropsEqual,
        areMergedPropsEqual = _ref2$areMergedPropsE === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areMergedPropsE,
        extraOptions = _objectWithoutProperties(_ref2, ['pure', 'areStatesEqual', 'areOwnPropsEqual', 'areStatePropsEqual', 'areMergedPropsEqual']);

    var initMapStateToProps = match(mapStateToProps, mapStateToPropsFactories, 'mapStateToProps');
    var initMapDispatchToProps = match(mapDispatchToProps, mapDispatchToPropsFactories, 'mapDispatchToProps');
    var initMergeProps = match(mergeProps, mergePropsFactories, 'mergeProps');

    return connectHOC(selectorFactory, _extends({
      // used in error messages
      methodName: 'connect',

      // used to compute Connect's displayName from the wrapped component's displayName.
      getDisplayName: function getDisplayName(name) {
        return 'Connect(' + name + ')';
      },

      // if mapStateToProps is falsy, the Connect component doesn't subscribe to store state changes
      shouldHandleStateChanges: Boolean(mapStateToProps),

      // passed through to selectorFactory
      initMapStateToProps: initMapStateToProps,
      initMapDispatchToProps: initMapDispatchToProps,
      initMergeProps: initMergeProps,
      pure: pure,
      areStatesEqual: areStatesEqual,
      areOwnPropsEqual: areOwnPropsEqual,
      areStatePropsEqual: areStatePropsEqual,
      areMergedPropsEqual: areMergedPropsEqual

    }, extraOptions));
  };
}

/* harmony default export */ __webpack_exports__["a"] = (createConnect());

/***/ }),
/* 135 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = shallowEqual;
var hasOwn = Object.prototype.hasOwnProperty;

function is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}

function shallowEqual(objA, objB) {
  if (is(objA, objB)) return true;

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) return false;

  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwn.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

/***/ }),
/* 136 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export whenMapDispatchToPropsIsFunction */
/* unused harmony export whenMapDispatchToPropsIsMissing */
/* unused harmony export whenMapDispatchToPropsIsObject */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_redux__ = __webpack_require__(43);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__ = __webpack_require__(74);



function whenMapDispatchToPropsIsFunction(mapDispatchToProps) {
  return typeof mapDispatchToProps === 'function' ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["b" /* wrapMapToPropsFunc */])(mapDispatchToProps, 'mapDispatchToProps') : undefined;
}

function whenMapDispatchToPropsIsMissing(mapDispatchToProps) {
  return !mapDispatchToProps ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function (dispatch) {
    return { dispatch: dispatch };
  }) : undefined;
}

function whenMapDispatchToPropsIsObject(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === 'object' ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function (dispatch) {
    return Object(__WEBPACK_IMPORTED_MODULE_0_redux__["bindActionCreators"])(mapDispatchToProps, dispatch);
  }) : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMapDispatchToPropsIsFunction, whenMapDispatchToPropsIsMissing, whenMapDispatchToPropsIsObject]);

/***/ }),
/* 137 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global, module) {/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ponyfill_js__ = __webpack_require__(139);
/* global window */


var root;

if (typeof self !== 'undefined') {
  root = self;
} else if (typeof window !== 'undefined') {
  root = window;
} else if (typeof global !== 'undefined') {
  root = global;
} else if (true) {
  root = module;
} else {
  root = Function('return this')();
}

var result = Object(__WEBPACK_IMPORTED_MODULE_0__ponyfill_js__["a" /* default */])(root);
/* harmony default export */ __webpack_exports__["a"] = (result);

/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(33), __webpack_require__(138)(module)))

/***/ }),
/* 138 */
/***/ (function(module, exports) {

module.exports = function(originalModule) {
	if(!originalModule.webpackPolyfill) {
		var module = Object.create(originalModule);
		// module.parent = undefined by default
		if(!module.children) module.children = [];
		Object.defineProperty(module, "loaded", {
			enumerable: true,
			get: function() {
				return module.l;
			}
		});
		Object.defineProperty(module, "id", {
			enumerable: true,
			get: function() {
				return module.i;
			}
		});
		Object.defineProperty(module, "exports", {
			enumerable: true,
		});
		module.webpackPolyfill = 1;
	}
	return module;
};


/***/ }),
/* 139 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = symbolObservablePonyfill;
function symbolObservablePonyfill(root) {
	var result;
	var Symbol = root.Symbol;

	if (typeof Symbol === 'function') {
		if (Symbol.observable) {
			result = Symbol.observable;
		} else {
			result = Symbol('observable');
			Symbol.observable = result;
		}
	} else {
		result = '@@observable';
	}

	return result;
};


/***/ }),
/* 140 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__baseGetTag_js__ = __webpack_require__(141);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__getPrototype_js__ = __webpack_require__(146);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__isObjectLike_js__ = __webpack_require__(148);




/** `Object#toString` result references. */
var objectTag = '[object Object]';

/** Used for built-in method references. */
var funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to infer the `Object` constructor. */
var objectCtorString = funcToString.call(Object);

/**
 * Checks if `value` is a plain object, that is, an object created by the
 * `Object` constructor or one with a `[[Prototype]]` of `null`.
 *
 * @static
 * @memberOf _
 * @since 0.8.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a plain object, else `false`.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 * }
 *
 * _.isPlainObject(new Foo);
 * // => false
 *
 * _.isPlainObject([1, 2, 3]);
 * // => false
 *
 * _.isPlainObject({ 'x': 0, 'y': 0 });
 * // => true
 *
 * _.isPlainObject(Object.create(null));
 * // => true
 */
function isPlainObject(value) {
  if (!Object(__WEBPACK_IMPORTED_MODULE_2__isObjectLike_js__["a" /* default */])(value) || Object(__WEBPACK_IMPORTED_MODULE_0__baseGetTag_js__["a" /* default */])(value) != objectTag) {
    return false;
  }
  var proto = Object(__WEBPACK_IMPORTED_MODULE_1__getPrototype_js__["a" /* default */])(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;
  return typeof Ctor == 'function' && Ctor instanceof Ctor &&
    funcToString.call(Ctor) == objectCtorString;
}

/* harmony default export */ __webpack_exports__["a"] = (isPlainObject);


/***/ }),
/* 141 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Symbol_js__ = __webpack_require__(76);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__getRawTag_js__ = __webpack_require__(144);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__objectToString_js__ = __webpack_require__(145);




/** `Object#toString` result references. */
var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

/** Built-in value references. */
var symToStringTag = __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */] ? __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */].toStringTag : undefined;

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? Object(__WEBPACK_IMPORTED_MODULE_1__getRawTag_js__["a" /* default */])(value)
    : Object(__WEBPACK_IMPORTED_MODULE_2__objectToString_js__["a" /* default */])(value);
}

/* harmony default export */ __webpack_exports__["a"] = (baseGetTag);


/***/ }),
/* 142 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__freeGlobal_js__ = __webpack_require__(143);


/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = __WEBPACK_IMPORTED_MODULE_0__freeGlobal_js__["a" /* default */] || freeSelf || Function('return this')();

/* harmony default export */ __webpack_exports__["a"] = (root);


/***/ }),
/* 143 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/* harmony default export */ __webpack_exports__["a"] = (freeGlobal);

/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(33)))

/***/ }),
/* 144 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Symbol_js__ = __webpack_require__(76);


/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Built-in value references. */
var symToStringTag = __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */] ? __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */].toStringTag : undefined;

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/* harmony default export */ __webpack_exports__["a"] = (getRawTag);


/***/ }),
/* 145 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/* harmony default export */ __webpack_exports__["a"] = (objectToString);


/***/ }),
/* 146 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__overArg_js__ = __webpack_require__(147);


/** Built-in value references. */
var getPrototype = Object(__WEBPACK_IMPORTED_MODULE_0__overArg_js__["a" /* default */])(Object.getPrototypeOf, Object);

/* harmony default export */ __webpack_exports__["a"] = (getPrototype);


/***/ }),
/* 147 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/* harmony default export */ __webpack_exports__["a"] = (overArg);


/***/ }),
/* 148 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/* harmony default export */ __webpack_exports__["a"] = (isObjectLike);


/***/ }),
/* 149 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export whenMapStateToPropsIsFunction */
/* unused harmony export whenMapStateToPropsIsMissing */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__ = __webpack_require__(74);


function whenMapStateToPropsIsFunction(mapStateToProps) {
  return typeof mapStateToProps === 'function' ? Object(__WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__["b" /* wrapMapToPropsFunc */])(mapStateToProps, 'mapStateToProps') : undefined;
}

function whenMapStateToPropsIsMissing(mapStateToProps) {
  return !mapStateToProps ? Object(__WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function () {
    return {};
  }) : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMapStateToPropsIsFunction, whenMapStateToPropsIsMissing]);

/***/ }),
/* 150 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export defaultMergeProps */
/* unused harmony export wrapMergePropsFunc */
/* unused harmony export whenMergePropsIsFunction */
/* unused harmony export whenMergePropsIsOmitted */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__ = __webpack_require__(75);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };



function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return _extends({}, ownProps, stateProps, dispatchProps);
}

function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, _ref) {
    var displayName = _ref.displayName,
        pure = _ref.pure,
        areMergedPropsEqual = _ref.areMergedPropsEqual;

    var hasRunOnce = false;
    var mergedProps = void 0;

    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      var nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);

      if (hasRunOnce) {
        if (!pure || !areMergedPropsEqual(nextMergedProps, mergedProps)) mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;

        if (true) Object(__WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__["a" /* default */])(mergedProps, displayName, 'mergeProps');
      }

      return mergedProps;
    };
  };
}

function whenMergePropsIsFunction(mergeProps) {
  return typeof mergeProps === 'function' ? wrapMergePropsFunc(mergeProps) : undefined;
}

function whenMergePropsIsOmitted(mergeProps) {
  return !mergeProps ? function () {
    return defaultMergeProps;
  } : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMergePropsIsFunction, whenMergePropsIsOmitted]);

/***/ }),
/* 151 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export impureFinalPropsSelectorFactory */
/* unused harmony export pureFinalPropsSelectorFactory */
/* harmony export (immutable) */ __webpack_exports__["a"] = finalPropsSelectorFactory;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__verifySubselectors__ = __webpack_require__(152);
function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }



function impureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch) {
  return function impureFinalPropsSelector(state, ownProps) {
    return mergeProps(mapStateToProps(state, ownProps), mapDispatchToProps(dispatch, ownProps), ownProps);
  };
}

function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, _ref) {
  var areStatesEqual = _ref.areStatesEqual,
      areOwnPropsEqual = _ref.areOwnPropsEqual,
      areStatePropsEqual = _ref.areStatePropsEqual;

  var hasRunAtLeastOnce = false;
  var state = void 0;
  var ownProps = void 0;
  var stateProps = void 0;
  var dispatchProps = void 0;
  var mergedProps = void 0;

  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }

  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);

    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);

    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps) stateProps = mapStateToProps(state, ownProps);

    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);

    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewState() {
    var nextStateProps = mapStateToProps(state, ownProps);
    var statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;

    if (statePropsChanged) mergedProps = mergeProps(stateProps, dispatchProps, ownProps);

    return mergedProps;
  }

  function handleSubsequentCalls(nextState, nextOwnProps) {
    var propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    var stateChanged = !areStatesEqual(nextState, state);
    state = nextState;
    ownProps = nextOwnProps;

    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }

  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
}

// TODO: Add more comments

// If pure is true, the selector returned by selectorFactory will memoize its results,
// allowing connectAdvanced's shouldComponentUpdate to return false if final
// props have not changed. If false, the selector will always return a new
// object and shouldComponentUpdate will always return true.

function finalPropsSelectorFactory(dispatch, _ref2) {
  var initMapStateToProps = _ref2.initMapStateToProps,
      initMapDispatchToProps = _ref2.initMapDispatchToProps,
      initMergeProps = _ref2.initMergeProps,
      options = _objectWithoutProperties(_ref2, ['initMapStateToProps', 'initMapDispatchToProps', 'initMergeProps']);

  var mapStateToProps = initMapStateToProps(dispatch, options);
  var mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  var mergeProps = initMergeProps(dispatch, options);

  if (true) {
    Object(__WEBPACK_IMPORTED_MODULE_0__verifySubselectors__["a" /* default */])(mapStateToProps, mapDispatchToProps, mergeProps, options.displayName);
  }

  var selectorFactory = options.pure ? pureFinalPropsSelectorFactory : impureFinalPropsSelectorFactory;

  return selectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}

/***/ }),
/* 152 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = verifySubselectors;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_warning__ = __webpack_require__(42);


function verify(selector, methodName, displayName) {
  if (!selector) {
    throw new Error('Unexpected value for ' + methodName + ' in ' + displayName + '.');
  } else if (methodName === 'mapStateToProps' || methodName === 'mapDispatchToProps') {
    if (!selector.hasOwnProperty('dependsOnOwnProps')) {
      Object(__WEBPACK_IMPORTED_MODULE_0__utils_warning__["a" /* default */])('The selector for ' + methodName + ' of ' + displayName + ' did not specify a value for dependsOnOwnProps.');
    }
  }
}

function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps, displayName) {
  verify(mapStateToProps, 'mapStateToProps', displayName);
  verify(mapDispatchToProps, 'mapDispatchToProps', displayName);
  verify(mergeProps, 'mergeProps', displayName);
}

/***/ }),
/* 153 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(1);

var _react2 = _interopRequireDefault(_react);

var _reactDom = __webpack_require__(25);

var _reactDom2 = _interopRequireDefault(_reactDom);

var _axios = __webpack_require__(44);

var _axios2 = _interopRequireDefault(_axios);

var _reactRedux = __webpack_require__(41);

var _data = __webpack_require__(46);

var _Items = __webpack_require__(173);

var _actions = __webpack_require__(82);

var _items = __webpack_require__(266);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Scan = function (_Component) {
  _inherits(Scan, _Component);

  function Scan() {
    var _ref;

    var _temp, _this, _ret;

    _classCallCheck(this, Scan);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, (_ref = Scan.__proto__ || Object.getPrototypeOf(Scan)).call.apply(_ref, [this].concat(args))), _this), _this.state = {
      activeIndex: 0
    }, _this.handleKeyDown = function (event) {
      if (event.key === 'Enter' && !event.shiftKey) {
        _this.goToNextItem();
      } else if (event.key === 'Enter' && event.shiftKey) {
        _this.goToPreviousItem();
      }
    }, _this.goToNextItem = function () {
      var activeKey = _this.props.interview.activeKey;
      var nextItem = (0, _items.getNextItemByKey)(activeKey);
      _this.setActiveIndex();
      _this.props.dispatch((0, _actions.setActiveItem)(nextItem.key));
    }, _this.goToPreviousItem = function () {
      var activeKey = _this.props.interview.activeKey;
      var previousItem = (0, _items.getPreviousItemByKey)(activeKey);
      _this.setActiveIndex();
      _this.props.dispatch((0, _actions.setActiveItem)(previousItem.key));
    }, _this.setActiveIndex = function () {
      var activeKey = _this.props.interview.activeKey;
      var activeIndex = _items.items.indexOf((0, _items.getItemByKey)(activeKey));
      _this.setState({
        activeIndex: activeIndex
      });
    }, _temp), _possibleConstructorReturn(_this, _ret);
  }

  _createClass(Scan, [{
    key: 'componentDidMount',
    value: function componentDidMount() {
      window.addEventListener('keydown', this.handleKeyDown);
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      window.removeEventListener('keydown', this.handleKeyDown);
    }
  }, {
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'div',
        null,
        _react2.default.createElement(
          'div',
          { style: { display: 'flex' } },
          _react2.default.createElement(_Items.ItemList, { items: _items.items, activeIndex: this.state.activeIndex }),
          _react2.default.createElement(_Items.ResponseContainer, { items: _items.items })
        ),
        _react2.default.createElement('textarea', {
          placeholder: 'Save som JSON',
          defaultValue: JSON.stringify(_data.scanData.data)
        })
      );
    }
  }]);

  return Scan;
}(_react.Component);

exports.default = (0, _reactRedux.connect)(function (state) {
  return state;
})(Scan);

/***/ }),
/* 154 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);
var bind = __webpack_require__(77);
var Axios = __webpack_require__(156);
var defaults = __webpack_require__(45);

/**
 * Create an instance of Axios
 *
 * @param {Object} defaultConfig The default config for the instance
 * @return {Axios} A new instance of Axios
 */
function createInstance(defaultConfig) {
  var context = new Axios(defaultConfig);
  var instance = bind(Axios.prototype.request, context);

  // Copy axios.prototype to instance
  utils.extend(instance, Axios.prototype, context);

  // Copy context to instance
  utils.extend(instance, context);

  return instance;
}

// Create the default instance to be exported
var axios = createInstance(defaults);

// Expose Axios class to allow class inheritance
axios.Axios = Axios;

// Factory for creating new instances
axios.create = function create(instanceConfig) {
  return createInstance(utils.merge(defaults, instanceConfig));
};

// Expose Cancel & CancelToken
axios.Cancel = __webpack_require__(81);
axios.CancelToken = __webpack_require__(171);
axios.isCancel = __webpack_require__(80);

// Expose all/spread
axios.all = function all(promises) {
  return Promise.all(promises);
};
axios.spread = __webpack_require__(172);

module.exports = axios;

// Allow use of default import syntax in TypeScript
module.exports.default = axios;


/***/ }),
/* 155 */
/***/ (function(module, exports) {

/*!
 * Determine if an object is a Buffer
 *
 * @author   Feross Aboukhadijeh <https://feross.org>
 * @license  MIT
 */

// The _isBuffer check is for Safari 5-7 support, because it's missing
// Object.prototype.constructor. Remove this eventually
module.exports = function (obj) {
  return obj != null && (isBuffer(obj) || isSlowBuffer(obj) || !!obj._isBuffer)
}

function isBuffer (obj) {
  return !!obj.constructor && typeof obj.constructor.isBuffer === 'function' && obj.constructor.isBuffer(obj)
}

// For Node v0.10 support. Remove this eventually.
function isSlowBuffer (obj) {
  return typeof obj.readFloatLE === 'function' && typeof obj.slice === 'function' && isBuffer(obj.slice(0, 0))
}


/***/ }),
/* 156 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var defaults = __webpack_require__(45);
var utils = __webpack_require__(10);
var InterceptorManager = __webpack_require__(166);
var dispatchRequest = __webpack_require__(167);

/**
 * Create a new instance of Axios
 *
 * @param {Object} instanceConfig The default config for the instance
 */
function Axios(instanceConfig) {
  this.defaults = instanceConfig;
  this.interceptors = {
    request: new InterceptorManager(),
    response: new InterceptorManager()
  };
}

/**
 * Dispatch a request
 *
 * @param {Object} config The config specific for this request (merged with this.defaults)
 */
Axios.prototype.request = function request(config) {
  /*eslint no-param-reassign:0*/
  // Allow for axios('example/url'[, config]) a la fetch API
  if (typeof config === 'string') {
    config = utils.merge({
      url: arguments[0]
    }, arguments[1]);
  }

  config = utils.merge(defaults, {method: 'get'}, this.defaults, config);
  config.method = config.method.toLowerCase();

  // Hook up interceptors middleware
  var chain = [dispatchRequest, undefined];
  var promise = Promise.resolve(config);

  this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
    chain.unshift(interceptor.fulfilled, interceptor.rejected);
  });

  this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
    chain.push(interceptor.fulfilled, interceptor.rejected);
  });

  while (chain.length) {
    promise = promise.then(chain.shift(), chain.shift());
  }

  return promise;
};

// Provide aliases for supported request methods
utils.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {
  /*eslint func-names:0*/
  Axios.prototype[method] = function(url, config) {
    return this.request(utils.merge(config || {}, {
      method: method,
      url: url
    }));
  };
});

utils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {
  /*eslint func-names:0*/
  Axios.prototype[method] = function(url, data, config) {
    return this.request(utils.merge(config || {}, {
      method: method,
      url: url,
      data: data
    }));
  };
});

module.exports = Axios;


/***/ }),
/* 157 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 158 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

module.exports = function normalizeHeaderName(headers, normalizedName) {
  utils.forEach(headers, function processHeader(value, name) {
    if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {
      headers[normalizedName] = value;
      delete headers[name];
    }
  });
};


/***/ }),
/* 159 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var createError = __webpack_require__(79);

/**
 * Resolve or reject a Promise based on response status.
 *
 * @param {Function} resolve A function that resolves the promise.
 * @param {Function} reject A function that rejects the promise.
 * @param {object} response The response.
 */
module.exports = function settle(resolve, reject, response) {
  var validateStatus = response.config.validateStatus;
  // Note: status is not exposed by XDomainRequest
  if (!response.status || !validateStatus || validateStatus(response.status)) {
    resolve(response);
  } else {
    reject(createError(
      'Request failed with status code ' + response.status,
      response.config,
      null,
      response.request,
      response
    ));
  }
};


/***/ }),
/* 160 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Update an Error with the specified config, error code, and response.
 *
 * @param {Error} error The error to update.
 * @param {Object} config The config.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 * @returns {Error} The error.
 */
module.exports = function enhanceError(error, config, code, request, response) {
  error.config = config;
  if (code) {
    error.code = code;
  }
  error.request = request;
  error.response = response;
  return error;
};


/***/ }),
/* 161 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

function encode(val) {
  return encodeURIComponent(val).
    replace(/%40/gi, '@').
    replace(/%3A/gi, ':').
    replace(/%24/g, '$').
    replace(/%2C/gi, ',').
    replace(/%20/g, '+').
    replace(/%5B/gi, '[').
    replace(/%5D/gi, ']');
}

/**
 * Build a URL by appending params to the end
 *
 * @param {string} url The base of the url (e.g., http://www.google.com)
 * @param {object} [params] The params to be appended
 * @returns {string} The formatted url
 */
module.exports = function buildURL(url, params, paramsSerializer) {
  /*eslint no-param-reassign:0*/
  if (!params) {
    return url;
  }

  var serializedParams;
  if (paramsSerializer) {
    serializedParams = paramsSerializer(params);
  } else if (utils.isURLSearchParams(params)) {
    serializedParams = params.toString();
  } else {
    var parts = [];

    utils.forEach(params, function serialize(val, key) {
      if (val === null || typeof val === 'undefined') {
        return;
      }

      if (utils.isArray(val)) {
        key = key + '[]';
      } else {
        val = [val];
      }

      utils.forEach(val, function parseValue(v) {
        if (utils.isDate(v)) {
          v = v.toISOString();
        } else if (utils.isObject(v)) {
          v = JSON.stringify(v);
        }
        parts.push(encode(key) + '=' + encode(v));
      });
    });

    serializedParams = parts.join('&');
  }

  if (serializedParams) {
    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
  }

  return url;
};


/***/ }),
/* 162 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

// Headers whose duplicates are ignored by node
// c.f. https://nodejs.org/api/http.html#http_message_headers
var ignoreDuplicateOf = [
  'age', 'authorization', 'content-length', 'content-type', 'etag',
  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',
  'last-modified', 'location', 'max-forwards', 'proxy-authorization',
  'referer', 'retry-after', 'user-agent'
];

/**
 * Parse headers into an object
 *
 * ```
 * Date: Wed, 27 Aug 2014 08:58:49 GMT
 * Content-Type: application/json
 * Connection: keep-alive
 * Transfer-Encoding: chunked
 * ```
 *
 * @param {String} headers Headers needing to be parsed
 * @returns {Object} Headers parsed into an object
 */
module.exports = function parseHeaders(headers) {
  var parsed = {};
  var key;
  var val;
  var i;

  if (!headers) { return parsed; }

  utils.forEach(headers.split('\n'), function parser(line) {
    i = line.indexOf(':');
    key = utils.trim(line.substr(0, i)).toLowerCase();
    val = utils.trim(line.substr(i + 1));

    if (key) {
      if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) {
        return;
      }
      if (key === 'set-cookie') {
        parsed[key] = (parsed[key] ? parsed[key] : []).concat([val]);
      } else {
        parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;
      }
    }
  });

  return parsed;
};


/***/ }),
/* 163 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

module.exports = (
  utils.isStandardBrowserEnv() ?

  // Standard browser envs have full support of the APIs needed to test
  // whether the request URL is of the same origin as current location.
  (function standardBrowserEnv() {
    var msie = /(msie|trident)/i.test(navigator.userAgent);
    var urlParsingNode = document.createElement('a');
    var originURL;

    /**
    * Parse a URL to discover it's components
    *
    * @param {String} url The URL to be parsed
    * @returns {Object}
    */
    function resolveURL(url) {
      var href = url;

      if (msie) {
        // IE needs attribute set twice to normalize properties
        urlParsingNode.setAttribute('href', href);
        href = urlParsingNode.href;
      }

      urlParsingNode.setAttribute('href', href);

      // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils
      return {
        href: urlParsingNode.href,
        protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',
        host: urlParsingNode.host,
        search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, '') : '',
        hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',
        hostname: urlParsingNode.hostname,
        port: urlParsingNode.port,
        pathname: (urlParsingNode.pathname.charAt(0) === '/') ?
                  urlParsingNode.pathname :
                  '/' + urlParsingNode.pathname
      };
    }

    originURL = resolveURL(window.location.href);

    /**
    * Determine if a URL shares the same origin as the current location
    *
    * @param {String} requestURL The URL to test
    * @returns {boolean} True if URL shares the same origin, otherwise false
    */
    return function isURLSameOrigin(requestURL) {
      var parsed = (utils.isString(requestURL)) ? resolveURL(requestURL) : requestURL;
      return (parsed.protocol === originURL.protocol &&
            parsed.host === originURL.host);
    };
  })() :

  // Non standard browser envs (web workers, react-native) lack needed support.
  (function nonStandardBrowserEnv() {
    return function isURLSameOrigin() {
      return true;
    };
  })()
);


/***/ }),
/* 164 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// btoa polyfill for IE<10 courtesy https://github.com/davidchambers/Base64.js

var chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=';

function E() {
  this.message = 'String contains an invalid character';
}
E.prototype = new Error;
E.prototype.code = 5;
E.prototype.name = 'InvalidCharacterError';

function btoa(input) {
  var str = String(input);
  var output = '';
  for (
    // initialize result and counter
    var block, charCode, idx = 0, map = chars;
    // if the next str index does not exist:
    //   change the mapping table to "="
    //   check if d has no fractional digits
    str.charAt(idx | 0) || (map = '=', idx % 1);
    // "8 - idx % 1 * 8" generates the sequence 2, 4, 6, 8
    output += map.charAt(63 & block >> 8 - idx % 1 * 8)
  ) {
    charCode = str.charCodeAt(idx += 3 / 4);
    if (charCode > 0xFF) {
      throw new E();
    }
    block = block << 8 | charCode;
  }
  return output;
}

module.exports = btoa;


/***/ }),
/* 165 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

module.exports = (
  utils.isStandardBrowserEnv() ?

  // Standard browser envs support document.cookie
  (function standardBrowserEnv() {
    return {
      write: function write(name, value, expires, path, domain, secure) {
        var cookie = [];
        cookie.push(name + '=' + encodeURIComponent(value));

        if (utils.isNumber(expires)) {
          cookie.push('expires=' + new Date(expires).toGMTString());
        }

        if (utils.isString(path)) {
          cookie.push('path=' + path);
        }

        if (utils.isString(domain)) {
          cookie.push('domain=' + domain);
        }

        if (secure === true) {
          cookie.push('secure');
        }

        document.cookie = cookie.join('; ');
      },

      read: function read(name) {
        var match = document.cookie.match(new RegExp('(^|;\\s*)(' + name + ')=([^;]*)'));
        return (match ? decodeURIComponent(match[3]) : null);
      },

      remove: function remove(name) {
        this.write(name, '', Date.now() - 86400000);
      }
    };
  })() :

  // Non standard browser env (web workers, react-native) lack needed support.
  (function nonStandardBrowserEnv() {
    return {
      write: function write() {},
      read: function read() { return null; },
      remove: function remove() {}
    };
  })()
);


/***/ }),
/* 166 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

function InterceptorManager() {
  this.handlers = [];
}

/**
 * Add a new interceptor to the stack
 *
 * @param {Function} fulfilled The function to handle `then` for a `Promise`
 * @param {Function} rejected The function to handle `reject` for a `Promise`
 *
 * @return {Number} An ID used to remove interceptor later
 */
InterceptorManager.prototype.use = function use(fulfilled, rejected) {
  this.handlers.push({
    fulfilled: fulfilled,
    rejected: rejected
  });
  return this.handlers.length - 1;
};

/**
 * Remove an interceptor from the stack
 *
 * @param {Number} id The ID that was returned by `use`
 */
InterceptorManager.prototype.eject = function eject(id) {
  if (this.handlers[id]) {
    this.handlers[id] = null;
  }
};

/**
 * Iterate over all the registered interceptors
 *
 * This method is particularly useful for skipping over any
 * interceptors that may have become `null` calling `eject`.
 *
 * @param {Function} fn The function to call for each interceptor
 */
InterceptorManager.prototype.forEach = function forEach(fn) {
  utils.forEach(this.handlers, function forEachHandler(h) {
    if (h !== null) {
      fn(h);
    }
  });
};

module.exports = InterceptorManager;


/***/ }),
/* 167 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);
var transformData = __webpack_require__(168);
var isCancel = __webpack_require__(80);
var defaults = __webpack_require__(45);
var isAbsoluteURL = __webpack_require__(169);
var combineURLs = __webpack_require__(170);

/**
 * Throws a `Cancel` if cancellation has been requested.
 */
function throwIfCancellationRequested(config) {
  if (config.cancelToken) {
    config.cancelToken.throwIfRequested();
  }
}

/**
 * Dispatch a request to the server using the configured adapter.
 *
 * @param {object} config The config that is to be used for the request
 * @returns {Promise} The Promise to be fulfilled
 */
module.exports = function dispatchRequest(config) {
  throwIfCancellationRequested(config);

  // Support baseURL config
  if (config.baseURL && !isAbsoluteURL(config.url)) {
    config.url = combineURLs(config.baseURL, config.url);
  }

  // Ensure headers exist
  config.headers = config.headers || {};

  // Transform request data
  config.data = transformData(
    config.data,
    config.headers,
    config.transformRequest
  );

  // Flatten headers
  config.headers = utils.merge(
    config.headers.common || {},
    config.headers[config.method] || {},
    config.headers || {}
  );

  utils.forEach(
    ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],
    function cleanHeaderConfig(method) {
      delete config.headers[method];
    }
  );

  var adapter = config.adapter || defaults.adapter;

  return adapter(config).then(function onAdapterResolution(response) {
    throwIfCancellationRequested(config);

    // Transform response data
    response.data = transformData(
      response.data,
      response.headers,
      config.transformResponse
    );

    return response;
  }, function onAdapterRejection(reason) {
    if (!isCancel(reason)) {
      throwIfCancellationRequested(config);

      // Transform response data
      if (reason && reason.response) {
        reason.response.data = transformData(
          reason.response.data,
          reason.response.headers,
          config.transformResponse
        );
      }
    }

    return Promise.reject(reason);
  });
};


/***/ }),
/* 168 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var utils = __webpack_require__(10);

/**
 * Transform the data for a request or a response
 *
 * @param {Object|String} data The data to be transformed
 * @param {Array} headers The headers for the request or response
 * @param {Array|Function} fns A single function or Array of functions
 * @returns {*} The resulting transformed data
 */
module.exports = function transformData(data, headers, fns) {
  /*eslint no-param-reassign:0*/
  utils.forEach(fns, function transform(fn) {
    data = fn(data, headers);
  });

  return data;
};


/***/ }),
/* 169 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Determines whether the specified URL is absolute
 *
 * @param {string} url The URL to test
 * @returns {boolean} True if the specified URL is absolute, otherwise false
 */
module.exports = function isAbsoluteURL(url) {
  // A URL is considered absolute if it begins with "<scheme>://" or "//" (protocol-relative URL).
  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed
  // by any combination of letters, digits, plus, period, or hyphen.
  return /^([a-z][a-z\d\+\-\.]*:)?\/\//i.test(url);
};


/***/ }),
/* 170 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Creates a new URL by combining the specified URLs
 *
 * @param {string} baseURL The base URL
 * @param {string} relativeURL The relative URL
 * @returns {string} The combined URL
 */
module.exports = function combineURLs(baseURL, relativeURL) {
  return relativeURL
    ? baseURL.replace(/\/+$/, '') + '/' + relativeURL.replace(/^\/+/, '')
    : baseURL;
};


/***/ }),
/* 171 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var Cancel = __webpack_require__(81);

/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 *
 * @class
 * @param {Function} executor The executor function.
 */
function CancelToken(executor) {
  if (typeof executor !== 'function') {
    throw new TypeError('executor must be a function.');
  }

  var resolvePromise;
  this.promise = new Promise(function promiseExecutor(resolve) {
    resolvePromise = resolve;
  });

  var token = this;
  executor(function cancel(message) {
    if (token.reason) {
      // Cancellation has already been requested
      return;
    }

    token.reason = new Cancel(message);
    resolvePromise(token.reason);
  });
}

/**
 * Throws a `Cancel` if cancellation has been requested.
 */
CancelToken.prototype.throwIfRequested = function throwIfRequested() {
  if (this.reason) {
    throw this.reason;
  }
};

/**
 * Returns an object that contains a new `CancelToken` and a function that, when called,
 * cancels the `CancelToken`.
 */
CancelToken.source = function source() {
  var cancel;
  var token = new CancelToken(function executor(c) {
    cancel = c;
  });
  return {
    token: token,
    cancel: cancel
  };
};

module.exports = CancelToken;


/***/ }),
/* 172 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Syntactic sugar for invoking a function and expanding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(x, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this example can be re-written.
 *
 *  ```js
 *  spread(function(x, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 * @returns {Function}
 */
module.exports = function spread(callback) {
  return function wrap(arr) {
    return callback.apply(null, arr);
  };
};


/***/ }),
/* 173 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ResponseContainer = exports.Items = exports.ItemList = undefined;

var _react = __webpack_require__(1);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(41);

var _actions = __webpack_require__(82);

var _reactVirtualized = __webpack_require__(174);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var ItemList = exports.ItemList = function ItemList(_ref) {
  var items = _ref.items,
      activeIndex = _ref.activeIndex;

  var rowRenderer = function rowRenderer(_ref2) {
    var index = _ref2.index,
        isScrolling = _ref2.isScrolling,
        key = _ref2.key,
        style = _ref2.style;

    var item = items[index];
    return _react2.default.createElement(ItemContainer, _defineProperty({ key: item.key, item: item, style: style }, 'key', key));
  };

  return _react2.default.createElement(_reactVirtualized.List, {
    style: { outline: 'none' },
    scrollToIndex: activeIndex,
    height: 400,
    width: 200,
    rowHeight: function rowHeight(_ref3) {
      var index = _ref3.index;
      return items[index].input ? 90 : 40;
    },
    rowCount: items.length,
    overscanRowCount: 10,
    rowRenderer: rowRenderer
  });
};

var Items = exports.Items = function Items(_ref4) {
  var items = _ref4.items;
  return _react2.default.createElement(
    'ul',
    null,
    items.map(function (item) {
      return _react2.default.createElement(ItemContainer, { key: item.key, item: item });
    })
  );
};

var Item = function Item(_ref5) {
  var item = _ref5.item,
      dispatch = _ref5.dispatch,
      interview = _ref5.interview,
      style = _ref5.style,
      key = _ref5.key;

  var isActive = item.key === interview.activeKey;
  var input = item.input;
  if (input === 'integer') {
    input = 'number';
  }

  return _react2.default.createElement(
    'div',
    {
      onClick: function onClick() {
        return dispatch((0, _actions.setActiveItem)(item.key));
      },
      style: style,
      key: key
    },
    _react2.default.createElement(
      'button',
      {
        onClick: function onClick() {
          return dispatch((0, _actions.setActiveItem)(item.key));
        },
        style: { backgroundColor: isActive ? 'grey' : '' }
      },
      item.key
    )
  );
};

var ItemContainer = (0, _reactRedux.connect)(function (state) {
  return state;
})(Item);

var Response = function Response(_ref6) {
  var items = _ref6.items,
      dispatch = _ref6.dispatch,
      interview = _ref6.interview;

  var item = items.find(function (item) {
    return item.key === interview.activeKey;
  });
  return _react2.default.createElement(
    'div',
    { key: item.key },
    _react2.default.createElement(
      'h4',
      null,
      item.key
    ),
    item.description,
    _react2.default.createElement('input', {
      type: item.input,
      onChange: function onChange(event) {
        return dispatch((0, _actions.setResponse)(item.key, event.target.value));
      },
      defaultValue: interview.responses[item.key] || '',
      autoFocus: true
    })
  );
};

var ResponseContainer = exports.ResponseContainer = (0, _reactRedux.connect)(function (state) {
  return state;
})(Response);

/***/ }),
/* 174 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ArrowKeyStepper__ = __webpack_require__(175);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "ArrowKeyStepper", function() { return __WEBPACK_IMPORTED_MODULE_0__ArrowKeyStepper__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__AutoSizer__ = __webpack_require__(225);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "AutoSizer", function() { return __WEBPACK_IMPORTED_MODULE_1__AutoSizer__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__CellMeasurer__ = __webpack_require__(101);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "CellMeasurer", function() { return __WEBPACK_IMPORTED_MODULE_2__CellMeasurer__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "CellMeasurerCache", function() { return __WEBPACK_IMPORTED_MODULE_2__CellMeasurer__["b"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__Collection__ = __webpack_require__(227);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Collection", function() { return __WEBPACK_IMPORTED_MODULE_3__Collection__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__ColumnSizer__ = __webpack_require__(234);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "ColumnSizer", function() { return __WEBPACK_IMPORTED_MODULE_4__ColumnSizer__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__Grid__ = __webpack_require__(9);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "accessibilityOverscanIndicesGetter", function() { return __WEBPACK_IMPORTED_MODULE_5__Grid__["accessibilityOverscanIndicesGetter"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultCellRangeRenderer", function() { return __WEBPACK_IMPORTED_MODULE_5__Grid__["defaultCellRangeRenderer"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultOverscanIndicesGetter", function() { return __WEBPACK_IMPORTED_MODULE_5__Grid__["defaultOverscanIndicesGetter"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Grid", function() { return __WEBPACK_IMPORTED_MODULE_5__Grid__["Grid"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6__InfiniteLoader__ = __webpack_require__(236);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "InfiniteLoader", function() { return __WEBPACK_IMPORTED_MODULE_6__InfiniteLoader__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__List__ = __webpack_require__(238);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "List", function() { return __WEBPACK_IMPORTED_MODULE_7__List__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__Masonry__ = __webpack_require__(242);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "createMasonryCellPositioner", function() { return __WEBPACK_IMPORTED_MODULE_8__Masonry__["b"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Masonry", function() { return __WEBPACK_IMPORTED_MODULE_8__Masonry__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__MultiGrid__ = __webpack_require__(255);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "MultiGrid", function() { return __WEBPACK_IMPORTED_MODULE_9__MultiGrid__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__ScrollSync__ = __webpack_require__(258);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "ScrollSync", function() { return __WEBPACK_IMPORTED_MODULE_10__ScrollSync__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_11__Table__ = __webpack_require__(260);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "createTableMultiSort", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["e"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultTableCellDataGetter", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["f"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultTableCellRenderer", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["g"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultTableHeaderRenderer", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["h"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultTableHeaderRowRenderer", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["i"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "defaultTableRowRenderer", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["j"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Table", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["d"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Column", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "SortDirection", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["b"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "SortIndicator", function() { return __WEBPACK_IMPORTED_MODULE_11__Table__["c"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_12__WindowScroller__ = __webpack_require__(263);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "WindowScroller", function() { return __WEBPACK_IMPORTED_MODULE_12__WindowScroller__["a"]; });














/***/ }),
/* 175 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ArrowKeyStepper__ = __webpack_require__(83);
/* unused harmony reexport default */
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__ArrowKeyStepper__["a"]; });






/***/ }),
/* 176 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(177);
module.exports = __webpack_require__(8).Object.getPrototypeOf;


/***/ }),
/* 177 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.9 Object.getPrototypeOf(O)
var toObject = __webpack_require__(34);
var $getPrototypeOf = __webpack_require__(84);

__webpack_require__(50)('getPrototypeOf', function () {
  return function getPrototypeOf(it) {
    return $getPrototypeOf(toObject(it));
  };
});


/***/ }),
/* 178 */
/***/ (function(module, exports) {

module.exports = function (it) {
  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
  return it;
};


/***/ }),
/* 179 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(180), __esModule: true };

/***/ }),
/* 180 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(181);
var $Object = __webpack_require__(8).Object;
module.exports = function defineProperty(it, key, desc) {
  return $Object.defineProperty(it, key, desc);
};


/***/ }),
/* 181 */
/***/ (function(module, exports, __webpack_require__) {

var $export = __webpack_require__(15);
// 19.1.2.4 / 15.2.3.6 Object.defineProperty(O, P, Attributes)
$export($export.S + $export.F * !__webpack_require__(17), 'Object', { defineProperty: __webpack_require__(16).f });


/***/ }),
/* 182 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(183), __esModule: true };

/***/ }),
/* 183 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(52);
__webpack_require__(59);
module.exports = __webpack_require__(60).f('iterator');


/***/ }),
/* 184 */
/***/ (function(module, exports, __webpack_require__) {

var toInteger = __webpack_require__(53);
var defined = __webpack_require__(47);
// true  -> String#at
// false -> String#codePointAt
module.exports = function (TO_STRING) {
  return function (that, pos) {
    var s = String(defined(that));
    var i = toInteger(pos);
    var l = s.length;
    var a, b;
    if (i < 0 || i >= l) return TO_STRING ? '' : undefined;
    a = s.charCodeAt(i);
    return a < 0xd800 || a > 0xdbff || i + 1 === l || (b = s.charCodeAt(i + 1)) < 0xdc00 || b > 0xdfff
      ? TO_STRING ? s.charAt(i) : a
      : TO_STRING ? s.slice(i, i + 2) : (a - 0xd800 << 10) + (b - 0xdc00) + 0x10000;
  };
};


/***/ }),
/* 185 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var create = __webpack_require__(55);
var descriptor = __webpack_require__(36);
var setToStringTag = __webpack_require__(58);
var IteratorPrototype = {};

// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
__webpack_require__(19)(IteratorPrototype, __webpack_require__(12)('iterator'), function () { return this; });

module.exports = function (Constructor, NAME, next) {
  Constructor.prototype = create(IteratorPrototype, { next: descriptor(1, next) });
  setToStringTag(Constructor, NAME + ' Iterator');
};


/***/ }),
/* 186 */
/***/ (function(module, exports, __webpack_require__) {

var dP = __webpack_require__(16);
var anObject = __webpack_require__(20);
var getKeys = __webpack_require__(27);

module.exports = __webpack_require__(17) ? Object.defineProperties : function defineProperties(O, Properties) {
  anObject(O);
  var keys = getKeys(Properties);
  var length = keys.length;
  var i = 0;
  var P;
  while (length > i) dP.f(O, P = keys[i++], Properties[P]);
  return O;
};


/***/ }),
/* 187 */
/***/ (function(module, exports, __webpack_require__) {

// false -> Array#indexOf
// true  -> Array#includes
var toIObject = __webpack_require__(18);
var toLength = __webpack_require__(188);
var toAbsoluteIndex = __webpack_require__(189);
module.exports = function (IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIObject($this);
    var length = toLength(O.length);
    var index = toAbsoluteIndex(fromIndex, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare
    if (IS_INCLUDES && el != el) while (length > index) {
      value = O[index++];
      // eslint-disable-next-line no-self-compare
      if (value != value) return true;
    // Array#indexOf ignores holes, Array#includes - not
    } else for (;length > index; index++) if (IS_INCLUDES || index in O) {
      if (O[index] === el) return IS_INCLUDES || index || 0;
    } return !IS_INCLUDES && -1;
  };
};


/***/ }),
/* 188 */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.15 ToLength
var toInteger = __webpack_require__(53);
var min = Math.min;
module.exports = function (it) {
  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
};


/***/ }),
/* 189 */
/***/ (function(module, exports, __webpack_require__) {

var toInteger = __webpack_require__(53);
var max = Math.max;
var min = Math.min;
module.exports = function (index, length) {
  index = toInteger(index);
  return index < 0 ? max(index + length, 0) : min(index, length);
};


/***/ }),
/* 190 */
/***/ (function(module, exports, __webpack_require__) {

var document = __webpack_require__(13).document;
module.exports = document && document.documentElement;


/***/ }),
/* 191 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var addToUnscopables = __webpack_require__(192);
var step = __webpack_require__(193);
var Iterators = __webpack_require__(26);
var toIObject = __webpack_require__(18);

// 22.1.3.4 Array.prototype.entries()
// 22.1.3.13 Array.prototype.keys()
// 22.1.3.29 Array.prototype.values()
// 22.1.3.30 Array.prototype[@@iterator]()
module.exports = __webpack_require__(89)(Array, 'Array', function (iterated, kind) {
  this._t = toIObject(iterated); // target
  this._i = 0;                   // next index
  this._k = kind;                // kind
// 22.1.5.2.1 %ArrayIteratorPrototype%.next()
}, function () {
  var O = this._t;
  var kind = this._k;
  var index = this._i++;
  if (!O || index >= O.length) {
    this._t = undefined;
    return step(1);
  }
  if (kind == 'keys') return step(0, index);
  if (kind == 'values') return step(0, O[index]);
  return step(0, [index, O[index]]);
}, 'values');

// argumentsList[@@iterator] is %ArrayProto_values% (9.4.4.6, 9.4.4.7)
Iterators.Arguments = Iterators.Array;

addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');


/***/ }),
/* 192 */
/***/ (function(module, exports) {

module.exports = function () { /* empty */ };


/***/ }),
/* 193 */
/***/ (function(module, exports) {

module.exports = function (done, value) {
  return { value: value, done: !!done };
};


/***/ }),
/* 194 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(195), __esModule: true };

/***/ }),
/* 195 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(196);
__webpack_require__(201);
__webpack_require__(202);
__webpack_require__(203);
module.exports = __webpack_require__(8).Symbol;


/***/ }),
/* 196 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// ECMAScript 6 symbols shim
var global = __webpack_require__(13);
var has = __webpack_require__(14);
var DESCRIPTORS = __webpack_require__(17);
var $export = __webpack_require__(15);
var redefine = __webpack_require__(90);
var META = __webpack_require__(197).KEY;
var $fails = __webpack_require__(22);
var shared = __webpack_require__(49);
var setToStringTag = __webpack_require__(58);
var uid = __webpack_require__(35);
var wks = __webpack_require__(12);
var wksExt = __webpack_require__(60);
var wksDefine = __webpack_require__(61);
var enumKeys = __webpack_require__(198);
var isArray = __webpack_require__(199);
var anObject = __webpack_require__(20);
var isObject = __webpack_require__(21);
var toIObject = __webpack_require__(18);
var toPrimitive = __webpack_require__(51);
var createDesc = __webpack_require__(36);
var _create = __webpack_require__(55);
var gOPNExt = __webpack_require__(200);
var $GOPD = __webpack_require__(63);
var $DP = __webpack_require__(16);
var $keys = __webpack_require__(27);
var gOPD = $GOPD.f;
var dP = $DP.f;
var gOPN = gOPNExt.f;
var $Symbol = global.Symbol;
var $JSON = global.JSON;
var _stringify = $JSON && $JSON.stringify;
var PROTOTYPE = 'prototype';
var HIDDEN = wks('_hidden');
var TO_PRIMITIVE = wks('toPrimitive');
var isEnum = {}.propertyIsEnumerable;
var SymbolRegistry = shared('symbol-registry');
var AllSymbols = shared('symbols');
var OPSymbols = shared('op-symbols');
var ObjectProto = Object[PROTOTYPE];
var USE_NATIVE = typeof $Symbol == 'function';
var QObject = global.QObject;
// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
var setter = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;

// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
var setSymbolDesc = DESCRIPTORS && $fails(function () {
  return _create(dP({}, 'a', {
    get: function () { return dP(this, 'a', { value: 7 }).a; }
  })).a != 7;
}) ? function (it, key, D) {
  var protoDesc = gOPD(ObjectProto, key);
  if (protoDesc) delete ObjectProto[key];
  dP(it, key, D);
  if (protoDesc && it !== ObjectProto) dP(ObjectProto, key, protoDesc);
} : dP;

var wrap = function (tag) {
  var sym = AllSymbols[tag] = _create($Symbol[PROTOTYPE]);
  sym._k = tag;
  return sym;
};

var isSymbol = USE_NATIVE && typeof $Symbol.iterator == 'symbol' ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  return it instanceof $Symbol;
};

var $defineProperty = function defineProperty(it, key, D) {
  if (it === ObjectProto) $defineProperty(OPSymbols, key, D);
  anObject(it);
  key = toPrimitive(key, true);
  anObject(D);
  if (has(AllSymbols, key)) {
    if (!D.enumerable) {
      if (!has(it, HIDDEN)) dP(it, HIDDEN, createDesc(1, {}));
      it[HIDDEN][key] = true;
    } else {
      if (has(it, HIDDEN) && it[HIDDEN][key]) it[HIDDEN][key] = false;
      D = _create(D, { enumerable: createDesc(0, false) });
    } return setSymbolDesc(it, key, D);
  } return dP(it, key, D);
};
var $defineProperties = function defineProperties(it, P) {
  anObject(it);
  var keys = enumKeys(P = toIObject(P));
  var i = 0;
  var l = keys.length;
  var key;
  while (l > i) $defineProperty(it, key = keys[i++], P[key]);
  return it;
};
var $create = function create(it, P) {
  return P === undefined ? _create(it) : $defineProperties(_create(it), P);
};
var $propertyIsEnumerable = function propertyIsEnumerable(key) {
  var E = isEnum.call(this, key = toPrimitive(key, true));
  if (this === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return false;
  return E || !has(this, key) || !has(AllSymbols, key) || has(this, HIDDEN) && this[HIDDEN][key] ? E : true;
};
var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(it, key) {
  it = toIObject(it);
  key = toPrimitive(key, true);
  if (it === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return;
  var D = gOPD(it, key);
  if (D && has(AllSymbols, key) && !(has(it, HIDDEN) && it[HIDDEN][key])) D.enumerable = true;
  return D;
};
var $getOwnPropertyNames = function getOwnPropertyNames(it) {
  var names = gOPN(toIObject(it));
  var result = [];
  var i = 0;
  var key;
  while (names.length > i) {
    if (!has(AllSymbols, key = names[i++]) && key != HIDDEN && key != META) result.push(key);
  } return result;
};
var $getOwnPropertySymbols = function getOwnPropertySymbols(it) {
  var IS_OP = it === ObjectProto;
  var names = gOPN(IS_OP ? OPSymbols : toIObject(it));
  var result = [];
  var i = 0;
  var key;
  while (names.length > i) {
    if (has(AllSymbols, key = names[i++]) && (IS_OP ? has(ObjectProto, key) : true)) result.push(AllSymbols[key]);
  } return result;
};

// 19.4.1.1 Symbol([description])
if (!USE_NATIVE) {
  $Symbol = function Symbol() {
    if (this instanceof $Symbol) throw TypeError('Symbol is not a constructor!');
    var tag = uid(arguments.length > 0 ? arguments[0] : undefined);
    var $set = function (value) {
      if (this === ObjectProto) $set.call(OPSymbols, value);
      if (has(this, HIDDEN) && has(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
      setSymbolDesc(this, tag, createDesc(1, value));
    };
    if (DESCRIPTORS && setter) setSymbolDesc(ObjectProto, tag, { configurable: true, set: $set });
    return wrap(tag);
  };
  redefine($Symbol[PROTOTYPE], 'toString', function toString() {
    return this._k;
  });

  $GOPD.f = $getOwnPropertyDescriptor;
  $DP.f = $defineProperty;
  __webpack_require__(93).f = gOPNExt.f = $getOwnPropertyNames;
  __webpack_require__(37).f = $propertyIsEnumerable;
  __webpack_require__(62).f = $getOwnPropertySymbols;

  if (DESCRIPTORS && !__webpack_require__(54)) {
    redefine(ObjectProto, 'propertyIsEnumerable', $propertyIsEnumerable, true);
  }

  wksExt.f = function (name) {
    return wrap(wks(name));
  };
}

$export($export.G + $export.W + $export.F * !USE_NATIVE, { Symbol: $Symbol });

for (var es6Symbols = (
  // 19.4.2.2, 19.4.2.3, 19.4.2.4, 19.4.2.6, 19.4.2.8, 19.4.2.9, 19.4.2.10, 19.4.2.11, 19.4.2.12, 19.4.2.13, 19.4.2.14
  'hasInstance,isConcatSpreadable,iterator,match,replace,search,species,split,toPrimitive,toStringTag,unscopables'
).split(','), j = 0; es6Symbols.length > j;)wks(es6Symbols[j++]);

for (var wellKnownSymbols = $keys(wks.store), k = 0; wellKnownSymbols.length > k;) wksDefine(wellKnownSymbols[k++]);

$export($export.S + $export.F * !USE_NATIVE, 'Symbol', {
  // 19.4.2.1 Symbol.for(key)
  'for': function (key) {
    return has(SymbolRegistry, key += '')
      ? SymbolRegistry[key]
      : SymbolRegistry[key] = $Symbol(key);
  },
  // 19.4.2.5 Symbol.keyFor(sym)
  keyFor: function keyFor(sym) {
    if (!isSymbol(sym)) throw TypeError(sym + ' is not a symbol!');
    for (var key in SymbolRegistry) if (SymbolRegistry[key] === sym) return key;
  },
  useSetter: function () { setter = true; },
  useSimple: function () { setter = false; }
});

$export($export.S + $export.F * !USE_NATIVE, 'Object', {
  // 19.1.2.2 Object.create(O [, Properties])
  create: $create,
  // 19.1.2.4 Object.defineProperty(O, P, Attributes)
  defineProperty: $defineProperty,
  // 19.1.2.3 Object.defineProperties(O, Properties)
  defineProperties: $defineProperties,
  // 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
  getOwnPropertyDescriptor: $getOwnPropertyDescriptor,
  // 19.1.2.7 Object.getOwnPropertyNames(O)
  getOwnPropertyNames: $getOwnPropertyNames,
  // 19.1.2.8 Object.getOwnPropertySymbols(O)
  getOwnPropertySymbols: $getOwnPropertySymbols
});

// 24.3.2 JSON.stringify(value [, replacer [, space]])
$JSON && $export($export.S + $export.F * (!USE_NATIVE || $fails(function () {
  var S = $Symbol();
  // MS Edge converts symbol values to JSON as {}
  // WebKit converts symbol values to JSON as null
  // V8 throws on boxed symbols
  return _stringify([S]) != '[null]' || _stringify({ a: S }) != '{}' || _stringify(Object(S)) != '{}';
})), 'JSON', {
  stringify: function stringify(it) {
    var args = [it];
    var i = 1;
    var replacer, $replacer;
    while (arguments.length > i) args.push(arguments[i++]);
    $replacer = replacer = args[1];
    if (!isObject(replacer) && it === undefined || isSymbol(it)) return; // IE8 returns string on undefined
    if (!isArray(replacer)) replacer = function (key, value) {
      if (typeof $replacer == 'function') value = $replacer.call(this, key, value);
      if (!isSymbol(value)) return value;
    };
    args[1] = replacer;
    return _stringify.apply($JSON, args);
  }
});

// 19.4.3.4 Symbol.prototype[@@toPrimitive](hint)
$Symbol[PROTOTYPE][TO_PRIMITIVE] || __webpack_require__(19)($Symbol[PROTOTYPE], TO_PRIMITIVE, $Symbol[PROTOTYPE].valueOf);
// 19.4.3.5 Symbol.prototype[@@toStringTag]
setToStringTag($Symbol, 'Symbol');
// 20.2.1.9 Math[@@toStringTag]
setToStringTag(Math, 'Math', true);
// 24.3.3 JSON[@@toStringTag]
setToStringTag(global.JSON, 'JSON', true);


/***/ }),
/* 197 */
/***/ (function(module, exports, __webpack_require__) {

var META = __webpack_require__(35)('meta');
var isObject = __webpack_require__(21);
var has = __webpack_require__(14);
var setDesc = __webpack_require__(16).f;
var id = 0;
var isExtensible = Object.isExtensible || function () {
  return true;
};
var FREEZE = !__webpack_require__(22)(function () {
  return isExtensible(Object.preventExtensions({}));
});
var setMeta = function (it) {
  setDesc(it, META, { value: {
    i: 'O' + ++id, // object ID
    w: {}          // weak collections IDs
  } });
};
var fastKey = function (it, create) {
  // return primitive with prefix
  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
  if (!has(it, META)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return 'F';
    // not necessary to add metadata
    if (!create) return 'E';
    // add missing metadata
    setMeta(it);
  // return object ID
  } return it[META].i;
};
var getWeak = function (it, create) {
  if (!has(it, META)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return true;
    // not necessary to add metadata
    if (!create) return false;
    // add missing metadata
    setMeta(it);
  // return hash weak collections IDs
  } return it[META].w;
};
// add metadata on freeze-family methods calling
var onFreeze = function (it) {
  if (FREEZE && meta.NEED && isExtensible(it) && !has(it, META)) setMeta(it);
  return it;
};
var meta = module.exports = {
  KEY: META,
  NEED: false,
  fastKey: fastKey,
  getWeak: getWeak,
  onFreeze: onFreeze
};


/***/ }),
/* 198 */
/***/ (function(module, exports, __webpack_require__) {

// all enumerable object keys, includes symbols
var getKeys = __webpack_require__(27);
var gOPS = __webpack_require__(62);
var pIE = __webpack_require__(37);
module.exports = function (it) {
  var result = getKeys(it);
  var getSymbols = gOPS.f;
  if (getSymbols) {
    var symbols = getSymbols(it);
    var isEnum = pIE.f;
    var i = 0;
    var key;
    while (symbols.length > i) if (isEnum.call(it, key = symbols[i++])) result.push(key);
  } return result;
};


/***/ }),
/* 199 */
/***/ (function(module, exports, __webpack_require__) {

// 7.2.2 IsArray(argument)
var cof = __webpack_require__(56);
module.exports = Array.isArray || function isArray(arg) {
  return cof(arg) == 'Array';
};


/***/ }),
/* 200 */
/***/ (function(module, exports, __webpack_require__) {

// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
var toIObject = __webpack_require__(18);
var gOPN = __webpack_require__(93).f;
var toString = {}.toString;

var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
  ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function (it) {
  try {
    return gOPN(it);
  } catch (e) {
    return windowNames.slice();
  }
};

module.exports.f = function getOwnPropertyNames(it) {
  return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : gOPN(toIObject(it));
};


/***/ }),
/* 201 */
/***/ (function(module, exports) {



/***/ }),
/* 202 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(61)('asyncIterator');


/***/ }),
/* 203 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(61)('observable');


/***/ }),
/* 204 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(205), __esModule: true };

/***/ }),
/* 205 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(206);
module.exports = __webpack_require__(8).Object.setPrototypeOf;


/***/ }),
/* 206 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.3.19 Object.setPrototypeOf(O, proto)
var $export = __webpack_require__(15);
$export($export.S, 'Object', { setPrototypeOf: __webpack_require__(207).set });


/***/ }),
/* 207 */
/***/ (function(module, exports, __webpack_require__) {

// Works with __proto__ only. Old v8 can't work with null proto objects.
/* eslint-disable no-proto */
var isObject = __webpack_require__(21);
var anObject = __webpack_require__(20);
var check = function (O, proto) {
  anObject(O);
  if (!isObject(proto) && proto !== null) throw TypeError(proto + ": can't set as prototype!");
};
module.exports = {
  set: Object.setPrototypeOf || ('__proto__' in {} ? // eslint-disable-line
    function (test, buggy, set) {
      try {
        set = __webpack_require__(85)(Function.call, __webpack_require__(63).f(Object.prototype, '__proto__').set, 2);
        set(test, []);
        buggy = !(test instanceof Array);
      } catch (e) { buggy = true; }
      return function setPrototypeOf(O, proto) {
        check(O, proto);
        if (buggy) O.__proto__ = proto;
        else set(O, proto);
        return O;
      };
    }({}, false) : undefined),
  check: check
};


/***/ }),
/* 208 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(209), __esModule: true };

/***/ }),
/* 209 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(210);
var $Object = __webpack_require__(8).Object;
module.exports = function create(P, D) {
  return $Object.create(P, D);
};


/***/ }),
/* 210 */
/***/ (function(module, exports, __webpack_require__) {

var $export = __webpack_require__(15);
// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
$export($export.S, 'Object', { create: __webpack_require__(55) });


/***/ }),
/* 211 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(212), __esModule: true };

/***/ }),
/* 212 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(213);
module.exports = __webpack_require__(8).Object.assign;


/***/ }),
/* 213 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.3.1 Object.assign(target, source)
var $export = __webpack_require__(15);

$export($export.S + $export.F, 'Object', { assign: __webpack_require__(214) });


/***/ }),
/* 214 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// 19.1.2.1 Object.assign(target, source, ...)
var getKeys = __webpack_require__(27);
var gOPS = __webpack_require__(62);
var pIE = __webpack_require__(37);
var toObject = __webpack_require__(34);
var IObject = __webpack_require__(92);
var $assign = Object.assign;

// should work with symbols and should have deterministic property order (V8 bug)
module.exports = !$assign || __webpack_require__(22)(function () {
  var A = {};
  var B = {};
  // eslint-disable-next-line no-undef
  var S = Symbol();
  var K = 'abcdefghijklmnopqrst';
  A[S] = 7;
  K.split('').forEach(function (k) { B[k] = k; });
  return $assign({}, A)[S] != 7 || Object.keys($assign({}, B)).join('') != K;
}) ? function assign(target, source) { // eslint-disable-line no-unused-vars
  var T = toObject(target);
  var aLen = arguments.length;
  var index = 1;
  var getSymbols = gOPS.f;
  var isEnum = pIE.f;
  while (aLen > index) {
    var S = IObject(arguments[index++]);
    var keys = getSymbols ? getKeys(S).concat(getSymbols(S)) : getKeys(S);
    var length = keys.length;
    var j = 0;
    var key;
    while (length > j) if (isEnum.call(S, key = keys[j++])) T[key] = S[key];
  } return T;
} : $assign;


/***/ }),
/* 215 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__);



var babelPluginFlowReactPropTypes_proptype_VisibleCellRange = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_VisibleCellRange || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellSizeGetter = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellSizeGetter || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Alignment = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_Alignment || __webpack_require__(0).any;

/**
 * Just-in-time calculates and caches size and position information for a collection of cells.
 */

var CellSizeAndPositionManager = function () {

  // Used in deferred mode to track which cells have been queued for measurement.

  // Cache of size and position data for cells, mapped by cell index.
  // Note that invalid values may exist in this map so only rely on cells up to this._lastMeasuredIndex
  function CellSizeAndPositionManager(_ref) {
    var cellCount = _ref.cellCount,
        cellSizeGetter = _ref.cellSizeGetter,
        estimatedCellSize = _ref.estimatedCellSize;

    __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default()(this, CellSizeAndPositionManager);

    this._cellSizeAndPositionData = {};
    this._lastMeasuredIndex = -1;
    this._lastBatchedIndex = -1;

    this._cellSizeGetter = cellSizeGetter;
    this._cellCount = cellCount;
    this._estimatedCellSize = estimatedCellSize;
  }

  // Measurements for cells up to this index can be trusted; cells afterward should be estimated.


  __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default()(CellSizeAndPositionManager, [{
    key: 'areOffsetsAdjusted',
    value: function areOffsetsAdjusted() {
      return false;
    }
  }, {
    key: 'configure',
    value: function configure(_ref2) {
      var cellCount = _ref2.cellCount,
          estimatedCellSize = _ref2.estimatedCellSize;

      this._cellCount = cellCount;
      this._estimatedCellSize = estimatedCellSize;
    }
  }, {
    key: 'getCellCount',
    value: function getCellCount() {
      return this._cellCount;
    }
  }, {
    key: 'getEstimatedCellSize',
    value: function getEstimatedCellSize() {
      return this._estimatedCellSize;
    }
  }, {
    key: 'getLastMeasuredIndex',
    value: function getLastMeasuredIndex() {
      return this._lastMeasuredIndex;
    }
  }, {
    key: 'getOffsetAdjustment',
    value: function getOffsetAdjustment() {
      return 0;
    }

    /**
     * This method returns the size and position for the cell at the specified index.
     * It just-in-time calculates (or used cached values) for cells leading up to the index.
     */

  }, {
    key: 'getSizeAndPositionOfCell',
    value: function getSizeAndPositionOfCell(index) {
      if (index < 0 || index >= this._cellCount) {
        throw Error('Requested index ' + index + ' is outside of range 0..' + this._cellCount);
      }

      if (index > this._lastMeasuredIndex) {
        var lastMeasuredCellSizeAndPosition = this.getSizeAndPositionOfLastMeasuredCell();
        var _offset = lastMeasuredCellSizeAndPosition.offset + lastMeasuredCellSizeAndPosition.size;

        for (var i = this._lastMeasuredIndex + 1; i <= index; i++) {
          var _size = this._cellSizeGetter({ index: i });

          // undefined or NaN probably means a logic error in the size getter.
          // null means we're using CellMeasurer and haven't yet measured a given index.
          if (_size === undefined || isNaN(_size)) {
            throw Error('Invalid size returned for cell ' + i + ' of value ' + _size);
          } else if (_size === null) {
            this._cellSizeAndPositionData[i] = {
              offset: _offset,
              size: 0
            };

            this._lastBatchedIndex = index;
          } else {
            this._cellSizeAndPositionData[i] = {
              offset: _offset,
              size: _size
            };

            _offset += _size;

            this._lastMeasuredIndex = index;
          }
        }
      }

      return this._cellSizeAndPositionData[index];
    }
  }, {
    key: 'getSizeAndPositionOfLastMeasuredCell',
    value: function getSizeAndPositionOfLastMeasuredCell() {
      return this._lastMeasuredIndex >= 0 ? this._cellSizeAndPositionData[this._lastMeasuredIndex] : {
        offset: 0,
        size: 0
      };
    }

    /**
     * Total size of all cells being measured.
     * This value will be completely estimated initially.
     * As cells are measured, the estimate will be updated.
     */

  }, {
    key: 'getTotalSize',
    value: function getTotalSize() {
      var lastMeasuredCellSizeAndPosition = this.getSizeAndPositionOfLastMeasuredCell();
      var totalSizeOfMeasuredCells = lastMeasuredCellSizeAndPosition.offset + lastMeasuredCellSizeAndPosition.size;
      var numUnmeasuredCells = this._cellCount - this._lastMeasuredIndex - 1;
      var totalSizeOfUnmeasuredCells = numUnmeasuredCells * this._estimatedCellSize;
      return totalSizeOfMeasuredCells + totalSizeOfUnmeasuredCells;
    }

    /**
     * Determines a new offset that ensures a certain cell is visible, given the current offset.
     * If the cell is already visible then the current offset will be returned.
     * If the current offset is too great or small, it will be adjusted just enough to ensure the specified index is visible.
     *
     * @param align Desired alignment within container; one of "auto" (default), "start", or "end"
     * @param containerSize Size (width or height) of the container viewport
     * @param currentOffset Container's current (x or y) offset
     * @param totalSize Total size (width or height) of all cells
     * @return Offset to use to ensure the specified cell is visible
     */

  }, {
    key: 'getUpdatedOffsetForIndex',
    value: function getUpdatedOffsetForIndex(_ref3) {
      var _ref3$align = _ref3.align,
          align = _ref3$align === undefined ? 'auto' : _ref3$align,
          containerSize = _ref3.containerSize,
          currentOffset = _ref3.currentOffset,
          targetIndex = _ref3.targetIndex;

      if (containerSize <= 0) {
        return 0;
      }

      var datum = this.getSizeAndPositionOfCell(targetIndex);
      var maxOffset = datum.offset;
      var minOffset = maxOffset - containerSize + datum.size;

      var idealOffset = void 0;

      switch (align) {
        case 'start':
          idealOffset = maxOffset;
          break;
        case 'end':
          idealOffset = minOffset;
          break;
        case 'center':
          idealOffset = maxOffset - (containerSize - datum.size) / 2;
          break;
        default:
          idealOffset = Math.max(minOffset, Math.min(maxOffset, currentOffset));
          break;
      }

      var totalSize = this.getTotalSize();

      return Math.max(0, Math.min(totalSize - containerSize, idealOffset));
    }
  }, {
    key: 'getVisibleCellRange',
    value: function getVisibleCellRange(params) {
      var containerSize = params.containerSize,
          offset = params.offset;


      var totalSize = this.getTotalSize();

      if (totalSize === 0) {
        return {};
      }

      var maxOffset = offset + containerSize;
      var start = this._findNearestCell(offset);

      var datum = this.getSizeAndPositionOfCell(start);
      offset = datum.offset + datum.size;

      var stop = start;

      while (offset < maxOffset && stop < this._cellCount - 1) {
        stop++;

        offset += this.getSizeAndPositionOfCell(stop).size;
      }

      return {
        start: start,
        stop: stop
      };
    }

    /**
     * Clear all cached values for cells after the specified index.
     * This method should be called for any cell that has changed its size.
     * It will not immediately perform any calculations; they'll be performed the next time getSizeAndPositionOfCell() is called.
     */

  }, {
    key: 'resetCell',
    value: function resetCell(index) {
      this._lastMeasuredIndex = Math.min(this._lastMeasuredIndex, index - 1);
    }
  }, {
    key: '_binarySearch',
    value: function _binarySearch(high, low, offset) {
      while (low <= high) {
        var middle = low + Math.floor((high - low) / 2);
        var _currentOffset = this.getSizeAndPositionOfCell(middle).offset;

        if (_currentOffset === offset) {
          return middle;
        } else if (_currentOffset < offset) {
          low = middle + 1;
        } else if (_currentOffset > offset) {
          high = middle - 1;
        }
      }

      if (low > 0) {
        return low - 1;
      } else {
        return 0;
      }
    }
  }, {
    key: '_exponentialSearch',
    value: function _exponentialSearch(index, offset) {
      var interval = 1;

      while (index < this._cellCount && this.getSizeAndPositionOfCell(index).offset < offset) {
        index += interval;
        interval *= 2;
      }

      return this._binarySearch(Math.min(index, this._cellCount - 1), Math.floor(index / 2), offset);
    }

    /**
     * Searches for the cell (index) nearest the specified offset.
     *
     * If no exact match is found the next lowest cell index will be returned.
     * This allows partially visible cells (with offsets just before/above the fold) to be visible.
     */

  }, {
    key: '_findNearestCell',
    value: function _findNearestCell(offset) {
      if (isNaN(offset)) {
        throw Error('Invalid offset ' + offset + ' specified');
      }

      // Our search algorithms find the nearest match at or below the specified offset.
      // So make sure the offset is at least 0 or no match will be found.
      offset = Math.max(0, offset);

      var lastMeasuredCellSizeAndPosition = this.getSizeAndPositionOfLastMeasuredCell();
      var lastMeasuredIndex = Math.max(0, this._lastMeasuredIndex);

      if (lastMeasuredCellSizeAndPosition.offset >= offset) {
        // If we've already measured cells within this range just use a binary search as it's faster.
        return this._binarySearch(lastMeasuredIndex, 0, offset);
      } else {
        // If we haven't yet measured this high, fallback to an exponential search with an inner binary search.
        // The exponential search avoids pre-computing sizes for the full set of cells as a binary search would.
        // The overall complexity for this approach is O(log n).
        return this._exponentialSearch(lastMeasuredIndex, offset);
      }
    }
  }]);

  return CellSizeAndPositionManager;
}();

/* harmony default export */ __webpack_exports__["a"] = (CellSizeAndPositionManager);

/***/ }),
/* 216 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return getMaxElementSize; });
var DEFAULT_MAX_ELEMENT_SIZE = 1500000;
var CHROME_MAX_ELEMENT_SIZE = 1.67771e7;

var isBrowser = function isBrowser() {
  return typeof window !== 'undefined';
};

var isChrome = function isChrome() {
  return !!window.chrome && !!window.chrome.webstore;
};

var getMaxElementSize = function getMaxElementSize() {
  if (isBrowser()) {
    if (isChrome()) {
      return CHROME_MAX_ELEMENT_SIZE;
    }
  }
  return DEFAULT_MAX_ELEMENT_SIZE;
};

/***/ }),
/* 217 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return raf; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return caf; });


// Properly handle server-side rendering.
var win = void 0;

if (typeof window !== 'undefined') {
  win = window;
} else if (typeof self !== 'undefined') {
  win = self;
} else {
  win = {};
}

// requestAnimationFrame() shim by Paul Irish
// http://paulirish.com/2011/requestanimationframe-for-smart-animating/
var request = win.requestAnimationFrame || win.webkitRequestAnimationFrame || win.mozRequestAnimationFrame || win.oRequestAnimationFrame || win.msRequestAnimationFrame || function (callback) {
  return win.setTimeout(callback, 1000 / 60);
};

var cancel = win.cancelAnimationFrame || win.webkitCancelAnimationFrame || win.mozCancelAnimationFrame || win.oCancelAnimationFrame || win.msCancelAnimationFrame || function (id) {
  win.clearTimeout(id);
};

var raf = request;
var caf = cancel;

/***/ }),
/* 218 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = calculateSizeAndPositionDataAndUpdateScrollOffset;


function calculateSizeAndPositionDataAndUpdateScrollOffset(_ref) {
  var cellCount = _ref.cellCount,
      cellSize = _ref.cellSize,
      computeMetadataCallback = _ref.computeMetadataCallback,
      computeMetadataCallbackProps = _ref.computeMetadataCallbackProps,
      nextCellsCount = _ref.nextCellsCount,
      nextCellSize = _ref.nextCellSize,
      nextScrollToIndex = _ref.nextScrollToIndex,
      scrollToIndex = _ref.scrollToIndex,
      updateScrollOffsetForScrollToIndex = _ref.updateScrollOffsetForScrollToIndex;

  // Don't compare cell sizes if they are functions because inline functions would cause infinite loops.
  // In that event users should use the manual recompute methods to inform of changes.
  if (cellCount !== nextCellsCount || (typeof cellSize === 'number' || typeof nextCellSize === 'number') && cellSize !== nextCellSize) {
    computeMetadataCallback(computeMetadataCallbackProps);

    // Updated cell metadata may have hidden the previous scrolled-to item.
    // In this case we should also update the scrollTop to ensure it stays visible.
    if (scrollToIndex >= 0 && scrollToIndex === nextScrollToIndex) {
      updateScrollOffsetForScrollToIndex();
    }
  }
}

/**
 * Helper method that determines when to recalculate row or column metadata.
 */

/***/ }),
/* 219 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(220);
module.exports = __webpack_require__(8).Object.keys;


/***/ }),
/* 220 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.14 Object.keys(O)
var toObject = __webpack_require__(34);
var $keys = __webpack_require__(27);

__webpack_require__(50)('keys', function () {
  return function keys(it) {
    return $keys(toObject(it));
  };
});


/***/ }),
/* 221 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = updateScrollIndexHelper;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ScalingCellSizeAndPositionManager_js__ = __webpack_require__(64);
var babelPluginFlowReactPropTypes_proptype_CellSize = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_CellSize || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Alignment = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_Alignment || __webpack_require__(0).any;



/**
 * Helper function that determines when to update scroll offsets to ensure that a scroll-to-index remains visible.
 * This function also ensures that the scroll ofset isn't past the last column/row of cells.
 */

function updateScrollIndexHelper(_ref) {
  var cellSize = _ref.cellSize,
      cellSizeAndPositionManager = _ref.cellSizeAndPositionManager,
      previousCellsCount = _ref.previousCellsCount,
      previousCellSize = _ref.previousCellSize,
      previousScrollToAlignment = _ref.previousScrollToAlignment,
      previousScrollToIndex = _ref.previousScrollToIndex,
      previousSize = _ref.previousSize,
      scrollOffset = _ref.scrollOffset,
      scrollToAlignment = _ref.scrollToAlignment,
      scrollToIndex = _ref.scrollToIndex,
      size = _ref.size,
      sizeJustIncreasedFromZero = _ref.sizeJustIncreasedFromZero,
      updateScrollIndexCallback = _ref.updateScrollIndexCallback;

  var cellCount = cellSizeAndPositionManager.getCellCount();
  var hasScrollToIndex = scrollToIndex >= 0 && scrollToIndex < cellCount;
  var sizeHasChanged = size !== previousSize || sizeJustIncreasedFromZero || !previousCellSize || typeof cellSize === 'number' && cellSize !== previousCellSize;

  // If we have a new scroll target OR if height/row-height has changed,
  // We should ensure that the scroll target is visible.
  if (hasScrollToIndex && (sizeHasChanged || scrollToAlignment !== previousScrollToAlignment || scrollToIndex !== previousScrollToIndex)) {
    updateScrollIndexCallback(scrollToIndex);

    // If we don't have a selected item but list size or number of children have decreased,
    // Make sure we aren't scrolled too far past the current content.
  } else if (!hasScrollToIndex && cellCount > 0 && (size < previousSize || cellCount < previousCellsCount)) {
    // We need to ensure that the current scroll offset is still within the collection's range.
    // To do this, we don't need to measure everything; CellMeasurer would perform poorly.
    // Just check to make sure we're still okay.
    // Only adjust the scroll position if we've scrolled below the last set of rows.
    if (scrollOffset > cellSizeAndPositionManager.getTotalSize() - size) {
      updateScrollIndexCallback(cellCount - 1);
    }
  }
}

/***/ }),
/* 222 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = !!(typeof window !== 'undefined' && window.document && window.document.createElement);
module.exports = exports['default'];

/***/ }),
/* 223 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export SCROLL_DIRECTION_BACKWARD */
/* unused harmony export SCROLL_DIRECTION_FORWARD */
/* unused harmony export SCROLL_DIRECTION_HORIZONTAL */
/* unused harmony export SCROLL_DIRECTION_VERTICAL */
/* harmony export (immutable) */ __webpack_exports__["a"] = defaultOverscanIndicesGetter;
var babelPluginFlowReactPropTypes_proptype_OverscanIndices = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_OverscanIndices || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams = __webpack_require__(2).babelPluginFlowReactPropTypes_proptype_OverscanIndicesGetterParams || __webpack_require__(0).any;

var SCROLL_DIRECTION_BACKWARD = -1;
var SCROLL_DIRECTION_FORWARD = 1;

var SCROLL_DIRECTION_HORIZONTAL = 'horizontal';
var SCROLL_DIRECTION_VERTICAL = 'vertical';

/**
 * Calculates the number of cells to overscan before and after a specified range.
 * This function ensures that overscanning doesn't exceed the available cells.
 */

function defaultOverscanIndicesGetter(_ref) {
  var cellCount = _ref.cellCount,
      overscanCellsCount = _ref.overscanCellsCount,
      scrollDirection = _ref.scrollDirection,
      startIndex = _ref.startIndex,
      stopIndex = _ref.stopIndex;

  // Make sure we render at least 1 cell extra before and after (except near boundaries)
  // This is necessary in order to support keyboard navigation (TAB/SHIFT+TAB) in some cases
  // For more info see issues #625
  overscanCellsCount = Math.max(1, overscanCellsCount);

  if (scrollDirection === SCROLL_DIRECTION_FORWARD) {
    return {
      overscanStartIndex: Math.max(0, startIndex - 1),
      overscanStopIndex: Math.min(cellCount - 1, stopIndex + overscanCellsCount)
    };
  } else {
    return {
      overscanStartIndex: Math.max(0, startIndex - overscanCellsCount),
      overscanStopIndex: Math.min(cellCount - 1, stopIndex + 1)
    };
  }
}

/***/ }),
/* 224 */
/***/ (function(module, exports, __webpack_require__) {

var babelPluginFlowReactPropTypes_proptype_ScrollIndices =  false ? null : {
  scrollToColumn: __webpack_require__(0).number.isRequired,
  scrollToRow: __webpack_require__(0).number.isRequired
};
if (true) Object.defineProperty(exports, "babelPluginFlowReactPropTypes_proptype_ScrollIndices", {
  value: babelPluginFlowReactPropTypes_proptype_ScrollIndices,
  configurable: true
});

/***/ }),
/* 225 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__AutoSizer__ = __webpack_require__(99);
/* unused harmony reexport default */
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__AutoSizer__["a"]; });



/***/ }),
/* 226 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react_dom__ = __webpack_require__(25);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react_dom___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react_dom__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__CellMeasurerCache_js__ = __webpack_require__(102);









/**
 * Wraps a cell and measures its rendered content.
 * Measurements are stored in a per-cell cache.
 * Cached-content is not be re-measured.
 */
var CellMeasurer = function (_React$PureComponent) {
  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default()(CellMeasurer, _React$PureComponent);

  function CellMeasurer() {
    var _ref;

    var _temp, _this, _ret;

    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, CellMeasurer);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(this, (_ref = CellMeasurer.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(CellMeasurer)).call.apply(_ref, [this].concat(args))), _this), _this._measure = function () {
      var _this$props = _this.props,
          cache = _this$props.cache,
          _this$props$columnInd = _this$props.columnIndex,
          columnIndex = _this$props$columnInd === undefined ? 0 : _this$props$columnInd,
          parent = _this$props.parent,
          _this$props$rowIndex = _this$props.rowIndex,
          rowIndex = _this$props$rowIndex === undefined ? _this.props.index || 0 : _this$props$rowIndex;

      var _this$_getCellMeasure = _this._getCellMeasurements(),
          height = _this$_getCellMeasure.height,
          width = _this$_getCellMeasure.width;

      if (height !== cache.getHeight(rowIndex, columnIndex) || width !== cache.getWidth(rowIndex, columnIndex)) {
        cache.set(rowIndex, columnIndex, width, height);

        if (parent && typeof parent.recomputeGridSize === 'function') {
          parent.recomputeGridSize({
            columnIndex: columnIndex,
            rowIndex: rowIndex
          });
        }
      }
    }, _temp), __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(_this, _ret);
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(CellMeasurer, [{
    key: 'componentDidMount',
    value: function componentDidMount() {
      this._maybeMeasureCell();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      this._maybeMeasureCell();
    }
  }, {
    key: 'render',
    value: function render() {
      var children = this.props.children;


      return typeof children === 'function' ? children({ measure: this._measure }) : children;
    }
  }, {
    key: '_getCellMeasurements',
    value: function _getCellMeasurements() {
      var cache = this.props.cache;


      var node = Object(__WEBPACK_IMPORTED_MODULE_6_react_dom__["findDOMNode"])(this);

      // TODO Check for a bad combination of fixedWidth and missing numeric width or vice versa with height

      if (node && node.ownerDocument && node.ownerDocument.defaultView && node instanceof node.ownerDocument.defaultView.HTMLElement) {
        var styleWidth = node.style.width;
        var styleHeight = node.style.height;

        // If we are re-measuring a cell that has already been measured,
        // It will have a hard-coded width/height from the previous measurement.
        // The fact that we are measuring indicates this measurement is probably stale,
        // So explicitly clear it out (eg set to "auto") so we can recalculate.
        // See issue #593 for more info.
        // Even if we are measuring initially- if we're inside of a MultiGrid component,
        // Explicitly clear width/height before measuring to avoid being tainted by another Grid.
        // eg top/left Grid renders before bottom/right Grid
        // Since the CellMeasurerCache is shared between them this taints derived cell size values.
        if (!cache.hasFixedWidth()) {
          node.style.width = 'auto';
        }
        if (!cache.hasFixedHeight()) {
          node.style.height = 'auto';
        }

        var height = Math.ceil(node.offsetHeight);
        var width = Math.ceil(node.offsetWidth);

        // Reset after measuring to avoid breaking styles; see #660
        if (styleWidth) {
          node.style.width = styleWidth;
        }
        if (styleHeight) {
          node.style.height = styleHeight;
        }

        return { height: height, width: width };
      } else {
        return { height: 0, width: 0 };
      }
    }
  }, {
    key: '_maybeMeasureCell',
    value: function _maybeMeasureCell() {
      var _props = this.props,
          cache = _props.cache,
          _props$columnIndex = _props.columnIndex,
          columnIndex = _props$columnIndex === undefined ? 0 : _props$columnIndex,
          parent = _props.parent,
          _props$rowIndex = _props.rowIndex,
          rowIndex = _props$rowIndex === undefined ? this.props.index || 0 : _props$rowIndex;


      if (!cache.has(rowIndex, columnIndex)) {
        var _getCellMeasurements2 = this._getCellMeasurements(),
            height = _getCellMeasurements2.height,
            width = _getCellMeasurements2.width;

        cache.set(rowIndex, columnIndex, width, height);

        // If size has changed, let Grid know to re-render.
        if (parent && typeof parent.invalidateCellSizeAfterRender === 'function') {
          parent.invalidateCellSizeAfterRender({
            columnIndex: columnIndex,
            rowIndex: rowIndex
          });
        }
      }
    }
  }]);

  return CellMeasurer;
}(__WEBPACK_IMPORTED_MODULE_5_react__["PureComponent"]);

// Used for DEV mode warning check


CellMeasurer.__internalCellMeasurerFlag = false;
CellMeasurer.propTypes =  false ? null : {
  cache: typeof __WEBPACK_IMPORTED_MODULE_7__CellMeasurerCache_js__["a" /* default */] === 'function' ? __webpack_require__(0).instanceOf(__WEBPACK_IMPORTED_MODULE_7__CellMeasurerCache_js__["a" /* default */]).isRequired : __webpack_require__(0).any.isRequired,
  children: __webpack_require__(0).oneOfType([__webpack_require__(0).func, __webpack_require__(0).node]).isRequired,
  columnIndex: __webpack_require__(0).number,
  index: __webpack_require__(0).number,
  parent: __webpack_require__(0).shape({
    invalidateCellSizeAfterRender: __webpack_require__(0).func,
    recomputeGridSize: __webpack_require__(0).func
  }).isRequired,
  rowIndex: __webpack_require__(0).number
};
/* harmony default export */ __webpack_exports__["a"] = (CellMeasurer);
if (true) {
  CellMeasurer.__internalCellMeasurerFlag = true;
}

/***/ }),
/* 227 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Collection__ = __webpack_require__(228);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__Collection__["a"]; });


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__Collection__["a" /* default */]);


/***/ }),
/* 228 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties__ = __webpack_require__(65);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_8_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__CollectionView__ = __webpack_require__(229);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__utils_calculateSizeAndPositionData__ = __webpack_require__(230);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_11__utils_getUpdatedOffsetForIndex__ = __webpack_require__(233);













/**
 * Renders scattered or non-linear data.
 * Unlike Grid, which renders checkerboard data, Collection can render arbitrarily positioned- even overlapping- data.
 */
var babelPluginFlowReactPropTypes_proptype_SizeInfo = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_SizeInfo || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_ScrollPosition = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_ScrollPosition || __webpack_require__(0).any;

var Collection = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default()(Collection, _PureComponent);

  function Collection(props, context) {
    __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default()(this, Collection);

    var _this = __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default()(this, (Collection.__proto__ || __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default()(Collection)).call(this, props, context));

    _this._cellMetadata = [];
    _this._lastRenderedCellIndices = [];

    // Cell cache during scroll (for perforamnce)
    _this._cellCache = [];

    _this._isScrollingChange = _this._isScrollingChange.bind(_this);
    _this._setCollectionViewRef = _this._setCollectionViewRef.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default()(Collection, [{
    key: 'forceUpdate',
    value: function forceUpdate() {
      if (this._collectionView !== undefined) {
        this._collectionView.forceUpdate();
      }
    }

    /** See Collection#recomputeCellSizesAndPositions */

  }, {
    key: 'recomputeCellSizesAndPositions',
    value: function recomputeCellSizesAndPositions() {
      this._cellCache = [];
      this._collectionView.recomputeCellSizesAndPositions();
    }

    /** React lifecycle methods */

  }, {
    key: 'render',
    value: function render() {
      var props = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default()(this.props, []);

      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_9__CollectionView__["a" /* default */], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
        cellLayoutManager: this,
        isScrollingChange: this._isScrollingChange,
        ref: this._setCollectionViewRef
      }, props));
    }

    /** CellLayoutManager interface */

  }, {
    key: 'calculateSizeAndPositionData',
    value: function calculateSizeAndPositionData() {
      var _props = this.props,
          cellCount = _props.cellCount,
          cellSizeAndPositionGetter = _props.cellSizeAndPositionGetter,
          sectionSize = _props.sectionSize;


      var data = Object(__WEBPACK_IMPORTED_MODULE_10__utils_calculateSizeAndPositionData__["a" /* default */])({
        cellCount: cellCount,
        cellSizeAndPositionGetter: cellSizeAndPositionGetter,
        sectionSize: sectionSize
      });

      this._cellMetadata = data.cellMetadata;
      this._sectionManager = data.sectionManager;
      this._height = data.height;
      this._width = data.width;
    }

    /**
     * Returns the most recently rendered set of cell indices.
     */

  }, {
    key: 'getLastRenderedIndices',
    value: function getLastRenderedIndices() {
      return this._lastRenderedCellIndices;
    }

    /**
     * Calculates the minimum amount of change from the current scroll position to ensure the specified cell is (fully) visible.
     */

  }, {
    key: 'getScrollPositionForCell',
    value: function getScrollPositionForCell(_ref) {
      var align = _ref.align,
          cellIndex = _ref.cellIndex,
          height = _ref.height,
          scrollLeft = _ref.scrollLeft,
          scrollTop = _ref.scrollTop,
          width = _ref.width;
      var cellCount = this.props.cellCount;


      if (cellIndex >= 0 && cellIndex < cellCount) {
        var cellMetadata = this._cellMetadata[cellIndex];

        scrollLeft = Object(__WEBPACK_IMPORTED_MODULE_11__utils_getUpdatedOffsetForIndex__["a" /* default */])({
          align: align,
          cellOffset: cellMetadata.x,
          cellSize: cellMetadata.width,
          containerSize: width,
          currentOffset: scrollLeft,
          targetIndex: cellIndex
        });

        scrollTop = Object(__WEBPACK_IMPORTED_MODULE_11__utils_getUpdatedOffsetForIndex__["a" /* default */])({
          align: align,
          cellOffset: cellMetadata.y,
          cellSize: cellMetadata.height,
          containerSize: height,
          currentOffset: scrollTop,
          targetIndex: cellIndex
        });
      }

      return {
        scrollLeft: scrollLeft,
        scrollTop: scrollTop
      };
    }
  }, {
    key: 'getTotalSize',
    value: function getTotalSize() {
      return {
        height: this._height,
        width: this._width
      };
    }
  }, {
    key: 'cellRenderers',
    value: function cellRenderers(_ref2) {
      var _this2 = this;

      var height = _ref2.height,
          isScrolling = _ref2.isScrolling,
          width = _ref2.width,
          x = _ref2.x,
          y = _ref2.y;
      var _props2 = this.props,
          cellGroupRenderer = _props2.cellGroupRenderer,
          cellRenderer = _props2.cellRenderer;

      // Store for later calls to getLastRenderedIndices()

      this._lastRenderedCellIndices = this._sectionManager.getCellIndices({
        height: height,
        width: width,
        x: x,
        y: y
      });

      return cellGroupRenderer({
        cellCache: this._cellCache,
        cellRenderer: cellRenderer,
        cellSizeAndPositionGetter: function cellSizeAndPositionGetter(_ref3) {
          var index = _ref3.index;
          return _this2._sectionManager.getCellMetadata({ index: index });
        },
        indices: this._lastRenderedCellIndices,
        isScrolling: isScrolling
      });
    }
  }, {
    key: '_isScrollingChange',
    value: function _isScrollingChange(isScrolling) {
      if (!isScrolling) {
        this._cellCache = [];
      }
    }
  }, {
    key: '_setCollectionViewRef',
    value: function _setCollectionViewRef(ref) {
      this._collectionView = ref;
    }
  }]);

  return Collection;
}(__WEBPACK_IMPORTED_MODULE_8_react__["PureComponent"]);

Collection.defaultProps = {
  'aria-label': 'grid',
  cellGroupRenderer: defaultCellGroupRenderer
};
/* harmony default export */ __webpack_exports__["a"] = (Collection);
Collection.propTypes =  true ? {
  'aria-label': __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.string,

  /**
   * Number of cells in Collection.
   */
  cellCount: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.number.isRequired,

  /**
   * Responsible for rendering a group of cells given their indices.
   * Should implement the following interface: ({
   *   cellSizeAndPositionGetter:Function,
   *   indices: Array<number>,
   *   cellRenderer: Function
   * }): Array<PropTypes.node>
   */
  cellGroupRenderer: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.func.isRequired,

  /**
   * Responsible for rendering a cell given an row and column index.
   * Should implement the following interface: ({ index: number, key: string, style: object }): PropTypes.element
   */
  cellRenderer: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.func.isRequired,

  /**
   * Callback responsible for returning size and offset/position information for a given cell (index).
   * ({ index: number }): { height: number, width: number, x: number, y: number }
   */
  cellSizeAndPositionGetter: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.func.isRequired,

  /**
   * Optionally override the size of the sections a Collection's cells are split into.
   */
  sectionSize: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.number
} : {};


function defaultCellGroupRenderer(_ref4) {
  var cellCache = _ref4.cellCache,
      cellRenderer = _ref4.cellRenderer,
      cellSizeAndPositionGetter = _ref4.cellSizeAndPositionGetter,
      indices = _ref4.indices,
      isScrolling = _ref4.isScrolling;

  return indices.map(function (index) {
    var cellMetadata = cellSizeAndPositionGetter({ index: index });

    var cellRendererProps = {
      index: index,
      isScrolling: isScrolling,
      key: index,
      style: {
        height: cellMetadata.height,
        left: cellMetadata.x,
        position: 'absolute',
        top: cellMetadata.y,
        width: cellMetadata.width
      }
    };

    // Avoid re-creating cells while scrolling.
    // This can lead to the same cell being created many times and can cause performance issues for "heavy" cells.
    // If a scroll is in progress- cache and reuse cells.
    // This cache will be thrown away once scrolling complets.
    if (isScrolling) {
      if (!(index in cellCache)) {
        cellCache[index] = cellRenderer(cellRendererProps);
      }

      return cellCache[index];
    } else {
      return cellRenderer(cellRendererProps);
    }
  }).filter(function (renderedCell) {
    return !!renderedCell;
  });
}

/***/ }),
/* 229 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_8_classnames__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__utils_createCallbackMemoizer__ = __webpack_require__(66);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10_dom_helpers_util_scrollbarSize__ = __webpack_require__(98);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10_dom_helpers_util_scrollbarSize___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_10_dom_helpers_util_scrollbarSize__);












// @TODO Merge Collection and CollectionView

/**
 * Specifies the number of milliseconds during which to disable pointer events while a scroll is in progress.
 * This improves performance and makes scrolling smoother.
 */
var IS_SCROLLING_TIMEOUT = 150;

/**
 * Controls whether the Grid updates the DOM element's scrollLeft/scrollTop based on the current state or just observes it.
 * This prevents Grid from interrupting mouse-wheel animations (see issue #2).
 */
var SCROLL_POSITION_CHANGE_REASONS = {
  OBSERVED: 'observed',
  REQUESTED: 'requested'
};

/**
 * Monitors changes in properties (eg. cellCount) and state (eg. scroll offsets) to determine when rendering needs to occur.
 * This component does not render any visible content itself; it defers to the specified :cellLayoutManager.
 */

var CollectionView = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(CollectionView, _PureComponent);

  function CollectionView(props, context) {
    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, CollectionView);

    var _this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (CollectionView.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(CollectionView)).call(this, props, context));

    _this.state = {
      isScrolling: false,
      scrollLeft: 0,
      scrollTop: 0
    };

    _this._calculateSizeAndPositionDataOnNextUpdate = false;

    // Invokes callbacks only when their values have changed.
    _this._onSectionRenderedMemoizer = Object(__WEBPACK_IMPORTED_MODULE_9__utils_createCallbackMemoizer__["a" /* default */])();
    _this._onScrollMemoizer = Object(__WEBPACK_IMPORTED_MODULE_9__utils_createCallbackMemoizer__["a" /* default */])(false);

    // Bind functions to instance so they don't lose context when passed around.
    _this._invokeOnSectionRenderedHelper = _this._invokeOnSectionRenderedHelper.bind(_this);
    _this._onScroll = _this._onScroll.bind(_this);
    _this._setScrollingContainerRef = _this._setScrollingContainerRef.bind(_this);
    _this._updateScrollPositionForScrollToCell = _this._updateScrollPositionForScrollToCell.bind(_this);
    return _this;
  }

  /**
   * Forced recompute of cell sizes and positions.
   * This function should be called if cell sizes have changed but nothing else has.
   * Since cell positions are calculated by callbacks, the collection view has no way of detecting when the underlying data has changed.
   */


  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(CollectionView, [{
    key: 'recomputeCellSizesAndPositions',
    value: function recomputeCellSizesAndPositions() {
      this._calculateSizeAndPositionDataOnNextUpdate = true;
      this.forceUpdate();
    }

    /* ---------------------------- Component lifecycle methods ---------------------------- */

  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      var _props = this.props,
          cellLayoutManager = _props.cellLayoutManager,
          scrollLeft = _props.scrollLeft,
          scrollToCell = _props.scrollToCell,
          scrollTop = _props.scrollTop;

      // If this component was first rendered server-side, scrollbar size will be undefined.
      // In that event we need to remeasure.

      if (!this._scrollbarSizeMeasured) {
        this._scrollbarSize = __WEBPACK_IMPORTED_MODULE_10_dom_helpers_util_scrollbarSize___default()();
        this._scrollbarSizeMeasured = true;
        this.setState({});
      }

      if (scrollToCell >= 0) {
        this._updateScrollPositionForScrollToCell();
      } else if (scrollLeft >= 0 || scrollTop >= 0) {
        this._setScrollPosition({ scrollLeft: scrollLeft, scrollTop: scrollTop });
      }

      // Update onSectionRendered callback.
      this._invokeOnSectionRenderedHelper();

      var _cellLayoutManager$ge = cellLayoutManager.getTotalSize(),
          totalHeight = _cellLayoutManager$ge.height,
          totalWidth = _cellLayoutManager$ge.width;

      // Initialize onScroll callback.


      this._invokeOnScrollMemoizer({
        scrollLeft: scrollLeft || 0,
        scrollTop: scrollTop || 0,
        totalHeight: totalHeight,
        totalWidth: totalWidth
      });
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate(prevProps, prevState) {
      var _props2 = this.props,
          height = _props2.height,
          scrollToAlignment = _props2.scrollToAlignment,
          scrollToCell = _props2.scrollToCell,
          width = _props2.width;
      var _state = this.state,
          scrollLeft = _state.scrollLeft,
          scrollPositionChangeReason = _state.scrollPositionChangeReason,
          scrollTop = _state.scrollTop;

      // Make sure requested changes to :scrollLeft or :scrollTop get applied.
      // Assigning to scrollLeft/scrollTop tells the browser to interrupt any running scroll animations,
      // And to discard any pending async changes to the scroll position that may have happened in the meantime (e.g. on a separate scrolling thread).
      // So we only set these when we require an adjustment of the scroll position.
      // See issue #2 for more information.

      if (scrollPositionChangeReason === SCROLL_POSITION_CHANGE_REASONS.REQUESTED) {
        if (scrollLeft >= 0 && scrollLeft !== prevState.scrollLeft && scrollLeft !== this._scrollingContainer.scrollLeft) {
          this._scrollingContainer.scrollLeft = scrollLeft;
        }
        if (scrollTop >= 0 && scrollTop !== prevState.scrollTop && scrollTop !== this._scrollingContainer.scrollTop) {
          this._scrollingContainer.scrollTop = scrollTop;
        }
      }

      // Update scroll offsets if the current :scrollToCell values requires it
      if (height !== prevProps.height || scrollToAlignment !== prevProps.scrollToAlignment || scrollToCell !== prevProps.scrollToCell || width !== prevProps.width) {
        this._updateScrollPositionForScrollToCell();
      }

      // Update onRowsRendered callback if start/stop indices have changed
      this._invokeOnSectionRenderedHelper();
    }
  }, {
    key: 'componentWillMount',
    value: function componentWillMount() {
      var cellLayoutManager = this.props.cellLayoutManager;


      cellLayoutManager.calculateSizeAndPositionData();

      // If this component is being rendered server-side, getScrollbarSize() will return undefined.
      // We handle this case in componentDidMount()
      this._scrollbarSize = __WEBPACK_IMPORTED_MODULE_10_dom_helpers_util_scrollbarSize___default()();
      if (this._scrollbarSize === undefined) {
        this._scrollbarSizeMeasured = false;
        this._scrollbarSize = 0;
      } else {
        this._scrollbarSizeMeasured = true;
      }
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      if (this._disablePointerEventsTimeoutId) {
        clearTimeout(this._disablePointerEventsTimeoutId);
      }
    }

    /**
     * @private
     * This method updates scrollLeft/scrollTop in state for the following conditions:
     * 1) Empty content (0 rows or columns)
     * 2) New scroll props overriding the current state
     * 3) Cells-count or cells-size has changed, making previous scroll offsets invalid
     */

  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      var _state2 = this.state,
          scrollLeft = _state2.scrollLeft,
          scrollTop = _state2.scrollTop;


      if (nextProps.cellCount === 0 && (scrollLeft !== 0 || scrollTop !== 0)) {
        this._setScrollPosition({
          scrollLeft: 0,
          scrollTop: 0
        });
      } else if (nextProps.scrollLeft !== this.props.scrollLeft || nextProps.scrollTop !== this.props.scrollTop) {
        this._setScrollPosition({
          scrollLeft: nextProps.scrollLeft,
          scrollTop: nextProps.scrollTop
        });
      }

      if (nextProps.cellCount !== this.props.cellCount || nextProps.cellLayoutManager !== this.props.cellLayoutManager || this._calculateSizeAndPositionDataOnNextUpdate) {
        nextProps.cellLayoutManager.calculateSizeAndPositionData();
      }

      if (this._calculateSizeAndPositionDataOnNextUpdate) {
        this._calculateSizeAndPositionDataOnNextUpdate = false;
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _props3 = this.props,
          autoHeight = _props3.autoHeight,
          cellCount = _props3.cellCount,
          cellLayoutManager = _props3.cellLayoutManager,
          className = _props3.className,
          height = _props3.height,
          horizontalOverscanSize = _props3.horizontalOverscanSize,
          id = _props3.id,
          noContentRenderer = _props3.noContentRenderer,
          style = _props3.style,
          verticalOverscanSize = _props3.verticalOverscanSize,
          width = _props3.width;
      var _state3 = this.state,
          isScrolling = _state3.isScrolling,
          scrollLeft = _state3.scrollLeft,
          scrollTop = _state3.scrollTop;

      var _cellLayoutManager$ge2 = cellLayoutManager.getTotalSize(),
          totalHeight = _cellLayoutManager$ge2.height,
          totalWidth = _cellLayoutManager$ge2.width;

      // Safely expand the rendered area by the specified overscan amount


      var left = Math.max(0, scrollLeft - horizontalOverscanSize);
      var top = Math.max(0, scrollTop - verticalOverscanSize);
      var right = Math.min(totalWidth, scrollLeft + width + horizontalOverscanSize);
      var bottom = Math.min(totalHeight, scrollTop + height + verticalOverscanSize);

      var childrenToDisplay = height > 0 && width > 0 ? cellLayoutManager.cellRenderers({
        height: bottom - top,
        isScrolling: isScrolling,
        width: right - left,
        x: left,
        y: top
      }) : [];

      var collectionStyle = {
        boxSizing: 'border-box',
        direction: 'ltr',
        height: autoHeight ? 'auto' : height,
        position: 'relative',
        WebkitOverflowScrolling: 'touch',
        width: width,
        willChange: 'transform'
      };

      // Force browser to hide scrollbars when we know they aren't necessary.
      // Otherwise once scrollbars appear they may not disappear again.
      // For more info see issue #116
      var verticalScrollBarSize = totalHeight > height ? this._scrollbarSize : 0;
      var horizontalScrollBarSize = totalWidth > width ? this._scrollbarSize : 0;

      // Also explicitly init styles to 'auto' if scrollbars are required.
      // This works around an obscure edge case where external CSS styles have not yet been loaded,
      // But an initial scroll index of offset is set as an external prop.
      // Without this style, Grid would render the correct range of cells but would NOT update its internal offset.
      // This was originally reported via clauderic/react-infinite-calendar/issues/23
      collectionStyle.overflowX = totalWidth + verticalScrollBarSize <= width ? 'hidden' : 'auto';
      collectionStyle.overflowY = totalHeight + horizontalScrollBarSize <= height ? 'hidden' : 'auto';

      return __WEBPACK_IMPORTED_MODULE_7_react___default.a.createElement(
        'div',
        {
          ref: this._setScrollingContainerRef,
          'aria-label': this.props['aria-label'],
          className: __WEBPACK_IMPORTED_MODULE_8_classnames___default()('ReactVirtualized__Collection', className),
          id: id,
          onScroll: this._onScroll,
          role: 'grid',
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, collectionStyle, style),
          tabIndex: 0 },
        cellCount > 0 && __WEBPACK_IMPORTED_MODULE_7_react___default.a.createElement(
          'div',
          {
            className: 'ReactVirtualized__Collection__innerScrollContainer',
            style: {
              height: totalHeight,
              maxHeight: totalHeight,
              maxWidth: totalWidth,
              overflow: 'hidden',
              pointerEvents: isScrolling ? 'none' : '',
              width: totalWidth
            } },
          childrenToDisplay
        ),
        cellCount === 0 && noContentRenderer()
      );
    }

    /* ---------------------------- Helper methods ---------------------------- */

    /**
     * Sets an :isScrolling flag for a small window of time.
     * This flag is used to disable pointer events on the scrollable portion of the Collection.
     * This prevents jerky/stuttery mouse-wheel scrolling.
     */

  }, {
    key: '_enablePointerEventsAfterDelay',
    value: function _enablePointerEventsAfterDelay() {
      var _this2 = this;

      if (this._disablePointerEventsTimeoutId) {
        clearTimeout(this._disablePointerEventsTimeoutId);
      }

      this._disablePointerEventsTimeoutId = setTimeout(function () {
        var isScrollingChange = _this2.props.isScrollingChange;


        isScrollingChange(false);

        _this2._disablePointerEventsTimeoutId = null;
        _this2.setState({
          isScrolling: false
        });
      }, IS_SCROLLING_TIMEOUT);
    }
  }, {
    key: '_invokeOnSectionRenderedHelper',
    value: function _invokeOnSectionRenderedHelper() {
      var _props4 = this.props,
          cellLayoutManager = _props4.cellLayoutManager,
          onSectionRendered = _props4.onSectionRendered;


      this._onSectionRenderedMemoizer({
        callback: onSectionRendered,
        indices: {
          indices: cellLayoutManager.getLastRenderedIndices()
        }
      });
    }
  }, {
    key: '_invokeOnScrollMemoizer',
    value: function _invokeOnScrollMemoizer(_ref) {
      var _this3 = this;

      var scrollLeft = _ref.scrollLeft,
          scrollTop = _ref.scrollTop,
          totalHeight = _ref.totalHeight,
          totalWidth = _ref.totalWidth;

      this._onScrollMemoizer({
        callback: function callback(_ref2) {
          var scrollLeft = _ref2.scrollLeft,
              scrollTop = _ref2.scrollTop;
          var _props5 = _this3.props,
              height = _props5.height,
              onScroll = _props5.onScroll,
              width = _props5.width;


          onScroll({
            clientHeight: height,
            clientWidth: width,
            scrollHeight: totalHeight,
            scrollLeft: scrollLeft,
            scrollTop: scrollTop,
            scrollWidth: totalWidth
          });
        },
        indices: {
          scrollLeft: scrollLeft,
          scrollTop: scrollTop
        }
      });
    }
  }, {
    key: '_setScrollingContainerRef',
    value: function _setScrollingContainerRef(ref) {
      this._scrollingContainer = ref;
    }
  }, {
    key: '_setScrollPosition',
    value: function _setScrollPosition(_ref3) {
      var scrollLeft = _ref3.scrollLeft,
          scrollTop = _ref3.scrollTop;

      var newState = {
        scrollPositionChangeReason: SCROLL_POSITION_CHANGE_REASONS.REQUESTED
      };

      if (scrollLeft >= 0) {
        newState.scrollLeft = scrollLeft;
      }

      if (scrollTop >= 0) {
        newState.scrollTop = scrollTop;
      }

      if (scrollLeft >= 0 && scrollLeft !== this.state.scrollLeft || scrollTop >= 0 && scrollTop !== this.state.scrollTop) {
        this.setState(newState);
      }
    }
  }, {
    key: '_updateScrollPositionForScrollToCell',
    value: function _updateScrollPositionForScrollToCell() {
      var _props6 = this.props,
          cellLayoutManager = _props6.cellLayoutManager,
          height = _props6.height,
          scrollToAlignment = _props6.scrollToAlignment,
          scrollToCell = _props6.scrollToCell,
          width = _props6.width;
      var _state4 = this.state,
          scrollLeft = _state4.scrollLeft,
          scrollTop = _state4.scrollTop;


      if (scrollToCell >= 0) {
        var scrollPosition = cellLayoutManager.getScrollPositionForCell({
          align: scrollToAlignment,
          cellIndex: scrollToCell,
          height: height,
          scrollLeft: scrollLeft,
          scrollTop: scrollTop,
          width: width
        });

        if (scrollPosition.scrollLeft !== scrollLeft || scrollPosition.scrollTop !== scrollTop) {
          this._setScrollPosition(scrollPosition);
        }
      }
    }
  }, {
    key: '_onScroll',
    value: function _onScroll(event) {
      // In certain edge-cases React dispatches an onScroll event with an invalid target.scrollLeft / target.scrollTop.
      // This invalid event can be detected by comparing event.target to this component's scrollable DOM element.
      // See issue #404 for more information.
      if (event.target !== this._scrollingContainer) {
        return;
      }

      // Prevent pointer events from interrupting a smooth scroll
      this._enablePointerEventsAfterDelay();

      // When this component is shrunk drastically, React dispatches a series of back-to-back scroll events,
      // Gradually converging on a scrollTop that is within the bounds of the new, smaller height.
      // This causes a series of rapid renders that is slow for long lists.
      // We can avoid that by doing some simple bounds checking to ensure that scrollTop never exceeds the total height.
      var _props7 = this.props,
          cellLayoutManager = _props7.cellLayoutManager,
          height = _props7.height,
          isScrollingChange = _props7.isScrollingChange,
          width = _props7.width;

      var scrollbarSize = this._scrollbarSize;

      var _cellLayoutManager$ge3 = cellLayoutManager.getTotalSize(),
          totalHeight = _cellLayoutManager$ge3.height,
          totalWidth = _cellLayoutManager$ge3.width;

      var scrollLeft = Math.max(0, Math.min(totalWidth - width + scrollbarSize, event.target.scrollLeft));
      var scrollTop = Math.max(0, Math.min(totalHeight - height + scrollbarSize, event.target.scrollTop));

      // Certain devices (like Apple touchpad) rapid-fire duplicate events.
      // Don't force a re-render if this is the case.
      // The mouse may move faster then the animation frame does.
      // Use requestAnimationFrame to avoid over-updating.
      if (this.state.scrollLeft !== scrollLeft || this.state.scrollTop !== scrollTop) {
        // Browsers with cancelable scroll events (eg. Firefox) interrupt scrolling animations if scrollTop/scrollLeft is set.
        // Other browsers (eg. Safari) don't scroll as well without the help under certain conditions (DOM or style changes during scrolling).
        // All things considered, this seems to be the best current work around that I'm aware of.
        // For more information see https://github.com/bvaughn/react-virtualized/pull/124
        var scrollPositionChangeReason = event.cancelable ? SCROLL_POSITION_CHANGE_REASONS.OBSERVED : SCROLL_POSITION_CHANGE_REASONS.REQUESTED;

        // Synchronously set :isScrolling the first time (since _setNextState will reschedule its animation frame each time it's called)
        if (!this.state.isScrolling) {
          isScrollingChange(true);
        }

        this.setState({
          isScrolling: true,
          scrollLeft: scrollLeft,
          scrollPositionChangeReason: scrollPositionChangeReason,
          scrollTop: scrollTop
        });
      }

      this._invokeOnScrollMemoizer({
        scrollLeft: scrollLeft,
        scrollTop: scrollTop,
        totalWidth: totalWidth,
        totalHeight: totalHeight
      });
    }
  }]);

  return CollectionView;
}(__WEBPACK_IMPORTED_MODULE_7_react__["PureComponent"]);

CollectionView.defaultProps = {
  'aria-label': 'grid',
  horizontalOverscanSize: 0,
  noContentRenderer: function noContentRenderer() {
    return null;
  },
  onScroll: function onScroll() {
    return null;
  },
  onSectionRendered: function onSectionRendered() {
    return null;
  },
  scrollToAlignment: 'auto',
  scrollToCell: -1,
  style: {},
  verticalOverscanSize: 0
};
/* harmony default export */ __webpack_exports__["a"] = (CollectionView);
CollectionView.propTypes =  true ? {
  'aria-label': __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.string,

  /**
   * Removes fixed height from the scrollingContainer so that the total height
   * of rows can stretch the window. Intended for use with WindowScroller
   */
  autoHeight: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.bool,

  /**
   * Number of cells in collection.
   */
  cellCount: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Calculates cell sizes and positions and manages rendering the appropriate cells given a specified window.
   */
  cellLayoutManager: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.object.isRequired,

  /**
   * Optional custom CSS class name to attach to root Collection element.
   */
  className: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.string,

  /**
   * Height of Collection; this property determines the number of visible (vs virtualized) rows.
   */
  height: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Optional custom id to attach to root Collection element.
   */
  id: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.string,

  /**
   * Enables the `Collection` to horiontally "overscan" its content similar to how `Grid` does.
   * This can reduce flicker around the edges when a user scrolls quickly.
   */
  horizontalOverscanSize: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  isScrollingChange: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func,

  /**
   * Optional renderer to be used in place of rows when either :rowCount or :cellCount is 0.
   */
  noContentRenderer: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Callback invoked whenever the scroll offset changes within the inner scrollable region.
   * This callback can be used to sync scrolling between lists, tables, or grids.
   * ({ clientHeight, clientWidth, scrollHeight, scrollLeft, scrollTop, scrollWidth }): void
   */
  onScroll: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Callback invoked with information about the section of the Collection that was just rendered.
   * This callback is passed a named :indices parameter which is an Array of the most recently rendered section indices.
   */
  onSectionRendered: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Horizontal offset.
   */
  scrollLeft: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number,

  /**
   * Controls scroll-to-cell behavior of the Grid.
   * The default ("auto") scrolls the least amount possible to ensure that the specified cell is fully visible.
   * Use "start" to align cells to the top/left of the Grid and "end" to align bottom/right.
   */
  scrollToAlignment: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.oneOf(['auto', 'end', 'start', 'center']).isRequired,

  /**
   * Cell index to ensure visible (by forcefully scrolling if necessary).
   */
  scrollToCell: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Vertical offset.
   */
  scrollTop: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number,

  /**
   * Optional custom inline style to attach to root Collection element.
   */
  style: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.object,

  /**
   * Enables the `Collection` to vertically "overscan" its content similar to how `Grid` does.
   * This can reduce flicker around the edges when a user scrolls quickly.
   */
  verticalOverscanSize: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Width of Collection; this property determines the number of visible (vs virtualized) columns.
   */
  width: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired
} : {};

/***/ }),
/* 230 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = calculateSizeAndPositionData;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__SectionManager__ = __webpack_require__(231);


function calculateSizeAndPositionData(_ref) {
  var cellCount = _ref.cellCount,
      cellSizeAndPositionGetter = _ref.cellSizeAndPositionGetter,
      sectionSize = _ref.sectionSize;

  var cellMetadata = [];
  var sectionManager = new __WEBPACK_IMPORTED_MODULE_0__SectionManager__["a" /* default */](sectionSize);
  var height = 0;
  var width = 0;

  for (var index = 0; index < cellCount; index++) {
    var cellMetadatum = cellSizeAndPositionGetter({ index: index });

    if (cellMetadatum.height == null || isNaN(cellMetadatum.height) || cellMetadatum.width == null || isNaN(cellMetadatum.width) || cellMetadatum.x == null || isNaN(cellMetadatum.x) || cellMetadatum.y == null || isNaN(cellMetadatum.y)) {
      throw Error('Invalid metadata returned for cell ' + index + ':\n        x:' + cellMetadatum.x + ', y:' + cellMetadatum.y + ', width:' + cellMetadatum.width + ', height:' + cellMetadatum.height);
    }

    height = Math.max(height, cellMetadatum.y + cellMetadatum.height);
    width = Math.max(width, cellMetadatum.x + cellMetadatum.width);

    cellMetadata[index] = cellMetadatum;
    sectionManager.registerCell({
      cellMetadatum: cellMetadatum,
      index: index
    });
  }

  return {
    cellMetadata: cellMetadata,
    height: height,
    sectionManager: sectionManager,
    width: width
  };
}

/***/ }),
/* 231 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys__ = __webpack_require__(95);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__Section__ = __webpack_require__(232);



/**
 * Window Sections are used to group nearby cells.
 * This enables us to more quickly determine which cells to display in a given region of the Window.
 * 
 */


var babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_Index = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_Index || __webpack_require__(0).any;

var SECTION_SIZE = 100;

/**
 * Contains 0 to many Sections.
 * Grows (and adds Sections) dynamically as cells are registered.
 * Automatically adds cells to the appropriate Section(s).
 */
var SectionManager = function () {
  function SectionManager() {
    var sectionSize = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : SECTION_SIZE;

    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, SectionManager);

    this._sectionSize = sectionSize;

    this._cellMetadata = [];
    this._sections = {};
  }

  /**
   * Gets all cell indices contained in the specified region.
   * A region may encompass 1 or more Sections.
   */


  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(SectionManager, [{
    key: 'getCellIndices',
    value: function getCellIndices(_ref) {
      var height = _ref.height,
          width = _ref.width,
          x = _ref.x,
          y = _ref.y;

      var indices = {};

      this.getSections({ height: height, width: width, x: x, y: y }).forEach(function (section) {
        return section.getCellIndices().forEach(function (index) {
          indices[index] = index;
        });
      });

      // Object keys are strings; this function returns numbers
      return __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default()(indices).map(function (index) {
        return indices[index];
      });
    }

    /** Get size and position information for the cell specified. */

  }, {
    key: 'getCellMetadata',
    value: function getCellMetadata(_ref2) {
      var index = _ref2.index;

      return this._cellMetadata[index];
    }

    /** Get all Sections overlapping the specified region. */

  }, {
    key: 'getSections',
    value: function getSections(_ref3) {
      var height = _ref3.height,
          width = _ref3.width,
          x = _ref3.x,
          y = _ref3.y;

      var sectionXStart = Math.floor(x / this._sectionSize);
      var sectionXStop = Math.floor((x + width - 1) / this._sectionSize);
      var sectionYStart = Math.floor(y / this._sectionSize);
      var sectionYStop = Math.floor((y + height - 1) / this._sectionSize);

      var sections = [];

      for (var sectionX = sectionXStart; sectionX <= sectionXStop; sectionX++) {
        for (var sectionY = sectionYStart; sectionY <= sectionYStop; sectionY++) {
          var key = sectionX + '.' + sectionY;

          if (!this._sections[key]) {
            this._sections[key] = new __WEBPACK_IMPORTED_MODULE_3__Section__["a" /* default */]({
              height: this._sectionSize,
              width: this._sectionSize,
              x: sectionX * this._sectionSize,
              y: sectionY * this._sectionSize
            });
          }

          sections.push(this._sections[key]);
        }
      }

      return sections;
    }

    /** Total number of Sections based on the currently registered cells. */

  }, {
    key: 'getTotalSectionCount',
    value: function getTotalSectionCount() {
      return __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default()(this._sections).length;
    }

    /** Intended for debugger/test purposes only */

  }, {
    key: 'toString',
    value: function toString() {
      var _this = this;

      return __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_keys___default()(this._sections).map(function (index) {
        return _this._sections[index].toString();
      });
    }

    /** Adds a cell to the appropriate Sections and registers it metadata for later retrievable. */

  }, {
    key: 'registerCell',
    value: function registerCell(_ref4) {
      var cellMetadatum = _ref4.cellMetadatum,
          index = _ref4.index;

      this._cellMetadata[index] = cellMetadatum;

      this.getSections(cellMetadatum).forEach(function (section) {
        return section.addCellIndex({ index: index });
      });
    }
  }]);

  return SectionManager;
}();

/* harmony default export */ __webpack_exports__["a"] = (SectionManager);

/***/ }),
/* 232 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__);



/**
 * A section of the Window.
 * Window Sections are used to group nearby cells.
 * This enables us to more quickly determine which cells to display in a given region of the Window.
 * Sections have a fixed size and contain 0 to many cells (tracked by their indices).
 */
var babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_SizeAndPositionInfo || __webpack_require__(0).any; /** @rlow */


var babelPluginFlowReactPropTypes_proptype_Index = __webpack_require__(24).babelPluginFlowReactPropTypes_proptype_Index || __webpack_require__(0).any;

var Section = function () {
  function Section(_ref) {
    var height = _ref.height,
        width = _ref.width,
        x = _ref.x,
        y = _ref.y;

    __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default()(this, Section);

    this.height = height;
    this.width = width;
    this.x = x;
    this.y = y;

    this._indexMap = {};
    this._indices = [];
  }

  /** Add a cell to this section. */


  __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default()(Section, [{
    key: 'addCellIndex',
    value: function addCellIndex(_ref2) {
      var index = _ref2.index;

      if (!this._indexMap[index]) {
        this._indexMap[index] = true;
        this._indices.push(index);
      }
    }

    /** Get all cell indices that have been added to this section. */

  }, {
    key: 'getCellIndices',
    value: function getCellIndices() {
      return this._indices;
    }

    /** Intended for debugger/test purposes only */

  }, {
    key: 'toString',
    value: function toString() {
      return this.x + ',' + this.y + ' ' + this.width + 'x' + this.height;
    }
  }]);

  return Section;
}();

/* harmony default export */ __webpack_exports__["a"] = (Section);

/***/ }),
/* 233 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = getUpdatedOffsetForIndex;
/**
 * Determines a new offset that ensures a certain cell is visible, given the current offset.
 * If the cell is already visible then the current offset will be returned.
 * If the current offset is too great or small, it will be adjusted just enough to ensure the specified index is visible.
 *
 * @param align Desired alignment within container; one of "auto" (default), "start", or "end"
 * @param cellOffset Offset (x or y) position for cell
 * @param cellSize Size (width or height) of cell
 * @param containerSize Total size (width or height) of the container
 * @param currentOffset Container's current (x or y) offset
 * @return Offset to use to ensure the specified cell is visible
 */
function getUpdatedOffsetForIndex(_ref) {
  var _ref$align = _ref.align,
      align = _ref$align === undefined ? 'auto' : _ref$align,
      cellOffset = _ref.cellOffset,
      cellSize = _ref.cellSize,
      containerSize = _ref.containerSize,
      currentOffset = _ref.currentOffset;

  var maxOffset = cellOffset;
  var minOffset = maxOffset - containerSize + cellSize;

  switch (align) {
    case 'start':
      return maxOffset;
    case 'end':
      return minOffset;
    case 'center':
      return maxOffset - (containerSize - cellSize) / 2;
    default:
      return Math.max(minOffset, Math.min(maxOffset, currentOffset));
  }
}

/***/ }),
/* 234 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ColumnSizer__ = __webpack_require__(235);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__ColumnSizer__["a"]; });


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__ColumnSizer__["a" /* default */]);


/***/ }),
/* 235 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);








/**
 * High-order component that auto-calculates column-widths for `Grid` cells.
 */

var ColumnSizer = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default()(ColumnSizer, _PureComponent);

  function ColumnSizer(props, context) {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, ColumnSizer);

    var _this = __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(this, (ColumnSizer.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(ColumnSizer)).call(this, props, context));

    _this._registerChild = _this._registerChild.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(ColumnSizer, [{
    key: 'componentDidUpdate',
    value: function componentDidUpdate(prevProps) {
      var _props = this.props,
          columnMaxWidth = _props.columnMaxWidth,
          columnMinWidth = _props.columnMinWidth,
          columnCount = _props.columnCount,
          width = _props.width;


      if (columnMaxWidth !== prevProps.columnMaxWidth || columnMinWidth !== prevProps.columnMinWidth || columnCount !== prevProps.columnCount || width !== prevProps.width) {
        if (this._registeredChild) {
          this._registeredChild.recomputeGridSize();
        }
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _props2 = this.props,
          children = _props2.children,
          columnMaxWidth = _props2.columnMaxWidth,
          columnMinWidth = _props2.columnMinWidth,
          columnCount = _props2.columnCount,
          width = _props2.width;


      var safeColumnMinWidth = columnMinWidth || 1;

      var safeColumnMaxWidth = columnMaxWidth ? Math.min(columnMaxWidth, width) : width;

      var columnWidth = width / columnCount;
      columnWidth = Math.max(safeColumnMinWidth, columnWidth);
      columnWidth = Math.min(safeColumnMaxWidth, columnWidth);
      columnWidth = Math.floor(columnWidth);

      var adjustedWidth = Math.min(width, columnWidth * columnCount);

      return children({
        adjustedWidth: adjustedWidth,
        columnWidth: columnWidth,
        getColumnWidth: function getColumnWidth() {
          return columnWidth;
        },
        registerChild: this._registerChild
      });
    }
  }, {
    key: '_registerChild',
    value: function _registerChild(child) {
      if (child && typeof child.recomputeGridSize !== 'function') {
        throw Error('Unexpected child type registered; only Grid/MultiGrid children are supported.');
      }

      this._registeredChild = child;

      if (this._registeredChild) {
        this._registeredChild.recomputeGridSize();
      }
    }
  }]);

  return ColumnSizer;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

/* harmony default export */ __webpack_exports__["a"] = (ColumnSizer);
ColumnSizer.propTypes =  true ? {
  /**
   * Function responsible for rendering a virtualized Grid.
   * This function should implement the following signature:
   * ({ adjustedWidth, getColumnWidth, registerChild }) => PropTypes.element
   *
   * The specified :getColumnWidth function should be passed to the Grid's :columnWidth property.
   * The :registerChild should be passed to the Grid's :ref property.
   * The :adjustedWidth property is optional; it reflects the lesser of the overall width or the width of all columns.
   */
  children: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.func.isRequired,

  /** Optional maximum allowed column width */
  columnMaxWidth: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.number,

  /** Optional minimum allowed column width */
  columnMinWidth: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.number,

  /** Number of columns in Grid or Table child */
  columnCount: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.number.isRequired,

  /** Width of Grid or Table child */
  width: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.number.isRequired
} : {};

/***/ }),
/* 236 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__InfiniteLoader__ = __webpack_require__(237);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__InfiniteLoader__["a"]; });


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__InfiniteLoader__["a" /* default */]);


/***/ }),
/* 237 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export isRangeVisible */
/* unused harmony export scanForUnloadedRanges */
/* unused harmony export forceUpdateReactVirtualizedComponent */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__utils_createCallbackMemoizer__ = __webpack_require__(66);









/**
 * Higher-order component that manages lazy-loading for "infinite" data.
 * This component decorates a virtual component and just-in-time prefetches rows as a user scrolls.
 * It is intended as a convenience component; fork it if you'd like finer-grained control over data-loading.
 */

var InfiniteLoader = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default()(InfiniteLoader, _PureComponent);

  function InfiniteLoader(props, context) {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, InfiniteLoader);

    var _this = __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(this, (InfiniteLoader.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(InfiniteLoader)).call(this, props, context));

    _this._loadMoreRowsMemoizer = Object(__WEBPACK_IMPORTED_MODULE_7__utils_createCallbackMemoizer__["a" /* default */])();

    _this._onRowsRendered = _this._onRowsRendered.bind(_this);
    _this._registerChild = _this._registerChild.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(InfiniteLoader, [{
    key: 'resetLoadMoreRowsCache',
    value: function resetLoadMoreRowsCache(autoReload) {
      this._loadMoreRowsMemoizer = Object(__WEBPACK_IMPORTED_MODULE_7__utils_createCallbackMemoizer__["a" /* default */])();

      if (autoReload) {
        this._doStuff(this._lastRenderedStartIndex, this._lastRenderedStopIndex);
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var children = this.props.children;


      return children({
        onRowsRendered: this._onRowsRendered,
        registerChild: this._registerChild
      });
    }
  }, {
    key: '_loadUnloadedRanges',
    value: function _loadUnloadedRanges(unloadedRanges) {
      var _this2 = this;

      var loadMoreRows = this.props.loadMoreRows;


      unloadedRanges.forEach(function (unloadedRange) {
        var promise = loadMoreRows(unloadedRange);
        if (promise) {
          promise.then(function () {
            // Refresh the visible rows if any of them have just been loaded.
            // Otherwise they will remain in their unloaded visual state.
            if (isRangeVisible({
              lastRenderedStartIndex: _this2._lastRenderedStartIndex,
              lastRenderedStopIndex: _this2._lastRenderedStopIndex,
              startIndex: unloadedRange.startIndex,
              stopIndex: unloadedRange.stopIndex
            })) {
              if (_this2._registeredChild) {
                forceUpdateReactVirtualizedComponent(_this2._registeredChild, _this2._lastRenderedStartIndex);
              }
            }
          });
        }
      });
    }
  }, {
    key: '_onRowsRendered',
    value: function _onRowsRendered(_ref) {
      var startIndex = _ref.startIndex,
          stopIndex = _ref.stopIndex;

      this._lastRenderedStartIndex = startIndex;
      this._lastRenderedStopIndex = stopIndex;

      this._doStuff(startIndex, stopIndex);
    }
  }, {
    key: '_doStuff',
    value: function _doStuff(startIndex, stopIndex) {
      var _this3 = this;

      var _props = this.props,
          isRowLoaded = _props.isRowLoaded,
          minimumBatchSize = _props.minimumBatchSize,
          rowCount = _props.rowCount,
          threshold = _props.threshold;


      var unloadedRanges = scanForUnloadedRanges({
        isRowLoaded: isRowLoaded,
        minimumBatchSize: minimumBatchSize,
        rowCount: rowCount,
        startIndex: Math.max(0, startIndex - threshold),
        stopIndex: Math.min(rowCount - 1, stopIndex + threshold)
      });

      // For memoize comparison
      var squashedUnloadedRanges = unloadedRanges.reduce(function (reduced, unloadedRange) {
        return reduced.concat([unloadedRange.startIndex, unloadedRange.stopIndex]);
      }, []);

      this._loadMoreRowsMemoizer({
        callback: function callback() {
          _this3._loadUnloadedRanges(unloadedRanges);
        },
        indices: { squashedUnloadedRanges: squashedUnloadedRanges }
      });
    }
  }, {
    key: '_registerChild',
    value: function _registerChild(registeredChild) {
      this._registeredChild = registeredChild;
    }
  }]);

  return InfiniteLoader;
}(__WEBPACK_IMPORTED_MODULE_5_react__["PureComponent"]);

/**
 * Determines if the specified start/stop range is visible based on the most recently rendered range.
 */


InfiniteLoader.defaultProps = {
  minimumBatchSize: 10,
  rowCount: 0,
  threshold: 15
};
/* harmony default export */ __webpack_exports__["a"] = (InfiniteLoader);
InfiniteLoader.propTypes =  true ? {
  /**
   * Function responsible for rendering a virtualized component.
   * This function should implement the following signature:
   * ({ onRowsRendered, registerChild }) => PropTypes.element
   *
   * The specified :onRowsRendered function should be passed through to the child's :onRowsRendered property.
   * The :registerChild callback should be set as the virtualized component's :ref.
   */
  children: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Function responsible for tracking the loaded state of each row.
   * It should implement the following signature: ({ index: number }): boolean
   */
  isRowLoaded: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Callback to be invoked when more rows must be loaded.
   * It should implement the following signature: ({ startIndex, stopIndex }): Promise
   * The returned Promise should be resolved once row data has finished loading.
   * It will be used to determine when to refresh the list with the newly-loaded data.
   * This callback may be called multiple times in reaction to a single scroll event.
   */
  loadMoreRows: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.func.isRequired,

  /**
   * Minimum number of rows to be loaded at a time.
   * This property can be used to batch requests to reduce HTTP requests.
   */
  minimumBatchSize: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Number of rows in list; can be arbitrary high number if actual number is unknown.
   */
  rowCount: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired,

  /**
   * Threshold at which to pre-fetch data.
   * A threshold X means that data will start loading when a user scrolls within X rows.
   * This value defaults to 15.
   */
  threshold: __WEBPACK_IMPORTED_MODULE_6_prop_types___default.a.number.isRequired
} : {};
function isRangeVisible(_ref2) {
  var lastRenderedStartIndex = _ref2.lastRenderedStartIndex,
      lastRenderedStopIndex = _ref2.lastRenderedStopIndex,
      startIndex = _ref2.startIndex,
      stopIndex = _ref2.stopIndex;

  return !(startIndex > lastRenderedStopIndex || stopIndex < lastRenderedStartIndex);
}

/**
 * Returns all of the ranges within a larger range that contain unloaded rows.
 */
function scanForUnloadedRanges(_ref3) {
  var isRowLoaded = _ref3.isRowLoaded,
      minimumBatchSize = _ref3.minimumBatchSize,
      rowCount = _ref3.rowCount,
      startIndex = _ref3.startIndex,
      stopIndex = _ref3.stopIndex;

  var unloadedRanges = [];

  var rangeStartIndex = null;
  var rangeStopIndex = null;

  for (var index = startIndex; index <= stopIndex; index++) {
    var loaded = isRowLoaded({ index: index });

    if (!loaded) {
      rangeStopIndex = index;
      if (rangeStartIndex === null) {
        rangeStartIndex = index;
      }
    } else if (rangeStopIndex !== null) {
      unloadedRanges.push({
        startIndex: rangeStartIndex,
        stopIndex: rangeStopIndex
      });

      rangeStartIndex = rangeStopIndex = null;
    }
  }

  // If :rangeStopIndex is not null it means we haven't ran out of unloaded rows.
  // Scan forward to try filling our :minimumBatchSize.
  if (rangeStopIndex !== null) {
    var potentialStopIndex = Math.min(Math.max(rangeStopIndex, rangeStartIndex + minimumBatchSize - 1), rowCount - 1);

    for (var _index = rangeStopIndex + 1; _index <= potentialStopIndex; _index++) {
      if (!isRowLoaded({ index: _index })) {
        rangeStopIndex = _index;
      } else {
        break;
      }
    }

    unloadedRanges.push({
      startIndex: rangeStartIndex,
      stopIndex: rangeStopIndex
    });
  }

  // Check to see if our first range ended prematurely.
  // In this case we should scan backwards to try filling our :minimumBatchSize.
  if (unloadedRanges.length) {
    var firstUnloadedRange = unloadedRanges[0];

    while (firstUnloadedRange.stopIndex - firstUnloadedRange.startIndex + 1 < minimumBatchSize && firstUnloadedRange.startIndex > 0) {
      var _index2 = firstUnloadedRange.startIndex - 1;

      if (!isRowLoaded({ index: _index2 })) {
        firstUnloadedRange.startIndex = _index2;
      } else {
        break;
      }
    }
  }

  return unloadedRanges;
}

/**
 * Since RV components use shallowCompare we need to force a render (even though props haven't changed).
 * However InfiniteLoader may wrap a Grid or it may wrap a Table or List.
 * In the first case the built-in React forceUpdate() method is sufficient to force a re-render,
 * But in the latter cases we need to use the RV-specific forceUpdateGrid() method.
 * Else the inner Grid will not be re-rendered and visuals may be stale.
 *
 * Additionally, while a Grid is scrolling the cells can be cached,
 * So it's important to invalidate that cache by recalculating sizes
 * before forcing a rerender.
 */
function forceUpdateReactVirtualizedComponent(component) {
  var currentIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

  var recomputeSize = typeof component.recomputeGridSize === 'function' ? component.recomputeGridSize : component.recomputeRowHeights;

  if (recomputeSize) {
    recomputeSize.call(component, currentIndex);
  } else {
    component.forceUpdate();
  }
}

/***/ }),
/* 238 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__List__ = __webpack_require__(103);
/* unused harmony reexport default */
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__List__["a"]; });






/***/ }),
/* 239 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(240), __esModule: true };

/***/ }),
/* 240 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(241);
var $Object = __webpack_require__(8).Object;
module.exports = function getOwnPropertyDescriptor(it, key) {
  return $Object.getOwnPropertyDescriptor(it, key);
};


/***/ }),
/* 241 */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
var toIObject = __webpack_require__(18);
var $getOwnPropertyDescriptor = __webpack_require__(63).f;

__webpack_require__(50)('getOwnPropertyDescriptor', function () {
  return function getOwnPropertyDescriptor(it, key) {
    return $getOwnPropertyDescriptor(toIObject(it), key);
  };
});


/***/ }),
/* 242 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__createCellPositioner__ = __webpack_require__(243);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__Masonry__ = __webpack_require__(68);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return __WEBPACK_IMPORTED_MODULE_0__createCellPositioner__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_1__Masonry__["default"]; });



/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_1__Masonry__["default"]);


/***/ }),
/* 243 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createCellPositioner;
var babelPluginFlowReactPropTypes_proptype_Positioner = __webpack_require__(68).babelPluginFlowReactPropTypes_proptype_Positioner || __webpack_require__(0).any;

var babelPluginFlowReactPropTypes_proptype_CellMeasurerCache = __webpack_require__(68).babelPluginFlowReactPropTypes_proptype_CellMeasurerCache || __webpack_require__(0).any;

function createCellPositioner(_ref) {
  var cellMeasurerCache = _ref.cellMeasurerCache,
      columnCount = _ref.columnCount,
      columnWidth = _ref.columnWidth,
      _ref$spacer = _ref.spacer,
      spacer = _ref$spacer === undefined ? 0 : _ref$spacer;

  var columnHeights = void 0;

  initOrResetDerivedValues();

  function cellPositioner(index) {
    // Find the shortest column and use it.
    var columnIndex = 0;
    for (var i = 1; i < columnHeights.length; i++) {
      if (columnHeights[i] < columnHeights[columnIndex]) {
        columnIndex = i;
      }
    }

    var left = columnIndex * (columnWidth + spacer);
    var top = columnHeights[columnIndex] || 0;

    columnHeights[columnIndex] = top + cellMeasurerCache.getHeight(index) + spacer;

    return {
      left: left,
      top: top
    };
  }

  function initOrResetDerivedValues() {
    // Track the height of each column.
    // Layout algorithm below always inserts into the shortest column.
    columnHeights = [];
    for (var i = 0; i < columnCount; i++) {
      columnHeights[i] = 0;
    }
  }

  function reset(params) {
    columnCount = params.columnCount;
    columnWidth = params.columnWidth;
    spacer = params.spacer;

    initOrResetDerivedValues();
  }

  cellPositioner.reset = reset;

  return cellPositioner;
}

/***/ }),
/* 244 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_slicedToArray__ = __webpack_require__(245);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_slicedToArray___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_slicedToArray__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__vendor_intervalTree__ = __webpack_require__(253);





// Position cache requirements:
//   O(log(n)) lookup of cells to render for a given viewport size
//   O(1) lookup of shortest measured column (so we know when to enter phase 1)
var PositionCache = function () {
  function PositionCache() {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, PositionCache);

    this._columnSizeMap = {};
    this._intervalTree = Object(__WEBPACK_IMPORTED_MODULE_3__vendor_intervalTree__["a" /* default */])();
    this._leftMap = {};
  }
  // Tracks the height of each column


  // Store tops and bottoms of each cell for fast intersection lookup.


  // Maps cell index to x coordinates for quick lookup.


  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(PositionCache, [{
    key: 'estimateTotalHeight',
    value: function estimateTotalHeight(cellCount, columnCount, defaultCellHeight) {
      var unmeasuredCellCount = cellCount - this.count;
      return this.tallestColumnSize + Math.ceil(unmeasuredCellCount / columnCount) * defaultCellHeight;
    }

    // Render all cells visible within the viewport range defined.

  }, {
    key: 'range',
    value: function range(scrollTop, clientHeight, renderCallback) {
      var _this = this;

      this._intervalTree.queryInterval(scrollTop, scrollTop + clientHeight, function (_ref) {
        var _ref2 = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_slicedToArray___default()(_ref, 3),
            top = _ref2[0],
            _ = _ref2[1],
            index = _ref2[2];

        return renderCallback(index, _this._leftMap[index], top);
      });
    }
  }, {
    key: 'setPosition',
    value: function setPosition(index, left, top, height) {
      this._intervalTree.insert([top, top + height, index]);
      this._leftMap[index] = left;

      var columnSizeMap = this._columnSizeMap;
      var columnHeight = columnSizeMap[left];
      if (columnHeight === undefined) {
        columnSizeMap[left] = top + height;
      } else {
        columnSizeMap[left] = Math.max(columnHeight, top + height);
      }
    }
  }, {
    key: 'count',
    get: function get() {
      return this._intervalTree.count;
    }
  }, {
    key: 'shortestColumnSize',
    get: function get() {
      var columnSizeMap = this._columnSizeMap;

      var size = 0;

      for (var i in columnSizeMap) {
        var height = columnSizeMap[i];
        size = size === 0 ? height : Math.min(size, height);
      }

      return size;
    }
  }, {
    key: 'tallestColumnSize',
    get: function get() {
      var columnSizeMap = this._columnSizeMap;

      var size = 0;

      for (var i in columnSizeMap) {
        var height = columnSizeMap[i];
        size = Math.max(size, height);
      }

      return size;
    }
  }]);

  return PositionCache;
}();

/* harmony default export */ __webpack_exports__["a"] = (PositionCache);

/***/ }),
/* 245 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _isIterable2 = __webpack_require__(246);

var _isIterable3 = _interopRequireDefault(_isIterable2);

var _getIterator2 = __webpack_require__(249);

var _getIterator3 = _interopRequireDefault(_getIterator2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function () {
  function sliceIterator(arr, i) {
    var _arr = [];
    var _n = true;
    var _d = false;
    var _e = undefined;

    try {
      for (var _i = (0, _getIterator3.default)(arr), _s; !(_n = (_s = _i.next()).done); _n = true) {
        _arr.push(_s.value);

        if (i && _arr.length === i) break;
      }
    } catch (err) {
      _d = true;
      _e = err;
    } finally {
      try {
        if (!_n && _i["return"]) _i["return"]();
      } finally {
        if (_d) throw _e;
      }
    }

    return _arr;
  }

  return function (arr, i) {
    if (Array.isArray(arr)) {
      return arr;
    } else if ((0, _isIterable3.default)(Object(arr))) {
      return sliceIterator(arr, i);
    } else {
      throw new TypeError("Invalid attempt to destructure non-iterable instance");
    }
  };
}();

/***/ }),
/* 246 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(247), __esModule: true };

/***/ }),
/* 247 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(59);
__webpack_require__(52);
module.exports = __webpack_require__(248);


/***/ }),
/* 248 */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(104);
var ITERATOR = __webpack_require__(12)('iterator');
var Iterators = __webpack_require__(26);
module.exports = __webpack_require__(8).isIterable = function (it) {
  var O = Object(it);
  return O[ITERATOR] !== undefined
    || '@@iterator' in O
    // eslint-disable-next-line no-prototype-builtins
    || Iterators.hasOwnProperty(classof(O));
};


/***/ }),
/* 249 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = { "default": __webpack_require__(250), __esModule: true };

/***/ }),
/* 250 */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(59);
__webpack_require__(52);
module.exports = __webpack_require__(251);


/***/ }),
/* 251 */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(20);
var get = __webpack_require__(252);
module.exports = __webpack_require__(8).getIterator = function (it) {
  var iterFn = get(it);
  if (typeof iterFn != 'function') throw TypeError(it + ' is not iterable!');
  return anObject(iterFn.call(it));
};


/***/ }),
/* 252 */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(104);
var ITERATOR = __webpack_require__(12)('iterator');
var Iterators = __webpack_require__(26);
module.exports = __webpack_require__(8).getIteratorMethod = function (it) {
  if (it != undefined) return it[ITERATOR]
    || it['@@iterator']
    || Iterators[classof(it)];
};


/***/ }),
/* 253 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createWrapper;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__binarySearchBounds__ = __webpack_require__(254);
/**
 * Binary Search Bounds
 * https://github.com/mikolalysenko/interval-tree-1d
 * Mikola Lysenko
 *
 * Inlined because of Content Security Policy issue caused by the use of `new Function(...)` syntax in an upstream dependency.
 * Issue reported here: https://github.com/mikolalysenko/binary-search-bounds/issues/5
 **/



var NOT_FOUND = 0;
var SUCCESS = 1;
var EMPTY = 2;

function IntervalTreeNode(mid, left, right, leftPoints, rightPoints) {
  this.mid = mid;
  this.left = left;
  this.right = right;
  this.leftPoints = leftPoints;
  this.rightPoints = rightPoints;
  this.count = (left ? left.count : 0) + (right ? right.count : 0) + leftPoints.length;
}

var proto = IntervalTreeNode.prototype;

function copy(a, b) {
  a.mid = b.mid;
  a.left = b.left;
  a.right = b.right;
  a.leftPoints = b.leftPoints;
  a.rightPoints = b.rightPoints;
  a.count = b.count;
}

function rebuild(node, intervals) {
  var ntree = createIntervalTree(intervals);
  node.mid = ntree.mid;
  node.left = ntree.left;
  node.right = ntree.right;
  node.leftPoints = ntree.leftPoints;
  node.rightPoints = ntree.rightPoints;
  node.count = ntree.count;
}

function rebuildWithInterval(node, interval) {
  var intervals = node.intervals([]);
  intervals.push(interval);
  rebuild(node, intervals);
}

function rebuildWithoutInterval(node, interval) {
  var intervals = node.intervals([]);
  var idx = intervals.indexOf(interval);
  if (idx < 0) {
    return NOT_FOUND;
  }
  intervals.splice(idx, 1);
  rebuild(node, intervals);
  return SUCCESS;
}

proto.intervals = function (result) {
  result.push.apply(result, this.leftPoints);
  if (this.left) {
    this.left.intervals(result);
  }
  if (this.right) {
    this.right.intervals(result);
  }
  return result;
};

proto.insert = function (interval) {
  var weight = this.count - this.leftPoints.length;
  this.count += 1;
  if (interval[1] < this.mid) {
    if (this.left) {
      if (4 * (this.left.count + 1) > 3 * (weight + 1)) {
        rebuildWithInterval(this, interval);
      } else {
        this.left.insert(interval);
      }
    } else {
      this.left = createIntervalTree([interval]);
    }
  } else if (interval[0] > this.mid) {
    if (this.right) {
      if (4 * (this.right.count + 1) > 3 * (weight + 1)) {
        rebuildWithInterval(this, interval);
      } else {
        this.right.insert(interval);
      }
    } else {
      this.right = createIntervalTree([interval]);
    }
  } else {
    var l = __WEBPACK_IMPORTED_MODULE_0__binarySearchBounds__["a" /* default */].ge(this.leftPoints, interval, compareBegin);
    var r = __WEBPACK_IMPORTED_MODULE_0__binarySearchBounds__["a" /* default */].ge(this.rightPoints, interval, compareEnd);
    this.leftPoints.splice(l, 0, interval);
    this.rightPoints.splice(r, 0, interval);
  }
};

proto.remove = function (interval) {
  var weight = this.count - this.leftPoints;
  if (interval[1] < this.mid) {
    if (!this.left) {
      return NOT_FOUND;
    }
    var rw = this.right ? this.right.count : 0;
    if (4 * rw > 3 * (weight - 1)) {
      return rebuildWithoutInterval(this, interval);
    }
    var r = this.left.remove(interval);
    if (r === EMPTY) {
      this.left = null;
      this.count -= 1;
      return SUCCESS;
    } else if (r === SUCCESS) {
      this.count -= 1;
    }
    return r;
  } else if (interval[0] > this.mid) {
    if (!this.right) {
      return NOT_FOUND;
    }
    var lw = this.left ? this.left.count : 0;
    if (4 * lw > 3 * (weight - 1)) {
      return rebuildWithoutInterval(this, interval);
    }
    var r = this.right.remove(interval);
    if (r === EMPTY) {
      this.right = null;
      this.count -= 1;
      return SUCCESS;
    } else if (r === SUCCESS) {
      this.count -= 1;
    }
    return r;
  } else {
    if (this.count === 1) {
      if (this.leftPoints[0] === interval) {
        return EMPTY;
      } else {
        return NOT_FOUND;
      }
    }
    if (this.leftPoints.length === 1 && this.leftPoints[0] === interval) {
      if (this.left && this.right) {
        var p = this;
        var n = this.left;
        while (n.right) {
          p = n;
          n = n.right;
        }
        if (p === this) {
          n.right = this.right;
        } else {
          var l = this.left;
          var r = this.right;
          p.count -= n.count;
          p.right = n.left;
          n.left = l;
          n.right = r;
        }
        copy(this, n);
        this.count = (this.left ? this.left.count : 0) + (this.right ? this.right.count : 0) + this.leftPoints.length;
      } else if (this.left) {
        copy(this, this.left);
      } else {
        copy(this, this.right);
      }
      return SUCCESS;
    }
    for (var l = __WEBPACK_IMPORTED_MODULE_0__binarySearchBounds__["a" /* default */].ge(this.leftPoints, interval, compareBegin); l < this.leftPoints.length; ++l) {
      if (this.leftPoints[l][0] !== interval[0]) {
        break;
      }
      if (this.leftPoints[l] === interval) {
        this.count -= 1;
        this.leftPoints.splice(l, 1);
        for (var r = __WEBPACK_IMPORTED_MODULE_0__binarySearchBounds__["a" /* default */].ge(this.rightPoints, interval, compareEnd); r < this.rightPoints.length; ++r) {
          if (this.rightPoints[r][1] !== interval[1]) {
            break;
          } else if (this.rightPoints[r] === interval) {
            this.rightPoints.splice(r, 1);
            return SUCCESS;
          }
        }
      }
    }
    return NOT_FOUND;
  }
};

function reportLeftRange(arr, hi, cb) {
  for (var i = 0; i < arr.length && arr[i][0] <= hi; ++i) {
    var r = cb(arr[i]);
    if (r) {
      return r;
    }
  }
}

function reportRightRange(arr, lo, cb) {
  for (var i = arr.length - 1; i >= 0 && arr[i][1] >= lo; --i) {
    var r = cb(arr[i]);
    if (r) {
      return r;
    }
  }
}

function reportRange(arr, cb) {
  for (var i = 0; i < arr.length; ++i) {
    var r = cb(arr[i]);
    if (r) {
      return r;
    }
  }
}

proto.queryPoint = function (x, cb) {
  if (x < this.mid) {
    if (this.left) {
      var r = this.left.queryPoint(x, cb);
      if (r) {
        return r;
      }
    }
    return reportLeftRange(this.leftPoints, x, cb);
  } else if (x > this.mid) {
    if (this.right) {
      var r = this.right.queryPoint(x, cb);
      if (r) {
        return r;
      }
    }
    return reportRightRange(this.rightPoints, x, cb);
  } else {
    return reportRange(this.leftPoints, cb);
  }
};

proto.queryInterval = function (lo, hi, cb) {
  if (lo < this.mid && this.left) {
    var r = this.left.queryInterval(lo, hi, cb);
    if (r) {
      return r;
    }
  }
  if (hi > this.mid && this.right) {
    var r = this.right.queryInterval(lo, hi, cb);
    if (r) {
      return r;
    }
  }
  if (hi < this.mid) {
    return reportLeftRange(this.leftPoints, hi, cb);
  } else if (lo > this.mid) {
    return reportRightRange(this.rightPoints, lo, cb);
  } else {
    return reportRange(this.leftPoints, cb);
  }
};

function compareNumbers(a, b) {
  return a - b;
}

function compareBegin(a, b) {
  var d = a[0] - b[0];
  if (d) {
    return d;
  }
  return a[1] - b[1];
}

function compareEnd(a, b) {
  var d = a[1] - b[1];
  if (d) {
    return d;
  }
  return a[0] - b[0];
}

function createIntervalTree(intervals) {
  if (intervals.length === 0) {
    return null;
  }
  var pts = [];
  for (var i = 0; i < intervals.length; ++i) {
    pts.push(intervals[i][0], intervals[i][1]);
  }
  pts.sort(compareNumbers);

  var mid = pts[pts.length >> 1];

  var leftIntervals = [];
  var rightIntervals = [];
  var centerIntervals = [];
  for (var i = 0; i < intervals.length; ++i) {
    var s = intervals[i];
    if (s[1] < mid) {
      leftIntervals.push(s);
    } else if (mid < s[0]) {
      rightIntervals.push(s);
    } else {
      centerIntervals.push(s);
    }
  }

  //Split center intervals
  var leftPoints = centerIntervals;
  var rightPoints = centerIntervals.slice();
  leftPoints.sort(compareBegin);
  rightPoints.sort(compareEnd);

  return new IntervalTreeNode(mid, createIntervalTree(leftIntervals), createIntervalTree(rightIntervals), leftPoints, rightPoints);
}

//User friendly wrapper that makes it possible to support empty trees
function IntervalTree(root) {
  this.root = root;
}

var tproto = IntervalTree.prototype;

tproto.insert = function (interval) {
  if (this.root) {
    this.root.insert(interval);
  } else {
    this.root = new IntervalTreeNode(interval[0], null, null, [interval], [interval]);
  }
};

tproto.remove = function (interval) {
  if (this.root) {
    var r = this.root.remove(interval);
    if (r === EMPTY) {
      this.root = null;
    }
    return r !== NOT_FOUND;
  }
  return false;
};

tproto.queryPoint = function (p, cb) {
  if (this.root) {
    return this.root.queryPoint(p, cb);
  }
};

tproto.queryInterval = function (lo, hi, cb) {
  if (lo <= hi && this.root) {
    return this.root.queryInterval(lo, hi, cb);
  }
};

Object.defineProperty(tproto, 'count', {
  get: function get() {
    if (this.root) {
      return this.root.count;
    }
    return 0;
  }
});

Object.defineProperty(tproto, 'intervals', {
  get: function get() {
    if (this.root) {
      return this.root.intervals([]);
    }
    return [];
  }
});

function createWrapper(intervals) {
  if (!intervals || intervals.length === 0) {
    return new IntervalTree(null);
  }
  return new IntervalTree(createIntervalTree(intervals));
}

/***/ }),
/* 254 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/**
 * Binary Search Bounds
 * https://github.com/mikolalysenko/binary-search-bounds
 * Mikola Lysenko
 *
 * Inlined because of Content Security Policy issue caused by the use of `new Function(...)` syntax.
 * Issue reported here: https://github.com/mikolalysenko/binary-search-bounds/issues/5
 **/

function _GEA(a, l, h, y) {
  var i = h + 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (x >= y) {
      i = m;
      h = m - 1;
    } else {
      l = m + 1;
    }
  }
  return i;
}
function _GEP(a, l, h, y, c) {
  var i = h + 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (c(x, y) >= 0) {
      i = m;
      h = m - 1;
    } else {
      l = m + 1;
    }
  }
  return i;
}
function dispatchBsearchGE(a, y, c, l, h) {
  if (typeof c === 'function') {
    return _GEP(a, l === void 0 ? 0 : l | 0, h === void 0 ? a.length - 1 : h | 0, y, c);
  } else {
    return _GEA(a, c === void 0 ? 0 : c | 0, l === void 0 ? a.length - 1 : l | 0, y);
  }
}

function _GTA(a, l, h, y) {
  var i = h + 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (x > y) {
      i = m;
      h = m - 1;
    } else {
      l = m + 1;
    }
  }
  return i;
}
function _GTP(a, l, h, y, c) {
  var i = h + 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (c(x, y) > 0) {
      i = m;
      h = m - 1;
    } else {
      l = m + 1;
    }
  }
  return i;
}
function dispatchBsearchGT(a, y, c, l, h) {
  if (typeof c === 'function') {
    return _GTP(a, l === void 0 ? 0 : l | 0, h === void 0 ? a.length - 1 : h | 0, y, c);
  } else {
    return _GTA(a, c === void 0 ? 0 : c | 0, l === void 0 ? a.length - 1 : l | 0, y);
  }
}

function _LTA(a, l, h, y) {
  var i = l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (x < y) {
      i = m;
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return i;
}
function _LTP(a, l, h, y, c) {
  var i = l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (c(x, y) < 0) {
      i = m;
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return i;
}
function dispatchBsearchLT(a, y, c, l, h) {
  if (typeof c === 'function') {
    return _LTP(a, l === void 0 ? 0 : l | 0, h === void 0 ? a.length - 1 : h | 0, y, c);
  } else {
    return _LTA(a, c === void 0 ? 0 : c | 0, l === void 0 ? a.length - 1 : l | 0, y);
  }
}

function _LEA(a, l, h, y) {
  var i = l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (x <= y) {
      i = m;
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return i;
}
function _LEP(a, l, h, y, c) {
  var i = l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (c(x, y) <= 0) {
      i = m;
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return i;
}
function dispatchBsearchLE(a, y, c, l, h) {
  if (typeof c === 'function') {
    return _LEP(a, l === void 0 ? 0 : l | 0, h === void 0 ? a.length - 1 : h | 0, y, c);
  } else {
    return _LEA(a, c === void 0 ? 0 : c | 0, l === void 0 ? a.length - 1 : l | 0, y);
  }
}

function _EQA(a, l, h, y) {
  l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    if (x === y) {
      return m;
    } else if (x <= y) {
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return -1;
}
function _EQP(a, l, h, y, c) {
  l - 1;
  while (l <= h) {
    var m = l + h >>> 1,
        x = a[m];
    var p = c(x, y);
    if (p === 0) {
      return m;
    } else if (p <= 0) {
      l = m + 1;
    } else {
      h = m - 1;
    }
  }
  return -1;
}
function dispatchBsearchEQ(a, y, c, l, h) {
  if (typeof c === 'function') {
    return _EQP(a, l === void 0 ? 0 : l | 0, h === void 0 ? a.length - 1 : h | 0, y, c);
  } else {
    return _EQA(a, c === void 0 ? 0 : c | 0, l === void 0 ? a.length - 1 : l | 0, y);
  }
}

/* harmony default export */ __webpack_exports__["a"] = ({
  ge: dispatchBsearchGE,
  gt: dispatchBsearchGT,
  lt: dispatchBsearchLT,
  le: dispatchBsearchLE,
  eq: dispatchBsearchEQ
});

/***/ }),
/* 255 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__MultiGrid__ = __webpack_require__(256);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__MultiGrid__["a"]; });


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__MultiGrid__["a" /* default */]);


/***/ }),
/* 256 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties__ = __webpack_require__(65);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_7_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_8_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__CellMeasurerCacheDecorator__ = __webpack_require__(257);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__Grid__ = __webpack_require__(9);












var SCROLLBAR_SIZE_BUFFER = 20;

/**
 * Renders 1, 2, or 4 Grids depending on configuration.
 * A main (body) Grid will always be rendered.
 * Optionally, 1-2 Grids for sticky header rows will also be rendered.
 * If no sticky columns, only 1 sticky header Grid will be rendered.
 * If sticky columns, 2 sticky header Grids will be rendered.
 */

var MultiGrid = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_6_babel_runtime_helpers_inherits___default()(MultiGrid, _PureComponent);

  function MultiGrid(props, context) {
    __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_classCallCheck___default()(this, MultiGrid);

    var _this = __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_possibleConstructorReturn___default()(this, (MultiGrid.__proto__ || __WEBPACK_IMPORTED_MODULE_2_babel_runtime_core_js_object_get_prototype_of___default()(MultiGrid)).call(this, props, context));

    _this.state = {
      scrollLeft: 0,
      scrollTop: 0,
      scrollbarSize: 0,
      showHorizontalScrollbar: false,
      showVerticalScrollbar: false
    };

    _this._deferredInvalidateColumnIndex = null;
    _this._deferredInvalidateRowIndex = null;

    _this._bottomLeftGridRef = _this._bottomLeftGridRef.bind(_this);
    _this._bottomRightGridRef = _this._bottomRightGridRef.bind(_this);
    _this._cellRendererBottomLeftGrid = _this._cellRendererBottomLeftGrid.bind(_this);
    _this._cellRendererBottomRightGrid = _this._cellRendererBottomRightGrid.bind(_this);
    _this._cellRendererTopRightGrid = _this._cellRendererTopRightGrid.bind(_this);
    _this._columnWidthRightGrid = _this._columnWidthRightGrid.bind(_this);
    _this._onScroll = _this._onScroll.bind(_this);
    _this._onScrollbarPresenceChange = _this._onScrollbarPresenceChange.bind(_this);
    _this._onScrollLeft = _this._onScrollLeft.bind(_this);
    _this._onScrollTop = _this._onScrollTop.bind(_this);
    _this._rowHeightBottomGrid = _this._rowHeightBottomGrid.bind(_this);
    _this._topLeftGridRef = _this._topLeftGridRef.bind(_this);
    _this._topRightGridRef = _this._topRightGridRef.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_createClass___default()(MultiGrid, [{
    key: 'forceUpdateGrids',
    value: function forceUpdateGrids() {
      this._bottomLeftGrid && this._bottomLeftGrid.forceUpdate();
      this._bottomRightGrid && this._bottomRightGrid.forceUpdate();
      this._topLeftGrid && this._topLeftGrid.forceUpdate();
      this._topRightGrid && this._topRightGrid.forceUpdate();
    }

    /** See Grid#invalidateCellSizeAfterRender */

  }, {
    key: 'invalidateCellSizeAfterRender',
    value: function invalidateCellSizeAfterRender() {
      var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref$columnIndex = _ref.columnIndex,
          columnIndex = _ref$columnIndex === undefined ? 0 : _ref$columnIndex,
          _ref$rowIndex = _ref.rowIndex,
          rowIndex = _ref$rowIndex === undefined ? 0 : _ref$rowIndex;

      this._deferredInvalidateColumnIndex = typeof this._deferredInvalidateColumnIndex === 'number' ? Math.min(this._deferredInvalidateColumnIndex, columnIndex) : columnIndex;
      this._deferredInvalidateRowIndex = typeof this._deferredInvalidateRowIndex === 'number' ? Math.min(this._deferredInvalidateRowIndex, rowIndex) : rowIndex;
    }

    /** See Grid#measureAllCells */

  }, {
    key: 'measureAllCells',
    value: function measureAllCells() {
      this._bottomLeftGrid && this._bottomLeftGrid.measureAllCells();
      this._bottomRightGrid && this._bottomRightGrid.measureAllCells();
      this._topLeftGrid && this._topLeftGrid.measureAllCells();
      this._topRightGrid && this._topRightGrid.measureAllCells();
    }

    /** See Grid#recomputeGridSize */

  }, {
    key: 'recomputeGridSize',
    value: function recomputeGridSize() {
      var _ref2 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref2$columnIndex = _ref2.columnIndex,
          columnIndex = _ref2$columnIndex === undefined ? 0 : _ref2$columnIndex,
          _ref2$rowIndex = _ref2.rowIndex,
          rowIndex = _ref2$rowIndex === undefined ? 0 : _ref2$rowIndex;

      var _props = this.props,
          fixedColumnCount = _props.fixedColumnCount,
          fixedRowCount = _props.fixedRowCount;


      var adjustedColumnIndex = Math.max(0, columnIndex - fixedColumnCount);
      var adjustedRowIndex = Math.max(0, rowIndex - fixedRowCount);

      this._bottomLeftGrid && this._bottomLeftGrid.recomputeGridSize({
        columnIndex: columnIndex,
        rowIndex: adjustedRowIndex
      });
      this._bottomRightGrid && this._bottomRightGrid.recomputeGridSize({
        columnIndex: adjustedColumnIndex,
        rowIndex: adjustedRowIndex
      });
      this._topLeftGrid && this._topLeftGrid.recomputeGridSize({
        columnIndex: columnIndex,
        rowIndex: rowIndex
      });
      this._topRightGrid && this._topRightGrid.recomputeGridSize({
        columnIndex: adjustedColumnIndex,
        rowIndex: rowIndex
      });

      this._leftGridWidth = null;
      this._topGridHeight = null;
      this._maybeCalculateCachedStyles(null, this.props, null, this.state);
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      var _props2 = this.props,
          scrollLeft = _props2.scrollLeft,
          scrollTop = _props2.scrollTop;


      if (scrollLeft > 0 || scrollTop > 0) {
        var newState = {};

        if (scrollLeft > 0) {
          newState.scrollLeft = scrollLeft;
        }

        if (scrollTop > 0) {
          newState.scrollTop = scrollTop;
        }

        this.setState(newState);
      }
      this._handleInvalidatedGridSize();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      this._handleInvalidatedGridSize();
    }
  }, {
    key: 'componentWillMount',
    value: function componentWillMount() {
      var _props3 = this.props,
          deferredMeasurementCache = _props3.deferredMeasurementCache,
          fixedColumnCount = _props3.fixedColumnCount,
          fixedRowCount = _props3.fixedRowCount;


      this._maybeCalculateCachedStyles(null, this.props, null, this.state);

      if (deferredMeasurementCache) {
        this._deferredMeasurementCacheBottomLeftGrid = fixedRowCount > 0 ? new __WEBPACK_IMPORTED_MODULE_9__CellMeasurerCacheDecorator__["a" /* default */]({
          cellMeasurerCache: deferredMeasurementCache,
          columnIndexOffset: 0,
          rowIndexOffset: fixedRowCount
        }) : deferredMeasurementCache;

        this._deferredMeasurementCacheBottomRightGrid = fixedColumnCount > 0 || fixedRowCount > 0 ? new __WEBPACK_IMPORTED_MODULE_9__CellMeasurerCacheDecorator__["a" /* default */]({
          cellMeasurerCache: deferredMeasurementCache,
          columnIndexOffset: fixedColumnCount,
          rowIndexOffset: fixedRowCount
        }) : deferredMeasurementCache;

        this._deferredMeasurementCacheTopRightGrid = fixedColumnCount > 0 ? new __WEBPACK_IMPORTED_MODULE_9__CellMeasurerCacheDecorator__["a" /* default */]({
          cellMeasurerCache: deferredMeasurementCache,
          columnIndexOffset: fixedColumnCount,
          rowIndexOffset: 0
        }) : deferredMeasurementCache;
      }
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps, nextState) {
      var _props4 = this.props,
          columnWidth = _props4.columnWidth,
          fixedColumnCount = _props4.fixedColumnCount,
          fixedRowCount = _props4.fixedRowCount,
          rowHeight = _props4.rowHeight;


      if (columnWidth !== nextProps.columnWidth || fixedColumnCount !== nextProps.fixedColumnCount) {
        this._leftGridWidth = null;
      }

      if (fixedRowCount !== nextProps.fixedRowCount || rowHeight !== nextProps.rowHeight) {
        this._topGridHeight = null;
      }

      if (nextProps.scrollLeft !== this.props.scrollLeft || nextProps.scrollTop !== this.props.scrollTop) {
        var newState = {};

        if (nextProps.scrollLeft != null && nextProps.scrollLeft >= 0) {
          newState.scrollLeft = nextProps.scrollLeft;
        }

        if (nextProps.scrollTop != null && nextProps.scrollTop >= 0) {
          newState.scrollTop = nextProps.scrollTop;
        }

        this.setState(newState);
      }

      this._maybeCalculateCachedStyles(this.props, nextProps, this.state, nextState);
    }
  }, {
    key: 'render',
    value: function render() {
      var _props5 = this.props,
          onScroll = _props5.onScroll,
          onSectionRendered = _props5.onSectionRendered,
          onScrollbarPresenceChange = _props5.onScrollbarPresenceChange,
          scrollLeftProp = _props5.scrollLeft,
          scrollToColumn = _props5.scrollToColumn,
          scrollTopProp = _props5.scrollTop,
          scrollToRow = _props5.scrollToRow,
          rest = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default()(_props5, ['onScroll', 'onSectionRendered', 'onScrollbarPresenceChange', 'scrollLeft', 'scrollToColumn', 'scrollTop', 'scrollToRow']);

      // Don't render any of our Grids if there are no cells.
      // This mirrors what Grid does,
      // And prevents us from recording inaccurage measurements when used with CellMeasurer.


      if (this.props.width === 0 || this.props.height === 0) {
        return null;
      }

      // scrollTop and scrollLeft props are explicitly filtered out and ignored

      var _state = this.state,
          scrollLeft = _state.scrollLeft,
          scrollTop = _state.scrollTop;


      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(
        'div',
        { style: this._containerOuterStyle },
        __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(
          'div',
          { style: this._containerTopStyle },
          this._renderTopLeftGrid(rest),
          this._renderTopRightGrid(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
            onScroll: onScroll,
            scrollLeft: scrollLeft
          }))
        ),
        __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(
          'div',
          { style: this._containerBottomStyle },
          this._renderBottomLeftGrid(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
            onScroll: onScroll,
            scrollTop: scrollTop
          })),
          this._renderBottomRightGrid(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
            onScroll: onScroll,
            onSectionRendered: onSectionRendered,
            scrollLeft: scrollLeft,
            scrollToColumn: scrollToColumn,
            scrollToRow: scrollToRow,
            scrollTop: scrollTop
          }))
        )
      );
    }
  }, {
    key: '_bottomLeftGridRef',
    value: function _bottomLeftGridRef(ref) {
      this._bottomLeftGrid = ref;
    }
  }, {
    key: '_bottomRightGridRef',
    value: function _bottomRightGridRef(ref) {
      this._bottomRightGrid = ref;
    }
  }, {
    key: '_cellRendererBottomLeftGrid',
    value: function _cellRendererBottomLeftGrid(_ref3) {
      var rowIndex = _ref3.rowIndex,
          rest = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default()(_ref3, ['rowIndex']);

      var _props6 = this.props,
          cellRenderer = _props6.cellRenderer,
          fixedRowCount = _props6.fixedRowCount,
          rowCount = _props6.rowCount;


      if (rowIndex === rowCount - fixedRowCount) {
        return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement('div', {
          key: rest.key,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest.style, {
            height: SCROLLBAR_SIZE_BUFFER
          })
        });
      } else {
        return cellRenderer(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
          parent: this,
          rowIndex: rowIndex + fixedRowCount
        }));
      }
    }
  }, {
    key: '_cellRendererBottomRightGrid',
    value: function _cellRendererBottomRightGrid(_ref4) {
      var columnIndex = _ref4.columnIndex,
          rowIndex = _ref4.rowIndex,
          rest = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default()(_ref4, ['columnIndex', 'rowIndex']);

      var _props7 = this.props,
          cellRenderer = _props7.cellRenderer,
          fixedColumnCount = _props7.fixedColumnCount,
          fixedRowCount = _props7.fixedRowCount;


      return cellRenderer(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
        columnIndex: columnIndex + fixedColumnCount,
        parent: this,
        rowIndex: rowIndex + fixedRowCount
      }));
    }
  }, {
    key: '_cellRendererTopRightGrid',
    value: function _cellRendererTopRightGrid(_ref5) {
      var columnIndex = _ref5.columnIndex,
          rest = __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_objectWithoutProperties___default()(_ref5, ['columnIndex']);

      var _props8 = this.props,
          cellRenderer = _props8.cellRenderer,
          columnCount = _props8.columnCount,
          fixedColumnCount = _props8.fixedColumnCount;


      if (columnIndex === columnCount - fixedColumnCount) {
        return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement('div', {
          key: rest.key,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest.style, {
            width: SCROLLBAR_SIZE_BUFFER
          })
        });
      } else {
        return cellRenderer(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rest, {
          columnIndex: columnIndex + fixedColumnCount,
          parent: this
        }));
      }
    }
  }, {
    key: '_columnWidthRightGrid',
    value: function _columnWidthRightGrid(_ref6) {
      var index = _ref6.index;
      var _props9 = this.props,
          columnCount = _props9.columnCount,
          fixedColumnCount = _props9.fixedColumnCount,
          columnWidth = _props9.columnWidth;
      var _state2 = this.state,
          scrollbarSize = _state2.scrollbarSize,
          showHorizontalScrollbar = _state2.showHorizontalScrollbar;

      // An extra cell is added to the count
      // This gives the smaller Grid extra room for offset,
      // In case the main (bottom right) Grid has a scrollbar
      // If no scrollbar, the extra space is overflow:hidden anyway

      if (showHorizontalScrollbar && index === columnCount - fixedColumnCount) {
        return scrollbarSize;
      }

      return typeof columnWidth === 'function' ? columnWidth({ index: index + fixedColumnCount }) : columnWidth;
    }
  }, {
    key: '_getBottomGridHeight',
    value: function _getBottomGridHeight(props) {
      var height = props.height;


      var topGridHeight = this._getTopGridHeight(props);

      return height - topGridHeight;
    }
  }, {
    key: '_getLeftGridWidth',
    value: function _getLeftGridWidth(props) {
      var fixedColumnCount = props.fixedColumnCount,
          columnWidth = props.columnWidth;


      if (this._leftGridWidth == null) {
        if (typeof columnWidth === 'function') {
          var leftGridWidth = 0;

          for (var index = 0; index < fixedColumnCount; index++) {
            leftGridWidth += columnWidth({ index: index });
          }

          this._leftGridWidth = leftGridWidth;
        } else {
          this._leftGridWidth = columnWidth * fixedColumnCount;
        }
      }

      return this._leftGridWidth;
    }
  }, {
    key: '_getRightGridWidth',
    value: function _getRightGridWidth(props) {
      var width = props.width;


      var leftGridWidth = this._getLeftGridWidth(props);

      return width - leftGridWidth;
    }
  }, {
    key: '_getTopGridHeight',
    value: function _getTopGridHeight(props) {
      var fixedRowCount = props.fixedRowCount,
          rowHeight = props.rowHeight;


      if (this._topGridHeight == null) {
        if (typeof rowHeight === 'function') {
          var topGridHeight = 0;

          for (var index = 0; index < fixedRowCount; index++) {
            topGridHeight += rowHeight({ index: index });
          }

          this._topGridHeight = topGridHeight;
        } else {
          this._topGridHeight = rowHeight * fixedRowCount;
        }
      }

      return this._topGridHeight;
    }
  }, {
    key: '_handleInvalidatedGridSize',
    value: function _handleInvalidatedGridSize() {
      if (typeof this._deferredInvalidateColumnIndex === 'number') {
        var columnIndex = this._deferredInvalidateColumnIndex;
        var rowIndex = this._deferredInvalidateRowIndex;

        this._deferredInvalidateColumnIndex = null;
        this._deferredInvalidateRowIndex = null;

        this.recomputeGridSize({
          columnIndex: columnIndex,
          rowIndex: rowIndex
        });
        this.forceUpdate();
      }
    }

    /**
     * Avoid recreating inline styles each render; this bypasses Grid's shallowCompare.
     * This method recalculates styles only when specific props change.
     */

  }, {
    key: '_maybeCalculateCachedStyles',
    value: function _maybeCalculateCachedStyles(prevProps, props) {
      var columnWidth = props.columnWidth,
          enableFixedColumnScroll = props.enableFixedColumnScroll,
          enableFixedRowScroll = props.enableFixedRowScroll,
          height = props.height,
          fixedColumnCount = props.fixedColumnCount,
          fixedRowCount = props.fixedRowCount,
          rowHeight = props.rowHeight,
          style = props.style,
          styleBottomLeftGrid = props.styleBottomLeftGrid,
          styleBottomRightGrid = props.styleBottomRightGrid,
          styleTopLeftGrid = props.styleTopLeftGrid,
          styleTopRightGrid = props.styleTopRightGrid,
          width = props.width;


      var firstRender = !prevProps;
      var sizeChange = firstRender || height !== prevProps.height || width !== prevProps.width;
      var leftSizeChange = firstRender || columnWidth !== prevProps.columnWidth || fixedColumnCount !== prevProps.fixedColumnCount;
      var topSizeChange = firstRender || fixedRowCount !== prevProps.fixedRowCount || rowHeight !== prevProps.rowHeight;

      if (firstRender || sizeChange || style !== prevProps.style) {
        this._containerOuterStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          height: height,
          overflow: 'visible', // Let :focus outline show through
          width: width
        }, style);
      }

      if (firstRender || sizeChange || topSizeChange) {
        this._containerTopStyle = {
          height: this._getTopGridHeight(props),
          position: 'relative',
          width: width
        };

        this._containerBottomStyle = {
          height: height - this._getTopGridHeight(props),
          overflow: 'visible', // Let :focus outline show through
          position: 'relative',
          width: width
        };
      }

      if (firstRender || styleBottomLeftGrid !== prevProps.styleBottomLeftGrid) {
        this._bottomLeftGridStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          left: 0,
          overflowX: 'hidden',
          overflowY: enableFixedColumnScroll ? 'auto' : 'hidden',
          position: 'absolute'
        }, styleBottomLeftGrid);
      }

      if (firstRender || leftSizeChange || styleBottomRightGrid !== prevProps.styleBottomRightGrid) {
        this._bottomRightGridStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          left: this._getLeftGridWidth(props),
          position: 'absolute'
        }, styleBottomRightGrid);
      }

      if (firstRender || styleTopLeftGrid !== prevProps.styleTopLeftGrid) {
        this._topLeftGridStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          left: 0,
          overflowX: 'hidden',
          overflowY: 'hidden',
          position: 'absolute',
          top: 0
        }, styleTopLeftGrid);
      }

      if (firstRender || leftSizeChange || styleTopRightGrid !== prevProps.styleTopRightGrid) {
        this._topRightGridStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({
          left: this._getLeftGridWidth(props),
          overflowX: enableFixedRowScroll ? 'auto' : 'hidden',
          overflowY: 'hidden',
          position: 'absolute',
          top: 0
        }, styleTopRightGrid);
      }
    }
  }, {
    key: '_onScroll',
    value: function _onScroll(scrollInfo) {
      var scrollLeft = scrollInfo.scrollLeft,
          scrollTop = scrollInfo.scrollTop;

      this.setState({
        scrollLeft: scrollLeft,
        scrollTop: scrollTop
      });
      var onScroll = this.props.onScroll;
      if (onScroll) {
        onScroll(scrollInfo);
      }
    }
  }, {
    key: '_onScrollbarPresenceChange',
    value: function _onScrollbarPresenceChange(_ref7) {
      var horizontal = _ref7.horizontal,
          size = _ref7.size,
          vertical = _ref7.vertical;
      var _state3 = this.state,
          showHorizontalScrollbar = _state3.showHorizontalScrollbar,
          showVerticalScrollbar = _state3.showVerticalScrollbar;


      if (horizontal !== showHorizontalScrollbar || vertical !== showVerticalScrollbar) {
        this.setState({
          scrollbarSize: size,
          showHorizontalScrollbar: horizontal,
          showVerticalScrollbar: vertical
        });

        var onScrollbarPresenceChange = this.props.onScrollbarPresenceChange;

        if (typeof onScrollbarPresenceChange === 'function') {
          onScrollbarPresenceChange({
            horizontal: horizontal,
            size: size,
            vertical: vertical
          });
        }
      }
    }
  }, {
    key: '_onScrollLeft',
    value: function _onScrollLeft(scrollInfo) {
      var scrollLeft = scrollInfo.scrollLeft;

      this._onScroll({
        scrollLeft: scrollLeft,
        scrollTop: this.state.scrollTop
      });
    }
  }, {
    key: '_onScrollTop',
    value: function _onScrollTop(scrollInfo) {
      var scrollTop = scrollInfo.scrollTop;

      this._onScroll({
        scrollTop: scrollTop,
        scrollLeft: this.state.scrollLeft
      });
    }
  }, {
    key: '_renderBottomLeftGrid',
    value: function _renderBottomLeftGrid(props) {
      var enableFixedColumnScroll = props.enableFixedColumnScroll,
          fixedColumnCount = props.fixedColumnCount,
          fixedRowCount = props.fixedRowCount,
          rowCount = props.rowCount,
          scrollTop = props.scrollTop;
      var showVerticalScrollbar = this.state.showVerticalScrollbar;


      if (!fixedColumnCount) {
        return null;
      }

      var additionalRowCount = showVerticalScrollbar ? 1 : 0;

      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_10__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
        cellRenderer: this._cellRendererBottomLeftGrid,
        className: this.props.classNameBottomLeftGrid,
        columnCount: fixedColumnCount,
        deferredMeasurementCache: this._deferredMeasurementCacheBottomLeftGrid,
        height: this._getBottomGridHeight(props),
        onScroll: enableFixedColumnScroll ? this._onScrollTop : undefined,
        ref: this._bottomLeftGridRef,
        rowCount: Math.max(0, rowCount - fixedRowCount) + additionalRowCount,
        rowHeight: this._rowHeightBottomGrid,
        scrollTop: scrollTop,
        style: this._bottomLeftGridStyle,
        tabIndex: null,
        width: this._getLeftGridWidth(props)
      }));
    }
  }, {
    key: '_renderBottomRightGrid',
    value: function _renderBottomRightGrid(props) {
      var columnCount = props.columnCount,
          fixedColumnCount = props.fixedColumnCount,
          fixedRowCount = props.fixedRowCount,
          rowCount = props.rowCount,
          scrollToColumn = props.scrollToColumn,
          scrollToRow = props.scrollToRow;


      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_10__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
        cellRenderer: this._cellRendererBottomRightGrid,
        className: this.props.classNameBottomRightGrid,
        columnCount: Math.max(0, columnCount - fixedColumnCount),
        columnWidth: this._columnWidthRightGrid,
        deferredMeasurementCache: this._deferredMeasurementCacheBottomRightGrid,
        height: this._getBottomGridHeight(props),
        onScroll: this._onScroll,
        onScrollbarPresenceChange: this._onScrollbarPresenceChange,
        ref: this._bottomRightGridRef,
        rowCount: Math.max(0, rowCount - fixedRowCount),
        rowHeight: this._rowHeightBottomGrid,
        scrollToColumn: scrollToColumn - fixedColumnCount,
        scrollToRow: scrollToRow - fixedRowCount,
        style: this._bottomRightGridStyle,
        width: this._getRightGridWidth(props)
      }));
    }
  }, {
    key: '_renderTopLeftGrid',
    value: function _renderTopLeftGrid(props) {
      var fixedColumnCount = props.fixedColumnCount,
          fixedRowCount = props.fixedRowCount;


      if (!fixedColumnCount || !fixedRowCount) {
        return null;
      }

      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_10__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
        className: this.props.classNameTopLeftGrid,
        columnCount: fixedColumnCount,
        height: this._getTopGridHeight(props),
        ref: this._topLeftGridRef,
        rowCount: fixedRowCount,
        style: this._topLeftGridStyle,
        tabIndex: null,
        width: this._getLeftGridWidth(props)
      }));
    }
  }, {
    key: '_renderTopRightGrid',
    value: function _renderTopRightGrid(props) {
      var columnCount = props.columnCount,
          enableFixedRowScroll = props.enableFixedRowScroll,
          fixedColumnCount = props.fixedColumnCount,
          fixedRowCount = props.fixedRowCount,
          scrollLeft = props.scrollLeft;
      var showHorizontalScrollbar = this.state.showHorizontalScrollbar;


      if (!fixedRowCount) {
        return null;
      }

      var additionalColumnCount = showHorizontalScrollbar ? 1 : 0;

      return __WEBPACK_IMPORTED_MODULE_8_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_10__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, props, {
        cellRenderer: this._cellRendererTopRightGrid,
        className: this.props.classNameTopRightGrid,
        columnCount: Math.max(0, columnCount - fixedColumnCount) + additionalColumnCount,
        columnWidth: this._columnWidthRightGrid,
        deferredMeasurementCache: this._deferredMeasurementCacheTopRightGrid,
        height: this._getTopGridHeight(props),
        onScroll: enableFixedRowScroll ? this._onScrollLeft : undefined,
        ref: this._topRightGridRef,
        rowCount: fixedRowCount,
        scrollLeft: scrollLeft,
        style: this._topRightGridStyle,
        tabIndex: null,
        width: this._getRightGridWidth(props)
      }));
    }
  }, {
    key: '_rowHeightBottomGrid',
    value: function _rowHeightBottomGrid(_ref8) {
      var index = _ref8.index;
      var _props10 = this.props,
          fixedRowCount = _props10.fixedRowCount,
          rowCount = _props10.rowCount,
          rowHeight = _props10.rowHeight;
      var _state4 = this.state,
          scrollbarSize = _state4.scrollbarSize,
          showVerticalScrollbar = _state4.showVerticalScrollbar;

      // An extra cell is added to the count
      // This gives the smaller Grid extra room for offset,
      // In case the main (bottom right) Grid has a scrollbar
      // If no scrollbar, the extra space is overflow:hidden anyway

      if (showVerticalScrollbar && index === rowCount - fixedRowCount) {
        return scrollbarSize;
      }

      return typeof rowHeight === 'function' ? rowHeight({ index: index + fixedRowCount }) : rowHeight;
    }
  }, {
    key: '_topLeftGridRef',
    value: function _topLeftGridRef(ref) {
      this._topLeftGrid = ref;
    }
  }, {
    key: '_topRightGridRef',
    value: function _topRightGridRef(ref) {
      this._topRightGrid = ref;
    }
  }]);

  return MultiGrid;
}(__WEBPACK_IMPORTED_MODULE_8_react__["PureComponent"]);

MultiGrid.defaultProps = {
  classNameBottomLeftGrid: '',
  classNameBottomRightGrid: '',
  classNameTopLeftGrid: '',
  classNameTopRightGrid: '',
  enableFixedColumnScroll: false,
  enableFixedRowScroll: false,
  fixedColumnCount: 0,
  fixedRowCount: 0,
  scrollToColumn: -1,
  scrollToRow: -1,
  style: {},
  styleBottomLeftGrid: {},
  styleBottomRightGrid: {},
  styleTopLeftGrid: {},
  styleTopRightGrid: {}
};
/* harmony default export */ __webpack_exports__["a"] = (MultiGrid);
MultiGrid.propTypes =  true ? {
  classNameBottomLeftGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.string.isRequired,
  classNameBottomRightGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.string.isRequired,
  classNameTopLeftGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.string.isRequired,
  classNameTopRightGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.string.isRequired,
  enableFixedColumnScroll: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.bool.isRequired,
  enableFixedRowScroll: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.bool.isRequired,
  fixedColumnCount: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.number.isRequired,
  fixedRowCount: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.number.isRequired,
  onScrollbarPresenceChange: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.func,
  style: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.object.isRequired,
  styleBottomLeftGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.object.isRequired,
  styleBottomRightGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.object.isRequired,
  styleTopLeftGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.object.isRequired,
  styleTopRightGrid: __WEBPACK_IMPORTED_MODULE_7_prop_types___default.a.object.isRequired
} : {};

/***/ }),
/* 257 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__CellMeasurer__ = __webpack_require__(101);




/**
 * Caches measurements for a given cell.
 */
var CellMeasurerCacheDecorator = function () {
  function CellMeasurerCacheDecorator() {
    var _this = this;

    var params = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

    __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_classCallCheck___default()(this, CellMeasurerCacheDecorator);

    this.columnWidth = function (_ref) {
      var index = _ref.index;

      _this._cellMeasurerCache.columnWidth({
        index: index + _this._columnIndexOffset
      });
    };

    this.rowHeight = function (_ref2) {
      var index = _ref2.index;

      _this._cellMeasurerCache.rowHeight({
        index: index + _this._rowIndexOffset
      });
    };

    var cellMeasurerCache = params.cellMeasurerCache,
        _params$columnIndexOf = params.columnIndexOffset,
        columnIndexOffset = _params$columnIndexOf === undefined ? 0 : _params$columnIndexOf,
        _params$rowIndexOffse = params.rowIndexOffset,
        rowIndexOffset = _params$rowIndexOffse === undefined ? 0 : _params$rowIndexOffse;


    this._cellMeasurerCache = cellMeasurerCache;
    this._columnIndexOffset = columnIndexOffset;
    this._rowIndexOffset = rowIndexOffset;
  }

  __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_createClass___default()(CellMeasurerCacheDecorator, [{
    key: 'clear',
    value: function clear(rowIndex, columnIndex) {
      this._cellMeasurerCache.clear(rowIndex + this._rowIndexOffset, columnIndex + this._columnIndexOffset);
    }
  }, {
    key: 'clearAll',
    value: function clearAll() {
      this._cellMeasurerCache.clearAll();
    }
  }, {
    key: 'hasFixedHeight',
    value: function hasFixedHeight() {
      return this._cellMeasurerCache.hasFixedHeight();
    }
  }, {
    key: 'hasFixedWidth',
    value: function hasFixedWidth() {
      return this._cellMeasurerCache.hasFixedWidth();
    }
  }, {
    key: 'getHeight',
    value: function getHeight(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      return this._cellMeasurerCache.getHeight(rowIndex + this._rowIndexOffset, columnIndex + this._columnIndexOffset);
    }
  }, {
    key: 'getWidth',
    value: function getWidth(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      return this._cellMeasurerCache.getWidth(rowIndex + this._rowIndexOffset, columnIndex + this._columnIndexOffset);
    }
  }, {
    key: 'has',
    value: function has(rowIndex) {
      var columnIndex = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

      return this._cellMeasurerCache.has(rowIndex + this._rowIndexOffset, columnIndex + this._columnIndexOffset);
    }
  }, {
    key: 'set',
    value: function set(rowIndex, columnIndex, width, height) {
      this._cellMeasurerCache.set(rowIndex + this._rowIndexOffset, columnIndex + this._columnIndexOffset, width, height);
    }
  }, {
    key: 'defaultHeight',
    get: function get() {
      return this._cellMeasurerCache.defaultHeight;
    }
  }, {
    key: 'defaultWidth',
    get: function get() {
      return this._cellMeasurerCache.defaultWidth;
    }
  }]);

  return CellMeasurerCacheDecorator;
}();

/* harmony default export */ __webpack_exports__["a"] = (CellMeasurerCacheDecorator);

/***/ }),
/* 258 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ScrollSync__ = __webpack_require__(259);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__ScrollSync__["a"]; });


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__ScrollSync__["a" /* default */]);


/***/ }),
/* 259 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_react__);








/**
 * HOC that simplifies the process of synchronizing scrolling between two or more virtualized components.
 */

var ScrollSync = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_inherits___default()(ScrollSync, _PureComponent);

  function ScrollSync(props, context) {
    __WEBPACK_IMPORTED_MODULE_1_babel_runtime_helpers_classCallCheck___default()(this, ScrollSync);

    var _this = __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_possibleConstructorReturn___default()(this, (ScrollSync.__proto__ || __WEBPACK_IMPORTED_MODULE_0_babel_runtime_core_js_object_get_prototype_of___default()(ScrollSync)).call(this, props, context));

    _this.state = {
      clientHeight: 0,
      clientWidth: 0,
      scrollHeight: 0,
      scrollLeft: 0,
      scrollTop: 0,
      scrollWidth: 0
    };

    _this._onScroll = _this._onScroll.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_createClass___default()(ScrollSync, [{
    key: 'render',
    value: function render() {
      var children = this.props.children;
      var _state = this.state,
          clientHeight = _state.clientHeight,
          clientWidth = _state.clientWidth,
          scrollHeight = _state.scrollHeight,
          scrollLeft = _state.scrollLeft,
          scrollTop = _state.scrollTop,
          scrollWidth = _state.scrollWidth;


      return children({
        clientHeight: clientHeight,
        clientWidth: clientWidth,
        onScroll: this._onScroll,
        scrollHeight: scrollHeight,
        scrollLeft: scrollLeft,
        scrollTop: scrollTop,
        scrollWidth: scrollWidth
      });
    }
  }, {
    key: '_onScroll',
    value: function _onScroll(_ref) {
      var clientHeight = _ref.clientHeight,
          clientWidth = _ref.clientWidth,
          scrollHeight = _ref.scrollHeight,
          scrollLeft = _ref.scrollLeft,
          scrollTop = _ref.scrollTop,
          scrollWidth = _ref.scrollWidth;

      this.setState({
        clientHeight: clientHeight,
        clientWidth: clientWidth,
        scrollHeight: scrollHeight,
        scrollLeft: scrollLeft,
        scrollTop: scrollTop,
        scrollWidth: scrollWidth
      });
    }
  }]);

  return ScrollSync;
}(__WEBPACK_IMPORTED_MODULE_6_react__["PureComponent"]);

/* harmony default export */ __webpack_exports__["a"] = (ScrollSync);
ScrollSync.propTypes =  true ? {
  /**
   * Function responsible for rendering 2 or more virtualized components.
   * This function should implement the following signature:
   * ({ onScroll, scrollLeft, scrollTop }) => PropTypes.element
   */
  children: __WEBPACK_IMPORTED_MODULE_5_prop_types___default.a.func.isRequired
} : {};

/***/ }),
/* 260 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__createMultiSort__ = __webpack_require__(261);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__defaultCellDataGetter__ = __webpack_require__(105);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__defaultCellRenderer__ = __webpack_require__(106);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__defaultHeaderRowRenderer_js__ = __webpack_require__(107);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__defaultHeaderRenderer__ = __webpack_require__(108);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__defaultRowRenderer__ = __webpack_require__(110);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6__Column__ = __webpack_require__(111);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__SortDirection__ = __webpack_require__(38);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__SortIndicator__ = __webpack_require__(109);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__Table__ = __webpack_require__(262);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return __WEBPACK_IMPORTED_MODULE_0__createMultiSort__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return __WEBPACK_IMPORTED_MODULE_1__defaultCellDataGetter__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "g", function() { return __WEBPACK_IMPORTED_MODULE_2__defaultCellRenderer__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "i", function() { return __WEBPACK_IMPORTED_MODULE_3__defaultHeaderRowRenderer_js__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "h", function() { return __WEBPACK_IMPORTED_MODULE_4__defaultHeaderRenderer__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "j", function() { return __WEBPACK_IMPORTED_MODULE_5__defaultRowRenderer__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_6__Column__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return __WEBPACK_IMPORTED_MODULE_7__SortDirection__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return __WEBPACK_IMPORTED_MODULE_8__SortIndicator__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return __WEBPACK_IMPORTED_MODULE_9__Table__["a"]; });











/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_9__Table__["a" /* default */]);


/***/ }),
/* 261 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createMultiSort;


function createMultiSort(sortCallback) {
  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
      defaultSortBy = _ref.defaultSortBy,
      _ref$defaultSortDirec = _ref.defaultSortDirection,
      defaultSortDirection = _ref$defaultSortDirec === undefined ? {} : _ref$defaultSortDirec;

  if (!sortCallback) {
    throw Error('Required parameter "sortCallback" not specified');
  }

  var sortBy = defaultSortBy || [];
  var sortDirection = {};

  sortBy.forEach(function (dataKey) {
    sortDirection[dataKey] = defaultSortDirection.hasOwnProperty(dataKey) ? defaultSortDirection[dataKey] : 'ASC';
  });

  function sort(_ref2) {
    var defaultSortDirection = _ref2.defaultSortDirection,
        event = _ref2.event,
        dataKey = _ref2.sortBy;

    if (event.shiftKey) {
      // Shift + click appends a column to existing criteria
      if (sortDirection.hasOwnProperty(dataKey)) {
        sortDirection[dataKey] = sortDirection[dataKey] === 'ASC' ? 'DESC' : 'ASC';
      } else {
        sortDirection[dataKey] = defaultSortDirection;
        sortBy.push(dataKey);
      }
    } else if (event.ctrlKey || event.metaKey) {
      // Control + click removes column from sort (if pressent)
      var index = sortBy.indexOf(dataKey);
      if (index >= 0) {
        sortBy.splice(index, 1);
        delete sortDirection[dataKey];
      }
    } else {
      sortBy.length = 0;
      sortBy.push(dataKey);

      if (sortDirection.hasOwnProperty(dataKey)) {
        sortDirection[dataKey] = sortDirection[dataKey] === 'ASC' ? 'DESC' : 'ASC';
      } else {
        sortDirection[dataKey] = defaultSortDirection;
      }
    }

    // Notify application code
    sortCallback({
      sortBy: sortBy,
      sortDirection: sortDirection
    });
  }

  return {
    sort: sort,
    sortBy: sortBy,
    sortDirection: sortDirection
  };
}

/***/ }),
/* 262 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__ = __webpack_require__(11);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__ = __webpack_require__(5);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__ = __webpack_require__(3);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__ = __webpack_require__(4);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__ = __webpack_require__(6);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_classnames__ = __webpack_require__(23);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6_classnames___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_6_classnames__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__Column__ = __webpack_require__(111);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_prop_types__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_8_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9_react__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9_react___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_9_react__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10_react_dom__ = __webpack_require__(25);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10_react_dom___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_10_react_dom__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_11__Grid__ = __webpack_require__(9);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_12__defaultRowRenderer__ = __webpack_require__(110);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_13__defaultHeaderRowRenderer__ = __webpack_require__(107);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_14__SortDirection__ = __webpack_require__(38);







var babelPluginFlowReactPropTypes_proptype_CellPosition = __webpack_require__(9).babelPluginFlowReactPropTypes_proptype_CellPosition || __webpack_require__(0).any;












/**
 * Table component with fixed headers and virtualized rows for improved performance with large data sets.
 * This component expects explicit width, height, and padding parameters.
 */

var Table = function (_PureComponent) {
  __WEBPACK_IMPORTED_MODULE_5_babel_runtime_helpers_inherits___default()(Table, _PureComponent);

  function Table(props) {
    __WEBPACK_IMPORTED_MODULE_2_babel_runtime_helpers_classCallCheck___default()(this, Table);

    var _this = __WEBPACK_IMPORTED_MODULE_4_babel_runtime_helpers_possibleConstructorReturn___default()(this, (Table.__proto__ || __WEBPACK_IMPORTED_MODULE_1_babel_runtime_core_js_object_get_prototype_of___default()(Table)).call(this, props));

    _this.state = {
      scrollbarWidth: 0
    };

    _this._createColumn = _this._createColumn.bind(_this);
    _this._createRow = _this._createRow.bind(_this);
    _this._onScroll = _this._onScroll.bind(_this);
    _this._onSectionRendered = _this._onSectionRendered.bind(_this);
    _this._setRef = _this._setRef.bind(_this);
    return _this;
  }

  __WEBPACK_IMPORTED_MODULE_3_babel_runtime_helpers_createClass___default()(Table, [{
    key: 'forceUpdateGrid',
    value: function forceUpdateGrid() {
      if (this.Grid) {
        this.Grid.forceUpdate();
      }
    }

    /** See Grid#getOffsetForCell */

  }, {
    key: 'getOffsetForRow',
    value: function getOffsetForRow(_ref) {
      var alignment = _ref.alignment,
          index = _ref.index;

      if (this.Grid) {
        var _Grid$getOffsetForCel = this.Grid.getOffsetForCell({
          alignment: alignment,
          rowIndex: index
        }),
            scrollTop = _Grid$getOffsetForCel.scrollTop;

        return scrollTop;
      }
      return 0;
    }

    /** CellMeasurer compatibility */

  }, {
    key: 'invalidateCellSizeAfterRender',
    value: function invalidateCellSizeAfterRender(_ref2) {
      var columnIndex = _ref2.columnIndex,
          rowIndex = _ref2.rowIndex;

      if (this.Grid) {
        this.Grid.invalidateCellSizeAfterRender({
          rowIndex: rowIndex,
          columnIndex: columnIndex
        });
      }
    }

    /** See Grid#measureAllCells */

  }, {
    key: 'measureAllRows',
    value: function measureAllRows() {
      if (this.Grid) {
        this.Grid.measureAllCells();
      }
    }

    /** CellMeasurer compatibility */

  }, {
    key: 'recomputeGridSize',
    value: function recomputeGridSize() {
      var _ref3 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
          _ref3$columnIndex = _ref3.columnIndex,
          columnIndex = _ref3$columnIndex === undefined ? 0 : _ref3$columnIndex,
          _ref3$rowIndex = _ref3.rowIndex,
          rowIndex = _ref3$rowIndex === undefined ? 0 : _ref3$rowIndex;

      if (this.Grid) {
        this.Grid.recomputeGridSize({
          rowIndex: rowIndex,
          columnIndex: columnIndex
        });
      }
    }

    /** See Grid#recomputeGridSize */

  }, {
    key: 'recomputeRowHeights',
    value: function recomputeRowHeights() {
      var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.recomputeGridSize({
          rowIndex: index
        });
      }
    }

    /** See Grid#scrollToPosition */

  }, {
    key: 'scrollToPosition',
    value: function scrollToPosition() {
      var scrollTop = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.scrollToPosition({ scrollTop: scrollTop });
      }
    }

    /** See Grid#scrollToCell */

  }, {
    key: 'scrollToRow',
    value: function scrollToRow() {
      var index = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;

      if (this.Grid) {
        this.Grid.scrollToCell({
          columnIndex: 0,
          rowIndex: index
        });
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      this._setScrollbarWidth();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      this._setScrollbarWidth();
    }
  }, {
    key: 'render',
    value: function render() {
      var _this2 = this;

      var _props = this.props,
          children = _props.children,
          className = _props.className,
          disableHeader = _props.disableHeader,
          gridClassName = _props.gridClassName,
          gridStyle = _props.gridStyle,
          headerHeight = _props.headerHeight,
          headerRowRenderer = _props.headerRowRenderer,
          height = _props.height,
          id = _props.id,
          noRowsRenderer = _props.noRowsRenderer,
          rowClassName = _props.rowClassName,
          rowStyle = _props.rowStyle,
          scrollToIndex = _props.scrollToIndex,
          style = _props.style,
          width = _props.width;
      var scrollbarWidth = this.state.scrollbarWidth;


      var availableRowsHeight = disableHeader ? height : height - headerHeight;

      var rowClass = typeof rowClassName === 'function' ? rowClassName({ index: -1 }) : rowClassName;
      var rowStyleObject = typeof rowStyle === 'function' ? rowStyle({ index: -1 }) : rowStyle;

      // Precompute and cache column styles before rendering rows and columns to speed things up
      this._cachedColumnStyles = [];
      __WEBPACK_IMPORTED_MODULE_9_react___default.a.Children.toArray(children).forEach(function (column, index) {
        var flexStyles = _this2._getFlexStyleForColumn(column, column.props.style);

        _this2._cachedColumnStyles[index] = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, flexStyles, {
          overflow: 'hidden'
        });
      });

      // Note that we specify :rowCount, :scrollbarWidth, :sortBy, and :sortDirection as properties on Grid even though these have nothing to do with Grid.
      // This is done because Grid is a pure component and won't update unless its properties or state has changed.
      // Any property that should trigger a re-render of Grid then is specified here to avoid a stale display.
      return __WEBPACK_IMPORTED_MODULE_9_react___default.a.createElement(
        'div',
        {
          className: __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table', className),
          id: id,
          role: 'grid',
          style: style },
        !disableHeader && headerRowRenderer({
          className: __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table__headerRow', rowClass),
          columns: this._getHeaderColumns(),
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, rowStyleObject, {
            height: headerHeight,
            overflow: 'hidden',
            paddingRight: scrollbarWidth,
            width: width
          })
        }),
        __WEBPACK_IMPORTED_MODULE_9_react___default.a.createElement(__WEBPACK_IMPORTED_MODULE_11__Grid__["default"], __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, this.props, {
          autoContainerWidth: true,
          className: __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table__Grid', gridClassName),
          cellRenderer: this._createRow,
          columnWidth: width,
          columnCount: 1,
          height: availableRowsHeight,
          id: undefined,
          noContentRenderer: noRowsRenderer,
          onScroll: this._onScroll,
          onSectionRendered: this._onSectionRendered,
          ref: this._setRef,
          role: 'rowgroup',
          scrollbarWidth: scrollbarWidth,
          scrollToRow: scrollToIndex,
          style: __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, gridStyle, {
            overflowX: 'hidden'
          })
        }))
      );
    }
  }, {
    key: '_createColumn',
    value: function _createColumn(_ref4) {
      var column = _ref4.column,
          columnIndex = _ref4.columnIndex,
          isScrolling = _ref4.isScrolling,
          parent = _ref4.parent,
          rowData = _ref4.rowData,
          rowIndex = _ref4.rowIndex;
      var _column$props = column.props,
          cellDataGetter = _column$props.cellDataGetter,
          cellRenderer = _column$props.cellRenderer,
          className = _column$props.className,
          columnData = _column$props.columnData,
          dataKey = _column$props.dataKey,
          id = _column$props.id;


      var cellData = cellDataGetter({ columnData: columnData, dataKey: dataKey, rowData: rowData });
      var renderedCell = cellRenderer({
        cellData: cellData,
        columnData: columnData,
        columnIndex: columnIndex,
        dataKey: dataKey,
        isScrolling: isScrolling,
        parent: parent,
        rowData: rowData,
        rowIndex: rowIndex
      });

      var style = this._cachedColumnStyles[columnIndex];

      var title = typeof renderedCell === 'string' ? renderedCell : null;

      // Avoid using object-spread syntax with multiple objects here,
      // Since it results in an extra method call to 'babel-runtime/helpers/extends'
      // See PR https://github.com/bvaughn/react-virtualized/pull/942
      return __WEBPACK_IMPORTED_MODULE_9_react___default.a.createElement(
        'div',
        {
          'aria-describedby': id,
          className: __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table__rowColumn', className),
          key: 'Row' + rowIndex + '-' + 'Col' + columnIndex,
          role: 'gridcell',
          style: style,
          title: title },
        renderedCell
      );
    }
  }, {
    key: '_createHeader',
    value: function _createHeader(_ref5) {
      var column = _ref5.column,
          index = _ref5.index;
      var _props2 = this.props,
          headerClassName = _props2.headerClassName,
          headerStyle = _props2.headerStyle,
          onHeaderClick = _props2.onHeaderClick,
          sort = _props2.sort,
          sortBy = _props2.sortBy,
          sortDirection = _props2.sortDirection;
      var _column$props2 = column.props,
          columnData = _column$props2.columnData,
          dataKey = _column$props2.dataKey,
          defaultSortDirection = _column$props2.defaultSortDirection,
          disableSort = _column$props2.disableSort,
          headerRenderer = _column$props2.headerRenderer,
          id = _column$props2.id,
          label = _column$props2.label;

      var sortEnabled = !disableSort && sort;

      var classNames = __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table__headerColumn', headerClassName, column.props.headerClassName, {
        ReactVirtualized__Table__sortableHeaderColumn: sortEnabled
      });
      var style = this._getFlexStyleForColumn(column, __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, headerStyle, column.props.headerStyle));

      var renderedHeader = headerRenderer({
        columnData: columnData,
        dataKey: dataKey,
        disableSort: disableSort,
        label: label,
        sortBy: sortBy,
        sortDirection: sortDirection
      });

      var headerOnClick = void 0,
          headerOnKeyDown = void 0,
          headerTabIndex = void 0,
          headerAriaSort = void 0,
          headerAriaLabel = void 0;

      if (sortEnabled || onHeaderClick) {
        // If this is a sortable header, clicking it should update the table data's sorting.
        var isFirstTimeSort = sortBy !== dataKey;

        // If this is the firstTime sort of this column, use the column default sort order.
        // Otherwise, invert the direction of the sort.
        var newSortDirection = isFirstTimeSort ? defaultSortDirection : sortDirection === __WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].DESC ? __WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].ASC : __WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].DESC;

        var onClick = function onClick(event) {
          sortEnabled && sort({
            defaultSortDirection: defaultSortDirection,
            event: event,
            sortBy: dataKey,
            sortDirection: newSortDirection
          });
          onHeaderClick && onHeaderClick({ columnData: columnData, dataKey: dataKey, event: event });
        };

        var onKeyDown = function onKeyDown(event) {
          if (event.key === 'Enter' || event.key === ' ') {
            onClick(event);
          }
        };

        headerAriaLabel = column.props['aria-label'] || label || dataKey;
        headerTabIndex = 0;
        headerOnClick = onClick;
        headerOnKeyDown = onKeyDown;
      }

      if (sortBy === dataKey) {
        headerAriaSort = sortDirection === __WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].ASC ? 'ascending' : 'descending';
      }

      // Avoid using object-spread syntax with multiple objects here,
      // Since it results in an extra method call to 'babel-runtime/helpers/extends'
      // See PR https://github.com/bvaughn/react-virtualized/pull/942
      return __WEBPACK_IMPORTED_MODULE_9_react___default.a.createElement(
        'div',
        {
          'aria-label': headerAriaLabel,
          'aria-sort': headerAriaSort,
          className: classNames,
          id: id,
          key: 'Header-Col' + index,
          onClick: headerOnClick,
          onKeyDown: headerOnKeyDown,
          role: 'columnheader',
          style: style,
          tabIndex: headerTabIndex },
        renderedHeader
      );
    }
  }, {
    key: '_createRow',
    value: function _createRow(_ref6) {
      var _this3 = this;

      var index = _ref6.rowIndex,
          isScrolling = _ref6.isScrolling,
          key = _ref6.key,
          parent = _ref6.parent,
          style = _ref6.style;
      var _props3 = this.props,
          children = _props3.children,
          onRowClick = _props3.onRowClick,
          onRowDoubleClick = _props3.onRowDoubleClick,
          onRowRightClick = _props3.onRowRightClick,
          onRowMouseOver = _props3.onRowMouseOver,
          onRowMouseOut = _props3.onRowMouseOut,
          rowClassName = _props3.rowClassName,
          rowGetter = _props3.rowGetter,
          rowRenderer = _props3.rowRenderer,
          rowStyle = _props3.rowStyle;
      var scrollbarWidth = this.state.scrollbarWidth;


      var rowClass = typeof rowClassName === 'function' ? rowClassName({ index: index }) : rowClassName;
      var rowStyleObject = typeof rowStyle === 'function' ? rowStyle({ index: index }) : rowStyle;
      var rowData = rowGetter({ index: index });

      var columns = __WEBPACK_IMPORTED_MODULE_9_react___default.a.Children.toArray(children).map(function (column, columnIndex) {
        return _this3._createColumn({
          column: column,
          columnIndex: columnIndex,
          isScrolling: isScrolling,
          parent: parent,
          rowData: rowData,
          rowIndex: index,
          scrollbarWidth: scrollbarWidth
        });
      });

      var className = __WEBPACK_IMPORTED_MODULE_6_classnames___default()('ReactVirtualized__Table__row', rowClass);
      var flattenedStyle = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, style, rowStyleObject, {
        height: this._getRowHeight(index),
        overflow: 'hidden',
        paddingRight: scrollbarWidth
      });

      return rowRenderer({
        className: className,
        columns: columns,
        index: index,
        isScrolling: isScrolling,
        key: key,
        onRowClick: onRowClick,
        onRowDoubleClick: onRowDoubleClick,
        onRowRightClick: onRowRightClick,
        onRowMouseOver: onRowMouseOver,
        onRowMouseOut: onRowMouseOut,
        rowData: rowData,
        style: flattenedStyle
      });
    }

    /**
     * Determines the flex-shrink, flex-grow, and width values for a cell (header or column).
     */

  }, {
    key: '_getFlexStyleForColumn',
    value: function _getFlexStyleForColumn(column) {
      var customStyle = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

      var flexValue = column.props.flexGrow + ' ' + column.props.flexShrink + ' ' + column.props.width + 'px';

      var style = __WEBPACK_IMPORTED_MODULE_0_babel_runtime_helpers_extends___default()({}, customStyle, {
        flex: flexValue,
        msFlex: flexValue,
        WebkitFlex: flexValue
      });

      if (column.props.maxWidth) {
        style.maxWidth = column.props.maxWidth;
      }

      if (column.props.minWidth) {
        style.minWidth = column.props.minWidth;
      }

      return style;
    }
  }, {
    key: '_getHeaderColumns',
    value: function _getHeaderColumns() {
      var _this4 = this;

      var _props4 = this.props,
          children = _props4.children,
          disableHeader = _props4.disableHeader;

      var items = disableHeader ? [] : __WEBPACK_IMPORTED_MODULE_9_react___default.a.Children.toArray(children);

      return items.map(function (column, index) {
        return _this4._createHeader({ column: column, index: index });
      });
    }
  }, {
    key: '_getRowHeight',
    value: function _getRowHeight(rowIndex) {
      var rowHeight = this.props.rowHeight;


      return typeof rowHeight === 'function' ? rowHeight({ index: rowIndex }) : rowHeight;
    }
  }, {
    key: '_onScroll',
    value: function _onScroll(_ref7) {
      var clientHeight = _ref7.clientHeight,
          scrollHeight = _ref7.scrollHeight,
          scrollTop = _ref7.scrollTop;
      var onScroll = this.props.onScroll;


      onScroll({ clientHeight: clientHeight, scrollHeight: scrollHeight, scrollTop: scrollTop });
    }
  }, {
    key: '_onSectionRendered',
    value: function _onSectionRendered(_ref8) {
      var rowOverscanStartIndex = _ref8.rowOverscanStartIndex,
          rowOverscanStopIndex = _ref8.rowOverscanStopIndex,
          rowStartIndex = _ref8.rowStartIndex,
          rowStopIndex = _ref8.rowStopIndex;
      var onRowsRendered = this.props.onRowsRendered;


      onRowsRendered({
        overscanStartIndex: rowOverscanStartIndex,
        overscanStopIndex: rowOverscanStopIndex,
        startIndex: rowStartIndex,
        stopIndex: rowStopIndex
      });
    }
  }, {
    key: '_setRef',
    value: function _setRef(ref) {
      this.Grid = ref;
    }
  }, {
    key: '_setScrollbarWidth',
    value: function _setScrollbarWidth() {
      if (this.Grid) {
        var _Grid = Object(__WEBPACK_IMPORTED_MODULE_10_react_dom__["findDOMNode"])(this.Grid);
        var clientWidth = _Grid.clientWidth || 0;
        var offsetWidth = _Grid.offsetWidth || 0;
        var scrollbarWidth = offsetWidth - clientWidth;

        this.setState({ scrollbarWidth: scrollbarWidth });
      }
    }
  }]);

  return Table;
}(__WEBPACK_IMPORTED_MODULE_9_react__["PureComponent"]);

Table.defaultProps = {
  disableHeader: false,
  estimatedRowSize: 30,
  headerHeight: 0,
  headerStyle: {},
  noRowsRenderer: function noRowsRenderer() {
    return null;
  },
  onRowsRendered: function onRowsRendered() {
    return null;
  },
  onScroll: function onScroll() {
    return null;
  },
  overscanIndicesGetter: __WEBPACK_IMPORTED_MODULE_11__Grid__["accessibilityOverscanIndicesGetter"],
  overscanRowCount: 10,
  rowRenderer: __WEBPACK_IMPORTED_MODULE_12__defaultRowRenderer__["a" /* default */],
  headerRowRenderer: __WEBPACK_IMPORTED_MODULE_13__defaultHeaderRowRenderer__["a" /* default */],
  rowStyle: {},
  scrollToAlignment: 'auto',
  scrollToIndex: -1,
  style: {}
};
/* harmony default export */ __webpack_exports__["a"] = (Table);
Table.propTypes =  true ? {
  'aria-label': __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /**
   * Removes fixed height from the scrollingContainer so that the total height
   * of rows can stretch the window. Intended for use with WindowScroller
   */
  autoHeight: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.bool,

  /** One or more Columns describing the data displayed in this row */
  children: function children(props) {
    var children = __WEBPACK_IMPORTED_MODULE_9_react___default.a.Children.toArray(props.children);
    for (var i = 0; i < children.length; i++) {
      var childType = children[i].type;
      if (childType !== __WEBPACK_IMPORTED_MODULE_7__Column__["a" /* default */] && !(childType.prototype instanceof __WEBPACK_IMPORTED_MODULE_7__Column__["a" /* default */])) {
        return new Error('Table only accepts children of type Column');
      }
    }
  },

  /** Optional CSS class name */
  className: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /** Disable rendering the header at all */
  disableHeader: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.bool,

  /**
   * Used to estimate the total height of a Table before all of its rows have actually been measured.
   * The estimated total height is adjusted as rows are rendered.
   */
  estimatedRowSize: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /** Optional custom CSS class name to attach to inner Grid element. */
  gridClassName: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /** Optional inline style to attach to inner Grid element. */
  gridStyle: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.object,

  /** Optional CSS class to apply to all column headers */
  headerClassName: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /** Fixed height of header row */
  headerHeight: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /**
   * Responsible for rendering a table row given an array of columns:
   * Should implement the following interface: ({
   *   className: string,
   *   columns: any[],
   *   style: any
   * }): PropTypes.node
   */
  headerRowRenderer: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /** Optional custom inline style to attach to table header columns. */
  headerStyle: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.object,

  /** Fixed/available height for out DOM element */
  height: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /** Optional id */
  id: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /** Optional renderer to be used in place of table body rows when rowCount is 0 */
  noRowsRenderer: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Optional callback when a column's header is clicked.
   * ({ columnData: any, dataKey: string }): void
   */
  onHeaderClick: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked when a user clicks on a table row.
   * ({ index: number }): void
   */
  onRowClick: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked when a user double-clicks on a table row.
   * ({ index: number }): void
   */
  onRowDoubleClick: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked when the mouse leaves a table row.
   * ({ index: number }): void
   */
  onRowMouseOut: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked when a user moves the mouse over a table row.
   * ({ index: number }): void
   */
  onRowMouseOver: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked when a user right-clicks on a table row.
   * ({ index: number }): void
   */
  onRowRightClick: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked with information about the slice of rows that were just rendered.
   * ({ startIndex, stopIndex }): void
   */
  onRowsRendered: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /**
   * Callback invoked whenever the scroll offset changes within the inner scrollable region.
   * This callback can be used to sync scrolling between lists, tables, or grids.
   * ({ clientHeight, scrollHeight, scrollTop }): void
   */
  onScroll: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func.isRequired,

  /** See Grid#overscanIndicesGetter */
  overscanIndicesGetter: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func.isRequired,

  /**
   * Number of rows to render above/below the visible bounds of the list.
   * These rows can help for smoother scrolling on touch devices.
   */
  overscanRowCount: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /**
   * Optional CSS class to apply to all table rows (including the header row).
   * This property can be a CSS class name (string) or a function that returns a class name.
   * If a function is provided its signature should be: ({ index: number }): string
   */
  rowClassName: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string, __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func]),

  /**
   * Callback responsible for returning a data row given an index.
   * ({ index: number }): any
   */
  rowGetter: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func.isRequired,

  /**
   * Either a fixed row height (number) or a function that returns the height of a row given its index.
   * ({ index: number }): number
   */
  rowHeight: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number, __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func]).isRequired,

  /** Number of rows in table. */
  rowCount: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /**
   * Responsible for rendering a table row given an array of columns:
   * Should implement the following interface: ({
   *   className: string,
   *   columns: Array,
   *   index: number,
   *   isScrolling: boolean,
   *   onRowClick: ?Function,
   *   onRowDoubleClick: ?Function,
   *   onRowMouseOver: ?Function,
   *   onRowMouseOut: ?Function,
   *   rowData: any,
   *   style: any
   * }): PropTypes.node
   */
  rowRenderer: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /** Optional custom inline style to attach to table rows. */
  rowStyle: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.object, __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func]).isRequired,

  /** See Grid#scrollToAlignment */
  scrollToAlignment: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.oneOf(['auto', 'end', 'start', 'center']).isRequired,

  /** Row index to ensure visible (by forcefully scrolling if necessary) */
  scrollToIndex: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired,

  /** Vertical offset. */
  scrollTop: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number,

  /**
   * Sort function to be called if a sortable header is clicked.
   * Should implement the following interface: ({
   *   defaultSortDirection: 'ASC' | 'DESC',
   *   event: MouseEvent,
   *   sortBy: string,
   *   sortDirection: SortDirection
   * }): void
   */
  sort: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.func,

  /** Table data is currently sorted by this :dataKey (if it is sorted at all) */
  sortBy: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.string,

  /** Table data is currently sorted in this direction (if it is sorted at all) */
  sortDirection: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.oneOf([__WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].ASC, __WEBPACK_IMPORTED_MODULE_14__SortDirection__["a" /* default */].DESC]),

  /** Optional inline style */
  style: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.object,

  /** Tab index for focus */
  tabIndex: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number,

  /** Width of list */
  width: __WEBPACK_IMPORTED_MODULE_8_prop_types___default.a.number.isRequired
} : {};

/***/ }),
/* 263 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__WindowScroller__ = __webpack_require__(112);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__WindowScroller__["a"]; });
/* unused harmony reexport IS_SCROLLING_TIMEOUT */


/* unused harmony default export */ var _unused_webpack_default_export = (__WEBPACK_IMPORTED_MODULE_0__WindowScroller__["a" /* default */]);


/***/ }),
/* 264 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = registerScrollListener;
/* harmony export (immutable) */ __webpack_exports__["b"] = unregisterScrollListener;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_requestAnimationTimeout__ = __webpack_require__(28);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__WindowScroller_js__ = __webpack_require__(112);



var mountedInstances = [];
var originalBodyPointerEvents = null;
var disablePointerEventsTimeoutId = null;

function enablePointerEventsIfDisabled() {
  if (disablePointerEventsTimeoutId) {
    disablePointerEventsTimeoutId = null;

    if (document.body && originalBodyPointerEvents != null) {
      document.body.style.pointerEvents = originalBodyPointerEvents;
    }

    originalBodyPointerEvents = null;
  }
}

function enablePointerEventsAfterDelayCallback() {
  enablePointerEventsIfDisabled();
  mountedInstances.forEach(function (instance) {
    return instance.__resetIsScrolling();
  });
}

function enablePointerEventsAfterDelay() {
  if (disablePointerEventsTimeoutId) {
    Object(__WEBPACK_IMPORTED_MODULE_0__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(disablePointerEventsTimeoutId);
  }

  var maximumTimeout = 0;
  mountedInstances.forEach(function (instance) {
    maximumTimeout = Math.max(maximumTimeout, instance.props.scrollingResetTimeInterval);
  });

  disablePointerEventsTimeoutId = Object(__WEBPACK_IMPORTED_MODULE_0__utils_requestAnimationTimeout__["requestAnimationTimeout"])(enablePointerEventsAfterDelayCallback, maximumTimeout);
}

function onScrollWindow(event) {
  if (event.currentTarget === window && originalBodyPointerEvents == null && document.body) {
    originalBodyPointerEvents = document.body.style.pointerEvents;

    document.body.style.pointerEvents = 'none';
  }
  enablePointerEventsAfterDelay();
  mountedInstances.forEach(function (instance) {
    if (instance.props.scrollElement === event.currentTarget) {
      instance.__handleWindowScrollEvent();
    }
  });
}

function registerScrollListener(component, element) {
  if (!mountedInstances.some(function (instance) {
    return instance.props.scrollElement === element;
  })) {
    element.addEventListener('scroll', onScrollWindow);
  }
  mountedInstances.push(component);
}

function unregisterScrollListener(component, element) {
  mountedInstances = mountedInstances.filter(function (instance) {
    return instance !== component;
  });
  if (!mountedInstances.length) {
    element.removeEventListener('scroll', onScrollWindow);
    if (disablePointerEventsTimeoutId) {
      Object(__WEBPACK_IMPORTED_MODULE_0__utils_requestAnimationTimeout__["cancelAnimationTimeout"])(disablePointerEventsTimeoutId);
      enablePointerEventsIfDisabled();
    }
  }
}

/***/ }),
/* 265 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = getDimensions;
/* harmony export (immutable) */ __webpack_exports__["b"] = getPositionOffset;
/* harmony export (immutable) */ __webpack_exports__["c"] = getScrollOffset;


/**
 * Gets the dimensions of the element, accounting for API differences between
 * `window` and other DOM elements.
 */

var isWindow = function isWindow(element) {
  return element === window;
};

// TODO Move this into WindowScroller and import from there


var getBoundingBox = function getBoundingBox(element) {
  return element.getBoundingClientRect();
};

function getDimensions(scrollElement, props) {
  if (!scrollElement) {
    return {
      height: props.serverHeight,
      width: props.serverWidth
    };
  } else if (isWindow(scrollElement)) {
    var _window = window,
        innerHeight = _window.innerHeight,
        innerWidth = _window.innerWidth;

    return {
      height: typeof innerHeight === 'number' ? innerHeight : 0,
      width: typeof innerWidth === 'number' ? innerWidth : 0
    };
  } else {
    return getBoundingBox(scrollElement);
  }
}

/**
 * Gets the vertical and horizontal position of an element within its scroll container.
 * Elements that have been “scrolled past” return negative values.
 * Handles edge-case where a user is navigating back (history) from an already-scrolled page.
 * In this case the body’s top or left position will be a negative number and this element’s top or left will be increased (by that amount).
 */
function getPositionOffset(element, container) {
  if (isWindow(container) && document.documentElement) {
    var containerElement = document.documentElement;
    var elementRect = getBoundingBox(element);
    var containerRect = getBoundingBox(containerElement);
    return {
      top: elementRect.top - containerRect.top,
      left: elementRect.left - containerRect.left
    };
  } else {
    var scrollOffset = getScrollOffset(container);
    var _elementRect = getBoundingBox(element);
    var _containerRect = getBoundingBox(container);
    return {
      top: _elementRect.top + scrollOffset.top - _containerRect.top,
      left: _elementRect.left + scrollOffset.left - _containerRect.left
    };
  }
}

/**
 * Gets the vertical and horizontal scroll amount of the element, accounting for IE compatibility
 * and API differences between `window` and other DOM elements.
 */
function getScrollOffset(element) {
  if (isWindow(element) && document.documentElement) {
    return {
      top: 'scrollY' in window ? window.scrollY : document.documentElement.scrollTop,
      left: 'scrollX' in window ? window.scrollX : document.documentElement.scrollLeft
    };
  } else {
    return {
      top: element.scrollTop,
      left: element.scrollLeft
    };
  }
}

/***/ }),
/* 266 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.items = exports.getPreviousItemByKey = exports.getNextItemByKey = exports.getItemByIndex = exports.getItemByKey = undefined;

var _scan21ItemsDa = __webpack_require__(287);

var getItemByKey = exports.getItemByKey = function getItemByKey(key) {
  return _scan21ItemsDa.items.find(function (item) {
    return item.key === key;
  });
};
var getItemByIndex = exports.getItemByIndex = function getItemByIndex(index) {
  return _scan21ItemsDa.items[index];
};
var getNextItemByKey = exports.getNextItemByKey = function getNextItemByKey(key) {
  var index = _scan21ItemsDa.items.indexOf(getItemByKey(key)) + 1;
  if (!_scan21ItemsDa.items[index]) index = 0;
  return _scan21ItemsDa.items[index];
};
var getPreviousItemByKey = exports.getPreviousItemByKey = function getPreviousItemByKey(key) {
  var index = _scan21ItemsDa.items.indexOf(getItemByKey(key)) - 1;
  if (!_scan21ItemsDa.items[index]) index = _scan21ItemsDa.items.length - 1;
  return _scan21ItemsDa.items[index];
};

exports.items = _scan21ItemsDa.items;

/***/ }),
/* 267 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {
  module.exports = require('./configureStore.prod');
} else {
  module.exports = __webpack_require__(268);
}

/***/ }),
/* 268 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _redux = __webpack_require__(43);

var _middlewares = __webpack_require__(269);

var _reducers = __webpack_require__(270);

var _reducers2 = _interopRequireDefault(_reducers);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var composeEnhancers = (typeof window === 'undefined' ? 'undefined' : _typeof(window)) === 'object' && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__({
  // Specify extension’s options like name, actionsBlacklist, actionsCreators, serialize...
}) : _redux.compose;

exports.default = function (persistedState) {
  var enhancer = composeEnhancers((0, _redux.applyMiddleware)(_middlewares.loggerMiddleware));

  return (0, _redux.createStore)(_reducers2.default, persistedState, enhancer);
};

/***/ }),
/* 269 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var loggerMiddleware = exports.loggerMiddleware = function loggerMiddleware(store) {
  return function (next) {
    return function (action) {
      console.log('Dispatching:', action);
      next(action);
    };
  };
};

/***/ }),
/* 270 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _redux = __webpack_require__(43);

var _interview = __webpack_require__(271);

var _interview2 = _interopRequireDefault(_interview);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = (0, _redux.combineReducers)({
  interview: _interview2.default
});

/***/ }),
/* 271 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _data = __webpack_require__(46);

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var initialState = {
  id: window.scanInfo.record_id,
  activeKey: '1.001',
  responses: {}
};

if (_data.scanData.data) {
  initialState = _data.scanData.data;
}

var interview = function interview() {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initialState;
  var action = arguments[1];

  switch (action.type) {
    case 'SET_ACTIVE_ITEM':
      return _extends({}, state, {
        activeKey: action.payload.key
      });
    case 'SET_RESPONSE':
      return _extends({}, state, {
        responses: _extends({}, state.responses, _defineProperty({}, action.payload.key, action.payload.value))
      });
    default:
      return state;
  }
};

exports.default = interview;

/***/ }),
/* 272 */
/***/ (function(module, exports, __webpack_require__) {

var debounce = __webpack_require__(273),
    isObject = __webpack_require__(69);

/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/**
 * Creates a throttled function that only invokes `func` at most once per
 * every `wait` milliseconds. The throttled function comes with a `cancel`
 * method to cancel delayed `func` invocations and a `flush` method to
 * immediately invoke them. Provide `options` to indicate whether `func`
 * should be invoked on the leading and/or trailing edge of the `wait`
 * timeout. The `func` is invoked with the last arguments provided to the
 * throttled function. Subsequent calls to the throttled function return the
 * result of the last `func` invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the throttled function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.throttle` and `_.debounce`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to throttle.
 * @param {number} [wait=0] The number of milliseconds to throttle invocations to.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=true]
 *  Specify invoking on the leading edge of the timeout.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new throttled function.
 * @example
 *
 * // Avoid excessively updating the position while scrolling.
 * jQuery(window).on('scroll', _.throttle(updatePosition, 100));
 *
 * // Invoke `renewToken` when the click event is fired, but not more than once every 5 minutes.
 * var throttled = _.throttle(renewToken, 300000, { 'trailing': false });
 * jQuery(element).on('click', throttled);
 *
 * // Cancel the trailing throttled invocation.
 * jQuery(window).on('popstate', throttled.cancel);
 */
function throttle(func, wait, options) {
  var leading = true,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  if (isObject(options)) {
    leading = 'leading' in options ? !!options.leading : leading;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }
  return debounce(func, wait, {
    'leading': leading,
    'maxWait': wait,
    'trailing': trailing
  });
}

module.exports = throttle;


/***/ }),
/* 273 */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(69),
    now = __webpack_require__(274),
    toNumber = __webpack_require__(276);

/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max,
    nativeMin = Math.min;

/**
 * Creates a debounced function that delays invoking `func` until after `wait`
 * milliseconds have elapsed since the last time the debounced function was
 * invoked. The debounced function comes with a `cancel` method to cancel
 * delayed `func` invocations and a `flush` method to immediately invoke them.
 * Provide `options` to indicate whether `func` should be invoked on the
 * leading and/or trailing edge of the `wait` timeout. The `func` is invoked
 * with the last arguments provided to the debounced function. Subsequent
 * calls to the debounced function return the result of the last `func`
 * invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the debounced function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.debounce` and `_.throttle`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to debounce.
 * @param {number} [wait=0] The number of milliseconds to delay.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=false]
 *  Specify invoking on the leading edge of the timeout.
 * @param {number} [options.maxWait]
 *  The maximum time `func` is allowed to be delayed before it's invoked.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new debounced function.
 * @example
 *
 * // Avoid costly calculations while the window size is in flux.
 * jQuery(window).on('resize', _.debounce(calculateLayout, 150));
 *
 * // Invoke `sendMail` when clicked, debouncing subsequent calls.
 * jQuery(element).on('click', _.debounce(sendMail, 300, {
 *   'leading': true,
 *   'trailing': false
 * }));
 *
 * // Ensure `batchLog` is invoked once after 1 second of debounced calls.
 * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
 * var source = new EventSource('/stream');
 * jQuery(source).on('message', debounced);
 *
 * // Cancel the trailing debounced invocation.
 * jQuery(window).on('popstate', debounced.cancel);
 */
function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  wait = toNumber(wait) || 0;
  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;

    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any `maxWait` timer.
    lastInvokeTime = time;
    // Start the timer for the trailing edge.
    timerId = setTimeout(timerExpired, wait);
    // Invoke the leading edge.
    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        timeWaiting = wait - timeSinceLastCall;

    return maxing
      ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke)
      : timeWaiting;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime;

    // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the `maxWait` limit.
    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||
      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));
  }

  function timerExpired() {
    var time = now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // Restart the timer.
    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    timerId = undefined;

    // Only invoke if we have `lastArgs` which means `func` has been
    // debounced at least once.
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);

    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        // Handle invocations in a tight loop.
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}

module.exports = debounce;


/***/ }),
/* 274 */
/***/ (function(module, exports, __webpack_require__) {

var root = __webpack_require__(113);

/**
 * Gets the timestamp of the number of milliseconds that have elapsed since
 * the Unix epoch (1 January 1970 00:00:00 UTC).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Date
 * @returns {number} Returns the timestamp.
 * @example
 *
 * _.defer(function(stamp) {
 *   console.log(_.now() - stamp);
 * }, _.now());
 * // => Logs the number of milliseconds it took for the deferred invocation.
 */
var now = function() {
  return root.Date.now();
};

module.exports = now;


/***/ }),
/* 275 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

module.exports = freeGlobal;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(33)))

/***/ }),
/* 276 */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(69),
    isSymbol = __webpack_require__(277);

/** Used as references for various `Number` constants. */
var NAN = 0 / 0;

/** Used to match leading and trailing whitespace. */
var reTrim = /^\s+|\s+$/g;

/** Used to detect bad signed hexadecimal string values. */
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

/** Used to detect binary string values. */
var reIsBinary = /^0b[01]+$/i;

/** Used to detect octal string values. */
var reIsOctal = /^0o[0-7]+$/i;

/** Built-in method references without a dependency on `root`. */
var freeParseInt = parseInt;

/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}

module.exports = toNumber;


/***/ }),
/* 277 */
/***/ (function(module, exports, __webpack_require__) {

var baseGetTag = __webpack_require__(278),
    isObjectLike = __webpack_require__(281);

/** `Object#toString` result references. */
var symbolTag = '[object Symbol]';

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && baseGetTag(value) == symbolTag);
}

module.exports = isSymbol;


/***/ }),
/* 278 */
/***/ (function(module, exports, __webpack_require__) {

var Symbol = __webpack_require__(114),
    getRawTag = __webpack_require__(279),
    objectToString = __webpack_require__(280);

/** `Object#toString` result references. */
var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

/** Built-in value references. */
var symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

module.exports = baseGetTag;


/***/ }),
/* 279 */
/***/ (function(module, exports, __webpack_require__) {

var Symbol = __webpack_require__(114);

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Built-in value references. */
var symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

module.exports = getRawTag;


/***/ }),
/* 280 */
/***/ (function(module, exports) {

/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

module.exports = objectToString;


/***/ }),
/* 281 */
/***/ (function(module, exports) {

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

module.exports = isObjectLike;


/***/ }),
/* 282 */
/***/ (function(module, exports) {

// removed by extract-text-webpack-plugin

/***/ }),
/* 283 */,
/* 284 */,
/* 285 */,
/* 286 */,
/* 287 */
/***/ (function(module, exports) {

module.exports = {"item_templates":{"1.002-1.006":{"description":"Hvis patienten ikke spontant fremkommer med oplysninger kan\nman spørge:\n\n*Der findes særlige områder, hvor mange mennesker oplever\nproblemer eller vanskeligheder. Har du nogensinde haft\nvanskeligheder m.h.t. (1.002-1.006)?*\n\n*Hvor generende og besværligt har det været?*\n\nFor hvert gruppe af symptomer spørges:\n\n*Kan du beskrive nærmere, hvordan det føltes?*\n\n*Har det været tilstede siden, i perioder eller hele tiden?*\n\n*Hvor lang tid har du haft det nu?*\n\n**CUT OFF => til 2.001, hvis 1.002-1.006 alle rates 0**\n","input":"integer","options":{"0":"Ingen af disse symptomer tilstede nogen sinde","1":"Tilstede, men kun i let grad","2":"Tilstede i moderat grad","3":"Tilstede i svær grad"}},"1a":{"input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2},"2.114-2.120":{"input":"integer","description":"Rating baseret på svar fra spørgsmålene fra **2.007**-\n**2.084** eller andre dele af interviewet, eller hvis det ikke\ner tilstrækkeligt, kan patienten spørges direkte.\n\nTror patienten, eller er patienten blevet fortalt, at han\neller hun lider af en syndrom- eller symptomdiagnose?\n","options":{"0":"Fraværende","1":"Ja, men ikke 2","2":"Fast og vedholdende overbevist"},"periods":2},"2.147-2.153":{"input":"integer","options":{"0":"Fraværende","1":"Til stede"},"periods":2},"2.154-2.157":{"input":"integer","options":{"0":"Fraværende","1":"Til stede"},"periods":2}},"sections":[{"section":0,"title":"SCAN forside"},{"section":1,"title":"Indledning til interview"},{"section":2,"title":"Somatoforme og dissociative symptomer"},{"section":3,"title":"Bekymring, anspændthed og andre uspecifikke symptomer"},{"section":4,"title":"Angst og fobier"},{"section":5,"title":"Tvangssymptomer"},{"section":6,"title":"Depressivt stemningsleje og tankeindhold"},{"section":7,"title":"Tænkning, koncentration, energi, interesser"},{"section":8,"title":"Fysiologiske funktioner"},{"section":9,"title":"Spiseforstyrrelser"},{"section":10,"title":"Ekspansivt stemningsleje og tankevirksomhed"},{"section":11,"title":"Brug af alkohol"},{"section":12,"title":"Brug af psykoaktive substanser andre end alkohol"},{"section":13,"title":"Påvirkning af aktiviteter og organiske årsager for Part 1 symptomer"},{"section":14,"title":"Screening items for Part 2 symptomer"},{"section":15,"title":"Sprog og tale-problemer ved undersøgelsen"},{"section":16,"title":"Perceptionsforstyrrelser"},{"section":17,"title":"Hallucinationer"},{"section":18,"title":"Tankeforstyrrelses- og styrings-oplevelser"},{"section":19,"title":"Vrangforestillinger"},{"section":20,"title":"Yderligere information til brug for diagnostiske klassifikation og Part 2 symptomer"},{"section":21,"title":"Kognitiv forstyrrelse eller svækkelse"},{"section":22,"title":"Motorik og adfærd"},{"section":23,"title":"Følelser - affekt"},{"section":24,"title":"Sprogforstyrrelser - tale"},{"section":25,"title":"Checkliste for autistisk spektrum"},{"section":26,"title":"Itemgruppe checkliste"},{"section":27,"title":"Skema til anamnestiske baggrundsoplysninger (CHS)"}],"items":[{"key":"0.001","title":"Version af SCAN","input":"string","description":"0.001\n0.002\n","section_start":"Section_0","glossary":0.001},{"key":"0.002","title":"Projekt/Centernummer","input":"string","glossary":0.001},{"key":"0.003","title":"Patient/respondent nummer","input":"string","glossary":0.001},{"key":"0.004","title":"Rater nummer","input":"string","validate":["1-999"],"glossary":0.001},{"key":"0.005","title":"Dato for første PSE-undersøgelse (Dag Måned År )","input":"date","fitb_size":"DATE_NORMAL","glossary":0.001},{"key":"0.006","title":"Periode(r) rated med PSE - uden hensyn til informationskilde","input":"integer","options":{"0":"PSE ikke rated","1":"PSE rated for anførte periode Present state (PS)"},"validate":["0-1"],"used_in_dsmiv":1,"glossary":0.001},{"key":"0.007","title":"Periode(r) rated med PSE - uden hensyn til informationskilde","input":"integer","options":{"0":"PSE ikke rated","1":"PSE rated for anførte periode Repræsentativ tidligere episode (RE)"},"validate":["0-1"],"glossary":0.001},{"key":"0.008","title":"Livstidsperioden forud for PS (LB)","input":"integer","options":{"0":"PSE ikke rated","1":"PSE rated for anførte periode"},"validate":["0-1"],"glossary":0.001},{"key":"0.009","title":"Denne PSE rated fra","input":"integer","options":{"0":"PSE ikke anvendt (IGC anvendt).","1":"Eget interview","2":"Observeret interview.","3":"Videotaped interview.","4":"Audiotaped interview.","5":"Andet, specificeret....."},"validate":["0-5"],"glossary":0.001},{"key":"0.010","title":"Informationskilde anvendt ved PSE-rating (anvend venstre box for PS, højre box for RE eller LB)","input":"integer","options":{"0":"PSE ikke anvendt (IGC anvendt)","1":"Patient/respondent, udelukkende","2":"Patient/respondent og andre kilder","3":"Journalmateriale, udelukkende","4":"Andre personer (pårørende, bekendte, personale), udelukkende","5":"Journalmateriale og andre personer (specificeres...)","6":"Udarbejdet resumé"},"validate":["0-6"],"episodes":1,"glossary":0.001},{"key":"0.011","title":"Anvendelse af IGC: 0.011 Present state (PS)","input":"integer","options":{"0":"IGC ikke anvendt","1":"IGC anvendt for anførte periode"},"validate":["0-1"],"glossary":0.001},{"key":"0.012","title":"Anvendelse af IGC: 0.012 Repræsentativ tidligere episode (RE)","input":"integer","options":{"0":"IGC ikke anvendt","1":"IGC anvendt for anførte periode"},"validate":["0-1"],"glossary":0.001},{"key":"0.013","title":"Anvendelse af IGC: 0.013 Livstidsperioden forud for PS (LB)","input":"integer","options":{"0":"IGC ikke anvendt","1":"IGC anvendt for anførte periode"},"validate":["0-1"],"glossary":0.001},{"key":"0.014","title":"Informationskilde anvendt ved IGC rating (anvend venstre box for PS, højre box for RE eller LB)","input":"integer","options":{"0":"IGC ikke anvendt","1":"Patient/respondent alene (fx rating kun af observeret adfærd)","2":"Patient/respondent og andre kilder","3":"Journalmateriale, udelukkende","4":"Andre personer (pårørende/personale), udelukkende","5":"Journalmateriale og andre personer (specificeres.....)","6":"Udarbejdet resumé"},"validate":["0-6"],"episodes":1,"glossary":0.001},{"key":"0.015","title":"Anvendelse af CHS","input":"integer","options":{"0":"Ikke anvendt","1":"CHS rated"},"validate":["0-1"],"glossary":0.001},{"key":"0.016","title":"Antal perioder rated","input":"integer","options":{"0":"Ikke anvendt 1-6 Antal episoder rated"},"validate":["0-6"],"glossary":0.001},{"key":"0.017","title":"Rating boxe i SCAN","input":"integer","options":{"0":"Ikke anvendt","1":"Anvendt"},"validate":["0-1"],"glossary":0.001},{"key":"0.018","title":"Rating boxe i kodebog","input":"integer","options":{"0":"Ikke anvendt","1":"Anvendt"},"validate":["0-1"],"glossary":0.001},{"key":"0.019","title":"Computerregistrering","input":"integer","options":{"0":"Ikke anvendt","1":"Anvendt"},"validate":["0-1"],"glossary":0.001},{"key":"0.020","title":"Anden registreringsmetode, specificeres...","input":"integer","options":{"0":"Ikke anvendt","1":"Anvendt"},"validate":["0-1"],"glossary":0.001},{"key":"0.021","title":"SOCIODEMOGRAFISKE ITEMS Alder","input":"string","validate":["1-120"],"used_in_dsmiv":1,"glossary":0.021},{"key":"0.022","title":"Fødselsdato (Dag Måned År)","input":"date","fitb_size":"DATE_NORMAL","glossary":0.021},{"key":"0.023","title":"Køn","input":"integer","options":{"1":"Mand","2":"Kvinde"},"validate":["1-2"],"glossary":0.021},{"key":"0.024","title":"Ægteskabelig status","input":"integer","options":{"1":"Gift","2":"Samlevende","3":"Separeret","4":"Skilt","5":"Enkestand","6":"Aldrig gift og ikke samlevende"},"validate":["1-6"],"glossary":0.021},{"key":"0.025","title":"Antal levendefødte børn","input":"string","validate":["0-99"],"glossary":0.021},{"key":"0.026","title":"Antal år i uddannelse","description":"Antal år med fuldført formel uddannelse, ikke omfattende praktik- eller\nlærlingeperioder.\n","input":"string","validate":["0-40",98,99],"glossary":0.021},{"key":"0.027","title":"Arbejdsmæssig status","input":"integer","options":{"1":"Betalt fuldtidsarbejde","2":"Betalt deltidsarbejde","3":"Husligt arbejde","4":"Arbejdsløs","5":"Pensionist, herunder førtidspensionist","6":"Beskyttet arbejde","7":"Studerende","8":"NK uoplyst","9":"NA non-applikabel (fx barselsorlov)"},"validate":["1-9"],"glossary":0.021},{"key":"0.028","title":"Arten af betalt arbejde","description":"Rates ved hjælp af lokalt relevante koder\n","input":"string","glossary":0.021},{"key":"0.029","title":"Antal måneder med betalt arbejde inden for det sidste år 98 = NK 99 = NA","input":"integer","validate":["0-12",98,99],"glossary":0.021},{"key":"1.001.section","type":"divider","title":"Sektion 1","description":"# Indledning til interview\n\nFormålet med sektion 1 er at opnå et overblik over mulige symptomer, som\nden undersøgte person (R) kan have.\n\nFra sektion 1 kan man fortsætte til de sektioner, som indeholder særligt\nfremtrædende symptomer eller symptomer, som R ønsker at fortælle om først.\nOgså i tilfælde af, at interviewet ikke kan gennemføres fuldt ud, bør man\nsøge at få udspurgt om de væsentligste områder først. Intervieweren bør\nforud for interviewet have sat sig grundigt ind i alle foreliggende\noplysninger, og bør være opmærksom på særlige omstændigheder, der kan få\nbetydning for gennemførelsen for interviewet.\n\n## Særlige omstændigheder\n\n  1. Svære sprog og forståelsesmæssige vanskeligheder, eller kognitiv\n     svækkelse\n\n     - Rate sektion 15 for sprogmæssige og forståelsesmæssige\n       vanskeligheder\n     - Rate sektion 21 for kognitiv svækkelse\n     - Rate sektion 22-25 for objektivt iagttagelige sproglige\n       forstyrrelser, adfærds- og følelsesmæssige abnormiteter\n     - Gennemgå CHS\n\n  2. Svært forstyrret adfærd, svigtende kooperation eller anden risiko for\n     at interviewet ikke kan gennemføres fuldt ud\n\n     Begynd med de sektioner, der forekommer at have størst betydning for\n     R. Forsøg at holde samtalen i gang under samtidig observation af den sproglige\n"},{"key":"1.001","title":"R's spontane svar på de indledende spørgsmål","description":"## Indledende bemærkninger\n\nTil at begynde med, vil jeg bede dig fortælle om, hvilke problemer eller\nvanskeligheder der har været af fysisk eller psykisk art i den senere tid.\n\nHar du følt dig sund og rask i fysisk og psykisk henseende, eller har der\nværet noget i vejen?\n\nGiv R mulighed for at komme med en spontan beskrivelse, anvend evt. kun få\nklargørende spørgsmål, såsom:\n  - Kan du forklare lidt nærmere, hvordan det var med.....?\n  - Kan du give et eksempel......?\n  - Har du stadig.......?\n  - Hvornår begyndte......?\n  - Har du fået nogen behandling for......?\n  - Hvilken?\n  - Tabletter, samtaler, eller anden behandling?\n\nHvis R ikke beskriver relevante symptomer, kan man spørge: Synes du\nligefrem, at du har haft et strålende fysisk eller psykisk helbred den\nsenere tid, uden problemer eller vanskeligheder af nogen art?\n","input":"integer","options":{"0":"R beskriver ingen psykotiske, affektive eller neurotiske symptomer den senere tid","1":"R beskriver spontant sådanne symptomer","2":"R utilbøjelig til at beskrive symptomer og kun efter nærmere udspørgen","8":"R svarer uden at give brugbare oplysninger"},"section_start":"Section_1","validate":["0-2",8],"glossary":1.001},{"key":"1.002.section","title":"Symptomgrupper","description":"Benyt alle foreliggende oplysninger, og rate, om der nu eller nogensinde\nhar været symptomer til stede for grupperne 1.002-1.006.\n\nHvis patienten ikke spontant fremkommer med oplysninger kan man spørge:\n\nDer findes særlige områder, hvor mange mennesker oplever problemer eller\nvanskeligheder. Har du nogensinde har haft vanskeligheder m.h.t. (1.002-1.006)?\n\nHvor generende og besværligt har det været?\n\nFor hver gruppe af symptomer spørges:\n  - Kan du beskrive nærmere, hvordan det føltes?\n  - Hvornår begyndte det første gang, hvor gammel var du da?\n  - Har det været til stede siden, i perioder eller hele tiden?\n  - Hvor lang tid har du haft det nu?\n"},{"key":"1.002","title":"Angst eller panikfølelse, ofte i særlige situationer?","description":"- Kan du beskrive nærmere, hvordan det føltes?\n- Hvornår begyndte det første gang, hvor gammel var du da?\n- Har det været til stede siden, i perioder eller hele tiden?\n- Hvor lang tid har du haft det nu?\n","input":"integer","options":{"0":"Ingen af disse symptomer til stede nogen sinde","1":"Til stede, men kun i let grad","2":"Til stede i moderat grad","3":"Til stede i svær grad"},"validate":["0-3",8,9],"glossary":1.001},{"key":"1.003","title":"Nedtrykthed","description":"- Kan du beskrive nærmere, hvordan det føltes?\n- Hvornår begyndte det første gang, hvor gammel var du da?\n- Har det været til stede siden, i perioder eller hele tiden?\n- Hvor lang tid har du haft det nu?\n","input":"integer","options":{"0":"Ingen af disse symptomer til stede nogen sinde","1":"Til stede, men kun i let grad","2":"Til stede i moderat grad","3":"Til stede i svær grad"},"validate":["0-3",8,9],"glossary":1.001},{"key":"1.004","title":"Opstemthed - med følelse af at være højt oppe eller meget irritabel uden grund","description":"- Kan du beskrive nærmere, hvordan det føltes?\n- Hvornår begyndte det første gang, hvor gammel var du da?\n- Har det været til stede siden, i perioder eller hele tiden?\n- Hvor lang tid har du haft det nu?\n","input":"integer","options":{"0":"Ingen af disse symptomer til stede nogen sinde","1":"Til stede, men kun i let grad","2":"Til stede i moderat grad","3":"Til stede i svær grad"},"validate":["0-3",8,9],"glossary":1.001},{"key":"1.005","title":"Oplevelser, som er vanskelige at forstå eller forklare, såsom at kunne høre stemmer eller se syner","description":"- Kan du beskrive nærmere, hvordan det føltes?\n- Hvornår begyndte det første gang, hvor gammel var du da?\n- Har det været til stede siden, i perioder eller hele tiden?\n- Hvor lang tid har du haft det nu?\n","input":"integer","options":{"0":"Ingen af disse symptomer til stede nogen sinde","1":"Til stede, men kun i let grad","2":"Til stede i moderat grad","3":"Til stede i svær grad"},"validate":["0-3",8,9],"glossary":1.001},{"key":"1.006","title":"Problemer med alkohol, stoffer, og medikamenter Kan du beskrive nærmere, hvordan det føltes?","description":"- Kan du beskrive nærmere, hvordan det føltes?\n- Hvornår begyndte det første gang, hvor gammel var du da?\n- Har det været til stede siden, i perioder eller hele tiden?\n- Hvor lang tid har du haft det nu?\n\nCUT OFF => til 2.001, hvis 1.002-1.006 alle rates 0\n","input":"integer","options":{"0":"Ingen af disse symptomer til stede nogen sinde","1":"Til stede, men kun i let grad","2":"Til stede i moderat grad","3":"Til stede i svær grad"},"validate":["0-3",8,9],"glossary":1.001},{"key":"1.007","title":"Begyndelsestidspunktet for nuværende episode (PE)","description":"Afgør ud fra det oplyste, hvilken undersøgelsesperiode for de klinisk\nmest betydende symptomer, der skal anvendes. Anfør tidsangivelser\nnedenfor. I slutningen af hver sektion er der mulighed for at anføre\nafvigende tidsangivelser for symptomer i den pågældende sektion.\n","input":"date","fitb_size":"DATE_NORMAL","glossary":1.007},{"key":"1.008","title":"Antal dage i PS Sædvanligvis 28 dage.","input":"string","used_in_dsmiv":1,"glossary":1.007},{"key":"1.009","title":"Tidsperiode for RE (mdr., år)","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.007},{"key":"1.010","title":"Antal uger i RE","description":"## Tidsangivelse for livsperioden forud for PS\nLivsperioden forud for PS anføres fra alderen ved første optræden af\nsymptomer. Disse kan herefter optræde kontinuerligt eller i episoder.\n\nAnvend LB hvis PS og RE tilsammen ikke giver adækvat beskrivelse af de\nklinisk mest betydende symptomer igennem forløbet.\n","input":"string","glossary":1.007},{"key":"1.011","title":"Episode med neurotiske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.007},{"key":"1.012","title":"Episode med depressive symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.007},{"key":"1.013","title":"Episode med maniske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.007},{"key":"1.014","title":"Episode med psykotiske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.007},{"key":"1.015","title":"Alder ved første optræden af neurotiske symptomer","input":"string","validate":["0-99"],"glossary":1.015},{"key":"1.016","title":"Alder ved første optræden af depressive symptomer","input":"string","validate":["0-99"],"glossary":1.015},{"key":"1.017","title":"Alder første optræden af maniske symptomer","input":"string","validate":["0-99"],"glossary":1.015},{"key":"1.018","title":"Alder ved første optræden af psykotiske symptomer","description":"Tidsangivelser ved somatoforme og dissociative symptomer,\nspiseforstyrrelser, forstyrrelser betinget af brug af alkohol eller\npsykoaktive substanser og kognitiv svækkelse anføres i sektionerne\n2, 9, 11, 12 og 21.\n","input":"string","validate":["0-99"],"glossary":1.015},{"key":"1.019","title":"Kvaliteten af remissionsperioder mellem episoderne","description":"Hvis der har været 2 eller flere episoder af samme eller forskellig\nkarakter rates for eventuelle klinisk betydende symptomer mellem episoderne:\n","input":"integer","options":{"0":"Ingen eller kun én episode uden efterfølgende betydende symptomer","1":"Ingen eller kun få symptomer mellem episoderne","2":"Nogle symptomer i nogle af intervallerne","3":"En del symptomer i de fleste intervaller","4":"Udtalte symptomer i alle intervallerne","5":"Vedvarende symptomer, ingen remission"},"validate":["0-5"],"used_in_dsmiv":1,"glossary":1.019},{"key":"1.020","title":"Episode 1","description":"Til brug ved særlige undersøgelser kan yderligere rates for op til 6\nspecielle perioder (med hver sit kodeskema) Tidsangivelse for specielle\nenkeltepisoder\n","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.021","title":"Episode 2","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.022","title":"Episode 3","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.023","title":"Episode 4","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.024","title":"Episode 5","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.025","title":"Episode 6","input":"date_interval","fitb_size":"DATE_NODAY","glossary":1.02},{"key":"1.026.section","title":"Medikamentel behandling ved behandling ved tidspunktet for undersøgelsen","description":"Du nævnte at du tog medicin/tabletter efter lægens ordination.\n\n- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n"},{"key":"1.026","title":"Hypnotika","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.027","title":"Anxiolytika","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.028","title":"Antidepressiva","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.029","title":"Antimanica","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.030","title":"Antipsykotika","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.031","title":"Anti-Parkinsonmidler","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":1.026},{"key":"1.032","title":"Antabus","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.033","title":"Analgetika","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.034","title":"Anden medicin, specificeres...","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.035","title":"Ikke identificeret medicin","description":"- Hvad drejer det sig om for medicin?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen medikamenter anvendes","1":"Medikamenter anvendes, skønnes ikke at have påvirket symptomerne","2":"Medikamenter anvendes, kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.026},{"key":"1.036","title":"Virkningen af medikamentel behandling ved undersøgelsestidspunktet","input":"integer","options":{"0":"Tilsyneladende ingen virkning","1":"Moderat bedring af symptomerne","2":"Betydelig bedring af symptomerne"},"validate":["0-2",8,9],"glossary":1.026},{"key":"1.037","title":"Bivirkninger af medikamentel behandling ved undersøgelsestidspunktet","input":"integer","options":{"0":"Tilsyneladende ingen","1":"Moderate bivirkninger","2":"Svære bivirkninger","3":"Svære bivirkninger, som påvirker vurderingen af symptomer"},"validate":["0-3",8,9],"glossary":1.026},{"key":"1.038","title":"Psykosocial behandling ved undersøgelsestidspunktet","description":"Du nævnte at du var i samtalebehandling.\n\n- Ved du hvilken form for samtalebehandling det drejer sig om?\n- Hvilken virkning har det haft?\n- Har det hjulpet på ........ symptomerne?\n- Har behandlingen haft oprivende eller ubehagelige virkning?\n","input":"integer","options":{"0":"Ingen psykosocial behandling","1":"Anvendt, men skønnes ikke at have påvirket symptomerne","2":"Anvendt og kan have betydning for til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.039","title":"Formaliseret psykoterapi","description":"- Hvad drejer det sig om for psykoterapi?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Har behandlingen haft oprivende eller ubehagelig virkning?\nHvilke?\n","input":"integer","options":{"0":"Ingen psykoterapi","1":"Behandlingen skønnes ikke at have påvirket symptomerne","2":"Behandlingen kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.040","title":"Krisebehandling","description":"- Hvilken form for krisebehandling?\n- Hvem ordinerede den?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige virkninger ved behandlingen?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen kriseterapi","1":"Behandlingen skønnes ikke at have påvirket symptomerne","2":"Behandlingen kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.041","title":"Anden formaliseret samtalebehandling","description":"- Hvad drejer det sig om forbehandling?\n- Hvem ordinerede det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige virkninger af behandlingen?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen behandling","1":"Behandlingen skønnes ikke at have påvirket symptomerne","2":"Behandlingen kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.042","title":"Støtte fra familie og venner","description":"- På hvilken måde støtter de dig?\n- Hvem tog initiativ til det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige virkninger ved det?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen støtte","1":"Skønnes ikke at have påvirket symptomerne","2":"Kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.043","title":"Anden støtte eller hjælp, specificeres...","description":"- Hvilken støtte drejer det sig om?\n- Hvem tog initiativ til det?\n- Hvilken virkning har det haft?\n- Har det hjulpet på................[symptomerne]?\n- Er der ubehagelige bivirkninger?\n- Hvilke?\n","input":"integer","options":{"0":"Ingen støtte eller hjælp","1":"Skønnes ikke at have påvirket symptomerne","2":"Kan have påvirket til stedeværelsen eller sværhedsgraden af symptomerne","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.038},{"key":"1.044","title":"Global vurdering af virkningen af psykosocial behandling ved undersøgelsestidspunktet","input":"integer","options":{"0":"Ingen virkning","1":"Moderat virkning på symptomerne","2":"Betydelig bedring"},"validate":["0-2",8],"glossary":1.038},{"key":"1.045.section","title":"Andre anamnestiske oplysninger"},{"key":"1.045","title":"Familiær disposition: forældre, søskende, børn","input":"integer","options":{"0":"Ingen","1":"Ja, lignende symptomer som hos R","2":"Ja, andre symptomer end hos R","8":"Uoplyst"},"validate":["0-2",8],"glossary":1.045},{"key":"1.046","title":"Alder ved første prodromalsymptomer","description":"98 Uoplyst\n99 Ikke anvendelig\n","input":"integer","validate":["0-99"],"glossary":1.045},{"key":"1.047","title":"Alder ved første klare erkendelse af tilstanden","description":"98 Uoplyst\n99 Ikke anvendelig\n","input":"integer","validate":["0-99"],"glossary":1.045},{"key":"1.048","title":"Alder ved første behandlingskontakt","description":"98 Uoplyst\n99 Ikke anvendelig\n","input":"integer","validate":["0-99"],"glossary":1.045},{"key":"1.049","title":"Begyndelsesmåde for første sygdomsepisode","input":"integer","options":{"1":"Inden for 1 måned","2":"I løbet af 1–3 måneder","3":"I løbet af 3–12 måneder","4":"Over mere end 1 år","8":"Uoplyst"},"validate":["1-4",8],"glossary":1.045},{"key":"1.050","title":"Social kompetence før erkendt sygdomsbegyndelse","input":"integer","options":{"0":"Sædvanlige inden for pågældende kulturkreds","1":"Moderat social funktionsnedsættelse","2":"Udtalt social funktionsnedsættelse"},"validate":["0-2",8],"glossary":1.045},{"key":"1.051","title":"Totale antal hospitalsindlæggelser for psykisk lidelse","description":"98 Uoplyst\n99 Ikke anvendelig\n","input":"integer","validate":["0-99"],"glossary":1.045},{"key":"1.052","title":"Længste hospitalshopholds varighed i måneder","description":"998 Uoplyst\n999 Ikke anvendelig\n","input":"integer","validate":["0-99",998,999],"glossary":1.045},{"key":"1.053","title":"Antal tidligere episoder med klinisk betydende symptomer","options":{"98":"Uoplyst","99":"Ikke anvendelig"},"input":"integer","glossary":1.045},{"key":"2.001.section","title":"Funktionelle og dissociative symptomer","description":"Denne sektion afviger fra de følgende sektioner ved at anvende *afvigende\ntidsperioder* for present state/present episode (2 eller 1 år). Der\nanvendes Rating Skala 1a eller specifikke ratings for hvert item.\nTidsperioden rates i item 2.134–2.137.\n"},{"key":"2.001","title":"Fysiske helbred gennem sidste måned","description":"Hvordan har dit helbred været gennem den sidste måned?\n\nHar det været strålende, godt, mindre godt eller dårligt?\n","input":"integer","options":{"1":"Strålende","2":"Godt","3":"Mindre godt","4":"Dårligt","8":"Usikkert","9":"Ukendt"},"section_start":"Section_2","validate":["1-4",8,9],"glossary":2.001},{"key":"2.002","title":"Varighed af helbredsbesvær (mdr.) (800=800 mdr. eller derover 888=NK 999=NA)","description":"Hvor længe har dit fysiske helbred været dårligt?\nRates i antal måneder.\n","input":"integer","options":{"0":"Ingen helbredsbesvær"},"validate":["0-800",888,999],"glossary":2.002},{"key":"2.003","title":"Fysisk sygdom eller svækkelse gennem det sidste år","description":"Har du inden for det sidste år haft fysiske sygdomme, skader, smerter eller andre lidelser, som har begrænset din udfoldelse på én eller anden måde? Hvad drejer det sig om? Hvad sagde lægen, at der var i vejen?\n","input":"integer","options":{"0":"Ingen","1":"Beskriver legemlig lidelse eller symptomer uden klar diagnose","2":"Angiver somatisk diagnose","3":"Både somatisk diagnose og funktionelle symptomer/sygdom","8":"Usikkert","9":"Ukendt"},"validate":[0,1,2,3,8,9]},{"key":"2.004a","title":"VELDEFINEREDE SOMATISKE LIDELSER 2.004a-r Veldefinerede og lægediagnosticerede fysiske sygdomme (lifetime)","description":"[Vis et huskekort, hvis nødvendigt]\na. Har du fået at vide af en læge, at du lider af én eller flere af disse sygdomme (a-r)?\nb. Har du fået medicin for (a-r)? c. Har du fået laboratorieundersøgelser for (a-r) d. Er du blevet tilrådet at ændre din kost eller livsstil p.g.a. (a-r)? e. Har du været indlagt eller blevet opereret for (a-r)?\nf. Har du på noget tidspunkt stoppet eller indskrænket dine daglige aktiviteter i en uge eller mere (a-r)? 2.004a Forhøjet blodtryk (ICD-10: I10-I15)\n","input":"string","fitb_size":"ICD10","glossary":2.004},{"key":"2.004b","title":"Sukkersyge (Diabetes) (ICD-10: E10-E14)","input":"string","fitb_size":"ICD10"},{"key":"2.004c","title":"Slid-/ledegigt (Reumatisme) (ICD-10: M00-M19)","input":"string","fitb_size":"ICD10"},{"key":"2.004d","title":"Migræne/specificeret hovedpine (ICD-10: G43, G44.0-1, G44.3-8)","input":"string","fitb_size":"ICD10"},{"key":"2.004e","title":"Ryg- og discussygdom (ICD-10: M40-M53.8, M54.0-1, 3-4)","input":"string","fitb_size":"ICD10"},{"key":"2.004f","title":"Hjertelidelse (ICD-10: I05-I09, I20-I25)","input":"string","fitb_size":"ICD10"},{"key":"2.004g","title":"Tuberkulose (ICD-10: A15-A19, B90, J65)","input":"string","fitb_size":"ICD10"},{"key":"2.004h","title":"Astma/kronisk bronkitis (ICD-10: J30-J32, J35, J37, J40-J47)","input":"string","fitb_size":"ICD10"},{"key":"2.004i","title":"Mavekatar/sår (Gastritis/ulcus) (ICD-10: K25-K27)","input":"string","fitb_size":"ICD10"},{"key":"2.004j","title":"Leverbetændelse (hepatitis) (ICD-10: B18, K73, K75.9)","input":"string","fitb_size":"ICD10"},{"key":"2.004k","title":"Nyresygdom (ICD-10: N03-07, N11-15, N18-19, N25-28)","input":"string","fitb_size":"ICD10"},{"key":"2.004l","title":"Parasitiske sygdomme/malaria (ICD-10: B50-B83)","input":"string","fitb_size":"ICD10"},{"key":"2.004m","title":"Stofskifte sygdom (Thyroidea sygdomme) (ICD-10: E00-E07)","input":"string","fitb_size":"ICD10"},{"key":"2.004n","title":"Svulst (Tumor/cancer) (ICD-10: C00-C97)","input":"string","fitb_size":"ICD10"},{"key":"2.004o","title":"Slagtilfælde (ICD-10: I60-I69)","input":"string","fitb_size":"ICD10"},{"key":"2.004p","title":"HIV/AIDS (ICD-10: B20-B24)","input":"string","fitb_size":"ICD10"},{"key":"2.004q","title":"Andre 1 (ICD-10 kode:)","input":"string","fitb_size":"ICD10"},{"key":"2.004r","title":"Andre 2 (ICD-10 kode:)","input":"string","fitb_size":"ICD10"},{"key":"2.005","title":"Påvirkning af aktiviteter gennem det sidste år på grund af veldefineret somatisk sygdom (se også 2.131)","description":"Har nogle af disse forhold ( symptomer eller lidelser fra 2.004a-2.004r ) påvirket dine aktiviteter gennem det sidste år?\nHvor meget har de nævnte symptomer forstyrret dine sædvanlige dagligdags- aktiviteter gennem det sidste år?\n","input":"integer","options":{"0":"Ingen påvirkning eller ingen veldefineret somatisk sygdom","1":"Kun lille påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9]},{"key":"2.006","title":"Tilfredshed med given behandling gennem det sidste år","description":"- Har du søgt hjælp eller behandling for de omtalte problemer eller symptomer?\n- Hvor ofte har du søgt hjælp hos læge eller andre?\n- Hvad har lægen sagt, der var i vejen?\n- Har du været rimeligt tilfreds med undersøgelser og behandling?\n- Går du fortsat til behandling?\n","input":"integer","options":{"0":"R tilfreds med undersøgelse og behandling eller er utilfreds med rimelig grund eller har slet ikke søgt hjælp","1":"R utilfreds med behandlingen uden rimelig grund","8":"Uklart om utilfredshed skyldes R's overbekymring, eller om der er rimelig grund hertil","9":"Ukendt CUT OFF => til 3.001, hvis der ikke ud fra foreliggende oplysninger er mistanke om funktionelle eller dissociative symptomer. Man bør altid fortsætte, hvis R har symptomer uden overbevisende fysisk forklaring."},"validate":[0,1,8,9]},{"key":"2.007.section.0","title":"Funktionelle symptomer (legemlige symptomer uden fysisk forklaring)","description":"Brug Rating Skala 1a hvis andet ikke er angivet\n\nHvis present episode ikke kan defineres, rates for de sidste 2 år (men kan have været til stede gennem længere tid end 2 år).\n\nRates ud fra alle foreliggende oplysninger, herunder journaloplysninger.\n\nFor at et symptom skal rates som funktionelt skal det ikke kunne tilskrives en kendt somatisk sygdom eller en evt. tilstedeværende legemlig sygdom kan ikke forklare sværhedsgraden, omfanget, kombinationen eller vedholdenheden af de fysiske symptomer eller den ledsagende funktionsnedsættelse.\n\nUdspørgen af patienten/respondenten:\n\nGennemgå symptomerne, som patienten allerede har angivet, og check 2.014-2.094 .\nUddybende spørgsmål: Hvordan føltes det (symptom)? Hvor udtalt var det? Hvordan påvirkede det dig? Søgte du lægehjælp? Fik du foretaget særlige undersøgelser? Har du fået behandling? Hvilken? Hvad sagde lægen, at der var i vejen? Hvad tror du selv? [Vis evt. huskekort]\n"},{"key":"2.007.section.1","title":"Almene symptomer"},{"key":"2.007","title":"Appetitændring (jf. 8.005)","input":"string","validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.008","title":"Vægtændring (jf. 8.006-7)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.009","title":"Sløjhed, uoplagthed","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.010","title":"Excessiv og generende træthed (efter fysisk anstrengelse)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.011","title":"Excessiv og generende træthed (efter psykisk anstrengelse)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.012","title":"Manglende evne til at rekreere sig efter træthedstilstand","description":"Hvis du føler dig udmattet efter fysisk eller psykisk anstrengelse, hvor lang tid varer udmattelsen da?\n- Kan du overvinde den ved hvile eller afslapning?\nBrug Rating Skala Ia\n","input":"integer","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.013","title":"Andre almene, ikke organspecifikke symptomer Specificeres:","description":"Brug Rating Skala Ia\n","input":"integer","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.014","title":"SMERTESYMPTOMER 2_014 Spændingshovedpine","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.015","title":"Hovedpine af anden karakter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.016","title":"Rygsmerter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.017","title":"Muskelsmerter eller -ømhed","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.018","title":"Smerte i armene eller benene","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.019","title":"Ledsmerter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.020","title":"Brystsmerter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.021","title":"Mavesmerter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.022","title":"Smerter i forbindelse med menstruation","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.023","title":"Smerter i forbindelse med samleje","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.024","title":"Vandladningssmerter","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.025","title":"Smerter i endetarmen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.026","title":"Smerter i øre, næse eller hals","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.027","title":"Smerter i tænder, kæbe eller mundhule","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.028","title":"Smerter i andre organer eller dele af kroppen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.029","title":"Smerter, som flytter sig fra sted til sted","input":"string","validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.030.section","title":"gastrointestinale symptomer"},{"key":"2.030","title":"Utilpashed, kvalme","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.031","title":"Opkastninger (ikke i forbindelse med svangerskab)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.032","title":"Reflux eller opstød","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.033","title":"Oppustethed, spændings- eller tyngdefornemmelse i maven","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.034","title":"Forstoppelse","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.035","title":"Løse afføringer","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.036","title":"Diarré","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.037","title":"Udflåd fra endetarmen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.038","title":"Dårlig smag i munden, belagt tunge","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.039","title":"Luftslugen, hikke","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.040","title":"Brændende fornemmelse i brystet eller epigastriet","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.041","title":"Vanskelighed ved at tåle forskellige fødeemner","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.042","title":"Andre gastrointestinale klager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.043.section","title":"kardiovaskulære symptomer"},{"key":"2.043","title":"Palpitationer","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.044","title":"Trykken i prækordiet","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.045","title":"Forpustethed uden anstrengelse","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.046","title":"Anstrengelsesdyspnø","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.047","title":"Hyperventilation","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.048","title":"Andre kardiovaskulære klager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.049.section","title":"Urogenitale symptomer"},{"key":"2.049","title":"Hyppig vandladning","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.050","title":"Blæretømningsbesvær","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.051","title":"Ubehagelige fornemmelser i kønsorganerne","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.052","title":"Rigeligt eller usædvanligt udflåd fra skeden","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.053","title":"Uregelmæssig menstruationer","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.054","title":"Svære menstruationsblødninger","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.055","title":"Sparsomme menstruationsblødninger, amenoré","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.056","title":"Manglende seksuallyst","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.057","title":"Erektiv eller ejakulatorisk dysfunktion","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.058","title":"Andre urogenitale klager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.059.section","title":"Neurologiske symptomer"},{"key":"2.059","title":"Hukommelsesbesvær","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.060","title":"Koncentrationsbesvær (cf. 7.001-7.003)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.061","title":"Besvimelsestendens","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.062","title":"Svimmelhed","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.063","title":"Følelse af koordinations- eller balancebesvær","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.064","title":"Følelse af lammelse eller lokaliseret kraftnedsættelse","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.065","title":"Ubehagelig dødhedsfornemmelse eller paræstesier","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.066","title":"Tab af berørings- eller smertesans","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.067","title":"Synkebesvær eller følelse af klump i halsen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.068","title":"Afoni, manglende stemmeklang","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.069","title":"Dobbeltsyn","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.070","title":"Synsforstyrrelser","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.071","title":"Forstyrrelser af hørelsen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.072","title":"Kramper i benene","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.073","title":"Krampeanfald eller -trækninger","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.074","title":"Andre former for bevidsthedstab","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.075","title":"Andre neurologiske klager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.076.section","title":"Hud- og kirtelsymptomer"},{"key":"2.076","title":"Hudpletter eller misfarvninger","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.077","title":"Ødemer","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.078","title":"Udtalt svedtendens","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.079","title":"Brændende eller kløende hudfornemmelser","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.080","title":"Hævelse eller udflåd fra brystet","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.081","title":"Andre hud- eller kirtelklager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.082.section","title":"Symptomer fra andre organsystemer"},{"key":"2.082","title":"Tinnitus","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.083","title":"Øre, næse eller hals (andet end smerter)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.084","title":"Tænder, kæbe eller mundhule (andet end smerter)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.085","title":"Øjne (ud over pseudoneurologiske)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.086","title":"Muskler og led (ud over smerter)","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.087.section","title":"Autonome symptomer"},{"key":"2.087","title":"Hjertebanken eller hurtig puls","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.088","title":"Varm eller kold sveden","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.089","title":"Rysten eller sitren","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.090","title":"Slimhindetørhed i munden","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.091","title":"Rødmen eller blussen","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.092","title":"Køren i maven eller sommerfugle-fornemmelse","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.093","title":"Andre autonome klager","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.094","title":"Andre somatoforme symptomer, specificeres","input":"integer","description":"Brug Rating Skala 1a\n\n- Hvordan føltes det (symptom)?\n- Hvor udtalt var det?\n- Hvordan påvirkede det dig?\n- Søgte du lægehjælp?\n- Fik du foretaget særlige undersøgelser?\n- Har du fået behandling? Hvilken?\n- Hvad sagde lægen, at der var i vejen?\n- Hvad tror du selv?\n","periods":2,"validate":["0-3",5,6,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.095","title":"Hovedklagen eller mest dominerende symptom (2.007-2.094)","description":"Specificeres:\n\n(rate 888 hvis pt. ikke kan angive et enkelt hoved symptom)\n\nHvilket af disse symptomer har generet dig mest?\n","input":"string","episodes":1},{"key":"2.096","title":"Varighed af lidelse eller ubehag på grund af symptomerne (mdr.)","description":"(800=800 mdr. eller derover 888=NK 999=NA)\n\nHar du være bekymret for, at de nævnte symptomer skyldes en fysisk sygdom, måske en alvorlig sygdom?\n\n- Har bekymringen været meget generende?\n- Hvor længe har du haft det på den måde?\n- Hvor gammel var du, da det begyndte?\n","input":"integer","options":{"0":"Ikke til stede"},"validate":["0-799",800,888,999],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.097","title":"Vekslen af funktionelle symptomer i tid og skift af sygdomsbillede (\"langsomme\" variationer)","description":"Over måneder eller år.\n\nSkift i sygdomsbillede der kan være relativ stabilt i en periode for derefter skifte til et andet organsystem/sygdomsbillede.\n\nOplysninger fra anden kilde bør også indgå i vurderingen, fx fra journaler, henvisende læge, familie.\n\n- Har dine symptomer eller sygdom ændret sig?\n- Har du fejlet flere forskellige ting?\n","input":"integer","options":{"0":"Stabilt med små ændringer, fokuseret på det samme relativt stabile symptom / sygdomsbillede","1":"Symptomer/sygdomsbillede har ændret sig fra et til et eller flere andre relativt stabile symptomer / sygdomsbillede","2":"Flere symptomer/sygdomsbilleder samtidigt","3":"Ingen afgrænsede symptomer/sygdoms- billede","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.098","title":"Vekslen af funktionelle symptomer (\"hurtige\" variationer)","description":"Fra time til time, dag til dag eller uge for uge.\n\nOplysninger fra anden kilde bør også indgå i vurderingen, fx journaler, henvisende læge, familie\n\n- Kan der være forskelle i, hvordan du har det fra dag til dag eller uge for uge?\n- Hvor hurtigt skifter det?\n- Kan du være helt frisk de dage, hvor du har det godt?\n","input":"integer","options":{"0":"Ingen fluktuation","1":"Moderat fluktuation","2":"Udtalt fluktuation","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.099","title":"Funktionel og/eller hypokonderoptagethed og bekymring over fysisk helbred","description":"- Hvad mener du selv, der er i vejen?\n- Er du bange for at fejle noget alvorligt?\n- Har du søgt hjælp hos flere forskellige læger i løbet af de sidste 2 år?\n\n- Hvilke undersøgelser blev der foretaget? Hvad var resultatet?\n- Blev der foretaget flere hold undersøgelser?\n- Hvorfor tror du, der er noget fysisk i vejen?\n- Er du bange for, at du har en alvorlig sygdom?\n- Har du selv prøvet at behandle dig med nogen form for medicin?\n- Hvilke medikamenter tager du?\n","help":"Bedømmes ud fra graden af optagethed, sygdomsfølelse eller gener, og hyppigheden af konsultatio og undersøgelser til trods for lægens forsikring om, at der ikke er fysisk forklaring på symptomet eller tilstanden.\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let til moderat optagethed","2":"Svær optagethed med lægekonsultationer, undersøgelser, vedvarende selvmedicinering eller udtalt besvær i hverdagen","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.100","title":"Optagethed af specifik sygdom","description":"- Er det nogen bestemte sygdomme, du tænker på?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller patienten/R mener ikke, at symptomerne skyldes en eller flere navngivne betydende/alvorlige fysiske sygdomme","1":"Frygt for 1 højst 2 navngivne betydende/alvorlige fysiske sygdomme","2":"Frygt for 3 eller flere navngivne betydende/alvorlige fysiske sygdomme","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.101","title":"Dysmorfofobi","input":"integer","options":{"0":"Ingen dysmorfofobi","1":"Optagethed af formodet deformitet eller forandring af udseendet","8":"Usikkert","9":"Ukendt cf. dysmorfofobi 16.011"},"validate":["0-1",8,9],"episodes":1},{"key":"2.102","title":"Kropslig optagethed","description":"Er du i almindelighed meget opmærksom på, hvordan din krop har det, og hvad der sker i den?\n\n- Hvad har du lagt mærke til?\n- Kan du nogle gange have den tanke, at de fornemmelser, du har i kroppen, er unaturlige eller farlige?\n- Kan et blåt mærke eller en hudirritation gøre dig bekymret for, at det er tegn på noget farligt?\n- Lytter du ofte til din egen puls?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let optagethed eller påvirkelighed men ikke betydeligt indgribende i hverdagen","2":"Overdreven optagethed eller påvirkelighed resulterende i betydeligt besvær i hverdagen eller talrige konsultationer eller selvmedicinering","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.103","title":"Optagethed af sundhedslitteratur, helse og sygdom","description":"- Er du i almindelighed meget optaget af sundhedsstof i aviser, ugeblade og TV.\n- Går du meget op i det?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let optagethed eller påvirkelighed men ikke betydeligt indgribende i hverdagen","2":"Overdreven optagethed medførende betydeligt besvær i hverdagen eller talrige konsultationer eller selvmedicinering","8":"Usikkert","9":"Uklart"},"validate":["0-2",8,9],"episodes":1},{"key":"2.104","title":"Sundhedsadfærd","description":"- Er du i almindelighed meget opmærksom på sunde kostvaner, motion og naturprodukter?\n","input":"integer","options":{"0":"Ingen særlig opmærksomhed eller tilstanden ikke til stede","1":"Let opmærksomhed men ikke betydeligt indgribende i hverdagen","2":"Overdreven opmærksomhed og optagethed resulterende i betydelig påvirkning af hverdagen","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.105","title":"Suggestibilitet og autosuggestibilitet","description":"Hvis du læser eller hører om sygdomme i medierne,\n\n- Kan du blive bange for, at du selv har en sådan sygdom?\n- Kan familiemedlemmers sygdom gøre dig bange for, at du fejler det samme?\n- Undgår du at læse helsestof for at undgå at provokere frygt for sygdom?\n- Hvis du kommer til at tænke på en sygdom, kan du så blive bange for selv at have den?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let til moderat optagethed eller nogen påvirkelighed","2":"Let påvirkelighed og svær optagethed, som giver hyppigt inducerede symptomer eller betydelige spekulationer eller hyppige lægebesøg","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.106","title":"Frygt for smitte","description":"- Er du ofte bange for at blive smittet eller pådrage dig en sygdom fra andre ved at være i kontakt med dem eller røre ved snavsede ting (eksempelvis toiletsæder)?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let til moderat optagethed","2":"Svær optagethed, hyppigt inducerede symptomer med betydelige spekulationer eller hyppige lægebesøg","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.107","title":"Obsessive ruminationer over sygdom","description":"Hvis du får tanker om, at du måske fejler noget, har du så svært ved at slå tanken ud af hovedet?\n- Går du og tænker på det næsten hele tiden, eller kan det gå op i en spids?\n","input":"integer","options":{"0":"Ingen særlig optagethed eller tilstanden ikke til stede","1":"Let til moderat optagethed","2":"Svær optagethed, der indvirker betydeligt på hverdagens gøremål","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.108","title":"Varighed af hypokonder-optagethed/bekymring (mdr.)","description":"(800=800 mdr. eller derover 888=NK 999=NA)\n","input":"integer","options":{"0":"Ikke tilstede"},"validate":["0-799",800,888,999],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.109","title":"Sygdoms- eller hypokonder-overbevisning og korrigerbarhed","description":"- Har lægens forsikringer overbevist dig om, at du ikke har en legemlig (medicinsk/kirurgisk) sygdom?\n- Tror du det kan skyldes stress eller belastning?\n- Tror du det kan skyldes at det er en måde din krop reagerer på?\n- Kan det skyldes at du er mere fintfølende overfor kropslige signaler og symptomer?\n\n- Har du nævnt for lægen, at du ikke er overbevist?\n- Hvad siger han til det?\n- Følte du dig beroliget, da lægen sagde, at der ikke var nogen grund til bekymring?\n- Kan du ikke lade være med at tro, at der er nogen grund til flere undersøgelser eller behandling?\n","help":"Patienten giver udtryk for mere eller mindre udtalt grad af overbevisning om, at han har en alvorlig sygdom eller generende deformitet til trods for lægens forsikringer om, at der ikke er noget i vejen. Må ikke forveksles med frygt for at pådrage sig sygdom 4.044 . Dysmorfofobi rates ved 16.011 . Hypokondre vrangforestillinger (urokkelig overbevisning om at fejle noget alvorligt) rates ved 19.027 og 19.028 .\n","input":"integer","options":{"0":"Accepterer forklaringen","1":"Partiel, har ikke kunnet fastholde forsikring ved én eller flere lejligheder","2":"Kan kun kortvarigt fastholde forsikring","3":"Har kontinuerligt ikke kunnet acceptere lægens forsikring eller forklaringer.","8":"Usikkert","9":"Uklart"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.110","title":"Årsagssøgende adfærd","description":"Er det vigtigt for dig, at du præcist ved og forstår, hvad du fejler? - eller er du tilfreds, blot lægen behandler dine symptomer?\n","input":"integer","options":{"0":"Ringe grad af begge eller lige udtalt","1":"Mere fokuseret på årsag end symptomer","2":"Mere fokuseret på symptomer end årsag","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.111","title":"Medicinfrygt og compliance","description":"Er du i almindelighed tryg ved at tage medicin, eller har du bekymringer over de eventuelle bivirkninger, det måtte have?\n\nTager du al den medicin, lægen ordinerer, eller må du somme tider opgive at tage medicinen eller springe doseringer over af frygt for medicinen?\n","input":"integer","options":{"0":"Normal grad af tryghed/utryghed","1":"En vis grad af medicinfrygt eller en vis grad af uhensigtsmæssig administration af medicin","2":"Høj grad af utryghed og/eller manglende compliance","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.112","title":"Uoverensstemmelser med behandlersystemet gennem de sidste 2 år","description":"Har der været konflikter eller uoverensstemmelser med din(e) læge(r) eller behandler(e) de sidste 2 år?\n\nHar der været situationer, hvor du har følt dig udsat for afvisning eller at blive behandlet dårligt?\n\nHar du overvejet at skifte læge eller behandler (hospital), fordi du ikke har tiltro til disse?\n\nHar du nogensinde lagt sag an mod en behandler eller behandlersystemet?\n","help":"(Som positive besvarelser henregnes også, hvis patienten har udtrykt mistillid eller fjendskab mod nuværende eller tidligere læger eller behandlere under interviewet)\n","input":"integer","options":{"0":"R udtrykker sig rimeligt og afstemt i forhold til anamnesen","1":"R udtrykker fjendtlighed eller uoverensstemmelser uden rimelig grund","8":"Uklart om fjendtlighed er med rimelig grund i forhold til anamnesen","9":"Ukendt"},"validate":[0,1,8,9],"episodes":1},{"key":"2.113","title":"Behandlingssøgende adfærd gennem de sidste 2 år i forb. med somatisering","description":"Hvor mange læger/behandlere har du set for din sygdom/dine symptomer gennem de sidste 2 år?\n\nHvorfor så mange forskellige?\n\nHar det været nødvendigt for dig at søge andre læger eller eksperter for en yderligere vurdering eller behandling (\"for en sikkerheds skyld\")?\n","input":"integer","options":{"0":"Patienten har ikke uopfordret set andre læger","1":"Patienten har kun én gang søgt en anden læge for medicinsk uforklarlige symptomer","2":"Patienten har aktivt opsøgt 2-3 andre læger/behandlere for somatoformt problem (medicinsk uforklarlige symptomer)","3":"Patienten har aktivt opsøgt 4 eller flere læger/behandlere for somatoformt problem (medicinsk uforklarlige symptomer)","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1},{"key":"2.114.section","title":"Syndrom- eller symptombaserede diagnoser ifølge patienten","description":"Rating baseret på svar på spørgsmålene fra 2.007-2.094 eller andre dele af interviewet, eller hvis det ikke er tilstrækkeligt, kan patienten spørges direkte.\n\nTror patienten, eller er patienten blevet fortalt, at han eller hun lider af en syndrom- eller symptomdiagnose?\n"},{"key":"2.114","title":"Fibromyalgi","input":"integer","options":{"0":"Fraværende","1":"Ja, men ikke 2","2":"Fast og vedholdende overbevist"},"validate":["0-2"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.115","title":"CFS (kronisk træthedssyndrom)","input":"integer","validate":["0-2"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.116","title":"IBS (irritabel tyktarm)","input":"integer","validate":["0-2"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.117","title":"Smertetilstand (ud over fibromyalgi)","input":"integer","validate":["0-2"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.118","title":"Anden: WAD","input":"integer","validate":["0-2"],"episodes":1},{"key":"2.119","title":"Anden: Duft- og parfumeallergi","input":"integer","validate":["0-2"],"episodes":1},{"key":"2.120","title":"Anden","input":"integer","validate":["0-2"],"episodes":1},{"key":"2.121","title":"Varighed af syndrom tilstand (mdr.) (2.114-2.120)","description":"(800=800 mdr. eller derover 888=NK 999=NA)\n\nHvis flere er tilstede rates den længstvarende eller mest dominerende\n","help":"Rates i antal måneder\n","input":"integer","validate":["0-799",800,888,999],"episodes":1},{"key":"2.122.section","title":"Begivenheder i forbindelse med symptom eller sygdoms debut.","description":"(hvis ikke fremkommet under den øvrige del af interviewet spørges pt direkte)\n\n- Hvordan begyndte din sygdom (eller symptomerne)\n- Skete der andre ting i dit liv samtidigt\n- Var der noget der udløste din sygdom.\n"},{"key":"2.122","title":"Fysisk traume (f.eks. whiplash, knoglebrud, slag mod hovedet)","input":"integer","options":{"0":"Nej, ingen særlige begivenheder","1":"Ja, men ikke 2","2":"Ja, klar sammenhæng /sammenfald","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.123","title":"Somatisk sygdom (f.eks. infektionssygdom)","input":"integer","options":{"0":"Nej, ingen særlige begivenheder","1":"Ja, men ikke 2","2":"Ja, klar sammenhæng /sammenfald","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.124","title":"Emotionelt traume, konflikter eller belastning","input":"integer","options":{"0":"Nej, ingen særlige begivenheder","1":"Ja, men ikke 2","2":"Ja, klar sammenhæng /sammenfald","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.125","title":"Betydelig social begivenhed (f.eks. skilsmisse, fyring, dødsfald)","input":"integer","options":{"0":"Nej, ingen særlige begivenheder","1":"Ja, men ikke 2 2 Ja, klar sammenhæng /sammenfald","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.126","title":"Symptom- eller sygdomsattribution","description":"Hvad tror du årsagen er til din sygdom (eller symptomerne)?\n\n- Tror du at stress, belastninger eller din livssituation kunne være årsagen?\n- Er du fast overbevist om det eller er du i tvivl?\n- Hvorfor tror du det?.\n","input":"integer","options":{"0":"Usikker, har ikke teorier om årsagen, udelukker nødvendigvis ikke belastninger og stress","1":"Har tænkt over muligheden, udelukker nødvendigvis ikke belastninger og stress","2":"Er overbevist om 1 eller flere årsags sammenhænge (eksklusive psykosociale og stress)","3":"Har en udbygget teori og rigid opfattelse af årsagssammenhæng (eksklusive psykosociale og stress)?","4":"Psykotisk vrangforestilling","6":"Veldefineret somatisk lidelse","8":"Usikkert","9":"Ukendt"},"validate":["0-4",6,8,9],"episodes":1},{"key":"2.127","title":"Identifikation af lindrende eller forværrende faktorer","description":"Har du bemærket om der er noget der hjælper dig på din sygdom eller noget der gør den værre?\n\nKan du selv gøre noget for at få det bedre?\n\nKan patienten identificere faktorer der hjælper på generne eller faktorer der forværrer symptomerne eller sygdommen, eller disse er talrige og diffuse?\n","input":"integer","options":{"0":"Veldefinerede og få","1":"Lettere vanskeligheder ved at angive disse","2":"Store vanskeligheder eller talrige og diffuse","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9],"episodes":1},{"key":"2.128","title":"Psykisk udbygning af fysiske symptomer","description":"Legemlige symptomer, som oprindeligt skyldtes en konstateret fysisk sygdom eller tilstand, men som er forstærket eller forlænget på grund af psykosociale faktorer såsom bekymring for forværring eller kronisk udvikling, skuffelse over resultater af undersøgelse og behandling, mulighed for erstatning eller pension. cf. 27.069.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede","8":"Usikkert","9":"Ukendt"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.129","title":"Kun kvinder : [Vis et kort, hvis det hjælper] 2_129 Præmenstruelle symptomer gennem det sidste år","description":"Har du i tiden op til menstruationen haft symptomer, som forsvinder, når menstruationen begynder? Såsom:\nIrritabilitet/nedtrykthed Oppustethed eller vægtstigning Ømhed eller spænding i brysterne Muskelspændinger Hovedpine, nakke- og rygsmerter Koncentrationsbesvær Trang til at indtage usædvanlige føde-emner eller drikke\nBrug Rating Skala 1a\n","input":"string","validate":["0-3",5,6,8,9],"episodes":1},{"key":"2.130","title":"Sygdomsefterligninger (factitious disorder)","description":"Tilsigtet frembringelse eller efterligning af fysiske og psykiske symptomer, undertiden ved selvpåført skade, uden klar ydre motivation (som ved simulation) Check 27.068.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med fysiske men ikke psykiske symptomer","2":"Til stede med psykiske men ikke fysiske symptomer","3":"Til stede med både fysiske og psykiske symptomer","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.131","title":"Påvirkninger af aktiviteter pga. funktionelle sektion 2 symptomer","description":"Du har nævnt ( opsumér symptomer) igennem ( angiv tid ). Hvor meget har de nævnte symptomer forstyrret dine sædvanlige dagliglivsaktiviteter?\n","input":"integer","options":{"0":"Ingen sektion 2 symptomer","1":"Kun lille påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.132","title":"BEGYNDELSESALDER 2_132 Begyndelsesalder for aktuelle funktionelle tilstand","description":"(hvis patienten siger ''altid'', og dette ikke kan kommes nærmere rates 1)\n98 = 98 år eller ældre 99 = N/A : NK\n","input":"string","validate":["1-99"]},{"key":"2.133","title":"Begyndelsesalder for første funktionelle tilstand","description":"(hvis patienten siger ''altid'', og dette ikke kan kommes nærmere rates 1)\n98 = 98 år eller ældre 99 = N/A : NK\n","input":"string","validate":["1-99"]},{"key":"2.134","title":"Begyndelsesdato for PS eller PE DD/MD/ÅR","input":"date","fitb_size":"DATE_NORMAL"},{"key":"2.135","title":"Varighed af PS (dage)","input":"string"},{"key":"2.136","title":"Begyndelsestidspunkt for RE eller LB MD/ÅR","input":"date","fitb_size":"DATE_NODAY"},{"key":"2.137","title":"Varighed af RE eller LB (uger)","input":"string"},{"key":"2.138","title":"Aktuelle kontaktårsag til behandlersystemet","description":"(Forbeholdt kliniske undersøgelser) Hvad var årsagen til, at du i dette tilfælde henvendte dig til behandlersystemet?\n","input":"integer","options":{"0":"Har ikke aktuelt henvendt sig til behandlersystemet (befolkningsundersøgelser m.m.)","1":"Medicinsk helt eller delvist uforklarede symptomer (=funktionel lidelse) (Med overvejende sandsynlighed) en medicinsk fuldt ud forklarlig sygdom","3":"Helt overvejende emotionelle symptomer og gener","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1},{"key":"2.139","title":"Sammenspil mellem funktionelle symptomer og veldefineret somatisk sygdom","input":"integer","options":{"0":"Ikke tilstede eller kun symptomer af en type","1":"Funktionelle symptomer mest dominerende i sygdomsbilledet","2":"Funktionelle symptomer og somatisk sygdom er lige dominerende","3":"Somatisk sygdom mest dominerende i sygdomsbilledet","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1},{"key":"2.140","title":"Somatoform tilstand eller panikagtige angstanfald primær","input":"integer","options":{"0":"Ikke tilstede eller kun symptomer af en type","1":"Somatoforme symptomer mere udtalte eller startede først","2":"Somatoforme symptomer og panikanfald optræder sammen (komorbiditet)","3":"Panikanfalds symptomer mere udtalte eller startede først","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.141","title":"Somatoform tilstand eller depression primær","input":"integer","options":{"0":"Ikke tilstede eller kun symptomer af en type","1":"Somatoforme symptomer mere udtalte eller startede først","2":"Somatoforme symptomer og depression optræder sammen (komorbiditet)","3":"Depressionssymptomer mere udtalte eller startede først","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.142","title":"Somatoform tilstand eller anden psykisk sygdom primær","input":"integer","options":{"0":"Ikke tilstede eller kun symptomer af en type","1":"Somatoforme symptomer mere udtalte eller startede først","2":"Somatoforme symptomer og anden psykisk sygdom optræder sammen (komorbiditet)","3":"Symptomer fra anden psykiske sygdom mere udtalte eller startede først","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1},{"key":"2.143","title":"DISSOCIATIVE SYMPTOMER","description":"Symptomerne kan også være rated tidligere. Supplerende og uddybende items rates i 2.007-2.094 . 2_143 Dissociativ amnesi\nDu nævnte hukommelsessvigt. Kan du forklare lidt nærmere herom? Er det noget, der kommer og går, eller er det til stede hele tiden?\n","input":"integer","options":{"0":"Ingen hukommelsesproblemer eller kun almindelig glemsomhed","1":"Partiel og vekslende amnesi"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.144","title":"Amnesi forbundet med traume eller belastning","input":"integer","options":{"0":"Ingen hukommelsesproblemer","1":"Partiel og fluktuerende amnesi forbundet med aktuelt traume eller belastning"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.145","title":"Dissociativ fugue","description":"Har du nogensinde befundet dig et usædvanligt sted, uden at du var i stand til at huske, hvordan du var kommet der?\n- Optrådte det i forbindelse med en særlig oplevelse eller belastning?\n","input":"integer","options":{"0":"Ingen mistanke om dissociativ fugue","1":"Evidens for dissociativ fugue med amnesi"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.146","title":"Dissociativ stupor","description":"Du nævnte, at du havde haft tilfælde eller anfald med bevidsthedstab. Kan du fortælle mere om det?\n- Lå du ubevægelig hen uden at reagere i længere tid?\nPersonale og pårørende kan oplyse om nedsat eller manglende muskelbevægelser, talen, reaktioner på lys eller støj, men med normal muskeltonus, åndedræt og øjenbevægelser.\n","input":"integer","options":{"0":"Ingen mistanke om dissociativ stupor","1":"Evidens for dissociativ stupor"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.147","title":"Items 2.147-2.153 rates:","input":"integer","options":{"0":"Fraværende","1":"Til stede 2_147 Tranceoplevelser Har du oplevet at være i en tilstand med ændret bevidsthed eller en trance-agtig tilstand? - Følte du, at du ikke længere var dig selv? - Var din opfattelse af omgivelserne mangelfuld? - Var din opmærksomhed fæstnet til noget helt bestemt? - Gentog du de samme bevægelser eller ord hele tiden? - Var du ligesom fastfrosset i en bestemt stilling? - Var det ubehageligt? -Ønskede du, at det skulle ske? Hvis tranceoplevelsen initialt var tilsigtet - Fortsatte tilstanden længere, end du havde ønsket? - Skete det i en særlig sammenhæng, fx af religiøs karakter?"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.148","title":"Besættelsesoplevelse","description":"Har du haft en oplevelse af at være besat af en særlig kraft eller vilje?\n- Var det en ånd, en guddom eller en anden person? - Kan du beskrive det nærmere? - Følte du dig som en anden? -Ønskede du, eller var du indstillet på, at det skulle ske? - Var det ubehageligt?\nHvis besættelsesoplevelsen initialt var tilsigtet\n- Fortsatte det længere, end du ønskede det? - Optrådte det i særlig sammenhæng eller forbindelse, fx af religiøs karakter? - Har du haft besættelsesoplevelser uden at komme i en trancetilstand?\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.149","title":"Besættelsesoplevelser forbundet med trance","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.150","title":"Dissociative kramper","description":"Har du haft anfald med trækninger i musklerne?\n- Hvordan viste det sig? - Var du helt bevidstløs? - Kom du til skade ved at falde eller bide dig i tungen? - Kom du til at lade vandet eller få afføring i forbindelse med dette?\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.151","title":"Dissociative sanseforstyrrelser Har du oplevet tab af syn, hørelse, lugt eller berøringssans?","description":"Kan du føle varme og kulde, som du plejer?\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.152","title":"Dissociative bevægelsesforstyrrelser","description":"Har du været ude af stand til at bevæge en hånd, en arm eller et ben? Har dine bevægelser været usikre eller klodsede?\nHar du haft svært ved at stå eller gå uden støtte?\n- Har du haft svært ved at tale eller synke? - Prøv at fortælle lidt nærmere om det\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.153","title":"Dissociative symptomer forbundet med traumer eller belastning","description":"Da du oplevede (symptom), havde du da været udsat for en rystende oplevelse eller belastning?\n- Havde du personlige problemer på det tidspunkt?\n(fx traumatiske begivenheder, konfliktforhold, uløselige problemer)\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.154","title":"Checkliste for multipel personlighed","description":"2.154-2.157 rates\n","input":"integer","options":{"0":"Fraværende","1":"Til stede 2_154 To eller flere adskilte personligheder"},"validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.155","title":"Hver personlighed komplet","description":"(med egne erindringer, præferencer og adfærdsmønstre)\n","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.156","title":"Hver personlighed fremtræder i adskilte perioder","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.157","title":"Amnesi for væsentlige personlige forhold","input":"string","validate":[0,1],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.158","title":"Checkliste for andre dissociative tilstande 2_158 Andre dissociative tilstande","input":"integer","options":{"0":"Fraværende","1":"Forbigående amnesi eller trancetilstand i adolescensen","2":"Ganser's syndrom","3":"Multipel personlighed med dårlig differentiering","4":"Depersonalisations-derealisations-syndrom","5":"Kulturspecifikke dissociative tilstande","6":"Andre, specificeres ______________________________","8":"Usikkert","9":"Ukendt"},"validate":["0-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1},{"key":"2.159","title":"Begyndelsesalder for aktuelle dissociative tilstand","input":"string"},{"key":"2.160","title":"Begyndelsesalder for første dissociative tilstand","input":"string"},{"key":"2.161","title":"Observeret adfærd 2_161 Egodyston eller egosynton","description":"Egodyston : Patienten er helt eller delvis tilbøjeligheden til at betegne symptomer eller sygdomsbekymring som fremmed, uvelkommen, plagsom eller ''fremmed for min natur'' .\nEgosynton : Patienten kan føle sine symptomer eller sygdomsbekymring som en integreret del af sig selv og ikke som en kognitiv tilbøjelighed, uanset om patienten er i tvivl om han har en sygdom eller ej.\n","input":"integer","options":{"0":"Ingen funktionel lidelse","1":"Den funktionelle lidelse føles overvejende egodyston","2":"Den funktionelle lidelse blandet egodyston og egosynton","3":"Den funktionelle lidelse føles overvejende egosynton","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9]},{"key":"2.162","title":"Emotionel diskrepans (Belle indifférence)","description":"Patientens fremstilling af sin tilstand viser inadækvat affekt i forhold til sværhedsgraden af symptomerne og sygdommen, som patienten plages af. Eksempelvis kan patienten smile med en underfundig resignation trods svær grad af smerte eller funktionsindskrænkning, og patienten kan synes følelsesmæssigt upåvirket af sin tilstand .\n","input":"integer","options":{"0":"Ingen diskrepans","1":"Til stede i ubetydelig eller let grad","2":"Definitivt til stede og sammen med funktionelle symptomer","6":"Definitivt til stede, men forårsaget af organisk lidelse, fx dissemineret sklerose","8":"Usikkert","9":"Ukendt"},"validate":["0-2",6,8,9]},{"key":"2.163","title":"Diskrepans mellem subjektive klager og objektiv adfærd","description":"Patientens fremstilling af sine symptomer står i kontrast til den adræthed og mimik, som observeres under interviewet. Eksempelvis kan patienten tvangsfrit flytte sig rundt i stolen trods klager over svære rygsmerter eller give en detaljeret anamnese trods klager over svær hukommelsesforstyrrelse.\n","input":"integer","options":{"0":"Ingen diskrepans","1":"Til stede i ubetydelig eller let grad","2":"Definitivt til stede og i betydelig grad","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.164","title":"Psykosocial kommunikationsform","description":"Patienten er mere optaget af at kommunikere den funktionsnedsættelse, graden af ubehag og indskrænkning i livsførelse som sygdommen eller symptomerne forårsager, end i hvilken betydning det har for helbredet.\n","input":"integer","options":{"0":"Ingen psykosocial kommunikationsform","1":"Til stede i let eller ubetydelig grad","2":"Betydelig grad af psykosocial kommunikationsform","8":"Usikkert 9 Ukendt"},"validate":["0-2",8,9]},{"key":"2.165","title":"Mangel på kronologi i symptom og sygehisorie","description":"Interviewet bærer præg af at patienten har vanskeligheder med kronologien af symptomerne, sygdom, undersøgelser, indlæggelser m.m. Det kan feks. være vanskeligt at finde ud af, om patienten taler om symptomer, der har været tilstede på et tidligere tidspunkt eller det er symptomer som han/hun aktuelt er plaget af, eller om det er en undersøgelse, der er lavet for nylig eller for længe siden. Må vurderes i forhold til om problemet ligger ud over, hvad der kan forventes hos enhver person, der har haft et langvarigt og kompliceret sygdomsforløb.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i let grad","2":"Udtalte","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.166","title":"Vag, diffus symptom-beskrivelse og anamnese","description":"Symptom klager bliver beskrevet i vage vendinger, er diffuse, måske atypiske/ukarakteristiske og skiftende igennem interviewet. Interviewet bærer præg af, at patienten har vanskeligheder ved at svare på uddybende spørgsmål og at specificere sine klager (lokalisering, intensitet, kvaliteten (fx om en smerte er brændende eller stikkende). Patienten kan i nogle tilfælde ligefrem blive irriteret på intervieweren som holder fast i at få beskrevet symptomernes lokalisation, deres intensitet, og periodicitet mv.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i let grad","2":"Udtalte","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.167","title":"Vag, diffus beskrivelse af tidligere behandlinger","description":"Patienten har vanskeligt ved at beskrive hvilke behandlinger han/hun har gennemgået og effekten er vagt eller usikkert beskrevet. Kan sammenblande diagnostiske procedurer og behandling. Er kritisk overfor tidligere behandlinger og giver ofte udtryk for at behandlingerne har medført en forværring i tilstanden.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i let grad","2":"Udtalte","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.168","title":"Affektiv beskrive-måde","description":"Symptom-klager, sygdom, indlæggelser, undersøgelser mv. bliver beskrevet i malende vendinger med tilbøjelighed til dramatisering og overdrivelser.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i let grad","2":"Udtalte","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.169","title":"Inkonsistens i oplysningerne","description":"Patienten giver forskellige og modstridende oplysninger igennem interviewet eller der er diskrepans mellem oplysninger fra andre kilder (journaler, henvisende læge, socialforvaltningen, familien mv) og patientens egne oplysninger igennem interviewet.\n","input":"integer","options":{"0":"Ingen inkonsistens","1":"Nogen inkonsistens","2":"Udtalt inkonsistens","8":"Usikkert","9":"Ukendt"},"validate":["0-2",8,9]},{"key":"2.170","title":"Tidligere sygdomsforløb og behandlinger (ud fra journaloplysninger mv)","description":"Rates ud fra en gennemgang af pt. journal og lægelige oplysninger mv.\n","input":"integer","options":{"0":"Ingen eller kun kontakter til egen læge og ikke 1","1":"hyppige kontakter primært til egen læge uden sikker somatisk forklaring","2":"Gentagne kontakter til hospitaler og speciallæger uden sikker somatisk forklaring","3":"Mange og hyppige kontakter over en årrække til hospitaler og speciallæger mv. uden sikker somatisk forklaring","8":"Usikkert","9":"Ukendt"},"validate":["0-3",8,9],"episodes":1},{"key":"3.001.section","title":"Bekymring, anspændthed og andre uspecifikke symptomer"},{"key":"3.001","title":"Bekymringstendens","description":"- Har du haft mange bekymringer i (perioden)?\n- Hvordan føles det, når du bekymrer dig?\n- Kører ubehagelige tanker rundt i hovedet hele tiden?\n- Bekymrer du dig mere end nødvendigt, problemet taget i betragtning?\n- Hvad sker der, hvis du prøver at tænke på noget andet?\n- Kan du holde op med at spekulere, hvis du ser TV, læser eller tænker på noget rart?\nRating scale I\n","input":"string","section_start":"Section_3","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.001,"ratingscale":1},{"key":"3.002","title":"Følelse af nervøs spænding Har du ofte følt dig anspændt, presset, med nerverne på højkant?","description":"- Hvordan føles det?\n- Hvor udtalt er det?\n- Føles dagligdagsproblemer overvældende?\n- Er du tilbøjelig til at fare sammen?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.002,"ratingscale":1},{"key":"3.003","title":"Generel muskelspænding Har du svært ved at slappe af i (perioden)?","description":"? Føles musklerne spændte? ? Hvor føler du det? ? Letter det at slappe af?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.003,"ratingscale":1},{"key":"3.004","title":"Fravær af nervøse symptomer i forbindelse med problemer Rates kun hvis 3.001 - 3.003 alle rates 0.","description":"Er du tværtimod mere rolig og afslappet end de fleste, mindre tilbøjelig til at blive nervøs, rastløs, irritabel end andre?\n","input":"integer","options":{"1":"Mindre tilbøjelig til sektion 3 symptomer end de fleste","2":"Som gennemsnittet","3":"Mere nervøs end gennemsnittet","4":"Udtalt nervøs CUT OFF => til 4.001 hvis 3.001 - 3.003 alle rates 0 og 3.004 rates 1 eller 2"},"validate":["1-4",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":3.004},{"key":"3.005","title":"Lokaliserede spændingssmerter","description":"Har du ondt eller smerter nogen steder, fx hovedpine, smerter i nakken eller ryggen, ømme muskler?\n? Hvordan føles det?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.005,"ratingscale":1},{"key":"3.006","title":"Subjektivt beskrevet rastløshed","description":"Har du følt dig så urolig og rastløs, at du ikke kunne sidde stille?\n? Er du nødt til at trave op og ned af gulvet?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.006,"ratingscale":1},{"key":"3.007","title":"Øget trætbarhed og udmattelse","description":"Har du følt dig udmattet og udkørt op ad dagen, selvom du ikke har arbejdet særlig hårdt?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.007,"ratingscale":1},{"key":"3.008","title":"Støjoverfølsomhed","description":"Generer støj dig voldsomt?\n? Er det som om støj, som andre kan affinde sig med, borer sig ind i eller trænger igennem dit hoved?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":3.008,"ratingscale":1},{"key":"3.009","title":"Irritabilitet","description":"Har du følt dig mere irritabel eller utålmodig end sædvanligt i (perioden)?\n? Hvordan har det vist sig? ? Kan du holde det for dig selv, eller hæver du stemmen eller farer op? ? Har du faktisk mistet selvbeherskelsen eller forløbet?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.009},{"key":"3.010","title":"Selvhenførende tanker eller fornemmelser","description":"Er du meget følsom, når du er sammen med andre?\n? Er du hele tiden opmærksom på dine egne fornemmelser og reaktioner, på hvad andre tænker eller mener om dig?\n? Har du en fornemmelse af, at andre lægger specielt mærke til dig, fx på gaden eller på en restaurant?\n? Er det tilfældet, eller er det bare en fornemmelse du har?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":3.01,"ratingscale":1},{"key":"3.011","title":"Mistroiskhed","description":"Har du været mere mistroisk end sædvanligt?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.011,"ratingscale":1},{"key":"3.012","title":"Depersonalisations-derealisations-oplevelser","description":"Har du følt at omgivelserne eller du selv var uvirkelige, ligesom ikke rigtig til stede?\nCf. 16.006-16.009 samt 4.026.\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.012,"ratingscale":1},{"key":"3.013","title":"Jalousi-tendens (ikke paranoid)","description":"Er du tilbøjelig til at føle dig jaloux?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":3.013,"ratingscale":1},{"key":"3.014A","title":"Tidsangivelse for sektion 3 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.114.\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":3.014},{"key":"3.014B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.014},{"key":"3.014C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":3.014},{"key":"3.014D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.014},{"key":"3.015","title":"Påvirkning af aktiviteter på grund af sektion 3 symptomer","description":"Hvor meget har de nævnte symptomer påvirket dine dagligdagsaktiviteter?\n","input":"integer","options":{"0":"Ingen sektion 3 symptomer i betydende grad","1":"Symptomer til stede men ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":3.015},{"key":"3.016","title":"Organisk årsag til sektion 3 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":3.016},{"key":"3.017","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":3.017},{"key":"4.001.section","title":"Angst og fobier"},{"key":"4.001","title":"Screening for angst","description":"Har du haft angstfornemmelser eller angstanfald, med hjertebanken, rysten, sveden eller tørhedsfornemmelser i munden?\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"section_start":"Section_4","validate":[0,1,8,9],"episodes":1,"attributional":1,"glossary":4.001},{"key":"4.002","title":"Screening for fobier","description":"Har du haft angstfornemmelser eller angstanfald i særlige situationer, som du derfor har prøvet at undgå, fx at komme i højden, køre i bus, stå i kø, køre med elevator?\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.002},{"key":"4.003.section","title":"Angstsymptomer","description":"Jeg vil spørge dig om en række symptomer, som man kan have i forbindelse med angst eller panikanfald.\nGennemgå 4.003-4.019 (autonome items er markeret med +). Afgør først om de er til stede og brug herefter følgende ratings:\n  0. Ikke til stede\n  1. Symptomerne til stede men ikke samtidigt\n  2. Symptomerne til stede samtidigt (fx ved panikanfald)\n  3. Til stede både samtidigt og til forskellig tid\n"},{"key":"4.003","title":"Vejrtrækningsbesvær","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.004","title":"+ Hjertebanken, ekstraslag, hurtig puls","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.005","title":"Svimmelhed, ørhed i hovedet, besvimelsesfornemmelse","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.006","title":"Prikken eller dødhedsfornemmelse i ansigt eller fingre","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.007","title":"Trykken eller smerter i brystet","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.008","title":"+ Mundtørhed som ikke skyldes medicin eller dehydrering","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.009","title":"Synkebesvær, følelse af klump i halsen","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.010","title":"+ Sveden, fx i hænderne","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.011","title":"+ Rysten eller sitren, fx af hænder, arme eller ben","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.012","title":"Varm- eller koldsveden, hedeture","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.013","title":"Uvirkelighedsfølelse","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.014","title":"Sommerfuglefornemmelse, køren i maven, kvalme","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.015","title":"Angst for at dø","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.016","title":"Kvælningsfornemmelse","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.017","title":"Angst for at blive sindssyg eller miste besindelsen","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.018","title":"Frygtsomhed eller sammenfaringstendens","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.019","title":"Andre angstsymptomer, fx øget vandladningshyppighed","input":"integer","options":{"0":"Ikke til stede","1":"Symptomerne til stede men ikke samtidigt","2":"Symptomerne til stede samtidigt (fx ved panikanfald)","3":"Til stede både samtidigt og til forskellig tid CUT OFF => til 5.001 hvis 4.001 - 4.019 alle rates 0. SKIP => til 4.027 hvis der ikke har været angstsymptomer i perioden på grund af undgåelsesadfærd."},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.003},{"key":"4.020","title":"Antal panikanfald i en 4 ugers periode","description":"Har du haft pludselige anfald med angst eller panik med de nævnte angstsymptomer, som i løbet af kort tid føltes uudholdelige?\n? Kan du beskrive et typisk anfald? ? Hvor ofte har du haft sådanne anfald inden for den sidste 4 ugers\nperiode (eller anden 4 ugers periode)?\n? Optrådte de kun i særlige situationer? ? Optrådte de uventet som lyn fra en klar himmel uden forbindelse med særlige\nsituationer?\n00 Ingen anfald\n40 40 anfald eller mere 55 Vanskelig at rate pga. psykotiske symptomer 88 Uvist 99 Ikke kendt SKIP => til 4.023 hvis der ikke optræder panikanfald med autonome symptomer.\n","input":"string","validate":["0-40",55,88],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.02},{"key":"4.021","title":"Vedvarende frygt for nye panikanfald","description":"Har du følt dig bange for nye panikanfald? Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.021,"ratingscale":1},{"key":"4.022","title":"Handling for at forhindre eller standse panikanfald","description":"Har du været nødt til at gøre et eller andet for at undgå et anfald eller for at få anfaldet til at holde op, fx tilkalde hjælp, åbne et vindue, løbe ud af huset?\n","input":"integer","options":{"0":"Ingen","1":"Handling for at forhindre et anfald","2":"Handling for at standse et anfald","3":"Handling for både at forhindre og standse et anfald"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.022},{"key":"4.023","title":"Fritflydende autonom angst","description":"Har du haft mere eller mindre udtalt angst det meste af tiden, ikke kun i anfald, i løbet af perioden?\n? Er du ind i mellem helt fri for angstsymptomer? ? Hvor ofte optræder de? ? Hvor svære er de? Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.023},{"key":"4.024","title":"Ængstelig forudanelse med autonome symptomer","description":"Har du haft fornemmelse af, at noget frygteligt kunne ske, fx sygdom, ulykke eller dødsfald for dig selv eller dine nærmeste? Følte du angst med hjertebanken, sveden eller rysten i forbindelse hermed?\nCF. 6.022. Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":4.024},{"key":"4.025","title":"Varighed af fritflydende angst (mdr.)","description":"81 Mere end 80 88 Uvist 99 Ikke kendt\n","input":"string","validate":["0-81",88,99],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.025},{"key":"4.026","title":"Depersonalisation eller derealisation i forbindelse med angst","description":"Har du følt at omgivelserne eller du selv var uvirkelige, ligesom ikke rigtig til stede?\nCf. 3.012 og 16.006-16.009. Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.026,"ratingscale":1},{"key":"4.027","title":"FOBIER","description":"Checkliste for fobier 4.027-4.045.\nRates for situationer med generende angst eller undgåelsesadfærd, som patienten erkender er overdrevne eller urimelige. Angstsymptomerne kan i undersøgesesperioden være afløst af undgåelsesadfærd, men skal have forekommet tidligere, på et eller andet tidspunkt siden lidelsens begyndelse. AGORAFOBIER\n4_027 Angst for offentlige steder, åbne pladser, tomme gader\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.028","title":"Angst for menneskemængder, forretninger, stormagasiner, biografer,","description":"hvor man ikke let kan komme ud\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.029","title":"Angst for at tage alene med bus, tog eller fly","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.030","title":"Angst for at færdes borte fra hjemmet","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.031","title":"Angst for at falde om eller bryde sammen, når man er alene og uden","description":"hjælp i nærheden\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.032","title":"Angst for at være alene","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":4.027},{"key":"4.033","title":"SOCIALFOBIER","description":"4_033 Angst for at være genstand for opmærksomhed eller for at opføre sig pinligt i forbindelse med at spise, drikke eller skrive i andres påsyn, benytte offentlige toiletter\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033},{"key":"4.034","title":"Angst for at være genstand for opmærksomhed eller for at opføre sig pinligt, i forbindelse med at tale til eller være sammen med en mindre gruppe mennesker, man kender","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033},{"key":"4.035","title":"Angst for at være genstand for opmærksomhed eller for at opføre sig pinligt, i forbindelse med at tale til en større gruppe mennesker, i offentlige forsamlinger, til fremmede eller i telefon","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033},{"key":"4.036","title":"Angst for at rødme eller ryste, for at komme til at kaste op, skulle lade","description":"vandet eller have afføring i forbindelse med de almindelige angstsymptomer i de frygtede situationer (4.033-4.035).\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033},{"key":"4.037","title":"ENKELFOBIER - FOBISK ANGST I SÆRLIGE SITUATIONER","description":"4_037 Angst for uvejr, torden og lynild\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.038","title":"Angst for vand - damme, søer, havet, kar- eller brusebad","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.039","title":"Angst for dyr: insekter, slanger, fugle, rotter, mus","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.040","title":"Angst for lukkede rum - elevatorer, tunneler, telefonbokse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.041","title":"Angst for at flyve","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.042","title":"Angst for højder - broer, balkoner, vinduer i høje bygninger, trapper","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.043","title":"Angst for situationer i forbindelse med sygdom og død: blod, indsprøjtninger,","description":"tandlægebehandling, hospitalsbesøg\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.044","title":"Angst for at pådrage sig sygdomme, fx kønssygdomme, AIDS, cancer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere)"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.045","title":"Angst for andre specifikke situationer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede med følelse af nervøs spænding eller angstsymptomer uden autonome symptomer","2":"Til stede med autonome angstsymptomer (nu eller tidligere) SKIP => til 4.059 hvis der ikke er fundet tegn på fobisk angst."},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.037},{"key":"4.046","title":"Rating for sværeste agorafobi","description":"Hvilken var den sværeste af de nævnte situationer med (agorafobi)?\n? Hvor generende eller invaliderende var det? ? Hvor meget af tiden har det været til stede?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027,"ratingscale":1},{"key":"4.047","title":"Undgåelsesadfærd i forbindelse med sværeste agorafobi","description":"Har du prøvet at undgå den omtalte (agorafobiske) situation?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.027,"ratingscale":1},{"key":"4.048","title":"Påvirkning af aktiviteter på grund af sværeste agorafobi","description":"Hvor meget har det påvirket dine dagligdagsaktiviteter?\n","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":4.027},{"key":"4.049","title":"Sværeste socialfobi","description":"Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033,"ratingscale":1},{"key":"4.050","title":"Undgåelsesadfærd i forbindelse med sværeste socialfobi","description":"Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.033,"ratingscale":1},{"key":"4.051","title":"Påvirkning af aktiviteter på grund af sværeste socialfobi","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":4.033},{"key":"4.052","title":"Sværeste enkelfobi","description":"Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.051,"ratingscale":1},{"key":"4.053","title":"Undgåelsesadfærd i forbindelse med sværeste enkelfobi","description":"Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.053,"ratingscale":1},{"key":"4.054","title":"Påvirkning af aktiviteter på grund af sværeste enkelfobi","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":4.054},{"key":"4.055","title":"Forhold mellem panikanfald og fobier","description":"Optrådte panikanfaldene altid kun i forbindelse med en af disse fobier, eller kunne de også optræde uventet og uden grund?\n","input":"integer","options":{"0":"Ingen fobier og ingen panikanfald","1":"Kun fobier (uden panikanfald)","2":"Panikanfald ved fobier (fobisk udløst)","3":"Panikanfald uden fobier","4":"Panikanfald både ved fobier og alene"},"validate":["0-4",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.055},{"key":"4.056","title":"Begyndelsesalder ved panikanfald eller fobier","description":"Hvor gammel var du da panikanfaldene/fobierne først blev rigtig generende, eller begrænsede dine aktiviteter?\n","input":"string","validate":["0-99"],"glossary":4.056},{"key":"4.057","title":"Fobier begrænset til eller begyndt efter en skizofren episode","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.057},{"key":"4.058","title":"Fobier begrænset til eller begyndt efter en affektiv episode","input":"integer","options":{"0":"Nej","1":"Ja Cf. 6.022."},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.058},{"key":"4.059A","title":"Tidsangivelse for periode med sektion 4 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014.\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","episodes":1,"glossary":4.059},{"key":"4.059B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"used_in_dsmiv":1,"glossary":4.059},{"key":"4.059C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":4.059},{"key":"4.059D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"used_in_dsmiv":1,"glossary":4.059},{"key":"4.060","title":"Påvirkning af aktiviteter på grund af sektion 4 symptomer Hvor meget har de nævnte symptomer påvirket dine dagligdagsaktiviteter ?","input":"integer","options":{"0":"Ingen sektion 4 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":4.06},{"key":"4.061","title":"Organisk årsag til sektion 4 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.061},{"key":"4.062","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":4.062},{"key":"5.001","title":"Tvangssymptomer","description":"5_001 Screening for tvangssymptomer\n","input":"integer","options":{"0":"Ingen tegn på tvangssymptomer","1":"Tegn på tvangssymptomer Nogle mennesker finder det nødvendigt at kontrollere flere gange hvad de har gjort, for at føle sig sikre på, at det er gjort rigtig, fx i forbindelse med dørlåse, elektriske afbrydere, vandhaner og lignende . Kender du det fra dig selv? Andre er nødt til hele tiden at holde alting i en særlig orden eller symmetri, gælder det for dig? Der er mennesker, der må vaske hænder eller tage bad mange gange dagligt af frygt for smuds eller smitte. Hvordan er det for dig? Nogle mennesker har besvær med ubehagelige ord eller tanker, der hele tiden bryder ind i deres bevidsthed. Kender du til noget sådant? Hvis der er tegn på eller mistanke om tvangssymptomer, kan yderligere spørges: ? Kan du beskrive det nærmere? ? Hvor kommer dine tanker eller tilskyndelser fra? ? Har du prøvet at modstå dem, fx at lade være med ...........? - Hvad sker der så? ? Føler du dig foruroliget eller generet? ? Hvor slemt har det været? ? Hvor længe har det stået på i løbet af perioden? 14 dage eller mere? Hver dag? CUT OFF => til 6.001 hvis der ikke er tegn på sektion 5 symptomer Rate items 5.002-5.006 efter Rating Skala I, men undlad at anvende ''1'' som ikke er relevant for tvangssymptomer."},"section_start":"Section_5","validate":[0,1,5,8,9],"glossary":5.001},{"key":"5.002","title":"Kontrollerings- og gentagelses-tvang Brug Rating Skala I, men undlad at anvende ''1''","input":"string","validate":[0,2,3,5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.002,"ratingscale":1},{"key":"5.003","title":"Tvangshandlinger forbundet med orden og symmetri Brug Rating Skala I, men undlad at anvende ''1''","input":"string","validate":[0,2,3,5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.003,"ratingscale":1},{"key":"5.004","title":"Tvangstanker om at komme til skade eller gøre skade på sig selv eller andre Brug Rating Skala I, men undlad at anvende ''1''","input":"string","validate":[0,2,3,5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.004,"ratingscale":1},{"key":"5.005","title":"Tvangshandlinger forbundet med renlighed, smuds- og smitte-frygt Brug Rating Skala I, men undlad at anvende ''1''","input":"string","validate":[0,2,3,5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.005,"ratingscale":1},{"key":"5.006","title":"Tvangspræget tvivlen, grublen eller følelse af manglende fuldførelse (ikke at have gjort det færdigt, man har udført) Brug Rating Skala I, men undlad at anvende ''1''","input":"string","validate":[0,2,3,5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.006},{"key":"5.007","title":"Svækket bevidst modstand mod tvangssymptomer","input":"integer","options":{"0":"Ingen svækkelse af modstanden (dvs. modstanden består)","1":"Modstand mod et eller flere symptomer (men ikke alle)","2":"Ophør af bevidst modstand"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.007},{"key":"5.008","title":"Forholdet mellem angst- og tvangssymptomer","input":"integer","options":{"0":"Ingen angstsymptomer, eller angst og tvangssymptomer lige fremtrædende","1":"Angstsymptomer mest fremtrædende","2":"Tvangssymptomerne mest fremtrædende"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"glossary":5.008},{"key":"5.009","title":"Forholdet mellem depressive og obsessive-kompulsive symptomer","input":"integer","options":{"0":"Den ene eller begge symptomtyper fraværende","1":"Depressive symptomer mest udtalte eller begyndte først","2":"Depressive og obsessive symptomer optræder samtidigt og er lige udtalte","3":"Obsessive symptomer mest udtalte eller begyndte først"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"glossary":5.009},{"key":"5.010","title":"Forholdet mellem obsessive og kompulsive symptomer","input":"integer","options":{"0":"Kun en type symptomer til stede","1":"Obsessive symptomer klinisk mest betydende","2":"Kompulsive symptomer mest betydende","8":"Begge type lige betydende, eller usikkert"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.01},{"key":"5.011","title":"Indsigt i tvangssymptomernes urimelighed","input":"integer","options":{"0":"Ingen tvangssymptomer","1":"Patienten erkender altid tvangssymptomerne som urimelige eller overdrevne","2":"Patienten erkender kun delvis eller ikke hele tiden, at angstsymptomerne er urimelige eller overdrevne","3":"Patienten erkender det meste af tiden ikke, at tvangssymptomerne er urimelige eller overdrevne"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.011},{"key":"5.012","title":"Tvangssymptomernes emne og indhold begrænset til anden psykisk lidelse","input":"integer","options":{"0":"Ingen symptomer eller emnet ikke begrænset til anden lidelse","1":"Emnet delvist begrænset til anden lidelse","2":"Emnet udelukkende begrænset til anden lidelse, fx tvangspræget depressiv grublen"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.012},{"key":"5.013","title":"Begyndelsesalder for tvangssymptomer","input":"string","validate":["0-99"],"glossary":5.013},{"key":"5.014A","title":"Tidsangivelse for periode med sektion 5 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","episodes":1,"glossary":5.014},{"key":"5.014B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.014},{"key":"5.014C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":5.014},{"key":"5.014D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.014},{"key":"5.015","title":"Påvirkning af aktiviteter på grund af sektion 5 symptomer","input":"integer","options":{"0":"Ingen sektion 5 symptomer","1":"Kun let påvirkning (op til 1 time om dagen)","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":5.015},{"key":"5.016","title":"Organisk årsag til sektion 5 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":5.016},{"key":"5.017","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_dsmiv":1,"glossary":5.017},{"key":"6.001","title":"Depressivt stemningsleje og tankeindhold","description":"6_001 Nedtrykthed\nHar du følt dig nedtrykt i perioden?\n? Var du trist, i dårligt humør, ked af det, tungsindig, langt nede eller dybt fortvivlet?\n? Hvor meget af tiden følte du dig nedtrykt? Var det dagligt hver dag i 14 dage eller mere? ? Hvor længe er det siden dit humør var normalt?\nRates efter subjektiv beskrivelse af humøret, da det var værst. Objektivt iagttagelig nedtrykthed rates i 23.001.\n","input":"string","section_start":"Section_6","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.001,"ratingscale":1},{"key":"6.002","title":"Maskeret depression","description":"Rates hvis 6.001 er rated 0 eller 1, hvis man har indtryk af forsænket stemningsleje, trods manglende evne til at give sprogligt udtryk for nedtrykthed (alexithymi), egnsbetinget tendens til underdrivelse, kulturbetinget tilbøjelighed til at opretholde normal eller smilende facade udadtil, fremtrædende irritabilitet, angst eller somatoforme symptomer, eller kognitiv svækkelse.\n","input":"integer","options":{"1":"Mistanke om nedtrykt stemningsleje","2":"Nedtrykt stemningsleje skønnes til stede bag maskeringen"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"glossary":6.002,"ratingscale":1},{"key":"6.003","title":"Lethed til tårer, grådtendens","description":"Kommer du let til at græde?\n? Har du grædeture? ? Hvor ofte, hvor længe, hvorfor? Brug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":6.003,"ratingscale":1},{"key":"6.004","title":"Anhedoni - manglende evne til at føle glæde","description":"Har du haft svært ved at glæde dig over noget? (Gåture, samvær med venner, beskæftigelse med hobby eller fritidsinteresser, at vinde i sport, eller modtage en kompliment)\nHar du tabt lysten til det, du plejer at være glad for? - Til det hele?\n? Hvor meget af tiden i perioden har du haft det sådan? ? Hvornår har du sidst nydt eller kunnet glæde dig over noget? ? Kan man se det eller mærke det på dig?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.004,"ratingscale":1},{"key":"6.005","title":"Varighed af nedtrykthed eller anhedoni (uger)","description":"Rates i antal uger op til 80 (da 88 = uvist hvor længe) Andre depresssive symptomer (6.006-6.017) (kan forekomme ved andre lidelser end depressionstilstand)\n","input":"string","validate":["0-80",88],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.005},{"key":"6.006","title":"Håbløshedsfølelse","description":"Hvordan ser det ud for dig fremover?\n? Kan du se lyspunkter eller lys forude? Forekommer det hele håbløst? ? Hvor længe har du følt det sådan? ? Hvor længe er det siden, du så anderledes på det? Hvad tænkte du da?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":6.006,"ratingscale":1},{"key":"6.007","title":"Fornemmelse af tab af følelser","description":"Somme tider opleves det som om man har tabt evnen til at føle noget som helst, man kan måske ikke engang føle sig ked af det eller græde. Har du haft det sådan?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":6.007,"ratingscale":1},{"key":"6.008","title":"Tab af påvirkelighed (reaktivitet)","description":"Føler du dig mere eller mindre nedtrykt hele tiden? Svinger dit humør med forholdene, så du liver op eller ikke føler dig nedtrykt, fx hvis du får besøg eller bliver optaget af noget?\n","input":"integer","options":{"0":"Ingen nedtrykthed","1":"Nedtryktheden varierer med omstændighederne","2":"Nedtryktheden varierer, men ikke med omstændighederne","3":"Nedtryktheden er mere eller mindre til stede hele tiden"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.008,"ratingscale":1},{"key":"6.009","title":"Morgenforværring","description":"Hvornår i dagens løb føles nedtryktheden værst?\n? Er der noget tidspunkt, hvor den føles mindre udtalt?\n","input":"integer","options":{"0":"Ingen nedtrykthed eller nedtryktheden ikke værst om morgenen","1":"Nedtrykthed værst om morgenen eller først på dagen"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.009},{"key":"6.010","title":"Optagethed af tanker om død og ulykke","description":"Tænker du usædvanligt meget på døden? Spekulerer du meget på ulykker, sygdom eller ruinering, som kan ramme dig eller dine nærmeste?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.01,"ratingscale":1},{"key":"6.011","title":"Selvmordstanker eller tanker om at skade sig selv","description":"Har du haft tanker om selv at gøre en ende på det hele? Har du planlagt hvordan eller prøvet at gøre det? Har du haft tanker om at skade dig selv på anden måde?\nRates uden hensyn til, om tankerne skyldes depressionstilstand eller ikke.\n","input":"integer","options":{"0":"Ikke til stede","1":"Påtrængende tanker eller overvejelser (planer) om selvmord eller selvskadende handlinger, men intet forsøg","2":"Selvmordsforsøg eller selvskadende handling, uden alvorlig skade","3":"Selvmordsforsøg eller selvskadende handling, med udtalte følger eller skade","4":"Alvorligt selvmordsforsøg med overbevisende selvmordshensigt"},"validate":["0-4",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.011},{"key":"6.012","title":"Livslede - tedium vitae","description":"Har du følt, at livet ikke var værd at leve? At du ikke ville have noget imod det, hvis du ikke vågnede op næste morgen? Kunne du ligefrem ønske, at en alvorlig sygdom eller et ulykkestilfælde ville gøre\nen ende på det hele?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.012,"ratingscale":1},{"key":"6.013","title":"Patologisk skyldfølelse","description":"Har du været tilbøjelig til at bebrejde dig selv noget, du har gjort eller tænkt? Har du følt dig skyldig eller skamfuld?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.013,"ratingscale":1},{"key":"6.014","title":"Skyldbetonede selvhenføringsidéer","description":"Har du følt, at andre bebrejdede dig eller anklagede dig for noget, du havde gjort eller undladt at gøre?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.014,"ratingscale":1},{"key":"6.015","title":"Nedsat selvtillid over for andre","description":"Har din selvtillid været mindre end sædvanlig?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.015,"ratingscale":1},{"key":"6.016","title":"Social tilbagetrækning","description":"Er du blevet mere indesluttet? - Tilbøjelig til at holde dig for dig selv? Tager du ikke telefonen, når den ringer?\nLukker du ikke op, når der kommer nogen?\n","input":"string","validate":["0-3",6,8,9],"episodes":1,"attributional":1,"glossary":6.016,"ratingscale":1},{"key":"6.017","title":"Nedsat selvfølelse Hvordan har du følt dig i forhold til andre?","description":"? Har du følt dig mindre dygtig og kompetent end andre? - På hvilken måde? ? Har du følt dig mindre værd end andre? ? Har du følt dig helt uduelig eller værdiløs?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.017,"ratingscale":1},{"key":"6.018","title":"PSYKOTISKE DEPRESSIONSSYMPTOMER","description":"6_018 Depressive vrangforestillinger om skyld eller uduelighed (cf. 19.025)\nRates efter Rating Skala II\n","input":"string","validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":6.018,"ratingscale":2},{"key":"6.019","title":"Depressive vrangforestillinger om mulig død eller ulykke (cf. 19.026)","description":"Rates efter Rating Skala II\n","input":"string","validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":6.019,"ratingscale":2},{"key":"6.020","title":"Depressive hypokondre eller nihilistiske vrangforestillinger (cf. 19.027)","description":"Rates efter Rating Skala II\n","input":"string","validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":6.02,"ratingscale":2},{"key":"6.021","title":"Stemningskongruens af hørelseshallucinationer (cf. 17.010)","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Fuld stemningskongruens","2":"Overvejende stemningskongruens","3":"Stemningskongruente og -inkongruente hørelseshallucinationer i lige grad","4":"Overvejende stemningsinkongruens","5":"Ingen stemningskongruens"},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.021},{"key":"6.022","title":"GENEREL DEPRESSIONSRATING","description":"6_022 Primær depressions- eller angsttilstand\nRates hvis der foreligger angstsymptomer og depressionssymptomer.\n","input":"integer","options":{"0":"Kun angst- eller kun depressionssymptomer, eller ingen af delene","1":"Angsttilstanden primær","2":"Angstsymptomer og depressionssymptomer begge til stede, uafhængigt af hinanden","3":"Depressionstilstanden primær","8":"Uvist, fx på grund af manglende oplysninger"},"validate":["0-3",8,9],"episodes":1,"glossary":6.022},{"key":"6.023","title":"Forholdet mellem somatoforme og depressive symptomer","input":"integer","options":{"0":"Kun somatoforme eller kun depressive symptomer eller ingen af delene","1":"Depressive symptomer mest udtalte eller begyndte først","2":"Depressive og somatoforme symptomer optræder samtidigt","3":"Somatoforme symptomer mest udtalte eller begyndte først","8":"Uvist"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":6.023},{"key":"6.024","title":"Forholdet mellem tvangssymptomer og depressive symptomer","input":"integer","options":{"0":"Kun tvangs- eller kun depressionssymptomer eller ingen af delene","1":"Depressive symptomer mest udtalte eller begyndte først","2":"Tvangs- og depressionssymptomer til stede samtidigt","3":"Tvangssymptomer mest udtalte eller begyndte først","8":"Uvist"},"validate":["0-3",8,9],"episodes":1,"glossary":6.024},{"key":"6.025","title":"Begyndelsesalder for depressive symptomer","input":"string","validate":["0-99"],"glossary":6.025},{"key":"6.026A","title":"Tidsangivelse for perioden med sektion 6 depressive symptomer Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014.","description":"Begyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":6.026},{"key":"6.026B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.026},{"key":"6.026C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":6.026},{"key":"6.026D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.026},{"key":"6.027","title":"Påvirkning af aktiviteter på grund af sektion 6 depressive symptomer","input":"integer","options":{"0":"Ingen depressive symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":6.027},{"key":"6.028","title":"Organisk årsag til sektion 6 depressive symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.028},{"key":"6.029","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.029},{"key":"6.030","title":"ANAMNESTISKE OPLYSNINGER","description":"6_030 Antal episoder med affektiv sindslidelse\nNB! En episode afgrænses fra en lige-artet episode ved en periode på mindst 2 måneder uden affektive symptomer, eller fra en anderledes-artet episode ved skift til en episode af modsat eller blandet polaritet.\n","input":"integer","options":{"0":"Ikke rated","1":"Kun én affektiv episode i hele forløbet","2":"Mindst 2 affektive episoder","3":"3 eller flere episoder","4":"4 eller flere episoder inden for en 12 måneders periode","5":"De seneste 4 episoder optrådte inden for en 12 måneders periode","8":"Uvist","9":"NA"},"validate":["0-5",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.03},{"key":"6.031","title":"Habituel personlighedstruktur forud for første depressionstilstand","input":"integer","options":{"0":"Inden for normal variationsbredde","1":"Signifikant personlighedsforstyrrelse forud for første depressive episode"},"validate":[0,1,8,9],"glossary":6.031},{"key":"6.032","title":"Sværhedsgrad af affektive episoder","input":"integer","options":{"0":"Ikke rated","1":"Overvejende lette episoder","2":"Overvejende moderate episoder","3":"Overvejende svære episoder","8":"Uvist","9":"NA"},"validate":["0-3",8,9],"glossary":6.032},{"key":"6.033","title":"To eller flere depressive episoder med fuld remission Perioden med remission skal have varet mindst 2 måneder uden betydende","description":"affektive symptomer. Hele forløbet tages i betragtning.\n","input":"integer","options":{"0":"Ikke rated","1":"Kun én episode med fuld remission","2":"Mindst 2 episoder med fuld remission","3":"Fuld remission ved seneste 2 episoder","8":"Uvist","9":"NA"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.033},{"key":"6.034","title":"Effekt af adækvat antidepressiv behandling","input":"integer","options":{"0":"Ikke rated","1":"Effekten ikke tilfredsstillende","2":"Effekten tilfredsstillende","8":"Uvist","9":"NA"},"validate":["0-2",8,9],"glossary":6.034},{"key":"6.035","title":"En eller flere episoder med mani, hypomani eller blandingstilstand","description":"Hele forløbet tages i betragtning.\n","input":"integer","options":{"0":"Ikke rated","1":"Ingen episoder med hypomani, mani eller blandingstilstand","2":"Mindst en episode med hypomani, mani eller blandingstilstand","8":"Uvist","9":"NA"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.035},{"key":"6.036","title":"Episoder med blandingstilstand","description":"Rates for episoder med blandingstilstand, aktuelt eller tidligere i forløbet.\n","input":"integer","options":{"0":"Ikke rated","1":"Ingen episoder med blandingstilstand","2":"En eller flere tidligere episoder med blandingstilstand af mindst en uges varighed","3":"En eller flere tidligere episoder med blandingstilstand af mindst 2 ugers varighed","4":"Aktuel episode med blandingstilstand af mindst en uges varighed","5":"Aktuel episode med blandingstilstand af mindst 2 ugers varighed","8":"Uvist"},"validate":["0-5",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.036},{"key":"6.037","title":"Tidangivelser for 6 seneste depressive episoder forud for nuværende","description":"Anvendes særligt ved registrering af seasonal affective disorder eller rapid cycling.\n","input":"integer","options":{"0":"Episodelisten (6.038-6.043) ikke rated","1":"Tidsangivelser for op til 6 seneste depressive episoder forud for nuværende","2":"Tidsangivelser for op til 6 seneste depressive episoder forud for nuværende med separate SCAN ratings","3":"Kun nogle af op til 6 tidligere episoder med SCAN rating"},"validate":["0-3",8,9],"glossary":6.037},{"key":"6.038","title":"Tidligere depressive episode nr. 1","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.039","title":"Tidligere depressive episode nr. 2","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.040","title":"Tidligere depressive episode nr. 3","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.041","title":"Tidligere depressive episode nr. 4","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.042","title":"Tidligere depressive episode nr. 5","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.043","title":"Tidligere depressive episode nr. 6","input":"date_interval","fitb_size":"DATE_NODAY","glossary":6.037},{"key":"6.044","title":"CHECKLISTE FOR DYSTHYMI","description":"6.044 Nedtrykthed af mere end 2 års varighed\nDet meste af tiden, højst med få uger varende remissioner.\n","input":"integer","options":{"0":"Nej","1":"Ja","8":"Uvist SKIP => til 6.072 hvis 6.044 = 0"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.045","title":"Nedsat energi og aktivitet","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.046","title":"Søvnløshed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.047","title":"Øget søvntrang","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"used_in_dsmiv":1,"glossary":6.044},{"key":"6.048","title":"Nedsat selvtillid, selvfølelse eller utilstrækkelighedsfølelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.049","title":"Koncentrations- eller beslutningsbesvær","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.050","title":"Letrørthed , grådtendens","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.051","title":"Tab af lyst eller interesse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.052","title":"Nedsat seksuallyst","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.053","title":"Håbløshedsfølelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.054","title":"Nedsat produktivitet, effektivitet eller vanskelighed ved at klare","description":"dagligdagens rutinekrav\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.055","title":"Pessimisme med hensyn til fremtiden, rugen over fortiden","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.056","title":"Tilbøjelighed til at isolere sig","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.057","title":"Fåmælthed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.058","title":"Nedsat appetit eller overdreven fødeindtagelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"used_in_dsmiv":1,"glossary":6.044},{"key":"6.059","title":"Kronisk træthed eller øget trætbarhed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"used_in_dsmiv":1,"glossary":6.044},{"key":"6.060","title":"Skyldfølelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"glossary":6.044},{"key":"6.061","title":"Subjektiv følelse af irritabilitet eller overdreven vrede","input":"integer","options":{"0":"Ikke til stede","1":"Til stede som isoleret symptom","2":"Til stede sammen med andre dysthymi-items","9":"Ukendt, kan ikke rates"},"validate":["0-2",8,9],"glossary":6.044},{"key":"6.062","title":"Påvirkning af aktiviteter på grund af dysthymisymptomer","input":"integer","options":{"0":"Ingen dysthymisymptomer","1":"Let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":6.044},{"key":"6.063","title":"Begyndelsesalder for aktuelle dysthymiperiode","input":"string","validate":["0-99"],"glossary":6.044},{"key":"6.064","title":"Depressionsepisode forud for aktuelle dysthymiperiode","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"glossary":6.044},{"key":"6.065","title":"Komplet remission af mindst 6 måneders varighed mellem depressionsepisode og dysthymiperioden","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"glossary":6.044},{"key":"6.066","title":"Depressionstilstand under de første 2 år af nuværende dysthymiperiode","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.067","title":"Begyndelsesalder for første dysthymiperiode","input":"string","validate":["0-99"],"glossary":6.044},{"key":"6.068","title":"Depressiv episode forud for første dysthymiperiode","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"glossary":6.044},{"key":"6.069","title":"Komplet remission af mindst 6 måneders varighed mellem den depressive episode og den første dysthymiperiode","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"glossary":6.044},{"key":"6.070","title":"Organisk årsag til dysthymisymptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialudersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.044},{"key":"6.071","title":"ICD-10 diagnosekode for organisk årsag Cyklothymi rates ved 10.031-10.057.","input":"string","fitb_size":"ICD10","episodes":1,"glossary":6.044},{"key":"6.072","title":"CHECKLISTE FOR TILBAGEVENDENDE KORTVARIGE DEPRESSIVE EPISODER","description":"6_072 Kortvarige depressive episoder, mindst en gang månedlig gennem det sidste år\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.072},{"key":"6.073","title":"Varigheden af korte depressive episoder få dage og ikke over 2 uger","input":"integer","options":{"0":"Nej","1":"Ja","8":"Ukendt"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.072},{"key":"6.074","title":"Optræden uden for menstruationscyklus","description":"(ikke udelukkende i forbindelse med menstruationscyklus).\n","input":"integer","options":{"0":"Nej","1":"Ja","8":"Ukendt"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":6.072},{"key":"6.075","title":"Påvirkning af aktiviteter på grund af kortvarige depressive episoder","input":"integer","options":{"0":"Ikke til stede","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":6.072},{"key":"6.076","title":"Organisk årsag til kortvarige depressive episoder","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet ved specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"glossary":6.072},{"key":"6.077","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":6.072},{"key":"7.001","title":"Tænkning, koncentration , energi, interesser","description":"7_001 Screening for tænkning, koncentration, energi og interesser\nHar du været i stand til at tænke og koncentrere dig som du plejer? Har din energi eller dine interesser ændret sig?\n","input":"integer","options":{"1":"Kan tænke og koncentrere sig bedre end de fleste, god energi, mange interesser","2":"Ingen eller kun ringe nedsættelse","3":"Tydelig nedsættelse","4":"Udtalt nedsættelse, næsten totalt tab"},"section_start":"Section_7","validate":["1-4",8,9],"episodes":1,"attributional":1,"glossary":7.001},{"key":"7.002","title":"Nedsat koncentrationsevne","description":"Har din koncentration være lige så god som sædvanligt i perioden?\n? Har du fået svært ved at læse bøger eller blade, eller følge en TV udsendelse?\n? Har du fået svært ved at koncentrere dig om det, du skal gøre, fx dit arbejde, føre en samtale, lave mad? ? Hvor længe har det været sådan?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.002,"ratingscale":1},{"key":"7.003","title":"Subjekt beskrevet ineffektiv tankevirksomhed","description":"Har du været i stand til at tænke klart og sammenhængende? Har du fået svært ved at træffe beslutninger selv om enkle dagligdags forhold?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.003},{"key":"7.004","title":"Tab af interesser","description":"Har du mistet interessen for det, du plejer at interessere dig for, fx arbejde, familie, fritidsinteresser, tøj og udseende?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.004},{"key":"7.005","title":"Subjektiv følelse af motorisk hæmning","description":"Er du blevet langsommere i din tale eller i dine bevægelser? Har du følt dig tung i armene eller benene, blytung?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.005},{"key":"7.006","title":"Tab af energi (livskraft)","description":"Har du tabt energien og kræfterne, ikke følt dig i vigør?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.006},{"key":"7.007","title":"Uoverkommelighedsfølelse i dagligdagen","description":"Føler du det hele overvældende? - Tungt eller uoverkommeligt? Føler du dig overvældet af selv almindelige dagligdags opgaver?\nBrug Rating Skala I\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.007},{"key":"7.008A","title":"Tidsangivelse for sektion 7 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014.\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":7.008},{"key":"7.008B","title":"Varighed af PS (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.008},{"key":"7.008C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":7.008},{"key":"7.008D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.008},{"key":"7.009","title":"Påvirkning af aktiviteter på grund af sektion 7 symptomer","input":"integer","options":{"0":"Ingen sektion 7 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":7.009},{"key":"7.010","title":"Organisk årsag til sektion 7 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":7.01},{"key":"7.011","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":7.011},{"key":"8.001","title":"Fysiologiske funktioner","description":"8_001 Vægt i kg\n","input":"string","section_start":"Section_8","validate":["20-200"],"glossary":8.001},{"key":"8.002","title":"Højde i cm","input":"string","validate":["50-250"],"glossary":8.001},{"key":"8.003","title":"Vægtøgning eller -tab siden adolescensen (kg)","input":"string","validate":["0-100"],"glossary":8.001},{"key":"8.004","title":"Vægten i 18-20 års alderen","input":"string","validate":["20-200"],"glossary":8.001},{"key":"8.005","title":"Appetitændring","description":"Hvordan har appetitten været?\n? Hvor længe har den været ændret ? ? Hvad skyldes det?\n","input":"integer","options":{"0":"Ingen ændring","1":"Udtalt appetitnedsættelse i mindre end 2 uger","2":"Udtalt appetitnedsættelse i 2-4 uger","3":"Udtalt appetitnedsættelse i mere end 1 måned","4":"Øget appetit i mindre end 2 uger","5":"Øget appetit i mere end 2 uger","6":"Altid dårlig appetit","7":"Appetitten vekslende"},"validate":["0-7",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.005},{"key":"8.006","title":"Vægttab","description":"Har du tabt i vægt i løbet af perioden?\n","input":"integer","options":{"0":"Intet tab","1":"Moderat tab","2":"Tab af mere end 5% af legemsvægten i løbet af en måned (3,5 kg svarer til 70 kg)","3":"Vægttab til mere end 15% under normalvægten i løbet af et år","4":"Manglende forventet vægtøgning på et år, svarende til mere end 15% af normalvægten (for børn og unge eller rekonvalescenter)"},"validate":["0-4",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.006},{"key":"8.007","title":"Vægtøgning","description":"Har du taget på i vægt i løbet af perioden?\n","input":"integer","options":{"0":"Ingen øgning","1":"Moderat vægtøgning","2":"Vægtøgning på mere end 5% af legemsvægten i løbet af en måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.007},{"key":"8.008","title":"Vægtproblemer","description":"Har du haft alvorlige problemer med vægten? - Hvilke?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"glossary":8.008},{"key":"8.009","title":"SØVNBESVÆR Hvordan er dit sædvanlige søvnmønster? Hvornår plejer du at gå til ro? Hvornår plejer du at vågne op?","description":"8_009 Søvnbesvær i forbindelse med nedtrykthed\n","input":"integer","options":{"0":"Ingen forbindelse","1":"Definitiv forbindelse med nedtrykthed"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.009},{"key":"8.010","title":"Virkning af sovetabletter","description":"Tager du sovetabletter? Hvilke? Hvor mange? Hjælper de?\n","input":"integer","options":{"0":"Bruger ikke sovetabletter eller sedativa","1":"God virkning","2":"Dårlig virkning"},"validate":["0-2",8,9],"episodes":1,"glossary":8.009},{"key":"8.011","title":"Indsovningsbesvær","description":"Har du besvær med at falde i søvn?\n? Hvor længe ligger du vågen før du falder i søvn? - En time eller mere? ? Hvor ofte sker det? 3 gange ugentligt eller mere? ? Hvor det længe har det været sådan?\n","input":"integer","options":{"0":"Intet indsovningsbesvær","1":"Indsovningsbesvær (mindst en time) gennem mindre end 1 måned","2":"Indsovningsbesvær (mindst 1 time > 3 gange ugentligt) gennem mindst 1 måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.009},{"key":"8.012","title":"Søvnkvalitet","description":"Er søvnen naturlig og forfriskende? Er søvnen unaturlig? Føler du dig træt når du vågner?\n","input":"integer","options":{"0":"God nattesøvn","1":"Søvnen ikke forfriskende, let til moderat grad","2":"Søvnen ikke forfriskende, udtalt grad, 3 eller flere gange ugentligt gennem mindst 1 måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.009},{"key":"8.013","title":"Afbrudt søvn","description":"Vågner du op i løbet af natten? Hvor ofte? Hvor længe? Gennem hvor lang tid?\n","input":"integer","options":{"0":"Søvnen ikke afbrudt","1":"Afbrudt nattesøvn (mindst 1 time), let til moderat grad","2":"Afbrudt nattesøvn, 1 time eller mere, 3 eller flere gange ugentligt gennem mindst 1 måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.013},{"key":"8.014","title":"Tidlig opvågnen","description":"Vågner du op om morgenen tidligere end du plejer? Hvor længe før? Hvor ofte? Gennem hvor lang tid?\nEr det optrådt samtidig med nedtrykthed?\n","input":"integer","options":{"0":"Ingen tidlig opvågnen","1":"Tidlig opvågnen, mindst en time, men ikke (2)","2":"Vågner mindst 2 timer før normalt, > 3 gange ugentligt gennem mindst 1 måned","3":"Som (2), i forbindelse med depressiv episode"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.014},{"key":"8.015","title":"Forstyrret søvnmønster","description":"Har dit søvnmønster ændret sig, fx så du er vågen om natten og sover om dagen? Hvor ofte? Hvor længe?\n","input":"integer","options":{"0":"Ingen ændring","1":"Ændret søvnmønster men ikke (2)","2":"Ændret søvnmønster dagligt gennem mindst 1 måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.015},{"key":"8.016","title":"Øget søvntrang - hypersomni","description":"Skal du sove mere end du plejer? Hvor meget mere? Føler du dig meget søvnig om dagen med tendens til at falde i søvn ind imellem, selvom du sover hele natten?\n","input":"integer","options":{"0":"Ingen hypersomni","1":"Øget søvntrang men ikke (2)","2":"Dagligt gennem mindst en måned"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.016},{"key":"8.017","title":"Forstyrrende drømme eller mareridt","description":"Har du ubehagelige drømme eller mareridt, der har forstyrret søvnen?\n","input":"integer","options":{"0":"Ingen","1":"Mareridt men ikke (2)","2":"Hyppige mareridt med forstyrret nattesøvn"},"validate":["0-2",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.017},{"key":"8.018","title":"Søvnrædsel","description":"Natlige anfald med pludselig opvågnen i en tilstand af panikagtig angst, ofte med ændret bevidsthedstilstand, desorientering og efterfølgende amnesi. Optræder oftest først på natten, og varer kun kort (mindre end 10 minutter).\n","input":"integer","options":{"0":"Ingen","1":"Gentagne anfald med søvnrædsel i løbet af perioden"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.018},{"key":"8.019","title":"Søvngængeri","description":"Anfald, hvor man i søvne bevæger sig omkring med tomt ansigtsudtryk uden at reagere på omgivelserne og er vanskelig at vække. Ved opvågningen kan der være kortvarig konfusion. Efterfølges af amnesi.\n","input":"integer","options":{"0":"Ingen","1":"Gentagne episoder i løbet af perioden"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.019},{"key":"8.020A","title":"Tidsangivelse for søvnbesvær","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014.\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":8.02},{"key":"8.020B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.02},{"key":"8.020C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":8.02},{"key":"8.020D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.02},{"key":"8.021","title":"Påvirkning af aktivitet på grund af søvnbesvær","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.021},{"key":"8.022","title":"Organisk årsag til søvnbesvær","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuld bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.022},{"key":"8.023","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_dsmiv":1,"glossary":8.023},{"key":"8.024","title":"SEKSUEL DYSFUNKTION","description":"8_024 Nedsat seksuallyst - tab af libido\nHar din lyst til seksuel udfoldelse været mindre end sædvanligt i løbet af perioden?\n","input":"integer","options":{"1":"Noget nedsat","2":"Udtalt nedsættelse","3":"Helt manglende"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.024},{"key":"8.025","title":"Nedsat seksuallyst i forbindelse med depression","input":"integer","options":{"0":"Ingen forbindelse","1":"Tydeligt forbundet med nedtrykthed"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.025},{"key":"8.026","title":"Andre seksuelle problemer","description":"Er der ubehag eller smerter i forbindelse med seksuel udfoldelse? Er der andre seksuelle problemer?\n","input":"integer","options":{"0":"Ingen andre seksuelle problemer","1":"Andre seksuelle problemer (cf. 27.039 - 27.048)"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":8.026},{"key":"8.027A","title":"Tidsangivelse for seksuel dysfunktion","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014.\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":8.027},{"key":"8.027B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"glossary":8.027},{"key":"8.027C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":8.027},{"key":"8.027D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"glossary":8.027},{"key":"8.028","title":"Påvirkning af aktiviteter på grund af seksuel dysfunktion","input":"integer","options":{"0":"Ingen seksuel dysfunktion","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":8.028},{"key":"8.029","title":"Organisk årsag til seksuel dysfunktion","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":8.029},{"key":"8.030","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_dsmiv":1,"glossary":8.03},{"key":"9.001","title":"Spiseforstyrrelser","description":"NB! Tidsperioden for spiseforstyrrelser er sædvanligvis det sidste år.\n9_001 Frygt for fedme eller overvægt Har du gennem det sidste år været bekymret for at veje for meget eller blive for fed?\n? Er du meget optaget af dit udseende, din form eller din figur, eller hvor meget du spiser? ? Får du skyldfølelse når du spiser?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"section_start":"Section_9","validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":9.001},{"key":"9.002","title":"Tidligere perioder med nedsat fødeindtagelse","input":"integer","options":{"0":"Aldrig","1":"Har haft tidligere periode(r) på grund af frygt for fedme"},"validate":[0,1,8,9],"glossary":9.002},{"key":"9.003","title":"Uimodståelig og vedholdende trang til mad","description":"Har du haft en følelse af ubændig spisetrang, eller været ude af stand til at holde op med at spise, når du først var begyndt?\n? Selvom du var bange for at blive overvægtig,? Hvor ofte? Hvor meget?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.003},{"key":"9.004","title":"Tidligere episoder med abnorm spisetrang","input":"integer","options":{"0":"Aldrig","1":"Har haft tidligere episode(r) med abnorm spisetilbøjelighed"},"validate":[0,1,8,9],"glossary":9.004},{"key":"9.005","title":"Påvirkning af aktiviteter på grund af spiseforstyrrelse","input":"integer","options":{"0":"Ingen spiseforstyrrelse","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær påvirkning CUT OFF => 10.001 hvis der ikke er oplysninger om anorexi eller bulimi ."},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":9.005},{"key":"9.006","title":"ANOREXI","description":"9_006 Selvopfattelse af at være overvægtig med påtrængende frygt for fedme\nFøler du dig overvægtig eller fed? Synes andre at du er overvægtig eller fed?\n? Synes andre at du er for tynd? ? Har du været optaget af en følelse af at være overvægtig eller fed, selvom du godt var klar over, at du vejede mindre end normalt?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.006},{"key":"9.007","title":"Ansættelse af lav vægtgrænse for egen vægt","description":"Afvisning af at skulle veje over 85% af den forventede normalvægt.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.007},{"key":"9.008","title":"Undgåelse af fedende kost","description":"Har du undgået at spise alt det, der feder? Følger du en særlig diæt? - Selvom du ved at din vægt ligger under den normale?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.008},{"key":"9.009","title":"Vægtreducerende aktiviteter ved hjælp af fysisk udfoldelse eller appetithæmning","description":"Har du prøvet at nedsætte vægten ved hjælp af fysisk udfoldelse, ekstra meget motion eller træning? Har du prøvet at nedsætte vægten ved hjælp af afmagringstabletter eller slankepiller?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"glossary":9.009},{"key":"9.010","title":"Andre vægtreducerende aktiviteter, ved hjælp af udrensning Har du prøvet at skaffe dig af med maden ved at kaste op? Har du brugt afføringsmidler eller vanddrivende midler?","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_dsmiv":1,"glossary":9.01},{"key":"9.011","title":"Forsinket pubertetsudvikling på grund af anorexi","description":"Kvinder: primær amenore, væksthæmning. Mænd: manglende kønsmodning, væksthæmning.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.011},{"key":"9.012","title":"Endokrine forstyrrelser på grund af anorexi Kvinder:","description":"Er dine menstruationer sprunget over eller udeblevet? Hvor ofte?\n0: Ingen\n1: Ja, men ikke (2)\n2: Amenore: udeblivende menstruationer gennem 3 på hinanden følgende perioder\n3: Kun på grund af hormonbehandling\n8: Usikkert (vaginalblødning under hormon- eller p-pille-behandling)\n9: Postmenopausal Mænd: Har din seksuelle udfoldelse ændret sig?\n? Er du mindre interesseret, eller fungerer det ikke så godt?\n","input":"integer","options":{"0":"Ingen ændring","1":"Moderat eller intermitterende nedsættelse","2":"Mere udtalt vedvarende nedsættelse eller komplet tab"},"validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.012},{"key":"9.013","title":"BULIMI","description":"9_013 Spiseanfald med følelse af kontroltab\nHar du haft perioder med anfald af abnorm spisetrang, hvor du i løbet af kort tid har indtaget store mængder føde?\n? Hvor ofte? Hvor længe? ? Kunne du ikke styre det?\n","input":"integer","options":{"0":"Ingen","1":"Anfald men med bevaret kontrol","2":"Spredte anfald med følelse af kontroltab","3":"Anfald gennemsnitligt 2 gange ugentligt over en 3 måneders periode med følelse af tab af kontrol, men ikke (4).","4":"Hyppige anfald med tab af kontrol."},"validate":["0-4",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.013},{"key":"9.014","title":"Frygt for fedme eller overvægt trods uimodståelig spisetrang Er du bange for at blive overvægtig eller for fed til trods for spisetrangen, eller den manglende evne til at holde op, når du først er begyndt?","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"glossary":9.014},{"key":"9.015","title":"Vægtreducerende aktiviteter ved hjælp af udrensning","description":"Har du prøvet at skaffe dig af med maden ved at kaste op? Har du taget afføringspiller eller vanddrivende tabletter?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.015},{"key":"9.016","title":"Vægtbegrænsende aktiviteter","description":"Har du prøvet at begrænse vægten ved at lade være med at spise, holde en streng diæt, eller ved hjælp af overdreven motion? Har du taget afmagringstabletter eller slankepiller?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":9.016},{"key":"9.017","title":"ANAMNESTISKE OPLYSNINGER","description":"9_017 Begyndelsesalder for aktuelle anorexiperiode\n","input":"string","validate":["0-99"],"glossary":9.017},{"key":"9.018","title":"Begyndelsesalder for aktuelle bulimiperiode","input":"string","validate":["0-99"],"glossary":9.018},{"key":"9.019","title":"Begyndelsesalder for første anorexiperiode","input":"string","validate":["0-99"],"glossary":9.019},{"key":"9.020","title":"Begyndelsesalder for første bulimiperiode","input":"string","validate":["0-99"],"glossary":9.02},{"key":"9.021","title":"Tidsangivelse for aktuelle anorexiperiode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":9.021},{"key":"9.022","title":"Tidsangivelse for aktuelle bulimiperiode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":9.022},{"key":"9.023","title":"Forholdet mellem bulimi og anorexi","input":"integer","options":{"0":"Bulimi ikke til stede eller kun i forbindelse med anorexi","1":"Bulimi til stede uden forbindelse med anorexi"},"validate":[0,1,8,9],"episodes":1,"glossary":9.023},{"key":"9.024","title":"Tidsangivelse for tidligere anorexiperiode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":9.024},{"key":"9.025","title":"Tidsangivelse for tidligere bulimiperiode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":9.025},{"key":"9.026","title":"Begrænsning af aktiviteter på grund af anorexi eller bulimi","input":"integer","options":{"0":"Ingen symptomer","1":"Kun let begrænsning","2":"Moderat eller intermitterende begrænsning","3":"Svær eller invaliderende begrænsning"},"validate":["0-3",8,9],"episodes":1,"glossary":9.026},{"key":"9.027","title":"Organisk årsag til sektion 9 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"glossary":9.027},{"key":"9.028","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":9.028},{"key":"10.001","title":"Ekspansivt stemningsleje og tankevirksomhed","description":"- Har du følt dig løftet, opstemt eller ovenud glad uden ydre grund?\n- I en sådan grad, at du følte det unaturligt?\n- Var det tydeligt anderledes end det plejer?\n- Hvor længe varede det?\n  - Nogle dage?\n  - Mere end en uge?\n  - Var det hver dag, hele dagen?\n\nBrug Rating Skala I :\n","input":"integer","options":{"1":"Let","2":"Moderat","3":"Svær"},"section_start":"Section_10","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.001,"ratingscale":1},{"key":"10.002","title":"Irritabelt stemningsleje","description":"- Har du følt dig i en tilstand af irritabilitet og utålmodighed?\n- Har du følt dig ilter, hidsig, tilbøjelig til at fare op, på en måde som er unaturlig for dig?\n- Blev du aggressiv, skældte ud eller smed med tingene?\n\nBrug Rating Skala I :\n","input":"integer","options":{"1":"Let","2":"Moderat","3":"Svær"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.002,"ratingscale":1},{"key":"10.003","title":"Varighed af ekspansivt eller irritabelt stemningsleje","input":"integer","options":{"0":"Fraværende","1":"Til stede, men mindre end 4 dage","2":"Til stede igennem 4-6 dage","3":"Til stede igennem 7-60 dage","4":"Til stede igennem mere end 2 måneder","5":"Medførte hospitalsindlæggelse uden hensyn til varigheden Andre symptomer på ekspansivt stemningsleje og tankevirksomhed (10.004-10.018) (kan forekomme ved andre lidelser end mani eller hypomani)."},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.003},{"key":"10.004","title":"Tankemylder, tankepres","description":"Har du haft mange tanker og idéer i hovedet, flere end du kunne magte? Har\ndu tankemylder eller raser tankerne afsted?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.004,"ratingscale":1},{"key":"10.005","title":"Taletrang","description":"Har du talt mere eller hurtigere end du plejer? Føler du trang til at\nsnakke hele tiden?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.005,"ratingscale":1},{"key":"10.006","title":"Letafledelighed, usamlethed","description":"Har du kunnet fastholde din opmærksomhed om det du skulle? Bliver du hele\ntiden distraheret?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.006,"ratingscale":1},{"key":"10.007","title":"Subjektivt beskrevet overaktivitet Har du været mere aktiv end vanligt? - Så aktiv at andre syntes det blev for meget?","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.007,"ratingscale":1},{"key":"10.008","title":"Skærpet tankevirksomhed","description":"Har du været i stand til at tænke klarere, hurtigere og mere effektivt\nend vanligt? Har du været bedre til at koncentrere dig, løse problemer\neller tænke kreativt?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":10.008,"ratingscale":1},{"key":"10.009","title":"Overdrevent underholdende tale","description":"Har du været mere underholdende, vittig eller morsom end du plejer?\n? Har andre også fundet det underholdende?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":10.009,"ratingscale":1},{"key":"10.010","title":"Øget selvfølelse, grandiøsitet","description":"Har du følt dig dygtigere, mere effektiv end vanligt? Mere intelligent, begavet og kompetent?\n? Raskere, stærkere og kønnere end du plejer?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.01,"ratingscale":1},{"key":"10.011","title":"Overoptimisme Ser det hele lyst og strålende ud fremover, lige meget hvad du tænker på? Hvordan har du klaret dig før i tiden?","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":10.011,"ratingscale":1},{"key":"10.012","title":"Handlinger baseret på ekspansivt stemningsleje","description":"Har du haft uimodståelig trang til at købe nye ting? Har du brugt mange penge? Har du foretaget dig overmodige, måske farlige handlinger?\n","input":"integer","options":{"0":"Ikke til stede","1":"Let, ikke alvorlige problemer","2":"Moderat overforbrug","3":"Svær grad: hensynsløs kørsel, købemani, sagt sit job op"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.012,"ratingscale":1},{"key":"10.013","title":"Nedsat søvnbehov","description":"Har du kunnet klare dig med mindre søvn uden at blive træt?\n","input":"integer","options":{"0":"Ikke til stede","1":"Let: mindre end 1 time","2":"Moderat: mindre end 3 timer","3":"Svær: søvnbehovet reduceret med mere end 3 timer"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.013},{"key":"10.014","title":"Socialt kompromitterende adfærd","description":"Har du blandet dig i noget, du ikke skulle? Har du opført dig påtrængende eller optrådt pinligt?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.014,"ratingscale":1},{"key":"10.015","title":"Øget seksualdrift eller seksuel aktivitet","description":"Har du haft større lyst til det seksuelle end sædvanligt? Har du udfoldet dig mere eller anderledes på det seksuelle område i løbet af perioden?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.015,"ratingscale":1},{"key":"10.016","title":"PSYKOTISKE MANISYMPTOMER","description":"10_016 Vrangforestillinger om grandiøse evner (cf. 19.029)\nRates efter skala II.\n","input":"string","validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.016,"ratingscale":2},{"key":"10.017","title":"Vrangforestillinger om grandiøs identitet (cf. 19.030)","description":"Rates efter skala II.\n","input":"string","validate":["0-3",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.016,"ratingscale":2},{"key":"10.018","title":"Begyndelsesalder for maniske symptomer","input":"string","validate":["0-120",888,999],"glossary":10.018},{"key":"10.019","title":"Episoder med blandingstilstand (identisk med 6.036)","description":"Rates for episoder med blandingstilstand, aktuelt eller tidligere i forløbet.\n","input":"integer","options":{"0":"Ikke rated","1":"Ingen episoder med blandingstilstand","2":"Én eller flere tidligere episoder med blandingstilstand af mindst én uges varighed","3":"Én eller flere tidligere episoder med blandingstilstand af mindst 2 ugers varighed","4":"Aktuel episode med blandingstilstand af mindst én uges varighed","5":"Aktuel episode med blandingstilstand af mindst 2 ugers varighed","8":"Uvist"},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.019},{"key":"10.020A","title":"Tidsangivelse for sektion 10 symptomer Rates kun hvis forskellig fra 1.007-1.014.","description":"Begyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":10.02},{"key":"10.020B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.02},{"key":"10.020C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":10.02},{"key":"10.020D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"glossary":10.02},{"key":"10.021","title":"Tidsangivelser for 6 seneste episoder med maniske symptomer forud for nuværende","description":"Anvendes særligt ved registrering af SAD eller rapid cycling.\n","input":"integer","options":{"0":"Episodelisten (10.022-10.027) ikke rated","1":"Tidsangivelser for op til 6 seneste episoder af maniske symptomer forud for nuværende","2":"Tidsangivelser for op til 6 seneste episoder af maniske symptomer forud for nuværende med separate SCAN ratings","8":"Ukendt","9":"Ikke anvendelig"},"validate":["0-2",8,9],"glossary":10.021},{"key":"10.022","title":"Tidligere episode med maniske symptomer nr. 1","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.023","title":"Tidligere episode med maniske symptomer nr. 2","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.024","title":"Tidligere episode med maniske symptomer nr. 3","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.025","title":"Tidligere episode med maniske symptomer nr. 4","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.026","title":"Tidligere episode med maniske symptomer nr. 5","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.027","title":"Tidligere episode med maniske symptomer nr. 6","input":"date_interval","fitb_size":"DATE_NODAY","glossary":10.021},{"key":"10.028","title":"Påvirkning af aktiviteter på grund af sektion 10 symptomer","input":"integer","options":{"0":"Ingen symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.028},{"key":"10.029","title":"Organisk årsag til sektion 10 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.029},{"key":"10.030","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.03},{"key":"10.031","title":"Checkliste for hypomane symptomer ved cyklothymi","description":"Tilstand af mere end 2 års varighed med lette såvel hypomane som subdepressive stemningssvingninger det meste af tiden, med remissioner af mindre end 2 måneders varighed.\nFor hypomane symptomer:\n0: Ikke til stede\n1: Til stede\n8: Ukendt\n10_031 Opstemthed\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.032","title":"Øget formålsrettet aktivitet","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.033","title":"Letafledelighed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.034","title":"Nedsat søvntrang","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.035","title":"Øget selvfølelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.036","title":"Skærpet eller usædvanligt kreativ tankevirksomhed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.037","title":"Øget selskabelighed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.038","title":"Snakkesalighed","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.039","title":"Tankemylder","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.040","title":"Usædvanligt underholdende eller vittig","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.041","title":"Øget seksuel udfoldelse","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.042","title":"Øget udfoldelse med hensyn til andre lystbetonede aktiviteter","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.043","title":"Overoptimisme","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.044","title":"Irritabilitet","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.045","title":"Hypomane episoder gennem mere end 2 år med mindre end 2 måneders remissioner imellem","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.046","title":"Begyndelsesalder for aktuelle cyklothymiperiode","input":"string","validate":["0-99"],"glossary":10.031},{"key":"10.047","title":"Begyndelsesalder for første cyklothymiperiode","input":"string","validate":["0-99"],"glossary":10.031},{"key":"10.048","title":"Begyndelsesalder for førte hypomane syndrom","input":"string","validate":["0-99"],"glossary":10.031},{"key":"10.049","title":"Periode på > 2 år med kun subdepressive episoder","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.050","title":"Periode på 2 eller flere år med hypomane og subdepressive episoder","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.051","title":"Begyndelsesalder for aktuelle cyklothymiperiode (=10.046)","input":"string","validate":["0-99"],"glossary":10.031},{"key":"10.052","title":"Depressionstilstand eller mani umiddelbart forud for aktuelle","description":"cyklothymiperiode\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.053","title":"Begyndelsesalder for første cyklothymiperiode (=10.047)","input":"string","validate":["0-99"],"glossary":10.031},{"key":"10.054","title":"Depressionstilstand eller mani umiddelbart forud for tidligere","description":"cyklothymiperiode\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Ukendt"},"validate":[0,1,8,9],"glossary":10.031},{"key":"10.055","title":"Påvirkning af aktiviteter på grund af vedvarende hypomani eller cyklothymi","input":"integer","options":{"0":"Fraværende","1":"Let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":10.031},{"key":"10.056","title":"Organisk årsag til hypomani- eller cyklothymiperioder","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Uvist om organisk årsag"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":10.031},{"key":"10.057","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":10.031},{"key":"11.001","title":"Brug af alkohol","description":"NB! Tidsperioden for brug af alkohol og psykoaktive substanser er det sidste år (PY) (i venstre rating-box) eller livsperioden forud (LB) (i højre rating-box).\n11_001 Brug af alkohol\nDrikker du alkohol? Hvor ofte? Har du aldrig drukket alkohol? Heller ikke ved festlige lejligheder?\n","input":"integer","options":{"1":"Har aldrig drukket alkohol","2":"Har drukket alkohol én eller to gange i løbet af sit liv","3":"Har drukket alkohol oftere end (2) CUT OFF => til 12.001 hvis 11.001 rates 1 eller 2"},"section_start":"Section_11","validate":["1-3",8,9],"glossary":11.001},{"key":"11.002","title":"Hvad drikker du? (øl, vin, stærke drikke). Hvordan virker det på dig? Hvorfor drikker du? Har du svært ved at undvære det? Har det givet anledning til problemer? Er det gået ud over dit helbred eller dine aktiviteter? Har du drukket mere tidligere?","description":"11_002 Hyppighed af alkoholforbrug\n","input":"integer","options":{"0":"Intet alkoholforbrug","1":"Mindre end én gang om måneden","2":"En gang månedligt","3":"2-3 dage pr. måned","4":"1-2 dage om ugen","5":"Praktisk talt dagligt, på ét eller andet tidspunkt i løbet af et år","6":"Praktisk talt dagligt, gentagne gange inden for et år","7":"Praktisk talt dagligt, gennem mindst en måned","8":"Ukendt"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.002},{"key":"11.003","title":"Sædvanlige daglige forbrug af alkohol","description":"Hvad drikker du? (øl, vin, snaps, vodka, whisky etc.). Hvor meget drikker du (hvor mange genstande), de dage du drikker?\n","input":"integer","options":{"0":"Intet forbrug","1":"1-2 genstande","2":"3-4 genstande","3":"5-9 genstande","4":"10-14 genstande","5":"15-19 genstande","6":"20-25 genstande","7":"Mere end 25 genstande","8":"Ukendt"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.003},{"key":"11.004","title":"Højeste daglige forbrug af alkohol","description":"Har der været dage, hvor du har drukket mere end sædvanligt (fester, weekender, ferier)?\nHvornår var det? Hvor meget fik du at drikke på de dage, hvor du drak ekstra meget?\n","input":"integer","options":{"0":"Ingen perioder med højere forbrug","1":"1-2 genstande","2":"3-4 genstande","3":"5-9 genstande","4":"10-14 genstande","5":"15-19 genstande","6":"20-25 genstande","7":"Mere end 25 genstande","8":"Ukendt"},"validate":["0-9"],"episodes":1,"glossary":11.004},{"key":"11.005","title":"Hyppighed af højeste daglige alkoholforbrug","description":"Hvor ofte er det sket, at du har fået særlig meget at drikke?\n","input":"integer","options":{"0":"Ingen perioder med ekstra forbrug","1":"Lejlighedsvis, mindre end én gang månedlig","2":"Én gang månedlig","3":"Mere end en gang månedlig men ikke (4)","4":"Ugentlig eller oftere"},"validate":["0-4",8,9],"episodes":1,"glossary":11.005},{"key":"11.006","title":"Seneste alkoholindtagelse","description":"Hvornår fik du sidst noget at drikke? Som sent som i dag eller i går?\n","input":"integer","options":{"0":"Intet alkoholforbrug","1":"Inden for de sidste 24 timer","2":"For 1 - 3 dage siden","3":"For 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Ukendt","9":"?"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.006},{"key":"11.007","title":"Subjektiv trang til alkohol","description":"Har du følt, at du absolut måtte have alkohol og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få noget at drikke? ? Var du ved sådanne lejligheder plaget af så stærk en trang, at du ikke kunne tænke på andet? ? Har du haft denne trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen alkoholtrang","1":"Følte sig utilpas og bevidst om alkoholtrang, men ikke (2)","2":"Stærk og pågående alkoholtrang"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.007},{"key":"11.008","title":"Nedsat evne til at afholde sig fra eller nedsætte forbrug af alkohol","description":"Har du for nyligt eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre med at drikke uden at kunne gennemføre det?\n? Har du kunnet lade være at drikke ind imellem, fx i weekender eller når du var alene? ? Har du været i stand til at nedsætte forbruget eller helt holde op med at drikke i så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med forbruget","1":"Har været i stand til at nedsætte eller ophøre med forbruget igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med forbruget gennem så lang tid som en måned"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.008},{"key":"11.009","title":"Nedsat evne til at styre forbruget når det først er begyndt","description":"Sker det, at du drikker mere, end du havde planlagt, da du begyndte at drikke? Har du haft svært ved at holde op med at drikke, når du først var begyndt på det?\n? Har du fortsat med at drikke igennem, flere dage i træk?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre forbruget","1":"Har sommetider vanskeligheder ved at styre forbruget","2":"Drikker sædvanligvis mere end planlagt","3":"Udtalt vanskelighed ved at styre forbruget, drikker igennem"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.009},{"key":"11.010","title":"Sociale problemer som følge af alkoholforbrug","description":"Har dit alkoholforbrug ført til problemer i forhold til familien, vennerne eller arbejdet? Ved mere end én lejlighed?\n? Er du kommet op at skændes, kommet for sent på arbejde, haft fravær eller økonomiske problemer på grund af alkoholforbrug? ? Har du haft mere alvorlige problemer, mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.01},{"key":"11.011","title":"Retslige problemer som følge af alkoholforbrug","description":"Har dit alkoholforbrug medført retslige problemer? - Ved mere end én lejlighed?\n? Slagsmål, gaduorden, vold i hjemmet, spirituskørsel eller lign.? ? Har du fået en dom som følge af dit alkoholforbrug?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":11.011},{"key":"11.012","title":"Vedvarende alkoholforbrug efter erkendt social eller retslig skadevirkning","description":"Har du fortsat med at drikke selvom du vidste, at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.012},{"key":"11.013","title":"Svigten med hensyn til fremtrædende rolleforpligtelser","description":"Har du forsømt at passe det man forventede af dig, som følge af alkoholforbruget, fx dit arbejde eller din uddannelse, at tage dig af børnene, passe vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":11.013},{"key":"11.014","title":"Risikofyldt adfærd i forbindelse med alkoholforbrug","description":"Har du drukket i situationer, hvor det kunne medføre en risiko for dig selv eller andre? Ved mere end én lejlighed?\n? Fx hvis du skulle passe maskiner, køre bil, gå over gaden?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd i forbindelse med alkohol","1":"Gentagne tilfælde med risikofyldt adfærd, uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre CUT OFF => til 12.001 hvis højeste daglige alkoholindtagelse var mindre en 4 genstande for mænd eller 3 genstande for kvinder og 11.007-11.014 alle er rated 0."},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":11.014},{"key":"11.015","title":"Prioritering af alkoholrelaterede aktiviteter","description":"Hvor vigtigt er det at få noget at drikke sammenlignet med andre aktiviteter?\n? Betyder det at alkoholindtagelsen går forud for alt andet? ? Har du fx brugt flere penge på alkohol end på familien? ? Er alkoholindtagelse gået forud for arbejde, vigtige aftaler eller for vigtige sociale og rekreative aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.015},{"key":"11.016","title":"Tid brugt på alkoholrelaterede aktiviteter","description":"Har du brugt megen tid på at drikke eller komme dig efter at have drukket?\n? Mere tid end du kunne tillade dig? ? Er det gået ud over andre aktiviteter, fordi det har taget så megen tid?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig gennem en måned eller mere"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.016},{"key":"11.017","title":"Indsnævret rutinepræg af drikkemønster","description":"Har du udviklet et fast drikkemønster, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?\n","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"validate":["0-2",8,9],"episodes":1,"glossary":11.017},{"key":"11.018","title":"Toleransudvikling over for alkohol","description":"Virker alkohol mindre stærkt på dig end tidligere? Behøver du mere for at opnå samme virkning?\n? Er du i stand til at drikke meget større mængder alkohol end tidligere uden at det virker så stærkt? ? Hvor meget mere?\n","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.018},{"key":"11.019","title":"Psykiske problemer som følge af alkoholforbrug Har dine symptomer forbindelse med dit alkoholforbrug? Forværres dine symptomer i forbindelse af alkoholindtagelse?","description":"00 Ingen psykiske symptomer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse eller obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser og seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser, andre end hallucinationer 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\nVed psykisk skadevirkning skal dette yderligere anføres i de relevante symptomsektioner under items for organisk årsag til symptomerne.\n","input":"string","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.019},{"key":"11.020","title":"Omstændigheder i forbindelse med udviklingen af alkoholbetingede","description":"psykiske problemer\n","input":"integer","options":{"0":"Ingen symptomer som følge af alkoholforbrug","1":"Symptomer udviklet under brugen eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer opstået i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- eller abstinenstilstand","8":"Usikkert"},"validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":11.02},{"key":"11.021","title":"Påvirkning af aktiviteter på grund af alkoholbetingede symptomer","input":"integer","options":{"0":"Ingen alkoholbetingede psykiske symptomer","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Usikkert"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":11.021},{"key":"11.022","title":"Vedvarende alkoholforbrug efter erkendt psykisk skadevirkning","description":"Har du fortsat med at drikke efter at du blev klar over, at dine psykiske symptomer havde forbindelse med dit alkoholforbrug?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.022},{"key":"11.023","title":"Fysiske helbredsproblemer som følge af alkoholproblemer","description":"Har du haft problemer med dit fysiske helbred på grund af alkoholforbrug?\n? Fx leverbeskadigelse, bugspytkirtelbetændelse, blodige opkastninger, føle-forstyrrelser eller smerter i hænder eller fødder, gangbesvær?\n","input":"integer","options":{"0":"Ingen fysiske helbredsproblemer","1":"Lette fysiske symptomer","2":"Moderate symptomer","3":"Livstruende tilstand"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.023},{"key":"11.024","title":"Vedvarende alkoholforbrug efter erkendt fysisk skadevirkning","description":"Har du fortsat med at drikke, selv efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.024},{"key":"11.025","title":"Alkohol-abstinenssymptomer","description":"Har du haft abstinenssymptomer, når du holdt op med at drikke?\n? Hvad mærkede du (rysten, sveden, hjertebanken, urofølelse eller lignende)? ? Hvor ofte fx hver morgen? ? Optrådte det særligt i forbindelse med at du prøvede at nedsætte eller helt ophøre med forbruget?\n(Tachycardi, hypertension, sveden, rysten, muskelsvaghed, perceptionsforstyrrelser, kvalme eller opkastning, diarré, hovedpine, søvnløshed, rastløshed, autonom angst).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"Lette abstinenssymptomer, en gang om måneden eller oftere","2":"Moderat eller svær abstinenstilstand (med mindst 3 symptomer)","3":"Abstinenstilstand med delirium eller kramper (cf. 21.137 og 21.138)"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.025},{"key":"11.026","title":"Alkoholindtagelse for at modvirke abstinenssymptomer","description":"Har du drukket for at modvirke abstinenssymptomerne, for at få dem til at gå væk? Drikker du lige fra morgenstunden for at undgå abstinenssymptomer?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til en gang ugentlig","2":"Ja, flere gange ugentlig"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.026},{"key":"11.027","title":"Patologisk eller idiosynkratisk reaktion på alkohol (patologisk rus)","description":"Har du oplevet at alkohol har en ganske særlig virkning på dig, anderledes end hos andre?\n? På hvilken måde? ? Hvor hurtigt udvikler det sig? ? Forandrer du dig totalt, når du bare får lidt at drikke?\n","input":"integer","options":{"0":"Ingen patologisk reaktion","1":"Definitiv patologisk reaktion efter små doser alkohol","2":"Upassende social adfærd (fx seksuelle tilnærmelser)","3":"Voldelig eller aggressiv adfærd","4":"Både upassende og voldelig eller aggressiv adfærd","8":"Usikkert"},"validate":["0-4",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.027},{"key":"11.028","title":"Akut alkoholintoksikation, ukompliceret","description":"Har du været rigtig beruset?\n? Hvad skete der? Hvordan opførte du dig?\n","input":"integer","options":{"0":"Ingen intoksikationssymptomer","1":"Lette intoksikationssymptomer","2":"Amnesi uden andre toksiske virkninger","3":"Moderate neurologiske toksiske virkninger uden amnesi","4":"Moderate neurologiske toksiske virkninger med amnesi","5":"Svære toksiske virkninger uden amnesi (cf. 11.025 og 11.031)","6":"Svære toksiske virkninger med amnesi (cf. 11.025 og 11.031)","8":"Usikkert","9":"Ukendt"},"validate":["0-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.028},{"key":"11.029","title":"Fysiske traumer eller kvæstelser som følge af alkoholforbrug","description":"Er du kommet til skade i forbindelse med alkoholindtagelse? - Hvordan?\n","input":"integer","options":{"0":"Ingen skader","1":"Lette skader","2":"Moderate skader","3":"Svære skader"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.029,"ratingscale":1},{"key":"11.030","title":"Hæmatemese, aspiration af opkast, andre komplikationer","description":"Har du haft andre fysiske problemer, fx opkastninger, måske blodige opkastninger?\n","input":"integer","options":{"0":"Ingen","1":"Lette","2":"Moderate","3":"Svære"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.03,"ratingscale":1},{"key":"11.031","title":"Bevidsthedsændring eller perceptionsforstyrrelse som følge af alkohol","description":"Har du haft besvimelses- eller bevidsthedstab som følge af alkoholforbruget? Har du haft mærkelige oplevelser, hvor du kunne se eller høre noget, der ikke var der?\n","input":"integer","options":{"0":"Ikke til stede","1":"Perceptionsforstryrrelser","2":"Delirium","3":"Koma","4":"Koma med epileptiske kramper"},"validate":["0-4",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.031},{"key":"11.032","title":"Bestående alkoholforbrug","description":"Har du fået hjælp til at afbryde alkoholforbruget eller har du fortsat drukket?\n","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af antabus","3":"Abstinent under tilsyn (fx på alkoholklinik)","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt alkoholforbrug","6":"Uregelmæssigt alkoholforbrug","7":"Turevist svært alkoholforbrug"},"validate":["1-7",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":11.032},{"key":"11.033","title":"Begyndelsesalder for alkoholafhængighedstilstand","input":"string","validate":["0-99"],"glossary":11.033},{"key":"11.034","title":"Seneste alder for alkoholafhængighedstilstand","input":"string","validate":["0-99"],"glossary":11.034},{"key":"11.035","title":"Begyndelsesalder for skadeligt brug af alkohol (alcohol abuse)","input":"string","validate":["0-99"],"glossary":11.035},{"key":"11.036","title":"Seneste alder for skadeligt brug af alkohol (alcohol abuse)","input":"string","validate":["0-99"],"episodes":1,"glossary":11.036},{"key":"11.037","title":"Påvirkning af aktiviteter som følge af alkoholforbrug","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":11.037},{"key":"12.001","title":"Brug af psykoaktive substanser andre end alkohol","description":"NB! Tidsperioden er det sidste år (PY) rated i venstre box eller øverste box-række, eller livsperioden forud for det sidste år (LB), som rates i højre box eller underste box-række.\n12_001 Brug af psykoaktive substanser\nHar du brugt medikamenter eller stoffer? - Hvor ofte?\n","input":"integer","options":{"1":"Har aldrig brug stoffer eller medikamenter","2":"Har brugt stoffer eller medikamenter én eller to gange i løbet af sit liv","3":"Har brugt stoffer eller medikamenter oftere end (2) CUT OFF => til 13.001 hvis 12.001 rates 1 eller 2."},"section_start":"Section_12","validate":["1-3",8,9],"glossary":12.001},{"key":"12.002","title":"Receptordinerede opioider","description":"Har du taget recept-ordinerede medikamenter fx beroligende tabletter, sovetabletter, smertestillende midler eller opkvikkende midler, afmagringstabletter eller lignende?\n? Hvilke tabletter? Hvor mange om dagen? Hvor længe? ? Hvad blev du behandlet for af lægen?\n","input":"integer","options":{"0":"Intet brug","1":"Op til 5 gange inden for et år","2":"6-12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang om ugen","5":"Daglig, på et eller andet tidspunkt inden for et år","6":"Daglig, gentagne gange inden for et år","7":"Daglig, igennem mindst en måned","8":"Usikkert"},"validate":["0-9"],"episodes":1,"glossary":12.002},{"key":"12.003","title":"Receptordinerede sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Intet brug","1":"Op til 5 gange inden for et år","2":"6-12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang om ugen","5":"Daglig, på et eller andet tidspunkt inden for et år","6":"Daglig, gentagne gange inden for et år","7":"Daglig, igennem mindst en måned","8":"Usikkert"},"validate":["0-9"],"episodes":1,"glossary":12.003},{"key":"12.004","title":"Receptordinerede centralstimulerende medikamenter","input":"integer","options":{"0":"Intet brug","1":"Op til 5 gange inden for et år","2":"6-12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang om ugen","5":"Daglig, på et eller andet tidspunkt inden for et år","6":"Daglig, gentagne gange inden for et år","7":"Daglig, igennem mindst en måned","8":"Usikkert"},"validate":["0-9"],"episodes":1,"glossary":12.004},{"key":"12.005","title":"Brug af medikamenter som ordineret, eller ud over det ordinerede","description":"Har du brugt medikamenterne i større doser eller gennem længere tid end ordineret?\n? Har du fået ekstra forsyning fra andre(familie, venner, det sorte marked)?\n","input":"integer","options":{"1":"Har brugt medikamentet som ordineret","2":"Har brugt medikamentet i større doser eller for længere tid end ordineret","3":"Har brugt medikamenter/stoffer ved siden af det ordinerede","4":"Forbruget rent illegalt SKIP => til 12.022 hvis medikamenterne kun er brugt som ordineret (rating 1)"},"validate":["1-4",8,9],"episodes":1,"glossary":12.005},{"key":"12.006","title":"Opioider (1) BRUG AF STOFFER ELLER MEDIKAMENTER UDEN FOR ORDINATION","description":"Hvilke stoffer har du brugt? Hvor meget? Hvor længe? Hvordan tager du dem (som tabletter, ved rygning, indsnusning eller indsprøjtning)? Hvordan virker det på dig? Hvorfor bruger du det? Har du svært ved at undvære det? Har det givet anledning til problemer? Er det gået ud over dit helbred eller dine aktiviteter? Har du brugt mere tidligere hen end nu? Hvor gammel var du da du begyndte at bruge det? Rating af hyppigheden af brugen:\n","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.006},{"key":"12.007","title":"Cannabinoider (2)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.007},{"key":"12.008","title":"Sedativa, hypnotika, anxiolytika (3)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.008},{"key":"12.009","title":"Kokain (4)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.009},{"key":"12.010","title":"Centralstimulerende stoffer (5)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.01},{"key":"12.011","title":"Hallucinogener (6)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.011},{"key":"12.012","title":"Phencyclidin (PCP) (7)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.012},{"key":"12.013","title":"Flygtige opløsningsmidler (8)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.013},{"key":"12.014","title":"Andre eller ukendte (9)","input":"integer","options":{"0":"Ingen brug","1":"Op til 5 gange inden for et år","2":"6 til 12 gange inden for et år eller mindre end én gang månedlig","3":"Cirka én gang månedlig","4":"Cirka én gang ugentlig","5":"Dagligt, på et eller andet tidspunkt i løbet af et år","6":"Dagligt, gentagne gange inden for et år","7":"Dagligt, igennem mindst én måned"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"validate":["0-9"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.014,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0151","title":"Seneste brug (Opioider)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0152","title":"Seneste brug (Cannabinoider)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0153","title":"Seneste brug (Sedativa, hypnotika, anxiolytika)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0154","title":"Seneste brug (Kokain)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0155","title":"Seneste brug (Centralstimulerende stoffer)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0156","title":"Seneste brug (Hallucinogener)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0157","title":"Seneste brug (Phencyclidin (PCP)))","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0158","title":"Seneste brug (Flygtige opløsningsmidler)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015},{"key":"12.0159","title":"Seneste brug (Andre ukendte)","description":"Hvornår har du sidst fået noget af stoffet/medikamentet?\n","input":"integer","options":{"0":"Ingen brug","1":"Inden for de sidste 24 timer","2":"For 24-72 timer siden","3":"Fra 4-7 dage siden","4":"Mellem for en uge og en måned siden","5":"Mellem for en måned og et halvt år siden","6":"Mellem for et halvt og et helt år siden","7":"For mere end et år siden","8":"Usikkert","9":"Ikke anvendelig"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.015,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0161","title":"ADMINISTRATIONSMÅDE","description":"12.016.1 Per os\nOpioider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0162","title":"ADMINISTRATIONSMÅDE","description":"12.016.2 Per os\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0163","title":"ADMINISTRATIONSMÅDE","description":"12.016.3 Per os\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0164","title":"ADMINISTRATIONSMÅDE","description":"12_016_4 Per os\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0165","title":"ADMINISTRATIONSMÅDE","description":"12_016_5 Per os\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0166","title":"ADMINISTRATIONSMÅDE","description":"12_016_6 Per os\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0167","title":"ADMINISTRATIONSMÅDE","description":"12_016_7 Per os\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0168","title":"ADMINISTRATIONSMÅDE","description":"12_016_8 Per os\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0169","title":"ADMINISTRATIONSMÅDE","description":"12_016_9 Per os\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0171","title":"Ved intravenøs injektion","description":"Opioider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0172","title":"ADMINISTRATIONSMÅDE","description":"12_017_2 Ved intravenøs injektion\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0173","title":"ADMINISTRATIONSMÅDE","description":"12_017_3 Ved intravenøs injektion\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0174","title":"ADMINISTRATIONSMÅDE","description":"12_017_4 Ved intravenøs injektion\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0175","title":"ADMINISTRATIONSMÅDE","description":"12_017_5 Ved intravenøs injektion\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0176","title":"ADMINISTRATIONSMÅDE","description":"12_017_6 Ved intravenøs injektion\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0177","title":"ADMINISTRATIONSMÅDE","description":"12_017_7 Ved intravenøs injektion\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0178","title":"ADMINISTRATIONSMÅDE","description":"12_017_8 Ved intravenøs injektion\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0179","title":"ADMINISTRATIONSMÅDE","description":"12_017_9 Ved intravenøs injektion\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0181","title":"Ved subkutan eller intramuskulær injektion","description":"Opioider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0182","title":"ADMINISTRATIONSMÅDE","description":"12_018_2 Ved subkutan eller intramuskulær injektion\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0183","title":"ADMINISTRATIONSMÅDE","description":"12_018_3 Ved subkutan eller intramuskulær injektion\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0184","title":"ADMINISTRATIONSMÅDE","description":"12_018_4 Ved subkutan eller intramuskulær injektion\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0185","title":"ADMINISTRATIONSMÅDE","description":"12_018_5 Ved subkutan eller intramuskulær injektion\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0186","title":"ADMINISTRATIONSMÅDE","description":"12_018_6 Ved subkutan eller intramuskulær injektion\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0187","title":"ADMINISTRATIONSMÅDE","description":"12_018_7 Ved subkutan eller intramuskulær injektion\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0188","title":"ADMINISTRATIONSMÅDE","description":"12_018_8 Ved subkutan eller intramuskulær injektion\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0189","title":"ADMINISTRATIONSMÅDE","description":"12_018_9 Ved subkutan eller intramuskulær injektion\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0191","title":"Ved rygning Opioider","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0192","title":"ADMINISTRATIONSMÅDE","description":"12_019_2 Ved rygning\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0193","title":"ADMINISTRATIONSMÅDE","description":"12_019_3 Ved rygning\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0194","title":"ADMINISTRATIONSMÅDE","description":"12_019_4 Ved rygning\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0195","title":"ADMINISTRATIONSMÅDE","description":"12_019_5 Ved rygning\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0196","title":"ADMINISTRATIONSMÅDE","description":"12_019_6 Ved rygning\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0197","title":"ADMINISTRATIONSMÅDE","description":"12_019_7 Ved rygning\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0198","title":"ADMINISTRATIONSMÅDE","description":"12_019_8 Ved rygning\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0199","title":"ADMINISTRATIONSMÅDE","description":"12_019_9 Ved rygning\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0201","title":"Ved indsnusning eller sniffing Opioider","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0202","title":"ADMINISTRATIONSMÅDE","description":"12_020_2 Ved indsnusning eller sniffing\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0203","title":"ADMINISTRATIONSMÅDE","description":"12_020_3 Ved indsnusning eller sniffing\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0204","title":"ADMINISTRATIONSMÅDE","description":"12_020_4 Ved indsnusning eller sniffing\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0205","title":"ADMINISTRATIONSMÅDE","description":"12_020_5 Ved indsnusning eller sniffing\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0206","title":"ADMINISTRATIONSMÅDE","description":"12_020_6 Ved indsnusning eller sniffing\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0207","title":"ADMINISTRATIONSMÅDE","description":"12_020_7 Ved indsnusning eller sniffing\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0208","title":"ADMINISTRATIONSMÅDE","description":"12_020_8 Ved indsnusning eller sniffing\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0209","title":"ADMINISTRATIONSMÅDE","description":"12_020_9 Ved indsnusning eller sniffing\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0211","title":"ADMINISTRATIONSMÅDE","description":"12_021_1 Ved anden anvendelsesmåde Opioider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0212","title":"ADMINISTRATIONSMÅDE","description":"12_021_2 Ved anden anvendelsesmåde\nCannabinoider\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0213","title":"ADMINISTRATIONSMÅDE","description":"12_021_3 Ved anden anvendelsesmåde\nSedativa, hypnotika, anxiolytika\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0214","title":"ADMINISTRATIONSMÅDE","description":"12_021_4 Ved anden anvendelsesmåde\nKokain\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0215","title":"ADMINISTRATIONSMÅDE","description":"12_021_5 Ved anden anvendelsesmåde\nCentralstimulerende stoffer\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0216","title":"ADMINISTRATIONSMÅDE","description":"12_021_6 Ved anden anvendelsesmåde\nHallucinogener\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0217","title":"ADMINISTRATIONSMÅDE","description":"12_021_7 Ved anden anvendelsesmåde\nPhencyclidin (PCP)\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0218","title":"ADMINISTRATIONSMÅDE","description":"12_021_8 Ved anden anvendelsesmåde\nFlygtige opløsningsmidler\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016},{"key":"12.0219","title":"ADMINISTRATIONSMÅDE","description":"12_021_9 Ved anden anvendelsesmåde\nAndre eller ukendte\n","input":"integer","options":{"0":"Måden ikke anvendt","1":"Måden anvendt, men er ikke den sædvanlige","2":"Sædvanlige anvendelsesmåde"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.016,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0221","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_1 Subjektiv trang Opoider\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0222","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_2 Subjektiv trang Cannabinoider\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0223","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_3 Subjektiv trang Sedative, hypnotika, anxiolytika\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0224","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_4 Subjektiv trang Kokain\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0225","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_5 Subjektiv trang Centralstimulerende stoffer\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0226","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_6 Subjektiv trang Hallucinogener\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0227","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_7 Subjektiv trang Phencyclidin (PCP)\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0228","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_8 Subjektiv trang Flygtige opløsningsmidler\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022},{"key":"12.0229","title":"ITEMS FOR SKADELIGT BRUG (ABUSUS) OG AFHÆNGIGHED","description":"12_022_9 Subjektiv trang Andre eller ukendte\nHar du følt, at du absolut måtte have stoffet/medikamentet og ikke kunne klare dig uden?\n? Hvor stærkt føltes det? ? Har du følt dig utilpas ved lejligheder, hvor du ikke kunne få fat på det? ? Var du ved sådanne lejligheder plaget af så stærk trang, at du ikke kunne tænke på andet?\n?? Har du haft en sådan trang inden for det sidste år?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Følelse af utilpashed og bevidsthed om trangen, men ikke (2)","2":"Stærk og pågående trang"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.022,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0231","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Opioider","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0232","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Cannabinoider","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0233","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Sedativa, hypnotika, anxiolytika","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0234","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Kokain","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0235","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Centralstimulerende stoffer","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0236","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Hallucinogener","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0237","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Phencyclidin (PCP)","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0238","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Flygtige opløsningsmidler","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023},{"key":"12.0239","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen Andre eller ukendte","description":"Har du for nylig eller eventuelt tidligere prøvet at nedskære forbruget eller helt ophøre uden at kunne gennemføre det?\n? Har du kunnet undlade brugen i nogle dage ind imellem fx i weekender eller når du var alene?\n? Har du været i stand til at nedsætte brugen eller helt ophøre med den gennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at nedsætte eller ophøre med brugen","1":"Har været i stand til at nedsætte eller ophøre med brugen igennem en måned eller mere","2":"Har ikke været i stand til at nedsætte eller ophøre med brugen igennem så lang tid som en måned"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.023,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0241","title":"Nedsat evne til at styre brugen, når den først er begyndt Opioider","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0242","title":"Nedsat evne til at styre brugen, når den først er begyndt Cannabinoider","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0243","title":"Nedsat evne til at styre brugen, når den først er begyndt Sedativa, hypnotika, anxiolytika","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0244","title":"Nedsat evne til at styre brugen, når den først er begyndt Kokain","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0245","title":"Nedsat evne til at styre brugen, når den først er begyndt Centralstimulerende stoffer","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0246","title":"Nedsat evne til at styre brugen, når den først er begyndt Hallucinogener","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0247","title":"Nedsat evne til at styre brugen, når den først er begyndt Phencyclidin (PCP)","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0248","title":"Nedsat evne til at styre brugen, når den først er begyndt Flygtige opløsningsmidler","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024},{"key":"12.0249","title":"Nedsat evne til at styre brugen, når den først er begyndt Andre eller ukendte","description":"Sker det, at du bruger mere end du havde regnet med da du begyndte?\n? Har du haft svært ved at standse brugen, når du først var begyndt? ? Har du fortsat brugen igennem flere dage uden at standse op?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at styre brugen","1":"Somme tider vanskeligheder ved at styre brugen","2":"Bruger sædvanligvis mere end tilsigtet","3":"Udtalt vanskelighed ved at styre brugen (fortsætter i dagevis)"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.024,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0251","title":"Sociale problemer som følge af brugen Opioider","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0252","title":"Sociale problemer som følge af brugen Cannabinoider","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0253","title":"Sociale problemer som følge af brugen Sedativa, hypnotika, anxiolytika","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0254","title":"Sociale problemer som følge af brugen Kokain","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0255","title":"Sociale problemer som følge af brugen Centralstimulerende stoffer","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0256","title":"Sociale problemer som følge af brugen Hallucinogener","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0257","title":"Sociale problemer som følge af brugen Phencyclidin (PCP)","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0258","title":"Sociale problemer som følge af brugen Flygtige opløsningsmidler","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025},{"key":"12.0259","title":"Sociale problemer som følge af brugen Andre eller ukendte","description":"Har brugen ført til problemer i forhold til familien, vennerne eller arbejdet (uddannelsen eller studiet)? Ved flere lejligheder?\n? Er du kommet op at skændes med andre? Er du kommet for sent på arbejde eller haft fravær? ? Har det forårsaget økonomiske problemer? ? Har du haft alvorligere problemer? Mistet ægtefælle eller samlever, en god ven eller dit arbejde?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.025,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0261","title":"Retslige problemer som følge af brugen Opioider Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0262","title":"Retslige problemer som følge af brugen Cannabinoider Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0263","title":"Retslige problemer som følge af brugen Sedativa, hypnotika, anxilytika Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0264","title":"Retslige problemer som følge af brugen Kokain Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0265","title":"Retslige problemer som følge af brugen Centralstimulerende stoffer Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0266","title":"Retslige problemer som følge af brugen Hallucinogener Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0267","title":"Retslige problemer som følge af brugen Phencyclidin (PCP) Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0268","title":"Retslige problemer som følge af brugen Flygtige opløsningsmidler Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026},{"key":"12.0269","title":"Retslige problemer som følge af brugen Andre eller ukendte Har brugen medført retslige problemer? - Ved mere end én lejlighed?","description":"? Slagsmål, voldsepisoder? ? Kontakt med politiet? ? Har du fået en dom som følge af brugen?\n","input":"integer","options":{"0":"Ingen problemer","1":"Lette problemer","2":"Moderate problemer","3":"Alvorlige problemer SKIP => til 12.028 hvis 12.026 alle = 0"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.026,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0271","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Opioider","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0272","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Cannabinoider","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0273","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Sedativa, hypnotika, anxiolytika","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0274","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Kokain","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0275","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Centralstimulerende stoffer","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0276","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Hallucinogener","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0277","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Phencyclidin (PCP)","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0278","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Flygtige opløsningsmidler","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027},{"key":"12.0279","title":"Vedvarende brug efter erkendt social eller retslig skadevirkning Andre eller ukendte","description":"Har du fortsat brugen, selvom du vidste at det havde medført et af disse problemer?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.027,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0281","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Opioider","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0282","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Cannabinoider","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0283","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Sedativa, hypnotika, anxiolytika","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0284","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Kokain","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0285","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Centralstimulerende stoffer","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0286","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Hallucinogener","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0287","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Phencyclidin (PCP)","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0288","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Flygtige opløsningsmidler","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028},{"key":"12.0289","title":"Svigten med hensyn til fremtrædende rolleforpligtelser Andre eller ukendte","description":"Har du på grund af brugen forsømt at passe det, man ventede af dig, fx dit arbejde eller din uddannelse, børnepasning, vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen svigten","1":"Nogle forpligtelser svigtet","2":"De fleste forpligtelser svigtet","3":"Praktisk talt alle forpligtelser svigtet"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.028,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0291","title":"Risikofyldt adfærd i forbindelse med brugen Opioider","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0292","title":"Risikofyldt adfærd i forbindelse med brugen Cannabinoider","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0293","title":"Risikofyldt adfærd i forbindelse med brugen Sedativa, hypnotika, anxiolytika","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0294","title":"Risikofyldt adfærd i forbindelse med brugen Kokain","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0295","title":"Risikofyldt adfærd i forbindelse med brugen Centralstimulerende stoffer","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0296","title":"Risikofyldt adfærd i forbindelse med brugen Hallucinogener","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0297","title":"Risikofyldt adfærd i forbindelse med brugen Phencyclidin (PCP)","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0298","title":"Risikofyldt adfærd i forbindelse med brugen Flygtige opløsningsmidler","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029},{"key":"12.0299","title":"Risikofyldt adfærd i forbindelse med brugen Andre eller ukendte","description":"Har du brugt stoffer/medikamenter i situationer hvor det kunne medføre en risiko for dig selv eller andre ? Ved flere lejligheder?\n","input":"integer","options":{"0":"Ingen risikofyldt adfærd","1":"Gentagne tilfælde med risikofyldt adfærd men uden at der skete skade","2":"Gentagne tilfælde med risikofyldt adfærd med skade på sig selv eller andre CUT OFF => til 13.001 hvis hyppigste brug var mindre end én gang om måneden og 12.022-12.029 alle rates 0."},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.029,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0301","title":"Prioritering af aktiviteter i forbindelse med brugen Opioider Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0302","title":"Prioritering af aktiviteter i forbindelse med brugen Cannabinoider Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0303","title":"Prioritering af aktiviteter i forbindelse med brugen Sedativa, hypnotika, anxiolytika Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0304","title":"Prioritering af aktiviteter i forbindelse med brugen Kokain Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0305","title":"Prioritering af aktiviteter i forbindelse med brugen Centralstimulerende stoffer Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0306","title":"Prioritering af aktiviteter i forbindelse med brugen Hallucinogener Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"string","ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0307","title":"Prioritering af aktiviteter i forbindelse med brugen Phencyclidin (PCP) Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0308","title":"Prioritering af aktiviteter i forbindelse med brugen Flygtige opløsningsmidler Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03},{"key":"12.0309","title":"Prioritering af aktiviteter i forbindelse med brugen Andre eller ukendte Hvor vigtig er brugen sammenlignet med andre aktiviteter?","description":"? Betyder det, at brugen går forud for alt andet? ? Har du brugt flere penge på brugen end på din familie? ? Er brugen gået forud for arbejdet , vigtige aftaler eller aktiviteter?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere end andet","2":"Prioriteres altid højest"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.03,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0311","title":"Tid anvendt på aktiviteter i forbindelse med brugen Opioider","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0312","title":"Tid anvendt på aktiviteter i forbindelse med brugen Cannabinoider","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0313","title":"Tid anvendt på aktiviteter i forbindelse med brugen Sedativa, hypnotika, anxiolytika","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0314","title":"Tid anvendt på aktiviteter i forbindelse med brugen Kokain","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0315","title":"Tid anvendt på aktiviteter i forbindelse med brugen Centralstimulerende stoffer","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0316","title":"Tid anvendt på aktiviteter i forbindelse med brugen Hallucinogener","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0317","title":"Tid anvendt på aktiviteter i forbindelse med brugen Phencyclidin (PCP)","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0318","title":"Tid anvendt på aktiviteter i forbindelse med brugen Flygtige opløsningsmidler","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031},{"key":"12.0319","title":"Tid anvendt på aktiviteter i forbindelse med brugen Andre eller ukendte","description":"Har du anvendt megen tid på brugen eller på at komme dig efter brugen?\n? Så meget, at det er gået ud over andre aktiviteter gennem en måned eller mere?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"En del tidsforbrug men ikke (2)","2":"Brugt størstedelen af tiden daglig, gennem mindst en måned"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.031,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0321","title":"Indsnævret rutinepræg som følge af brugen Opioider Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0322","title":"Indsnævret rutinepræg som følge af brugen Cannabinoider Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0323","title":"Indsnævret rutinepræg som følge af brugen Sedativa, hypnotika, anxiolytika Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0324","title":"Indsnævret rutinepræg som følge af brugen Kokain Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0325","title":"Indsnævret rutinepræg som følge af brugen Centralstimulerende stoffer Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0326","title":"Indsnævret rutinepræg som følge af brugen Hallucinogener Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0327","title":"Indsnævret rutinepræg som følge af brugen Phencyclidin (PCP) Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0328","title":"Indsnævret rutinepræg som følge af brugen Flygtige opløsningsmidler Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032},{"key":"12.0329","title":"Indsnævret rutinepræg som følge af brugen Andre eller ukendte Har du udviklet et fast mønster for brugen, som du følger hver dag, også i weekenden? Har du svært ved at bryde mønstret?","input":"integer","options":{"0":"Ingen indsnævring","1":"Nogen indsnævring","2":"Udtalt indsnævring, fast mønster med stereotype rutiner"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"glossary":12.032,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0331","title":"Toleransudvikling Opioider Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0332","title":"Toleransudvikling Cannabinoider Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0333","title":"Toleransudvikling Sedativa, hypnotika, anxiolytika Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0334","title":"Toleransudvikling Kokain Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0335","title":"Toleransudvikling Centralstimulerende stoffer Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0336","title":"Toleransudvikling Hallucinogener Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0337","title":"Toleransudvikling Phencyclidin (PCP) Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0338","title":"Toleransudvikling Flygtige opløsningsmidler Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033},{"key":"12.0339","title":"Toleransudvikling Andre eller ukendte Virker stoffet/medikamentet mindre stærkt på dig end tidligere? Behøver du mere af stoffet for at opnå samme virkning?","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen, men ikke udtalt tolerans","2":"Udtalt tolerans (mindst 50% mere end tidligere)","3":"Omvendt tolerans"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.033,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0341","title":"Psykiske problemer som følge af brugen Opioider Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0342","title":"Psykiske problemer som følge af brugen Cannabinoider Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0343","title":"Psykiske problemer som følge af brugen Sedativa, hypnotika, anxiolytika Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0344","title":"Psykiske problemer som følge af brugen Kokain Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0345","title":"Psykiske problemer som følge af brugen Centralstimulerende stoffer Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0346","title":"Psykiske problemer som følge af brugen Hallucinogener Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0347","title":"Psykiske problemer som følge af brugen Phencyclidin (PCP) Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0348","title":"Psykiske problemer som følge af brugen Flygtige opløsningsmidler Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse\n","input":"string","ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034},{"key":"12.0349","title":"Psykiske problemer som følge af brugen Andre eller ukendte Har dine symptomer forbindelse med brugen? Forværres dine symptomer i forbindelse med brugen?","description":"00 Ingen psykiske problemer 01 Nedsat dømmekraft og uhensigtsmæssig adfærd 02 Anxiøse og obsessive-kompulsive symptomer 03 Depressive symptomer inkl. psykotisk depression 04 Søvnforstyrrelser, seksuel dysfunktion 05 Maniske symptomer inkl. bipolar affektiv sindslidelse 06 Perceptionsforstyrrelser 07 Hallucinationer, særligt auditive 08 Sektion 18 oplevelser eller bizarre vrangforestillinger 09 Non-affektive vrangforestillinger inkl. sygelig jalousi 10 Polymorfe symptomer 11 Blandede symptomer 12 Kognitiv svækkelse SKIP => til 12.038 hvis 12.034 alle = 0.\n","input":"string","routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-12"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.034,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0351","title":"Udviklingen af psykiske problemer i forbindelse med brugen Opioider","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0352","title":"Udviklingen af psykiske problemer i forbindelse med brugen Cannabinoider","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0353","title":"Udviklingen af psykiske problemer i forbindelse med brugen Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0354","title":"Udviklingen af psykiske problemer i forbindelse med brugen Kokain","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0355","title":"Udviklingen af psykiske problemer i forbindelse med brugen Centralstimulerende stoffer","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0356","title":"Udviklingen af psykiske problemer i forbindelse med brugen Hallucinogener","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0357","title":"Udviklingen af psykiske problemer i forbindelse med brugen Phencyclidin (PCP)","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0358","title":"Udviklingen af psykiske problemer i forbindelse med brugen Flygtige opløsningsmidler","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035},{"key":"12.0359","title":"Udviklingen af psykiske problemer i forbindelse med brugen Andre eller ukendte","input":"integer","options":{"0":"Ingen symptomer","1":"Symptomer udviklet under brug eller inden for få dage efter ophør, men ikke (5)","2":"Symptomer udviklet i forbindelse med intoksikation, men ikke (5)","3":"Symptomer udviklet i forbindelse med abstinenstilstand, men ikke (5)","4":"Symptomer udviklet i forbindelse med blandet intoksikations- og abstinenstilstand, men ikke (5)","5":"Vedvarende symptomer efter ophør af intoksikations- og abstinenstilstand","8":"Uvist","9":"Ukendt"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-5",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.035,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0361","title":"Påvirkning af aktiviteter på grund af brug Opioider","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0362","title":"Påvirkning af aktiviteter på grund af brug Cannabinoider","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0363","title":"Påvirkning af aktiviteter på grund af brug Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0364","title":"Påvirkning af aktiviteter på grund af brug Kokain","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0365","title":"Påvirkning af aktiviteter på grund af brug Centralstimulerende stoffer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0366","title":"Påvirkning af aktiviteter på grund af brug Hallucinogener","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0367","title":"Påvirkning af aktiviteter på grund af brug Phencyclidin (PCP)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0368","title":"Påvirkning af aktiviteter på grund af brug Flygtige opløsningsmidler","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036},{"key":"12.0369","title":"Påvirkning af aktiviteter på grund af brug Andre eller ukendte","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun ringe påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning","8":"Uvist","9":"Ukendt"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.036,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0371","title":"Vedvarende brug efter erkendt psykisk skadevirkning Opioider","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0372","title":"Vedvarende brug efter erkendt psykisk skadevirkning Cannabinoider","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0373","title":"Vedvarende brug efter erkendt psykisk skadevirkning Sedativa, hypnotika, anxiolytika","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0374","title":"Vedvarende brug efter erkendt psykisk skadevirkning Kokain","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0375","title":"Vedvarende brug efter erkendt psykisk skadevirkning Centralstimulerende stoffer","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0376","title":"Vedvarende brug efter erkendt psykisk skadevirkning Hallucinogener","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0377","title":"Vedvarende brug efter erkendt psykisk skadevirkning Phencyclidin (PCP)","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0378","title":"Vedvarende brug efter erkendt psykisk skadevirkning Flygtige opløsningsmidler","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037},{"key":"12.0379","title":"Vedvarende brug efter erkendt psykisk skadevirkning Andre eller ukendte","description":"Har du fortsat brugen efter, at du blev klar over, at dine psykiske problemer havde forbindelse med brugen?\n? Hvornår fandt du ud af det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.037,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0381","title":"Fysiske helbredsproblemer som følge af brugen Opioider Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0382","title":"Fysiske helbredsproblemer som følge af brugen Cannabinoider Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0383","title":"Fysiske helbredsproblemer som følge af brugen Sedativa, hypnotika, anxiolytika Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0384","title":"Fysiske helbredsproblemer som følge af brugen Kokain Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0385","title":"Fysiske helbredsproblemer som følge af brugen Centralstimulerende stoffer Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0386","title":"Fysiske helbredsproblemer som følge af brugen Hallucinogener Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0387","title":"Fysiske helbredsproblemer som følge af brugen Phencyclidin (PCP) Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0388","title":"Fysiske helbredsproblemer som følge af brugen Flygtige opløsningsmidler Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038},{"key":"12.0389","title":"Fysiske helbredsproblemer som følge af brugen Andre eller ukendte Har du haft problemer med dit fysiske helbred på grund af brugen?","input":"integer","options":{"0":"Ingen helbredsproblemer","1":"Lette fysiske problemer","2":"Moderate til svære symptomer","3":"Livstruende tilstand"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.038,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0391","title":"Vedvarende brug efter erkendt fysisk skadevirkning Opioider","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0392","title":"Vedvarende brug efter erkendt fysisk skadevirkning Cannabinoider","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0393","title":"Vedvarende brug efter erkendt fysisk skadevirkning Sedativa, hypnotika, anxiolytika","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0394","title":"Vedvarende brug efter erkendt fysisk skadevirkning Kokain","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0395","title":"Vedvarende brug efter erkendt fysisk skadevirkning Centralstimulerende stoffer","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0396","title":"Vedvarende brug efter erkendt fysisk skadevirkning Hallucinogener","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0397","title":"Vedvarende brug efter erkendt fysisk skadevirkning Phencyclidin (PCP)","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0398","title":"Vedvarende brug efter erkendt fysisk skadevirkning Flygtige opløsningsmidler","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039},{"key":"12.0399","title":"Vedvarende brug efter erkendt fysisk skadevirkning Andre eller ukendte","description":"Har du fortsat brugen efter at du blev klar over, at det medførte problemer med dit fysiske helbred?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.039,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0401","title":"Abstinenssymptomer Opioider Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0402","title":"Abstinenssymptomer Cannabinoider Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0403","title":"Abstinenssymptomer Sedativa, hypnotika, anxiolytika Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0404","title":"Abstinenssymptomer Kokain Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0405","title":"Abstinenssymptomer Centralstimulerende stoffer Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0406","title":"Abstinenssymptomer Hallucinogener Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0407","title":"Abstinenssymptomer Phencyclidin (PCP) Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0408","title":"Abstinenssymptomer Flygtige opløsningsmidler Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04},{"key":"12.0409","title":"Abstinenssymptomer Andre eller ukendte Har du haft abstinenssymptomer, når du holdt op med brugen?","description":"? Hvilke? Hvor ofte? ? Optrådte de i forbindelse med, at du prøvede at nedsætte eller helt ophøre med brugen? (Se Glossary for typiske abstinenssymptomer for de forskellige stofgrupper).\n","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.04,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0411","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Opioider","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"section_start":null,"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0412","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Cannabinoider","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0413","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0414","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Kokain","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0415","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Centralstimulerende stoffer","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0416","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Hallucinogener","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0417","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Phencyclidin (PCP)","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0418","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Flygtige opløsningmidler","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041},{"key":"12.0419","title":"Abstinenssymptomer i forbindelse med flere stoffer samtidigt Andre eller ukendte","input":"integer","options":{"0":"Ingen abstinenssymptomer","1":"1-2 lettere abstinenssymptomer en gang om måneden eller mere","2":"Moderate til svære abstinenssymptomer, mindst 3 samtidigt, en gang om måneden eller oftere","3":"Abstinenstilstand med delirium eller kramper"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.041,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0421","title":"Fortsat brug for at modvirke abstinenssymptomer Opioider","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0422","title":"Fortsat brug for at modvirke abstinenssymptomer Cannabinoider","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0423","title":"Fortsat brug for at modvirke abstinenssymptomer Sedativa, hypnotika, anxiolytika","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0424","title":"Fortsat brug for at modvirke abstinenssymptomer Kokain","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0425","title":"Fortsat brug for at modvirke abstinenssymptomer Centralstimulerende stoffer","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0426","title":"Fortsat brug for at modvirke abstinenssymptomer Hallucinogener","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0427","title":"Fortsat brug for at modvirke abstinenssymptomer Phencyclidin (PCP)","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0428","title":"Fortsat brug for at modvirke abstinenssymptomer Flygtige opløsningsmidler","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042},{"key":"12.0429","title":"Fortsat brug for at modvirke abstinenssymptomer Andre eller ukendte","description":"Har du taget stoffer/medikamenter for at ophæve eller for at undgå abstinenssymptomerne?\n","input":"integer","options":{"0":"Nej","1":"Ja, op til én gang ugentlig","2":"Ja, flere gange ugentlig"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.042,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0431","title":"Toksisk virkning Opioider (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0432","title":"Toksisk virkning Cannabinoider (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0433","title":"Toksisk virkning Sedativa, hypnotika, anxiolytika (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0434","title":"Toksisk virkning Kokain (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0435","title":"Toksisk virkning Centralstimulerende stoffer (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0436","title":"Toksisk virkning Hallucinogener (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0437","title":"Toksisk virkning Phencyclidin (PCP) (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0438","title":"Toksisk virkning Flygtige opløsningsmidler (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043},{"key":"12.0439","title":"Toksisk virkning Andre eller ukendte (Se Glossary for typiske intoksikationssymptomer for de forskellige stofgrupper).","input":"integer","options":{"0":"Ingen toksisk virkning","1":"Lette intoksikationssymptomer","2":"Moderate neurologiske symptomer","3":"Moderate adfærdsforstyrrelser","4":"Svære toksiske virkninger, neurologiske og adfærdsmæssige","5":"Delirium, kramper, koma"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.043,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0441","title":"Bestående brug Opioider Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0442","title":"Bestående brug Cannabinoider Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0443","title":"Bestående brug Sedativa, hypnotika, anxiolytika Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0444","title":"Bestående brug Kokain Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0445","title":"Bestående brug Centralstimulerende stoffer Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0446","title":"Bestående brug Hallucinogener Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0447","title":"Bestående brug Phencyclidin (PCP) Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0448","title":"Bestående brug Flygtige opløsningsmidler Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044},{"key":"12.0449","title":"Bestående brug Andre eller ukendte Har du fået hjælp til at ophøre med brugen?","input":"integer","options":{"1":"Abstinent på grund af indlæggelse","2":"Abstinent på grund af aversiv eller blokerende medikamentel behandling","3":"Abstinent under tilsyn","4":"Abstinent uden tilsyn","5":"Fortsat regelmæssigt brug","6":"Fortsæt uregelmæssigt brug"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["1-6",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.044,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0451","title":"Begyndelsesalder for afhængighedstilstand Opioider","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0452","title":"Begyndelsesalder for afhængighedstilstand Cannabinoider","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0453","title":"Begyndelsesalder for afhængighedstilstand Sedativa, hypnotika, anxiolytika 0 Afhængighedstilstand ikke til stede","input":"string","ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0454","title":"Begyndelsesalder for afhængighedstilstand Kokain 0 Afhængighedstilstand ikke til stede","input":"string","ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0455","title":"Begyndelsesalder for afhængighedstilstand Centralstimulerende stoffer","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0456","title":"Begyndelsesalder for afhængighedstilstand Hallucinogener","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0457","title":"Begyndelsesalder for afhængighedstilstand Phencyclidin (PCP)","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0458","title":"Begyndelsesalder for afhængighedstilstand Flygtige opløsningsmidler","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-99"],"glossary":12.045},{"key":"12.0459","title":"Begyndelsesalder for afhængighedstilstand Andre eller ukendte","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-99"],"glossary":12.045,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0461","title":"Seneste alder for afhængighedstilstand Opioider","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0462","title":"Seneste alder for afhængighedstilstand Cannabinoider","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0463","title":"Seneste alder for afhængighedstilstand Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0464","title":"Seneste alder for afhængighedstilstand Kokain","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0465","title":"Seneste alder for afhængighedstilstand Centralstimulerende stoffer","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0466","title":"Seneste alder for afhængighedstilstand Hallucinogener","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0467","title":"Seneste alder for afhængighedstilstand Phencyclidin (PCP)","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0468","title":"Seneste alder for afhængighedstilstand Flygtige opløsningsmidler","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-99"],"glossary":12.046},{"key":"12.0469","title":"Seneste alder for afhængighedstilstand Andre eller ukendte","input":"integer","options":{"0":"Afhængighedstilstand ikke til stede"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-99"],"glossary":12.046,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0471","title":"Begyndelsealder for skadeligt brug (substance abuse) Opioider","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0472","title":"Begyndelsealder for skadeligt brug (substance abuse) Cannabinoider","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0473","title":"Begyndelsealder for skadeligt brug (substance abuse) Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0474","title":"Begyndelsealder for skadeligt brug (substance abuse) Kokain","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0475","title":"Begyndelsealder for skadeligt brug (substance abuse) Centralstimulerende stoffer","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0476","title":"Begyndelsealder for skadeligt brug (substance abuse) Hallucinogener","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0477","title":"Begyndelsealder for skadeligt brug (substance abuse) Phencyclidin (PCP)","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0478","title":"Begyndelsealder for skadeligt brug (substance abuse) Flygtige opløsningsmidler","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-99"],"glossary":12.047},{"key":"12.0479","title":"Begyndelsealder for skadeligt brug (substance abuse) Andre eller ukendte","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-99"],"glossary":12.047,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0481","title":"Seneste alder for skadeligt brug (substance abuse) Opioider","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0482","title":"Seneste alder for skadeligt brug (substance abuse) Cannabinoider","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0483","title":"Seneste alder for skadeligt brug (substance abuse) Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0484","title":"Seneste alder for skadeligt brug (substance abuse) Kokain","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0485","title":"Seneste alder for skadeligt brug (substance abuse) Centralstimulerende stoffer","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0486","title":"Seneste alder for skadeligt brug (substance abuse) Hallucinogener","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0487","title":"Seneste alder for skadeligt brug (substance abuse) Phencyclidin (PCP)","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0488","title":"Seneste alder for skadeligt brug (substance abuse) Flygtige opløsningsmidler","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-99"],"glossary":12.048},{"key":"12.0489","title":"Seneste alder for skadeligt brug (substance abuse) Andre eller ukendte","input":"integer","options":{"0":"Skadeligt brug aldrig til stede"},"routing_rule":"($12_002.1=0 or MISSING($12_002.1)) and ($12_002.2=0 or MISSING($12_002.2)) and ($12_003.1=0 or MISSING($12_003.1)) and ($12_003.2=0 or MISSING($12_003.2)) and ($12_004.1=0 or MISSING($12_004.1)) and ($12_004.2=0 or MISSING($12_004.2)) and ($12_006.1=0 or MISSING($12_006.1)) and ($12_006.2=0 or MISSING($12_006.2)) and ($12_007.1=0 or MISSING($12_007.1)) and ($12_007.2=0 or MISSING($12_007.2)) and ($12_008.1=0 or MISSING($12_008.1)) and ($12_008.2=0 or MISSING($12_008.2)) and ($12_009.1=0 or MISSING($12_009.1)) and ($12_009.2=0 or MISSING($12_009.2)) and ($12_010.1=0 or MISSING($12_010.1)) and ($12_010.2=0 or MISSING($12_010.2)) and ($12_011.1=0 or MISSING($12_011.1)) and ($12_011.2=0 or MISSING($12_011.2)) and ($12_012.1=0 or MISSING($12_012.1)) and ($12_012.2=0 or MISSING($12_012.2)) and ($12_013.1=0 or MISSING($12_013.1)) and ($12_013.2=0 or MISSING($12_013.2)) and ($12_014.1=0 or MISSING($12_014.1)) and ($12_014.2=0 or MISSING($12_014.2))","goto_question":12050,"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-99"],"glossary":12.048,"algorithm":[{"AND":[{"OR":["$12.002.1 = 0","MISSING$12.002.1"]},{"OR":["$12.002.2 = 0","MISSING$12.002.2"]},{"OR":["$12.003.1 = 0","MISSING$12.003.1"]},{"OR":["$12.003.2 = 0","MISSING$12.003.2"]},{"OR":["$12.004.1 = 0","MISSING$12.004.1"]},{"OR":["$12.004.2 = 0","MISSING$12.004.2"]},{"OR":["$12.006.1 = 0","MISSING$12.006.1"]},{"OR":["$12.006.2 = 0","MISSING$12.006.2"]},{"OR":["$12.007.1 = 0","MISSING$12.007.1"]},{"OR":["$12.007.2 = 0","MISSING$12.007.2"]},{"OR":["$12.008.1 = 0","MISSING$12.008.1"]},{"OR":["$12.008.2 = 0","MISSING$12.008.2"]},{"OR":["$12.009.1 = 0","MISSING$12.009.1"]},{"OR":["$12.009.2 = 0","MISSING$12.009.2"]},{"OR":["$12.010.1 = 0","MISSING$12.010.1"]},{"OR":["$12.010.2 = 0","MISSING$12.010.2"]},{"OR":["$12.011.1 = 0","MISSING$12.011.1"]},{"OR":["$12.011.2 = 0","MISSING$12.011.2"]},{"OR":["$12.012.1 = 0","MISSING$12.012.1"]},{"OR":["$12.012.2 = 0","MISSING$12.012.2"]},{"OR":["$12.013.1 = 0","MISSING$12.013.1"]},{"OR":["$12.013.2 = 0","MISSING$12.013.2"]},{"OR":["$12.014.1 = 0","MISSING$12.014.1"]},{"OR":["$12.014.2 = 0","MISSING$12.014.2"]}]}]},{"key":"12.0491","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Opioider","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_002.1>0 or $12_002.2>0 or $12_006.1>0 or $12_006.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0492","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Cannabinoider","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_007.1>0 or $12_007.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0493","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Sedativa, hypnotika, anxiolytika","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_003.1>0 or $12_003.2>0 or $12_008.1>0 or $12_008.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0494","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Kokain","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_009.1>0 or $12_009.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0495","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Centralstimulerende stoffer","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_004.1>0 or $12_004.2>0 or $12_010.1>0 or $12_010.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0496","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Hallucinogener","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_011.1>0 or $12_011.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0497","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Phencyclidin (PCP)","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_012.1>0 or $12_012.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0498","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Flygtige opløsningsmidler","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_013.1>0 or $12_013.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.0499","title":"Påvirkning af aktiviteter som følge af brug af stoffer/medikamenter Andre eller ukendte","input":"integer","options":{"0":"Ingen påvirkning","1":"Let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"ask_this_question_if":"$12_014.1>0 or $12_014.2>0","validate":["0-3",8,9],"episodes":1,"glossary":12.049},{"key":"12.050","title":"CHECKLISTE FOR BRUG AF IKKE-DEPENDENSSKABENDE STOFFER/ MEDIKAMENTER","description":"12_050 Antidepressiva\n","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.051","title":"Laxativa","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.052","title":"Analgetika","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.053","title":"Antacida","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.054","title":"Vitaminer","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.055","title":"Diuretica","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.056","title":"Nikotin-plastre eller -tyggegummi","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.057","title":"Steroider eller hormoner","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.058","title":"Naturmedicin, alternativ medicin","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.059","title":"Andre, specificeres...","input":"integer","options":{"0":"Ingen brug","1":"Moderat påvirkning af helbred eller dagliglivsaktiviteter","2":"Svær påvirkning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.05},{"key":"12.060","title":"TOBAK - SKADELIGT BRUG OG AFHÆNGIGHEDSTILSTAND (12.060-12.107)","description":"12_060 Brug af tobak\n","input":"integer","options":{"1":"Har aldrig brugt tobak","2":"Har brugt tobak én eller to gange i løbet at sit liv","3":"Har brugt tobak oftere end (2) CUT OFF=> til 13.001 hvis 12.060 rates 1 eller 2."},"validate":["1-3",8,9],"glossary":12.06},{"key":"12.061","title":"SÆDVANLIGE DAGLIGE FORBRUG AF TOBAK, gennem det sidste år (PY - venstre box) eller livsperioden forud (LB - højre box).","description":"Hvor mange cigaretter, cigarer, piber eller andre tobaksformer har du anvendt til daglig?\n12_061 Cigaretter (antal)\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.062","title":"Cigarer (antal)","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.063","title":"Piber (antal)","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.064","title":"Skråtobak (antal skrå)","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.065","title":"Anden tobaksform (fx snustobak), specificeres...","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.066","title":"Højeste daglige tobaksforbrug","description":"12_066 Cigaretter (antal)\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.067","title":"Højeste daglige tobaksforbrug","description":"12_067 Cigarer (antal)\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.068","title":"Højeste daglige tobaksforbrug","description":"12_068 Pibetobak (antal piber)\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.069","title":"Højeste daglige tobaksforbrug","description":"12_069 Skråtobak (antal skrå)\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.070","title":"Højeste daglige tobaksforbrug","description":"12_070 Anden tobaksform (fx snustobak), specificeres...\n","input":"string","validate":["0-299"],"episodes":1,"glossary":12.06},{"key":"12.071","title":"Begyndelsesalder for brug af tobak","input":"string","validate":["0-99"],"glossary":12.06},{"key":"12.072","title":"Seneste alder for brug af tobak","input":"string","validate":["0-99"],"glossary":12.06},{"key":"12.073","title":"Subjektiv trang til tobak","description":"Har du følt, at du absolut måtte ryge eller bruge anden form for tobak og ikke kunne klare dig uden?\n? Hvor stærkt føltes det?\n","input":"integer","options":{"0":"Ingen subjektiv trang","1":"Nogen trang, men kan sommetider klare sig uden","2":"Stærk og pågående trang"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.074","title":"Nedsat evne til at afholde sig fra eller nedsætte brugen af tobak Har du ønsket at ophøre eller nedsætte brugen uden at være i stand til det?","description":"? Har du kunnet undlade brugen nogle dage ind imellem, fx i weekender eller når du var alene? ? Har du været i stand til at undvære tobak igennem så lang tid som en måned?\n","input":"integer","options":{"0":"Ingen vanskelighed ved at ophøre eller nedskære forbruget","1":"Har klaret det gennem en måned eller mere","2":"Har ikke været i stand til at klare det gennem så lang tid som en måned"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.075","title":"Nedsat evne til at styre forbruget når det først er begyndt","description":"Sker det at du bliver ved at ryge eller bruge tobak, når du først er begyndt, selvom det ikke var din mening?\n","input":"integer","options":{"0":"Ingen vanskeligheder ved at styre påbegyndt brug","1":"Ryger eller bruger ofte mere end tilsigtet","2":"Er ikke i stand til at styre forbruget, når det først er begyndt"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.076","title":"Begrænsning af sociale aktiviteter på grund af rygning Har rygning medført problemer på arbejdet, med familien eller vennerne?","description":"? Har det forårsaget diskussioner eller skænderier? ? Har det forhindret din deltagelse i forskellige sociale aktiviteter, fordi rygning ikke var tilladt?\n","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Alvorlige problemer"},"validate":["0-2",8,9],"episodes":1,"glossary":12.06},{"key":"12.077","title":"Vedvarende rygning trods sociale problemer Har du fortsat med at ryge til trods for disse vanskeligheder?","input":"integer","options":{"0":"Nej","1":"Ja CUT OFF => 13.001 hvis forbruget er mindre end en gang om ugen, og items 12.073-12.077 alle rates 0."},"validate":[0,1,8,9],"episodes":1,"glossary":12.06},{"key":"12.078","title":"Prioritering af aktiviteter i forbindelse med rygning Hvor vigtig er rygning for dig sammenlignet med andre aktiviteter?","description":"Har du måttet indskrænke eller opgive deltagelse i vigtige sociale, arbejdsmæssige eller rekreative aktiviteter til fordel for rygningen?\n","input":"integer","options":{"0":"Ingen prioritering","1":"Prioriteres ofte højere","2":"Prioriteres altid højest"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.079","title":"Tidsforbrug for aktiviteter forbundet med rygning Har du brugt megen tid på rygning?","description":"? Så meget, at det er gået ud over andre aktiviteter?\n","input":"integer","options":{"0":"Ringe eller intet tidsforbrug","1":"Moderat tidsforbrug","2":"Udtalt tidsforbrug"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.080","title":"Toleransudvikling","description":"Har du øget forbruget af tobak for at opnå samme virkning?\n? Ryger du mere eller oftere for at få samme virkning? ? Kæderyger du?\n","input":"integer","options":{"0":"Ingen toleransudvikling","1":"Nogen tolerans, men ikke udtalt","2":"Udtalt tolerans, fx kæderygning"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.081","title":"Begrænsning af fysiske aktiviteter på grund af rygning","description":"Har du opgivet eller indskrænket fysiske aktiviteter, fx sportsaktiviteter på grund af rygning?\n","input":"integer","options":{"0":"Ingen begrænsninger","1":"Moderate begrænsninger","2":"Udtalte begrænsninger"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.082","title":"HELBREDSPROBLEMER PÅ GRUND AF RYGNING","description":"Har rygning medført fysiske problemer? - Hvilke?\n12_082 Tobakshoste\n","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.083","title":"Tobaksbetinget tilbøjelighed til lufvejsinfektioner","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.084","title":"Tobaksbetinget bronkitis","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.085","title":"Tobaksbetinget blodtryksforhøjelse","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.086","title":"Tobaksbetinget hjertebesvær","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.087","title":"Lungecancer på grund af rygning","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.088","title":"Toksiske symptomer (nikotin-intoksikation - se Glossary)","input":"integer","options":{"0":"Ingen problemer","1":"Moderate problemer","2":"Svære problemer"},"validate":["0-2",8,9],"episodes":1,"glossary":12.06},{"key":"12.089","title":"Vedvarende brug af tobak trods fysisk skade","description":"Har du fortsat med at ryge eller bruge tobak selv efter at du var blevet klar over, at det medførte helbredsproblemer, og efter at du var blevet advaret imod det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.090","title":"ABSTINENSSYMPTOMER","description":"Når du har prøvet at nedsætte eller ophøre med at ryge eller bruge tobak, har du så oplevet nogle af følgende symptomer?\n12_090 Tobakshunger\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.091","title":"Irritabilitet (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.092","title":"Angstsymptomer (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.093","title":"Koncentrationsbesvær (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.094","title":"Rastløshed (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.095","title":"Øget appetit og vægtstigning (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.096","title":"Langsom puls (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.097","title":"Hovedpine, utilpashed, svaghedsfølelse (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.098","title":"Døsighed eller søvnløshed (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.099","title":"Rysten på hænderne (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":12.06},{"key":"12.100","title":"Depressionsfølelse (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.101","title":"Maveuro, mundsår (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.102","title":"Øget hostetendens (tobak)","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.103","title":"Rygning eller brug af tobak for at undgå abstinenssymptomer","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":12.06},{"key":"12.104","title":"Begyndelsesalder for tobaksafhængighedstilstand","input":"string","validate":["0-99"],"glossary":12.06},{"key":"12.105","title":"Seneste alder for tobaksafhængighedstilstand","input":"string","validate":["0-99"],"glossary":12.06},{"key":"12.106","title":"Begyndelsesalder for skadeligt brug af tobak","input":"string","validate":["0-99"],"glossary":12.06},{"key":"12.107","title":"Seneste alder for skadeligt brug af tobak","input":"string","validate":["0-99"],"glossary":12.06},{"key":"13.001","title":"Påvirkning af aktiviteter og organiske årsager for Part 1 symptomer","description":"13_001 Påvirkning af dagliglivsaktiviteter på grund af part 1 symptomer\n","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"section_start":"Section_13","validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.002","title":"S2 Somatoforme og dissociative symptomer (cf. 2.123)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.003","title":"S3 Uspecifikke nervøse symptomer (cf. 3.015)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.004","title":"S4 Angst og fobier (cf. 4.060)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.005","title":"S5 Tvangssymptomer (cf. 5.015)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.006","title":"S6 Depressive symptomer (cf. 6.027)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.007","title":"S7 Koncentration, energi mv. (cf. 7.009)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.008","title":"S8 Søvnforstyrrelser (cf. 8.021)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.009","title":"S8 Seksuel dysfunktion (cf. 8.028)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.010","title":"S9 Spiseforstyrrelser (cf. 9.005)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.011","title":"S10 Maniske symptomer (cf. 10.028)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.001},{"key":"13.012","title":"S11 Brug af alkohol (cf. 11.037)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.013","title":"S12 Brug af andre psykoaktive substanser (cf. 12.049)","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.014","title":"S13 Symptomer forbundet med traume eller belastninger","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.015","title":"Andre, specificeres...","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.016","title":"Ukendte eller multiple symptomer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":13.001},{"key":"13.017","title":"Hierarkiske forhold mellem part 1 symptomer og syndromer:","description":"Hvilke typer af symptomer er de værste eller betyder mest? Hvilke næst mest? Hvilke kommer så herefter?\nHvilken type problem synes at være hovedproblemet, som de andre måske er en følge af?\nRates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n13_017 S2 Somatoforme og dissociative symptomer\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.018","title":"S3 Uspecifikke nervøse symptomer","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.019","title":"S4 Angst og fobier","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.020","title":"S5 Tvangssymptomer","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.021","title":"S6 Depressive symptomer","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.022","title":"S7 Koncentration, energi mv.","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.023","title":"S8 Søvnforstyrrelser","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.024","title":"S8 Seksuel dysfunktion","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.025","title":"S9 Spiseforstyrrelser","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.026","title":"S10 Maniske symptomer","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.027","title":"S11 Brug af alkohol","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.028","title":"S12 Brug af andre psykoaktive substanser","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.029","title":"S13 Symptomer forbundet med traume eller belastning","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.030","title":"Andre, specificeres...","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.031","title":"Ukendte eller multiple symptomer","description":"Rates efter klinisk skøn ud fra alle foreliggende oplysninger med cifre fra 1-9 afhængig af hierarkisk prioritering af sektionerne 2-12 (flere af items 13.017 til 13.031) kan have samme rangciffer).\n","input":"string","validate":["1-9"],"episodes":1,"glossary":13.017},{"key":"13.032","title":"Positive aktiviteter og bevaret livskvalitet til trods for symptomer","input":"integer","options":{"1":"Mange positive aktiviteter/megen livskvalitet (trods eller uden symptomer)","2":"Let begrænsning på grund af sektion 2-12 symptomer, men bevarede områder med positive aktiviteter og livskvalitet","3":"Moderat begrænsning","4":"Svær begrænsning med kun få positive aktiviteter eller ringe livskvalitet"},"validate":["1-4",8,9],"episodes":1,"glossary":13.032},{"key":"13.033","title":"Kvaliteten af foreliggende data for registrering af organisk årsag","input":"integer","options":{"0":"Data utilstrækkelige","1":"Data fuldt tilstrækkelige FAKTORER AF BETYDNING FOR SEKTION 2-12 SYMPTOMER ORGANISKE ÅRSAGER De diagnostiske kategorier i ICD-10 og DSM-IV tager forbehold for organiske årsager, som fører til kategorisering under ICD-10 organiske eller alkohol/stof-betingede psykiske lidelser, og for DSM-IV disorders due to a general medical condition og substance induced disorders. Registrering af organiske årsager foretages på sektionsniveau i slutningen af hver af sektionerne 2-10. De omfatter hjerneorganiske lidelser og systemiske lidelser med påvirkning af hjernefunktionen, samt lidelser som følge af brug af alkohol eller andre psykoaktive stoffer eller medikamenter. I de følgende items, 13.033-13.049, er der mulighed for en samlet registrering af sådanne organiske årsager."},"validate":[0,1],"episodes":1,"glossary":13.033},{"key":"13.034","title":"S2 Somatoforme og dissociative symptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte Kriterier for rating af organisk årsag (13.034-13.049) A Der er objektiv evidens (fra somatisk, neurologisk eller laboratoriemæssig undersøgelse) eller anamnestiske oplysninger om cerebral skade, sygdom eller dysfunktion, eller systemisk sygdomstilstand førende til cerebral dysfunktion, inkl. hormonelle forstyrrelser, alkohol- eller psykoaktivsubstans-relateret virkning. B Der er sandsynlig forbindelse mellem opståen (eller tydelig forværring) af den grundliggende sygdom, skade eller dysfunktion og de psykiske symptomer (umiddelbart eller efter latenstid). C De psykiske symptomer eller lidelser er ikke bedre forklaret af andre årsager, (fx ved tidligere episoder af lignende karakter uden forbindelse med organiske faktorer, eller udtalt familiær disposition ved lidelser med kendt arvelighed. D Helbredelse eller tydelig bedring af de psykiske symptomer efter fjernelse eller bedring af de formodede fysiske årsager."},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.035","title":"S3 Uspecifikke nervøse symptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.036","title":"S4 Angst og fobier","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.037","title":"S5 Tvangssymptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.038","title":"S6 Depressive symptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.039","title":"S7 Koncentration, energi mv.","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.040","title":"S8 Søvnforstyrrelser","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.041","title":"S8 Seksuel dysfunktion","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.042","title":"S9 Spiseforstyrrelser","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.043","title":"S10 Maniske symptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.044","title":"S13 Symptomer forbundet med traume eller belastning","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.045","title":"Andre, specificeres...","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.046","title":"Ukendte og multiple symptomer","input":"integer","options":{"0":"Ingen organisk årsag for Part 1 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B, C opfyldte","3":"Definitiv organisk årsag A, B, C og D alle opfyldte"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.034},{"key":"13.047A","title":"A 1. Organisk årsag for Part 1 symptomer","description":"Anfør de to sidste cifre fra pågældende symptomgruppe (fx 41 for 13.041) i øverste box for PS og i nederste box for RE eller LB.\n","input":"string","validate":["1-46"],"episodes":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.047B","title":"B 1. Organisk årsag for Part 1 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.048A","title":"A 2. Organisk årsag for Part 1 symptomer","description":"Hvis det er nødvendigt med endnu en organisk årsag, anfør de to sidste cifre fra pågældende symptomgruppe (fx 42 for 13.042).\n","input":"string","validate":["1-46"],"episodes":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.048B","title":"B 2. Organisk årsag for Part 1 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.049A","title":"A 3. Organisk årsag for Part 1 symptomer","description":"Hvis en 3. organisk årsag er til stede, anfør de to sidste cifre fra pågældende symptomgruppe (fx 43 for 13.043).\n","input":"string","validate":["1-46"],"episodes":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.049B","title":"B 3. Organisk årsag for Part 1 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.047},{"key":"13.050","title":"ANDRE FORHOLD AF BETYDNING FOR PART 1 SYMPTOMER","description":"Har særlige omstændigheder haft betydning for opståen eller forværring af Part 1 symptomer?\nRates efter klinisk skøn ud fra alle foreliggende oplysninger.\n13_050 Fødsel (2 eller 3 kan kun rates hvis inden for seneste 4 uger)\n","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"glossary":13.05},{"key":"13.051","title":"Menopause","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"glossary":13.05},{"key":"13.052","title":"Tab af nærtstående (kan kun rates 2 eller 3 hvis inden for seneste 3 måneder)","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.053","title":"Adolescens","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"glossary":13.05},{"key":"13.054","title":"Medlemskab af særlig subkultur","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"glossary":13.05},{"key":"13.055","title":"Nylig tilflytning","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.056","title":"Særlige livsbegivenheder (life events)","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.057","title":"Sociale vanskeligheder","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.058","title":"Andre, specificeres...","input":"integer","options":{"0":"Ingen","1":"Faktor til stede, men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.059","title":"Checkliste for akut belastningsreaktion","description":"13_059 Pludseligt overvældende traume eller belastning\n","input":"integer","options":{"0":"Ingen","1":"Udsættelse for exceptionelt svær fysisk eller psykisk traume eller belastning, som vil medføre kraftig påvirkning hos praktisk talt enhver med intens frygt, rædsel eller afmagtsfølelse (katastrofeagtig belastning med oplevelse af overhængende livsfare, som fx naturkatastrofer, krigshandlinger, tog- eller skibs-ulykker, torturhandlinger, voldtægt)","2":"Traume eller belastning inden for det almenmenneskelige erfaringsområde og ikke af usædvanlig eller katastrofe-agtig karakter (dødsfald, tab af nærtstående, separation, skilsmisse, tab af arbejde) SKIP => til 13.125 hvis 13.059 rates 0."},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.060","title":"Tidsforløb mellem traume og symptomer","input":"integer","options":{"0":"Ingen symptomer","1":"Inden for en time","2":"Mellem 1 og 48 timer","3":"Mere end 48 timer men mindre en 4 uger","4":"Mellem 4 uger og 6 måneder","5":"Mere end 6 måneder"},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.061","title":"Varighed af traume eller belastning","input":"integer","options":{"0":"Intet traume, ingen belastning","1":"Kort, aftager inden for 8 timer","2":"Begynder at aftage inden for 48 timer","3":"Vedvarer ud over 48 timer op til en uge","4":"Vedvarer ud over en uge","5":"Vedvarende traumatisk eller truende påvirkning (fx deltagelse i krigshandlinger, udsættelse for fængsling med tortur)"},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.062","title":"Angst eller spændingssymptomer forbundet med belastning","description":"Har du følt dig ængstelig, rastløs eller spændt siden (traumatiske begivenhed eller belastning)?\n? Har du haft hjertebanken, rysten eller sveden, eller følelse af åndenød? ? Har du følt dig irritabel eller rastløs? ? Har du haft svært ved at sove eller koncentrere dig siden det skete?\n","input":"integer","options":{"0":"Angst og spændingssymptomer ikke til stede","1":"Angst og spændingssymptomer til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.063","title":"Hjertebanken, ekstraslag, hurtig puls","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.064","title":"Trykken eller smerter i brystet","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.065","title":"Varm eller kold sveden, hedeture","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.066","title":"Rysten eller sitren","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.067","title":"Mundtørhed","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.068","title":"Angst for at blive sindssyg eller miste besindelsen","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.069","title":"Dødsangst","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.070","title":"Sommerfuglefornemmelse, køren i maven, kvalme","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.071","title":"Vejrtrækningsbesvær","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.072","title":"Svimmelhed, ørhed i hovedet, besvimelsesfornemmelse","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.073","title":"Prikken eller dødhedsfornemmelser i ansigt eller fingre","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.074","title":"Uvirkelighedsfølelse","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.075","title":"Muskelspænding eller -smerte","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.076","title":"Rastløshed, vanskelighed ved at slappe af","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.077","title":"Psykisk spændingsfølelse","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.078","title":"Følelse af synkebesvær, klump i halsen","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.079","title":"Tendens til sammenfaren","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.080","title":"Koncentrationsbesvær pga. bekymring","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.081","title":"Irritabilitet","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"glossary":13.05},{"key":"13.082","title":"Søvnforstyrrelser pga. bekymring","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.083","title":"Social tilbagetrækning","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.084","title":"Uengagerthed eller fremmedgjorthed","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.085","title":"Indsnævret opmærksomhed, fjernhed","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.086","title":"Manglende emotionel reaktivitet","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.087","title":"Desorientering i tid og sted","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.088","title":"Vrede eller verbal aggression","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.089","title":"Håbløshedsfølelse","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.090","title":"Utilpasset eller formålsløs overaktivitet","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.091","title":"Ubehersket og overvældende sorgreaktion","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.092","title":"Dissociativ stupor","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.093","title":"Varighed af symptomer i items 13.063-13.092","input":"integer","options":{"1":"Bedring inden for 8 timer","2":"Bedring inden for 48 timer","3":"Bedring inden for 1 uge","4":"Bedring inden for 4 uger","5":"Bedring inden for 3 måneder","6":"Ingen bedring inden for 3 måneder","7":"Ingen bedring inden for 6 måneder"},"validate":["1-7",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.094","title":"POST-TRAUMATISK BELASTNINGSREAKTION","description":"Længerevarende tilstand efter udsættelse for exceptionelt svær fysisk eller psykisk traume eller belastning af katastrofekarakter (se akut belastningsreaktion). Tilstanden kan opstå umiddelbart efter belastningen som fortsættelse af en akut belastningsreaktion eller med op til 6 måneders latenstid.\n13_094 Tidsforløb mellem symptomer og belastning\n","input":"integer","options":{"0":"Ingen symptomer","1":"Mindre end 3 måneder","2":"Mellem 3 og 6 måneder","3":"Mere end 6 måneder SKIP => til 13.110 hvis 13.094 = 0."},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.095","title":"Nedsat social eller arbejdsmæssig funktionsevne (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.096","title":"Tilbagevendende påtrængende erindringer om traumet (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.097","title":"Undgåelse af situationer eller aktiviteter, der minder om traumet (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.098","title":"Undgåelse af andre forhold forbundet med traumet (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.099","title":"Ind- eller gennem-sovningsbesvær (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.100","title":"Irritabilitet eller vredesudbrud (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.101","title":"Koncentrationsbesvær (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.102","title":"Øget alarmberedskab med ængstelse (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.103","title":"Amnesi (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.104","title":"Ubehag ved udsættelse for forhold, der minder om traumet (posttraumatisk )","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.105","title":"Nedsat interesse (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.106","title":"Emotionel afstumpning (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.107","title":"Tilbagevendende angstdrømme eller mareridt (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.108","title":"Flashbacks (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.109","title":"Livagtig genoplevelse af traumet (posttraumatisk)","input":"integer","options":{"0":"Ikke forbundet med traume eller belastning","1":"Forbundet med traume eller belastning"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.110","title":"TILPASNINGSREAKTIONER","description":"Reaktioner på nylige traumer eller belastninger ikke af usædvanlig eller katastrofeagtig karakter (tab af nærtstående, separation, skilsmisse, tab af arbejde eller rollefunktion, flygtning- eller emigrant-status).\n13_110 Tidsforløb mellem belastning og begyndelse af symptomer 87 = 87 eller flere uger\n","input":"string","validate":["0-87",88,99],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.111","title":"Varighed af symptomer (uger)","description":"0 = Belastningen stadigvæk til stede 87 = 87 eller flere uger\n","input":"string","validate":["0-299"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.112","title":"Generende symptomer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.113","title":"Påvirkning af sociale eller arbejdsmæssige funktioner","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.114","title":"Kortvarig depressiv reaktion","description":"Let, varighed mindre end en måned.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.115","title":"Længerevarende depressiv reaktion","description":"Let, varighed fra 1 måned til 2 år.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.116","title":"Blandet angst-depressionsreaktion","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.117","title":"Tilpasningsreaktion med angstsymptomer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.118","title":"Tilpasningsreaktion med andre emotionelle symptomer","description":"Bekymringstendens, anspændthed, vrede.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.119","title":"Tilpasningsreaktion med overvejende adfærdssymptomer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.120","title":"Tilpasningsreaktion med adfærdssymptomer og emotionelle symptomer","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.121","title":"Tilpasningsreaktion med andre specificerede symptomer","description":"Maladaptive reaktioner, fx med somatoforme klager, social tilbagetrækning, arbejds- eller studiehæmning.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":13.05},{"key":"13.122","title":"Begyndelsesalder for akut belastningsreaktion","input":"string","validate":["0-99"],"episodes":1,"glossary":13.05},{"key":"13.123","title":"Begyndelsesalder for posttraumatisk belastningsreaktion","input":"string","validate":["0-99"],"episodes":1,"glossary":13.05},{"key":"13.124","title":"Begyndelsesalder for tilpasningsreaktion","input":"string","validate":["0-99"],"episodes":1,"glossary":13.05},{"key":"13.125","title":"PÅLIDELIGHED AF PART 1 INTERVIEWOPLYSNINGER","description":"13_125 Pålidelighed af ratings i sektionerne 2-13\n","input":"integer","options":{"0":"Ingen problemer, ratings pålidelige","1":"Nogle problemer, men de fleste ratings skønnes pålidelige","2":"Store problemer med at gennemføre interviewet, tvivl om pålideligheden af ratings","3":"Umuligt at rate symptomer pålideligt Rating af pålidelighed af interviewet for Part 2 symptomer 20.114 - 20.115."},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":13.125},{"key":"14.001","title":"Screeningsitems for Part 2 symptomer Kan anvendes som screening ved undersøgelse af patientgrupper med overvejende Part 1 symptomer.","description":"14_001 Perceptionsforstyrrelser\nHar du nogensinde haft en følelse af, at det hele var uvirkeligt? Eller har du følt dig selv uvirkelig, ligesom ''ikke rigtig til stede''?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"section_start":"Section_14","validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.002","title":"Vrangstemning eller forvirringsfølelse","description":"Har du nogensinde haft en følelse af, at der foregik noget mærkeligt, som du ikke kunne forklare?\nHar du følt dig forvirret over mærkelige oplevelser, som det var svært at gøre rede for?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.003","title":"Tankepåvirkningsoplevelse","description":"Har du været udsat for noget som forstyrrede eller påvirkede dine tanker på en underlig måde? Har du altid været herre over dine tanker og handlinger?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.004","title":"Underlige synsoplevelser eller fornemmelser","description":"Har du nogensinde haft usædvanlige oplevelser, hvor du kunne se noget, andre ikke kunne se? - Overnaturlige oplevelser? Eller fornemmelse af, at nogen eller noget var til stede, selvom du ikke kunne se det?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.005","title":"Hørelseshallucinationer","description":"Har du nogensinde kunnet høre lyde eller stemmer, når der ikke var nogen til stede og som andre ikke kunne høre?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.006","title":"Selvhenførende og forfølgelsesprægede forestillinger","description":"Har du nogensinde haft en fornemmelse af, at du var genstand for påfaldende opmærksomhed, blev overvåget eller forfulgt?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.007","title":"Sære eller ubehagelige oplevelser","description":"Fx påvirknings- eller styringsoplevelser.\nHar du været udsat for andre mærkelige eller ubehagelige oplevelser?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1,8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.008","title":"Subjektiv beskrevet hukommelsesbesvær","description":"Har du haft problemer med hukommelsen?\n? Husker du ikke så godt som tidligere? ? Hvad er det især du har svært ved at huske (eksempel)? ? Hvornår begyndte det?\n","input":"integer","options":{"0":"Intet hukommelsesbesvær","1":"Lettere hukommelsesbesvær, evt. i forbindelse med bekymringstendens eller koncentrationsbesvær","2":"Udtalt hukommelsesbesvær"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.009","title":"Kompenserende aktiviteter for hukommelsesbesvær Er du nødt til at skrive alting ned for at huske det?","input":"integer","options":{"0":"Ingen","1":"Lette vanskeligheder uden kompenserende aktiviteter","2":"Delvis brug af kompenserende aktiviteter for lettere hukommelsesbesvær","3":"Udstrakt brug af kompenserende aktiviteter for udtalt hukommelsesbesvær"},"validate":["0-3",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.010","title":"Screening for forstyrrelser af adfærd, følelser og tale","description":"14_010 Langsomhed, motorisk hæmning\n","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.011","title":"Rastløshed","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.012","title":"Aparte eller påfaldende udseende","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.013","title":"Selvforsømmelse","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.014","title":"Angst","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.015","title":"Forsænket stemningsleje","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.016","title":"Affektaffladning","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.017","title":"Inkoherent tale","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.018","title":"Magisk eller udtalt ulogisk tænkning","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.019","title":"Ordfattig tale","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"14.020","title":"Nedsat brug af non-verbal kommunikation","input":"integer","options":{"0":"Symptomet ses ikke","1":"Til stede i moderat grad","2":"Til stede i svær grad"},"validate":["0-2",8,9],"episodes":1,"glossary":"Section_14"},{"key":"15.001","title":"PSE Part 2 15 Sprog- og taleproblemer ved undersøgelsen","description":"For at kunne registrere til stedeværelsen af symptomerne i sektion 16 - 19 er det særligt vigtigt, at patienten fuldt ud forstår de stillede spørgsmål og er i stand til at give en spontan beskrivelse af sine oplevelser i et forståeligt sprog.\nRating af sprog eller taleproblemer:\n15_001 Dårlig udtryksevne på grund af mental retardering\n","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"section_start":"Section_15","validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.002","title":"Inkoherent tale","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.003","title":"Udviklingsmæssig sprogforstyrrelse (autistisk spektrum)","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.004","title":"Andre udviklingsforstyrrelser af sprog eller tale , fx dysfasi","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.005","title":"Ord- eller indholdsfattig tale","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.006","title":"Bevidsthedsforstyrrelser","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.007","title":"Fremmedsproget patient","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"15.008","title":"Andre sprog- eller tale-problemer","input":"integer","options":{"0":"Ingen sprog- eller tale-problemer","1":"Vanskeligheder som påvirker ratingen af symptomerne","2":"Udtalte vanskeligheder som umuliggør rating af symptomerne"},"validate":["0-2",8,9],"glossary":"Section_15"},{"key":"16.001","title":"Perceptionsforstyrrelser (andre end hallucinationer)","description":"16_001 Perceptionsforstyrrelser\nVirker omgivelserne normale, eller har de virket anderledes end sædvanligt?\nHar du haft en følelse af, at det hele var uvirkeligt? Har du selv følt dig uvirkelig, ligesom ''ikke rigtig til stede''?\n","input":"integer","options":{"0":"Nej","1":"Ja CUT OFF => til 17.001 hvis 16.001 rates 0."},"section_start":"Section_16","validate":[0,1,8,9],"episodes":1,"glossary":16.001},{"key":"16.002","title":"Forvrænget perception","description":"Har omgivelserne virket forvrængede eller forandrede i form eller størrelse? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.002,"ratingscale":2},{"key":"16.003","title":"Svækket perception","description":"Har omgivelserne virket mørke, flade eller farveløse? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.003,"ratingscale":2},{"key":"16.004","title":"Skærpet perception","description":"Har omgivelserne virket unaturligt livlige, farvede eller detaljerede? - Lydene unaturligt høje eller klare? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.004,"ratingscale":2},{"key":"16.005","title":"Ændret tidsoplevelse, déja vu, jamais vu","description":"Har det virket som om tiden gik på en usædvanlig måde? - Alt for hurtigt eller alt for langsomt?\nHar du haft en fornemmelse af, at det du har oplevet er noget, du havde oplevet\ntidligere på nøjagtig samme måde ?\nHar du haft en følelse af, at velkendte omgivelser eller situationer forekom dig\nfremmede, som om du aldrig havde set dem før? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.005,"ratingscale":2},{"key":"16.006","title":"Derealisation (ting)","description":"Har du haft en følelse af, at tingene omkring dig var uvirkelige, som i en drøm eller et teaterstykke? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.006,"ratingscale":2},{"key":"16.007","title":"Derealisation (personer)","description":"Har du haft en følelse af, at personerne omkring dig var uvirkelige, ligesom skuespillere eller marionetter? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.007,"ratingscale":2},{"key":"16.008","title":"Depersonalisation (egen person)","description":"Har du følt dig selv uvirkelig, som i et skuespil, eller ligesom ''ikke rigtig til stede''? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.008,"ratingscale":2},{"key":"16.009","title":"Fremmedfølelse over for eget udseende","description":"Har du følt dig uvirkelig eller fremmed, når du så på dig selv i et spejl eller på et fotografi? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.009,"ratingscale":2},{"key":"16.010","title":"Fremmedfølelse over for dele af kroppen","description":"Har du følt det som om en del af din krop virkede fremmed, så forkert ud, eller ikke tilhørte dig? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.01,"ratingscale":2},{"key":"16.011","title":"Følelse af ændret udseende (dysmorfofobi)","description":"Synes du dit udseende har forandret sig, fx næsen, tænderne, munden, hagen eller ørene? Er det en virkelig forandring, som andre også kan se, eller er det bare en\nfornemmelse?\nCf. 2.084 og 2.086. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.011,"ratingscale":2},{"key":"16.012","title":"Dysmorfofobe vrangforestillinger Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.012,"ratingscale":2},{"key":"16.013","title":"Depersonalisations- og derealisations-vrangforestillinger","description":"Er du helt sikker på, at det hele er uvirkeligt, at du selv er uvirkelig, fremmed eller mangler en del af kroppen, - eller er det bare en fornemmelse? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":16.013,"ratingscale":2},{"key":"16.014","title":"Andre perceptionsforstyrrelser Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":16.014,"ratingscale":2},{"key":"16.015A","title":"Tidsangivelse for sektion 16 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014 Begyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":16.015},{"key":"16.015B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-299"],"glossary":16.015},{"key":"16.015C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":16.015},{"key":"16.015D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-9999"],"glossary":16.015},{"key":"16.016","title":"Påvirkning af aktiviteter på grund af sektion 16 symptomer","input":"integer","options":{"0":"Ingen sektion 16 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":16.016},{"key":"16.017","title":"Organisk årsag til sektion 16 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag, bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":16.017},{"key":"16.018","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":16.018},{"key":"17.001","title":"Hallucinationer","description":"17_001 Screening for hallucinationer Har du hørt lyde eller stemmer, når der ikke var nogen til stede eller noget, der\nforklarede det? - Eller som andre ikke kunne høre?\nHar du haft syner, som andre ikke kunne se eller opleve? - Lugt-, smags- eller legemlige fornemmelser, som du ikke kunne forklare?\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede","8":"Usikkert"},"section_start":"Section_17","validate":[0,1,8,9],"episodes":1,"glossary":17.001},{"key":"17.002","title":"Screening for andre usædvanlige oplevelser","description":"Har du haft andre underlige eller ubehagelige oplevelser?\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede CUT OFF => til 18.001 hvis 17.001 og 17.002 begge rates 0."},"validate":[0,1,8,9],"episodes":1,"glossary":17.002},{"key":"17.003","title":"Non-verbale hørelseshallucinationer","description":"Har du hørt lyde, som banken, musik eller hvisken ?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.003,"ratingscale":2},{"key":"17.004","title":"Hørelseshallucinationer, hyppighed Har du hørt stemmer eller hørt nogen sige noget, når du var alene?","description":"Kunne du høre ordene? - Hvad der blev sagt ? Hvor tit har du kunnet høre det (somme tider, næsten dagligt, hele tiden)?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Sjældent","2":"Cirka én gang ugentlig","3":"De fleste dage","4":"Hele tiden"},"validate":["0-4",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.004},{"key":"17.005","title":"Længden af hørelseshallucinationer","description":"Hvad bliver der sagt? - Et enkelt ord eller hele sætninger? Prøv at nævne et par eksempler på, hvad der bliver sagt - i ordret citat. Gentages det samme hele tiden?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Kun få ord eller korte sætninger","2":"Lejlighedsvis længere sætninger","3":"Lange sætninger, mere eller mindre vedvarende hørelseshallucinationer"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.005},{"key":"17.006","title":"Kvaliteten af hørelseshallucinationer","description":"Hvordan lyder stemmerne? - Ligesom normale stemmer? - Eller som gennem et apparat eller en højtaler?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Særlig kvalitet, anderledes end virkelige stemmer","2":"Lyder som virkelige stemmer"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"glossary":17.006},{"key":"17.007","title":"Interne hallucinationer","description":"Hører du stemmerne eller det der bliver sagt, inde i hovedet eller for dit indre øre? Hører du stemmerne eller det der bliver sagt, som noget der kommer ude fra, ind gennem ørerne?\n- Hvor lyder det som om det kommer fra?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Overvejende eksterne hallucinationer","2":"Både interne og eksterne hallucinationer","3":"Overvejende interne hallucinationer"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":17.007},{"key":"17.008","title":"Kommenterende og diskuterende stemmer (3. person) Kommer stemmerne med bemærkninger om dine tanker eller handlinger, hvor de omtaler dig i tredie person? (Eksempler!)","description":"Kommenterer stemmerne hele tiden, hvad du foretager dig, hvad du læser eller ser i fjernsynet? (Eksempler!) Taler stemmerne indbyrdes med hinanden om dig? (Eksempler!)\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Ingen forbindelse mellem stemmerne og patientens tanker","2":"Stemmerne kommenterer eller diskuterer lejlighedsvist","3":"Hyppige kommenterende eller diskuterende stemmer"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.008},{"key":"17.009","title":"Anden eller trediepersons hallucinationer","description":"Taler stemmerne direkte til dig eller taler de om dig i tredie person?\n? Hvad siger de? (Eksempler!) ? Hører du kun stemmer, der taler direkte til dig i anden person (siger du)? ? Hører du kun stemmer, der taler om dig i tredie person (siger han eller hende)? ? Hører du både stemmer, der taler til dig og om dig? - Hvilke er hyppigst forekommende?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Kun andenpersons hørelseshallucinationer","2":"Andenpersons AH hyppigere end trediepersons AH","3":"Anden- og trediepersons hørelseshallucinationer lige hyppige","4":"Trediepersons AH hyppigere end andenpersons AH","5":"Kun trediepersons hørelseshallucinationer"},"validate":["0-5",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.009},{"key":"17.010","title":"Stemningskongruens","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Fuld stemningskongruens","2":"Overvejende stemningskongruens","3":"Ligelig stemningskongruens og -inkongruens","4":"Overvejende stemningsinkongruens","5":"Fuld inkongruens","8":"Uvist om kongruens","9":"Intet stemningspræg"},"validate":["0-5",8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.01},{"key":"17.011","title":"Stemningspræg","input":"integer","options":{"0":"Ingen kongruens","1":"Kongruent med depressiv stemningsleje","2":"Kongruent med både depressivt og manisk stemningsleje","3":"Kongruent med manisk stemningsleje"},"validate":["0-3",5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.011},{"key":"17.012","title":"Specielle træk ved hørelseshallucinationer Hører du stemmerne gennem andre lyde, fx gennem susen i vandrørene eller","description":"fuglenes sang? (Fakultative hørelseshallucinationer)\nKommer stemmerne fra en del af kroppen, fx maven eller det ene ben?\n? Kommer stemmerne nogensinde ud af din egen mund?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Fakultative hørelseshallucinationer","2":"Hørelseshallucinationer fra dele af kroppen","3":"Hørelseshallucinationer, både fakultative og fra dele af kroppen"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.012},{"key":"17.013","title":"Indsigt i hørelseshallucinationer","description":"Hvordan går det til, at du kan høre stemmerne? Hvor kommer de fra? Hvorfor kan netop du høre dem?\n","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Rationel forklaring, - ''egne tanker'', ''underbevidstheden'', ''sygdom''","2":"Ingen forklaring, - ''ved ikke''","3":"Ingen indsigt, - forklarende vrangforestillinger"},"validate":["0-3",5,8,9],"episodes":1,"glossary":17.013},{"key":"17.014","title":"Prominens af hørelseshallucinationer","input":"integer","options":{"0":"Ingen hørelseshallucinationer","1":"Hørelseshallucinationer ikke fremtrædende træk i sygdomsbilledet","2":"Hørelseshallucinationer fremtrædende træk i sygdomsbilledet, men ikke (3)","3":"Hørelseshallucinationer fremtrædende, påtrængende og vedholdende, sammenlignet med andre symptomer"},"validate":["0-3",5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.014},{"key":"17.015","title":"SYNSHALLUCINATIONER","description":"17_015 Uformede synshallucinationer (klar bevidsthed)\nHar du set noget, andre ikke kunne se?\n? Hvad så du? ? Var det lysglimt eller skygger, ting eller personer? ? Var det hele scenerier? ? Så du det inde i hovedet for dit indre blik eller ude i rummet? ? Var du helt vågen, da du så det? ? Var du sikker på, at det du så var virkeligt?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.015,"ratingscale":2},{"key":"17.016","title":"Formede synshallucinationer, men ikke hele scenerier (klar bevidsthed) Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.016,"ratingscale":2},{"key":"17.017","title":"Sceniske synshallucinationer (klar bevidsthed) Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.017,"ratingscale":2},{"key":"17.018","title":"Hypnagoge eller hypnopompe hallucinationer","description":"Synshallucinationer i forbindelse med indsovning eller opvågning. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.018,"ratingscale":2},{"key":"17.019","title":"Flygtige hallucinationer i forbindelse med tab af nærtstående person Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.019,"ratingscale":2},{"key":"17.020","title":"Dissociative hallucinationer","description":"Kombinerede eller komplekse hallucinatoriske oplevelser, hvor patienten kan føre en\nsamtale med en stemme, ofte tilhørende en velkendt person, og hvor patienten ofte samtidig kan se, fornemme eller føle personens til stedeværelse i rummet. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.02,"ratingscale":2},{"key":"17.021","title":"Prominens af synshallucinationer","input":"integer","options":{"0":"Ingen synshallucinationer","1":"Synshallucinationer ikke fremtrædende del af sygdomsbilledet","2":"Synshallucinationer fremtrædende del af sygdomsbilledet, men ikke (3)","3":"Synshallucinationer dominerer sygdomsbilledet sammenlignet med andre symptomer"},"validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.021},{"key":"17.022","title":"ANDRE HALLUCINATIONER OG RELATEREDE SOMATISKE VRANGFORESTILLINGER","description":"17_022 Lugthallucinationer\nHar du bemærket en usædvanlig lugt, som du ikke kan forklare? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.022,"ratingscale":2},{"key":"17.023","title":"Bizarre vrangforestillinger forbundet med lugthallucinationer","description":"Hvor kommer den mærkelige lugt fra? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.023,"ratingscale":2},{"key":"17.024","title":"Hallucinatorisk oplevelse af at afgive særlig lugt","description":"Føler du, at du selv afgiver en særlig lugt? - Kan du selv lugte den? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.024,"ratingscale":2},{"key":"17.025","title":"Vrangforestillinger om at afgive særlig lugt","description":"Tror andre mennesker, at du afgiver en særlig lugt? - Kan du selv lugte den? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.025,"ratingscale":2},{"key":"17.026","title":"Seksuelle hallucinationer","description":"Har du haft usædvanlige seksuelle fornemmelser?\n? Hvad drejer det sig om? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.026,"ratingscale":2},{"key":"17.027","title":"Vrangforestillinger forbundet med seksuelle hallucinationer","description":"Hvordan opstår de seksuelle fornemmelser? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.027,"ratingscale":2},{"key":"17.028","title":"Hallucinationer for andre sansemodaliteter","description":"Har du bemærket andre mærkelige eller uforklarlige fornemmelser?\n? En uforklarlig smag?\n? Følelse af berøring eller smerte, varme eller kulde? ? Fornemmelse af noget der trykker, flytter sig eller bevæger sig inde i hovedet,\nkroppen eller under huden? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.028,"ratingscale":2},{"key":"17.029","title":"Bizarre vrangforestillinger forbundet med somatiske hallucinationer","description":"Hvordan opstår disse fornemmelser? - Hvad er det der sker? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.029,"ratingscale":2},{"key":"17.030","title":"Andre hallucinatoriske oplevelser Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.03,"ratingscale":2},{"key":"17.031","title":"Begyndelsesalder for hallucinatoriske oplevelser","input":"string","validate":["0-99"],"glossary":17.031},{"key":"17.032A","title":"Tidsangivelse for sektion 17 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.014\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":17.032},{"key":"17.032B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"glossary":17.032},{"key":"17.032C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":17.032},{"key":"17.032D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"glossary":17.032},{"key":"17.033","title":"Påvirkning af aktiviteter på grund af sektion 17 symptomer","input":"integer","options":{"0":"Ingen sektion 17 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svært eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.033},{"key":"17.034","title":"Organisk årsag til sektion 17 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag, bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.034},{"key":"17.035","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":17.035},{"key":"18.001","title":"Tankeforstyrrelses- og styrings-oplevelser","description":"For at undgå falsk-positive ratings er det vigtigt at få patienten til at beskrive sine oplevelser og forestillinger herom, helst med eksempler. Undgå ledende spørgsmål, der blot kan besvares med ja eller nej.\n18_001 Vrangstemning og forvirringsfølelse\nHar du haft fornemmelsen af at noget mærkeligt var ved at ske, som du ikke kunne forklare?\nHar du følt dig forvirret over mærkelige oplevelser, som det var svært at gøre rede for? Brug Rating Skala II\n","input":"string","section_start":"Section_18","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":18.001,"ratingscale":2},{"key":"18.002","title":"Screening for sektion 18 symptomer Kan du tænke helt klart? - Eller er der noget, der forstyrrer dine tanker på én eller anden måde?","description":"? Hvordan føles det? ? Har du fuld kontrol over dine tanker og handlinger?\n","input":"integer","options":{"0":"Ingen sektion 18 symptomer","1":"Sektion 18 symptomer til stede CUT OFF => til 19.001 hvis 18.001 - 18.002 alle rates 0."},"validate":[0,1,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.002},{"key":"18.003","title":"Vrangforestillinger om at tankerne kan læses af andre","description":"Har du haft fornemmelsen af, at dine tanker kunne læses af andre?\n? Kan du beskrive det nærmere? ? Hvordan går det til? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.003,"ratingscale":2},{"key":"18.004","title":"Tankehørlighed","description":"Kan du høre dine tanker udtalt højt inde i hovedet? - Så højt, at nogen der stod ved siden af kunne høre dem, hvis der var helt stille? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.004,"ratingscale":2},{"key":"18.005","title":"Tankeekko Sker det, at dine tanker bliver gentaget lige efter, at du har tænkt dem? - Tavst, men ligesom et ekko?","description":"? Kan du beskrive det nærmere? NB! Stemme der gentager tankerne rates ved 17.008. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.005,"ratingscale":2},{"key":"18.006","title":"Tankepåføring","description":"Er der tanker i dit hoved, som ikke er dine egne? - Som synes at komme et andet sted fra?\n? Hvordan kommer de ind i hovedet på dig? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.006,"ratingscale":2},{"key":"18.007","title":"Tankeudspredning","description":"Er dine tanker på en eller anden måde tilgængelige for andre? - Ikke dine egne private, men sådan at andre ved hvad du tænker?\n? Hvordan går det til?\nNB! Forestillinger om at tanker, ikke umiddelbart men senere citeres i medierne, rates ved 19.006. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.007,"ratingscale":2},{"key":"18.008","title":"Kommenterende tanker","description":"Er der ved siden af dine tanker, inde i hovedet en anden række tanker, som du ikke har under kontrol, og som fx kommenterer dine tanker, noget du læser eller noget du har set eller gjort?\n? Er det en stemme, eller er det tanker (tavse)? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":18.008,"ratingscale":2},{"key":"18.009","title":"Tankestop","description":"Stopper dine tanker somme tider pludseligt op, så du bliver helt tom i hovedet, selvom du ikke selv ønsker at holde op med at tænke? - Lige så pludseligt og fuldstændigt som når fjernsynet går i sort?\n? Hvor længe varer det? ? Når det holder op, fortsætter tankerne så igen hvor de slap?\nNB! Må ikke forveksles med opmærksomhedssvigt, distraktion eller kunne opfattes som udtryk for tankeflygtighed eller angst. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":18.009,"ratingscale":2},{"key":"18.010","title":"Tankefradrag","description":"Bliver dine tanker taget ud af dit hoved? - Eller på en eller anden måde fjernet?\n? Er der nogen eller noget, der tager dine tanker? ? Føles det som om tankerne er uden for hovedet?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.01,"ratingscale":2},{"key":"18.011","title":"Andre tankeforstyrrelsesoplevelser","description":"Bliver dine tanker forstyrret på andre måder?\n? Bliver tankerne fx bevæget fra den ene side til den anden?\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":18.011,"ratingscale":2},{"key":"18.012","title":"Styringsoplevelser","description":"Har du oplevet at din vilje bliver erstattet af en anden vilje, så du bliver styret udefra af en fremmed kraft eller magt?\n? Kan du beskrive det nærmere? ? Var dine handlinger eller det du foretog dig ikke noget du selv gjorde, men noget, nogen gjorde gennem dig? ? Følte du dig som en robot eller marionetdukke, styret udefra? ? Hvad skete der? - Hvordan gik det til? (Besættelse, hypnose)\nNB! ''Som om'' oplevelser godtages ikke. Oplysninger om at blive styret af stemmer, idéer eller religiøse bud frem for egen vilje eller lyst er blot et udtryk for, at patienten har valgt at bøje sig herfor, og selv udfører de hertil hørende handlinger. Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.012,"ratingscale":2},{"key":"18.013","title":"Fremmed styring af stemmen","description":"Er der nogen eller noget, der taler med din stemme? - Så du hører dig selv sige noget, du ikke havde tænkt dig, uden at kunne lade være?\n? Kommer stemmen fra din egen mund? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.013,"ratingscale":2},{"key":"18.014","title":"Fremmed styring af håndskrift","description":"Er der nogen eller noget, der skriver med din hånd? - Selvom du ikke havde tænkt dig at skrive, og uden at du kunne forhindre det? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.014,"ratingscale":2},{"key":"18.015","title":"Påførte handlinger","description":"Er dine bevægelser eller handlinger påført af en fremmed vilje?\n? Kan du beskrive det nærmere? (Eksempler!) ? Har du fx oplevet, at du pludselig bevægede hånden, rejste dig og gik eller løb, uden at det var dig, der ville det? - Og uden at du kunne forhindre det? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.015,"ratingscale":2},{"key":"18.016","title":"Påførte følelser","description":"Har du oplevet, at der påføres dig følelser, som ikke er dine egne? - Hvilke? Hvor kommer de fra? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.016,"ratingscale":2},{"key":"18.017","title":"Andre styringsoplevelser","description":"Har du oplevet at få påført tilskyndelser eller impulser udefra? - Eller fornemmelser ét eller andet sted?\n? Hvad er det, der sker? - Hvor kommer det fra? Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.017,"ratingscale":2},{"key":"18.018","title":"Begyndelsesalder for sektion 18 symptomer","input":"string","validate":["0-99"],"glossary":18.018},{"key":"18.019A","title":"Tidsangivelse for sektion 18 symptomer","description":"Rates kun hvis forskellig fra tidsangivelsen i 1.007-1.114\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","episodes":1,"glossary":18.019},{"key":"18.019B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.019},{"key":"18.019C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":18.019},{"key":"18.019D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"glossary":18.019},{"key":"18.020","title":"Påvirkning af aktiviteter på grund af sektion 18 symptomer","input":"integer","options":{"0":"Ingen sektion 18 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",5,8,9],"episodes":1,"used_in_dsmiv":1,"glossary":18.02},{"key":"18.021","title":"Organisk årsag til sektion 18 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":18.021},{"key":"18.022","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":18.022},{"key":"19.001","title":"Vrangforestillinger","description":"19_001 Selvhenførende vrangforestillinger\nHar du følt, at du var genstand for en påfaldende opmærksomhed fra andre? Har dét, der skete omkring dig, haft en ganske særlig betydning? Har dén måde, det hele var lagt tilrette på, betydet noget særligt? Er du blevet generet, eller måske forfulgt?\n","input":"integer","options":{"0":"Nej","1":"Ja"},"section_start":"Section_19","validate":[0,1,5,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.001},{"key":"19.002","title":"Andre vrangforestillinger","description":"Har du usædvanlige evner eller kræfter? Har du haft mærkelige eller usædvanlige oplevelser? Har du været udsat for ejendommelige kræfter eller påvirkninger? Føler du, at du har gjort noget galt? Er der noget i vejen med dit helbred eller dit udseende?\n","input":"integer","options":{"0":"Nej","1":"Ja CUT OFF=> til 20.001 hvis 19.001 og 19.002 begge rates 0"},"validate":[0,1,5,8,9],"episodes":1,"glossary":19.001},{"key":"19.003","title":"Selvhenførende vrangforestillinger, mistydning og misidentifikation","description":"19_003 Forestillinger om overvågning\nSynes du, at folk taler om dig, holder øje med hvor du er ?\n? Føler du dig overvåget og udspioneret? ? Bliver din telefon aflyttet? ? Er der opsat mikrofoner? ? Er der nogen, der følger efter dig og noterer sig din færden?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.003,"ratingscale":2},{"key":"19.004","title":"Selvhenføringsforestillinger","description":"Kommer andre med udtalelser, som på en eller anden måde er møntet på dig? - Eller kommer de med dobbeltbundede bemærkninger?\nBliver der hentydet til dig i aviser eller fjernsyn? - Modtager du besked eller særligt budskab herigennem ?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.004,"ratingscale":2},{"key":"19.005","title":"Mistydning af omgivelserne","description":"Er det hele lagt tilrette på en særlig måde med en særlig betydning for dig? - Måske for at give dig en besked?\n? Fx på den måde tingene er lagt frem på eller stillet op på? ? Eller gennem den måde farverne er arrangeret på?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.005,"ratingscale":2},{"key":"19.006","title":"Forestilling om citering af idéer eller tanker","description":"Hænder det at noget du tidligere har tænkt på eller talt om bliver citeret i fjernsynet eller i aviserne, så det på en eller anden måde hentyder til dig?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.006,"ratingscale":2},{"key":"19.007","title":"Misidentifikationsforestillinger","description":"Er menneskene omkring dig andre end de ser ud til eller giver sig ud for at være?\n? Optræder de måske i forklædning? ? Har du set nogen du kender fra tidligere i dit liv?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.007,"ratingscale":2},{"key":"19.008","title":"Forestillinger om efterligning af nærtstående personer","description":"Har du haft tanker om at en af dine nærmeste er blevet byttet ud med en efterligning? Synes du at nogen af dine nærmeste har forandret udseende, så du fik mistanke om, at de var blevet byttet ud med andre, som efterlignede dem?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.008,"ratingscale":2},{"key":"19.009","title":"Primær vrangsansning","description":"Vrangagtige sansningsoplevelser - oplevelser af ganske særlig mening eller betydning i forbindelse med ellers normale sanseoplevelser - oftest af selvhenførende karakter og med åbenbaringslignende, mystisk eller truende kvalitet.\nHar du haft en oplevelse af, at du pludselig forstod, at dét du så eller hørte, havde en helt særlig betydning?\n? Er du sikker på, at det er sådan det forholder sig? ? Kunne det tænkes at have en naturlig forklaring? ? Hvordan kom du til at tænke på, at det havde denne betydning? ? Havde du luret på, om det kunne forholde sig sådan, eller var det en tanke, der lige pludselig gik op for dig?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.009,"ratingscale":2},{"key":"19.010","title":"Depressive selvhenførende forestillinger","description":"Selvhenførings- eller mistydningsforestillinger opstået i relation til depressiv tilstand, hvor R føler sig anklaget eller skyldig.\n","input":"integer","options":{"0":"Symptomet ikke til stede efter adækvat udspørgen","1":"Symptomet definitivt til stede i perioden, men sjældent eller forbigående","2":"Symptomet definitivt til stede, hyppigt","8":"Uvist om til stede efter adækvat udspørgen"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.01},{"key":"19.011","title":"Maniske selvhenførende forestillinger","description":"Selvhenførings- eller mistydningsforestillinger opstået i relation til manisk tilstand, hvor R føler sig beundret eller begæret.\n","input":"integer","options":{"0":"Symptomet ikke til stede efter adækvat udspørgen","1":"Symptomet definitivt til stede i perioden, men sjældent eller forbigående","2":"Symptomet definitivt til stede, hyppigt","8":"Uvist om til stede efter adækvat udspørgen"},"validate":["0-2",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.011},{"key":"19.012","title":"Forfølgelsesforestillinger","description":"19_012 Forfølgelsesforestillinger\nEr der nogen, der prøver at genere dig? Er der nogen, der prøver at skade dig eller gøre dig fortræd?\n? Eller måske forgifte dig? ? Eller ligefrem at slå dig ihjel? ? Har de særligt udset sig dig? - Hvorfor? ? Hvordan føles det? - Er du bange for dem?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.012,"ratingscale":2},{"key":"19.013","title":"Sammensværgelsesforestillinger","description":"Er der et komplot eller en hel sammensværgelse bag ved?\n? Hvordan har det vist sig?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.013,"ratingscale":2},{"key":"19.014","title":"Seksuelle vrangforestillinger","description":"19_014 Jalousiforestillinger\nEr du jaloux på din ægtefælle eller samlever?\n? Har han/hun været dig utro? ? Hvad får dig til at tro det?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.014,"ratingscale":2},{"key":"19.015","title":"Jalousitendens (ikke paranoid) (= 3.013)","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"glossary":19.015,"ratingscale":2},{"key":"19.016","title":"Graviditetsforestillinger","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.016,"ratingscale":2},{"key":"19.017","title":"Erotomane forestillinger","description":"Er der nogen, der er forelsket i dig uden at andre ved om det?\n? Hvem er det? ? Hvad får dig til at tro det? ? Har du prøvet at komme i kontakt med vedkommende?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.017,"ratingscale":2},{"key":"19.018","title":"Forestillinger om beskyldning for homoseksualitet","description":"Kommer andre med antydninger om, at du er homoseksuel?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.018,"ratingscale":2},{"key":"19.019","title":"Andre vrangforestillinger og forklarende vrangforestillinger","description":"19_019 Vrangerindringer og fantastiske vrangforestillinger\nEr du kommet i tanke om, at du kan huske, at du engang var udsat for særlige overgreb eller oplevelser? Har du været udsat for helt usædvanlige oplevelser? (Science fiction karakter)\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.019,"ratingscale":2},{"key":"19.020","title":"Optagethed af tidligere vrangforestillinger","description":"Optagethed og fortsat overbevisning om realiteten af tidligere ikke aktuelle vrangforestillinger.\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.02,"ratingscale":2},{"key":"19.021","title":"Religiøse vrangforestillinger","description":"Er du meget optaget af religiøse tanker?\n? Modtager du religiøse budskaber? ? Er du måske guddommelig selv?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.021,"ratingscale":2},{"key":"19.022","title":"Vrangforestillinger om overnaturlige forklaringer","description":"Foregår der noget okkult? - Noget med hypnose eller telepati?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.022,"ratingscale":2},{"key":"19.023","title":"Vrangforestillinger om fysisk forklaring","description":"Bliver du påvirket af stråler? - Strøm eller computere?\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.023,"ratingscale":2},{"key":"19.024","title":"Kulturspecifikke syndromer","description":"Se annex 2 i ICD-10 Diagnostic Criteria for Research.\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,5,8,9],"episodes":1,"glossary":19.024},{"key":"19.025","title":"Vrangforestillinger, som også er rated andetsteds","description":"19_025 Depressive vrangforestillinger om skyld eller uduelighed (= 6.018)\nBrug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.025,"ratingscale":2},{"key":"19.026","title":"Depressive vrangforestillinger om død eller ulykke (= 6.019) Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.026,"ratingscale":2},{"key":"19.027","title":"Depressive hypokondre eller nihilistiske vrangforestillinger (= 6.020)","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.027,"ratingscale":2},{"key":"19.028","title":"Hypokondre vrangforestillinger, ikke i forbindelse med depression","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.028,"ratingscale":2},{"key":"19.029","title":"Vrangforestillinger om grandiøse evner (= 10.016)","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.029,"ratingscale":2},{"key":"19.030","title":"Vrangforestillinger om grandiøs identitet (= 10.017)","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.03,"ratingscale":2},{"key":"19.031","title":"Dysmorfofobe vrangforestillinger (= 16.012) Brug Rating Skala II","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.031,"ratingscale":2},{"key":"19.032","title":"Depersonalisations- og derealisations-vrangforestillinger (= 16.013)","description":"Brug Rating Skala II\n","input":"string","validate":["0-3",5,8,9],"episodes":1,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.032,"ratingscale":2},{"key":"19.033","title":"Generelle ratings for vrangforestillinger","description":"19_033 Varighed af vrangforestilling(er) (uger)\n87 = 87 uger eller mere\n","input":"string","validate":["0-87",88,99],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.033},{"key":"19.034","title":"Monotematiske vrangforestillinger","description":"Kun én type vrangforestillinger er til stede.\n","input":"integer","options":{"0":"Ingen vrangforestillinger","1":"Vrangforestillingerne er ikke monotematiske","2":"Kun monotematiske vrangforestillinger"},"validate":["0-2",8,9],"episodes":1,"glossary":19.033},{"key":"19.035","title":"Systematisering af vrangforestillinger","input":"integer","options":{"0":"Ingen vrangforestillinger","1":"Ingen systematisering, - spredte og usammenhængende vrangforestillinger","2":"Nogen systematisering","3":"Overvejende systematisering","4":"Fuldstændig systematisering af vrangforestillinger"},"validate":["0-4",8,9],"episodes":1,"glossary":19.033},{"key":"19.036","title":"Prominens i sygdomsbilledet","input":"integer","options":{"0":"Ingen vrangforestillinger","1":"Vrangforestillinger til stede men ikke fremtrædende i sygdomsbilledet","2":"Vrangforestillinger fremtrædende i sygdomsbilledet i forhold til andre symptomer"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.033},{"key":"19.037","title":"Stemningskongruens","input":"integer","options":{"0":"Ingen vrangforestillinger","1":"Overvejende stemningskongruens","2":"Både kongruens og inkongruens","3":"Overvejende stemningsinkongruens"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.033},{"key":"19.038","title":"Stemningspræg","input":"integer","options":{"0":"Ingen kongruens","1":"Kongruent med depressivt stemningsleje","2":"Kongruent med både depressivt og manisk stemningsleje","3":"Kongruent med manisk stemningsleje"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.033},{"key":"19.039","title":"Overbevisning om rigtigheden af vrangforestillingerne/hallucinationerne (indsigt)","input":"integer","options":{"0":"Ingen vrangforestillinger eller hallucinationer","1":"Erkendelse af forestillingernes eller hallucinationernes sygelige natur","2":"Kan kortvarigt tvivle, men er overvejende overbevist","3":"Urokkelig overbevisning"},"validate":["0-3",8,9],"episodes":1,"glossary":19.033},{"key":"19.040","title":"Handlinger baseret på vrangforestillinger eller hallucinationer","input":"integer","options":{"0":"Ingen vrangforestillinger eller hallucinationer eller ingen handlinger","1":"Handlinger men uden alvorlige sociale eller familiemæssige konsekvenser","2":"Nogle handlinger med kompromitterende eller ulykkelige følger","3":"Handlinger med aggressive eller voldelige følger"},"validate":["0-3",8,9],"episodes":1,"glossary":19.033},{"key":"19.041","title":"Bizar karakter af vrangforestillinger","description":"Bizarre forestillinger er fuldstændig umulige og kulturelt uacceptable (ICD-10) - eller overordentlig usandsynlige og kulturelt uacceptable (DSM-IV).\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.041},{"key":"19.042","title":"Begyndelsesalder for vrangforestillinger","input":"string","validate":["0-99"],"glossary":19.042},{"key":"19.043A","title":"Tidsangivelse for sektion 19 symptomer","description":"Rates kun hvis den er forskellig fra tidsangivelsen i 1.007-1.114\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":19.043},{"key":"19.043B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.043},{"key":"19.043C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":19.043},{"key":"19.043D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"glossary":19.043},{"key":"19.044","title":"Påvirkning af aktiviteter på grund af sektion 19 symptomer","input":"integer","options":{"0":"Ingen sektion 19 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.044},{"key":"19.045","title":"Organisk årsag til sektion 19 symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.045},{"key":"19.046","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":19.046},{"key":"20.001","title":"Yderligere information til brug for diagnostisk klassifikation af Part 2 symptomer","description":"Det er kun nødvendigt, at rate items 20.001 - 20.013 vedrørende anamnesen én gang. Anvend venstre box. Det er vigtigt, især i forhold til DSM-IV, også at rate sektion 22, 23, 24, 25 og 27, hvis der kan være tale om skizofreni, paranoide psykotiske tilstande eller affektiv sindslidelse med psykotiske symptomer.\n20_001 Varighed af skizofren episode (måneder) DSM-IV\nInklusive prodromale, aktive og residuale fase.\nSymptomer, der inkluderes i den aktive fase, er nævnt under item 20.002. Prodromal- og residual-faserne er karakteriseret ved negative symptomer (se items 20.089 - 20.112 for definition) eller usædvanlige sanseoplevelser, sære idéer, paranoide tanker og lignende.\n888 = Ukendt\n","input":"string","section_start":"Section_20","validate":["0-99",888,999],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.002","title":"Varighed af skizofrent syndrom (DSM-IV aktive fase ), DSM-IV","description":"Følgende symptomer skal have været til stede i en betydelig del af tiden i den aktive fase: mindst ét 1.rangssymptom og /eller mindst to af følgende symptomer: vrangforestillinger, hallucinationer, sproglige tankeforstyrrelser, kataton adfærd eller negative symptomer.\n","input":"integer","options":{"0":"Episoden indeholdt ikke aktiv fase af en uges varighed","1":"Aktiv fase mindre end en uge på grund af behandling","2":"Aktiv fase mere end en uge, men mindre end en måned","3":"Aktiv fase mindre end en måned på grund af behandling","4":"Aktiv fase én måned eller mere","8":"Uvist","9":"NA (fx hvis 20.001 rates mindre end 006)"},"validate":["0-4",8,9],"used_in_dsmiv":1,"glossary":20.001},{"key":"20.003","title":"Forløb af skizofren episode, DSM-IV","description":"Anvendes når forløbet har varet mindst et år.\n","input":"integer","options":{"0":"Ingen skizofren episode","1":"Episodisk med residuale symptomer mellem episoderne","2":"Episodisk med fremtrædende negative symptomer mellem episoderne","3":"Episodisk uden residuale symptomer mellem episoderne","4":"Kontinuerlig med karakteristiske symptomer","5":"Kontinuerlig med karakteristiske symptomer og fremtrædende negative symptomer","6":"Enkeltepisode med delvis remission","7":"Enkeltepisode med fuld remission","8":"Enkeltepisode med delvis remission med fremtrædende negative symptomer","9":"Andet eller uspecificeret forløb, NA fx varighed mindre end ét år"},"validate":["0-9"],"used_in_dsmiv":1,"glossary":20.001},{"key":"20.004","title":"Relative varighed af affektive episoder i forbindelse med skizofren episode, DSM-IV","description":"Samlede varighed af episoder med fuldt manisk eller depressivt syndrom i forhold til varigheden af et samtidigt skizofrent syndrom (DSM-IV aktivfase).\n","input":"integer","options":{"0":"Ingen affektive episoder","1":"Varigheden af affektive episoder kort","2":"Varigheden af maniske eller bipolare episoder lang","3":"Varigheden af depressive episoder (major depression) lang","8":"Uvist om affektive episoder eller skizofrent syndrom varede længst","9":"NA"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":20.001},{"key":"20.005","title":"Autistisk syndrom og skizofreni, DSM-IV","input":"integer","options":{"0":"Ikke til stede","1":"Autistisk syndrom til stede uden fremtrædende hallucinationer eller vrangforestillinger","2":"Autistisk syndrom til stede samtidig med skizofrent syndrom med fremtrædende hallucinationer eller vrangforestillinger"},"validate":["0-2",8,9],"used_in_dsmiv":1,"glossary":20.001},{"key":"20.006","title":"Relative varighed af affektivt syndrom og paranoidt syndrom i samme forløb, ICD-10","input":"integer","options":{"0":"Ikke til stede","1":"Paranoidt syndrom relativt længerevarende","2":"Manisk eller bipolart syndrom relativt længerevarende","3":"Depressivt syndrom relativt længerevarende","4":"Affektivt og paranoidt syndrom begge fremtrædende samtidigt","8":"Uvist om affektivt eller paranoidt syndrom var længerevarende"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.007","title":"Varighed af skizofrent syndrom, ICD-10","input":"integer","options":{"0":"Ikke til stede","1":"Skizofrent syndrom varede mindre end en måned","2":"Syndromet varede mindst én måned"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.008","title":"Forløbet af skizofren sindslidelse, ICD-10","input":"integer","options":{"0":"Ikke til stede","1":"Kontinuerlig","2":"Episodisk med fremadskridende defekt (= udvikling af negative symptomer)","3":"Episodisk med stabil defekt (= udvikling af negative smptomer)","4":"Episodisk, remitterende","5":"Enkeltepisode med inkomplet remission","6":"Enkeltepisode med komplet remission","8":"Andet forløb","9":"Forløbet ubestemmeligt, fx af mindre varighed end et år"},"validate":["0-9"],"episodes":0,"used_in_icd10":1,"glossary":20.001},{"key":"20.009","title":"Tidligere skizofrent syndrom, ICD-10","input":"integer","options":{"0":"Ikke rated","1":"Ikke tidligere episode med skizofrent syndrom","2":"Mindst én tidligere episode med skizofrent syndrom","8":"Uvist"},"validate":["0-2",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.010","title":"Forholdet mellem affektivt syndrom og skizofrent syndrom, ICD-10","input":"integer","options":{"0":"Intet affektivt syndrom","1":"Affektivt syndrom først, skizofrent syndrom dominerende","2":"Affektivt syndrom først og dominerende","3":"Skizofrent syndrom først, affektivt syndrom dominerende","4":"Skizofrent syndrom først og dominerende","5":"Både skizofrent og affektivt syndrom til stede samtidigt i ligelig grad","8":"Uvist om hvilket syndrom, der var først og dominerende","9":"NA"},"validate":["0-5",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.011","title":"Post-skizofren depression, ICD-10","description":"Depressivt syndrom optrædende inden for et år efter et skizofrent syndrom, hvor der fortsat er ét eller flere skizofrene symptomer til stede, dog ikke 1.rangssymptomer. Det depressive syndrom forudsættes rated i sektion 6,7,8.\n","input":"integer","options":{"0":"Ikke rated","1":"Ingen post-skizofren depression","2":"Sandsynlig post-skizofren depression","3":"Definitiv post-skizofren depression","8":"Uvist","9":"NA"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.012","title":"Varighed af paranoidt syndrom (måneder), ICD-10","description":"888 = Uvist 999 = Ikke relevant\n","input":"string","validate":["0-99",888,999],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.013","title":"Persisterende hallucinationer eller vrangforestillinger efter samtidigt skizofrent og affektivt syndrom, DSM-IV","input":"integer","options":{"0":"Ikke rated","1":"Hallucinationer og vrangforestillinger altid samtidig med affektive symptomer","2":"Hallucinationer og vrangforestillinger til stede gennem mindst 2 uger uden affektive symptomer","3":"Hallucinationer og vrangforestillinger til stede gennem mindst en måned uden affektive symptomer","9":"NA"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.001},{"key":"20.014","title":"Checkliste for akut forbigående psykotisk tilstand","description":"20_014 Akut indsætten\nAkut indsætten af hallucinationer, vrangforestillinger, uforståelig eller usammenhængende tale af mindst flere timers varighed.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.015","title":"Fuld udvikling af psykotisk tilstand","input":"integer","options":{"1":"Inden for 48 timer","2":"Inden for 2 uger","3":"Inden for mere end 2 uger"},"validate":["1-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.016","title":"Varighed","description":"Varighed fra indsætten til fuld remission uden hensyn til behandling.\n","input":"integer","options":{"1":"Op til 1 måned","2":"1 - 3 måneder","3":"3 måneder eller mere"},"validate":["1-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.017","title":"Polymorfe symptomer","description":"Varierende, desorganiserede og hurtigt skiftende symptomer.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.018","title":"Forudgående traume eller belastning inden for seneste 2 uger","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.019","title":"Kaotisk og skiftende emotionelle symptomer","description":"Kaotisk emotionel tilstand skiftende mellem intens lykkefølelse eller ekstase, overvældende angst eller udtalt irritabilitet.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.020","title":"Affektivt syndrom","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"glossary":20.014},{"key":"20.021","title":"Forvirring, person- eller stedforveksling","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.022","title":"Motilitetsforstyrrelser","description":"Udtalt, øget eller nedsat motilitet.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede"},"validate":[0,1,8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.023","title":"Begyndelsesalder for akut forbigående psykotisk tilstand","input":"string","validate":["0-99"],"glossary":20.014},{"key":"20.024","title":"Organisk årsag til akut psykotisk tilstand","input":"string","validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.025","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":1,"glossary":20.014},{"key":"20.026","title":"Checkliste for induceret paranoid psykotisk tilstand","description":"20_026 Nært emotionelt tilknytningsforhold mellem to (eller flere) personer\n","input":"integer","options":{"0":"Fraværende","1":"Til stede, intermitterende","2":"Til stede gennem lang tid"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.027","title":"Inducerede vrangforestillinger","description":"Vrangforestillinger overført fra dominerende til passiv(e) person(er) (R) i forholdet.\n","input":"integer","options":{"0":"Ikke til stede","1":"Intermitterende, til stede","2":"Til stede gennem lang tid"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.028","title":"Tidligere psykotisk tilstand hos passiv(e) person(er)","description":"Klar evidens for at den dominerende person udviklede psykotisk tilstand med vrang- forestillinger først og at den passive person ikke havde haft tidligere uafhængig psykotisk tilstand med vrangforestillinger, forud for kontakten med den dominerende person.\n","input":"integer","options":{"0":"Tidligere uafhængig psykotisk tilstand hos passive person","1":"Dominerende person muligvis psykotisk tilstand før passive person","2":"Dominerende person først udviklet psykotisk tilstand med vrangforestillinger, ikke tidligere uafhængig psykotisk tilstand hos passive person"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.029","title":"Begyndelsesalder for induceret psykotisk tilstand","description":"888 = Uvist 999 = Ikke relevant\n","input":"string","validate":["0-99",888,999],"glossary":20.014},{"key":"20.030","title":"Checkliste for skizotypisk sindslidelse, ICD-10","description":"Tilstanden skal have varet gennem mindst 2 år med vedvarende eller tilbagevendende optræden af mindst 4 af de nævnte symptomer.\n20_030 Inadækvat eller indsnævrede følelser\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.031","title":"Excentrisk, sær eller aparte udseende eller adfærd","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.032","title":"Kontaktfattigdom og isolationstendens","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.033","title":"Ingen nære venner eller fortrolige, bortset fra førstegradsslægtninge","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"glossary":20.014},{"key":"20.034","title":"Udtalt og vedvarende socialangst","description":"Svinder ikke ved tilvænning, og er ofte forbundet med paranoid mistydning.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"glossary":20.014},{"key":"20.035","title":"Sære idéer eller magisk tænkning","description":"Ikke vrangforestillinger: svarer ikke til subkulturelt mønster, men påvirker adfærd.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.036","title":"Mistroiskhed eller paranoide tanker","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.037","title":"Obsessive ruminationer uden indre modstand","description":"Tvangspræget grublen, ofte med seksuelt, aggressivt eller dysmorfofobisk indhold.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.038","title":"Usædvanlige sanseoplevelser","description":"Somatosensoriske eller andre illusioner, depersonalisation eller derealisation.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.039","title":"Vag, omstændelig, metaforisk, kunstig eller stereotyp tankegang og tale","description":"Ikke større inkoherens eller irrelevans.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.040","title":"Mikropsykotiske episoder","description":"Kortvarige, forbigående psykoselignende episoder med hallucinationer og vrangagtige idéer, sædvanligvis uden ydre provokation.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.041","title":"Selvhenførende idéer (ikke vrangforestillinger)","input":"integer","options":{"0":"Fraværende","1":"Til stede med ringe påvirkning af dagliglivsaktiviteter","2":"Til stede med udtalt påvirkning af dagliglivsaktiviteter"},"validate":["0-2",5,8,9],"glossary":20.014},{"key":"20.042","title":"Begyndelsesalder for skizotypisk sindslidelse","input":"string","validate":["0-99"],"glossary":20.014},{"key":"20.043","title":"Checkliste for simplex skizofreni, ICD-10","description":"Tilstand med snigende udvikling af socialt invaliderende negative symptomer og adfærdstræk over mere end et år. R må aldrig have opfylde de generelle kriterier (ICD-10) for skizofreni.\n20_043 Personligheds- eller adfærdsændringer\nManglende fremdrift eller interesser, tom eller formålsløs adfærd, indsynken i sig selv, social tilbagetrækning eller ensomhedssøgen.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede gennem mindst et år"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.044","title":"Negative symptomer","description":"Affektaffladning, passivitet, sprogfattigdom, kontaktforringelse.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede gennem mindst et år"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.045","title":"Social funktionsnedsættelse","description":"Udtalt nedsættelse af sociale funktioner i forhold til arbejde, uddannelse, interpersonelle eller egne forhold.\n","input":"integer","options":{"0":"Fraværende","1":"Til stede gennem mindst et år"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.046","title":"Psykotiske symptomer Nu eller tidligere","input":"integer","options":{"0":"Aldrig psykotiske symptomer","1":"Til stede"},"validate":[0,1,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.014},{"key":"20.047","title":"Begyndelsesalder for simplex skizofreni","input":"string","validate":["0-99"],"glossary":20.014},{"key":"20.048","title":"Påvirkning af dagliglivsaktiviteter på grund af Part 2 symptomer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.049","title":"S16 Perceptionsforstyrrelser andre end hallucinationer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.050","title":"S17 Hørelseshallucinationer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.051","title":"S17 Synshallucinationer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.052","title":"S17 Andre hallucinationer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.053","title":"S18 Tankeforstyrrelsesoplevelser","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.054","title":"S18 Styringsoplevelser","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.055","title":"S19 Vrangforestillinger","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.056","title":"S20 Negative symptomer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.057","title":"S21 Kognitiv svækkelse","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.058","title":"S22 Motorik og adfærd","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.059","title":"S23 Følelser","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.060","title":"S24 Tale","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":20.048},{"key":"20.061","title":"Andre","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.062","title":"Multiple Part 2 symptomer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.048},{"key":"20.063","title":"Organiske årsager","description":"De diagnostiske kategorier i ICD-10 og DSM-IV tager forbehold for organiske årsager, som fører til kategorisering under ICD-10 organiske eller alkohol-/stof-betingede psykiske lidelser, og for DSM-IV disorders due to a general medical condition og substance induced disorders. Registrering af organiske årsager foretages på sektionsniveau i slutningen af hver sektion. De omfatter hjerneorganiske lidelser og systemiske sygdomme med påvirkning af hjernefunktionen, samt lidelser som følge af brug af alkohol eller andre psykoaktive stoffer eller medikamenter. Item 20.063 - 20.079 giver mulighed for en samlet registrering af sådanne organiske årsager. Kriterier for rating af organisk årsag:\nA Der er objektiv evidens for eller anamnestiske oplysninger om cerebral skade, sygdom eller dysfunktion eller systemisk sygdomstilstand førende til cerebral dysfunktion, inklusive hormonelle forstyrrelser, alkohol eller psykoaktiv substans-relateret virkning.\nB Der er sandsynlig forbindelse mellem opståen eller tydelig forværring af den grundliggende sygdom, skade eller dysfunktion og de psykiske symptomer (umiddelbart eller efter latenstid).\nC De psykiske symptomer eller lidelser er ikke bedre forklaret ved andre årsager (fx ved tidligere episoder af lignende karakter uden forbindelse med organiske faktorer, eller ved udtalt familiær disposition til lidelse med kendt arvelighed).\nD Helbredelse eller tydelig bedring af de psykiske symptomer efter afhjælpning af de formodede fysiske årsager.\n20_063 Kvaliteten af foreliggende data til registrering af organisk årsag\n","input":"integer","options":{"0":"Data utilstrækkelige","1":"Data fuldt tilstrækkelige x7920@'dc____"},"validate":[0,1],"episodes":1,"glossary":20.063},{"key":"20.064","title":"S16 Perceptionsforstyrrelser andre end hallucinationer","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.065","title":"S17 Hørelseshallucinationer","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.066","title":"S17 Synshallucinationer","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.067","title":"S17 Andre hallucinationer","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.068","title":"S18 Tankeforstyrrelsesoplevelser","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.069","title":"S18 Styringsoplevelser","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.070","title":"S19 Vrangforestillinger","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.071","title":"S20 Negative symptomer","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.072","title":"S21 Kognitiv svækkelse","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.073","title":"S22 Motorik og adfærd","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.074","title":"S23 Følelser","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.075","title":"S24 Tale","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.076","title":"Andre","input":"integer","options":{"0":"Ingen organisk årsag til Part 2 symptomer","1":"Mistanke om organisk årsag, men data utilstrækkelige","2":"Sandsynlig organisk årsag, A, B og C opfyldte","3":"Definitiv organisk årsag, A, B, C og D alle opfyldte"},"validate":["0-3",9],"episodes":1,"used_in_dsmiv":1,"glossary":20.064},{"key":"20.077A","title":"A 1. Organisk årsag for Part 2 symptomer","description":"Anfør de to sidste cifre fra pågældende symptomgruppe 20.064-20.076 (fx 69 for 20.069) i øverste box for PS og i nederste box for RE eller LB.\n","input":"string","validate":["48-76"],"episodes":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.077B","title":"B 1. Organisk årsag for Part 2 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.078A","title":"A 2. Organisk årsag for Part 2 symptomer","description":"Hvis det er nødvendigt med endnu en organisk årsag, anfør da de to sidste cifre fra pågældende symptomgruppe (fx 68 for 20.068)\n","input":"string","validate":["48-76"],"episodes":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.078B","title":"B 2. Organisk årsag for Part 2 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.079A","title":"A 3. Organisk årsag for Part 2 symptomer","description":"Hvis en 3. organisk årsag er til stede, anfør de to sidste cifre fra pågældende symptomgruppe (fx 69 for 20.069)\n","input":"string","validate":["48-76"],"episodes":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.079B","title":"B 3. Organisk årsag for Part 2 symptomer","description":"Anfør ICD-10-kode for den organiske årsag i øverste box (PS) eller nederste box (RE/LB).\n","input":"string","fitb_size":"ICD10","episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.077},{"key":"20.080","title":"Barnefødsel (rates kun 2 eller 3 hvis inden for seneste 4 uger), Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb Andre forhold af betydning for fremtræden af Part 2 Symptomer Har særlige omstændigheder haft betydning for opståen eller forværring af (Part 2 symptomer)? Rates efter klinisk skøn ud fra alle foreliggende oplysninger."},"validate":["0-3",9],"used_in_dsmiv":1,"glossary":20.08},{"key":"20.081","title":"Menopause, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.082","title":"Tab af nærtstående (rates kun 2 eller 3 hvis inden for seneste 3 måneder), Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.083","title":"Adolescens, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.084","title":"Medlemskab af særlig subkultur, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.085","title":"Nylig tilflytning, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.086","title":"Særlige livsbegivenheder (life events), Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.087","title":"Social nødstilstand, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.088","title":"Andre, Part 2","input":"integer","options":{"0":"Ingen","1":"Faktor til stede men af usikker betydning","2":"Faktor af medvirkende betydning for sygdomsbillede og forløb, men ikke (3)","3":"Forårsagende eller udløsende faktor, eller faktor af udtalt betydning for sygdomsbillede og forløb"},"validate":["0-3",9],"glossary":20.08},{"key":"20.089","title":"Affektaffladning","description":"Rates ud fra iagttagelse af ansigtsudtryk, gestikulation og stemmeføring.\nRating:\n","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder For hvert item bedømmes om det er primært eller forårsaget af andre forhold. Negative symptomer Negative symptomer optræder ved skizofreni, som mulige symptomer i forbindelse med de generelle kriterier, men især ved residual skizofreni og simplex skizofreni."},"validate":["0-4"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.089},{"key":"20.090","title":"Affektaffladning - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.09},{"key":"20.091","title":"Indsnævret følelses-intensitet og -omfang","description":"Kan du rigtig glæde dig over livet? Forekommer andre gladere eller har de lettere ved at nyde tilværelsen end dig?\nHvad er der i vejen? - Er der noget, der gør dig ked af det? - Noget du er utilfreds med, eller som irriterer dig?\n","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.091},{"key":"20.092","title":"Indsnævret følelses-intensitet og -omfang - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.092},{"key":"20.093","title":"Ord- eller indholds-fattig tale","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.093},{"key":"20.094","title":"Ord- eller indholds-fattig tale - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.094},{"key":"20.095","title":"Indsnævring af interesse","description":"Hvad interesserer du dig særligt for? Bruger du megen tid på dine interesser?\nFølger du med i aviser eller fjernsyn? - Kan du fortælle mig, hvad der er sket for\nnylig?\n","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.095},{"key":"20.096","title":"Indsnævring af interesse - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.096},{"key":"20.097","title":"Nedsat motivation og målrettet aktivitet","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.097},{"key":"20.098","title":"Nedsat motivation og målrettet aktivitet - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.098},{"key":"20.099","title":"Nedsat socialt initiativ","description":"Hvad foretager du dig? Er det alene eller sammen med andre? Vil du helst være alene? Føler du dig somme tider ensom? Vil du gerne være sammen med andre, foretage dig noget sammen med andre? Når du er sammen med andre, hvem bestemmer så, hvad der skal ske? Når du er sammen med andre, tager du så initiativ eller gør de andre?\n","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.099},{"key":"20.100","title":"Nedsat socialt initiativ - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.1},{"key":"20.101","title":"Nedsat social interaktion","description":"Har du svært ved at tale med andre? Føler du at de andre driller dig eller gør dig til grin? Hvordan er din selvtillid når du er sammen med andre?\n","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",5,8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.101},{"key":"20.102","title":"Nedsat social interaktion - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.102},{"key":"20.103","title":"Selvforsømmelse","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.103},{"key":"20.104","title":"Selvforsømmelse - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.104},{"key":"20.105","title":"Nedsat nonverbal kommunikation","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.105},{"key":"20.106","title":"Nedsat nonverbal kommunikation - primær eller sekundær Rating for årsager til negative symptomer:","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.106},{"key":"20.107","title":"Psykomotorisk langsomhed eller underaktivitet","input":"integer","options":{"0":"Ikke til stede, normalt omfang og intensitet af item","1":"Let nedsættelse","2":"Moderat nedsættelse","3":"Svær nedsættelse","4":"Til stede, men ikke inden for seneste 12 måneder"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.107},{"key":"20.108","title":"Psykomotorisk langsomhed eller underaktivitet - primær eller sekundær","input":"integer","options":{"0":"Symptomet er primært, ikke sekundært til andre forhold","1":"Skyldes angst","2":"Skyldes medikamentel behandling","4":"Skyldes mistroiskhed eller andre psykotiske symptomer","8":"Skyldes mental retardering 16 Skyldes depressionstilstand R kan godt rates for flere årsager, i så tilfælde læg scores sammen (max. 31 points)."},"validate":["0-31"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.108},{"key":"20.109A","title":"Tidsangivelse for negative symptomer","description":"Rates kun hvis den er forskellig fra tidsangivelsen i 1.007-1.114\nBegyndelsesdato for PS eller PE\n","input":"date","fitb_size":"DATE_NORMAL","glossary":20.109},{"key":"20.109B","title":"Varighed af PS eller PE (dage)","input":"string","validate":["0-999"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.109},{"key":"20.109C","title":"Begyndelsestidspunkt for RE eller LB","input":"date","fitb_size":"DATE_NODAY","glossary":20.109},{"key":"20.109D","title":"Varighed af RE eller LB (uger)","input":"string","validate":["0-299"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":20.109},{"key":"20.110","title":"Påvirkning af aktiviteter på grund af negative symptomer","input":"integer","options":{"0":"Ingen negative symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"glossary":20.11},{"key":"20.111","title":"Organisk årsag til negative symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"glossary":20.111},{"key":"20.112","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":20.112},{"key":"20.113","title":"Indsigt i Part 2 symptomer","input":"integer","options":{"0":"Ingen psykotiske symptomer","1":"God indsigt","2":"Usikker, tøvende indsigt - ''ved ikke''","3":"Ingen indsigt - forklarende vrangforestillinger"},"validate":["0-3",8,9],"episodes":1,"glossary":20.113},{"key":"20.114","title":"Pålidelighed af ratings i sektionerne 16 - 19","input":"integer","options":{"0":"Ingen problemer, ratings pålidelige","1":"Nogle problemer, men de fleste ratings skønnes pålidelige","2":"Store problemer med at gennemføre interviewet, tvivl om pålideligheden af ratings","3":"Umuligt at rate symptomer pålideligt"},"validate":["0-3",8,9],"episodes":1,"glossary":20.114},{"key":"20.115","title":"Pålidelighed af ratings i sektionerne 22 - 24","input":"integer","options":{"0":"Ingen problemer, ratings pålidelige","1":"Nogle problemer, men de fleste ratings skønnes pålidelige","2":"Store problemer med at gennemføre interviewet, tvivl om pålideligheden af ratings","3":"Umuligt at rate symptomer pålideligt"},"validate":["0-3",8,9],"episodes":1,"glossary":20.115},{"key":"21.001","title":"Kognitiv forstyrrelse eller svækkelse A: Screening for kognitiv forstyrrelse","description":"21_001 Tegn for kognitiv forstyrrelse eller svækkelse\n","input":"integer","options":{"0":"Kognitiv screening ikke udført, ingen mistanke om kognitiv forstyrrelse eller svækkelse fra øvrige del af interviewet","1":"Patienten kan ikke svare på spørgsmål eller deltage i interviewet på grund af udtalt kognitiv svækkelse","2":"Mistanke om kognitiv forstyrrelse eller svækkelse, bør undersøges nærmere","9":"Interview ikke gennemført, af andre grunde end (0) og (1) SKIP => til 21.025 hvis 21.001 rates 1 Sektion 21 har 3 dele A: screening for kognitiv forstyrrelse eller svækkelse B: symptomer og adfærdstræk ved kognitiv forstyrrelse eller svækkelse C: objektive iagttagelser, anamnestiske oplysninger og ætiologiske forhold Items omfatter mange funktions- og færdigheds-prøver med egne ratings, der registrerer graden af forstyrrelsen eller svækkelsen."},"section_start":"Section_21","validate":["0-2",8,9],"glossary":21.001},{"key":"21.002A","title":"Verbal sportest","description":"21_002A Verbal sportest\nPatienten opfordres til at skifte mellem talrækken og alfabetet: 1 - A, 2 - B, osv. indtil 13 - M. Ret eventuelle fejl ved at gentage det sidste korrekte par.\nRating: Antal sekunder med 3 cifre, for eksempel 045:\n888 = 2 minutter eller mere, eller ude af stand til at gennemføre på grund af kognitiv svækkelse\n999 = ikke gennemført af andre grunde end kognitiv svækkelse\nMere end 60 sekunder eller mere end 2 fejl er tegn på kognitiv forstyrrelse eller svækkelse hos personer under 65 år med op til 12 års skolegang eller uddannelse.\nMere end 45 sekunder eller mere end 2 fejl giver mistanke om kognitiv forstyrrelse hos personer under 65 år med mindst 16 års skolegang eller uddannelse.\nMindre end 35 sekunder og 0 fejl: ingen kognitiv forstyrrelse eller svækkelse.\n","input":"string","validate":["0-120",888,999],"glossary":21.002},{"key":"21.002B","title":"B Verbal sportest (fortsat)","description":"Rate antal fejl med 2 cifre:\n88 = ikke gennemført på grund af kognitiv svækkelse 99 = ikke rated af andre grunde end kognitiv svækkelse\n","input":"string","validate":["0-13"],"glossary":21.002},{"key":"21.003","title":"Mini - mental status eksamination (MMSE)","description":"21_003 Ugedag\nHvilken dag i ugen er det?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.003},{"key":"21.004","title":"Dato","description":"Hvilken dato har vi i dag?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.003},{"key":"21.005","title":"Måned","description":"Hvilken måned har vi nu?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.003},{"key":"21.006","title":"Årstal","description":"Hvilket år skriver vi?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.003},{"key":"21.007","title":"Årstid","description":"Hvilken årstid har vi?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.003},{"key":"21.008","title":"Områdets navn (forstad, bydel)","description":"Hvad hedder området her (forstaden, bydelen)?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.008},{"key":"21.009","title":"Byens navn","description":"Hvad hedder byen vi er i?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.009},{"key":"21.010","title":"Gadens navn","description":"Hvad hedder gaden her ude?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.01},{"key":"21.011","title":"Stedets navn","description":"Hvad hedder navnet på stedet, bygningen, afdelingen her?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"glossary":21.011},{"key":"21.012","title":"Landets navn","description":"Hvad hedder landet vi befinder os i?\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"glossary":21.012},{"key":"21.013L","title":"Umiddelbar genkaldelse","description":"Nævn navne på 3 genstande (tostavelses navne på genstande, som ikke springer i øjnene i patientens synsfelt, fx æble, lampe, krone).\nBed patienten gentage navnene umiddelbart efter. Hvis patienten ikke kan gengive dem, nævnes navnene på ny - indtil de nævnes korrekt - eller op til 6 gange.\n","input":"integer","options":{"0":"Korrekt i første forsøg 1-5 Antal af ekstra forsøg","6":"Forkert efter 6 forsøg"},"validate":["0-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.013},{"key":"21.013R","title":"Umiddelbar genkaldelse (fortsat)","input":"integer","options":{"0":"Forkert","1":"Husker ét af de 3 navne","2":"Husker to af de 3 navne","3":"Husker alle tre navne"},"validate":["0-3"],"glossary":21.013},{"key":"21.014L","title":"Opmærksomhed og koncentration","description":"Bed patienten trække 7 fra 100, derefter 7 fra det fremkomne tal i alt 5 gange (93, 86,79,72,65).\nRating: Anfør antal korrekte subtraktioner (max. 5).\n","input":"string","validate":["0-5"],"glossary":21.014},{"key":"21.014M","title":"Opmærksomhed og koncentration (fortsat)","description":"Bed patienten stave ord på 5 bogstaver baglæns (ikke for velkendt ord med 5 forskellige bogstaver - fx dansk).\nRating: Antal bogstaver placeret korrekt (max. 5).\n","input":"string","coding_rule":"$21_014L>$21_014M","question_to_be_coded":"21_014R","value_to_be_coded":"$21_014L","coding_else_value":"$21_014M","validate":["0-5"],"glossary":21.014},{"key":"21.014R","title":"","input":"string","ask_this_question_if":0,"glossary":21.014},{"key":"21.015","title":"Korttidshukommelse","description":"Bed patienten nævne de tre navne fra 21.013.\nRating: Antal rigtige svar (max. 3).\n","input":"string","validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.015},{"key":"21.016","title":"Benævne blyant","description":"Vis patienten en blyant eller kuglepen, og spørg hvad den kaldes.\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"glossary":21.016},{"key":"21.017","title":"Benævne ur","description":"Vis patienten et ur, og spørg hvad det hedder.\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"glossary":21.017},{"key":"21.018","title":"Ordremse-gentagelse","description":"Bed patienten gentage ''ingen og'er, hvis'er eller men'er'' eller ''ingen over, under eller ved siden af''.\nKun ét forsøg\n","input":"integer","options":{"0":"Forkert","1":"Korrekt"},"validate":[0,1],"glossary":21.018},{"key":"21.019","title":"Følge tre-delt mundtlig instruktion","description":"Bed patienten tage et stykke papir i sin højre hånd, folde det på midten og lægge det på gulvet.\nRating: 1 point for hver korrekt handling (max. 3).\n","input":"string","validate":["0-3"],"glossary":21.019},{"key":"21.020","title":"Følge læst, skriftlig instruktion","description":"Vis patienten et stykke papir, hvorpå der står skrevet ''luk øjnene''.\n","input":"integer","options":{"0":"Læser ikke, forstår ikke instruktion","1":"Læser og lukker øjnene"},"validate":[0,1],"glossary":21.02},{"key":"21.021","title":"Skrive sætning","description":"Giv patienten et stykke papir og en blyant, og bed ham/hende skrive en sætning med grundled og udsagnsled\n","input":"integer","options":{"0":"Kan ikke udføre instruktionen","1":"Korrekt opbygget sætning med grund- og udsagnsled"},"validate":[0,1],"glossary":21.021},{"key":"21.022","title":"Figurkopiering","description":"Bed patienten kopiere tegning af 2 fem-kanter, der overlejrer hinanden, så der afskæres en rude eller rombe-formet figur.\n","input":"integer","options":{"0":"Forkert","1":"Korrekt udført tegning"},"validate":[0,1],"glossary":21.022},{"key":"21.023","title":"MMSE sum-score","description":"Sammentæl ratings for items 21.003 - 21.022. Max. er 30 points.\nMMSE mindre end 24: er tegn på kognitiv forstyrrelse eller svækkelse hos person over 65 år med op til 12 års skolegang eller uddannelse.\nMMSE mindre end 26: mistanke om kognitiv forstyrrelse hos personer over 65 år med mindst 16 års skolegang eller uddannelse. MMSE over 26 sammen med verbal sportest med 0 fejl på mindre end 35 sekunder:\ningen mistanke om kognitiv forstyrrelse eller svækkelse.\n","input":"string","validate":["0-30"],"glossary":21.023},{"key":"21.024","title":"Tegn på kognitiv forstyrrelse eller svækkelse","input":"integer","options":{"0":"Ingen mistanke om kognitiv forstyrrelse eller svækkelse eller forstyrrelse ved screening","1":"Patienten ude af stand til at besvare spørgsmål eller deltage i undersøgelsen på grund af udtalt kognitiv svækkelse","2":"Mistanke om kognitiv forstyrrelse, bør undersøges nærmere","9":"Screening ikke foretaget af andre grunde end (1) SKIP => til 22.001 hvis der ikke er mistanke om kognitiv forstyrrelse eller svækkelse."},"validate":["0-2",8,9],"glossary":21.024},{"key":"21.025","title":"B: Symptomer og adfærdstræk ved kognitiv forstyrrelse eller svækkelse Bevidsthedstilstand","description":"21_025 Bevidsthedstilstand\n","input":"integer","options":{"0":"Klar bevidsthed, ingen bevidsthedsforstyrrelser","1":"Moderat eller forbigående svær bevidsthedsforstyrrelse","2":"Udtalt og vedvarende bevidsthedsforstyrrelse SKIP => til 21.032 hvis 21.025 rates 0."},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.025},{"key":"21.026","title":"Akut indsætten af kognitiv forstyrrelse","input":"integer","options":{"0":"Ingen akut indsætten","1":"Akut indsætten, inden for dage eller få uger"},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.026},{"key":"21.027","title":"Fluktuerende forløb af kognitiv forstyrrelse (i løbet af dagen )","input":"integer","options":{"0":"Ingen fluktuation","1":"Let til moderat fluktuation","2":"Udtalt fluktuation"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.027},{"key":"21.028","title":"Bevidsthedssvækkelse","input":"integer","options":{"0":"Ikke til stede","1":"Let til moderat (somnolens)","2":"Udtalt og vedvarende (stupor)"},"validate":["0-2"],"glossary":21.028},{"key":"21.029","title":"Bevidsthedsuklarhed eller -plumring","description":"Nedsat opfattelse af omgivelserne (apperception) og svigtende opmærksomhed (vigilitet og tenacitet)\n","input":"integer","options":{"0":"Ikke til stede","1":"Let til moderat bevidsthedsuklarhed","2":"Udtalt og vedvarende bevidsthedsuklarhed"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.029},{"key":"21.030","title":"Akinetisk mutisme","description":"Neurologisk tilstand med fravær af vilkårlige bevægelser og tale hos vågen patient, der eventuelt kan følge med øjnene\n","input":"integer","options":{"0":"Ikke til stede","1":"Til stede i let til moderat grad","2":"Vedvarende til stede i udtalt grad"},"validate":["0-2"],"glossary":21.03},{"key":"21.031","title":"Koma","input":"integer","options":{"0":"Ikke til stede","1":"Til stede af moderat dybde","2":"Dyb koma Hvis koma er betinget af alkohol eller andre psykoaktive substanser rates også ved 11.038, 11.031 eller 12.043."},"validate":["0-2"],"glossary":21.031},{"key":"21.032A","title":"Hukommelse","description":"21_032 Umiddelbar genkaldelse og korttidshukommelse - 6 items\nNævn for patienten følgende 6 ord: ''hovedbanegården, rød, Vestre Ringgade 16, Asien, telefon, dollar). Bed patienten gentage ordene indtil han kan sige dem rigtigt, eller op til 3 gange.\nRate antal ord, der mangler efter op til 3 forsøg.\n","input":"string","validate":["0-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.032},{"key":"21.032B","title":"Umiddelbar genkaldelse og korttidshukommelse - 6 items (fortsat)","description":"Rate antal ord, der mangler efter 3 minutter.\n","input":"string","validate":["0-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.032},{"key":"21.032C","title":"Umiddelbar genkaldelse og korttidshukommelse - 6 items (fortsat) Rate antal ord, der mangler efter 5 minutter.","input":"string","validate":["0-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.032},{"key":"21.033","title":"Korttidshukommelse efter hjælpeord (fortsat)","description":"Hvis patienten ikke kan huske alle ordene efter 5 minutter, kan der gives hjælp med ord, der minder om de ord, der skulle huskes, fx: ''Århus centrum, en farve, en adresse, et kontinent, et apparat man kan tale i, valutatype). Giv hjælpeord for ét ord af gangen og rate antal ord, der fortsat ikke huskes efter ét forsøg.\nRate 9 hvis alle ordene blev husket korrekt i 20.032.\n","input":"string","validate":["0-6"],"used_in_dsmiv":1,"glossary":21.033},{"key":"21.034","title":"Langtidshukommelse - personlige data","description":"Vælg 5 oplysninger fra patientens fortid, som de normalt ville kunne huske, fx patientens fødselsdato, ægtefælles eller samlevers fødselsdato, hvor mange år de har samlevet eller været gift, moders fødenavn, skolens navn, navn og fødselsdato for børn.\nRating: Antal ikke korrekt erindrede data (max. 5).\n","input":"string","validate":["0-5"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.034},{"key":"21.035","title":"Langtidshukommelse - baggrundsoplysninger","description":"Vælg 5 oplysninger fra patients lokale eller nationale baggrund, som de normalt skulle kunne huske, fx statsministerens navn, hovedstadens navn, navn på førende politiske parti, navnet på lokale teater eller forsamlingshus, lokale borgmesters eller sognefogeds navn.\nRating: Antal ikke korrekt huskede navne (max. 5).\n","input":"string","validate":["0-5"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.035},{"key":"21.036","title":"Cifferspændvidde","description":"Bed patienten gentagne cifrene: 6,2,9,3,7,1,4. Noter svaret.\nRating: Antal ikke korrekt huskede cifre (uden hensyn til ordenen).\n","input":"string","validate":["0-7"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.036},{"key":"21.037","title":"Subjektivt hukommelsesbesvær (indsigt)","description":"Har du haft vanskeligheder med hukommelsen den senere tid?\n? Er det blevet sværere at huske end det har været? ? Glemmer du hvor du har lagt tingene? ? Er det blevet værre den senere tid?\n?? Hvor længe har det været galt?\n","input":"integer","options":{"0":"Ingen indsigt i hukommelsessvækkelse","1":"Ringe indsigt","2":"Delvis indsigt","3":"Fuld indsigt eller ingen hukommelsessvækkelse"},"validate":["0-3"],"glossary":21.037},{"key":"21.038","title":"Forholdsregler for at kompensere for hukommelsesbesvær","description":"Skriver du det op, du skal gøre for at huske det? - Navne, telefonnumre?\n? Er det noget, du er begyndt på den senere tid?\n","input":"integer","options":{"0":"Ingen forholdsregler","1":"Nogen brug af nedskrivning for at støtte hukommelsen","2":"Udtalt brug af nedskrivning og anden hukommelsesstøtte","3":"Helt afhængig af ydre forholdsregler for at støtte hukommelsen"},"validate":["0-3"],"glossary":21.038},{"key":"21.039","title":"Hukommelsesbesvær for navne på nærtstående personer","description":"Glemmer du sommetider navnene på nogen du kender godt, fx nære venner eller familiemedlemmer?\n? Hvornår begyndte det at blive sådan?\n","input":"integer","options":{"0":"Intet besvær","1":"Let besvær","2":"Moderat besvær","3":"Udtalt besvær"},"validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.039},{"key":"21.040","title":"Desorientering i vante omgivelser","description":"Går du ikke så meget ud, som du tidligere har gjort? - For at handle ind eller gå på besøg?\nHar du haft svært ved at finde rundt? - Måske svært ved at finde tilbage igen til hvor du selv bor?\n","input":"integer","options":{"0":"Ingen desorientering","1":"Desorientering ved få lejligheder, men klaret sig uden hjælp","2":"Ofte desorienteret eller har haft brug for hjælp til at finde hjem","3":"Svær desorientering, ude af stand til at klare sig uden for hjemmet uden ledsagelse på grund af kognitiv svækkelse"},"validate":["0-3"],"glossary":21.04},{"key":"21.041","title":"Alvorligt hukommelsesbesvær","description":"Er det sket, at du har glemt at slukke for det elektriske komfur, eller at låse døren?\n? Hvornår skete det sidste gang? ? Er det sket flere gange?\n","input":"integer","options":{"0":"Ingen tilfælde med alvorligt hukommelsesbesvær","1":"Enkelt eller få tilfælde uden alvorlige konsekvenser","2":"Hyppige alvorlige tilfælde eller alvorlige konsekvenser","3":"Svær hukommelsessvækkelse, kræver konstant opsyn"},"validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.041},{"key":"21.042","title":"Sprogfunktion","description":"21_042 Læsning\nVis patienten et stykke papir, hvor der står skrevet ''man skal ikke kaste med sten, når man bor i glashus''.\n","input":"integer","options":{"0":"Læser sætningen korrekt","1":"Kan læse nogle ord korrekt, men ikke hele sætningen","2":"Kan ikke læse et eneste ord","8":"Kan ikke læse på grund af dårligt syn"},"validate":["0-2",8],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.042},{"key":"21.043","title":"Ordforråd","description":"Nævn så mange navne på forskellige dyr, som du kan komme i tanke om.\nRating: Antal navne på forskellige dyr, som nævnes inden for 60 sekunder.\n","input":"string","validate":["0-99"],"glossary":21.043},{"key":"21.044L","title":"Ordfinding Spørg efter navnene på 3 foreviste genstande, fx et bælte, briller, sko. Rate antal forkerte navne (max. 3)","input":"string","validate":["0-3"],"glossary":21.044},{"key":"21.044M","title":"Ordfinding (fortsat)","description":"Spørg efter navnene på dele af de viste genstande, fx bæltespænde, brilleglas, snørebånd. Rate antal forkerte navne (max. 3) Anfør samlede antal forkerte navne i højre box (max. 6)\n","input":"string","validate":["0-3"],"glossary":21.044},{"key":"21.045","title":"Gentagelse af sætning","description":"Bed patienten gentage følgende 2 sætninger:\n''Det var en skøn dag i maj måned'' ''Katolik, protestant, baptist''\nRates med 1 point for hver ikke korrekt gentaget sætning (max. 2).\n","input":"string","validate":["0-2"],"glossary":21.045},{"key":"21.046","title":"Skrivefærdighed","description":"Bed patienten skrive en sætning på et stykke papir og derefter læse den højt.\n","input":"integer","options":{"0":"Fuld sætning med grundled og verbum","1":"Læseligt forsøg men ikke (2)","2":"Intet forsøg eller ulæselige streger på papiret"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.046},{"key":"21.047","title":"Regnefærdigheder","description":"21_047 Addition\nVis patienten en 5 krone og en 2 krone og spørg hvor meget det bliver tilsammen.\n","input":"integer","options":{"0":"Korrekt","1":"Forkert"},"validate":[0,1],"glossary":21.047},{"key":"21.048","title":"Subtraktion","description":"Hvis nogen giver dig 15 kr tilbage på 20 kr, hvor meget har du så betalt?\n","input":"integer","options":{"0":"Korrekt","1":"Forkert"},"validate":[0,1],"glossary":21.048},{"key":"21.049","title":"Motorisk funktionsevne (praksi)","description":"21_049 Urskivetegning\nBed patienten tegne et ur med visere, der angiver at klokken er 20 minutter i to.\nRegistrer udførelsen af følgende 5 punkter:\n","input":"integer","options":{"1":"tegning af cirkel","2":"anbringelse af tal i højre side","3":"anbringelse af tal i venstre side","4":"anbringelse af lille viser på tallet 2","5":"anbringelse af store viser på tallet 8 Rating: Antal forkerte udførte punkter (max. 5)."},"validate":["0-5"],"glossary":21.049},{"key":"21.050","title":"Efterligning af håndstilling","description":"Bed patienten efterligne din håndstilling i forskellige positioner.\nPosition\n","input":"integer","options":{"1":"Adskilte hænder med håndfladen vendende mod patienten, og fingrene pegende opad. Position","2":"Samme håndstilling men tættere sammen så tommelfingerspidserne rører hinanden. Position","3":"Vend hænderne så tommelfingerspidserne og lillefingerspidserne rører hinanden. Position","4":"Drej hænderne mod hinanden så alle fingerspidserne rører hinanden. Position","5":"Drej hænderne modsat så tommelfingerspidserne rører lillefingerspidserne på modsatte hånd. Rating: Antal forkerte efterligninger."},"validate":["0-5"],"glossary":21.05},{"key":"21.051","title":"Nikken med hovedet","description":"Opforder patienten til at nikke.\n","input":"integer","options":{"0":"Korrekt","1":"Forkert"},"validate":[0,1],"glossary":21.051},{"key":"21.052","title":"Vinke med hænderne","description":"Opforder patienten til vise hvordan man vinker farvel.\n","input":"integer","options":{"0":"Korrekt","1":"Forkert"},"validate":[0,1],"glossary":21.052},{"key":"21.053","title":"Højre-venstre konfusion","description":"Bed patienten røre højre øre med venstre hånd.\n","input":"integer","options":{"0":"Korrekt","1":"Kun en af siderne korrekt","2":"Begge sider forkerte"},"validate":["0-2"],"glossary":21.053},{"key":"21.054","title":"Abstraktionsevne","description":"21_054Æble og banan\nBed patienten angive lighed mellem æble og banan.\n","input":"integer","options":{"0":"Relevant svar, fx ''frugter''","1":"Konkret svar","2":"Forkert svar"},"validate":["0-2"],"glossary":21.054},{"key":"21.055","title":"Båd og bil","description":"Bed patienten angive lighed mellem båd og bil.\n","input":"integer","options":{"0":"Relevant svar, fx ''transportmidler''","1":"Konkret svar","2":"Forkert svar"},"validate":["0-2"],"glossary":21.055},{"key":"21.056","title":"Ordsprog","description":"21_056 ''Man skal ikke kaste med sten, når man selv bor i glashus''\nKan du forklare hvad dette ordsprog betyder?\n","input":"integer","options":{"0":"Relevant svar, fx ''ikke beskylde andre for fejl, man selv har''","1":"Konkret svar, fx ''glasset vil gå i stykker''","2":"Forkert abstrakt fortolkning"},"validate":["0-2"],"glossary":21.056},{"key":"21.057","title":"''Tomme tønder buldrer mest''","description":"Kan du forklare hvad dette ordsprog betyder?\n","input":"integer","options":{"0":"Relevant svar, fx ''den, der kun har lidt at byde på, praler mest''","1":"Konkret svar, fx ''tomme tønder larmer mest, hvis man slår på dem''","2":"Forkert abstrakt fortolkning"},"validate":["0-2"],"glossary":21.057},{"key":"21.058","title":"''Mange bække små gør en stor å''","description":"Kan du forklare hvad dette ordsprog betyder?\n","input":"integer","options":{"0":"Relevant svar, fx ''hvis mange yder et lille bidrag, kan man opnå noget stort''","1":"Konkret svar","2":"Forkert abstrakt fortolkning"},"validate":["0-2"],"glossary":21.058},{"key":"21.059","title":"Bizar fortolkning af ordsprogsprøver","input":"integer","options":{"0":"Ingen bizar fortolkning","1":"Forståelige svar med nogen grad af bizar fortolkning","2":"Udtalt bizar fortolkning"},"validate":["0-2"],"glossary":21.059},{"key":"21.060","title":"Almen viden","description":"21_060 Gennemsnitshøjde\nHvad er gennemsnitshøjden for mænd?\n","input":"integer","options":{"0":"150 - 180 cm","1":"120-210 cm","2":"Mindre end 120 eller over 210 cm"},"validate":["0-2"],"glossary":21.06},{"key":"21.061","title":"Himlens farve","description":"Hvad er himlens farve på en skyfri dag?\n","input":"integer","options":{"0":"Blå","1":"Noget afvigende farve, fx blå-grå","2":"Totalt afvigende farve, fx orange eller violet"},"validate":["0-2"],"glossary":21.061},{"key":"21.062","title":"Hovedstaden i Italien","description":"Hvad hedder hovedstaden i Italien?\n","input":"integer","options":{"0":"Rom","1":"Anden større italiensk by, fx Milano","2":"Ikke italiensk by, fx Washington"},"validate":["0-2"],"glossary":21.062},{"key":"21.063","title":"Lungernes funktion","description":"Hvad bruget man lungerne til?\n","input":"integer","options":{"0":"Åndedrættet","1":"Delvis misvisende svar, fx rense luften","2":"Helt misvisende svar, fx fordøje føde"},"validate":["0-2"],"glossary":21.063},{"key":"21.064","title":"Et termometers funktion","description":"Hvad bruger man et termometer til?\n","input":"integer","options":{"0":"Til at måle temperatur","1":"Delvis misvisende, fx måle fugtigheden","2":"Helt misvisende, fx til at følge solens gang"},"validate":["0-2"],"glossary":21.064},{"key":"21.065","title":"Frontale - subkortikale tests","description":"21_065 Sekvensfærdighed\nPatienten opfordres til at eftergøre en følge af 3 håndstillinger.\n","input":"integer","options":{"1":"Knyttet hånd","2":"Åben hånd med håndfladen opad","3":"Saksestilling med anden og tredie finger som de to ben i en saks Registrer følgende 5 sekvenser én ad gangen: Saks, åben, knyttet. Åben, saks, knyttet. Knyttet, saks, knyttet. Saks, knyttet, saks. Knyttet, åben, knyttet. Rating: Antal forkerte sekvenser (max. 5)."},"validate":["0-5"],"glossary":21.065},{"key":"21.066","title":"Fingertapning","description":"Patienten skal tappe med pegefingerspidsen mod tommelfingeren efter følgende instruktion:\nHver gang jeg tapper én gang (vises) skal du tappe to gange (vises) - og hver gang jeg tapper to gange (vises), skal du slet ikke tappe (spred tommel- og pegefinger bort fra hinanden).\nGennemfør 10 sekvenser én ad gangen i følgende orden:\n1 1 2 2 1 2 1 1 1 2\nRating: Antal forkerte sekvenser (max. 10).\n","input":"string","validate":["0-10"],"glossary":21.066},{"key":"21.067","title":"Adfærdstræk forbundet med kognitiv forstyrrelse","description":"21_067 Personlighedsændring\nÆndring eller svækkelse af emotionel kontrol, motivation eller social adfærd, - i form af emotionel labilitet, flad eufori, irritabilitet, hæmningsløshed, mistroiskhed, rigiditet, apati, svigtende egenhygiene, ændret seksuel adfærd.\n","input":"integer","options":{"0":"Ingen personlighedsændring","1":"Let ændring, men tydelig for nærtstående","2":"Moderat ændring, tydelig for alle","3":"Svær ændring, tidligere personlighed vanskelig at genkende"},"validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.067},{"key":"21.068","title":"Personlighedsændring inden for 6 måneder efter non-traumatisk proces","description":"Reversibelt syndrom med forbigående personlighedsændringer af op til et par års varighed, ofte forbundet med neurologiske udfaldssymptomer, fx efter encefalit, kulilteforgiftning.\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1],"glossary":21.068},{"key":"21.069","title":"Personlighedsændring inden for 4 uger efter kranietraume","description":"Hjernerystelse med mindst 3 symptomer (hovedpine, svimmelhed, træthed, støjoverfølsomhed, emotionel ændring, koncentrationsbesvær, søvnløshed, nedsat alkoholtolerans).\n","input":"integer","options":{"0":"Nej","1":"Ja"},"validate":[0,1],"glossary":21.069},{"key":"21.070","title":"Selvforsømmelse","description":"Nedsat egen-omsorg med hensyn til bad og toilette, påklædning og lignende.\n","input":"integer","options":{"0":"Ingen selvforsømmelse","1":"Let","2":"Moderat","3":"Svær"},"validate":["0-3"],"glossary":21.07},{"key":"21.071","title":"Neurologisk screening","description":"Neurologisk undersøgelse med hensyn til kranienerver, motoriske og sensoriske forhold, koordination, reflekser, ligevægt og gang.\n","input":"integer","options":{"0":"Normale forhold","1":"Parkinsons syndrom","2":"Fokale udfaldssymptomer, tydende på infarkt","3":"Andre væsentlige fund","4":"Blandede fund","9":"Ikke undersøgt"},"validate":["0-4",9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.071},{"key":"21.072","title":"Screening for hørelse","description":"Undersøg hørelsen på begge sider ved at gnide fingrene mod hinanden ud for hvert øre.\n","input":"integer","options":{"0":"Normal hørelse","1":"Let hørenedsættelse","2":"Betydelig hørenedsættelse"},"validate":["0-2"],"glossary":21.072},{"key":"21.073","title":"Screening for synssans","description":"Vis patienten et stykke papir med et E-bogstav, drej papiret og bed patienten angive til hvilken side åbningen vender (højre, venstre, op eller ned).\nPapiret drejes i følgende sekvenser med åbningen vendende: ned, ned, til højre, op, til højre, til venstre. E E E E E E\n","input":"integer","options":{"0":"Normale forhold","1":"Let synsnedsættelse","2":"Betydelig synsnedsættelse"},"validate":["0-2"],"glossary":21.073},{"key":"21.074","title":"Screening for motorik","input":"integer","options":{"0":"Normale forhold","1":"Let motorisk forstyrrelse","2":"Moderat motorisk forstyrrelse","3":"Svær motorisk forstyrrelse","9":"Ikke undersøgt Følgende forhold undersøges: Håndtryk Negativisme, ambitendens, forced grasping. Passiv bøjning og strækning af underarmen Tonus, flexibilitas cerea, spasticitet, rigiditet. Bede patienten stå stille Akatisi. Bede patienten strække armene frem med håndfladerne ned ad Tremor, choreatiske bevægelser, atetose, holdning, axial hyperkinesi. Bede patienten berøre undersøgerens finger og egen næse med strakt arm Intentions-tremor, ataksi. Bede patienten imitere ansigtsbevægelser og udtryk (smilen, panderynken, blinken med øjet, udspilning af begge kinder) Nedsat mimik, bradykinesi, vanskelighed ved at skifte udtryk. Bede patienten åbne munden og stikke tungen frem Orale dyskinesier, savlen, tandstatus. Bede patienten gå frem og tilbage i lokalet Gangen, medsving af armene."},"validate":["0-3",9],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.074},{"key":"21.075","title":"Andre symptomer forbundet med kognitiv forstyrrelse","description":"21_075 Illusioner eller synshallucinationer ved bevidsthedsplumring\n","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.075},{"key":"21.076","title":"Fantastiske idéer eller vrangforestillinger ved bevidsthedsplumring","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.076},{"key":"21.077","title":"Psykomotoriske forstyrrelser ved bevidsthedsplumring","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.077},{"key":"21.078","title":"Forstyrret søvn-/vågen-cyklus","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.078},{"key":"21.079","title":"Søvnforstyrrelser ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"glossary":21.079},{"key":"21.080","title":"Vrangforestillinger ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.08},{"key":"21.081","title":"Hallucinationer ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"glossary":21.081},{"key":"21.082","title":"Depressionstilstand ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.082},{"key":"21.083","title":"Manisk tilstand ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"glossary":21.083},{"key":"21.084","title":"Aggressivitet ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.084},{"key":"21.085","title":"Omkringflakken ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.085},{"key":"21.086","title":"Andre adfærdsproblemer ved demens","input":"integer","options":{"0":"Fraværende","1":"Let grad","2":"Moderat grad","3":"Svær grad"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":21.086},{"key":"21.087","title":"C: Objektive iagttagelser, anamnestiske oplysninger og ætiologiske forhold Tilstedeværende kognitive forstyrrelser","description":"21_087 Bevidsthedsforstyrrelser\n","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.087},{"key":"21.088","title":"Hukommelsesbesvær (globalt)","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.088},{"key":"21.089","title":"Intellektuel funktionsnedsættelse (global)","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.089},{"key":"21.090","title":"Desorientering med hensyn til tid, sted og egne data","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"used_in_dsmiv":1,"glossary":21.09},{"key":"21.091","title":"Perseverationstendens","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":21.091},{"key":"21.092","title":"Retrograd amnesi","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":21.092},{"key":"21.093","title":"Konfabulationstendens","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":21.093},{"key":"21.094","title":"Svækket indlæring og korttidshukommelse","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.094},{"key":"21.095","title":"Bevægelses-/tale-besvær eller langsomhed","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":21.095},{"key":"21.096","title":"Tænke-/forståelses-besvær eller langsomhed","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":21.096},{"key":"21.097","title":"Afasi","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.097},{"key":"21.098","title":"Apraksi","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.098},{"key":"21.099","title":"Agnosi","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"used_in_dsmiv":1,"glossary":21.099},{"key":"21.100","title":"Akalkuli","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.1},{"key":"21.101","title":"Svækkelse af styringsfunktioner (frontale-subkortikale)","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"used_in_dsmiv":1,"glossary":21.101},{"key":"21.102","title":"Aktivt frontalt syndrom","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad Uhæmmet, stimulus- eller impulsdrevet, hyperoral, repetitiv eller udnyttende adfærd."},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.102},{"key":"21.103","title":"Passivt frontalt syndrom","description":"Apatisk, passiv, tilbagetrukket.\n","input":"integer","options":{"0":"Ingen","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.103},{"key":"21.104","title":"Global vurdering af kognitive deficits","input":"integer","options":{"0":"Fraværende","1":"Rent kortikale","2":"Blandet kortikale og subkortikale","3":"Rent subkortikale"},"validate":["0-3"],"episodes":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.104},{"key":"21.105","title":"Anamnestiske oplysninger","description":"21_105 Læseevne\n","input":"integer","options":{"0":"Har tidligere været i stand til at læse","1":"Har aldrig kunnet læse på grund af mangelfuld skolegang, men ikke (2)","2":"Har aldrig kunnet læse på grund af hjerneskade, mental retardering eller udviklingsforstyrrelse"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.105},{"key":"21.106","title":"Skrivefærdigheder","input":"integer","options":{"0":"Tidligere normal skrivefærdighed","1":"Har aldrig kunnet skrive på grund af mangelfuld skolegang","2":"Har aldrig kunnet skrive på grund af hjerneskade, mental retardering eller udviklingsforstyrrelse"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.106},{"key":"21.107","title":"Regnefærdighed, addition","input":"integer","options":{"0":"Tidligere normal færdighed","1":"Har aldrig kunnet lægge sammen på grund af mangelfuld skolegang","2":"Har aldrig kunnet lægge sammen på grund af hjerneskade, mental retardering eller udviklingsforstyrrelse"},"validate":["0-2"],"glossary":21.107},{"key":"21.108","title":"Regnefærdighed, subtraktion","input":"integer","options":{"0":"Tidligere normal færdighed","1":"Har aldrig kunnet trække fra på grund af mangelfuld skolegang","2":"Har aldrig kunnet trække fra på grund af hjerneskade, mental retardering eller udviklingsforstyrrelse"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.108},{"key":"21.109","title":"Tilstedeværende kognitiv svækkelse","input":"integer","options":{"0":"Fraværende","1":"Tilstede SKIP => til 22.001 hvis der ikke er tegn på kognitiv svækkelse eller hvis kognitiv svækkelse har begyndelse i barndommen og har bestået uændret livet igennem."},"validate":[0,1],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.109},{"key":"21.110","title":"Varighed af kognitiv svækkelse (måneder)","description":"888 Ukendt\n999 NA\n","input":"string","validate":["0-120",888,999],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.11},{"key":"21.111","title":"Begyndelsesalder for kognitiv svækkelse","description":"965 Præcis alder ukendt, men i aldersperioden 65 år og opefter\n964 Præcis alder ukendt, men i aldersperioden før 65 år 888 Ukendt 999 NA\n","input":"string","validate":["0-120",965,964,888,999],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.111},{"key":"21.112","title":"Forløb af kognitiv svækkelse","input":"integer","options":{"1":"Gradvis tiltagende","2":"Hurtig udvikling til svær grad, evt. med delirøs tilstand. Udvikling inden for en måned","3":"Trinvis udvikling med perioder med forværrelse","4":"Bedring med tiden, men med blivende svækkelse, fx efter kranietraume"},"validate":["1-4",9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.112},{"key":"21.113","title":"Fordeling af kognitive deficits","input":"integer","options":{"1":"Jævnt fordelte","2":"Ujævnt fordelte"},"validate":[1,2,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.113},{"key":"21.114","title":"Påvirkning af dagliglivets aktiviteter","input":"integer","options":{"1":"Let påvirkning","2":"Moderat påvirkning 3 Svær påvirkning"},"validate":["1-3",9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.114},{"key":"21.115","title":"Delirium og demens","input":"integer","options":{"0":"Intet delirium","1":"Delirium ved demens","2":"Delirium ikke i forbindelse med demens"},"validate":["0-2",9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.115},{"key":"21.116","title":"Demens med andre symptomer","input":"integer","options":{"0":"Uden andre symptomer","1":"Overvejende med vrangforestillinger","2":"Overvejende med hallucinationer","3":"Overvejende med depressive symptomer","4":"Med blandede symptomer"},"validate":["0-4",9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.116},{"key":"21.117","title":"Ætiologiske forhold","description":"21_117 Risikofaktorer for vaskulær cerebral lidelse\n","input":"integer","options":{"1":"Enkelt faktor tilstede, fx hypertension","2":"To risikofaktorer tilstede","3":"Multiple risikofaktorer tilstede"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.117},{"key":"21.118","title":"Forløbet ved vaskulær demens","description":"Vurderes ud fra optræden af fokale udfaldssymptomer, såsom spastisk hemiparese, hyperrefleksi, ekstensiv plantarrefleks, pseudobulbær parese.\n","input":"integer","options":{"1":"Hurtig udvikling med svær svækkelse inden for én måned","2":"Hurtig udvikling med svær svækkelse inden for 3 måneder","3":"Gradvis kumulativ svækkelse efter serie af infarkter inden for 3 -5 måneder","4":"Intermitterende udvikling med periodisk forværring","5":"Bedring men med blivende svækkelse"},"validate":["0-5",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.118},{"key":"21.119","title":"Evidens for hjerneskade, fra specialundersøgelser","description":"EEG, CT, MR, neuropsykologisk undersøgelse.\n","input":"integer","options":{"1":"Cerebral atrofi","2":"Iskæmiske forandringer i hvide substans","3":"Hjerneinfarkter","4":"Blanding af (2) og (3)","5":"Blanding af (1), (2) og (3)"},"validate":["0-5",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.119},{"key":"21.120","title":"Årsagsforhold ved demens","description":"21_120 Alzheimers demens\n","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.12},{"key":"21.121","title":"Vaskulær demens","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.121},{"key":"21.122","title":"Picks sygdom (eller fronto-temporal degeneration)","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.122},{"key":"21.123","title":"Jakob Creutzfeldts sygdom","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.123},{"key":"21.124","title":"Huntingtons sygdom","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.124},{"key":"21.125","title":"Parkinsons sygdom","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.125},{"key":"21.126","title":"HIV-infektion","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.126},{"key":"21.127","title":"Alkoholbetinget demens","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.127},{"key":"21.128","title":"Demens som følge af andre psykoaktive substanser","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.128},{"key":"21.129","title":"Kranietraume","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.129},{"key":"21.130","title":"Interkraniel rumopfyldende proces","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.13},{"key":"21.131","title":"Diffus Lewy Body sygdom","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.131},{"key":"21.132","title":"Anden specificeret årsag til demens","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.132},{"key":"21.133","title":"Demens uden specificeret årsag","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.133},{"key":"21.134","title":"Årsager til delirium eller amnestisk syndrom","description":"21_134 Amnestisk syndrom fremkaldt af alkohol\n","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.134},{"key":"21.135","title":"Amnestisk syndrom fremkaldt af andre psykoaktive stoffer","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.135},{"key":"21.136","title":"Amnestisk syndrom ikke fremkaldt af alkohol eller andre psykoaktive stoffer","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.136},{"key":"21.137","title":"Delirium eller intoksikation uden kramper som følge af alkohol","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.137},{"key":"21.138","title":"Delirium eller intoksikation med kramper som følge af alkohol","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.138},{"key":"21.139","title":"Delirium eller intoksikation uden kramper som følge af andre psykoaktive substanser","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.139},{"key":"21.140","title":"Delirium eller intoksikation med kramper som følge af andre psykoaktive substanser","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.14},{"key":"21.141","title":"Delirium, uspecificeret","input":"integer","options":{"0":"Ingen evidens","1":"Nogen, men ikke sikker evidens","2":"Sandsynlig evidens"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.141},{"key":"21.142","title":"Forårsagende psykoaktive substans","description":"1 = OPD, 2 = CAN, 3 = SED, 4 = COC, 5 = STM, 6 = HAL, 7 = PCP, 8 = VOL, 9 = OTH.\n","input":"string","validate":["1-9"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":21.142},{"key":"21.143","title":"ICD-10 diagnosekode for organisk årsag til kognitiv forstyrrelse eller svækkelse","input":"string","fitb_size":"ICD10","used_in_dsmiv":1,"glossary":21.143},{"key":"21.144","title":"Påvirkning af aktiviteter på grund af sektion 21 symptomer","input":"integer","options":{"0":"Ingen sektion 21 symptomer","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":21.144},{"key":"21.145","title":"Checkliste for organisk kognitiv forstyrrelse af lettere grad","description":"21_145 Subjektivt hukommelsesbesvær for nylige data\n","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.145},{"key":"21.146","title":"Koncentrations- og opmærksomhedssvækkelse","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.146},{"key":"21.147","title":"Subjektivt indlæringsbesvær","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.147},{"key":"21.148","title":"Svækkelse af tænkeevne (problemløsning eller abstraktion)","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.148},{"key":"21.149","title":"Svækkelse af sproglig funktion (forståelse og ordfinding)","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.149},{"key":"21.150","title":"Svækkelse af visuospatiale funktioner","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.15},{"key":"21.151","title":"Evidens for infektion, cerebral eller systemisk lidelse, forud for eller samtidig med symptomerne","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.151},{"key":"21.152","title":"Normal neurologisk undersøgelse, normale laboratorietal","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger"},"validate":[0,1],"glossary":21.152},{"key":"21.153","title":"Svækkelse eller forstyrrelse ved neuropsykologisk bedømmelse (kognitive tests)","input":"integer","options":{"0":"Fraværende","1":"Tilstede gennem mindst to uger Andre organiske psykiske lidelser som følge af hjerneorganiske lidelser, systemiske lidelser med påvirkning af hjernefunktion samt lidelser som følge af brug af alkohol eller andre psykoaktive substanser, rates i de hertil hørende sektioner af SCAN. Organisk årsag angives i slutningen af hver sektion og samlet i sektion 13 og 20."},"validate":[0,1],"glossary":21.153},{"key":"22.001","title":"Motorik og adfærd","description":"22_001 Sammenfalden holdning Brug Rating Skala III\n","input":"string","section_start":"Section_22","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.001,"ratingscale":3},{"key":"22.002","title":"Stillestående fremtoning Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.002,"ratingscale":3},{"key":"22.003","title":"Motorisk hæmning Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.003,"ratingscale":3},{"key":"22.004","title":"Øget latenstid ved bevægelser Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.004,"ratingscale":3},{"key":"22.005","title":"Underaktivitet Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.005,"ratingscale":3},{"key":"22.006","title":"Stupor Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.006,"ratingscale":3},{"key":"22.007","title":"Langsom, tøvende tale Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.007,"ratingscale":3},{"key":"22.008","title":"Slæbende gang Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":22.008,"ratingscale":3},{"key":"22.009","title":"Manglende medsving af armene Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":22.009,"ratingscale":3},{"key":"22.010","title":"Varighed af nedsat motorisk aktivitet","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.01},{"key":"22.011","title":"Påvirkning af aktiviteter på grund af nedsat psykomotorisk aktivitet","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":1,"used_in_dsmiv":1,"glossary":22.011},{"key":"22.012","title":"Organisk årsag til nedsat psykomotorisk aktivitet","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.012},{"key":"22.013","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.013},{"key":"22.014","title":"Øget psykomotorisk aktivitet","description":"22_014 Letafledelighed, usamlethed Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.014,"ratingscale":3},{"key":"22.015","title":"Rastløshed Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.015,"ratingscale":3},{"key":"22.016","title":"Agitation Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.016,"ratingscale":3},{"key":"22.017","title":"Hyperaktivitet eller psykomotorisk uro Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.017,"ratingscale":3},{"key":"22.018","title":"Voldelig eller destruktiv adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.018,"ratingscale":3},{"key":"22.019","title":"Hurtig skiften mellem stupor og psykomotorisk uro","input":"integer","options":{"0":"Ikke tilstede","1":"Mindre end 2 uger","2":"2 uger eller mere"},"validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.019},{"key":"22.020","title":"Varigheden af øget psykomotorisk aktivitet","input":"integer","options":{"0":"Ikke tilstede","1":"1 - 3 dage","2":"4-6 dage","3":"7 dage eller mere","4":"14 dage eller mere","5":"Gennem hele måneden"},"validate":["0-5"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.02},{"key":"22.021","title":"Påvirkning af aktiviteter på grund af øget psykomotorisk aktivitet","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.021},{"key":"22.022","title":"Organisk årsag til øget psykomotorisk aktivitet","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.022},{"key":"22.023","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.023},{"key":"22.024","title":"Kataton adfærd","description":"22_024 Negativisme Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.024,"ratingscale":3},{"key":"22.025","title":"Ambitendens Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.025,"ratingscale":3},{"key":"22.026","title":"Forced grasping (dvælende eller gentaget håndtryk) Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.026,"ratingscale":3},{"key":"22.027","title":"Ekkopraksi Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.027,"ratingscale":3},{"key":"22.028","title":"Flexibilitas cerea Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.028,"ratingscale":3},{"key":"22.029","title":"Opposition Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.029,"ratingscale":3},{"key":"22.030","title":"Rykvise bevægelser Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.03,"ratingscale":3},{"key":"22.031","title":"Stivnen Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.031,"ratingscale":3},{"key":"22.032","title":"Automatisk lydighed Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.032,"ratingscale":3},{"key":"22.033","title":"Manierthed, sære manerer Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.033,"ratingscale":3},{"key":"22.034","title":"Mærkelige legemsstillinger Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.034,"ratingscale":3},{"key":"22.035","title":"Simple stereotypier Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.035,"ratingscale":3},{"key":"22.036","title":"Katatone symptomer fremtrædende i sygdomsbilledet","input":"integer","options":{"0":"Ingen","1":"Tilstede, men ikke dominerende","2":"Dominerer sygdomsbilledet"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.036},{"key":"22.037","title":"Varighed af katatone symptomer","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.037},{"key":"22.038","title":"Påvirkning af aktiviteter på grund af katatone symptomer","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":22.038},{"key":"22.039","title":"Organisk årsag til katatone symptomer","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.039},{"key":"22.040","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.04},{"key":"22.041","title":"Aparte udseende eller adfærd","description":"22_041 Aparte eller utilpasset udseende Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.041,"ratingscale":3},{"key":"22.042","title":"Upassende adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.042,"ratingscale":3},{"key":"22.043","title":"Bizar adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.043,"ratingscale":3},{"key":"22.044","title":"Respektløs adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.044,"ratingscale":3},{"key":"22.045","title":"Histrionisk adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.045,"ratingscale":3},{"key":"22.046","title":"Selvforsømmelse Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.046,"ratingscale":3},{"key":"22.047","title":"Forsømmelse af helbred Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.047,"ratingscale":3},{"key":"22.048","title":"Personlig hygiejne og vaner Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.048,"ratingscale":3},{"key":"22.049","title":"Selvmutilerende adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.049,"ratingscale":3},{"key":"22.050","title":"Kaotisk adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.05,"ratingscale":3},{"key":"22.051","title":"Uopmærksomhed over for farer og risikomomenter Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.051,"ratingscale":3},{"key":"22.052","title":"Formålsløs indsamling af nyttesløse eller aparte genstande Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.052,"ratingscale":3},{"key":"22.053","title":"Utilpasset leen eller fnisen Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.053,"ratingscale":3},{"key":"22.054","title":"Hallucinatorisk adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":22.054,"ratingscale":3},{"key":"22.055","title":"Monoman adfærd Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.055,"ratingscale":3},{"key":"22.056","title":"Varighed af aparte adfærd eller udseende","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":22.056},{"key":"22.057","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":22.057},{"key":"22.058","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.058},{"key":"22.059","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":22.059},{"key":"22.060","title":"Nedsat non-verbal kommunikation","description":"22_060 Negativ holdning\nBrug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.06,"ratingscale":3},{"key":"22.061","title":"Nedsat gestikulation","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.061,"ratingscale":3},{"key":"22.062","title":"Nedsat mimik","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.062,"ratingscale":3},{"key":"22.063","title":"Nedsat stemmemodulation","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.063,"ratingscale":3},{"key":"22.064","title":"Nedsat øjenkontakt","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.064,"ratingscale":3},{"key":"22.065","title":"Varighed af nedsat non-verbal kommunikation","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":22.065},{"key":"22.066","title":"Påvirkning af aktiviteter på grund af nedsat non-verbal kommunikation","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":22.066},{"key":"22.067","title":"Organisk årsag til nedsat non-verbal kommunikation","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":22.067},{"key":"22.068","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":22.068},{"key":"23.001","title":"FØLELSER - AFFEKT Depressivt affektpræg","description":"23_001 Observeret depression\nBrug Rating Skala III\n","input":"string","section_start":"Section_23","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.001,"ratingscale":3},{"key":"23.002","title":"Varighed","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.002},{"key":"23.003","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"glossary":23.003},{"key":"23.004","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.004},{"key":"23.005","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":23.005},{"key":"23.006","title":"Manisk affektpræg","description":"22_006 Løftet stemningsleje\nBrug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.006,"ratingscale":3},{"key":"23.007","title":"Irritabelt stemningsleje Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.007},{"key":"23.008","title":"Varighed af manisk affektpræg","input":"integer","options":{"0":"Ikke tilstede","1":"1 - 3 dage","2":"4-6 dage","3":"7 dage eller mere","4":"Gennem hele måneden"},"validate":["0-4",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.008},{"key":"23.009","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.009},{"key":"23.010","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.01},{"key":"23.011","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":23.011},{"key":"23.012","title":"Skizofreniformt affektpræg","description":"23_012 Affektaffladning Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.012},{"key":"23.013","title":"Affektinkongruens Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.013},{"key":"23.014","title":"Varighed af skizofreniformt affektpræg","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":23.014},{"key":"23.015","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"glossary":23.015},{"key":"23.016","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.016},{"key":"23.017","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":23.017},{"key":"23.018","title":"Andet affektpræg","description":"23_018 Observeret angst\nBrug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":23.018,"ratingscale":3},{"key":"23.019","title":"Mistroiskhed","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.019,"ratingscale":3},{"key":"23.020","title":"Forvirring, rådvildhed","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":23.02,"ratingscale":3},{"key":"23.021","title":"Affektlabilitet","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.021,"ratingscale":3},{"key":"23.022","title":"Affektudladninger","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":23.022,"ratingscale":3},{"key":"23.023","title":"Varighed af andet affektpræg","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"glossary":23.023},{"key":"23.024","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"glossary":23.024},{"key":"23.025","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"glossary":23.025},{"key":"23.026","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":23.026},{"key":"24.001","title":"Sprogforstyrrelser - tale Depressive sprogforstyrrelser","description":"24_001 Monoton tale\nBrug Rating Skala III\n","input":"string","section_start":"Section_24","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.001,"ratingscale":3},{"key":"24.002","title":"Langsom tale, øget latenstid","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.002,"ratingscale":3},{"key":"24.003","title":"Varighed af depressive sprogforstyrrelser","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.003},{"key":"24.004","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"glossary":24.004},{"key":"24.005","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.005},{"key":"24.006","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":24.006},{"key":"24.007","title":"Maniske taleforstyrrelser","description":"24_007 Taletrang\nBrug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.007,"ratingscale":3},{"key":"24.008","title":"Tankeflugt, springende tankegang","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.008,"ratingscale":3},{"key":"24.009","title":"Omstændelighed, digredierende tankegang","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.009,"ratingscale":3},{"key":"24.010","title":"Varighed af maniske taleforstyrrelser","input":"integer","options":{"0":"Ikke tilstede","1":"1 - 3 dage","2":"4-6 dage","3":"7 dage eller mere","4":"Gennem hele måneden"},"validate":["0-4",8,9],"glossary":24.009},{"key":"24.011","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.011},{"key":"24.012","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.012},{"key":"24.013","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","glossary":24.013},{"key":"24.014","title":"Skizofreniforme sprogforstyrrelser","description":"24_014 Abnormt kraftig stemme\nBrug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.014,"ratingscale":3},{"key":"24.015","title":"Abnormt svag stemme","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.015,"ratingscale":3},{"key":"24.016","title":"Mumlen hen for sig","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.016,"ratingscale":3},{"key":"24.017","title":"Afsporende tale med uklar sammenhæng","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.017,"ratingscale":3},{"key":"24.018","title":"Perseverationstendens","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.018,"ratingscale":3},{"key":"24.019","title":"Ruminationstendens","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.019,"ratingscale":3},{"key":"24.020","title":"Forbisvaren","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.02,"ratingscale":3},{"key":"24.021","title":"Neologismer eller idiosynkratisk ordvalg Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.021,"ratingscale":3},{"key":"24.022","title":"Inkoherent tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.022,"ratingscale":3},{"key":"24.023","title":"Tale præget af magisk eller ulogisk tænkning","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":24.023,"ratingscale":3},{"key":"24.024","title":"Spærring","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.024,"ratingscale":3},{"key":"24.025","title":"Indholdsfattig tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.025,"ratingscale":3},{"key":"24.026","title":"Ordfattig tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.026,"ratingscale":3},{"key":"24.027","title":"Stumhed, mutisme Brug Rating Skala III","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.027},{"key":"24.028","title":"Nedsat non-verbal kommunikation i forbindelse med tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.028,"ratingscale":3},{"key":"24.029","title":"Manglende evne til at holde en samtale i gang","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.029,"ratingscale":3},{"key":"24.030","title":"Manglende reaktion eller genklang ved samtalen","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.03,"ratingscale":3},{"key":"24.031","title":"Bevidst vildledende svar","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_dsmiv":1,"glossary":24.031,"ratingscale":3},{"key":"24.032","title":"Talen hen for sig selv","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.032,"ratingscale":3},{"key":"24.033","title":"Pedantisk, opstyltet tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.033,"ratingscale":3},{"key":"24.034","title":"Forsinket ekkolali, repetitiv tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.034,"ratingscale":3},{"key":"24.035","title":"Forstyrret syntaks ved forsøg på kommunikation","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.035,"ratingscale":3},{"key":"24.036","title":"Forstyrret orddannelse ved forsøg på kommunikation","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.036,"ratingscale":3},{"key":"24.037","title":"Manglende evne til at fornemme andres tanker eller følelser (no ''theory of mind'')","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.037,"ratingscale":3},{"key":"24.038","title":"Undvigende tale","description":"Brug Rating Skala III\n","input":"string","validate":["0-2",8,9],"episodes":0,"attributional":1,"glossary":24.038,"ratingscale":3},{"key":"24.039","title":"Talens indhold præget af manglende initiativ og formål","input":"integer","options":{"0":"Normal","1":"Let nedsat initiativ, retning eller formål","2":"Moderat til udtalt mangel på initiativ og formål","3":"Total apati"},"validate":["0-3",8,9],"episodes":0,"attributional":1,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.039},{"key":"24.040","title":"Varighed af skizofreniforme sprogforstyrrelser","input":"integer","options":{"0":"Ikke tilstede","1":"1-13 dage","2":"14 dage eller mere","3":"Gennem hele måneden"},"validate":["0-3",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.04},{"key":"24.041","title":"Påvirkning af aktiviteter","input":"integer","options":{"0":"Ingen påvirkning","1":"Kun let påvirkning","2":"Moderat påvirkning","3":"Svær påvirkning"},"validate":["0-3",8,9],"used_in_dsmiv":1,"glossary":24.041},{"key":"24.042","title":"Organisk årsag","input":"integer","options":{"0":"Fraværende","1":"Sandsynlig organisk årsag, ikke fuldt bekræftet","2":"Definitiv organisk årsag bekræftet af specialundersøgelser","8":"Usikkert om organisk betinget"},"validate":["0-2",8,9],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":24.042},{"key":"24.043","title":"ICD-10 diagnosekode for organisk årsag","input":"string","fitb_size":"ICD10","used_in_dsmiv":1,"glossary":24.043},{"key":"24.044","title":"Indsigt i symptomer i Part 2","input":"integer","options":{"0":"Ingen psykotiske symptomer","1":"God indsigt","2":"Usikker og tøvende indsigt, ''ved ikke''","3":"Ingen indsigt"},"validate":["0-3",8,9],"glossary":24.044},{"key":"24.045","title":"Pålidelighed af ratings i sektion 22-24","input":"integer","options":{"0":"Ingen problemer, ratings pålidelige","1":"Nogle problemer, men de fleste ratings skønnes pålidelige","2":"Store problemer med at gennemføre interviewet, i tvivl om pålideligheden af ratings","3":"Umuligt at rate symptomerne pålideligt"},"validate":["0-3",8,9],"glossary":24.045},{"key":"25.001","title":"Checkliste for autistisk spektrum","description":"Anvendes ved mistanke om autistisk spektrumtilstand. Vurderes for tilstedeværelse gennem den sidste måned ud fra egne iagttagelser, journalnotater eller oplysninger fra personale eller pårørende.\n25_001 Rigid, overformel, opstyltet fremtræden\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"section_start":"Section_25","validate":["0-2"],"glossary":25.001},{"key":"25.002","title":"Excentrisk fremtræden","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.002},{"key":"25.003","title":"Aparte tilknytning til ét eller få specielle objekter","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.003},{"key":"25.004","title":"Kontaktønske, men magter ikke at skabe kontakt","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.004},{"key":"25.005","title":"Kan kun klare sig i selskab med andre på egne betingelser","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.005},{"key":"25.006","title":"Ingen virkelige venner","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.006},{"key":"25.007","title":"Manglende empati","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.007},{"key":"25.008","title":"Manglende forståelse for samarbejde eller konkurrence","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.008},{"key":"25.009","title":"Manglende common sense","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.009},{"key":"25.010","title":"Kejtede, ukoordinerede, aparte eller sære bevægelser","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.01},{"key":"25.011","title":"Tilbøjelighed til at lade sig udnytte eller underkue","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.011},{"key":"25.012","title":"Tilbøjelighed til at leve i sin egen verden","description":"Intens optagethed af få idiosynkratiske interesser, manglende evne til at klare dagligdagens forhold.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.012},{"key":"25.013","title":"Forbavsende specialfærdigheder","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.013},{"key":"25.014","title":"Stor hukommelse for specielle emner, men uden forståelse eller evne til at gøre brug heraf","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.014},{"key":"25.015","title":"Aparte kommunikation","description":"Formelt, opstyltet , pedantisk eller ''robot-agtigt'' sprog.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.015},{"key":"25.016","title":"Manglende situationsfornemmelse","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.016},{"key":"25.017","title":"Tilbøjelighed til upassende og naive bemærkninger","input":"integer","options":{"0":"Ikke tilstede","1":"Tilstede i nogen grad","2":"Tilstede i udtalt grad"},"validate":["0-2"],"glossary":25.017},{"key":"25.018","title":"Manglende non-verbal udtryksevne","description":"Monoton stemme, udtryksløs mimik, manglende eller utilpasset gestikulation, dårlig øjenkontakt.\n","input":"string","validate":["0-2"],"glossary":25.018},{"key":"26.001","title":"Itemgruppe checklist - ICG ICG kan anvendes til at indsamle oplysninger fra journalnotater eller fra pårørende, hvis patienten ikke kan eller ikke ønsker at gennemføre et PSE-interview. I hver gruppe samles items tilhørende samme psykopatologiske emne. Grupperne svarer ikke til diagnostiske kategorier, men er beregnet til registrering af psykopatologisk profil efter et særligt computerprogram. Selvom de indsamlede oplysninger er summariske, forudsættes der et nøje kendskab til de PSE-items, der indgår i grupperne.","description":"Checklistens 40 itemgrupper dækker lidelser og tilstande svarende til F2, F3 og F40 - F42.\n26_001 Varighed af nuværende episode (måneder)\n","input":"string","section_start":"Section_26","validate":["0-299"],"glossary":"Section_26"},{"key":"26.002","title":"Varighed af PS (dage)","input":"string","validate":["0-99"],"glossary":"Section_26"},{"key":"26.003","title":"Tidsperiode for RE (måneder og år)","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.004","title":"LB - episode med neurotiske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.005","title":"LB episode med depressive symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.006","title":"LB episode med maniske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.007","title":"LB episode med psykotiske symptomer","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.008","title":"Begyndelsesalder for neurotiske symptomer","description":"Indtast startdato\n","input":"string","validate":["0-99"],"glossary":"Section_26"},{"key":"26.009","title":"Begyndelsesalder for depressive symptomer","description":"Indtast startdato\n","input":"string","validate":["0-99"],"glossary":"Section_26"},{"key":"26.010","title":"Begyndelsesalder for maniske symptomer","description":"Indtast startdato\n","input":"string","validate":["0-99"],"glossary":"Section_26"},{"key":"26.011","title":"Enter approximate age of first onset of any episodes of symptom types dated in 26.004 - 26.007 . 26_011 Psychotic","input":"string","validate":["0-99"],"glossary":"Section_26"},{"key":"26.012","title":"Special Episode List","description":"See items 1.020 - 1.025 . Any mixture of PSE and IGC schedules may be used\nbut each will be processed as an individual case. If any PSE is used, enter initial details on the first Face Sheet, page 24. 26 _ 012 Episode 1\nEnter Start Date\n","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.013","title":"Episode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.014","title":"Episode 3","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.015","title":"Episode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.016","title":"Episode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.017","title":"Episode","input":"date_interval","fitb_size":"DATE_NODAY","glossary":"Section_26"},{"key":"26.018","title":"I ICG-grupperne er items anført med PSE-nummer og navn samt klinisk betydende ratings. For hvert item hvor der kan opnås tilstrækkelige oplysninger, indcirkles den tilsvarende rating. Item-grupperne rates herefter ved hjælp af ratingscale IV. Itemgrupper, med konstituerende items","description":"26_018 Nervøs anspændthed (IG1)\n2.009 Tilfredshed med given behandling 1\n3.001 Bekymringstendens 2 - 3 3.002 Følelse af nervøs spænding 2 - 3 3.008 Støjoverfølsomhed 2 - 3 3.009 Irritabilitet 2 - 3 4.024Ængstelig forudanelse med autonome symptomer 2 - 3 6.015 Nedsat selvtillid over for andre 2 - 3 6.016 Social tilbagetrækning 2 - 3 8.010 Virkning af sovetabletter 2 8.011 Indsovningsbesvær 2 8.017 Forstyrrende drømme eller mareridt 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.019","title":"Muskelspænding (IG2)","description":"3.003 Generelle muskelspændinger 2 - 3\n3.005 Lokaliserede spændingssmerter 2 - 3 3.006 Subjekt beskrevet rastløshed 2 - 3 3.007Øget trætbarhed og udmattelse 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.020","title":"Autonom angst og panikanfald (IG3) Mindst 3 positive items fra 4.003 - 4.019 samt 1-2 andre items.","description":"4.003 Vejrtrækningsbesvær 2 - 3\n4.004 Hjertebanken, hurtig puls 2 - 3 4.005 Svimmelhed, besvimelsesfornemmelse 2 - 3 4.006 Prikken eller dødhedsfornemmelse 2 - 3\n4.007 Trykken i brystet 2 - 3\n4.008 Mundtørhed 2 - 3 4.009 Synkebesvær 2 - 3 4.010 Sveden 2 - 3\n4.011 Rysten 2 - 3\n4.012 Varme- eller kulde-følelse 2 - 3 4.013 Uvirkelighedsfølelse 2 - 3 4.014 Køren i maven 2 - 3 4.015 Dødsangst 2 - 3 4.016 Kvælningsfornemmelser 2 - 3 4.017 Angst for at miste kontrollen 2 - 3 4.019 Andre angstsymptomer 2 - 3 4.020 Panikanfald se item 4.023 Fritflydende autonom angst 2 - 3 4.026 Depersonalisation-derealisation i forbindelse med angst 2 - 3 4.055 Panikanfald uden fobier 3 23.018 Observeret angst 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.021","title":"Agorafobi (IG4)","description":"Mindst ét positivt item fra 4.027 - 4.032 samt 4.046.\n4.027 Angst for offentlige steder 1 - 2\n4.028 Angst i situationer hvor man ikke let kan komme ud 1 - 2\n4.029 Angst for at rejse alene 1 - 2 4.030 Angst for at færdes borte fra hjemmet 1 - 2 4.031 Angst for at bryde sammen, når alene 1 - 2 4.032 Angst for at være alene 1 - 2 4.046 Sværhedsgrad af agorafobi 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.022","title":"Socialfobi (IG5)","description":"Mindst ét positivt item fra 4.033 - 4.036 samt 4.049.\n4.033 Angst for at spise og drikke i andres påsyn 1 - 2 4.034 Angst for at være i mindre gruppe 1 - 2 4.035 Angst for at tale offentligt 1 - 2 4.036 Angst for at rødme, ryste eller lignende 1 - 2 4.049 Sværhedsgrad af socialfobi 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.023","title":"Specifik fobi (IG6)","description":"Mindst ét positivt item fra 4.037 - 4.044 samt 4.052.\n4.037 Angst for vejrfænomener 1 - 2\n4.038 Angst for vand 1 - 2 4.039 Angst for dyr 1 - 2 4.040 Angst for lukkede rum 1 - 2 4.041 Angst for at flyve 1 - 2 4.042 Angst for højder 1 - 2\n4.043 Angst for situationer i forbindelse med sygdom eller 1 - 2 behandling\n4.044 Angst for at pådrage sig sygdom 1 - 2\n4.052 Sværhedsgrad af specifik fobi 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.024","title":"Obsessioner og kompulsioner (IG7)","description":"5.002 Kontrollerings- og gentagelses-tvang 2 - 3\n5.003 Tvang i forbindelse med orden og symmetri 2 - 3 5.004 Tvangstanker om at gøre skade eller fortræd 2 - 3 5.005 Tvang i forbindelse med renlighed 2 - 3 5.006 Tvangspræget grublen 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.025","title":"Depersonalisation-derealisation (IG8)","description":"3.012 Depersonalisations-/derealisations-oplevelser 2 - 3\n16.006 Derealisation med hensyn til ting 1 - 3 16.007 Derealisation med hensyn til personer 1 - 3 16.008 Depersonalisation (egen person) 1 - 3 16.009 Fremmedfølelse over for eget udseende 1 - 3 16.010 Fremmedfølelse over for dele af kroppen 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.026","title":"Subjektiv funktionsnedsættelse (IG9)","description":"7.002 Nedsat koncentrationsevne 2 - 3\n7.003 Ineffektiv tankevirksomhed 2 - 3 7.004 Tab af interesser 2 - 3 7.005 Subjektiv hæmning 2 - 3 7.006 Tab af energi 2 - 3 7.007 Uoverkommelighedsfølelse 2 - 3 21.037 Subjektivt hukommelsesbesvær 2 - 3 21.038 Forholdsregler for at imødegå 21.037 2 - 3 21.039 Glemmer navne på nærtstående 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.027","title":"Nedsættelse af fysiologiske funktioner (IG10)","description":"8.005 Appetitændring 2 - 3\n8.006 Vægttab 2 - 3 8.009 Søvnbesvær ved depression 1 8.014 Tidlig opvågnen 2 - 3 8.025 Nedsat seksuallyst ved depression 1\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.028","title":"Specielle træk ved depression (IG11)","description":"6.007 Fornemmelse af tab af følelser 2 - 3\n6.008 Tab af påvirkelighed (reaktivitet) 2 - 3 6.009 Morgenforværring 1 6.010 Optagethed af tanker om død og ulykke 2 - 3 6.013 Patologisk skyldfølelse 2 - 3 6.014 Skyldbetonede selvhenføringsidéer 2 - 3 6.017 Nedsat selvfølelse 2 - 3 16.003 Svækket perception 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.029","title":"Depression (IG12)","description":"6.001 Nedtrykthed 2 - 3\n6.002 Maskeret depression 2 6.003 Grådtendens 2 - 3 6.004 Manglende evne til at føle glæde 2 - 3 6.006 Håbløshedsfølelse 2 - 3 6.011 Selvmordstanker eller handlinger 1 - 3 6.012 Livslede 2 - 3 23.001 Observeret depression 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.030","title":"Depressive vrangforestillinger og hallucinationer (IG13)","description":"17.011 Depressivt stemningspræg ved hørelseshallucinationer 1 - 2\n17.019 Flygtige hallucinationer i forbindelse med tab af 1 - 3 nærtstående\n19.010 Depressive selvhenførende forestillinger 1 - 2 19.025 Depressive forestillinger om skyld eller ringhed 1 - 3 19.026 Depressive forestillinger om død eller ulykke 1 - 3\n19.027 Depressive hypokondre forestillinger 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.031","title":"Legemlige vrangforestillinger (IG14)","description":"16.011 Dysmorfofobi 1 - 3\n19.028 Hypokondre vrangforestillinger, ikke depressive 1 - 3 19.031 Dysmorfofobe vrangforestillinger 1 - 3 19.032 Depersonalisations-/derealisations-vrangforestillinger 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.032","title":"Subjektivt øget funktionsniveau (IG15)","description":"10.010Øget selvfølelse 2 - 3\n10.012 Handlinger baseret på ekspansivt stemningsleje 1 - 3 10.013 Nedsat søvnbehov 2 - 3\n10.015Øget seksualdrift 2 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.033","title":"Subjektivt øget tempo (IG16)","description":"10.004 Tankepres 2 - 3\n10.005 Taletrang 2 - 3 10.006 Usamlethed 2 - 3 10.007 Subjektiv overaktivitet 2 - 3 24.008 Tankeflugt 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.034","title":"Mani (IG17)","description":"10.001 Ekspansivt stemningsleje 1 - 3\n10.002 Irritabelt stemningsleje 1 - 3 23.006 Løftet stemningsleje 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.035","title":"Maniske vrangforestillinger og hallucinationer (IG18)","description":"17.011 Kongruens for manisk stemningsleje 2 - 3\n19.011 Maniske selvhenførende forestillinger 1 - 2 19.029 Vrangforestillinger om grandiøse evner 1 - 3 19.030 Vrangforestillinger om grandiøs identitet 1 - 3\n","input":"string","validate":["0-2",8,9],"episodes":1,"glossary":"Section_26","ratingscale":4},{"key":"26.036","title":"Hyperaktivitet (IG19)","description":"22.014 Usamlethed 1 - 2\n22.015 Rastløshed 1 - 2 22.016 Agitation 1 - 2 22.017 Psykomotorisk uro 1 - 2 22.018 Voldelig eller destruktiv adfærd 1 - 2 24.007 Taletrang 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.037","title":"Perceptionsforstyrrelser (IG20)","description":"16.002 Forvrænget perception 1 - 3\n16.004 Skærpet perception 1 - 3 16.005Ændret tidsoplevelse 1 - 3 16.014 Andre perceptionsforstyrrelser 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.038","title":"Uspecifikke hørelseshallucinationer (IG21)","description":"17.001 Hørelseshallucinationer 1\n17.003 Non-verbale hørelseshallucinationer 1 - 3 17.004 Hørelseshallucinationer hyppige 2 - 4 17.005 Længden af hørelseshallucinationer 1 - 3 17.012 Specielle træk ved hørelseshallucinationer 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.039","title":"Uspecifikke synshallucinationer (IG22)","description":"17.015 Uformede synshallucinationer 1 - 3\n17.016 Formede synshallucinationer 1 - 3 17.017 Sceniske synshallucinationer 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.040","title":"Uspecifikke psykotiske symptomer (IG23)","description":"3.013 Jalousitendens 2 - 3\n16.001 Perceptionsforstyrrelser 1 17.018 Hypnagoge hallucinationer 1 - 3 17.022 Lugthallucinationer 1 - 3 17.030 Andre hallucinationer 1 - 3 19.002 Andre vrangforestillinger 1 24.023 Magisk tænkning 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.041","title":"Non-affektive hørelseshallucinationer (IG24)","description":"17.008 Kommenterende og diskuterende stemmer 2 - 3\n17.009 Trediepersons hørelseshallucinationer 2 - 5 17.010 Stemningskongruens 4 - 5\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.042","title":"Tankeforstyrrelsesoplevelser (IG25)","description":"18.004 Tankehørlighed 1 - 3\n18.005 Tanke-ekko 1 - 3 18.006 Tankepåføring 1 - 3 18.007 Tankeudspredning 1 - 3 18.008 Kommenterende tanker 1 - 3 18.009 Tankestop 2 - 3 18.010 Tankefradrag 1 - 3 18.011 Andre tankeforstyrrelsesoplevelser 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.043","title":"Styringsoplevelser (IG26)","description":"18.012 Styringsoplevelser 1 - 3\n18.013 Fremmed styring af stemmen 1 - 3 18.014 Fremmed styring af håndskrift 1 - 3 18.015 Påførte handlinger 1 - 3 18.016 Påførte følelser 1 - 3 18.017 Andre styringsoplevelser 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.044","title":"Bizarre vrangforestillinger og mistydninger (IG27)","description":"17.023 Bizarre forestillinger forbundet med lugthallucinationer 1 - 3\n17.027 Forestillinger forbundet med seksuelle hallucinationer 1 - 3 17.029 Bizarre forestillinger forbundet med somatiske 1 - 3 hallucinationer\n18.001 Vrangstemning og forvirringsfølelse 1 - 3 19.009 Primær vrangsansning 1 - 3 19.019 Fantastiske vrangforestillinger 1 - 3 19.037 Stemningskongruens 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.045","title":"Blandede vrangforestillinger (IG28)","description":"17.025 Vrangforestillinger om at afgive særlig lugt 1 - 3\n18.003 Vrangforestillinger om at tankerne kan læses af andre 1 - 3 19.014 Jalousiforestillinger 1 - 3 19.016 Graviditetsforestillinger 1 - 3 19.017 Erotomane forestillinger 1 - 3 19.018 Forestillinger om beskyldning for homoseksualitet 1 - 3 19.020 Optagethed af tidligere vrangforestillinger 1 - 3 19.021 Religiøse vrangforestillinger 1\n19.022 Paranormale forklaringsforestillinger 1\n19.023 Fysiske forklaringsforestillinger 1\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.046","title":"Selvhenførende vrangforestillinger (IG29)","description":"19.003 Forestilling om overvågning 1 - 3\n19.004 Selvhenføringsforestillinger 1 - 3 19.005 Mistydning af omgivelserne 1 - 3 19.006 Forestilling om citering af idéer eller tanker 1 - 3 19.007 Misidentifikationsforestillinger 1 - 3 19.008 Forestilling om efterligning af nærtstående 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.047","title":"Forfølgelsesforestillinger (IG30)","description":"19.012 Forfølgelsesforestillinger 1 - 3\n19.013 Sammensværgelsesforestillinger 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.048","title":"Kaotiske og skiftende emotionelle symptomer (IG31)","description":"23.007 Irritabelt stemningsleje 1 - 2\n23.020 Forvirring, rådvildhed 1 - 2\n23.021 Affektlabilitet 1 - 2 23.022 Affektudladladninger 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.049","title":"Inkoherent tale (IG32)","description":"15.002 Inkoherent tale 1 - 2\n24.017 Afsporende tale med uklar sammenhæng 1 - 2 24.024 Neologismer, idiosynkratisk ordvalg 1 - 2 24.022 Inkoherent tale 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.050","title":"Andre sproglige forstyrrelser (IG33)","description":"24.009 Omstændelighed 1 - 2\n24.014 Abnormt kraftig stemme 1 - 2 24.015 Abnormt svag stemme 1 - 2 24.016 Mumlen hen for sig 1 - 2 24.018 Perseverationstendens 1 - 2 24.019 Ruminationstendens 1 - 2 24.020 Forbisvaren 1 - 2 24.024 Spærring 1 - 2 24.032 Talen hen for sig selv 1 - 2 24.033 Pedantisk, opstyltet tale 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.051","title":"Social upassende adfærd (IG34)","description":"22.041 Aparte udseende 1 - 2\n22.042 Upassende adfærd 1 - 2 22.043 Bizar adfærd 1 - 2 22.044 Respektløs adfærd 1 - 2 22.045 Histrionisk adfærd 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.052","title":"Affladet og inadækvat affekt (IG35)","description":"23.012 Affektaffladning 1 - 2 23.013 Affektinkongruens 1 - 2 24.039 Passiv, formålsløs tale 1 - 3\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.053","title":"Sprogfattigdom (IG36)","description":"15.005 Sprogfattigdom 1 - 2\n24.025 Indholdsfattig tale 1 - 2 24.026 Ordfattig tale 1 - 2 24.027 Mutisme 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.054","title":"Non-verbal kommunikation (IG37)","description":"22.060 Negativ holdning 1 - 2 22.061 Nedsat gestikulation 1 - 2 22.062 Nedsat mimik 1 - 2 22.063 Nedsat stemmemodulation 1 - 2 22.064 Nedsat øjenkontakt 1 - 2 24.028 Nedsat non-verbal kommunikation i forbindelse med tale 1 - 2 24.029 Manglende evne til at holde en samtale i gang 1 - 2 24.030 Manglende genklang ved samtale 1 - 2 24.037 Manglende evne til at fornemme andre 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.055","title":"Selvforsømmelse (IG38)","description":"22.047 Forsømmelse af helbred 1 - 2\n22.048 Forsømt personlig hygiejne og vaner 1 - 2 22.051 Uopmærksomhed over for farer 1 - 2 22.046 Selvforsømmelse (påklædning) 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.056","title":"Psykomotorisk hæmning (IG39)","description":"22.001 Sammenfalden holdning 1 - 2\n22.002 Stillestående fremtoning 1 - 2 22.003 Motorisk hæmning 1 - 2 22.004Øget latenstid ved bevægelse 1 - 2\n22.005 Underaktivitet 1 - 2\n22.006 Stupor 1 - 2 22.007 Langsom tale 1 - 2 22.008 Slæbende gang 1 - 2 22.009 Manglende medsving af armene 1 - 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.057","title":"Kataton adfærd (IG40)","description":"22.024 Negativisme 1 - 2\n22.025 Ambitendens 1 - 2 22.026 Forced grasping 1 - 2 22.027 Ekkopraksi 1 - 2\n22.028 Flexibilitas cerea 1 - 2\n22.029 Opposition 1 - 2 22.031 Stivnen 1 - 2 22.032 Automatisk lydighed 1 - 2 22.036 Fremtrædende katatone symptomer 2\n","input":"string","validate":["0-2",8,9],"glossary":"Section_26","ratingscale":4},{"key":"26.058","title":"Monotomatiske vrangforestillinger (IG41) (19.034)","input":"integer","options":{"1":"Sandsynligvis tilstede","2":"Definitivt tilstede"},"validate":["0-2",8,9],"glossary":"Section_26"},{"key":"26.059","title":"Pålidelighed af ICG-ratings","input":"integer","options":{"0":"Gode informationer til alle betydende ratings","1":"Forholdsvis god information til de fleste ratings","2":"Utilstrækkelig information, ratings mindre pålidelige","3":"Utilstrækkelig information til pålidelig rating"},"validate":["0-3",8,9],"glossary":26.059},{"key":"27.001","title":"Skema til anamnestiske baggrundsoplysninger (CHS)","description":"Registrering af sociale og udviklingsmæssige baggrundsoplysninger, sociale forhold, funktionsevne og funktionsnedsættelse, tilstedeværende forstyrrelse af personlighedsstruktur og adfærd og aktuelle kliniske diagnoser. 1. BARNDOM OG SKOLEGANG IND TIL 16 ÅRS ALDEREN\n27_001 Alder ved skolegangs ophør\n","input":"string","section_start":"Section_27","validate":["0-99"],"glossary":"Section_27"},{"key":"27.002","title":"Præstationsniveau i skolen","input":"integer","options":{"1":"Højere end gennemsnitligt ved skoletidens ophør","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt men acceptabelt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.003","title":"Specialskole-undervisning eller -ophold","input":"integer","options":{"1":"Ingen","2":"På grund af adfærdsvanskeligheder","3":"På grund af kriminalitet","4":"På grund af indlæringsproblemer","5":"Andet, specificeres...","6":"Påkrævet, men muligheden ikke tilstede"},"validate":["1-6"],"glossary":"Section_27"},{"key":"27.004","title":"Psykiske problemer før 16 års alderen","input":"integer","options":{"1":"Ingen betydende problemer","2":"Nogle problemer af tvivlsom betydning","3":"Betydende problemer SKIP => til 27.024 hvis 27.004 rates 1 eller 2."},"validate":["1-3"],"glossary":"Section_27"},{"key":"27.005","title":"Adfærdsmæssige emotionelle forstyrrelser","description":"27_005 F90 Forstyrrelse af aktiviteter og opmærksomhed\n","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.006","title":"F91 Adfærdsforstyrrelse","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.007","title":"F92 Blandet adfærds- og følelsesmæssig forstyrrelse","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.008","title":"F93 Emotionel forstyrrelse","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.009","title":"F94 Elektiv mutisme, tilknytningsforstyrrelse","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.010","title":"F95 Tics, inklusive Tourettes syndrom","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.011","title":"F98 Enuresis, encopresis, pica, stammen","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.012","title":"Specifikke udviklingsforstyrrelser","description":"27_012 F80 Specifikke udviklingsforstyrrelser af tale og sprog\n","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.013","title":"F81 Specifikke udviklingsforstyrrelser af skolefærdigheder","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.014","title":"F82 Specifik motorisk udviklingsforstyrrelse","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.015","title":"Gennemgribende udviklingsforstyrrelser (autistisk spektrum)","description":"27_015 Mangelfuldt social samspil\nManglende forståelse for andres tanker og følelser.\n","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.016","title":"Manglende kommunikationsfærdigheder","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.017","title":"Manglende udvikling af kreativ fantasi","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.018","title":"Begrænset, stereotyp og repetitiv adfærd, monomane interesser","input":"integer","options":{"0":"Ikke tilstede","1":"Let til moderat grad","2":"Svær grad"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.019","title":"Tidligere diagnosticeret infantil autisme eller Aspergers syndrom","input":"integer","options":{"0":"Nej","1":"Autisme (F84.0 -.1)","2":"Aspergers syndrom (F84.5)","3":"Anden undergruppe af F84"},"validate":["0-3"],"glossary":"Section_27"},{"key":"27.020","title":". INTELLEKTUELT NIVEAU","description":"27_020 Tidligere højeste IQ opnået ved psykologisk testning\n","input":"string","validate":["0-200"],"glossary":"Section_27"},{"key":"27.021","title":"Tidligere højeste IQ opnået ved skønsmæssig vurdering","input":"integer","options":{"1":"Under 20 (mental retardering af sværeste grad)","2":"10 - 34 (mental retardering af svær grad)","3":"35 - 49 (mental retardering af middelsvær grad)","4":"50 - 59 (mental retardering af lettere grad)","5":"70-84 (sinke)","6":"85 - 99","7":"100 eller derover","8":"Uvist"},"validate":["1-8"],"glossary":"Section_27"},{"key":"27.022","title":"Testvurdering af IQ på undersøgelsestidspunktet","input":"string","validate":["0-200"],"glossary":"Section_27"},{"key":"27.023","title":"Skønsmæssig vurdering af IQ på undersøgelsestidspunktet","input":"integer","options":{"1":"Under 20 (mental retardering af sværeste grad)","2":"10 - 34 (mental retardering af svær grad)","3":"35 - 49 (mental retardering af middelsvær grad)","4":"50 - 59 (mental retardering af lettere grad)","5":"70-84 (sinke)","6":"85 - 99","7":"100 eller derover","8":"Uvist"},"validate":["1-8"],"glossary":"Section_27"},{"key":"27.024","title":". SOCIAL ROLLEFUNKTION","description":"27_024 Social rollefunktion\n","input":"integer","options":{"1":"Ingen betydende problemer","2":"Nogen problemer af tvivlsom betydning","3":"Betydende problemer SKIP => til 27.049 hvis 27.024 rates 1 eller 2."},"validate":["1-3"],"glossary":"Section_27"},{"key":"27.025","title":"Funktionsniveau","description":"27_025 Akademisk funktionsniveau ved højere uddannelse\n","input":"integer","options":{"1":"Højere end gennemsnitligt ved uddannelsens ophør","2":"Gennemsnitligt","3":"Lavere ned gennemsnitligt, men acceptabelt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.026","title":"Højeste niveau af lønnet beskæftigelse","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt, men acceptabelt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.027","title":"Aktuelle lønnede beskæftigelse","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt, men acceptabelt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.028","title":"Husholdningsfærdigheder","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt, men acceptabelt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.029","title":"Globale beskæftigelsesmæssige funktionsniveau","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.030","title":"Socialt netværk i voksenalder","description":"27_030 Stabilitet af socialt netværk, da det fungerede bedst\n","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.031","title":"Stabilitet af aktuelle socialt netværk","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.032","title":"Kvalitet af socialt netværk, da det fungerede bedst","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.033","title":"Kvalitet af aktuelle socialt netværk","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.034","title":"Hypersensitivitet over for interpersonel afvisning","input":"integer","options":{"0":"Ikke tilstede","1":"Personlighedstræk tilstede, men kun let påvirkning af sociale eller arbejdsmæssige funktionsevne","2":"Moderat eller intermitterende påvirkning","3":"Svær eller invaliderende påvirkning"},"validate":["0-3"],"used_in_dsmiv":1,"glossary":"Section_27"},{"key":"27.035","title":"Seksuelle forhold","description":"27_035 Stabilitet af seksuelt forhold, da det fungerede bedst\n","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.036","title":"Stabilitet af aktuelle seksuelle forhold","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.037","title":"Kvalitet af seksuelt forhold, da det fungerede bedst","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.038","title":"Kvalitet af aktuelle seksuelle forhold","input":"integer","options":{"1":"Højere end gennemsnitligt","2":"Gennemsnitligt","3":"Lavere end gennemsnitligt","4":"Lavt"},"validate":["1-4"],"glossary":"Section_27"},{"key":"27.039","title":"F52.0 Nedsat eller manglende seksuallyst","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.040","title":"F52.1 Seksuel aversion eller manglende seksuel lystfølelse","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.041","title":"F52.2 Non-organisk genital dysfunktion","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.042","title":"F52.3 Hæmmet orgasme","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.043","title":"F52.4 Præmatur ejakulation","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.044","title":"F52.5 Non-organisk vaginisme","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.045","title":"F52.6 Non-organisk dyspareuni","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.046","title":"F52.7Øget seksuel aktivitet","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.047","title":"F52.8 Anden non-organisk seksuel dysfunktion","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.048","title":"F52.9 Non-organisk seksuel dysfunktion, uspecificeret","input":"integer","options":{"0":"Fraværende","1":"Tilstede, men sjældent optrædende","2":"Hyppigt optrædende","3":"Som (1) gennem mindst 6 måneder","4":"Som (2) gennem mindst 6 måneder"},"validate":["0-4"],"glossary":"Section_27"},{"key":"27.049","title":". GLOBAL VURDERING AF SOCIALT FUNKTIONSNIVEAU","description":"27_049 Tidligere socialt funktionsniveau\n","input":"integer","options":{"1":"Højt","2":"Over middel","3":"Middel","4":"Moderat nedsat","5":"Svært nedsat","6":"Manglende"},"validate":["1-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":"Section_27"},{"key":"27.050","title":"Sociale funktionsniveau på undersøgelsestidspunktet","input":"integer","options":{"1":"Højt","2":"Over middel","3":"Middel","4":"Moderat nedsat","5":"Svært nedsat","6":"Manglende"},"validate":["1-6"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":"Section_27"},{"key":"27.051","title":"Omfang af funktionsnedsættelse (i procent af funktionsevne)","input":"string","validate":["0-100"],"glossary":"Section_27"},{"key":"27.052","title":"Årsag til social funktionsnedsættelse","input":"integer","options":{"1":"Overvejende andre end psykisk lidelse eller tilstand (fx fysisk sygdom, følge af misbrug eller sociale forhold)","2":"Kombination af (1) og (3)","3":"Psykisk lidelse eller tilstand"},"validate":["1-3"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":"Section_27"},{"key":"27.053","title":". FORSTYRRELSE AF PERSONLIGHEDSSTRUKTUR OG ADFÆRD","description":"27_053 Personlighedsforstyrrelser\n","input":"integer","options":{"0":"Ikke tilstede","1":"IPDE anvendes i stedet for dette afsnit","2":"Tilstede men af tvivlsom betydning","3":"Tilstede i betydende grad SKIP => til 27.070 hvis 27.053 rates 0, 1 eller 2."},"validate":["0-3"],"glossary":"Section_27"},{"key":"27.054","title":"Personlighedsforstyrrelser","description":"27_054 F60.0 Paranoid personlighedsstruktur\n","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.055","title":"F60.1 Skizoid personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.056","title":"F60.2 Dyssocial personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.057","title":"F60.3 Emotionel ustabil personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.058","title":"F60.4 Histrionisk personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.059","title":"F60.5 Tvangspræget personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.060","title":"F60.6Ængstelig (evasiv) personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.061","title":"F60.7 Dependent personlighedsstruktur","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.062","title":"F60.8 Anden specifik forstyrrelse af personlighedsstrukturen","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.063","title":"F62 Non-organisk vedvarende personlighedsændring","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.064","title":"Adfærdsforstyrrelser","description":"27_064 F63 Vane- og impuls-handlinger\n","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.065","title":"Kønsidentitetsforstyrrelser","description":"27_065 F64 Kønsidentitetsforstyrrelser\n","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.066","title":"Seksuelle afvigelser","description":"27_066 F65 Seksuelle afvigelser\n","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"episodes":0,"used_in_icd10":1,"used_in_dsmiv":1,"glossary":"Section_27"},{"key":"27.067","title":"F66 Seksuel udviklings- og orienterings-forstyrrelser","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.068","title":"Andre forstyrrelser af personlighedsstruktur og adfærd","description":"27_068 F68.1 Sygdomsefterligning (factitious disorder)\n","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.069","title":"F68.0 Psykisk udbygning af somatiske symptomer","input":"integer","options":{"0":"Personligheden inden for normale grænser","1":"Forstærkede personlighedstræk, men ikke tilstrækkeligt til at opfylde kriterierne for personlighedsforstyrrelse","2":"Personlighedsforstyrrelse tilstede, kriterierne herfor opfyldte"},"validate":["0-2"],"glossary":"Section_27"},{"key":"27.070","title":". KVALITET AF DATA RATED I SCAN","description":"27_070 Kvalitet af SCAN data\n","input":"integer","options":{"0":"God kvalitet","1":"Nogle problemer, men generelt acceptable","2":"Tvivlsomme områder for data, bør tolkes med forsigtighed","3":"Usikre og tvivlsomme data"},"validate":["0-3"],"glossary":27.07},{"key":"27.071","title":". KLINISKE DIAGNOSER Diagnose (r) ved undersøgelsestidspunktet","description":"27_071 ICD-10 hoveddiagnose\n","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.072","title":"ICD-10 bidiagnose","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.073","title":"DSM-IV hoveddiagnose","input":"string","glossary":27.07},{"key":"27.074","title":"DSM-IV bidiagnose","input":"string","glossary":27.07},{"key":"27.075","title":"Lokale systems hoveddiagnose, specificeres...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.076","title":"Lokale systems bidiagnose, specificeres...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.077","title":"Diagnose(r) for tidligere episode(r)","description":"27_077 ICD-10 hoveddiagnose\n","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.078","title":"ICD-10 bidiagnose","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.079","title":"DSM-IV hoveddiagnose","input":"string","glossary":27.07},{"key":"27.080","title":"DSM-IV bidiagnose","input":"string","glossary":27.07},{"key":"27.081","title":"Lokale systems hoveddiagnose, specificeres...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.082","title":"Lokale systems bidiagnose, specificeres...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.083","title":"Diagnose 1...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.084","title":"Fysisk sygdom eller invaliditet ikke anført andetsteds","description":"1: Tilstede men uden indflydelse på den psykiske tilstand\n2: Tilstede med social funktionsnedsættelse, men uden indflydelse på de psykiske symptomer\n3: Påvirker de psykiske symptomer, men er ikke årsag hertil\n4: Bidragende årsag til den psykiske lidelse eller tilstand\n27_084 - - betydning for psykiske tilstand.....\n","input":"integer","options":{"1":"Tilstede men uden indflydelse på den psykiske tilstand","2":"Tilstede med social funktionsnedsættelse, men uden indflydelse på de psykiske symptomer","3":"Påvirker de psykiske symptomer, men er ikke årsag hertil","4":"Bidragende årsag til den psykiske lidelse eller tilstand"},"validate":["1-4"],"glossary":27.07},{"key":"27.085","title":"Diagnose 2....","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.086","title":"- - betydning for psykiske tilstand...","input":"integer","options":{"1":"Tilstede men uden indflydelse på den psykiske tilstand","2":"Tilstede med social funktionsnedsættelse, men uden indflydelse på de psykiske symptomer","3":"Påvirker de psykiske symptomer, men er ikke årsag hertil","4":"Bidragende årsag til den psykiske lidelse eller tilstand"},"validate":["1-4"],"glossary":27.07},{"key":"27.087","title":"Diagnose 3...","input":"string","fitb_size":"ICD10","glossary":27.07},{"key":"27.088","title":"- - betydning for psykiske tilstand...","input":"integer","options":{"1":"Tilstede men uden indflydelse på den psykiske tilstand","2":"Tilstede med social funktionsnedsættelse, men uden indflydelse på de psykiske symptomer","3":"Påvirker de psykiske symptomer, men er ikke årsag hertil","4":"Bidragende årsag til den psykiske lidelse eller tilstand"},"validate":["1-4"],"glossary":27.07}]}

/***/ })
/******/ ]);